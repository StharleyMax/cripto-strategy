# Fase 05 — S2-mínima: a primeira fatia de valor visível

**Epic:** `CST-3` (F1, segunda metade) · **Componentes alvo: `charts`** (geometria) e **`web`** (rota, sessão, auth) · **Gate: `Q16`**
**Depende de:** `04` **e de `T-01.2` + `T-01.3`** — as duas tasks de `Q16` migraram de gatear `02`/`03`/`04` para gatear esta fase, por `D-1` (owner, 2026-08-28 — [registro](../../context/plataforma-dados/decisoes-de-execucao-2026-08-28.md) §2), porque o relógio de retrabalho de `Q16` é *"antes do primeiro `.tsx`"* e o primeiro `.tsx` é esta fase. **Zero rede de mercado, zero API key** — o dado já está em disco.

**Honestidade sobre que valor é este:** a S2-mínima entrega valor **de verificação** — o owner olha uma série contra o preço e afirma que ela significa o que ele pensa. **Não** entrega valor operacional: não mostra o mercado agora (o painel de OI vem do dump com **~30,3 h** de idade e cobre 4 dias **com um buraco**). **As duas coisas se chamam "primeira tela" e não são a mesma** — escolher entre elas é `Q10`.

## Itens

| # | item | requisito | componente |
|---|---|---|---|
| 5.1 | S2-mínima: 1 símbolo (BTCUSDT), 4 dias, painéis **Preço** + **OI** + **CVD delta e acumulado** | `CA-F1-16` | `charts` |
| 5.2 | **Grade canônica como UMA função, dona de `charts`** — o motor a **importa**, nunca a reimplementa | `ADR-003`/FR-3 | `charts` |
| 5.3 | O **selo** de quatro campos, **visível sem hover** — tooltip não conta | `SPEC-001` §6.1 | `charts` |
| 5.4 | Içamento em três níveis (sessão / painel / número) | `CA-F4-14`, `ADR-005`/D3 | `charts` |
| 5.5 | Política de ausência **por `nature`** | `SPEC-001` §5.11 | `charts` |
| 5.6 | `knowledge_time` **na URL**; o bundle **É** a URL, não um CRUD | `SPEC-001` §7 | `web` |
| 5.7 | O painel de Preço declara **`price_source` E `price_use`** na linha do painel | `ADR-007`/PS-3 | `charts` |
| 5.8 | `pointer_mode ∈ {read, annotate}` declarado, com overlay reservado acima do plot e abaixo do crosshair | `SPEC-001` §3.6 | `charts` |
| 5.9 | Cor como **token nomeado por papel**; **`critical` fora do canal de cor** | `CA-F4-10` | `charts` |
| 5.10 | **Atribuição do Lightweight Charts** — notice do `NOTICE` + crédito à **TradingView** com link | `CA-F1-15`, `[GAP G4]` | `web` |
| 5.11 | ⚠️ **REBAIXADO em 2026-08-25 por declaração do owner** — *"vps n é problema agora, vai rodar muito local até lá"*. A `Q2` havia respondido *"VPS exposta, auth mínima"*, e este item nasceu como *"primeira superfície servida de host exposto"*. **Rodando LOCAL, auth como superfície é prematura** — o `PRD-001` §12 proíbe construir especulativamente. **O que FICA:** `principal_id` é **dimensão em toda linha que registre ato humano**, nunca constante implícita nem `NULL` (`SPEC-001` §4.4) — isso não depende de onde roda. **O que SAI desta fase:** mecanismo de login e tela de auth. A VPS é **destino, não presente** | `SPEC-001` §8.3, `Q2` | `web` |
| 5.12 | Transporte: **HTTP endereçável por conteúdo** para o histórico. **Nenhum tick chega ao browser** | `ADR-005`/D1 | `web` |

## DoD — comando e universo

| # | critério | ação | saída esperada |
|---|---|---|---|
| **D5.1** | o carimbo de idade é do FECHO na TELA | crosshair no primeiro ponto de `met/2026-08-23.csv` | **`00:05:00Z`**. **Três dos quatro desenhos de UX imprimiram o rótulo cru** — é o defeito que a fase existe para impedir |
| **D5.2** | ausência de OI não é lida como leitura da barra | crosshair em barra de 1 min **sem** ponto de OI | valor em tinta secundária + `de hh:mm:ssZ (−Xm)` + **linha-guia apontando para trás** até a marca real |
| **D5.3** | `FLOW` ausente é ausência | crosshair em bucket ausente de `cvd_delta` | **`—`** |
| **D5.4** | `COMO EM T` sobrevive à navegação | `COMO EM T` → navegar → voltar | `T` sobrevive aos **três** saltos. **Teste negativo obrigatório:** voltar para `AGORA` **não tem sintoma visível** ⇒ reprova |
| **D5.5** | a marcação fica amarrada à série de preço | marcar sob `price_source = klines_last` e reabrir sob `mark_price` | a marcação **NÃO é reexibida como se fosse a mesma** (ou vem rotulada `marcada sobre outra série de preço`) |
| **D5.6** | a paleta passa nos dois modos, com 4 papéis, **sob as TRÊS dicromacias** | `node scripts/validate_palette.js` | **exit 0 · 361 medições · `min(protan, deutan, tritan) >= 15` em todo par crítico nos dois modos.** Par que prova a regra: `#f23645 ↔ #eb6834` **FAIL em 5,3** (deutan) ⇒ **`critical` não cabe no canal de cor.** Supersedido por [`ADR-010`](../../adr/ADR-010-governanca-de-cor-por-tipo-de-marca.md) — os números anteriores (24,7/26,8 e 10,8) **não reproduzem** |
| **D5.7** | atribuição presente na página pública | inspecionar a página | notice do `NOTICE` + **TradingView** creditada com link |
| **D5.8** | **nenhum tick chega ao browser** | contar mensagens e inspecionar payload | taxa **≤ `max(1 Hz, 1/TF)`** por série, e **zero campo de nível de tick** (`agg_id`, preço por trade, quantidade por trade) |
| **D5.9** | a grade tem **UMA** implementação | comparar a saída da grade usada pelo gráfico com a usada pelo acessor | `sha256` da projeção canônica **igual** sobre **4 dias × 1 símbolo × 3 TFs** |
| **D5.10** | identidade é dimensão | inserir `<Anotacao>` | `principal_id` **preenchido**, nunca `NULL`, nunca constante implícita |
| **D5.11** | o eixo aguenta a carga que F4 vai exigir | coordenadas X contra os `event_time` originais | tolerância **0,5 px**. **⚠️ `[NÃO MEDIDO]` — declarado o MAIOR RISCO TÉCNICO desta especificação.** Aqui com carga menor; a carga cheia (**288 pontos + 1.440 candles**) é `08` |
| **D5.12** | **a fronteira `charts` ⇄ `web` é EXECUTÁVEL — o contrato reprova nas duas direções** — ⬅️ **RECEBIDO da fase `01` em 2026-08-29, onde era `D1.6`** | as duas metades de `1.8'` **na mesma passada**: **morde** — 2 violadores efêmeros, 1 em cada direção ⇒ `exit ≠ 0` **nomeando o contrato** · **cala** — os módulos **reais** de `charts` (`5.2`) e o lado `web` (`5.6`, `frontend/src/app/`) ⇒ verde | **2 imports proibidos, 1 em cada direção** — o universo que a fase `01` não tinha `[MEDIDO 2026-08-28 por `T-01.3`: `find frontend/src -type f` → **4**, e **zero** declaração de import; re-conferido em 2026-08-29 → os mesmos **4**, `grep -rnE '^\s*(import\|export)\s.*from\s' frontend/src` → **rc=1, nenhuma ocorrência**]`. **⚠️ O "cala" da fase `01` era VACUOSO** (contrato que nunca olhou nada), e é isso que esta fase conserta: aqui ele lê código real |

### ⬅️ Por que `D5.12` está aqui e não na fase `01` — e por que isto NÃO é o DoD sendo afrouxado

**Migrado em 2026-08-29 pelo `/architect`.** A **propriedade verificada é a mesma** e o **universo migrou literal** (*2 imports proibidos, 1 em cada direção*); o **comando ficou ESTRITAMENTE mais forte** — ganhou a metade **`cala`** de `1.8'`, que `D1.6` não exigia. **⚠️ Correção do `/review` 2026-08-29:** a primeira redação dizia *"não mudou uma palavra"*, e isso é **falso campo a campo** — só o universo é literal. A direção da mudança é de **aperto**, e a frase antiga **subdeclarava o próprio mérito da migração**. `D1.6` foi escrito na fase `01` porque a fronteira é decidida lá (`ADR-003`), mas **o instrumento que a torna executável só tem universo aqui** — e a fase `01` fechou com o DoD aberto, produzindo um documento em desacordo com o ledger (`f01·QA=APPROVED`).

**A recusa da fase `01` foi sustentada por `/build`, `/qa` e `/review` com dois fatos, e a ORDEM deles decide:**

1. **O que decide (vale em qualquer dia):** o único instrumento disponível para TypeScript é `no-restricted-imports`, que casa **especificador de módulo — isto é, CAMINHO**. Declará-lo gravaria `frontend/src/features/charts/**` no artefato de política, que é **a alternativa que [`ADR-003:46`](../../adr/ADR-003-fronteira-charts-web.md) recusa** (*"amarrar componente a caminho faz mover arquivo trocar de dono de julgamento"*). **Fechar `D1.6` assim seria inverter a ADR pela porta dos fundos para satisfazer um DoD.**
2. **O que adia (vale só hoje):** universo vazio.

⇒ **`D5.12` herda o `Fato 1` como pré-condição, e ele não vence com a mudança de fase.** Ter universo cheio resolve o `Fato 2` e **não** resolve o `Fato 1`.

**Por isso este DoD nasce com uma pergunta de arquitetura embutida, e ela é para `T-05.1` responder com medição:** *o contrato pode ser expresso sem que `charts` e `web` sejam definidos por caminho?* Se **sim** — via `import/no-restricted-paths` sobre grupos declarados, `project references` do TypeScript, ou um campo de manifesto por módulo — `ADR-003` fica de pé e `D5.12` fecha. **Se NÃO**, então `ADR-003:75` está certo ao dizer que a ADR *"nomeou um instrumento que não alcança a fronteira que ela mesma define"*, e o desfecho correto **não é** declarar o contrato por caminho e chamar de fechado: é **reabrir `ADR-003` e reescrever `FR-1`/`FR-2` com um instrumento que exista**. `[NÃO MEDIDO: nenhuma das três alternativas foi rodada — não há universo em que rodá-las até `5.2` existir]`

**O que NÃO é aceitável, e está escrito para que ninguém o faça depois** (herdado literal de `ADR-003:237-241`): declarar o contrato em `frontend/eslint.config.mjs` para o DoD "fechar". Ele passaria em `cala` por vacuidade, ninguém o rodaria contra violador real, e o repositório trocaria um DoD aberto e nomeado por um portão falso. **`D5.12` aberto com dono é mais barato que `D5.12` fechado com mentira.**

## Itens NOVOS — reabertura de 2026-09-03 (`A7`)

**Autorização do owner, literal:**

> *"Pode reabrir a fase5. São novas tasks, o que ta em done continua done, as novas tasks sinalizam as mudanças. Espelhe as decisoes no jira e nos artefatos de engenharia."*

`[PREMISSA-OWNER: 2026-09-03]` · registro: [`decisoes-do-owner.md` §`2026-09-03 · A4/A6/A7`](../../decisoes-do-owner.md) · parecer que originou: [`gates/consulta-fronteira-web-2026-09-03.md`](../../context/plataforma-dados/gates/consulta-fronteira-web-2026-09-03.md).

**Nada acima desta linha foi tocado.** `5.1`–`5.12` e `D5.1`–`D5.12` ficam como estavam, incluindo `5.11` (`principal_id`) — os itens novos **sinalizam** a mudança, e a numeração continua.

**O que a reabertura conserta, em uma linha:** `5.12` fechou entregando **a metade CLIENTE** de um transporte cujo **servidor não existe** — `grep -rn 'fastapi\|FastAPI\|APIRouter\|flask' backend/ -l` → **`rc=1`, zero arquivos**, e o cliente que hoje "lê" o dado o faz por **`spawnSync` de um CLI Python**, que não existe em browser `[MEDIDO 2026-09-03, fatos 1 e 4 do parecer]`. O diagrama [`arquitetura-fluxos.md:48-78`](../../arquitetura-fluxos.md) já desenhava `API --> WEB --> CH`; o container nunca foi construído.

**A decisão de fronteira que estes itens materializam** é `A4`, `[DECISÃO-OWNER: 2026-09-03, escolha entre alternativas apresentadas]`: **FastAPI é a única porta de leitura**; o `Next` renderiza e, se precisar, **proxia sessão/auth apenas — zero SQL, zero regra de domínio, zero subprocess**.

| # | item | requisito | componente |
|---|---|---|---|
| 5.13 | **A porta de leitura existe como PROCESSO, e ela é CAMADA CONSUMIDORA FORA do bounded context** — `backend/src/api/` (rotas) + o objeto de aplicação + raiz de composição, servindo a rota de histórico **endereçável por conteúdo** que `5.12` decidiu e nunca teve servidor. A camada **consome `use_cases` por injeção de dependência**; **nenhum contexto conhece seu consumidor**, e **`src/jobs/` (worker) é a MESMA classe de coisa** `[PREMISSA-OWNER: 2026-09-03]`. ⚠️ **A primeira redação deste item, de horas antes, punha a rota como `infra/` de `sentimento` e o owner RECUSOU a forma** — ver a seção de correção abaixo e [`ADR-009/D6`](../../adr/ADR-009-reuso-da-forma-do-anything.md). **O CORPO da resposta ATERROU** em `ADR-005/D6.1` (append puro do `quant-architect` em 2026-09-03): envelope `{ query, n_runs, n_gaps, runs[], gaps[] }`, **as 15 colunas de `ADR-008/D3`** por `run` e **as 8 de `md.ingest_gap`** por `gap`, nome de fio **`class`** (nunca `gap_class`). **⚠️ RESTRIÇÃO NOVA, e ela é de `ADR-008`:** o handler **CHAMA `ingest_health_query(source)`** (`backend/src/modules/sentimento/use_cases/ingest_health.py:32`) — **SQL próprio no handler o torna candidato a *"segunda definição"* de `DoD-1`**, na forma exata que `ADR-008/D3` nomeia (*"F3 reimplementa o mesmo registro"*) | `ADR-005`/D1,D5,D6.1, `A4`, `ADR-009`/D6, `ADR-008`/DoD-1 | **`infra`** — `[DECISÃO-OWNER: 2026-09-03, escolha entre alternativas apresentadas]`, opção **A** do menu abaixo. **Sem marca de forçamento:** o rótulo existe, o item declara **um** componente do vocabulário, e `F-3` fica satisfeito |
| 5.14 | **O cliente de `5.12` liga na rota real** — `frontend/src/app/history-transport.ts` deixa de provar contrato **sobre payload que ninguém serve**. O próprio módulo declara a lacuna em `:20-33` (*"NÃO cria servidor/endpoint algum … sem assumir um schema de resposta que nenhuma ADR fixou ainda"*). **✅ RESPONDIDO por `ADR-005/D6.0`/`D6.1`: rows em envelope JSON tipado, e a projeção canônica NUNCA é formato de transporte** — ela continua sendo o contrato de bytes de `ADR-008`, como **função derivada das linhas, calculada nas duas pontas**. ⇒ `SectionMarker` + `isHeaderLine` + `parseCanonicalProjection` (**:266-312, 47 linhas**) **morrem**, e **204 de 251 linhas reféns (81,3%) vivem** `[MEDIDO 2026-09-03 pelo `quant-architect`, `ADR-005` §"Alternativas recusadas em `D6`": 92-265 (174) + 471-500 (30); a região refém é 221+30=251, não "~250"]` | `ADR-005`/D6.0,D6.1,D6.3, `A4` | `web` |
| 5.15 | **O transporte por subprocesso SAI do universo de produção.** As **162 de 500 linhas** de `ingest-health-query.ts` (:313-470 + os 4 imports `node:` de :74-77) **morrem** — não é acoplamento a consertar, é código que existe só porque não havia rota. **A morte é forçada, não escolhida:** `spawnSync` não existe em browser em nenhuma variante. **E `D6.1` acrescenta as outras 47** (:266-312, o parser de texto canônico) ⇒ **209 de 500 linhas (41,8%)** saem do arquivo. **Este item NÃO decide onde passa a viver a testemunha cruzada Python×TS** — é `A1`, dono `quant-architect`, e a emenda registra que `D6` **não** o condiciona mais | `ADR-008`/D3, `ADR-005`/D6.1, `A4`, fato 5 | `web` |
| 5.16 | **O scaffold que nenhuma fase previu, com o portão de tipo que não existe** — app Next real renderizando os **3 `.tsx` / 409 linhas** que hoje **não importam `react`**, mais `tsconfig.json` e `typecheck` (`tsc --noEmit --strict`) **dentro de `make lint-frontend`**. A omissão está declarada em `frontend/src/app/routes.ts:11-13` (*"the Next.js pages themselves belong to the `web` component and are NOT created by this task"*) | `SPEC-001` §6, `index.md:72`, `ADR-009`/D3, `ADR-011`/D4 | `web` |
| 5.17 | **O falsificador de `ADR-008/DoD-2` fica SÍNCRONO — por decisão, não por sorte.** `ADR-005/D6.4` **remove o gatilho em vez de pagar o custo**: canonicalização e `fingerprint` são código de **Python e de Node, nunca do caminho de render do browser**, e o `fingerprint` viaja **fora da região hasheada**, no `ETag` (`D6.3`). Declarar a fronteira **hoje custa zero refatoração** — **3 de 3** importadores fora do módulo são `import type` `[MEDIDO 2026-09-03 em 8c002e4, n=3]`, e o preço é monotonicamente crescente depois. **⚠️ UMA METADE CONTINUA DO OWNER:** se o selo de integridade for **exibido/verificado na tela**, `crypto.subtle` é inevitável e o falsificador **fica assíncrono** — decisão de **produto** | `ADR-005`/D6.3,D6.4, `ADR-008`/DoD-2, fato 6 | `web` |

### DoD dos itens novos — comando e universo

| # | critério | ação | saída esperada |
|---|---|---|---|
| **D5.13** | **a rota é provada PELA REDE, e o portão prova nos dois lados** — não por subprocesso, não por payload hipotético. **E o código que a serve mora FORA de `src/modules/`** (`ADR-009/D6.2`) | teste que **sobe o processo ASGI em loopback** e faz **`GET` real** por `http.client`; **mais** `find backend/src -maxdepth 1 -type d` para a localização | **morde** — com o processo **derrubado**, o teste **reprova por conexão recusada**, não por asserção de payload · **cala** — processo de pé ⇒ **`200`** + corpo endereçável por conteúdo, e **zero campo de nível de tick** (`agg_id`, preço por trade, quantidade por trade) — que é `D5.8` **finalmente com sujeito**. **Precedente medido, a técnica já existe aqui:** `grep -rln 'TcpFakeServer\|HTTPServer\|socketserver\|bind(' backend/tests` → **3 arquivos** de **114** `[MEDIDO 2026-09-03: find backend/tests -name 'test_*.py' \| wc -l → 114]`, e o cliente HTTP que este backend fala é `http.client` — **5 dos 49** módulos de `infra/` o importam, e **`httpx`/`requests` não são declarados** em `backend/pyproject.toml` `[MEDIDO 2026-09-03]`. **⚠️ Este DoD mede EXISTÊNCIA E REDE** — *existe servidor · responde pela rede · não vaza tick*; a **forma do corpo** aterrou em `ADR-005/D6.1` e é medida por `D5.14`. **E `D5.8` NÃO foi tocado:** a emenda registra que corrigir o enunciado dele é *"ato do plano `05`"*, mas enunciado de DoD já `done` é **⛔ owner** — este DoD dá o **sujeito** ao predicado negativo de `D5.8` **sem reescrever uma palavra dele** |
| **D5.13b** | **a dependência de runtime nasce com pin exato** | `grep -nE '^dependencies' -A4 backend/pyproject.toml` | hoje **1 entrada** — `pyyaml (==6.0.3)` `[MEDIDO 2026-09-03]`. FastAPI + servidor ASGI são as entradas **2 e 3**, **com `==`**: faixa (`^`, `>=`) **reprova**, pela doutrina escrita no próprio arquivo (*"reprodutibilidade sobre conveniência"*). **A dependência é premissa do owner, não escolha de agente:** *"Já havia sido pontuado antes que o back subiria um servidor com fastapi"* `[PREMISSA-OWNER: 2026-09-03]`. **E o objeto de aplicação não nasce em `modules/`:** no vizinho, `grep -rln 'FastAPI(' anything_monorepo/backend/src` → **`src/main/__init__.py`**, **1 arquivo** `[MEDIDO 2026-09-03]`; aqui `find backend/src -maxdepth 2 -type d` devolve hoje **só `modules/` e `modules/sentimento`** `[MEDIDO 2026-09-03]` ⇒ os diretórios da camada consumidora **nascem nesta task** |
| **D5.13c** | **o handler NÃO é uma segunda definição de `ingest_health_query`** — restrição que a emenda de `ADR-005` criou e nomeou | `grep -n 'ingest_health_query' <handler>` **e** `grep -niE 'SELECT\|FROM \|psycopg\|sqlite3' <handler>` | **≥ 1** chamada a `ingest_health_query(source)` e **`0`** linha de SQL no handler. O ponto único existe e é medido: `backend/src/modules/sentimento/use_cases/ingest_health.py:32`, e `grep -c 'SELECT' ingest_health.py` → **`0`** `[MEDIDO 2026-09-03]`. **⚠️ `[NÃO SEI]` se a regra própria + corpus de `ADR-008/DoD-1` ALCANÇAM um *route handler* Python — dona: `ADR-008/D4`** (`ADR-005` §*"O que `D6` OBRIGA `ADR-008` a olhar"*). **E a lacuna é medida, não suposta: o instrumento de `DoD-1` NÃO EXISTE hoje** — `harness rules list` → **10 regras, 5 `core` + 5 `web-fullstack`, ZERO própria**, e as **4** ocorrências de `[[rules.own]]` em `harness.toml` estão **todas em linha de comentário** (:386, :455, :461, :463) `[MEDIDO 2026-09-03: grep -c '\[\[rules.own\]\]' harness.toml → 4; harness rules list | grep -oE '\((core|web-fullstack|own)' | sort | uniq -c → 5 core, 5 web-fullstack]` ⇒ **este DoD prova por SÍTIO DE CHAMADA, não por regra**, até `ADR-008/D4` decidir. **⚠️ E a restrição FICA MAIS FORTE com a correção do owner, não mais fraca:** fora do bounded context o handler **não tem como** alcançar o store sem furar a camada — o contrato **(4)** de `D5.13d` reprova `src.api → src.modules.sentimento.infra` **por grafo**, e o adaptador só chega **por injeção**. Dentro de `infra/` (a forma recusada) o mesmo handler teria o driver do store **como vizinho de diretório**, e nenhuma ferramenta o veria fazer SQL próprio |
| **D5.13d** | **os DOIS contratos de `import-linter` que a camada consumidora exige — e o par morde/cala de `1.8'`** | `make boundaries` (⇒ `poetry run lint-imports`) com os contratos **(3)** `layers` e **(4)** `forbidden` de [`ADR-009/D6.3`](../../adr/ADR-009-reuso-da-forma-do-anything.md) declarados: **(3)** `layers = ["main", "api | jobs", "modules"]`, `containers = ["src"]` · **(4)** `source_modules = ["src.api", "src.jobs"]`, `forbidden_modules = ["src.modules.*.infra"]` — **WILDCARD, e não o nome literal do contexto** | **morde** — **2 violadores plantados, 1 por contrato**: um `from src.api…` **dentro** de `backend/src/modules/sentimento/` (contrato 3) e um `from src.modules.sentimento.infra…` **dentro** de `backend/src/api/` (contrato 4) ⇒ `≠ 0` **nomeando o contrato** · **cala** — sem os violadores ⇒ **verde**, com a camada real de `5.13` na árvore. **Universo de hoje é ZERO e por isso mede erosão, não conquista:** `grep -rnE 'from (src\.)?(api|jobs)' backend/src/modules --include='*.py' | wc -l` → **`0`** `[MEDIDO 2026-09-03]`, e `find backend/src -maxdepth 1 -type d` → **só `modules/`**. **A sintaxe de irmão independente foi conferida na ferramenta, não na doc:** `_INDEPENDENT_LAYER_DELIMITER = "|"` `[MEDIDO 2026-09-03: backend/.venv/lib/python3.13/site-packages/importlinter/contracts/layers.py:20-21, import-linter 2.14]` ⇒ `api` e `jobs` **não se importam entre si**. **⚠️ Aqui este plano vai ALÉM do vizinho, de propósito:** `grep -cE 'src\.api|src\.jobs' anything_monorepo/backend/pyproject.toml` → **`0`** de **13** contratos `[MEDIDO 2026-09-03]` — o vizinho **pratica** e **não declara**; `jobs/` dele importa `infra` **2×** (de 4). **Recortar `src.jobs` do contrato (4) para fechar este DoD é o sinal de erosão que `ADR-009/F-D6-3` nomeia — o desfecho certo é a raiz de composição**. **⚠️ O WILDCARD em (4) é CONSERTO DE DEFEITO, e o achado é do `quant-architect`, não meu** — `gates/estudo-particao-sentimento-2026-09-03.md` §7.1/§7.3 (`A5`/`A6`), 858 linhas, 51 `[MEDIDO]`: **`forbidden_modules` apontando para módulo inexistente devolve `KEPT`, `0 broken`, `rc=0`** ⇒ a versão anterior desta linha, que enumerava `src.modules.sentimento.infra`, perderia **11 módulos de `infra`** da cobertura **sem um byte de aviso** se `sentimento` fosse partido ou renomeado — o `rc=0` indistinguível entre *"nada viola"* e *"o contrato não olhou nada"* que `ADR-012` nomeia. **Remedi as três metades eu mesmo antes de aceitar** `[MEDIDO 2026-09-03, import-linter 2.14, config em scratch, `backend/pyproject.toml` NÃO tocado]`: **morde** — `source_modules = ["src.modules.*.infra"]` + `forbidden_modules = ["socket"]` → **`BROKEN`, `rc=1`**, nomeando `src.modules.sentimento.infra.binance_stream_probe -> socket (l.14)` · **cala** — o mesmo com `*.domain` → **`KEPT`, `rc=0`** · **fantasma** — `forbidden_modules = ["src.modules.catalog.infra"]` (inexistente) → **`KEPT`, `0 broken`, `rc=0`**, o defeito reproduzido. **Contrato que enumera nome de contexto é contrato que CADUCA EM SILÊNCIO quando o contexto muda de nome — e o wildcard custa zero hoje, porque casa exatamente um contexto.** **⚠️ E o contrato (3) NÃO precisa de wildcard, por uma razão que medi e que é a assimetria do mecanismo:** ele não cita contexto nenhum (`containers = ["src"]`), e **`layers` FALHA ALTO** — `layers = ["main", "api | jobs", "modules"]` sobre a árvore de hoje devolve **`Missing layer in container 'src': module src.main does not exist.`, `rc=1`** `[MEDIDO 2026-09-03]`. **`forbidden` erra em silêncio; `layers` erra aos gritos** ⇒ (3) não pode virar fantasma, e a consequência de ordem é: **(3) só pode ser declarado na mesma task que cria os diretórios**, senão `make boundaries` fica vermelho por ausência. **Os contratos 1 e 3 que JÁ vivem em `backend/pyproject.toml` (o `layers` por contexto e o da natureza, que perde 31 de 79 módulos) são achado do mesmo estudo e NÃO são meus: donos `ADR-011/D3a` e `ADR-016/D4`** — não os toquei |
| **D5.14** | **o cliente decodifica o envelope de `D6.1`, e a paridade de `DoD-2` passa a comparar DUAS serializações INDEPENDENTES** — pela primeira vez, e é o argumento dirimente da emenda | o par `history-transport.ts` + consumidor de projeção contra a rota de `D5.13`, **mais os dois controles de `ADR-005/F-D6-1` e `F-D6-2`** | **morde** — **(i)** servidor ausente ⇒ **reprova** (é o defeito exato com que `5.12`/`T-05.9` fecharam: `frontend/src/app/history-transport.ts:20-33` declara *"NÃO cria servidor/endpoint algum"*); **(ii)** **reordenar as 15 colunas na rota MOVE o `sha256`** e reprova — controle negativo **obrigatório** de `F-D6-1`, e igual nos dois lados é `DoD-2` **vacuoso de novo**; **(iii)** campo novo **dentro de `runs[]` MOVE** o `sha256` e campo novo **no envelope NÃO move** (`F-D6-2`) — comportamento igual nos dois prova que **a região hasheada não está definida e `D6.3` está violada** · **cala** — envelope real ⇒ view-model montado e `fingerprint` do TS **igual** ao de `IngestHealthReport.fingerprint()` sobre o **mesmo fixture congelado**. **Forma do fio, e ela reprova por desvio:** **15 colunas** de `run` / **8** de `gap`, nome **`class`** e nunca `gap_class` — servir as **17** de `IngestRun` (`ingest_record.py:100-117`) reprova, porque `started_at`/`ended_at` são **colunas de tabela, não de projeção** (`ADR-005/D6.1`). **E o endereço não é literal:** `harness rules --mode file --path frontend/src/app/history-transport.ts` **não** acusa `web-fullstack.hardcoded-url` `[DOC: harness rules list, n=10 regras]` ⇒ a base da URL vem de configuração de ambiente. **⚠️ `parseCanonicalProjection` (:266-312, 47 linhas) SAI** — mantê-lo vivo depois de `D6.1` é manter **dois formatos para um contrato** |
| **D5.15** | **`node:` fora do universo de produção de `web`** | `grep -rln 'from "node:' frontend/src --include='*.ts' \| grep -v '\.test\.'` | **`rc=1`, zero arquivos.** Hoje **2 de 35** módulos não-teste, e o segundo (`app/threshold-spec-bundle.ts`) **não tem subprocesso nenhum** — é `createHash` `[MEDIDO 2026-09-03]`. **E o arquivo não vira teste por decreto:** `harness code-paths classify frontend/src/features/s1-console/ingest-health-query.ts` continua **`producao`** (casa `include_prefixes` + `include_globs`, não casa `test_globs`). **⚠️ O que este DoD NÃO fecha:** *onde* vive a testemunha cruzada Python×TS — hoje **1 de 10 ports** a tem e `grep -rln frontend backend/tests` → **`rc=1`, zero** `[MEDIDO 2026-09-03, fato 7]`. É **`A1`**, dono `quant-architect` |
| **D5.16** | **o portão de tipo MORDE e CALA, e é o `make` que o roda** — `1.8'` aplicado a `tsc` | **morde:** erro de tipo plantado ⇒ `make lint` **`≠ 0`** nomeando arquivo e linha, **e `git push --dry-run` RECUSADO** (o padrão de `D1.7c`/`D1.11`) · **cala:** árvore limpa ⇒ **`rc=0`** | universo: **35 módulos / 5.741 linhas** + **3 `.tsx` / 409 linhas** `[MEDIDO 2026-09-03]`. **O "antes" é ZERO, e é o argumento inteiro:** `ls frontend/tsconfig.json` → **inexistente**, `grep -cE 'typecheck\|noEmit' frontend/package.json` → **`0`** `[MEDIDO 2026-09-03]`, e a única passada de `tsc --noEmit --strict` do repositório foi **à mão, uma vez**, por `T-07.14` `[DOC: docs/INDEX.md:125]`. **`[NÃO MEDIDO]`: quantos dos 35 passam hoje em `--strict`** — medir é o primeiro ato da task, e **baixar o `strict` para o portão "fechar" REPROVA** (é o mesmo modo de falha que `D5.12` recusa por escrito) |
| **D5.16b** | **o app RENDERIZA, e não só compila** | subir o app e abrir a rota da S2-mínima | os **3 `.tsx`** importam `react` — hoje `grep -rn 'from "react"' frontend/src \| wc -l` → **`0`** `[MEDIDO 2026-09-03]`. **Reprova se o app subir sem renderizar nenhum dos 3**: compilar sem renderizar é o mesmo vácuo do transporte sem servidor |
| **D5.17** | **o falsificador de `DoD-2` fica SÍNCRONO, e a fronteira que o mantém assim é MEDIDA** — `ADR-005/D6.4` decidiu por remoção do gatilho, não por pagamento do custo | **(a)** a paridade de `F-D6-1` roda em **Python × Node**, fora do caminho de render; **(b)** `grep -rn 'from "\(\.\./s1-console/\|\./\)ingest-health-query\.ts"' frontend/src --include='*.ts' --include='*.tsx' \| grep -v '\.test\.'` cruzado com `import type` | **morde** — um importador **DE VALOR** (não `import type`) plantado do bundle de browser para dentro do módulo de canonicalização ⇒ o portão **reprova nomeando o arquivo** · **cala** — a árvore de hoje ⇒ **3 de 3 `import type`, zero aresta de runtime** `[MEDIDO 2026-09-03 em 8c002e4, n=3: `s3-inspector/{fixtures,domain,view-model}.ts`; `S1Console.tsx` importa só `S1ViewModel`]`. ⇒ **`fingerprint(): string` PERMANECE síncrona** (`ingest-health-query.ts:262-264`), e o `fingerprint` viaja no **`ETag`** (`D6.3`) **sem o cliente confiar nele** — *"servidor manda o `fingerprint` e o cliente confia"* é alternativa **RECUSADA** em `D6` por comparar **um número consigo mesmo**. **⚠️ `[NÃO SEI]` A METADE DE PRODUTO, E ELA É DO OWNER:** *o operador VÊ o selo de integridade na tela?* Se sim, `crypto.subtle` é inevitável e este DoD **muda de forma**. **⚠️ E o INSTRUMENTO do portão não é decidido aqui:** é a mesma pergunta aberta que `D5.12` carrega — `no-restricted-imports` casa **CAMINHO**, e `ADR-003:46` recusa amarrar componente a caminho; este item **herda** a pergunta, não a redecide. `frontend/src/app/threshold-spec-bundle.ts:36,371` (o 2º `createHash` do front) é **outra dona** — bundle-URL de `SPEC-001` §7: declarado, não decidido |

### A emenda de `ADR-005` ATERROU no mesmo dia — o que isto reconciliou, e o que continua aberto

**Cronologia, para o leitor não achar que o plano nasceu decidido:** os itens `5.13`–`5.17` foram escritos com `A5` **em voo**, e a emenda (`ADR-005` §`D5`+`D6`, **append puro** — `D1`–`D4` intocados) aterrou enquanto isto era redigido. Os DoD `D5.13`, `D5.14` e `D5.17` foram **reescritos contra a emenda lida na fonte**, e a emenda **não foi editada por este ato** (é do `quant-architect`).

| o que era `[NÃO SEI]` aqui | o que a emenda decidiu | efeito no plano |
|---|---|---|
| corpo da resposta — linhas canônicas × rows | **rows em envelope JSON tipado** (`D6.0`/`D6.1`); projeção canônica é **função derivada, calculada nas duas pontas**, nunca formato de transporte | `D5.14` **fecha o `[NÃO SEI]`**; `parseCanonicalProjection` (:266-312, **47 linhas**) sai; **204 de 251** linhas reféns vivem (**81,3%**) |
| onde vive o `fingerprint` | **`ETag`, FORA da região hasheada** (`D6.3`) — hash dentro da própria entrada não é reproduzível por ninguém | `D5.14` ganha o controle `F-D6-2`: campo no envelope **não** move o `sha256`, campo em `runs[]` **move** |
| `fingerprint` síncrono × assíncrono | **`D6.4`: canonicalização e `fingerprint` são código de Python e de Node, NUNCA do caminho de render** ⇒ o falsificador **fica síncrono**, e declarar a fronteira hoje custa **zero refatoração** (**3 de 3** importadores são `import type`, n=3) | `D5.17` **meio-fecha**: a fronteira é medível agora; **a pergunta de produto (*o operador vê o selo?*) segue do owner** |
| — (não estava no plano) | **`DoD-1` de `ADR-008` ganha candidato novo a "segunda definição": o handler FastAPI** | **`D5.13c` NOVO**, e `5.13` carrega a restrição: o handler **chama `ingest_health_query(source)`**, não faz SQL próprio |

**E uma coisa que a emenda pede e este ato NÃO fez, deliberadamente:** ela registra que *"corrigir o enunciado de `D5.8` é ato do plano `05`"*. **`D5.8` está `done` e enunciado de DoD fechado é ⛔ owner** — a autorização de `A7` é *"o que ta em done continua done"*. `D5.13` dá o **sujeito** ao predicado negativo de `D5.8` sem reescrever uma palavra dele; corrigir o texto de `D5.8` fica como **pedido ao owner**, não como ato tomado.

### ⚠️ A forma de `5.13` foi CORRIGIDA pelo owner em 2026-09-03 — e o que estava errado era MEU argumento

**A primeira redação deste item, escrita na manhã de 2026-09-03, punha a rota como camada `infra/` do contexto `sentimento`.** O owner recusou a forma, literalmente:

> *"precisa ser exposto uma camada de API, daí a camada de api n pertence ao bounded-context, é o consumidor, usando de injeção de dependencias dos módulos. works tendem a ser a mesma coisa."*

`[PREMISSA-OWNER: 2026-09-03]` — registro: [`decisoes-do-owner.md` §*"o owner CORRIGE a forma de `5.13`"*](../../decisoes-do-owner.md).

**O defeito não foi de gosto, e nomeá-lo importa mais que corrigi-lo:** eu fundamentei a atribuição em [`backend/pyproject.toml:202-207`](../../../backend/pyproject.toml) — o contrato `layers`/`containers` que **já existe** — e chamei isso de *"encaixe não forçado"*. Verdade sobre o contrato existente, **falso como argumento de forma**: `ADR-009` existe para *"reusar a FORMA do `anything`"*, e a forma do vizinho **tem uma camada consumidora fora de `modules/`** que a `D1` daquela ADR **nunca listou**. Remedi eu mesmo antes de aceitar:

```bash
find anything_monorepo/backend/src -maxdepth 2 -type d      # src/{api,api/routes,jobs,main,core,infra,config,modules}
grep -rln 'FastAPI(' anything_monorepo/backend/src          # -> src/main/__init__.py
grep -rhoE 'from src\.modules\.[a-z_]+\.(domain|use_cases|infra)' anything_monorepo/backend/src/api \
  --include='*.py' | awk -F. '{print $NF}' | sort | uniq -c  # 40 domain · 94 use_cases · ZERO infra
```

`[MEDIDO 2026-09-03]`: **`api/` = 34 módulos / 10.854 linhas**, **`jobs/` = 3 / 186**, e **68 módulos / 15.922 linhas (30,8% das linhas) vivem FORA de `modules/`** no vizinho (n=327 módulos). **`api/` importa `use_cases` 94× e `domain` 40×, e a `infra` de contexto ZERO vez**; **28 de 34** módulos importam a raiz de composição `src.config`. **E o vizinho não declara isso em contrato:** `grep -cE 'src\.api|src\.jobs' anything_monorepo/backend/pyproject.toml` → **`0`** de **13** contratos ⇒ lá é convenção, aqui passa a ser **portão** (`D5.13d`).

**A emenda de arquitetura está escrita, com falsificador:** [`ADR-009` §`D6`](../../adr/ADR-009-reuso-da-forma-do-anything.md) — append puro, `D1`–`D5` intocados, **inclusive a omissão de `D1`**, porque reescrevê-la apagaria a evidência de que o defeito existiu. `D6` decide **a camada, a injeção e os dois contratos**; **não** decide rótulo de componente.

### Menu para o owner — qual RÓTULO DE COMPONENTE a camada de API/worker recebe

**✅ ESCOLHIDA em 2026-09-03: opção `A` — `infra`.** `[DECISÃO-OWNER: 2026-09-03, escolha entre alternativas apresentadas]` — **e o rótulo é este, não `[PREMISSA-OWNER]`**: o owner escolheu uma opção de um menu que um agente redigiu, com o custo de cada uma declarado; **nenhuma frase abaixo é fala dele**. O custo que ele aceitou, como estava escrito no menu: *"põe schema HTTP e TLS/compose sob o mesmo juiz"*.

**O menu fica registrado como estava — as opções recusadas e o custo de cada uma são a metade que torna a decisão auditável.** Hoje `harness policy --key components` → **6** rótulos `[MEDIDO 2026-09-03]`; com a materialização, **7**.

| # | opção | o que muda | custo, medido |
|---|---|---|---|
| **A** ✅ | **`infra`** — o nome que `ADR-009/D5` já propôs · **ESCOLHIDA** | `components` **6 → 7**; a camada de API entra no mesmo rótulo de `deploy/`, backup-com-restauração e topologia do escritor único | **um rótulo para quatro coisas diferentes:** quem julga **schema HTTP** passa a ser quem julga **TLS e compose**. Fecha `ADR-009/D5` **de uma vez** — é a opção com menos atos de política |
| **B** | **`api`** — rótulo só para a camada consumidora | `components` **6 → 7** com nome estreito; **`jobs/` fica de fora ou o nome mente** — a premissa do owner diz *"works tendem a ser a mesma coisa"* | **duas decisões de vocabulário em vez de uma:** `ADR-009/D5` continua aberto para `deploy/`/backup. Nome mais preciso para a API, **impreciso para o worker** |
| **C** | **nenhum** — vocabulário fica em 6 | a camada declara `sentimento` (o contexto que consome) ou `docs` por eliminação | **é o defeito que `ADR-003:11-13` nomeia** — *"componente omitido é componente sem dono de julgamento"*. E `sentimento` seria **falso por construção**: `D6.2` acabou de decidir que a camada está **fora** do contexto. Zero ato de política, e a dívida fica **silenciosa** |

**O que NÃO muda em nenhuma das três — e isto CORRIGE uma suposição do registro de decisão:**

| superfície que o registro supôs afetada | medição | veredito |
|---|---|---|
| `[code_paths]` | `harness code-paths classify backend/src/api/routes/ingest_health.py` → **`producao`** — *"include_prefixes + include_globs casam e nada exclui"* | **não precisa de nada** |
| `require-code` | `harness pipeline require-code backend/src/api/routes/ingest_health.py` → ***"código permitido — feature 'plataforma-dados' (scope)"*** — `backend/src` já é um dos **19** prefixos (`harness pipeline scope plataforma-dados list \| wc -l` → **19**) | **não bloqueia** |
| `[agents.by_component]` | `V-16` reprova componente **fora do enum** (`lib/policy.py:539-543`) | **é o efeito REAL:** A e B exigem editar `components` **ANTES** de declarar qualquer juiz |

`[MEDIDO 2026-09-03]` — ⇒ **o efeito do rótulo é em `components` + `[agents.by_component]`, e só.** `docs/decisoes-do-owner.md` supôs efeito também em `[code_paths]` e `require-code`; medido, não há.

### Onde mora o item que MATERIALIZA o vocabulário — e a resposta é: já existe, e não é aqui

**Não é `05` e não é `01`. É a fase `09`, e o item já está escrito e ABERTO** `[MEDIDO 2026-09-03]`:

```bash
grep -n 'componente `infra`' docs/plans/SPEC-001-plataforma-dados/09_*.md   # item 9.6, DoD D9.6
grep -n -A6 '^id = "T-09.4"' docs/context/plataforma-dados/tasks.toml       # status = "todo"
```

| o que | onde | estado |
|---|---|---|
| item de plano | **`9.6`** — *"Decisão sobre o componente `infra` **registrada** — adotada ou recusada, **com o motivo escrito**"*, requisito `ADR-009/D5`, componente `docs` | **existe** |
| DoD | **`D9.6`** — *"adotado **ou** recusado, com o motivo — **nunca ausente**"* | **existe** |
| task | **`T-09.4`** / `CST-86`, `components = ["docs"]`, `depends_on = ["T-01.4","T-02.4a"]` | **`todo`** — nunca fechada |
| a proposta que a originou | **`1.7`** da fase `01`, materializada em **`T-01.4`** (`done`) — *"proposta do componente infra levada ao owner … NÃO decidida aqui"* | **cumprida** |

⇒ **nenhum item de plano novo é necessário em fase nenhuma, e nenhuma fase precisa ser reaberta:** `T-09.4` está `todo`, e o conteúdo dela é **exatamente** este registro. **Escrever a decisão em `05` criaria uma segunda verdade sobre a mesma coisa** — o defeito que `PRD-002` §3.2 chama de *"reabrir aqui criaria duas verdades sobre a mesma superfície"*.

**O que ISSO deixa como pergunta de SEQUÊNCIA, e é do owner:** `T-09.4` vive na fase **`09`** e `5.13` precisa do rótulo **na fase `05`**. Fazer `05` depender de `09` inverte a ordem do plano. Duas saídas, e o custo de cada uma:

| # | saída | custo |
|---|---|---|
| **(i)** | **antecipar `T-09.4`** (está `todo`; o `/tech-lead` a materializa agora) | **zero edição de plano, zero reabertura.** A fase `09` perde uma task que ela nunca começou, e o registro passa a existir antes de quem o consome |
| **(ii)** | task nova na fase **`01`**, junto de `A6` (é o mesmo ato: **política, não superfície**; `1.2`/`1.13` são os itens donos) | é a fase que `A6` já pediu para reabrir ⇒ **um ato de owner cobre os dois**; mas cria segunda superfície de registro se `T-09.4` continuar de pé |

**Eu não executei nenhuma das duas** — `tasks.toml` e ledger são intocáveis para mim, e `advance`/`build` são gates de owner.

### Menu do JUIZ de `infra` — `[agents.by_component.infra].architect`, e eu não escolho

**A chave precisa de dono, e hoje só existem DOIS agents:** `ls .claude/agents/` → **`quant-architect.md`, `ui-designer.md`** (**n=2**) `[MEDIDO 2026-09-03]`. E `V-16` **reprova componente fora do enum** (`lib/policy.py:539-543`) ⇒ **`components` tem de ganhar `infra` ANTES** de qualquer juiz ser declarado — a ordem não é preferência.

| # | opção | o que compra | o custo, e ele é o próprio argumento do menu anterior |
|---|---|---|---|
| **J1** | **`quant-architect`** acumula `infra` | zero arquivo novo; ele já é `architect` de `charts` e já decidiu `ADR-005/D6` (schema de transporte) | **ele não é dono de TLS, compose nem de topologia de fila.** É metade do custo que o owner aceitou (*"schema HTTP e TLS/compose sob o mesmo juiz"*) **realizada na pessoa errada** |
| **J2** | **agent novo** — `infra-architect`, portado do `anything` se houver forma reusável | o juiz cobre o que o rótulo cobre | **arquivo novo + `[agents.by_component]` + doutrina.** E `find anything_monorepo -path '*/agents/*.md'` → **12 arquivos** `[DOC: decisoes-do-owner.md §A6]`, **nenhum de infra** ⇒ é criação, não porte |
| **J3** | **só `design_gate`/nenhum `architect`** por ora | zero ato; o papel é **aberto** (`lib/policy.py:549-550`) e a chave pode nascer só com o que existe | **`ADR-003:11-13`**: *"componente omitido é componente sem dono de julgamento"* — o rótulo passaria a existir **sem** juiz, que é o defeito que `T-01.3` fechou para `charts`/`web`. **Declarar `infra` sem `architect` publica na política um componente sem dono** |
| **J4** | **owner como juiz declarado** | honesto sobre quem decide TLS/VPS hoje | não há ponteiro de arquivo para "owner"; `checa_ponteiro` exige **caminho relativo que EXISTA** ⇒ exigiria um `docs/…md` fazendo o papel, como `design_gate` já faz |

**⚠️ O que o custo que EU medi sugere, e é argumento, não escolha:** *"schema HTTP e TLS/compose sob o mesmo juiz"* é exatamente a razão de **nenhum dos dois agents existentes** servir inteiro. **A escolha é do owner** — inclusive a de dividir a chave em dois papéis, que o esquema comporta.

### ⏸ O que NÃO decidi, e não é por omissão

**`sentimento` deve ser partido em mais de um bounded context?** **`130` módulos / `18.946` linhas** contra **`50` / `9.313`** do maior contexto do vizinho (`messages`, de 12) `[MEDIDO 2026-09-03]` — **2,6×, sendo o único**. A pergunta é de **partição**, não de rótulo, tem **relógio de retrabalho** (contrato `containers` + imports), **não bloqueia `5.13`**, e o dono é o **owner**, assessorado pelo `quant-architect`. Registrada em `ADR-009/D6.4` e na tabela de decisões abaixo.

### `A6` NÃO está nesta fase — o julgamento, com o número

A materialização de `A6` (arquiteto de front próprio, `web.builder`/`web.qa`, os dois agents portados do `anything`) **pertence à fase `01`**, por três razões independentes:

1. **A superfície é de `01`, por item:** `[agents.by_component]` é o item **`1.2`** (*"ganha entrada para `charts` e `web`"*, DoD `D1.2`) e a **asserção** sobre ela é o item **`1.13`** (DoD `D1.12`). **Nenhum item da `05` nomeia `harness.toml`.**
2. **O componente não cabe:** todo item de `01` que toca política declara **`docs`**; o cabeçalho desta fase declara **`charts` + `web`**, e o falsificador **F-3** exige **um** componente declarado por item.
3. **O portão que deveria segurar a atribuição não existe — e isso agrava:** `grep -rn 'by_component' Makefile scripts/ backend/scripts/` → **`rc=1`, nenhuma linha**; `grep -c 'lint-shell' Makefile docs/context/plataforma-dados/tasks.toml` → **`0` e `0`**; `awk` sobre `tasks.toml` → **7 tasks `T-01.1`..`T-01.7`, todas `done`** `[MEDIDO 2026-09-03]` ⇒ os itens **`1.12`** e **`1.13`** da fase `01` **nunca foram materializados**. Trocar `web.architect` hoje passa **em silêncio** — exatamente a mutação que `docs/gate-de-design.md` mediu.

⇒ **Nada foi escrito na fase `01`:** reabri-la é ato do owner, e ele autorizou **só** a `05` (`A7`). **Pergunta ao owner**, com as duas opções e o custo de cada uma: **(i)** materializar `A6` como task do item **`1.13`** que já existe e nunca teve task — **nenhum item de plano é criado ou reescrito**, mas é task nova numa fase cujas 7 tasks estão `done` (gate `build` é do owner); **(ii)** autorizar **item novo** na fase `01` (`1.14`) — mais explícito no plano, e custa reabrir uma segunda fase.

### O que estes itens NÃO decidem — e de quem é cada decisão

| # | pergunta | dono |
|---|---|---|
| ~~`A5`~~ | **✅ ATERROU em 2026-09-03 — `ADR-005` §`D5`+`D6`**, append puro do `quant-architect` (`D1`–`D4` intocados): **rows em envelope JSON tipado**, a projeção canônica é **função derivada calculada nas duas pontas** e o `fingerprint` viaja no **`ETag`**, fora da região hasheada. **Fecha o `[NÃO SEI]` de `D5.14`** e **meio-fecha `D5.17`** | **decidido** — `quant-architect` |
| `A1`–`A3` | fonte de verdade do domínio portado, *delisting badge*, `bigint` × `Decimal` | `quant-architect` |
| **selo de integridade na TELA** | `D6.4` mantém o falsificador **síncrono** tirando canonicalização e `fingerprint` do caminho de render — e declara que **isso não antecipa a pergunta de produto**: *o operador VÊ o selo?* Se sim, `crypto.subtle` é inevitável e o falsificador de `DoD-2` fica **assíncrono**. É a metade de `D5.17` que **não** aterrou | **⛔ owner** |
| `ADR-008/DoD-1` | a regra própria **+ corpus** de `DoD-1` alcançam um *route handler* Python? A restrição de `D5.13`/`D5.13c` existe hoje **sem instrumento**: `harness rules list` → **10 regras, 0 próprias** `[MEDIDO 2026-09-03]` | `ADR-008/D4` |
| `threshold-spec-bundle.ts` | o **2º** `createHash` do front (:36, :371) é o bundle-URL de `SPEC-001` §7 — `ADR-005/D6.4` o declara e **não** o decide | `SPEC-001` §7 (dono do bundle-URL) |
| ~~**RÓTULO** da camada~~ | **✅ DECIDIDO 2026-09-03 — opção `A`: `infra`.** `[DECISÃO-OWNER: 2026-09-03, escolha entre alternativas apresentadas]`, com o custo *"schema HTTP e TLS/compose sob o mesmo juiz"* **aceito**. `components` **6 → 7**; efeito **só** em `components` + `[agents.by_component]` `[MEDIDO 2026-09-03]` | **decidido** — owner |
| **JUIZ de `infra`** | `[agents.by_component.infra].architect` — **`J1` acumular no `quant-architect` · `J2` agent novo · `J3` sem `architect` · `J4` owner por documento**; menu com custo em §*"Menu do JUIZ"* acima. **Só 2 agents existem** (`n=2`), e `V-16` exige `infra` no enum **antes** | **⛔ owner** |
| **cobertura de `deploy/`** | o owner autorizou o item; **ele NÃO nasce na `05`** — fechá-lo aqui contradiz `5.11` (*"vps n é problema agora"*, `PRD-001` §12). Nasce na **`01`**, por precedente literal do item `1.4`, **que está `done` e o owner não autorizou reabrir** ⇒ especificado em [`ADR-009/D6.7`](../../adr/ADR-009-reuso-da-forma-do-anything.md) e **não escrito em plano nenhum**. **Um único ato de owner sobre a `01` materializa `A6` + `deploy/`.** Medições que o item carrega: as **10** regras em vigor alcançam `.py` e `frontend/src/**`, **zero** alcança `*.yml`; `deploy/` não existe; `classify` não confere existência ⇒ **o DoD é BLOQUEIO com `exit=2` sobre arquivo real**, nunca `classify` | **⛔ owner** |
| **SEQUÊNCIA do registro** | `T-09.4`/`CST-86` (`todo`) é o item que registra a decisão, e ele vive na fase **`09`**; `5.13` precisa do rótulo na **`05`**. **(i)** antecipar `T-09.4` — zero edição de plano · **(ii)** task na `01` junto de `A6` — um ato de owner cobre os dois | **⛔ owner** |
| `ADR-009/D5` | o componente `infra` é adotado ou recusado? **A camada consumidora é o QUARTO item que força o vocabulário** — e o primeiro que é **código de produção executável**, não infra de operação (`ADR-009/D6.4`) ⇒ reforça a proposta, **não** a decide | **⛔ owner** |
| **partir `sentimento`?** | **130 módulos / 18.946 linhas** contra **50 / 9.313** do maior contexto do vizinho (**2,6×, sendo o único**) `[MEDIDO 2026-09-03]`. Partição de contexto, **não** de rótulo; relógio de **retrabalho** (contrato `containers` + imports); **não bloqueia `5.13`** | **⛔ owner**, assessorado pelo `quant-architect` |
| `A6` | fase `01` — opção (i) ou (ii) acima | **⛔ owner** |
| **`5.11` / auth** | **a porta de leitura servida ressuscita a metade que `5.11` rebaixou?** `5.11` saiu de escopo por *"vps n é problema agora, vai rodar muito local até lá"* `[PREMISSA-OWNER: 2026-08-25]`, e `A4` admite que o `Next` *"proxia sessão/auth apenas"* — o que **pressupõe** que exista sessão. **`[NÃO SEI]`, e não é decidido aqui:** o que FICA de `5.11` (`principal_id` como dimensão) **não foi reaberto** e continua valendo | **⛔ owner** |

### Falsificador dos itens novos

**Dois, e os dois medem o mesmo modo de falha que produziu esta reabertura — DoD satisfeito sem o sujeito existir:**

1. **Qualquer um de `D5.13`/`D5.14` fechando com o processo servidor ausente da passada.** Foi assim que `T-05.9` e `T-08.11` fecharam `done` (`5.12` e `8.8`, as duas com **metade cliente só**), e `D5.8` mede uma propriedade **negativa** (*"nenhum tick chega ao browser"*) que é verdadeira sobre payload que ninguém serve. **Se o teste novo passar com o servidor no chão, o item novo repetiu o defeito que ele existe para consertar.**
2. **`D5.14` fechando sem o controle NEGATIVO de `F-D6-1`/`F-D6-2`** — reordenar as 15 colunas, ou acrescentar campo dentro de `runs[]`, **tem de mover** o `sha256`. Igual nos dois lados é `DoD-2` comparando **um número consigo mesmo**, que é a vacuidade que `ADR-005/D6` recusou por escrito. **E o irmão disto no lado servidor:** handler com SQL próprio ⇒ `D5.13c` reprova e `ADR-008/DoD-1` ganha a segunda definição que ela existe para proibir.
3. **`D5.16` fechando com `strict` afrouxado ou com `typecheck` fora de `make lint`.** Portão que ninguém roda é o achado de `D1.7c`; portão cego é o de `D1.10`. Um `tsconfig.json` com `strict: false` passa nos dois e **não mede nada**.

## Não faz

Não detecta nada — **zero algoritmo de SMC, zero limiar, zero "sinal"**. A caixa que o owner desenha é **entrada** da fase seguinte, não saída desta. Não tem painel de liquidação. Não tem watchlist multi-símbolo ao vivo. Não dispara ordem. Não mostra o mercado agora.

## Falsificador da fase

**F-3 global:** um item desta fase que não consiga declarar **UM** componente. O caso mais próximo do limite é `5.2`: a linha-guia de D5.2 é **geometria derivada de `available_at`** ⇒ **`charts`**, e `web` só entrega o par `(valor, available_at)`. Se essa atribuição não se sustentar na implementação, `ADR-003` está errado.
