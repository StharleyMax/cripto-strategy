# Registro de artefatos

Append-only. Uma linha por artefato, na ordem em que foi produzido. O que manda é o
**ledger** (`harness pipeline show <feature>`); este arquivo é o índice legível dele.

Criado em 2026-08-25 pelo `/pm`: o dashboard já apontava para `docs/INDEX.md`
(`harness status`, linha de cabeçalho) e o arquivo não existia. As linhas anteriores à
desta sessão foram reconstruídas do que está em disco e da data de modificação dos
arquivos — **não são declaração nova**, e as três rodadas de discovery aconteceram antes
de o pipeline existir para esta feature (`init` em 2026-08-25T11:22Z), por isso não têm
evento de ledger correspondente.

| Data (UTC) | Feature | Papel | Artefato |
|---|---|---|---|
| 2026-08-22 | `plataforma-dados` | owner | `docs/proposta-discovery.md` (âncora: plano de discovery, Módulos A–D) |
| 2026-08-22 | — | owner | `harness.toml` (política do repositório; componentes fechados, tracker CST, lacunas declaradas) |
| 2026-08-24T18:36Z | `plataforma-dados` | discovery (rodada 1) | `docs/avaliacao-discovery.md` (avaliação crítica, 89 achados + Anexo de completude) |
| 2026-08-24T20:34Z | `plataforma-dados` | discovery (rodada 2) | `docs/recorte-plataforma.md` (contrato de dados em 4 definições `SOLIDA_COM_CORRECOES`; recorte 68 plataforma / 21 diferidos; §5 o que o `/pm` recebe) |
| 2026-08-24T21:08Z | `plataforma-dados` | discovery (rodada 3) | `docs/plataforma-superficies-e-faseamento.md` (superfícies S1–S5, 4 decisões de tela, sistema de honestidade do dado, ordem F0–F5; 4 desenhos `SUSTENTADO_COM_CORRECOES`) |
| 2026-08-24T21:08Z | `plataforma-dados` | discovery (rodada 3) | `docs/decisoes-do-owner.md` (16 perguntas consolidadas e deduplicadas, ordenadas por urgência; 3 capture-or-lose) |
| 2026-08-25T11:22Z | `plataforma-dados` | owner | `harness pipeline init` + `dispatch pm` — trilha nasce em `INIT` |
| 2026-08-25T11:39Z | `plataforma-dados` | `/pm` | `docs/specs/PRD-001-plataforma-dados.md` (6 unidades de valor F0–F5, **sem histórias**, por pedido do owner; as 16 decisões classificadas em **0 bloqueantes / 13 não-bloqueantes / 3 inferíveis**; **6 GAPs novos**, quatro deles resgatados do §Anexo da avaliação) |
| 2026-08-25T11:39Z | `plataforma-dados` | `/pm` | `docs/context/plataforma-dados/handoff_to_architect.md` (3 decisões de arquitetura deixadas explícitas; a verificação urgente da doc de *Liquidation Order Streams*) |
| 2026-08-25T11:39Z | `plataforma-dados` | `/pm` | `docs/INDEX.md` (este arquivo) |
| 2026-08-25T12:41Z | `plataforma-dados` | `/pm` (revisão **R1**) | `docs/specs/PRD-001-plataforma-dados.md` **revisado no lugar** após gate reprovado. Dois validadores em paralelo: protocolo (`harness-plugin:architect`) devolveu `[READY FOR SPEC]` / 0 bloqueantes com 13 correções; **domínio** (`quant-architect`, apontado pela política para `sentimento`) devolveu `DOMÍNIO OK COM CORREÇÕES` com **6 itens de gate** — o orquestrador reconciliou como **GATE NÃO PASSA**. **18 defeitos corrigidos** (mapa em PRD §0.1): a inversão de `ASOF` (`>=` era o operador SEGURO e estava proibido), quarentena sem `available_at`, `bar_policy` sem mecanismo (nasce **R-2**), carimbo MODELED sem estatística, `run_registry` sem `knowledge_time`, `s3_inferred` admissível no caminho de decisão, **CL-4 novo** (`bookTicker` fora do dump desde 2024-03 ⇒ spread capture-or-lose), probe de disponibilidade contínuo em F0, e **6 defeitos de citação deste próprio PRD**. Novos: `[GAP G7]`, `[GAP G8]`, §18 (IDs estáveis + contagem auditada). **Decisões do owner 16 → 19** (Q17 spread · Q18 profundidade do backfill · Q19 conjunto do probe); GAPs 6 → 8; critérios ~80 → ~95; medições 34 → 40. **Estrutura F0–F5 e fronteiras de valor entre as fases: inalteradas.** |
| 2026-08-25T12:41Z | `plataforma-dados` | `/pm` (revisão **R1**) | `docs/context/plataforma-dados/handoff_to_architect.md` **atualizado no lugar** — §0 novo (o gate reprovado e por que os dois vereditos não se contradizem), **A3 FECHADA** (`!forceOrder@arr` existe ⇒ Q5 não trava F0), **A4 nova** (semântica `latest\|largest` `[NÃO VERIFICÁVEL HOJE]`), §3.5 (o que mudou na estrutura, para o fatiamento dos Epics), e **quatro correções em que este PM discordou do que lhe foi passado, com o argumento** (§8) |
| 2026-08-25T12:41Z | `plataforma-dados` | `/pm` (revisão **R1**) | `docs/INDEX.md` (estas linhas). **Ledger intocado: `PRD_DRAFT` antes, `PRD_DRAFT` depois** — nenhum `advance`, nenhum `approve`, nada criado, editado ou comentado no Jira |
| 2026-08-25T12:23Z | `plataforma-dados` | discovery (medição Coinalyze) | `docs/medicao-coinalyze.md` — **11 endpoints da Coinalyze chamados com chave real**, o que **falsifica** o `[DOC-ONLY]` que atravessa o PRD e **responde Q4 de fato**. Derruba: "agregado multi-exchange" (não existe), a retenção como janela de tempo (é **contagem de pontos** ⇒ série esparsa retém mais relógio), e a forma de **CL-1** (`/liquidation-history?interval=daily` = **730 dias** ⇒ o agregado diário é recuperável 2 anos; **só a liquidação intraday continua capture-or-lose**). Ganha: **`bv` é volume de compra agressora, provado contra o dump** (erro mediano 0,0000 bp vs 2.584,87 bp da hipótese maker; cauda **não** zero, p99 29,34 bp), OI `daily` com **2.409 dias**, e dois defeitos de plataforma (**zero header de cota** no `200`; **OI como OHLC do bucket**, não ponto). **Existência registrada pelo `/pm` em R1; NÃO incorporado ao PRD — chegou depois da validação de R1, e as mudanças que exige são de fase, não de citação. Ver PRD §0.2 e handoff §0.1** |
| 2026-08-25 09:46 (mtime) | `plataforma-dados` | owner | `docs/direcionamento-operacional.md` — **intenção declarada do owner**, não achado de medição. Não é HFT · prazos operacionais **15m/1h/4h** · **decisão no fechamento/consolidação do bucket** (1m/5m/15m), sem micro-tick · tese em três camadas (estrutura de preço com **pivôs + Fibonacci + volume** · sentimento com **OI + funding** · order flow com **CVD**) · backtest exige OHLCV+OI+Funding+CVD **alinhados no tempo** · granularidade sem inflar custo **em nuvem** · **autonomia delegada** ao time em bruto-vs-agregado, motor de banco (**+Parquet/DuckDB**) e terceiros (**+Coinglass**). **Resolve Q17 (parte técnica), sustenta `bar_policy = final_only` por declaração, dissolve o problema de 1 min que dominou a rodada de UX, e ABRE Q20 (SMC × pivôs+Fibonacci)** |
| 2026-08-25T13:17Z | `plataforma-dados` | `/pm` (revisão **R2**) | `docs/specs/PRD-001-plataforma-dados.md` **revisado no lugar** — **quita a dívida que R1 declarou em §0.2** e absorve o direcionamento operacional. **§0.3 novo é o mapa, e ele separa FASE de CITAÇÃO de propósito: 7 mudanças de fase (`E-01`..`E-07`) e 8 de citação (`C-1`..`C-8`).** Fase: **`CL-1` muda de forma** (agregado diário de liquidação recuperável **730 d**; capture-or-lose é o **intraday**) · **F0 ganha 2 entregas** (one-shot Coinalyze `daily` — OI **2.409 d**, liq **730 d**, **1.140 chamadas ≈ 28,5 min**; e a **reconciliação diária**, primeira série de referência independente para `latest\|largest`) **e PERDE o non-goal "não chama Coinalyze"** · **`cvd_source += coinalyze_bv`** com erro publicado, **e a quarentena NÃO se abre** (o 3º termo, `available_at IS NULL`, não foi resolvido) · **mudança de TIPO: `SeriesKey += reduction`, `ts_convention += OHLC_OVER_BUCKET`** — sem o termo, 4 séries de OI da Coinalyze e 1 da Binance colapsavam na mesma identidade · **R-2 promovida a premissa do owner** · **`bookTicker` sai do escopo e `CL-4` NÃO morre** · **`aggTrade` cru sai do requisito de captura, com o argumento corrigido**. Citação: **1,76 TB/ano → 340–420 GB/ano (erro de 4,8×)** · retenção por **contagem de pontos**, não tempo · Coinalyze **mais profunda em `daily`** (2.409 d) · **sem agregado multi-exchange** · **broker cego**. **Rótulo novo: `[PREMISSA-OWNER: data]`** — premissa do owner não é falsificável por medição. **4 critérios novos** (`CA-F0-13`, `CA-F0-14`, `CA-F2-16`, `CA-F2-17`) ⇒ ~95 → **~99**; medições 40 → **54**; **R12** novo (retenção da liquidação anticorrelacionada com a necessidade). **§13.5 novo responde a pergunta do owner: ESTRUTURA F0–F5 E FRONTEIRAS DE VALOR INTACTAS — SEIS Epics, com um SÉTIMO contingente a `Q2` = "exposto"** |
| 2026-08-25T13:17Z | `plataforma-dados` | `/pm` (revisão **R2**) | `docs/decisoes-do-owner.md` **reescrito como FONTE ÚNICA** de numeração e de estado — **havia colisão real** (R1 renumerou para 19 só no PRD; o owner criou `Q17` aqui e referenciou `Q20` sem que 18/19 existissem; **os dois `Q17` eram o mesmo item, o que foi sorte e não acerto**). **20 perguntas** com **vocabulário de estado** (`ABERTA` · `RESPONDIDA` · `RESPONDIDA COM RESÍDUO` · `MORTA` · `INFERÍVEL`): **13 abertas · 3 inferíveis · 2 com resíduo nomeado (`Q4` plano free/pago/descartar · `Q17` assumir ou medir a 1/min) · 1 MORTA com o motivo escrito (`Q9`, morta por aritmética: ~87 GB uma vez para o histórico inteiro em bucket; o resíduo MIGROU para `Q20`) · 1 nova (`Q20`)**. **Nenhuma pergunta apagada em silêncio** |
| 2026-08-25T13:17Z | `plataforma-dados` | `/pm` (revisão **R2**) | `docs/context/plataforma-dados/handoff_to_architect.md` **atualizado no lugar** — bloco de abertura com R2 em 5 linhas · **§0.1 reescrito** (a dívida quitada, com a tabela `E-01..E-07`) · **§2.4 reescrito** (a Coinalyze medida **e ainda em quarentena, pelo termo nomeado**) · **A5** (Parquet/DuckDB com o falsificador nomeado: onde vive a lógica de unicidade num store de arquivos) e **A6** (`Q20`, e o **primitivo `swing_point`** que é decisão de hoje) novas · **§3.4** com o veredito de estrutura · **§9 novo: as CINCO correções em que este PM discordou do que lhe foi passado, com o argumento** · **§8: ciclo 3 de 3 — não sobra ciclo depois deste** |
| 2026-08-25T13:17Z | `plataforma-dados` | `/pm` (revisão **R2**) | `docs/INDEX.md` (estas linhas). **Ledger intocado: `PRD_DRAFT` antes, `PRD_DRAFT` depois** — nenhum `advance`, nenhum `approve`, nada criado, editado ou comentado no Jira. **Três medições próprias desta rodada, com o comando publicado no PRD:** `GET /fapi/v1/depth?symbol=BTCUSDT&limit=5` → **200, 295 B, peso 2** (`x-mbx-used-weight-1m` de 0 para 2) ⇒ **opção (c) de spread, ~110× mais barata que `bookTicker`** · `/futures/data/openInterestHist` → **200 com ZERO headers `x-mbx-*`** ⇒ **`avaliacao:A3` reproduzido, e dois dos três baldes são cegos, não um** · `curl -sI` em `data.binance.vision` (2026-08-18): `metrics` **11.531 B/dia**, `klines 1m` **58.262 B/dia** (ADAUSDT **46.746**), `aggTrades` **6.930.298 B** ⇒ **~87 GB para o histórico inteiro em bucket, uma vez**, o que **mata `Q9`** |
| 2026-08-25T14:52Z | `plataforma-dados` | owner + medição | `docs/premissas-de-infra-e-stack.md` **novo** — **fecha `Q2` nas DUAS metades** por declaração do owner (*"vai estar em uma VPS, a mesma que roda o anything_monorepo, então n temos tanto recurso assim"* · *"menor escopo possível de auth, um único user... extensível sem grande complicações"* · *"monorepo backend/ + frontend/, back modular em python com fastapi, bounded context e módulos, front com next"*). **Medição sobre o `anything_monorepo` no disco:** a VPS já roda **6 serviços** (`postgres:15`, `redis:7`, `evolution`, `backend`, `frontend`, `caddy` com TLS público) e está **sob pressão de disco documentada** (runbook `KAN-86` move mídia p/ **Cloudflare R2** com passo *"liberar disco"*, `ADR-006` ratificado) · backend **Poetry, py `^3.12`, FastAPI `^0.139`**, `src/modules/<ctx>/{domain,use_cases,infra,features}` com **`import-linter` executável** (contrato `layers` por módulo + contrato `forbidden` *"business logic imports no other module"* por módulo) e **piso de cobertura POR CAMADA** · frontend **Next 16.2, React 19, TS 6, Tailwind 4, react-query 5, zod 4, vitest 4**, `src/{app,features,components}`. **CORRIGE uma premissa do owner:** *"banco relacional está fora de contexto"* é **falso** — os três candidatos são relacionais e SQL (TimescaleDB **é** extensão do Postgres; DuckDB é SQL embarcado); o eixo é **row-OLTP × column-OLAP** e **daemon × embarcado**. **ACRESCENTA 2 candidatos ao ADR de motor:** **(4) TimescaleDB na instância `postgres:15` já de pé** (sem daemon novo, ao custo de acoplar a produção alheia) e **(5) Parquet no R2 já provisionado + DuckDB `httpfs`** (tira os ~87 GB do disco da VPS; **egress zero** é o que torna varredura de backtest viável). **NÃO cria Epic:** auth mínima com extensibilidade não realiza a condição de PRD §13.5 ⇒ **continuam 7**. **Resíduo `[NÃO MEDIDO]`: RAM, disco e REGIÃO da VPS** — os dois primeiros são teto no falsificador, a terceira é **`observer_region`, coluna de F0, impossível retroativamente** (`[GAP G7]`) |
| 2026-08-25T15:20Z | `plataforma-dados` | `/architect` | **`docs/specs/SPEC-001-plataforma-dados.md` (`DRAFT`)** + **9 ADRs** (`docs/adr/ADR-001..009`) + **plano em 9 fases** (`docs/plans/SPEC-001-plataforma-dados/`). **PRD já validado** (`approve prd` 2026-08-25T13:40:41Z) — **Gap Analysis NÃO refeito**; esta é a rodada de SPEC. **A pré-condição bloqueante do gate está RESOLVIDA COMO IDENTIDADE: `quantity_field ∈ {q, nq}` é termo da `SeriesKey`** (`ADR-001`), com **cinco medições novas desta rodada**, comando publicado, sobre `data/binance/rest/nq_*.json` e `data/binance/aggtrades/`: `nq > q` em **0/1000** (déficit **unidirecional**) · **4 de 16** divergentes têm `nq == 0` com `q > 0` (aggTrade **inteiramente RPI**) · `q` mediano dos divergentes **63.776 contra 6.945** (**9,2×** ⇒ a divergência vive na **cauda de tamanho**, que é a primitiva de absorção por tamanho de trade) · **15 de 16 com `m = false`** (um lado só) · e o que decide: **`cvd_delta(q) − cvd_delta(nq) = 243.197 = 6,01% de \|cvd_delta(q)\|`, contra déficit de volume de 243.317 ⇒ 99,95% do déficit cai no CVD, não se cancela entre os lados** — e `cvd_cum` é soma acumulada, logo o erro **cresce sem limite dentro da janela da âncora**. **Fato capture-or-lose NOVO, `CL-5`:** o dump S3 tem **7 colunas e nenhum `nq`**, o REST devolve **48 h** ⇒ `nq` não volta, e `E-07` o levou junto ao retirar `aggTrade` cru da captura **sem notar**. Resolução que **não** reabre captura de tick: **agregado por bucket de 1 min** (`Σq_buy·Σq_sell·Σnq_buy·Σnq_sell·tx·btx`), ordem de **10² B/bucket** e **zero chamada nova**. **Três correções que o gate mandou carregar, entregues:** `ADR-003` (fronteira `charts` ⇄ `web` **por contrato de dado, não por caminho** — hoje ela é *indeterminável por caminho* e nenhum dos dois tem dono em `[agents.by_component]`) · `ADR-004` (**reconexão POR CLASSE de stream** — `aggTrade` tem `agg_id` e o dump repõe; `!forceOrder@arr` **não tem nada e o buraco não volta** ⇒ sobreposição obrigatória, chave natural declarada, **taxa de colisão publicada e direção do viés escrita**; é **gate de F0**) · e o **DoD falsificável da query nomeada compartilhada** (`ADR-008`/DoD-3: **um `verdict` inédito ⇒ os dois consumidores mudam juntos ou os dois reprovam**, que é o único teste que expõe duplicação silenciosa). **`ADR-002` absorve `premissas-de-infra-e-stack.md`: são CINCO candidatos e a restrição que decide é VIZINHANÇA, não performance.** Decide o que pode: **store PARTIDO** — catálogo/registro/instrumento no **`postgres:15` que já está de pé** (OLTP, zero container novo), série em **store colunar**; **elimina ClickHouse como 7º container** (vizinhança + dedupe *"only during a merge, at an unknown time"*, e **unicidade eventual não serve para soma acumulada**); **defere o finalista (TimescaleDB na instância existente × Parquet no R2 + DuckDB) a spike com 5 critérios declarados ANTES**. E **responde onde vive a lógica de ler-antes-de-escrever: no ESCRITOR ÚNICO da aplicação** — com a discordância registrada de que *"escritor multiprocesso"* e *"compactação muda o hash"* **não são falsificadores do Parquet, são requisitos dos cinco**; o que é específico dele é **latência de rede** e **reescrever partição**, fechado por `compaction_epoch` (`D6`). **E derruba um dos quatro argumentos que o direcionamento usou:** *"DuckDB tem `ASOF JOIN` nativo"* é verdade e **não compra o que a plataforma precisa** — `as_of` é `argmin(observed_at)` sob `available_at <= t`, **redução bitemporal por janela**, não `ASOF JOIN`. **Defeito de citação corrigido:** PRD §8, linha de `Q17`, apresenta a escolha como **binária**; R2 mediu uma **opção (c)** (`/fapi/v1/depth?limit=5` → **200, 295 B, peso 2**, **1,67% do balde**, **~3,1 GB/ano**, **~110× mais barata que (a)**) — ela **não mata `CL-4`, encolhe-o ~110×**, e o resíduo do owner é **(b) ou (c)**. **Reconciliação de contagem que o PRD não fez:** §13.5 diz *"SEIS Epics, com um sétimo contingente a `Q2` = exposto"*; `Q2` foi respondida como **exposto com auth mínima** e essa condição **NÃO se realiza** — **o sétimo Epic existente (`CST-1`/`CST-7`) é a PARTIÇÃO DE F5 em F5a/F5b**, exatamente como §13.2 e §15 argumentaram e §13.5 não contou. **Quem ler §13.5 hoje conclui que o sétimo é auth. Não é.** **Lacuna nomeada:** `Q2` matou o non-goal *"login: não construir especulativamente"* e **nenhum dos 7 Epics carrega auth** ⇒ a **dimensão de identidade** é contrato de F1 e o **mecanismo mínimo** é item da fase `05`, **acrescentando itens a `CST-3`, não um Epic novo**. **Regras de borda novas, medidas e antes ausentes:** `ZL-1..ZL-3` — a Coinalyze devolve **361 buckets com `s = 0` literal** onde o `daily` reporta 289,65/154,53/4.547,61 BTC ⇒ o teto de retenção é **POR LADO** e o merge preenche o lado faltante com **zero, não nulo**: **ausência devolvida como zero legítimo pelo fornecedor**, e o ingestor converte zero-antes-do-primeiro-não-zero-daquele-lado em **`SEM_FONTE`**; e **`200` com corpo TRUNCADO** (`monthly/bookTicker` 2024-04: 200 com **37,7 MB** contra 6,7 GB) é modo de falha **pior que 404** porque não levanta exceção ⇒ `.CHECKSUM` obrigatório e `curl -sI` mensal em prefixo antigo **e** recente. **Plano: 9 fases sobre 7 Epics, divergência DECLARADA** — `CST-2` partido em `02`/`03` pela **fronteira do gate por coletor** (snapshot e one-shot **não precisam de `Q2`**) e `CST-3` em `04`/`05` (contrato **offline** × primeira superfície **exposta**). **Nenhum Epic novo, nenhuma fronteira de valor movida, nada criado, editado ou comentado no tracker.** **Recusei decidir:** as 14 perguntas abertas (cada uma com o ponto de acoplamento declarado em SPEC §9 e desenhada para que a resposta seja **parâmetro, não reescrita**), o **finalista do motor** (faltam `free -m`, `df -h` e a região da VPS, que é **`observer_region`, coluna de F0, impossível retroativamente**), o **componente `infra`** (proposto com falsificador; alterar vocabulário fechado em política versionada é do owner) e **`Q20`**. **Ledger: `PRD_VALIDATED` → `advance SPEC_DRAFT` DEPOIS de a SPEC existir. A SPEC nasce `DRAFT`; o gate `spec` é do OWNER e nenhuma linha deste conjunto declara aprovação.** |
| 2026-08-25T17:44Z | `plataforma-dados` | documentação de arquitetura | `docs/arquitetura-fluxos.md` **novo** + `README.md` **novo** (o repositório não tinha nenhum) — **os quatro fluxos que o owner pediu, em 16 diagramas mermaid**, todos derivados de SPEC/ADR/plano e **nenhum de desenho novo** (tabela de procedência em §7): visão geral com o **store partido** · a estrutura de dados (`SeriesKey` de 15 termos · as 7 colunas de procedência · as invariantes de append-only e `as_of` bitemporal · o predicado de quarentena de 3 termos) · **ingestão** (coletores partidos pelo gate por coletor · fila durável → escritor único com as 4 regras de ler-antes-de-escrever · o que se decide por linha) · **estratégia e convergência marcada como NON-GOAL, com a porta e não o mecanismo** · **gráfico** (as 2 rotas de transporte por classe de tempo · o içamento em 3 níveis · o selo de 4 campos · a fronteira `charts` ⇄ `web` decidível por inspeção · reprodutibilidade) · **alertas: NÃO EXISTEM, `Q3` aberta** — o detector já está fixado (contiguidade de `agg_id` + heartbeat, **nunca taxa**, porque a vazão do mesmo símbolo variou **3,66×** entre dois dias) e **só falta o transporte da notificação**. **Verificação: os 16 blocos parseiam sob mermaid 11.16.0** (`mermaid.parse` com DOM via jsdom). **Medição própria deste artefato, e ela confirma `CL-5` de forma independente:** `head -1` no dump de `aggTrades` → **7 colunas, `nq` não é uma delas**; `GET /fapi/v1/aggTrades` em T-48h → **200 com `nq`**, em T-49h → **400 `-4166` "Search window is restricted to recent 2 days only"** ⇒ **`nq` existe numa janela deslizante de exatamente 48 h e em nenhum histórico**. **Ledger intocado: `SPEC_DRAFT` antes, `SPEC_DRAFT` depois** |
