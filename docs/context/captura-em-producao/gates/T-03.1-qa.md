# QA — `T-03.1` (`backend/Dockerfile` + `.dockerignore`), fase `03`, `captura-em-producao`

**Papel:** `/qa` · **Data:** 2026-09-08 · **Worktree:** `wf_a7375708-4bc-1` (`tasks/T-03.1-backend-dockerfile`, `c6e4c87`, base `origin/master@164bc1c` — merge-base confere, sem divergência).

## Escopo lido
`docs/context/captura-em-producao/tasks_review.md` §3c linha `T-03.1`; `ADR-032/D2`, `/F5`; plano
`03_compose_em_dois_alvos.md` (`D3.2`, `D3.6`, `D3.8`); diff `origin/master...HEAD` (3 arquivos:
`backend/Dockerfile`, `backend/.dockerignore`, `docs/INDEX.md`).

## Reprodução independente (não confiei no relato do commit — refiz)

| checagem | comando | resultado |
|---|---|---|
| build limpo | `docker build -f backend/Dockerfile backend` | `rc=0` |
| tamanho | `docker image ls` | **239 MB** (≤ 400 MB, `ADR-032/F5`) |
| zero `.env*` na imagem | `find / -name ".env*" ...` dentro do container | **0** |
| zero `.env*` (variante do ADR) | `ls -a /app \| grep -c "^\.env"` | **0** |
| sem `tests/`/`.venv`/`data/` | `ls /app/{tests,.venv,data}` | inexistentes (3×) |
| deps de produção instaladas | `python -c "import fastapi, uvicorn, psycopg, pydantic"` | OK |
| deps de dev excluídas | `python -c "import pytest"` | `ModuleNotFoundError` (esperado — `--only main`) |
| sem `ENTRYPOINT` lógico | `docker inspect .Config.Entrypoint/.Cmd` | `[] \| [python3]` (default da base, não lógica de papel) |
| zero segredo literal (`D3.8`) | `harness rules --mode file --path backend/Dockerfile` e `--path backend/.dockerignore` | vazio, `rc=0` (ambos) |
| sweep completo do diff | `harness rules --mode sweep --changed-only` | vazio, `rc=0` |
| commit sem co-autoria | `git show -s --format=%B HEAD \| grep -i co-authored` | `rc=1` (ausente) |
| autor/committer = owner | `git show -s --format='%an <%ae>%n%cn <%ce>'` | `Stharley Maxwell <stharleymax@gmail.com>` (2×) |
| `docs/INDEX.md` append-only | `git diff --stat` | só `+1` linha, nenhuma reescrita |

## Mutação — falsificador provado, não só citado

Reproduzi o próprio morde do plano (`D3.6`/`D3.8`: `.dockerignore` sem `.env*` ⇒ ≥ 1):

1. Removida a linha `.env*` de `backend/.dockerignore`.
2. Plantado `backend/.env` com conteúdo fake.
3. `docker build --no-cache -f backend/Dockerfile backend` → `rc=0` (build não reprova sozinho — coerente, é `.dockerignore`, não é um portão de CI).
4. `find / -name ".env*" ...` dentro da imagem mutante: **1** (`/app/.env`) — subiu de 0 para 1, exatamente o falsificador declarado.
5. `.dockerignore` restaurado (`diff` contra o original: idêntico), `.env` plantado removido, imagens de teste (`t031-backend:test/mutant/check/final`) removidas. `git status --porcelain` limpo antes e depois — nenhum resíduo no worktree.

O teste morde: um mutante que quebra a garantia (`.env*` fora do `.dockerignore`) faz o comando de
verificação (`find`) mudar de veredito. Não é teste que sempre passa.

## Regras bloqueantes (`harness rules list --severity block`, 8 no total)

Aplicáveis a este diff (arquivos não-Python, sem compose): `core.hardcoded-secret` e
`own.compose-hardcoded-secret` — cobertas por `harness rules --mode file`/`sweep`, ambas `rc=0`. As
demais (`relative-import`, `silent-except`, `print-statement`, `browser-imports-server`,
`tenant-from-request`, `server-test-directory-present`) não têm superfície neste diff (nenhum `.py`
tocado) — confirmado que `backend/tests/` continua presente (regressão da regra de ausência de suíte
não se aplica: ela cobra ausência de diretório, e ele está lá).

## Fora de escopo, corretamente

Nenhum toque em `frontend/`, `deploy/`, `tasks.toml` ou ledger — confere com `T-03.1` sendo só
`backend/Dockerfile`+`.dockerignore`, e `T-03.2`/`T-03.4` como as tasks vizinhas que tocam o resto.

## Veredito

**APPROVED.** DoD `D3.2` (metade backend), `D3.6` (metade backend), `D3.8` e `ADR-032/F5` (≤ 400 MB)
verificados por reprodução independente + mutação provada. Nenhuma regra bloqueante violada. Commit
respeita `CLAUDE.md` (sem co-autoria, autor/committer owner, `docs/INDEX.md` append-only).

**Não gravo `gate-record`** — esta é validação de **1 de 8 tasks** da fase `03` (`T-03.2..8` seguem
abertas); `gate-record` é por fase, não por task (mesmo padrão já observado nas irmãs). Fase `03`
segue aberta para as próximas rodadas.
