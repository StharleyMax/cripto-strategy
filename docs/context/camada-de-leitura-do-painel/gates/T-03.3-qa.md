# Gate — QA — `T-03.3` (`CST-134`)

Feature: camada-de-leitura-do-painel

**Feature:** `camada-de-leitura-do-painel` · **Fase:** `03` · **Task:** `T-03.3` —
`parseCatalogEnvelope` estrito e `S3` exibe as 10 linhas vindas da API — `FIXTURE_CATALOG_ROWS`
fora do grafo de produção. **Componente:** `web`. **Branch:**
`tasks/T-03.3-s3-exibe-catalogo` (checkout local `qa-T-03.3-tmp`, mesmo commit `1caaa4c`).

## 1. O que foi verificado (execução real, não releitura do build report)

Setup: `df -h /` → 4,4G livres antes da cópia (>3G, prosseguiu); `node_modules`/`.venv` copiados
de `cripto-strategy` (não reinstalados — `diff` de `package-lock.json` e `pyproject.toml` contra o
checkout principal → 0 linhas nos dois). Ao fim, `df -h /` → 2,8G livres.

1. **Testes rodados de verdade, não só citados:**
   - `npm --prefix frontend run test:s3` → **84/84 passed** (bate com o build report).
   - `npm --prefix frontend run test:s1` → **104/104 passed**, sem regressão.
   - `npm --prefix frontend run test:app` → **97/98**, a falha é `universe-at.test.ts`
     (`ENOENT data/snapshots/...`, `data/` fora do versionamento — pré-existente, confirmado pelo
     próprio `ENOENT`, não pelo comentário do build report).
   - `npm --prefix frontend run test:charts` → 1 falha pré-existente, mesma causa (`data/` ausente).
   - `npm run lint` / `npm run typecheck` (`tsc --noEmit --strict`) → limpos.
2. **Mutação/falsificador aplicado por mim** (não só o falsificador que o próprio código já
   embute): removi `["labelShift", "number"]` de `KEY_FIELD_KINDS`
   (`series-catalog-query.ts`) — simulando uma regressão que silenciosamente para de validar um
   campo. Resultado: `test:s3` caiu de 84 para **82 passed / 2 failed**, exatamente os 2 testes
   que deveriam morder (`entries[0].key sem "labelShift"` e `"labelShift" não-número`). Arquivo
   restaurado byte a byte (`diff` → vazio) e suíte voltou a 84/84. Prova que o par MORDE/CALA é
   real, não decorativo.
3. **`harness rules --mode sweep --changed-only`**: `rc=0`, saída vazia — mas essa saída vazia é
   **ambígua** por construção (`_changed_paths` usa `git diff --name-only HEAD`, e o working tree
   está limpo/committed, então "changed" é vazio). Rodei `--mode file` explicitamente sobre os 12
   arquivos do diff `origin/master...HEAD` → também vazio/`rc=0`. Para descartar "instrumento
   nunca capaz de morder", criei um arquivo-controle com `import ... from "../../backend/..."` sob
   `frontend/src/features/s3-inspector/` e rodei a mesma regra: **disparou**
   (`web-fullstack.browser-imports-server`, `rc=2`), confirmando que a ausência de achado no diff
   real é um verdadeiro negativo. Arquivo de controle removido, `git status` limpo depois.
4. **`make verify`** (após `export HARNESS_MECHANISM=.../harness-plugin/0.13.0/bin/harness` — o
   mecanismo não resolvia sozinho neste worktree, mesmo gap ambiental já registrado em tasks
   anteriores): **VERDE, 6/6** — `lint-backend` rc=0 (345 arquivos), `lint-frontend` rc=0,
   `test` rc=0 (1749 passed, cobertura 97,91%), `boundaries` rc=0 (7 kept/0 broken), `regras` rc=0
   (0 bloqueio, 48 aviso), `política` rc=0.
5. **`make e2e` (API de pé)**, rodado por mim do zero: **19/19 passed**,
   `catalog_rows_data_fact="catalog_rows:10"`, `axe_violations=[]` (`axe_passes_count=22`).
   Igual ao build report.
6. **`make e2e E2E_API_UP=0`** (falsificador do DoD "no chão"), rodado por mim: **16/19 passed, 3
   failed** — exatamente `B1`, `B2` e `04-interacoes` (`catalog_rows_before_filter=0`, esperado
   `10`), com `D1.5b-porta-sem-listener_catalog_rows="catalog_rows:0"` e
   `error_kind:connection_refused` confirmados nos 2 casos de `02-rede-e-estados`. Nenhum
   `FIXTURE_CATALOG_ROWS` visível.
7. **Leitura de código, não só teste**: `series-catalog-query.ts` (parser estrito dos 21 campos —
   6 de `SeriesCatalogEntry` + 15 de `SeriesKey`, `ADR-019/D2`); `assertNoTickLevelFields` chamado
   no corpo antes do parse (`ADR-005/D1` — envelope de bucket, nunca tick); nenhum `fetch` a
   endpoint de exchange; `Completeness` hardcoded `{kind:"unmeasured"}`, nunca inventado.
   `view-model.ts`: `fullSeriesKeyId` agora junta os 15 termos de `SeriesKey` (corrigindo colisão
   real medida ao vivo, `q`/`nq` de `cvd_source`); teste de regressão presente
   (`view-model.test.ts`). `S3Inspector.tsx`: `INTEGRITY_INK_CLASS` trocado de `text-[#e0aaff]`
   hardcoded para `text-integrity-ink` — token já existe em `globals.css:21,77`
   (`--color-integrity-ink`, dark/light) e já está medido/aprovado em `DESIGN_SYSTEM.md` §1.4-bis;
   nenhuma cor nova, confirmado por grep no CSS.
8. **`grep -rn 'fixtures.ts' frontend/src/app frontend/src/features --include='*.tsx' | grep -v
   '\.test\.' | wc -l`** → `0`, rodado por mim.
9. **Doc delta**: `docs/INDEX.md` — linha nova é a ÚLTIMA do arquivo (append-only respeitado, não
   reescreve nenhuma anterior — conferido por leitura das últimas 5 linhas). `docs/
   arquitetura-fluxos.md` já cita o bounded context `catalog`/`series_catalog` — nenhum padrão
   novo de rota/contrato que precise de atualização. `STITCH_CONTEXT.md`/`DESIGN_SYSTEM.md`: badge
   `QUARENTENA`/`integrity-ink` já documentado extensivamente (linhas 437/460/732/971 em
   `DESIGN_SYSTEM.md`; múltiplas em `STITCH_CONTEXT.md`) — reuso confirmado, não introdução.
   Nenhuma ADR necessária (nenhuma decisão estrutural nova).

## 2. DoD (`T-03.3` + plano `03` `D3.1`/`D3.6`) — item a item

- [OK] Teste TS com 1 campo removido por vez ⇒ parser reprova — universo 21 (6+15), verificado
  sem duplicata pelo próprio teste, e por mim via mutação de controle (achado 3 acima).
- [OK] `FIXTURE_CATALOG_ROWS`/`fixtures.ts` fora do grafo de produção — grep → 0.
- [OK] e2e `S3 catalog_rows = 10` — `make e2e` real rodado por mim.
- [OK] DoD no chão: `error_kind:connection_refused` e `catalog_rows = 0` — `make e2e
      E2E_API_UP=0` real rodado por mim.

## 3. Achados

Nenhum `BLOCKER` novo. Os dois achados do build report (`seriesKeyId` colidente,
`INTEGRITY_INK_CLASS` hardcoded) foram lidos no código e confirmados corrigidos, com teste de
regressão para o primeiro e verificação de token/contraste para o segundo.

`WARNING` (não bloqueia): `harness rules --mode sweep --changed-only` depende de working tree
sujo para enxergar algo — sobre um branch já committed, ele sempre sai vazio por construção, não
porque o diff da task foi varrido. Recomendo ao coordenador registrar isso como lacuna conhecida do
comando (não desta task): quem quiser saber se o diff de uma PR já mergeada tem bloqueio precisa
rodar `--mode file --path <arquivos do diff>`, não `--changed-only`.

## 4. Veredito

**APPROVED.** DoD fechado item a item com comando próprio; `make verify` verde (6/6); `make e2e`
verde de pé (19/19) e o falsificador confirmado no chão (16/19, quedas exatamente as esperadas);
mutação de controle no parser prova que o par morde/cala é genuíno; doc delta correto
(`docs/INDEX.md` só acrescido, nenhum padrão novo sem doc).
