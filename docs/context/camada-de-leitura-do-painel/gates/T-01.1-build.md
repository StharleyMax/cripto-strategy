# QA Gate Context Block — Fase 01: T-01.1 (transporte server-only), rodada 2

```
[QA GATE — Fase 01: transporte server-only com erro tipado]
Feature: camada-de-leitura-do-painel     Componente: web
Arquivos alterados:
- frontend/src/features/s1-console/ingest-health-query.test.ts (modified)
Spec: docs/specs/SPEC-003-camada-de-leitura-do-painel.md §3.2
Plan: docs/plans/SPEC-003-camada-de-leitura-do-painel/01_pagina_diz_a_verdade.md item 1.2
DoD:
- [x] `grep -c 'import "server-only"' frontend/src/features/s1-console/ingest-health-query.ts` -> 1
- [x] `npm --prefix frontend run test:s1` -> 60/60 (universo: `ls frontend/src/features/s1-console/*.test.ts` = 6
      arquivos; `ingest-health-query.test.ts` sobe de 13 para 19 testes, `ingest-health-query-http.test.ts` 8/8)
- [x] `npm --prefix frontend run lint` e `npm --prefix frontend run typecheck` limpos
- [x] `harness rules --mode sweep --changed-only` e `--mode file --path .../ingest-health-query.test.ts` -> rc=0
- [x] Mensagens de `TransportError` em inglês (herdadas da rodada 1, não tocadas nesta rodada)
Comandos rodados (literais) e resultado:
- `npm --prefix frontend run lint` -> rc=0 (universo: `eslint src`)
- `npm --prefix frontend run typecheck` -> rc=0 (`tsc -p tsconfig.json --noEmit --strict`)
- `npm --prefix frontend run test:s1` -> tests 60, pass 60, fail 0 (universo: 6 arquivos `*.test.ts`)
- `harness rules --mode sweep --changed-only` -> rc=0
- `harness rules --mode file --path frontend/src/features/s1-console/ingest-health-query.test.ts` -> rc=0
Cobertura (falsificador, não %): as 2 mutações que o QA da rodada 1 registrou como não pegas foram
reaplicadas manualmente e revertidas depois de confirmar reprovação:
  1. `"connection_refused"` -> `"malformed_envelope"` no `catch` do `fetch` em
     `fetchIngestHealthProjectionViaHttp` -> `MORDE TransportErrorKind=connection_refused` reprova
     (1 fail, o resto continua verde). Revertido, `git diff --stat` sobre o arquivo de produção fica vazio.
  2. `etag: null` -> `etag: "should-not-matter"` no retorno de sucesso -> `CALA: ... etag e SEMPRE null`
     reprova (1 fail). Revertido, mesma checagem de `git diff --stat` vazia.
Doc delta:
- SPEC-003 §3.2: sem mudança — o contrato (4 `TransportErrorKind`, `etag: null` em F1) já estava
  especificado; esta rodada só fecha a lacuna de teste que faltava, não muda comportamento nem contrato.
- docs/INDEX.md: linha acrescentada, apontando para este gate block.
- ADR: não necessário — nenhuma decisão nova de arquitetura, só cobertura.
Bloqueado (se houver): nenhum.
```

## O que mudou desde a rodada 1 (QA `T-01.1-qa.md`, veredito `NEEDS_FIX`)

Rebase: o branch original (`9afa67b`) tinha `ca727fb` como base; `origin/master` já tinha avançado para
`55e99ad` (`T-01.7`, sem conflito com este arquivo). Recomeçado a partir de `origin/master` atual
(cherry-pick limpo de `9afa67b`, sem alterar o código de produção da rodada 1).

`ingest-health-query.test.ts` ganhou 6 testes novos (13 → 19), todos usando `fetchImpl` mockado
(`typeof fetch`, sem `any`) em vez do servidor `uvicorn` real que `ingest-health-query-http.test.ts` já
cobre — mais barato e mais direto para isolar cada `TransportErrorKind`:

- `MORDE TransportErrorKind=missing_base_url` (sem `baseUrl`, `INGEST_HEALTH_API_BASE_URL` removida do
  `process.env` só durante o teste, restaurada no `finally`)
- `MORDE TransportErrorKind=connection_refused` (`fetchImpl` que rejeita)
- `MORDE TransportErrorKind=non_2xx` (resposta `500`, `status` preservado no erro)
- `MORDE TransportErrorKind=malformed_envelope`, dois casos: corpo não-JSON e JSON válido mas fora do
  schema (`parseIngestHealthEnvelope` reprova) — o segundo prova que os dois caminhos que produzem o
  mesmo `kind` continuam distintos na causa, sem colapsar num só
- `CALA`: envelope bem formado via `fetchImpl` mockado -> `result.etag === null`

Achado #2 da rodada 1 (builder não publicou QA Gate Context Block) fecha com este arquivo.
