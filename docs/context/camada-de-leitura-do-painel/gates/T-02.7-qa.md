# QA Gate — Fase 02 [infra] — T-02.7

Branch `tasks/T-02.7-caddyfile-um-host`, commit `ad87e2c` (já rebaseado em `origin/master`, já
pushado). Worktree `wf_0cfc5e74-c0d-23`. Diff do commit: só `deploy/Caddyfile` (45 linhas,
arquivo novo) — `git diff origin/master...ad87e2c --name-only`.

## O que existia e o que faltava

`deploy/Caddyfile` (site único `{$PUBLIC_HOST}`, `{$API_PREFIX}/*` → `api`, resto → `web`,
`basic_auth` inerte por `@authEnabled expression {env.PANEL_BASIC_AUTH_HASH} != ""`, TLS deixado
em comentário) bate com `SPEC-003` §3.5 e `ADR-029/D1,D2,D6` — confirmado por leitura e por
`caddy validate`/`caddy adapt` reais (abaixo).

**Faltava o teste que o próprio DoD de `T-02.7` exige por nome** (`tasks.toml:337`, plano `02`
item 2.9/`D2.7`, narrativa `tasks_review.md:82`): *"teste: API_PREFIX=/x => openapi.json paths
/x/\* E Caddyfile renderizado roteia /x/\* (um mudar sem o outro => duas constantes)"*.
`backend/tests/api/test_api_prefix.py` (existente, de `T-02.2`) só prova o lado `openapi.json`;
nada na árvore rodava `caddy adapt` sobre `deploy/Caddyfile` e comparava contra
`_api_prefix_from_environment()`. O commit `ad87e2c` só adicionou o `Caddyfile`, nenhum teste.

## Prova, antes de escrever o teste (comandos, não opinião)

```
grep -c reverse_proxy deploy/Caddyfile                                    -> 2
docker run --rm -v $PWD/deploy/Caddyfile:/etc/caddy/Caddyfile caddy:2-alpine \
  caddy validate --config /etc/caddy/Caddyfile                            -> "Valid configuration", rc=0
docker run --rm -v $PWD/deploy/Caddyfile:/etc/caddy/Caddyfile caddy:2-alpine \
  caddy adapt --config ... --adapter caddyfile   (sem env)                -> path ["/api/v1/*"]
docker run --rm -e API_PREFIX=/x ... caddy adapt                          -> path ["/x/*"]
grep -cE '^  (api|web|caddy):' deploy/compose.yml                         -> 3
grep -A12 '^  api:' deploy/compose.yml | grep -c 'ports:'                 -> 0
grep -rn 'CORSMiddleware' backend/src --include='*.py' | wc -l            -> 0
grep -rn 'API_PREFIX' backend/src/main deploy 2>/dev/null | wc -l         -> 9 (>= 3 exigido)
harness rules --mode file --path deploy/Caddyfile                        -> vazio, rc=0
```

Confirma que a implementação em si está correta — o defeito era a AUSÊNCIA da prova
automatizada, não o `Caddyfile`.

## Teste escrito (só teste, nenhuma linha de produção tocada)

`backend/tests/api/test_api_prefix_moves_caddyfile_and_openapi_together.py` (novo, 3 testes):

1. `test_default_prefix_agrees_between_caddyfile_and_openapi` — sem `API_PREFIX`, os dois lados
   concordam em `/api/v1`.
2. `test_api_prefix_moves_caddyfile_and_openapi_together` — `API_PREFIX=/x` move
   `_api_prefix_from_environment()` **e** o `Caddyfile` renderizado via `caddy adapt`
   (dockerizado) para `/x`, comparados na MESMA asserção.
3. **Controle negativo (mutação), exigido pela filosofia do gate:** um `Caddyfile` mutante
   embutido no teste (nunca toca o arquivo real) que hardcoda `/api/v1` ignorando
   `API_PREFIX` — a asserção reprovaria se comparasse "um número consigo mesmo"
   (`ADR-005/D6`); rodei e confirmei que a asserção MORDE: `caddy_prefix == "/api/v1"` e
   `caddy_prefix != "/x"` quando `API_PREFIX=/x`, provando que a comparação dos dois testes
   acima não é vácua.

`caddy adapt`/`validate` via `docker run --rm ... caddy:2-alpine`, nunca binário local — mesmo
idioma do DoD e do `deploy/Caddyfile` (nunca implantado daqui). `pytest.mark.skipif` quando
`docker` ausente do `PATH` (anomalia declarada, não `FAIL` de regra).

Achados de lint/mypy no MEU PRÓPRIO teste, corrigidos antes deste veredito: `E501` (linha 104
chars) e `no-any-return` em `mypy` — ambos achados pelo portão real, não por inspeção manual.

## Comandos rodados

```
cd backend && .venv/bin/python -m pytest tests/api -v --no-cov
  -> 23 passed in 5.39s (20 pré-existentes + 3 novos)

cd backend && bash scripts/lint.sh
  -> "All checks passed!" / "336 files already formatted" / "Success: no issues found in 336 source files"

cd backend && bash scripts/test.sh   (suíte completa, backend/scripts/test.sh = test_cmd.sentimento.test)
  -> 1713 passed, 3 warnings in 381.62s
  -> Coverage XML: TOTAL 6809 stmts, 98% (Required 70.0% reached: 97.86%)
  -> domain 99.8% (meta 90%) · use_cases 100.0% (meta 80%) · infra 95.2% (meta 70%) — 3/3 camadas OK
```

## Regras bloqueantes (8 declaradas por `harness rules list --severity block`)

| id | veredito | evidência |
|---|---|---|
| core.relative-import | OK | `harness rules --mode sweep --changed-only` (diff `deploy/Caddyfile` + meu teste novo) → rc=0, vazio |
| core.silent-except | OK | idem — nenhum `except` no diff |
| core.print-statement | OK | idem — nenhum `print` |
| core.hardcoded-secret | OK | `harness rules --mode file --path deploy/Caddyfile` → rc=0, vazio; `Caddyfile` só tem `{$VAR}`/`{env.VAR}`, nenhum literal |
| web-fullstack.browser-imports-server | N/A | diff é `deploy/`+`backend/tests/`, nenhum código de browser |
| web-fullstack.tenant-from-request | N/A | nenhum handler de request no diff |
| web-fullstack.server-test-directory-present | OK | `backend/tests/` existe, com o novo arquivo dentro |
| own.compose-hardcoded-secret | OK | escopo é `deploy/compose.yml`; não tocado nesta task; `deploy/Caddyfile` usa só `${VAR}`/`{env.VAR}` |

`harness rules --mode sweep --changed-only` sobre o diff completo (`origin/master...HEAD`) →
`rc=0`, saída vazia — ambíguo por si só (doutrina deste repo); desambiguado acima, arquivo a
arquivo, e pelo próprio `harness rules --mode file` morder de verdade quando peço (já provado
por precedente em `T-02.5-qa.md`, mesmo mecanismo).

## DoD da fase (item 2.7 / 2.9, e D2.7/D2.8/D2.10 literais)

- [OK] `caddy validate --config deploy/Caddyfile` → rc=0, "Valid configuration"
- [OK] `grep -c reverse_proxy deploy/Caddyfile` → 2
- [OK] teste: `API_PREFIX=/x` move `openapi.json` **e** `Caddyfile` renderizado JUNTOS — escrito, roda, morde no controle negativo
- [OK] `basic_auth` condicionado a `{$PANEL_BASIC_AUTH_HASH}`, inerte sem valor (matcher `@authEnabled` lido do `docstring` do arquivo e confirmado no JSON adaptado: `expression` presente, `accounts` só existe dentro do `subroute` gated pelo matcher)
- [OK] TLS não decidido, `[Q10]` deixado em comentário (owner)
- [OK] `grep -rn 'CORSMiddleware' backend/src --include='*.py' | wc -l` → 0
- [OK] `grep -cE '^ (api|web|caddy):' deploy/compose.yml` → 3; `ports:` em `api` → 0 (D2.8, não é escopo desta task, mas não regrediu)
- [NÃO É ESTA TASK] `docker compose -f deploy/compose.yml config -q` → falha por `POSTGRES_DB` ausente sem `.env` — comportamento esperado (RN-9, nunca implantado daqui) e pertence a `T-02.6`/`T-02.8`, não a `T-02.7`

## Anomalia

Nenhuma. `docker` disponível no ambiente de QA; nenhuma checagem ficou sem veredito.

## Veredito: APPROVED

Justificativa: a implementação (`deploy/Caddyfile`) já batia com `SPEC-003`/`ADR-029` antes
deste gate — confirmado por `caddy validate`/`caddy adapt` reais. O DEFEITO real era a ausência
do teste que o próprio DoD de `T-02.7` nomeia (`API_PREFIX` move os dois lados JUNTOS); QA
escreveu esse teste (só teste, `git status --short` do worktree mostra 1 arquivo novo, nenhuma
produção tocada), rodou-o verde, e provou com um controle negativo embutido que a asserção
morde de verdade quando um lado diverge do outro — não é comparação vácua. Suíte completa
(1713 testes, `test.sh`) e `tests/api` (23 testes) verdes; lint e mypy verdes (depois de corrigir
2 achados nos MEUS próprios arquivos novos); cobertura 97.86% contra piso 70%, 3/3 camadas OK;
8/8 regras bloqueantes avaliadas, todas OK ou N/A por escopo.
