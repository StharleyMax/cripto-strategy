# Gate — `QA` — `T-03.7` (`CST-138`)

Feature: camada-de-leitura-do-painel

**Fase:** `03` · **Task:** `T-03.7` — Parser próprio do agregado (reprova campo ausente) e `S1`
passa a exibir o agregado por série, não o último run — `janela_de_perda` exibida como veio.
**Componente:** `web`. **Branch avaliada:** `origin/tasks/T-03.7-s1-exibe-agregado-por-serie`
(`e8cc276`, checkout local `T-03.7-qa-review13`, worktree próprio — a branch já estava aberta em
outro worktree). Build report: `gates/T-03.7-build.md`.

## 1. DoD (`tasks.toml` T-03.7 + plano 03 D3.3/D3.6/D3.7), item a item

- [OK] Teste TS com 1 campo removido por vez ⇒ parser reprova, universo = 15 (mesmo nº de
  `ADR-030` D5) — `collector-status-query.test.ts:142` faz o loop sobre `ADR_030_ROW_FIELDS`
  (declarado e verificado `assert.equal(ADR_030_ROW_FIELDS.length, 15)`). Confirmado por
  **mutação real** feita por mim (não só lido): substituí o `throw` de campo ausente por
  `continue` em `assertCollectorStatusRow` → `npm run test:s1` foi de **104/104** para **89
  pass / 15 fail**, exatamente as 15 linhas do loop MORDE — o falsificador de fato reprova.
  Arquivo restaurado ao estado do commit (`git status` limpo depois).
- [OK] e2e `S1` com dado real do agregado — `make e2e` (API de pé, seed real): **19/19 passed**,
  `rows_data_fact="rows:1"` via `/api/v1/collector-status`.
- [OK] No chão: `S1` mostra erro/vazio, nunca fixture/cache — `page.tsx:66-71` cai em
  `EMPTY_PROJECTION` só quando `TransportError`, nunca reaproveita linha anterior; `E2E_API_UP=0
  make e2e` → 17/19, `B1`/`B2` viram vermelho de propósito (`table_row_count=0`).
- [OK] `RN-4` (não recalcula `janela_de_perda`) — `grep -c janela_de_perda
  collector-status-query.ts` → `0`; o módulo nunca lê essa coluna (fórmulas de `ADR-030` operam
  só sobre `runs()`).
- [OK] `D3.6` fixture fora do grafo de produção não-teste — `grep -rn fixtures.ts
  frontend/src/app frontend/src/features --include='*.tsx' | grep -v '\.test\.' | wc -l` → **1**,
  mas o único hit é um comentário em `PainelClient.tsx` (não tocado por esta task, `d697bd6`,
  `T-01.6`) que **nomeia** a técnica de evitar o grep — falso positivo pré-existente do próprio
  falsificador de `D3.6`, não introduzido nem agravado por `T-03.7`. Registrado como achado
  cruzado, não bloqueia esta task.
- [OK] `D3.7` `make boundaries`/`make verify` verdes; `make e2e` de pé verde — ver §3.

## 2. Lógica fora do componente / contrato tipado na borda

`collector-status-query.ts` é o único ponto que fala HTTP; `assertCollectorStatusRow` valida as
15 colunas (`ADR-019/D2`: estrito em ausente/tipo errado, permissivo em extra) antes de qualquer
dado alcançar `view-model.ts`. Mapeamento para `CollectorRow` restrito aos 6 campos verbatim —
verificado por teste (`collector-status-query.test.ts`, bloco de mapeamento) e por `tsc --strict`
(estrutural, sem `as`/cast). `S1Console.tsx`/`view-model.ts`/`domain.ts` não foram tocados —
consomem o mesmo `CollectorRow` de sempre.

## 3. Comandos rodados (literais), universo e resultado

```
npm --prefix frontend run test:s1        # 104 passed, 0 failed (33 novos + 71 pré-existentes)
npm --prefix frontend run lint           # rc=0, limpo
npm --prefix frontend run typecheck      # tsc -p tsconfig.json --noEmit --strict, rc=0
npm --prefix frontend run test:app       # 97/98 — 1 falha PRÉ-EXISTENTE (universe-at.test.ts,
                                          #  fixture data/snapshots/2026-08-25_exchangeInfo.json
                                          #  ausente — data/ gitignored; confirmado ENOENT, não
                                          #  regressão desta task)
npm --prefix frontend run test:s3        # 37/37
npm --prefix frontend run test:charts    # 1 falha PRÉ-EXISTENTE (s2-panels.test.ts, charts
                                          #  intocado por esta task — diff de arquivos confere)
HARNESS_MECHANISM=.../bin/harness harness rules --mode sweep --changed-only
                                          # rc=0, saída vazia; universo = 9 arquivos
                                          # (git diff --name-only b566a68..HEAD)
HARNESS_MECHANISM=.../bin/harness make verify
  # [OK] lint-backend rc=0 341 arquivos · [OK] lint-frontend rc=0
  # [OK] test rc=0 1733 passed, cobertura 97,90% · [OK] boundaries rc=0 7 kept/0 broken
  # [OK] regras rc=0 0 bloqueio/46 aviso (nenhum dos 46 toca arquivo desta task — grep
  #      'collector-status-query' no log de regras -> 0)
  # [OK] política rc=0 · veredito: VERDE — 6 portões
make e2e                      # API de pé  -> 19/19 passed, rows_data_fact="rows:1"
E2E_API_UP=0 make e2e         # API no chão -> 17/19; B1/B2 vermelho — falsificador D1.11
```

**Mutação de controle negativo (feita pelo QA, não só lida do build report):** comentei o `throw`
de coluna ausente em `assertCollectorStatusRow` (`continue` em vez de lançar) → 15 dos 104 testes
reprovaram, exatamente as 15 linhas do loop `ADR_030_ROW_FIELDS`. Arquivo restaurado; `git status`
confirma zero diff residual.

## 4. Segredo no cliente / transporte

`import "server-only"` no topo; `INGEST_HEALTH_API_BASE_URL`/`API_PREFIX` só lidos via
`process.env`, nunca `NEXT_PUBLIC_*` (comentário próprio cita `ADR-019/D4`). Envelope
`ADR-005/D6.1` (bucket agregado, nunca tick) — `assertNoTickLevelFields(body)` chamado antes do
parse, reaproveitando o guard compartilhado de `history-transport.ts`. Nenhuma superfície nova
chama endpoint de exchange direto.

## 5. Doc delta

`docs/INDEX.md`: 1 linha ACRESCENTADA (`git diff b566a68..HEAD -- docs/INDEX.md` → só `+1` no
fim do arquivo, nenhuma linha existente tocada). `STITCH_CONTEXT.md`/`DESIGN_SYSTEM.md`: sem
mudança, correto — `S1Console.tsx` intocado, é troca de fonte de dado server-side, não de UI.
Nenhum padrão novo de componente/hook fora do documentado; nenhuma rota/contrato mudou fora do
que `ADR-030` já decide (dona da decisão, não desta task).

## 6. Achado cruzado (herdado do build report, não desta task — registrado, não escondido)

`ingest-health-query.ts` (fora do escopo de arquivo de `T-03.7`) ainda não lê `API_PREFIX` —
mesma lacuna que `collector-status-query.ts` tinha e corrigiu só em si mesmo. Não é regressão de
`T-03.7`; é candidata a task nova (fase `02`/`03`) para o coordenador/tech-lead, como o próprio
build report já sinaliza.

## Achados

1. [WARNING] `frontend/src/app/painel/PainelClient.tsx:26` menciona `fixtures.ts` literal num
   comentário, causando 1 falso positivo no falsificador de `D3.6` (`grep -rn fixtures.ts ...`).
   Pré-existente (`T-01.6`), não introduzido por `T-03.7` — não bloqueia esta task, mas caso o
   falsificador de `D3.6` seja cobrado ao pé da letra em fase futura, vale reescrever o
   comentário para descrever a técnica sem grafar o nome do arquivo (mesmo padrão que `page.tsx`
   já usa).
2. [WARNING] `ingest-health-query.ts` carrega a mesma lacuna de `API_PREFIX` que
   `collector-status-query.ts` corrigiu só em si — sinalizado pelo build report, ratificado aqui;
   dono é o coordenador/tech-lead (candidata a task nova).

Veredito: APPROVED

Relatório completo: docs/context/camada-de-leitura-do-painel/gates/T-03.7-qa.md
