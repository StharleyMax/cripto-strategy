# QA Gate Context Block — Fase 01: T-01.2 (portão ESLint por diretiva `"use client"`)

```
[QA GATE — Fase 01: Portao ESLint D5.17b reescrito por diretiva "use client"]
Feature: camada-de-leitura-do-painel     Componente: web
Arquivos alterados:
- frontend/eslint-rules/use-client-fingerprint-boundary.mjs (new)
- frontend/eslint.config.mjs (modified)
- frontend/src/features/s1-console/fingerprint-sync-boundary.test.ts (modified, REESCRITO — não apagado)
Spec: docs/specs/SPEC-003-camada-de-leitura-do-painel.md §3.2, §4
Plan: docs/plans/SPEC-003-camada-de-leitura-do-painel/01_pagina_diz_a_verdade.md item 1.3, DoD D1.2 (metade ESLint)
DoD:
- [x] `F-028-3` — probe sem `"use client"` com import de valor de `ingest-health-query.ts` -> `0 problems`
      (predecessora dava `1 error`); probe com a diretiva -> `1 error` nomeando `ADR-005/D6.4`
- [x] `F-028-1''` (transitivo) — probe `"use client"` -> `.ts` sem diretiva -> import de valor de
      `fingerprint` -> `npm run build` reprova nomeando `server-only`; `eslint` sobre os mesmos 2
      arquivos passa (a regra deste portão não vê o caso transitivo por desenho — quem julga é `next
      build`, `ADR-028` co-assinatura ponto 4)
- [x] `F-028-8` — `fingerprint-sync-boundary.test.ts` REESCRITO (probes MORDE ganham `"use client"`;
      entra um CONTROL sem diretiva com import de valor, que fica limpo — a forma `page.tsx`); nenhum
      teste apagado
- [x] `npm --prefix frontend run test:s1` verde
- [x] `npm --prefix frontend run lint` e `typecheck` limpos
- [x] `harness rules --mode sweep --changed-only` -> rc=0
- [x] `git log --diff-filter=D -- frontend/src/features/s1-console/fingerprint-sync-boundary.test.ts` vazio
Comandos rodados (literais) e resultado:
- `grep -c 'import "server-only"' frontend/src/features/s1-console/ingest-health-query.ts` -> `1`
- probe efêmero SEM diretiva + import de valor -> `npx --no-install eslint <probe>` -> `rc=0` (0 problems)
- probe efêmero COM `"use client"` + import de valor -> `rc=1`, 1 error, regra
  `local/use-client-fingerprint-boundary`, mensagem nomeia `ADR-005/D6.4` (texto completo no rule file)
- probe transitivo (`"use client"` -> `.ts` sem diretiva -> `fingerprint` por valor): `eslint` sobre os
  2 arquivos -> `rc=0`; o mesmo par, importado a partir de `page.tsx` (mutação efêmera, revertida
  depois) + `npm run build` -> `rc≠0`, Turbopack nomeia `"You're importing a module that depends on
  'server-only'"` e `"'server-only' cannot be imported from a Client Component module"`,
  `import trace` aponta `ingest-health-query.ts` -> probe -> `page.tsx`. `git diff --stat
  frontend/src/app/painel/page.tsx` VAZIO depois de reverter a mutação efêmera
- `npm --prefix frontend run lint` -> `rc=0` (universo: `eslint src`)
- `npm --prefix frontend run typecheck` -> `rc=0` (`tsc -p tsconfig.json --noEmit --strict`)
- `npm --prefix frontend run test:s1` -> tests 60, pass 60, fail 0 (universo: `ls
  frontend/src/features/s1-console/*.test.ts` = 6 arquivos; `fingerprint-sync-boundary.test.ts`
  continua 1 `test(` com 4 asserções MORDE/CONTROL internas, mais a CALA do fim)
- `npm --prefix frontend run test:s3` -> 37/37; `npm --prefix frontend run test:app` -> 97/98 (1 fail
  pré-existente e fora de escopo: `universe-at.test.ts` falta um fixture de `data/snapshots/` — dado
  bruto gitignored, `CLAUDE.md` §"Dado bruto não é versionado", não relacionado a este portão)
- `npm --prefix frontend run test:charts` -> falhas pré-existentes de "REAL FIXTURE" (mesmo motivo:
  dados brutos ausentes neste ambiente) — componente `charts`, fora de escopo desta task
- `harness rules --mode sweep --changed-only` -> `rc=0`
- `git log --diff-filter=D -- frontend/src/features/s1-console/fingerprint-sync-boundary.test.ts` -> vazio
Cobertura (falsificador, não %): o próprio arquivo de teste É o falsificador (MORDE plantado e
confirmado ANTES do CALA, no mesmo `test:s1` run, seguindo o precedente `ADR-012`/`T-05.1`). Além dele,
verificação manual fora do arquivo versionado: mutação efêmera em `page.tsx` (import do probe
transitivo) reproduziu o `next build` reprovando por `server-only`, depois revertida —
`git diff --stat frontend/src/app/painel/page.tsx` vazio, confirmando que nada residual ficou.
Doc delta:
- SPEC-003/ADR-028: sem mudança — `D3` já descrevia exatamente esta propriedade e delegava a forma
  ((i)/(ii)) ao implementador; esta task só a torna código, forma (i) escolhida (regra local por
  diretiva, ver comentário de topo de `use-client-fingerprint-boundary.mjs` pelo porquê sobre (ii))
- docs/INDEX.md: linha acrescentada, apontando para este gate block
- ADR: não necessário — nenhuma decisão nova, só o mecanismo que `D3` já autorizava
Bloqueado (se houver): nenhum. `make verify` (os 6 portões) roda `INDETERMINADO` neste worktree — os
2 sub-alvos `regras`/`política` recusam medir com `NAO RESOLVIVEL` (nenhuma das 3 vias de registro do
mecanismo casa o path do worktree isolado; `harness doctor` roda `CONFORME` de dentro da sessão, porque
usa o `harness` do PATH da sessão, não `.harness/mechanism` via `bash` puro). É o MESMO gap ambiental
que `T-01.1` não encontrou porque não rodou `make verify` inteiro — os 4 outros sub-alvos deram `OK`:
`lint-backend rc=0`, `lint-frontend rc=0`, `test rc=0` (1689 passed, 97.85% cobertura), `boundaries
rc=0` (7 kept, 0 broken). Não é código desta task; é ambiente do worktree.
```
