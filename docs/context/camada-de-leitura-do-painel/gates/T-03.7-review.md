# Review arquitetural — `T-03.7` (`CST-138`)

Feature: `camada-de-leitura-do-painel` · Fase: `03` · Componente: `web` · Juiz: `frontend-architect`
(`harness policy --key agents.by_component` → `web.architect = .claude/agents/frontend-architect.md`).
Branch: `tasks/T-03.7-s1-exibe-agregado-por-serie` (commit `e8cc276`, contra `origin/master`).

## Denominador

- Regras bloqueantes em vigor: `harness rules list --severity block` → **8** (`n=8`).
- Regras avaliadas contra os arquivos tocados por esta task: as 8, via `harness rules --mode file
  --path <arquivo>` em cada um dos 7 arquivos de código do diff + `harness rules --mode sweep
  --changed-only` sobre o checkout detached do commit `e8cc276` — todas `rc=0`, saída vazia (nenhum
  bloqueio).
- Arquivos varridos (universo do diff `origin/master...origin/tasks/T-03.7-s1-exibe-agregado-por-serie
  --name-status`, `n=8`): `docs/INDEX.md`, `docs/context/.../gates/T-03.7-build.md`,
  `frontend/e2e/01-painel-carrega.spec.ts`, `frontend/e2e/02-rede-e-estados.spec.ts`,
  `frontend/e2e/helpers.ts`, `frontend/src/app/painel/page.tsx`, `frontend/src/app/painel/source-state.ts`,
  `frontend/src/features/s1-console/collector-status-query.{ts,test.ts}` (2 arquivos novos).

## O que a máquina mediu

```
harness rules --mode file --path frontend/src/app/painel/page.tsx                              # rc=0
harness rules --mode file --path frontend/src/app/painel/source-state.ts                       # rc=0
harness rules --mode file --path frontend/src/features/s1-console/collector-status-query.ts    # rc=0
harness rules --mode file --path frontend/src/features/s1-console/collector-status-query.test.ts # rc=0
harness rules --mode file --path frontend/e2e/01-painel-carrega.spec.ts                          # rc=0
harness rules --mode file --path frontend/e2e/02-rede-e-estados.spec.ts                          # rc=0
harness rules --mode file --path frontend/e2e/helpers.ts                                          # rc=0
harness rules --mode sweep --changed-only                                                         # rc=0
```

Nenhuma das 8 regras bloqueantes (`core.relative-import`, `core.silent-except`,
`core.print-statement`, `core.hardcoded-secret`, `web-fullstack.browser-imports-server`,
`web-fullstack.tenant-from-request`, `web-fullstack.server-test-directory-present`,
`own.compose-hardcoded-secret`) disparou. `import "server-only"` está presente no topo do módulo
novo; nenhum import de `backend/` em código de valor (só em comentário, citando caminho por
referência).

## O que só a arquitetura declarada diz — auditado contra `ADR-030`/`SPEC-003`/`tasks.toml`

- **Forma do envelope** (`ADR-030` D5, `docs/adr/ADR-030-....md:84-101`): os 15 campos de
  `CollectorStatusRow` no `collector-status-query.ts` (`ROW_FIELD_KINDS`, linhas ~140-160) batem
  **1:1**, na mesma ordem, com o exemplo JSON da ADR — `series/source/endpoint/status/
  uptimePercent/statusDetail/retention/resilience/n_runs_total/n_runs_in_window/last_run_id/
  last_verdict/last_ended_at/age_s/liveness`. `KNOWN_STATUSES` = `{ATIVO,PARADO,ARQUIVO,PENDENTE}`
  bate com `domain.ts:150`'s `CollectorStatus`.
- **Mapeamento verbatim dos 6 campos** (`ADR-030` D5 "Os 6 primeiros campos... verbatim"):
  `collectorRowFromCollectorStatusRow` (linhas ~264-273) copia exatamente
  `series/retention/resilience/status/uptimePercent/statusDetail` para `CollectorRow`; os 9
  insumos (`n_runs_total` etc.) não vazam para `S1ViewModel` — confirmado lendo `view-model.ts`
  (`buildS1ViewModel`, linha 193, assinatura recebe só `CollectorRow[]`).
- **`RN-4`** (`SPEC-003` §3.7: *"a tela exibe `janela_de_perda` como veio"*): `grep -n
  janela_de_perda` nos 4 arquivos de código tocados só acha 2 ocorrências, ambas em comentário
  (`page.tsx:13`, `collector-status-query.ts:43`), nenhuma leitura de campo — o módulo novo nunca
  declara essa coluna no `ROW_FIELD_KINDS`. `S1Console.tsx` (não tocado por esta task) continua
  intocado.
- **Fronteira client/server** (`ADR-028/D2-D3`, `SPEC-003` §4 linha 1 da tabela de limites):
  `source-state.ts` segue importando `TransportErrorKind` só como `import type`, agora de
  `collector-status-query.ts` (que o re-exporta de `ingest-health-query.ts`) — nenhum import de
  valor cruza para um arquivo `"use client"`.
- **Reuso de tipos existentes em vez de nova 13ª superfície**: a task listava
  `domain.ts:59-79`/`view-model.ts`/`S1Console.tsx` como arquivos candidatos, mas nenhum foi
  tocado — confirmado que `domain.ts:59-96` já declara as 6 variantes de `RetentionWindow`
  (`computed_uniform/measured_sparse/doc_only/declared_constant/unmeasured/not_applicable`) e
  `view-model.ts:94-107` já trata exaustivamente `unmeasured/not_applicable/not_scored/
  unavailable` — os tipos novos e estreitos do parser (`Retention`/`Resilience`, 2 variantes cada)
  são estruturalmente atribuíveis aos tipos largos já existentes, sem type-cast. Não é violação:
  é o motivo declarado (`ADR-030` "a fórmula... TBD" já resolvida por T-03.6; T-03.7 só consome).
- **`F-9`/`NG-9`** (sha256 de `/ingest-health` não muda): fora do escopo desta task (é prova do
  lado `backend`, `T-03.6`); o relatório de build cita `make verify`/`boundaries rc=0 7 kept/0
  broken` como evidência indireta — não reexecutado aqui (escopo desta review é `web`).

## Achado cruzado (não-bloqueante, já registrado pelo builder)

`API_PREFIX` (`T-02.2`, fechado 2026-09-07) não é lido por `ingest-health-query.ts`, que carrega a
mesma lacuna estrutural que `collector-status-query.ts` corrigiu **só no arquivo novo** desta
task. Achado real (confirmado por leitura do código: `ingest-health-query.ts` segue montando
`new URL("/ingest-health", baseUrl)` sem prefixo), mas fora do escopo de arquivo declarado por
`T-03.7` (`tasks.toml:454-465`, `arquivos` não cita `ingest-health-query.ts`) — não é responsabilidade
desta task corrigir um arquivo que ela não toca. Fica como `[INFO]`, não `[WARNING]`/`[BLOQUEIO]`:
já está sinalizado no próprio gate de build (`T-03.7-build.md` §2) para o coordenador decidir task
nova.

## Ambiente — o que NÃO pôde ser reexecutado nesta review

`node_modules` ausente no worktree desta review (`ERR_MODULE_NOT_FOUND: server-only`) — os 33
testes novos de `collector-status-query.test.ts` e o `make verify` do builder não foram
re-executados aqui; a falha reproduzida é idêntica para o arquivo pré-existente
`ingest-health-query.test.ts` (mesmo erro de módulo ausente), confirmando que é lacuna de ambiente
desta review, não defeito do código sob revisão. Aceito por leitura estática do parser (campo a
campo contra `ADR-030` D5) e do relatório de build (`T-03.7-build.md` §3, comandos literais com
saída `104 passed, 0 failed`, `lint`/`typecheck` limpos, `make verify` verde).

## Veredito

**COMPLIANT.** 0 de 8 regras bloqueantes violadas (denominador acima); nenhuma violação de
arquitetura declarada encontrada com citação de documento. Achado cruzado do `API_PREFIX` é
`[INFO]`, fora do escopo de arquivo desta task, já registrado.
