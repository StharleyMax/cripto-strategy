# Gate — `frontend-builder` — `T-03.3` (`CST-134`)

Feature: camada-de-leitura-do-painel

**Feature:** `camada-de-leitura-do-painel` · **Fase:** `03` · **Task:** `T-03.3` —
`parseCatalogEnvelope` estrito e `S3` exibe as 10 linhas vindas da API — `FIXTURE_CATALOG_ROWS`
fora do grafo de produção. **Componente:** `web`. **Branch:** `tasks/T-03.3-s3-exibe-catalogo`.

## 1. O que foi feito

1. **`frontend/src/features/s3-inspector/series-catalog-query.ts`** (novo) — parser + transporte
   HTTP de `GET {API_PREFIX}/series-catalog` (`SPEC-003` §3.4, backend de `T-03.2`). Auto-contido
   (mesma postura de `collector-status-query.ts`): estrito em campo ausente/tipo errado sobre os
   **21** campos do envelope (6 de `SeriesCatalogEntry` + 15 de `SeriesKey`, `ADR-019/D2`);
   permissivo em campo extra. Depois da forma confirmada, delega a `assertValidCatalogEntry`
   (`series-catalog.ts`, `T-06.10`) a camada de REGRA DE NEGÓCIO — duas camadas, dois tipos de
   erro. `catalogRowsFromSeriesCatalogProjection` monta `CatalogRow`: `completeness` sempre
   `{kind:"unmeasured"}` (nunca vai no fio, `SPEC-003` §3.4 literal); `provenance` derivado de
   `reconstructedFrom` (`!== null` ⇒ `DERIVADO`, senão `OBSERVADO` — **medido ao vivo**: 9 das 10
   linhas reais são `OBSERVADO`, 1 é `DERIVADO` — o `cvd_source` da `coinalyze`, que de fato tem
   `reconstructed_from`/`published_error` preenchidos); `quarantine` mirrors
   `quarantine_terms.py::quarantine_terms_for_catalog_entry` (backend, feature irmã
   `plataforma-dados`) — `labelShift`/`unit` sempre presentes, `availableAtPresent` sempre
   `false` (não derivável do catálogo sozinho; `T-03.4`/`T-03.5` trazem a resposta real —
   "silêncio não é `ok`"). **Achado esperado, não bug:** as 10 linhas hoje mostram badge
   `QUARENTENA` com `available_at` como termo aberto.
2. **`frontend/src/features/s3-inspector/series-catalog-query.test.ts`** (novo) — 46 testes:
   `parseCatalogEnvelope` CALA/MORDE genérico; **o falsificador do DoD — 1 dos 21 campos removido
   por vez, universo declarado (6+15) e verificado sem duplicata**; tipo/forma errada; camada de
   negócio ainda reprova depois da forma passar; mapeamento para `CatalogRow`; os 4
   `TransportErrorKind` com `fetchImpl` mockado.
3. **`frontend/src/features/s3-inspector/domain.ts`** — `Completeness` ganhou a variante
   `{kind:"unmeasured"}`.
4. **`frontend/src/features/s3-inspector/view-model.ts`** — `completenessText` trata o novo
   `kind` (`"não medido"`, nunca um número). **Achado corrigido, não cosmético:**
   `buildCatalogRowView`'s `seriesKeyId` usava só 6 dos 15 termos de `SeriesKey` — medido AO VIVO
   contra a API real, isso colide: `cvd_source_catalog.py` builda DUAS linhas `binance`/`cvd_source`
   que só diferem em `quantityField` (`q` vs `nq`, `ADR-001`), e a chave de 6 campos as tornava
   IGUAIS. Efeito observado, não hipotético: filtrando por `"sum_open_interest"` (que deveria
   casar 5 de 10), o React renderizava **6** `<tr>` — uma linha `cvd_source` presa por causa da
   colisão de `key`. Corrigido para juntar os 15 termos (mesma garantia que o `series_key_id()` do
   backend documenta: "duas chaves que diferem em QUALQUER termo têm ids diferentes"); teste de
   regressão adicionado (`view-model.test.ts`) reproduzindo o par `q`/`nq` colidente.
5. **`frontend/src/features/s3-inspector/S3Inspector.tsx`** — **achado corrigido, não decisão de
   design nova:** `INTEGRITY_INK_CLASS` usava `text-[#e0aaff]` (hex arbitrário, hardcoded),
   ignorando o token JÁ EXISTENTE e JÁ APROVADO `--color-integrity-ink`/`text-integrity-ink`
   (`globals.css`, dark `#e0aaff` / light `#581c87` via `prefers-color-scheme`,
   `DESIGN_SYSTEM.md` §1.4-bis, contraste medido 9,54 claro/8,15 escuro — PASS nos dois). O
   arbitrário sempre pintava o hex ESCURO, invisível em `F1`/`F2` porque o catálogo era `[]` e o
   badge nunca renderizava de verdade; a primeira vez que ele renderizou de verdade (10 linhas
   reais, todas quarentenadas) o `axe-core` (`05-a11y.spec.ts`, `make e2e`) mediu
   `color-contrast serious, nodes: 20` — o hex escuro sobre a superfície CLARA (default de
   `prefers-color-scheme` neste `make e2e`) reprova AA. Troquei para a classe já existente,
   **nenhuma cor nova, nenhuma decisão de design** — os outros `text-*` deste componente já usam
   a forma theme-aware; só esta linha não tinha migrado.
6. **`frontend/src/app/painel/page.tsx`** — segunda chamada de rede, independente da de `S1`:
   `fetchSeriesCatalogProjectionViaHttp` + `catalogRowsFromSeriesCatalogProjection`. Try/catch
   PRÓPRIO — uma falha do catálogo nunca sobrescreve um `sourceState:"ok"` que a chamada de `S1`
   já tenha estabelecido.
7. **`frontend/src/app/painel/PainelClient.tsx`** — `catalog_rows:${N}` (novo `data-fact`,
   `sr-only`, NÃO condicionado a `sourceState.kind==="ok"`). **Corrigido em paralelo, achado
   próprio:** um comentário pré-existente citava `` `fixtures.ts` `` por extenso, virando falso
   positivo do próprio grep que a task deveria zerar — reescrito na mesma técnica que `page.tsx`
   já usava.
8. **`frontend/package.json`** — `test:s3` ganhou `--conditions=react-server`: primeiro consumidor
   de `server-only` sob `s3-inspector/`.
9. **`frontend/e2e/01-painel-carrega.spec.ts`/`02-rede-e-estados.spec.ts`** — `B1` ganhou
   `catalog_rows:10`; os dois casos `connection_refused`/`missing_base_url` ganharam
   `catalog_rows:0`.
10. **`frontend/e2e/04-interacoes.spec.ts`** — **reescrito, não só ajustado:** o teste antigo
    afirmava literalmente "0 linhas em F1, sem GET /series-catalog ainda" — premissa que esta
    task encerra. Agora prova as DUAS metades de `D1.9`: `before=10`; filtro `"sum_open_interest"`
    reduz para `5` (não todas as 10 — prova real de redução, não eco); filtro sem match reduz
    para `0`.

## 2. Comandos rodados (literais) e resultado

```
npm --prefix frontend run test:s3         # 84 passed (47 novos/alterados + 37 pré-existentes)
npm --prefix frontend run test:s1         # 104 passed — sem regressão
npm --prefix frontend run test:app        # 97/98 — 1 falha PRÉ-EXISTENTE (universe-at.test.ts,
                                           #  data/snapshots ausente, data/ não versionado)
npm --prefix frontend run test:charts     # 1 falha PRÉ-EXISTENTE (s2-panels.test.ts, dependente de data/)
npm --prefix frontend run lint            # limpo
npm --prefix frontend run typecheck       # limpo
HARNESS_MECHANISM=.../bin/harness make verify   # VERDE — 6 portões (rodado 2x, antes e depois
                                                 #  dos 2 achados corrigidos; ambas as vezes verde)
HARNESS_MECHANISM=.../bin/harness harness rules --mode sweep --changed-only   # rc=0, saída vazia
grep -rn 'fixtures.ts' frontend/src/app frontend/src/features --include='*.tsx' \
  | grep -v '\.test\.' | wc -l    # -> 0

# make e2e RODADO DE VERDADE (backend real + next build, não simulado):
make e2e                    # API de pé  -> 19/19 PASSED. catalog_rows:10 (B1); axe_violations=[]
                             #  (o achado do INTEGRITY_INK_CLASS estava aberto na 1a rodada, fechado
                             #  na 2a); 04-interacoes: before=10, matching=5, non-matching=0
make e2e E2E_API_UP=0        # API no chão -> 16/19 passed; 3 falhas são o FALSIFICADOR ESPERADO
                             #  (B1/B2, já documentados como "flip esperado no chão"; 04-interacoes
                             #  agora também flipa, pela MESMA razão: before=0 medido, não 10) —
                             #  catalog_rows:0 confirmado nos dois casos de 02-rede-e-estados
```

## 3. DoD (`T-03.3` refs) — todos fechados com o comando que os mede

- [x] Teste TS com 1 campo removido por vez ⇒ parser reprova — universo = 21 (6+15), sem duplicata.
- [x] `FIXTURE_CATALOG_ROWS` fora do grafo de produção — grep → `0`.
- [x] e2e S3 `catalog_rows = 10` — `make e2e` real, `E2E-FACT 01-painel-carrega
      catalog_rows_data_fact="catalog_rows:10"`.
- [x] DoD no chão: `error_kind:connection_refused` e `catalog_rows = 0` — `make e2e
      E2E_API_UP=0`, `E2E-FACT 02-rede-e-estados D1.5b-porta-sem-listener_catalog_rows="catalog_rows:0"`
      (mesmo `error_kind:connection_refused` já provado pela asserção pré-existente da linha).

## 4. Doc delta

- `docs/INDEX.md`: linha acrescentada (append-only).
- `docs/product/STITCH_CONTEXT.md`/`DESIGN_SYSTEM.md`: **sem mudança** — nenhuma cor/token novo;
  o achado do `INTEGRITY_INK_CLASS` REUSA o par já medido e aprovado em `DESIGN_SYSTEM.md`
  §1.4-bis, não introduz um terceiro. Nenhum gate de design (`ux-ui-mastery`) aplicável.
- `ADR`: não necessário.

## 5. Escopo respeitado

Só `web` (`frontend/`). Nenhum arquivo de `backend/`, `deploy/`, `docs/adr/` tocado. Os dois
achados corrigidos (`seriesKeyId`, `INTEGRITY_INK_CLASS`) vivem em arquivos que os `refs` desta
task já nomeiam (`view-model.ts`, `S3Inspector.tsx`) — não é escopo emprestado de outra task.

## 6. Achados registrados — os dois só ficaram visíveis porque o catálogo passou a ter dado real

1. **Colisão de `seriesKeyId`** (`view-model.ts`) — corrigida, com teste de regressão.
2. **`text-[#e0aaff]` hardcoded reprovando `color-contrast`** (`S3Inspector.tsx`) — corrigido,
   trocado pelo token já existente/aprovado.
3. **Quarentena sempre aberta em `available_at`** — comportamento correto por construção
   (mirrors o backend), não uma regressão; `T-03.4`/`T-03.5` trazem a resposta real.
4. **`fixtures.ts` falso-positivo pré-existente** em `PainelClient.tsx` — corrigido.

Os quatro foram encontrados PORQUE esta task, pela primeira vez, faz 10 linhas reais chegarem ao
DOM — os dois primeiros eram invisíveis com `catalog=[]` desde `T-06.10`.

## 7. Veredito

Pronto para QA. **`make e2e` verde de pé (19/19) e o falsificador confirmado no chão (16/19, as
3 quedas são o flip esperado, medido).** Nenhum bloqueio conhecido.
