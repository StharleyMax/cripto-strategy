# `T-02.4` — review arquitetural — `ETag = IngestHealthReport.fingerprint()`, `304` (`CST-128`)

**Branch:** `origin/tasks/T-02.4-etag-fingerprint-304` (`26483c9`), base `a832856` (`master`).
**Componente:** `infra` (juiz declarado: `infra-architect`, `harness policy --key agents.by_component`).
**Escopo:** 2 arquivos — `backend/src/api/routes/ingest_health.py` (+33/-6),
`backend/tests/api/test_ingest_health_etag_and_304.py` (novo, 172 linhas).

## Camada 1 — o que o runner mede

- `harness rules list --severity block` → **8 regras bloqueantes em vigor** neste repositório.
- `harness rules --mode file --path backend/src/api/routes/ingest_health.py` →
  **1 achado, severidade `[AVISO]` `core.module-docstring-single-line`** (docstring de módulo não
  abre/fecha na 1ª linha). **Não é regra bloqueante** (confirmado em `harness rules list`, ausente
  das 8; `packs/core/rules.toml` classifica esta regra como `AVISO`). **Pré-existente**: o docstring
  do módulo já era multi-linha em `a832856` (`git show a832856:...`), antes de `T-02.4` tocar o
  arquivo — o diff só ACRESCENTA um parágrafo a um docstring que já violava. `T-02.4` não introduz
  esta divergência, herda debt já registrado.
- `harness rules --mode file --path backend/tests/api/test_ingest_health_etag_and_304.py` →
  **0 achado** (`rc=0`).
- **Denominador:** 8 regras bloqueantes existiam, 8 avaliadas via `harness rules --mode file` sobre
  os 2 arquivos do escopo (`code_paths` inclui `backend/src/` e `backend/tests/`), **0 bloqueante
  violada**.

## Camada 2 — arquitetura declarada (`infra-architect`, `ADR-005/D6`, `ADR-029/D4`, `SPEC-003` §3.4)

- **Camada consumidora, não domínio novo**: o handler chama só `ingest_health_query(source)` e
  `report.fingerprint()`/`report.to_envelope()` — os dois já existiam em
  `backend/src/modules/sentimento/domain/ingest_record.py:173-179` (dono `quant-architect`), **zero
  cálculo de domínio novo escrito em `infra`** — consistente com a fronteira que o owner corrigiu à
  mão (`.claude/agents/infra-architect.md` §"A fronteira que o owner corrigiu à mão": a API é
  consumidora, não o bounded context).
- **`ADR-005/D6.3`** (`ETag` fora da região hasheada, nunca em `runs[]`): confirmado por leitura —
  `fingerprint()` opera sobre `canonical_projection()` = `runs[]`+`gaps[]` apenas
  (`ingest_record.py:173-179`); o header é anexado depois, `report.to_envelope()` continua
  intocado — testado por `test_etag_is_the_strong_fingerprint_and_the_200_body_is_untouched`
  (asserta `set(envelope) == {"query","n_runs","n_gaps","runs","gaps"}`, `NG-9` respeitado).
- **`ADR-005/D6`** (falsificador nomeado no próprio `ADR-005`: *"DoD-2 comparando um número consigo
  mesmo"*): o teste **não** compara o header com o valor produzido no mesmo processo — ele lê o
  MESMO store de novo, de um `SqliteIngestRecordStore` independente, e recomputa
  `fingerprint()` (`test_etag_is_the_strong_fingerprint_and_the_200_body_is_untouched:95-97`).
  Controle negativo presente: gravar um run a mais no mesmo store MUDA o `ETag`
  (`test_etag_changes_when_one_more_run_is_recorded_into_the_same_store`) — sem isso um `ETag`
  constante passaria a igualdade por acidente.
- **`ADR-029/D4`/`F-029-4`** (validador forte, `304` a `If-None-Match` igual, `200` a diferente):
  os 4 testes de DoD citados no `tasks.toml:285` — `not etag.startswith("W/")`, `304`+corpo vazio+
  `ETag` repetido, `If-None-Match` errado → `200` — todos presentes e **passam**
  (`poetry run pytest tests/api/test_ingest_health_etag_and_304.py -q` → `5 passed`).
- **Suíte completa do backend não regride**: `poetry run pytest -q` (backend inteiro, com os 2
  arquivos de `T-02.4` sobrepostos ao HEAD deste worktree) → **exit 0**, todas as linhas de progresso
  `.` (nenhum `F`/`E`).
- **`ruff check`** sobre os 2 arquivos → `All checks passed!`.
- **Idioma de identificador** (`CLAUDE.md` tabela, linhas 1-3): todo identificador novo em inglês
  (`etag`, `report`, `request`, `get_ingest_health`, nomes de teste) — nenhuma mensagem de
  `raise`/`Error`/`Exception` nova introduzida por este diff.
- **Escopo do FALSIFICADOR declarado na task** (`tasks.toml:286`: *"igualdade só de um lado... o lado
  TS é T-02.5"*) — respeitado: este diff não toca `frontend/`, a comparação de dois lados fica para
  `T-02.5`.

## Achados

Nenhum. A única divergência mecânica encontrada (`core.module-docstring-single-line`) é
**pré-existente, severidade `AVISO`**, não bloqueante, e não foi introduzida por este diff.

## Veredito

**COMPLIANT** — 0 de 8 regras bloqueantes violadas; implementação confere literalmente com
`ADR-005/D6.3`, `ADR-029/D4`/`F-029-4` e o DoD de `tasks.toml` `T-02.4`; suíte completa do backend
verde; nenhuma decisão de arquitetura (camada, direção de dependência) fora do que o
`infra-architect` já delimitou.
