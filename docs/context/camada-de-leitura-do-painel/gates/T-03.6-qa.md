# `T-03.6` — QA gate — agregação por série `collector-status` (`CST-137`)

**Feature:** `camada-de-leitura-do-painel` · **Fase:** `03` · **Componente:** `sentimento` + `infra`
**Data:** 2026-09-07 · **Branch avaliada:** `origin/tasks/T-03.6-agregacao-por-serie-collector-status`
(checkout local `qa-T-03.6-tmp`, tracking o mesmo commit).

## 1. Escopo e evidência independente

Reli `docs/context/camada-de-leitura-do-painel/gates/T-03.6-build.md` e reproduzi, sem confiar no
relato, cada afirmação quantitativa dele:

- `bash backend/scripts/lint.sh` → `ruff check`/`ruff format --check`/`mypy --strict` limpos,
  `341 files already formatted`, `Success: no issues found in 341 source files` — igual ao build.
- `bash backend/scripts/boundaries.sh` → `Contracts: 7 kept, 0 broken` (`Analyzed 214 files, 927
  dependencies`) — igual ao build.
- `bash backend/scripts/test.sh` (suíte completa) → **`1733 passed, 3 warnings in 396.13s`**, `rc=0`.
  Cobertura por camada (`ADR-009/D1`): `domain 99.8%` (meta 90%), `use_cases 100.0%` (meta 80%),
  `infra 95.2%` (meta 70%) — 3 de 3 camadas medidas, todas acima do alvo. Cobertura total `97.90%`
  ≥ piso `70.0%`. `collector_status.py` (domain) 66/66 linhas, `collector_status.py` (use_cases)
  52/52 linhas + 8/8 branches — **100%** nos dois arquivos novos.
- Isolado: `.venv/bin/python -m pytest tests/sentimento/test_collector_status_query.py
  tests/api/test_collector_status_route.py tests/api/test_api_prefix.py -v --no-cov` →
  **23 passed** (17 falsificadores de `ADR-030` D1-D4 + 3 da rota + 3 do prefixo, os dois últimos
  editados para incluir `/collector-status`).
- `grep -rn 'SELECT' backend/src/api | wc -l` → **0** (0 SQL no handler).
- `grep -rnE '"ARQUIVO"|"PENDENTE"' backend/src | wc -l` → **0**; `grep -rnE
  'computed_uniform|measured_sparse|declared_constant' backend/src | wc -l` → **0** (F-4/F-7 não
  disparam).
- `harness rules --mode file --path <3 arquivos novos>` → `rc=2`, mas o único achado é
  `[AVISO] core.module-docstring-single-line` (severidade **warn**, confirmado em `harness rules
  list`, não está entre as 8 regras `[BLOQUEIO]`) — não reprova a fase.
- `grep` manual por import relativo, `except: pass` silencioso e `print(` nos 3 arquivos novos →
  nenhuma ocorrência; o único `except ValueError` (linha 152 de `use_cases/collector_status.py`)
  relança `MalformedRunTimestampError` com o valor e o motivo, em inglês — não é
  `core.silent-except`.

## 2. Mutação — controle negativo rodado por mim, não copiado do relato

Mutei `use_cases/collector_status.py`, trocando `if last_run.verdict == "REJECTED":` (regra A de
`ADR-030` D1) por `if False:`. Rodei `pytest tests/sentimento/test_collector_status_query.py -v
--no-cov`:

```
FAILED test_f1_falsifier_rejected_last_verdict_is_always_stopped — assert 'ATIVO' == 'PARADO'
FAILED test_d3_d4_stopped_series_is_not_applicable_and_unavailable — assert 'ATIVO' == 'PARADO'
2 failed, 15 passed in 0.12s
```

Revertido via `cp` do arquivo original salvo antes da mutação; `git diff --stat` e `git status
--porcelain` vazios depois; suíte isolada re-executada, voltou a **23 passed**. A mutação foi
mordida pelos exatos dois testes que deveriam mordê-la (`F-1` e `D3/D4`), confirmando que o verde
da suíte não é acidental.

## 3. DoD (`tasks.toml` T-03.6) e falsificador da fase — item a item

- `to_envelope()`/15 colunas de `/ingest-health` intocados: confirmado por leitura —
  `use_cases/collector_status.py` importa só `IngestRun` de `domain/ingest_record.py`, nunca
  `to_envelope`. `test_f9_falsifier_ingest_health_sha256_is_unchanged_by_the_new_route` (linha 107
  de `test_collector_status_route.py`) chama `/ingest-health` → `/collector-status` →
  `/ingest-health` sobre o MESMO store real (uvicorn em thread) e compara `sha256` — **[OK]**.
- Envelope separado `{"query":"collector_status", ...}`: forma do `to_envelope()` em
  `domain/collector_status.py:195-203` bate campo a campo com o exemplo de `ADR-030` D5 (linha
  84-100 da ADR) — mesmos 6 campos `CollectorRow` verbatim + insumos em `snake_case`.
- `IngestRecordSource.runs()` sem método novo: confirmado — `use_cases/collector_status.py` só
  chama `source.runs()`.
- `make boundaries` verde, `0 SELECT` no handler: confirmados acima.
- Falsificador da fase (`sha256` de `/ingest-health` mudar ⇒ `NG-9` violado): não disparou — o
  teste F-9 prova o oposto.
- `depends_on`: `T-03.1` (`status = "done"`, `ADR-030` mergeada) e `T-02.2` (`status = "done"`) —
  confirmado em `tasks.toml`.

## 4. Regras bloqueantes (`harness rules list --severity block`, 8 no total)

| # | regra | veredito | evidência |
|---|---|---|---|
| 1 | `core.relative-import` | OK | `grep -n '^from \.\|^from \.\.'` nos 4 arquivos tocados → 0 |
| 2 | `core.silent-except` | OK | único `except` (linha 152) relança com contexto, não engole |
| 3 | `core.print-statement` | OK | `grep -n 'print('` → 0; usa `logger.debug` |
| 4 | `core.hardcoded-secret` | N/A | nenhum arquivo de produção com credencial nesta task |
| 5 | `web-fullstack.browser-imports-server` | N/A | task é 100% backend |
| 6 | `web-fullstack.tenant-from-request` | N/A | rota não lê tenant do pedido |
| 7 | `web-fullstack.server-test-directory-present` | OK | `backend/tests/` presente e populado |
| 8 | `own.compose-hardcoded-secret` | N/A | nenhum `compose.yml` tocado |

Regras bloqueantes avaliadas: 8 de 8.

## 5. Anomalias

Nenhuma. `harness rules --mode sweep --changed-only` devolveu vazio/`rc=0` (sem achados —
provavelmente por não haver base de diff configurada neste checkout ad-hoc); a checagem por
arquivo (`--mode file`) supriu a lacuna e não achou nada bloqueante.

## Veredito

```
## QA Gate — Fase 03 [sentimento+infra] T-03.6
- [OK] core.relative-import — grep -n '^from \.' nos 4 arquivos tocados -> 0
- [OK] core.silent-except — use_cases/collector_status.py:152 relança com contexto (MalformedRunTimestampError)
- [OK] core.print-statement — grep -n 'print(' -> 0, usa logger.debug
- [OK] web-fullstack.server-test-directory-present — backend/tests/ presente
- [OK] Testes existem e passam — bash backend/scripts/test.sh (1733 passed, 3 warnings, 396.13s)
- [OK] Testes isolados da task — pytest tests/sentimento/test_collector_status_query.py tests/api/test_collector_status_route.py tests/api/test_api_prefix.py -v --no-cov (23 passed)
- [OK] Cobertura medida 97.90% contra alvo declarado 70.0% (piso); por camada: domain 99.8%/90%, use_cases 100.0%/80%, infra 95.2%/70% — 3/3 OK; collector_status.py 100% nos dois arquivos novos
- [OK] Mutação (controle negativo) — if last_run.verdict=="REJECTED" -> if False: reproduz 2 failed, 15 passed; revertido, git diff vazio
- [OK] F-9/NG-9: sha256 de /ingest-health inalterado com /collector-status sobre o mesmo store (test_f9_falsifier...)
- [OK] 0 SQL no handler — grep -rn 'SELECT' backend/src/api -> 0
- [OK] make boundaries verde — Contracts: 7 kept, 0 broken
- [OK] make lint verde — ruff/mypy limpos
- [OK] DoD da fase (tasks.toml T-03.6): envelope separado conforme ADR-030 D5, runs() sem método novo, 15 colunas/to_envelope() intocados, depends_on T-03.1/T-02.2 done
- [anomalia] nenhuma
Regras bloqueantes avaliadas: 8 de 8
Veredito: APPROVED
```
