# Gate — `architect-review` (read-only) — `T-03.8` (`CST-139`)

Feature: `camada-de-leitura-do-painel` · Fase: `03` · Componente: `web`.
Branch auditada: `tasks/T-03.8-formatador-unico-pt-br` (`origin`, commit `2e80219`).
Arquiteto de referência do componente: `.claude/agents/frontend-architect.md`
(`harness policy --key agents.by_component` → `web.architect`).

## Denominador

- Regras bloqueantes em vigor: **8** (`harness rules list --severity block`), origem
  `packs [core, web-fullstack]` + declarações próprias.
- Avaliadas contra os arquivos do diff: **todas as 8** — via
  `harness --root <worktree-com-a-branch> rules --mode sweep --changed-only` (rc=0, saída vazia)
  e `rules --mode file --path <arquivo>` individual nos 4 arquivos tocados (rc=0 nos 4).
- Arquivos varridos: **6** do diff `master...origin/tasks/T-03.8-formatador-unico-pt-br`
  (`git diff --name-status`): `docs/INDEX.md`, `docs/context/.../T-03.8-build.md`,
  `frontend/e2e/01-painel-carrega.spec.ts`, `frontend/e2e/07-locale.spec.ts`,
  `frontend/src/features/s1-console/view-model.test.ts`,
  `frontend/src/features/s1-console/view-model.ts`.
- Comando: `harness --root .claude/worktrees/wf_ed18e6a5-68c-5 rules --mode sweep --changed-only`
  → `rc=0`, saída vazia. `rules --mode sweep` (full) → só avisos pré-existentes, nenhum nos
  4 arquivos tocados por esta task (`core.module-docstring-single-line` em módulos
  `backend/src/modules/sentimento/*` intocados; `web-fullstack.hardcoded-url` em
  `frontend/src/app/*.test.ts` intocados).

## 1. Camada mecânica (`harness rules`) — 0 achado citável

`rc=0` nos 4 arquivos tocados, individualmente e em sweep `--changed-only`. Nenhuma das 8 regras
bloqueantes dispara.

## 2. Camada de julgamento (arquitetura declarada) — auditoria contra `SPEC-003` e `frontend-architect.md`

- **Contrato que a task fecha**: `SPEC-003` §3.7 (`docs/specs/SPEC-003-camada-de-leitura-do-painel.md:197`)
  exige **um** formatador `Intl.NumberFormat("pt-BR")` na apresentação, **0** `toLocaleString`
  espalhado, e **no fio** ponto decimal (`SPEC-001` §3.8). A mudança implementa exatamente isto:
  `formatPtBrNumber` substitui as 3 funções hand-rolled anteriores; `grep -rn toLocaleString
  frontend/src --include='*.tsx' | grep -v test | wc -l` = 0 (medido pelo builder, reproduzido
  aqui via `npm run lint`/`npm run typecheck` limpos no worktree `wf_ed18e6a5-68c-5`, rc=0 nos
  dois). O fio (`/collector-status`, `/series-catalog`) não foi tocado — nenhum arquivo de
  `backend/` está no diff — logo `SPEC-001` §3.8 (dado locale-invariant no transporte) continua
  intacto por construção, não por medição nova desta revisão.
- **Determinismo do `Intl.NumberFormat` com locale explícito**: a docstring corrigida do módulo
  (linhas 1-24 de `view-model.ts`) distingue corretamente chamada COM locale explícito
  (determinística, âncora fixa) de chamada SEM locale (lê o host, a não-determinismo que `Q14`
  proíbe). O argumento é consistente com o contrato de `SPEC-001` §3.8 — o formatador só atua na
  camada de apresentação (rótulo), nunca realimenta cálculo ou comparação (comentário de topo do
  módulo, linhas 1-3, inalterado).
- **Fronteira `web` → `charts`/`backend`**: não tocada — diff restrito a
  `frontend/src/features/s1-console/` e `frontend/e2e/`. Nenhuma superfície de
  `frontend-architect.md` (transporte, contrato de borda, paridade de porte, estrutura do app)
  foi alterada.
- **Vocabulário de componentes / linha 11 da tabela de idioma** (`janela_de_perda`): não tocado —
  `grep -c '"janela_de_perda"' backend/.../ingest_record.py` inalterado, confirmado pelo builder
  e consistente com o diff (0 arquivos de `backend/` no diff).
- **Falsificador da própria task tratado com cuidado real**: o builder identificou e corrigiu uma
  degradação onde `01-painel-carrega.spec.ts`'s grep de vazamento de fixture (`B7`) usava o
  literal antigo em ponto (`/1\.6 GB|99\.8%/`), que teria virado `rc=0` silencioso e vácuo (nunca
  mais casaria) só porque a marca decimal mudou para vírgula — corrigido para
  `/1,6 GB|99,8%/`. Isto é exatamente a disciplina que este repositório cobra (`ADR-012`,
  citado em `CLAUDE.md`) sobre falsificador disparado vs. instrumento cego.

### Achado não-bloqueante (INFO) — doc-drift textual, não de contrato

`SPEC-003` §5, linha `B7` (`docs/specs/SPEC-003-camada-de-leitura-do-painel.md:230`), ainda
mostra o instrumento literal em ponto: `` grep -c '1.6 GB\|99.8%' <main> = 0 ``. Com o formatador
único agora produzindo vírgula (`"1,6 GB"`, `"99,8%"`), esse exemplo textual do documento
aprovado ficou desatualizado — o `<main>` real nunca mais vai casar com o literal em ponto que
o próprio `SPEC-003` cita, embora o e2e (`01-painel-carrega.spec.ts`) tenha sido corretamente
atualizado. Isto **não é violação de contrato** (a substância — nenhum número de fixture
sobrevive — está preservada e provada) e **não bloqueia** — é uma citação textual estática dentro
de uma tabela normativa que ficou órfã do código que descreve. Fora do DoD de `T-03.8` (que é
escopo `web`, não edição de `docs/specs/`); registrado aqui para quem fizer a próxima revisão de
`SPEC-003` atualizar o literal do exemplo.

## 3. Verificação independente (não apenas o relatório do builder)

Rodado neste review, no worktree onde a branch já está de pé
(`.claude/worktrees/wf_ed18e6a5-68c-5`, mesmo commit `2e80219` da branch remota):

```
npm run test:s1     -> 105 passed, 0 failed   (bate com o gate de build)
npm run lint        -> rc=0, saída vazia
npm run typecheck   -> rc=0, saída vazia
harness rules --mode sweep --changed-only  -> rc=0, saída vazia
```

## Veredito

**COMPLIANT** — 0 de 8 regras bloqueantes violadas; a única observação de camada arquitetural
(doc-drift do literal em `SPEC-003` §5/`B7`) é INFO, não BLOCKER/WARNING, porque não há
divergência de contrato, só de texto de exemplo num documento que esta task não tinha no
escopo de arquivo.
