# Gate — QA (Front) — `T-03.5` (`CST-136`)

Feature: `camada-de-leitura-do-painel` · Fase `03` · Branch `tasks/T-03.5-gaveta-de-quarentena`
(commit `7d5c2d8`). Build gate lido: `docs/context/camada-de-leitura-do-painel/gates/T-03.5-build.md`.

## Ambiente

`df -h /` → 19G livres (limiar 3GB, prosseguiu). `node_modules`/`.venv` copiados do checkout
principal (nenhuma mudança em `package-lock.json`/`poetry.lock`/`pyproject.toml` neste diff, então
sem reinstall). Branch já estava em uso por `wf_3acf6adf-3e8-1` (o builder) — checkout feito em
branch local nova `qa/T-03.5-gaveta-de-quarentena` apontando pro mesmo commit de
`origin/tasks/T-03.5-gaveta-de-quarentena` (leitura, sem push).

## DoD (`tasks.toml` T-03.5 + plano `03` D3.2/D3.6)

- [OK] `grep -c FIXTURE_DIVERGENCES frontend/src/app/painel/{page,PainelClient}.tsx` → `0`/`0`
      — confirmado por mim, medido de novo, não só relido do gate.
- [OK] `grep -rn 'fixtures.ts' frontend/src/app frontend/src/features --include='*.tsx' | grep -v '\.test\.' | wc -l` → `0`.
- [OK] Falsificador do parser: `npm --prefix frontend run test:s3` → **111/111** (33 novos em
      `series-quarantine-query.test.ts`), incluindo 1 campo removido por vez sobre os 9 campos de
      `QuarantineRow.to_wire()` (universo confere com o backend, linha a linha).
- [PARCIAL] "No chão: gaveta mostra erro, não a fixture" — só validado **manualmente** pelo
  builder (`next build`+`next start` contra porta sem listener), nenhum teste automatizado (nem
  `*.test.ts`, nem `frontend/e2e/*`) exercita `quarantineOk=false` no `S3Inspector`/`page.tsx`.
  Não é blocker: os `refs` da própria task não listam `frontend/e2e/*`, e não há Vitest/Testing
  Library neste repo ainda (chegam com `T-05.11`) para testar renderização de componente sem
  subir um servidor real. Mas fica registrado: esta parte específica do DoD não tem portão, só
  prova ao vivo não repetível.

## Mutação (morde/cala) — rodada por mim, não só relida do gate do builder

1. **Morde confirmado**: removi o `throw` explícito de campo ausente em `assertQuarantineRow`
   (`series-quarantine-query.ts`, revertido depois) — `npm run test:s3` caiu de 111/111 para
   102/111, os 9 testes MORDE de campo ausente reprovaram porque a suíte verifica a mensagem
   exata ("is missing field"), não só que lança. Prova de que o teste tem poder, não é
   decoração. Restaurado (`git status` limpo depois).
2. **Cala**: os 33 testes novos já cobrem o caminho feliz (envelope bem formado) e campo extra
   ignorado (`ADR-019/D2`) — não repeti, já é o padrão dos outros dois transportes desta pasta.

## Achado

1. **[WARNING]** `series-quarantine-query.ts:14` — docstring diz "`ROW_FIELD_KINDS`' 8 entries",
   mas o `Map` tem **9** entradas (confirmado por grep e pelo teste
   `series-quarantine-query.test.ts:61-63`, que declara `QUARANTINE_ROW_WIRE_FIELDS.length === 9`
   e casa com `QuarantineRow.to_wire()` no backend, 9 chaves). Número errado num comentário, neste
   repositório que tem disciplina de "nenhum número sem o comando" — vale corrigir o comentário
   para "9 entries".
2. **[WARNING]** DoD "no chão: gaveta mostra erro" (item acima) não tem teste automatizado em
   nenhum nível — nem unidade (sem framework de render de componente ainda), nem e2e (fora do
   escopo desta task). Registrado para não virar suposição silenciosa em revisão futura.

Nenhum achado BLOCKER.

## Comandos rodados por mim (não só relidos do gate do build)

```
npm --prefix frontend run test:s3           # 111/111 (confirmado 2x, com e sem mutação)
npm --prefix frontend run test:s1           # 104/104 (falhou 8/104 até eu criar backend/data/md
                                             #  ausente no worktree fresco — gap ambiental, não
                                             #  regressão desta task; arquivos de s1 não tocados)
npm --prefix frontend run test:app          # 97/98 (1 falha pré-existente, universe-at.test.ts,
                                             #  arquivo não tocado por esta task)
npm --prefix frontend run test:charts       # 141/157 (16 falhas — TODAS por fixture data/binance/
                                             #  ausente neste worktree, ~850MB gitignored e não
                                             #  copiado por instrução do despacho; charts não é
                                             #  tocado por esta task, gap é do ambiente, não do código)
HARNESS_MECHANISM=.../bin/harness make verify
  # VERDE — 6/6: lint-backend 350 arquivos · lint-frontend (eslint+tsc --noEmit --strict) ·
  # test 1760 passed, cobertura 97,91% · boundaries 7 kept/0 broken · regras 0 bloqueio/50 aviso ·
  # política rc=0
harness rules --mode sweep --changed-only   # rc=0, saída vazia
grep -c FIXTURE_DIVERGENCES frontend/src/app/painel/{page,PainelClient}.tsx   # 0 / 0
```

## Cobertura

`make verify`'s `test` mede **backend** (97,91%, universo = `backend/src`, comando acima) — não
inclui os 111 testes de `test:s3` nem os demais `test:*` do front: **os 34+ arquivos de teste do
front (agora 34+ com os novos) continuam fora de qualquer portão de `make verify`**
`[MEDIDO 2026-09-07: grep -rn 'node --test' scripts/verify.sh Makefile → 0 linhas]`. Isto não é
regressão desta task — é o estado geral do repositório, documentado no meu próprio perfil de QA.
Cito aqui porque "verde" acima é sobre o que roda manual, não sobre o que o portão protege.

## Doc delta

`docs/INDEX.md`: 1 linha acrescentada (append-only, confirmado por `git diff` — nenhuma linha
existente reescrita). `STITCH_CONTEXT.md`/`DESIGN_SYSTEM.md`: sem mudança, justificada (reusa
tokens já aprovados `font-data-sm`/`text-data-sm`/`text-provenance-weak`) — concordo, não vi
padrão visual novo. `tasks.toml`: não tocado por esta task (correto, é ato do coordenador).

## Veredito

**APPROVED.** DoD cumprido, falsificador do parser mordeu de verdade (mutação minha, não só a
declarada pelo builder), `make verify` verde, `harness rules --mode sweep --changed-only` limpo,
sem segredo no cliente, sem chamada direta a exchange, envelope de bucket (`assertNoTickLevelFields`
presente). Os 2 achados são WARNING, não bloqueiam: 1 comentário com número errado (fácil, não
funcional) e 1 lacuna de teste automatizado que é sistêmica (mesmo padrão de `page.tsx` inteiro,
não introduzida por esta task) e limitada pela ausência de Vitest/Testing Library neste repo hoje.

Ações sugeridas para o `frontend-builder` (não-bloqueantes, podem entrar em ciclo futuro):
1. Corrigir `series-quarantine-query.ts:14` — "8 entries" → "9 entries".
2. Quando `T-05.11` trouxer Vitest/Testing Library, cobrir `S3Inspector`'s ramo `quarantineOk=false`
   e a captura de `TransportError` em `page.tsx` — hoje só prova ao vivo, não repetível.
