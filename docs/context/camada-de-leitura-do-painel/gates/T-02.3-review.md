# Review arquitetural — T-02.3 (camada-de-leitura-do-painel, fase 02)

**Branch auditada:** `tasks/T-02.3-ready-tres-estados` (`origin`), commit `3dbbe94` — "feat(infra):
GET /ready discrimina tres estados do store (T-02.3)", sobre `a832856` (T-02.2, merged em `master`).
Auditado a partir de `docs/context/camada-de-leitura-do-painel/tasks.toml:256-271` (spec) e do
arquiteto do componente `infra` (`.claude/agents/infra-architect.md`), via
`harness policy --key agents.by_component`.

## Denominador

- Regras bloqueantes em vigor: **8** (`harness rules list --severity block`).
- Arquivos varridos com `harness rules --mode file --path <arquivo>`: **9** (todos os arquivos do
  diff — `git diff origin/master...HEAD --stat`, 9 files changed, 329 insertions(+), 8 deletions(-)).
- Testes visados executados: `backend/tests/api/test_ready_route.py`,
  `test_dependencies.py`, `test_api_prefix.py`,
  `backend/tests/sentimento/test_ingest_record_crash_borders.py` — **18 passed**
  (`pytest -q --no-cov`, via `.venv` do repositório principal, mesma árvore de dependências).
- `mypy src/api src/main src/modules/sentimento/infra/sqlite_ingest_record_store.py` →
  **Success: no issues found in 8 source files**.
- `ruff check` sobre os mesmos caminhos + `tests/api` + o teste novo de crash-borders →
  **All checks passed!**.
- `lint-imports` (7 contratos de `[tool.importlinter]`, incluindo "Consumidor nao importa infra de
  contexto: o adaptador vem por injecao" e "Camadas por contexto: infra > use_cases > domain") →
  **7 kept, 0 broken**.

## Achados

**Nenhuma violação de regra bloqueante.** As 8 regras (`core.relative-import`,
`core.silent-except`, `core.print-statement`, `core.hardcoded-secret`,
`web-fullstack.browser-imports-server`, `web-fullstack.tenant-from-request`,
`web-fullstack.server-test-directory-present`, `own.compose-hardcoded-secret`) não disparam em
nenhum dos 9 arquivos do diff.

- `[WARNING]` `core.module-docstring-single-line` — docstring de módulo não abre/fecha na primeira
  linha — `backend/src/api/__init__.py:1`, `backend/src/api/dependencies.py:1`,
  `backend/src/api/routes/ready.py:1`, `backend/src/main/__init__.py:1` — regra `core.module-docstring-single-line`
  (severidade AVISO, `harness rules list` confirma). **Não é defeito de T-02.3**: é convenção de
  estilo já presente em código pré-existente do mesmo pacote — `backend/src/api/routes/ingest_health.py`
  (de `T-01.x`/`T-02.1`) e `backend/src/main/__init__.py` já na versão de `T-02.2` (`a832856`) têm o
  mesmo formato de docstring multilinha. Correção concreta, se o dono do pacote quiser fechar a
  dívida: colapsar a primeira linha da docstring de cada módulo (não é ato desta task).

## Conformidade arquitetural (julgamento, citado)

- **Fronteira "camada de API é consumidora, não bounded context"** (`[PREMISSA-OWNER: 2026-09-03]`,
  citada em `.claude/agents/infra-architect.md` linhas 68-75): `backend/src/api/dependencies.py`
  define `StoreReadinessSource` como `Protocol` **novo**, deliberadamente separado de
  `IngestRecordSource` — o handler de `/ready` não tem como ler uma linha (`runs()`/`gaps()` fora do
  protocolo). `SqliteIngestRecordStore` satisfaz estruturalmente, sem classe adaptadora nova. A
  concretização só acontece em `backend/src/main/__init__.py::create_app` via
  `app.dependency_overrides` — confirmado também por `lint-imports`, contrato "Consumidor nao importa
  infra de contexto: o adaptador vem por injecao" → `KEPT`.
- **`/ready` não é health de processo, não consulta Postgres, não é health check** (`ADR-029/D3`,
  `tasks.toml:269`): `ready.py` só chama `source.describe_readiness()`; zero SQL na rota (grep de
  `SELECT`/`select` no arquivo → 0 ocorrências); `sqlite3.DatabaseError` sobre store corrompido
  propaga (500), provado por `test_ready_propagates_a_database_error_as_500_over_a_corrupted_store`.
- **DoD `D2.2`/falsificador `F-029-2`** (`ADR-029` linha 71, `tasks.toml:267-268`): os 3 estados do
  store (ausente/0-byte/válido) produzem 3 corpos distintos e códigos `503/503/200`, e o teste de
  "no chão" (`test_ready_refuses_the_connection_when_the_process_is_down`) verifica
  `ConnectionRefusedError`, nunca um `503` lido como falha — os 5 testes de `test_ready_route.py`
  cobrem exatamente essa tabela.
- **`/ingest-health` e o `200` vazio continuam intocados** (`ADR-005/D6.1`, `NG-9` por extensão): o
  diff não toca `ingest_health.py` nem `_fetch`; `test_api_prefix.py` só ganhou `/ready` no conjunto
  de paths esperado do `openapi.json`.
- **Reuso da guarda existente, não duplicação de implementação**: `describe_readiness()` chama a
  MESMA constante `_SELECT_TABLE_PRESENCE` que `_fetch` já usa — não há uma segunda string SQL
  competindo para ficar dessincronizada (`sqlite_ingest_record_store.py:239-266`).
- **Vocabulário de idioma** (`CLAUDE.md`, tabela de fronteira): identificadores, nomes de arquivo,
  docstrings e a única mensagem de exceção nova (`StoreParentDirectoryMissingError`, que é de
  `T-02.1`, não desta task) estão em inglês — nenhuma string nova em português encontrada em
  `ready.py`, `dependencies.py`, ou nos testes novos.

## Veredito

**COMPLIANT** — 0/8 regras bloqueantes violadas nos 9 arquivos do diff; 1 achado `WARNING` (regra
AVISO, pré-existente no pacote, não introduzida por esta task); 18/18 testes visados passam;
`mypy`, `ruff` e os 7 contratos de `import-linter` (incluindo a fronteira de injeção de dependência
que o owner fixou para `infra`) passam limpos.
