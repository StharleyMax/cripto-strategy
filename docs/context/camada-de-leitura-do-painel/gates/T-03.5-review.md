# Review arquitetural — `T-03.5` (`CST-136`) — gaveta de quarentena

Feature: `camada-de-leitura-do-painel` · Fase: `03` · Componente: `web`.
Branch auditada: `tasks/T-03.5-gaveta-de-quarentena` @ `7d5c2d8af5391cda184f6228346ecd1b7ae0e862`
(mesmo SHA já checado em outro worktree; nenhum divergente).
Arquiteto do componente: `.claude/agents/frontend-architect.md` (`harness policy --key agents.by_component`).

## Camada 1 — o que o runner mede

```
harness rules list --severity block                       # 8 regras bloqueantes em vigor
harness rules --mode file --path <cada um dos 5 arquivos alterados de código>
harness rules --mode sweep --changed-only                  # rc=0, saída vazia
```

**Denominador:** 8 regras bloqueantes em vigor no repositório; avaliadas as 8 sobre os 5 arquivos
de produção alterados por esta task (`series-quarantine-query.ts`, `domain.ts`, `S3Inspector.tsx`,
`page.tsx`, `PainelClient.tsx`) + varredura `--changed-only` sobre todo o diff do commit.
**Nenhuma das 8 disparou.** `harness rules --mode sweep` (full, sem `--changed-only`) tem 4 AVISOS
pré-existentes fora do escopo desta task (docstring de módulo em `backend/src/modules/sentimento/...`
e `hardcoded-url` em 4 `*.test.ts` de `frontend/src/app/` não tocados por este commit).

## Camada 2 — julgamento contra a arquitetura declarada

- **`A4` (porta única, `frontend-architect.md` §"A porta única"):** o novo transporte
  (`fetchSeriesQuarantineProjectionViaHttp`) só fala com `GET {API_PREFIX}/series-quarantine`
  (FastAPI). Zero SQL, zero subprocess, zero regra de domínio em `frontend/src/app/painel/page.tsx`.
  **Conforme.**
- **Contrato na borda (`frontend-architect.md` §"O que você decide"):** `assertQuarantineRow` é
  estrito em campo ausente/tipo errado sobre os 9 campos de
  `backend/src/modules/sentimento/domain/series_quarantine_report.py::QuarantineRow.to_wire()`
  (conferido campo a campo, ordem e nomes batem 1:1) e permissivo em campo extra — mesmo padrão de
  `ADR-019/D2` que os dois parsers irmãos (`series-catalog-query.ts`, `collector-status-query.ts`)
  já usam. **Conforme.**
- **`ADR-019/D4` (nunca `NEXT_PUBLIC_*` para URL de backend):** `INGEST_HEALTH_API_BASE_URL`,
  não prefixado. **Conforme.**
- **Fronteira servidor/browser (`web-fullstack.browser-imports-server`):** `import "server-only"`
  no topo do módulo novo; regra mecânica confirma rc=0. **Conforme.**
- **Correção do defeito real que a task nomeia:** antes, a gaveta derivava de `CatalogRow.quarantine`,
  cujo `availableAtPresent` era sempre `false` (`GET /series-catalog` não carrega o termo) — passou
  a ler sua própria fonte (`GET /series-quarantine`, `T-03.4`). `buildQuarantineDrawer` mudou de
  assinatura (`CatalogRow[]` + `labelFor` → `QuarantineSourceRow[]`), e os 4 testes que dependiam da
  assinatura antiga foram reescritos, não apagados (`domain.test.ts`, `view-model.test.ts`,
  conferido no diff). **Conforme com a doutrina "silêncio não é `ok`" que `T-03.3` já havia
  documentado.**
- **`quarantineOk` como sinal distinto de tabela vazia:** thread completo
  `page.tsx → PainelClient.tsx → S3Inspector.tsx`, com parágrafo de erro explícito
  (`role="alert"`, `data-fact="quarantine_ok:false"`) substituindo o corpo da gaveta quando o
  transporte falha — nunca a fixture de divergências (`grep -c FIXTURE_DIVERGENCES
  frontend/src/app/painel/{page,PainelClient}.tsx` → `0`/`0`, reproduzido nesta revisão).
  **Conforme.**
- **`docs/INDEX.md` (append-only):** linha nova acrescentada ao final; nenhuma linha anterior
  reescrita (`git show HEAD -- docs/INDEX.md` conferido — só `+1` linha, contexto das 3 linhas
  anteriores intacto). **Conforme.**

## Achado — não bloqueante, fora do veredito de regra, mas citável

**[WARNING-1] Número inconsistente dentro do próprio arquivo, não uma violação arquitetural mas
uma violação da disciplina do `CLAUDE.md` §"Nenhum número sem o comando que o produziu"** —
`frontend/src/features/s3-inspector/series-quarantine-query.ts:14` diz *"every one of
`ROW_FIELD_KINDS`' 8 entries"*, mas `ROW_FIELD_KINDS` (linhas 84-94 do mesmo arquivo) tem **9**
entradas, e as próprias linhas 57 e 83 do mesmo arquivo dizem corretamente *"9 keys"*. Medido:
`grep -c '\["' <bloco ROW_FIELD_KINDS>` → 9. Correção concreta: trocar "8 entries" por "9 entries"
na linha 14. Não reprova nenhuma regra bloqueante (não há regra mecânica para exatidão numérica em
comentário) e não é violação de camada/direção/contrato — é erro de dígito num comentário
autocontraditório dentro do mesmo arquivo.

## Veredito

**COMPLIANT.** 0 de 8 regras bloqueantes violadas nos 5 arquivos de produção alterados +
varredura `--changed-only` do commit inteiro. 1 achado WARNING (não bloqueante, comentário
autocontraditório) registrado acima.
