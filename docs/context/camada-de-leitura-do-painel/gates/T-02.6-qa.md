# `T-02.6` — QA gate report — `deploy/compose.yml` ganha api/web/caddy, sem `ports:` em `api` (`CST-130`)

**Feature:** `camada-de-leitura-do-painel` · **Fase:** `02` · **Componente:** `infra` · **Data:** 2026-09-07
**Branch:** `tasks/T-02.6-compose-api-web-caddy` — validado no commit `fa2d9a1` (2026-09-07 00:31:44 -0300),
`HEAD` no momento deste gate. `df -h /` no início da sessão: **13G livres** (acima do piso de 3G; depois
de copiar `frontend/node_modules` (1,4G) e `backend/.venv` (163M) da árvore principal: **11G livres**,
ainda folgado).

## 1. Escopo do diff (confirmado, não suposto)

`git diff fa2d9a1^ fa2d9a1 --stat` → só dois arquivos: `.env.example` (+9) e `deploy/compose.yml`
(+78/-9). Nenhuma linha em `backend/src`, `backend/tests` ou `frontend/src` — o que segue abaixo
("regressão do `sentimento`") é rede de segurança, não o falsificador central desta task.

## 2. DoD literal de `T-02.6` (`tasks.toml:320`) — cada item, comando e saída

| item do DoD | comando | resultado |
|---|---|---|
| 3 serviços novos | `grep -cE '^  (api\|web\|caddy):' deploy/compose.yml` | **3** ✓ |
| `config -q` válido | `docker compose -f deploy/compose.yml config -q` (numa réplica isolada em `/tmp/.../qa-compose-t026/` — `backend/`/`frontend/` symlinked, vars obrigatórias exportadas no shell: `POSTGRES_DB/USER/PASSWORD`, `APP_PORT`, `PUBLIC_HOST`; o `.env` real da raiz é gitignored e nunca existe aqui, igual o commit já documenta) | **rc=0** ✓ |
| `api` sem `ports:` | `grep -A12 '^  api:' deploy/compose.yml \| grep -c 'ports:'` | **0** ✓ |
| regra própria não morde o arquivo limpo | `harness rules --mode file --path deploy/compose.yml` | saída vazia, **rc=0** ✓ |
| **MORDE** (controle negativo) | mutação in-place: `POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?...}` → `POSTGRES_PASSWORD: hunter2literalsecret` em `deploy/compose.yml`, depois `harness rules --mode file --path deploy/compose.yml`, depois `git checkout -- deploy/compose.yml` para reverter | `{"decision":"block","reason":"[BLOQUEIO] [own.compose-hardcoded-secret] deploy/compose.yml:33: ... 1 ocorrencia(s) fora da allowlist"}`, **rc=2** ✓ — nomeia exatamente `own.compose-hardcoded-secret` como o DoD exige; arquivo restaurado e confirmado limpo de novo (`git status --short` vazio, `harness rules` volta a rc=0) |
| volumes novos | `git diff fa2d9a1^ fa2d9a1 -- deploy/compose.yml` (seção `volumes:`) | só `caddy_data`/`caddy_config` são adição; `postgres_data` já existia antes deste commit (linha de contexto do diff, sem `+`) ✓ |
| `.env.example` ganha `PUBLIC_HOST`/`PANEL_BASIC_AUTH_HASH` | `git show fa2d9a1:.env.example \| grep -n 'PUBLIC_HOST\|PANEL_BASIC_AUTH_HASH'` | linhas 20/21, ambas vazias (`=` sem valor) — nenhum segredo literal ✓ |
| `deploy/` está em `include_prefixes` (a regra realmente olha o arquivo) | `grep -n 'include_prefixes' harness.toml` | `harness.toml:369`: `["backend/src/", "backend/tests/", "frontend/src/", "deploy/"]` — casa a citação literal do DoD ✓ |

## 3. Regras bloqueantes (8 em vigor, `harness rules list --severity block`)

Só `own.compose-hardcoded-secret` tem alvo direto neste diff (`deploy/`); as outras 7 não têm
superfície no arquivo alterado (YAML de compose, não código Python/TS). Confirmado por
`harness rules --mode sweep` na árvore inteira neste commit: **rc=0**, zero linha `[BLOQUEIO]` (só
`[AVISO]` pré-existentes, não relacionados a este diff: docstring multi-linha em módulos de
`sentimento` e `hardcoded-url` em 4 specs de teste do frontend — nenhum tocado por `T-02.6`).
Regras bloqueantes avaliadas: **8 de 8**.

## 4. Rede de segurança — suíte declarada por `test_cmd` (componente `sentimento`, não `infra`)

`harness policy --key test_cmd` só declara `lint`/`test` para `sentimento`; `infra` não tem alvo
próprio (`harness.toml` linha ~72: *"RÓTULO NÃO É COBERTURA: `infra` no enum NÃO faz `deploy/` ser
medido por nada"*) — o DoD por falsificador da tabela acima **é** a verificação deste componente.
Rodei a suíte de `sentimento` como regressão, já que ela compartilha o `.env` de runtime:

| comando | rc | resultado |
|---|---|---|
| `bash backend/scripts/lint.sh` | **0** | `All checks passed!` / `332 files already formatted` / `mypy: Success: no issues found in 332 source files` |
| `npm --prefix frontend run lint` + `tsc --noEmit --strict` (`make lint-frontend`) | **0** | limpo |
| `bash backend/scripts/test.sh` (`pytest --cov`) | **0** | **1695 passed**, 3 warnings, 363.77s; cobertura total 97.85% (meta 70%); por camada: `domain` 99.8%, `use_cases` 100.0%, `infra` 95.2% — todas acima da meta declarada (`ADR-009/D1`) |

**Nota sobre o pytest de `sentimento`:** nenhum teste em `backend/tests` referencia `.env.example`
(`grep -rln '.env.example' backend/tests` → vazio) e o diff deste commit não toca `backend/src` nem
`backend/tests` — a suíte foi rodada por disciplina de "rodar antes de confiar" (item 1 do
procedimento), não porque `T-02.6` pudesse quebrá-la. Uma primeira tentativa (`timeout 300`) foi morta
em `rc=124` a 12% por contenção real de CPU (3 pytest concorrentes de outros worktrees rodando ao mesmo
tempo, confirmado por `ps aux`); a segunda tentativa, sem `timeout` artificial e após a contenção
esvaziar, completou normalmente.

## 5. `NAO sobe nada` (RN-9) — respeitado

Nenhum comando `docker compose ... up` foi executado, nem contra o arquivo real nem contra a réplica.
O único comando que toca o compose real é `config -q` (parse/validação estática) e `harness rules`
(leitura). A réplica em `/tmp/.../qa-compose-t026/` existiu só para suprir o `env_file: ../.env` que o
compose exige e que o `.env` real (gitignored, nunca criado neste worktree) não fornece — nunca foi
alvo de `up`.

## 6. Veredito

Todos os itens do DoD de `T-02.6` medidos com comando e saída, incluindo o controle negativo (MORDE)
exigido — a regra `own.compose-hardcoded-secret` bloqueia o compose mutado e libera o compose limpo,
provando que o "vazio, rc=0" não é cegueira do instrumento (`ADR-012`). Nenhuma regra bloqueante
reprovou. `APPROVED`.
