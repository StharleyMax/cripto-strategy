# Review arquitetural (front) — Fase 02 — T-02.5

Branch `tasks/T-02.5-server-component-if-none-match`, commit `77a8bf1` (worktree
`wf_0cfc5e74-c0d-21`, `git fetch origin` rodado antes da auditoria). QA já `APPROVED`
(`docs/context/camada-de-leitura-do-painel/gates/T-02.5-qa.md`).

## Escopo do diff (contra `dba4572`, merge-base com `origin/master`)

3 arquivos, todos sob `frontend/src/features/s1-console/`:
`ingest-health-query.ts` (+94/-11), `ingest-health-query.test.ts` (+88/-1),
`ingest-health-query-http.test.ts` (+83/-1) `[MEDIDO: git diff --stat dba4572 HEAD]`.

## Camada 1 — o runner mede

`harness rules --mode file --path <arquivo>` nos 3 arquivos do diff: `rc=0`, saída vazia em cada
um (nenhuma das 8 regras bloqueantes em vigor — `harness rules list --severity block`, universo
completo — dispara). `harness code-paths classify` confirma os 3 como `producao`
(`frontend/src/`). Reproduzi o mesmo resultado do QA de forma independente, não apenas confiei no
gate anterior.

## Camada 2 — julgamento contra a arquitetura declarada

- **`SPEC-003` §3.2** ("`F2` acrescenta `If-None-Match`… `304` ⇒ reutiliza a projeção anterior do
  MESMO PROCESSO, nunca do browser") e **`ADR-029/D4`** (`ETag` = `fingerprint()`, forte, dequote
  de aspas): o cache novo (`processCacheByBaseUrl`, module-scope `Map` keyed por `baseUrl`) bate
  exatamente com o contrato — nunca serializado ao browser, nunca `Next fetch cache`
  (`cache: "no-store"` mantido explícito), nunca mascara `connection_refused`/`non_2xx` (só
  consultado após round-trip bem-sucedido).
- **`SPEC-003` §3.2**: "F2 não acrescenta um 5º `TransportErrorKind`" — confirmado: `304` sem
  cache prévio reusa `malformed_envelope`, nenhum kind novo em `TransportErrorKind`.
- **`ADR-028/D2`**: `import "server-only"` continua linha 1 do módulo, antes até do docstring —
  intocado pelo diff.
- **`ADR-005/D5`/`SPEC-003` §2**: zero `route.ts` (`find frontend/src/app -name route.ts` = 0,
  invariante da SPEC), zero SQL/subprocess no Next, `Next` continua só renderizando —
  `fetchIngestHealthProjectionViaHttp` é a única porta de rede, chamada 1x por render em
  `frontend/src/app/painel/page.tsx:56`, sem `NEXT_PUBLIC_*` (`ADR-019/D4`).
- **Fronteira `charts`↔`web` / cliente↔servidor**: `PainelClient.tsx` (`"use client"`) só importa
  `ingest-health-query.ts` via `import type` — inalterado pelo diff, confirmado por `grep`.
- **`CLAUDE.md` §"mensagem de exceção"**: as duas mensagens novas de `TransportError` (304 sem
  cache) nascem em inglês — sem violação da fronteira de idioma.

## Comandos reproduzidos (independente do QA)

```
npm --prefix frontend run test:s1    → 66/66 pass, 0 fail
npm --prefix frontend run lint       → rc=0, saída vazia
npm --prefix frontend run typecheck  → rc=0, saída vazia
```

## Achados

Nenhum achado com id de regra ou citação de documento além dos já registrados pelo QA
(`[WARNING] D2.6 sem prova end-to-end via `next start` real` — não é violação de regra nem de
arquitetura declarada, é lacuna de cobertura já sinalizada e sem impacto em comportamento).

## Denominador

8 regras bloqueantes em vigor (`harness rules list --severity block`), 8 avaliadas, 3 arquivos
varridos (`harness rules --mode file` × 3 + `--mode sweep --changed-only`). 0 violação.

## Veredito: COMPLIANT

Gravado por mim: `harness gate-record camada-de-leitura-do-painel 02 REVIEW COMPLIANT "..."`.
