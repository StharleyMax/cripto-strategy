# Review arquitetural — T-03.4 (camada-de-leitura-do-painel, fase 03)

**Branch auditada:** `tasks/T-03.4-quarentena-porta-list-all` (`origin`), commit `454a7c9` —
"feat(sentimento,infra,T-03.4): list_all() da quarentena, QUARANTINE_STORE_PATH em src.main e
rota GET /series-quarantine sem points_json", sobre `8e59688` (fase 02, merged em `master`).
Auditado contra `docs/context/camada-de-leitura-do-painel/tasks.toml:402-416` (spec da task),
`docs/specs/SPEC-003-camada-de-leitura-do-painel.md` §3.5/§3.6/§4/§5 e os arquitetos dos dois
componentes declarados (`sentimento`, `infra`) via `harness policy --key agents.by_component`:
`.claude/agents/quant-architect.md` e `.claude/agents/infra-architect.md`.

## Denominador

- Regras bloqueantes em vigor: **8** (`harness rules list --severity block`).
- Arquivos varridos: **7** arquivos de produção do diff (extraídos via `git show <ref>:<path>`
  para um diretório de escopo, dado que a branch já estava checked out em outro worktree) com
  `harness rules --mode sweep --path <dir> --format ndjson` → **rc=0, 0 linhas** (nenhuma
  violação). `git diff origin/master...origin/tasks/T-03.4-quarentena-porta-list-all --stat` →
  **12 arquivos, 666 inserções(+), 16 deleções(-)** (7 produção + 5 teste, incluindo
  `conftest.py`).
- Testes novos lidos integralmente (não executados: branch não fez checkout neste worktree):
  `test_create_app_refuses_missing_quarantine_store_parent.py` (3 casos: CALA/MORDE isolando o
  check de quarentena do de ingest, controle negativo de parent existente/leaf ausente, e o
  comando literal do DoD via `subprocess`), `test_series_quarantine_route_over_the_network.py`
  (3 casos: servidor real em socket loopback, `points_json` ausente, envelope vazio quando o
  store nunca rodou, `ConnectionRefusedError` após teardown), `test_sqlite_series_quarantine_store.py`
  (ampliado, não lido linha a linha).

## Achados

**Nenhuma violação de regra bloqueante** nos 7 arquivos de produção (`core.relative-import`,
`core.silent-except`, `core.print-statement`, `core.hardcoded-secret`,
`web-fullstack.browser-imports-server`, `web-fullstack.tenant-from-request`,
`web-fullstack.server-test-directory-present`, `own.compose-hardcoded-secret`).

- `[WARNING]` **`.env.example` não foi tocado, apesar de a própria task exigir** —
  `docs/context/camada-de-leitura-do-painel/tasks.toml:412` (refs de `T-03.4`): *"arquivos: ...
  .env.example ganha QUARANTINE_STORE_PATH; pytest"*. `SPEC-003` §3.5 já lista a linha na
  tabela normativa (`docs/specs/SPEC-003-camada-de-leitura-do-painel.md:176`):
  `QUARANTINE_STORE_PATH | api (F3) | data/md/series_quarantine.sqlite3 [NÃO SEI: caminho em
  prod — [Q8]]` — e T-03.4 é exatamente a task de fase `03`/`F3` que introduz essa chave em
  código (`src/main/__init__.py::_QUARANTINE_STORE_PATH_ENV_VAR`). `git show
  origin/tasks/T-03.4-quarentena-porta-list-all:.env.example` não contém `QUARANTINE` (0
  ocorrências) — a chave nova existe em código e em `SPEC-003` mas não no arquivo que
  documenta "chaves e defaults não-secretos deste repositório" para quem sobe `make api`.
  Correção concreta: acrescentar a linha `QUARANTINE_STORE_PATH=data/md/series_quarantine.sqlite3`
  ao `.env.example`, no mesmo formato de `INGEST_HEALTH_STORE_PATH`, antes de fechar a task.
  **Severidade WARNING, não BLOCKER**: nenhuma das 8 regras mecânicas cobre `.env.example`
  como superfície (não está em `code_paths.include_prefixes`), e o comportamento em runtime
  (fallback para o default hardcoded) funciona sem a linha — é dívida de documentação
  operacional declarada pela própria task e pela SPEC, não quebra de contrato executável.

## Conformidade arquitetural (julgamento, citado)

- **Fronteira "camada de API é consumidora, não bounded context"** (`[PREMISSA-OWNER:
  2026-09-03]`, `.claude/agents/infra-architect.md:68-75`): `SqliteSeriesQuarantineStore` vive
  em `backend/src/modules/sentimento/infra/` (dentro do bounded context `sentimento`);
  `backend/src/api/dependencies.py` e `backend/src/api/routes/series_quarantine.py` só
  conhecem o `Protocol` `QuarantineSource` (`use_cases/series_quarantine.py`), nunca a classe
  concreta — a concretização acontece só em `src/main/__init__.py::create_app` via
  `app.dependency_overrides`, o mesmo padrão de `T-02.1`/`T-02.3`. Confirmado por leitura direta
  dos imports dos 3 arquivos (`grep -n import` sobre os arquivos extraídos): nenhum deles
  importa `src.modules.sentimento.infra`.
- **`SPEC-003` §4 "`src.api` → `src.modules.sentimento.infra`: proibido direto (só via
  `src.main` injeta)"**: respeitado, mesma evidência acima.
- **`SPEC-003` §4 "`src.api` handler → SQL: proibido, `grep -rn 'SELECT' backend/src/api` = 0"**:
  confirmado — nenhum `SELECT` em nenhum dos 6 arquivos de `backend/src/api/**` na árvore da
  branch (`__init__.py`, `dependencies.py`, `routes/__init__.py`, `routes/ingest_health.py`,
  `routes/ready.py`, `routes/series_quarantine.py`).
- **`D3.2`/DoD "`points_json` nunca vai por padrão"**: a garantia está na query
  (`_SELECT_ALL` em `sqlite_series_quarantine_store.py` não nomeia `points_json` nem `run_id`),
  não em um passo de projeção que poderia esquecer — mesma classe de garantia que
  `INGEST_HEALTH_RUN_COLUMNS` já dá ao relatório de ingest health. `QuarantineRow` (o tipo de
  retorno de `list_all()`) não tem esse campo para vazar por omissão.
- **`ADR-029/D3` "mesma regra de boot" reaplicada, não duplicada**: `_require_parent_directory`
  é uma função nova, compartilhada pelos dois stores (`kind="ingest health"` /
  `kind="series quarantine"`), em vez de duas checagens quase-idênticas que poderiam divergir —
  e o `StoreParentDirectoryMissingError` existente é reusado, não um segundo tipo de exceção.
- **Forma do envelope fechada por `SPEC-003` §3.6**: `to_wire()` produz os campos na MESMA
  ordem que a linha "quarentena" da tabela declara (`source, series_kind, binance_symbol,
  coinalyze_symbol, n_points, recorded_at, terms…`) e os 3 nomes de `terms…`
  (`labelShiftPresent`, `unitPresent`, `availableAtPresent`) casam byte a byte com a interface
  `QuarantineTerms` em `frontend/src/features/s3-inspector/quarantine.ts:15-18` — confirmado
  por leitura direta dos dois arquivos.
- **`NG-9` (nenhuma das 3 rotas de F3 toca as 15 colunas de `ingest_health`)**: o diff não toca
  `ingest_health.py` nem `sqlite_ingest_record_store.py`; `test_api_prefix.py` só ganhou
  `/series-quarantine` no conjunto de paths esperado do `openapi.json`.
- **Vocabulário de idioma** (`CLAUDE.md`, tabela de fronteira): identificadores, nomes de
  arquivo, docstrings e mensagens de exceção novas (`get_series_quarantine_source`'s
  `NotImplementedError`, `_require_parent_directory`'s `StoreParentDirectoryMissingError`) estão
  em inglês; nenhuma string nova em português nos 7 arquivos de produção.

## Veredito

**COMPLIANT** — 0/8 regras bloqueantes violadas nos 7 arquivos de produção varridos; 1 achado
`WARNING` (lacuna de documentação operacional em `.env.example`, exigida pela própria refs da
task e por `SPEC-003` §3.5, mas sem regra mecânica que a alcance e sem quebra de contrato
executável); fronteiras de camada (`src.api` → `infra` só por injeção; zero SQL no handler) e a
forma do envelope (`D3.2`, ordem de campos, nomes de `terms…`) verificadas por leitura direta.
Recomenda-se fechar o `WARNING` antes de mover a task para DONE, mas ele não bloqueia o veredito
arquitetural desta review.
