# Gate — Review arquitetural — `T-03.3` (`CST-134`)

Feature: `camada-de-leitura-do-painel` · Fase `03` · Componente: `web`.
Branch: `tasks/T-03.3-s3-exibe-catalogo` (`origin`, commit `1caaa4c` feat + `0d05d81` qa-doc).
Juiz: [`frontend-architect`](../../../../.claude/agents/frontend-architect.md) (`harness policy --key agents.by_component` → `web.architect`).

## 0. Denominador

- Regras bloqueantes em vigor: **8** (`harness rules list --severity block`) — packs `core` +
  `web-fullstack` + `own`.
- Avaliadas sobre os arquivos do escopo próprio de `T-03.3` (7 arquivos de produção + 2 de teste,
  isolados via `git diff 97c8ac3..1caaa4c --stat`, o commit `feat` contra seu pai `T-03.2`): **8/8**
  (`harness rules --mode file --path <arquivo>`, um a um) — 0 hits em todos.
- Arquivos varridos: `series-catalog-query.ts` (novo), `series-catalog-query.test.ts` (novo),
  `domain.ts`, `view-model.ts`, `view-model.test.ts` (novo), `S3Inspector.tsx`, `page.tsx`,
  `PainelClient.tsx`, `frontend/package.json`, `docs/INDEX.md`, `e2e/01-painel-carrega.spec.ts`,
  `e2e/02-rede-e-estados.spec.ts`, `e2e/04-interacoes.spec.ts`. Comando por arquivo:
  `harness rules --mode file --path <arquivo>` (rc=0, saída vazia em todos).

## 1. Escopo real do commit — confirmado, não citado do build report

`git diff 97c8ac3..1caaa4c --stat` (pai = merge de `T-03.2`, commit-feat = `1caaa4c`): 18 arquivos,
todos sob `frontend/` + `docs/INDEX.md` (1 linha) + 4 screenshots de `gates/e2e-shots/`. **Nenhum
arquivo de `backend/`, `deploy/` ou `docs/adr/` no commit-feat** — confirma a alegação do build
report (`T-03.3-build.md` §5). `docs/INDEX.md`: 1 linha **acrescentada**, nenhuma linha anterior
tocada (`git diff` mostra só `+`) — respeita o registro append-only do `CLAUDE.md`.

## 2. Camada 1 — o que a máquina mede (regras)

0 hits nos 8 IDs bloqueantes sobre todos os arquivos do escopo. Nenhum segredo hardcoded, nenhum
`print`, nenhuma exceção engolida, nenhum import relativo flagado, nenhum tenant-from-request,
nenhum import de servidor a partir do browser. `web-fullstack.server-test-directory-present` não
se aplica (task não toca `backend/`).

## 3. Camada 2 — julgamento contra a arquitetura declarada (`frontend-architect.md`)

1. **Transporte HTTP de leitura, contrato tipado na borda** (seção "o que você decide" do
   arquiteto) — `series-catalog-query.ts` importa `server-only` como primeira linha, parseia
   estrito os 21 campos (6 de `SeriesCatalogEntry` + 15 de `SeriesKey`) antes de delegar a
   `assertValidCatalogEntry` (regra de negócio, camada separada) — **conforme**.
2. **"FastAPI é a única porta de leitura … zero SQL, zero regra de domínio, zero subprocess"**
   (`A4`, mesmo doc) — `fetchSeriesCatalogProjectionViaHttp` só faz `fetch()` contra
   `INGEST_HEALTH_API_BASE_URL` + `API_PREFIX`; nenhum SQL, nenhum `child_process`/`spawnSync` em
   `page.tsx`/`PainelClient.tsx`/`series-catalog-query.ts` — **conforme**.
3. **Fronteira `charts` ↔ `web` (`ADR-003`, D5.12)** — nenhum arquivo do escopo importa
   `**/charts/**`; `eslint.config.mjs` (não tocado por este commit) já bloqueia a direção nos dois
   sentidos, estático e dinâmico — **conforme**, e não é regra nova nem enfraquecida aqui.
4. **Idioma de identificador** (`CLAUDE.md`, tabela de fronteira) — identificadores/comentários em
   inglês (`fullSeriesKeyId`, `catalogRowsFromSeriesCatalogProjection`, docstrings), microcopy de
   UI em português (`"QUARENTENA"`, `"não medido"`, `"nenhuma série em quarentena no momento"`) —
   **conforme** a linha 1/5/8 da tabela. `Provenance` (`OBSERVADO`/`DERIVADO`) é dado de contrato
   herdado do backend (`provenance.py`), não identificador novo — mesmo padrão já aceito para a
   linha 11 da tabela (coluna de contrato), não uma exceção nova criada por esta task.
5. **Escopo respeitado** — os dois achados corrigidos fora do arquivo citado nos `refs` da task
   (`view-model.ts`'s `fullSeriesKeyId`, `S3Inspector.tsx`'s `INTEGRITY_INK_CLASS`) vivem dentro do
   mesmo feature (`s3-inspector/`), documentados como achado encontrado ao vivo (colisão de chave
   React + falha de contraste), não escopo emprestado de outra task — julgamento aceito: ambos são
   consequência direta de `catalog` deixar de ser `[]` nesta mesma task.
6. **Token de design** — `INTEGRITY_INK_CLASS` trocou um hex arbitrário por
   `text-integrity-ink`, token **já existente e já aprovado** em `DESIGN_SYSTEM.md` §1.4-bis
   (conferido: `globals.css` já mapeia `--color-integrity-ink` claro/escuro antes deste commit).
   Nenhuma cor nova introduzida ⇒ não há decisão de UI/UX nova para o `design_gate`
   (`ux-ui-mastery`) validar — é correção de um desvio de um padrão já gated, não uma nova
   autonomia de design exercida sem gate.

## 4. Achados

Nenhum `BLOCKER`. Nenhum `WARNING` novo além do já registrado pelo próprio QA (`T-03.3-qa.md` §3,
sobre a ambiguidade de `--changed-only` num branch já committed — não é desta task, é lacuna do
comando).

## 5. Veredito

**COMPLIANT.** 8/8 regras bloqueantes avaliadas, 0 hits, sobre os 13 arquivos do escopo próprio do
commit `1caaa4c` (confirmado por `git diff 97c8ac3..1caaa4c --stat`, não pelo relato do build).
Camada de julgamento (`frontend-architect.md`): transporte único via FastAPI, contrato tipado
estrito na borda, fronteira `charts`↔`web` intacta, idioma de identificador respeitado, escopo de
arquivo justificado, token de design reusado (não decisão nova) — sem violação citável.
