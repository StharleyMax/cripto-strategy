# Review arquitetural — T-03.2 (camada-de-leitura-do-painel)

**Branch auditada:** `tasks/T-03.2-series-catalog-use-case` @ `53ab28d26cfbbb3ca7f554fa96c4a7e12f1a9e13`
(commit único, `master`/HEAD desta árvore = `b566a68`, NÃO ancestral de `53ab28d` — task ainda não mergeada).

**Comando de descoberta:** `git log --oneline master..53ab28d`; `git show --stat 53ab28d`
→ 9 arquivos, 753 inserções, 5 deleções.

## Camada 1 — regras mecânicas

`harness rules list --severity block` → **8 regras BLOQUEANTES em vigor** (5 `core`, 2
`web-fullstack`, 1 `own`). Universo varrido: os **9** arquivos do diff, extraídos por `git show
53ab28d:<path>` para arquivo temporário (a task não pôde ser `git checkout`ada nesta árvore —
já está em uso por outro worktree) e auditados com `harness rules --mode file --path <arquivo>`.

Resultado: **0/8 regras bloqueantes violadas** nos arquivos novos/modificados
(`backend/src/api/routes/series_catalog.py`, `backend/src/modules/sentimento/use_cases/series_catalog.py`,
`backend/src/api/dependencies.py`, `backend/src/api/__init__.py`, `backend/src/main/__init__.py`,
`backend/tests/api/test_series_catalog_route.py`, `backend/tests/sentimento/test_series_catalog_use_case.py`).

Único aviso mecânico observado (`core.module-docstring-single-line`, severidade **AVISO**, não
bloqueante) dispara na linha 1 de `backend/src/api/__init__.py`, `dependencies.py` e
`main/__init__.py` — mas essa linha **já existia antes deste commit** (o diff só acrescenta
imports/linhas no meio do arquivo); não é violação introduzida por T-03.2.

## Camada 2 — julgamento arquitetural (arquiteto do componente)

Componentes tocados: `sentimento` (`quant-architect`) e `infra` (`infra-architect`, dono de
`backend/src/api/` — `.claude/agents/infra-architect.md`: *"camada consumidora — backend/src/api/,
backend/src/jobs/ — rotas e workers que consomem os módulos por injeção de dependência"*).

- **`D5.13c`** (`docs/plans/SPEC-001-plataforma-dados/05_fatia_visivel.md:85`) — handler não pode
  ser segunda definição da query, 0 SQL. `grep -niE 'SELECT|FROM |psycopg|sqlite3'` sobre
  `routes/series_catalog.py` → 0. **Conforme.**
- **`D5.13d`** (mesmo doc) — `src.api` não importa `src.modules.*.infra`, `src.modules` não importa
  `src.api`. `routes/series_catalog.py` e `dependencies.py` só importam
  `src.modules.sentimento.domain.*` e `src.modules.sentimento.use_cases.*`. **Conforme.**
- **DI stub em `dependencies.py`** segue literalmente o padrão de `get_ingest_record_source`/
  `get_store_readiness_source` (mesma forma: `raise NotImplementedError`, overridden só em
  `src.main.create_app`). **Conforme** com o precedente arquitetural já aceito.
- **Linha 10 da tabela de idioma (`CLAUDE.md`)** — evento de log novo (`series_catalog_query_read`)
  e chave (`n_entries`) nascem em inglês. **Conforme.**
- **Mensagens de exceção** (`CLAUDE.md`, seção "mensagem de exceção") — `NotImplementedError` novo
  em `dependencies.py` nasce em inglês. **Conforme.**

### [WARNING] Divergência do envelope frente ao DoD/SPEC hoje aprovados — `SPEC-003 §3.6` + `tasks.toml:379`

`SPEC-003` §3.6 declara o contrato do catálogo **"fechado na forma"**:
`{"query":"series_catalog","n_entries":7,"entries":[…]}`, e `tasks.toml:379` (T-03.2, `status =
"todo"` nesta árvore) fixa o DoD como equação literal: `curl … -> 7 7 = grep -rn
'SeriesCatalogEntry(' backend/src --include='*.py' | grep -v test | wc -l`.

O código desta branch entrega **`n_entries = 10`** (`entries` = concatenação real dos três
catalog-builders — 3 CVD + 2 price + 5 OI), documentando com medição própria que o "7" do DoD
conta *call sites* de `SeriesCatalogEntry(`, não linhas de catálogo (`open_interest_catalog.py`
constrói 4 das 5 linhas de OI num único list-comprehension). O próprio commit e o teste
`test_series_catalog_use_case.py::test_the_real_catalog_has_ten_rows_not_seven` nomeiam a
divergência e a atribuem à correção do DoD, não a um bug do código — mas, **como está hoje**,
`SPEC-003`/`tasks.toml` (a spec/plano aprovados) e o código **não concordam sobre um contrato
declarado "fechado"**, e nenhuma edição a `SPEC-003`/`tasks.toml` acompanha este commit
(`53ab28d` só toca os 9 arquivos listados acima — nenhum sob `docs/`).

Não é violação de nenhuma das 8 regras bloqueantes (não há `[[rules.own]]` que meça isso), por
isso não muda o veredito por si — mas é achado real de camada 2 (`§3.6` citado, trecho citado) e
a task, como está nesta branch isolada, **não fecha o próprio DoD escrito** (`curl` devolveria
`10 10`, não `7 7`). Nota: já existe trabalho de reconciliação em commits fora desta branch
(`e19e75a`, `5bb1acb`, achados em `git log --all`), mas eles não fazem parte de
`tasks/T-03.2-series-catalog-use-case`/`53ab28d`, o objeto revisado aqui.

## Denominador

- Regras bloqueantes no universo: **8** (`harness rules list --severity block`).
- Regras avaliadas: **8** (mecanicamente, sobre os 9 arquivos do diff).
- Arquivos varridos: **9** (o diff completo do commit `53ab28d`), via `git show 53ab28d --stat` +
  `harness rules --mode file --path` por arquivo extraído.
- Achados de camada 2: **1 WARNING** (divergência de `n_entries` frente a `SPEC-003 §3.6` +
  `tasks.toml:379`, ambos hoje aprovados e não editados por este commit).

## Veredito

**COMPLIANT** — 0/8 regras bloqueantes violadas. 1 WARNING de camada 2 registrado acima
(divergência declarada, não silenciosa, mas pendente de reconciliação formal do DoD/SPEC nesta
branch específica).
