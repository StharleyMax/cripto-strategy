# QA Gate — Fase 01: T-01.4 (`page.tsx` vira Server Component que lê a API)

Commit avaliado: `dadbc02` (branch `tasks/T-01.4-server-component-le-api`, rebase limpo sobre
`origin/master` — rodada 2 do builder, só fechando doc delta; `git diff 371d093 HEAD` sobre os 8
arquivos de produção = 0 linhas, confirmado nesta sessão).

## DoD (`tasks.toml` T-01.4 + plano `01_pagina_diz_a_verdade.md`)

- [OK] D1.1 estrutura — `grep -c '"use client"' page.tsx` → 0; `PainelClient.tsx` → 1;
  `find frontend/src/app -name route.ts | wc -l` → 0; `loading.tsx`/`error.tsx` presentes.
  `not-found.tsx` ausente **por design** — é item `1.6`/`T-01.6` (depends_on `T-01.4`), não
  desta task.
- [OK] D1.3 a página lê a API — medido AO VIVO nesta sessão (não só confiado no gate do builder):
  `bash scripts/e2e-env.sh up 1 18321 18331` (seed 1 run, `uvicorn` real, `next build`+`start`),
  `curl localhost:3000→18331/painel` → access log do `uvicorn` +1 linha `GET /ingest-health`;
  `data-fact="ui_state:ok"`, `data-fact="rows:1"`. Com `up 0` (porta sem listener):
  `error_kind:connection_refused`, `0 <td>` (`grep -c '<td'` → 0).
- [OK] D1.4 conteúdo varia de verdade, não só entre builds — teste mais forte que o do gate do
  builder: com o MESMO build/processo `next start` de pé, adicionei uma 2ª run ao store SQLite
  efêmero (source/endpoint diferente) sem reiniciar nada; `rows:1` → `rows:2` no próximo `curl`,
  provando fetch dinâmico por requisição (`cache:"no-store"`), não HTML congelado — `next build`
  reporta `ƒ /painel` (Dynamic) quando a API está alcançável no build, `○` (Static) só no caso
  `missing_base_url` (nunca chama `fetch`, então o Next não detecta dinamismo — inofensivo,
  porque esse estado não muda por request mesmo).
- [OK] D1.5 três causas sob `next start` — testadas as três manualmente: env unset →
  `error_kind:missing_base_url`; porta sem listener → `connection_refused`; (`non_2xx`/`500`
  coberto pelo teste `MORDE`/`CALA` de `ingest-health-query-http.test.ts`, que já mede a
  distinção no nível do transporte).
- [OK] D1.6/D1.7 — `grep -c '1.6 GB\|99.8%' <main>` → 0; 5 blocos `source:none` (3 em
  `S1Console.tsx`, 2 em `S3Inspector.tsx`, contados linha a linha); `fixtures.ts` fora do grafo
  de produção (`grep -rn 'fixtures.ts' ... | grep -v '\.test\.' | wc -l` → 0).
- [OK] `npm --prefix frontend run lint`/`typecheck` limpos.
- [OK] `test:s1` 60/60, `test:s3` 37/37, `test:app` 97/98 (1 falha pré-existente e alheia —
  `universe-at.test.ts`, fixture `data/snapshots/` gitignored, não relacionada a `/painel`).
- [OK] `harness rules --mode sweep --format ndjson` (repo INTEIRO, não só `--changed-only`) →
  **0 achado `block`** em todo o repositório; 0 achado (warn ou block) tocando qualquer um dos 8
  arquivos desta task. `--changed-only` sozinho dá `rc=0` saída vazia porque a árvore de trabalho
  está limpa (0 diff não commitado) — ambíguo por construção (`CLAUDE.md`), por isso rodei sem
  `--changed-only` e confirmei com uma sonda sintética (import de `backend/` num `.ts` descartável
  sob `frontend/src/`) que o detector realmente dispara quando há violação.
- [PARCIAL] `make verify` (rodada completa, ~10min): `lint-backend` OK (329 arquivos),
  `lint-frontend` OK, `test` OK (**1689 passed, cobertura 97.85%** — backend não tem NENHUMA
  linha tocada por este diff, `git diff 872bd27..HEAD` = 0 arquivo `backend/`), `boundaries` OK
  (7 kept, 0 broken). `regras`/`política` devolveram `rc=3` **RECUSA** (não "passou", não
  "falhou" — "não conseguiu medir"): `.harness/mechanism` não resolve o plugin dentro do path
  deste worktree isolado (`/home/.../.claude/worktrees/wf_...`) — as três vias de registro
  (`$HARNESS_MECHANISM`, `harness.toml [plugin] local_path`, registro do Claude Code) não casam
  com este caminho. **Mesmo gap ambiental já documentado por `T-01.1`/`T-01.2`/`T-01.5` nesta
  mesma feature** — não é causado por este diff, e eu já medi o equivalente substantivo direto
  (`harness rules --mode sweep`, sem passar pelo `.harness/mechanism`): 0 achado `block` no
  repositório inteiro, ver seção seguinte.

## Mutação (a exigência "verde não prova nada até uma mutação reprovar")

Reproduzi o defeito nomeado pelo falsificador da fase (`05_fatia_visivel.md:225`): em
`page.tsx`, troquei o `catch` para **engolir** `TransportError` e sempre produzir
`sourceState = { kind: "ok" }` (código revertido ao final, `git diff` limpo — confirmado).

- `npm run lint` / `typecheck` / `test:s1` (60/60) / `test:s3` (37/37) / `test:app` (97/98):
  **todos permanecem verdes com a mutação** — nenhum teste versionado toca a lógica de
  `page.tsx`. Isto bate exatamente com o que o gate do builder já tinha disclosurado
  (`T-01.4-build.md`), e eu confirmei empiricamente, não só aceitei a palavra dele.
- Replay HTTP ao vivo com a API derrubada e o build mutante: `data-fact="ui_state:ok"` +
  `data-fact="rows:0"` — indistinguível de um store genuinamente vazio. **É exatamente o
  defeito que o DoD existe para proibir**, e só o replay manual/e2e o pega.

**Por que isto não é `NEEDS_FIX` (ao contrário do precedente de `T-01.1` na mesma feature, que
foi `NEEDS_FIX` pelo mesmo tipo de achado):** em `T-01.1` a mutação era num módulo `.ts`
(`ingest-health-query.ts`) — testável hoje, e um fix com 6 testes morde/cala já existia pronto
numa branch local, só não mesclado. Aqui a mutação vive em `page.tsx`, um **Server Component
`.tsx`** — e este toolchain **não consegue importar `.tsx` em `node --test`**
(`ERR_UNKNOWN_FILE_EXTENSION` para `.tsx`, medido nesta sessão tentando `import()` direto; JSX
não é "apagamento de tipo", é transformação de sintaxe, e o Node nativo só apaga tipo). Não há
Vitest/RTL neste repositório ainda — chegam com `T-05.11` (declarado no topo deste mesmo
documento de agente). `S1Console.tsx`/`S3Inspector.tsx` carregam a mesma limitação, no próprio
código: *"not executed by any test in this task, only linted"*. O mecanismo que a FASE declara
para discriminar exatamente esta classe de defeito é `D1.11`/e2e (`make e2e`, `T-01.9`),
propositalmente fora de `verify` (`M5`) e cuja reescrita é tarefa FUTURA e separada
(`tasks.toml` atribui item `1.9` a `T-01.9`, não a `T-01.4`). Exigir aqui um teste que o
toolchain atual não permite escrever seria reprovar `T-01.4` por uma lacuna estrutural do
repositório, já divulgada, não uma omissão desta task.

## Achados

1. [WARNING] Nenhum teste versionado (unitário OU e2e) hoje verifica `error_kind`/`ui_state`
   sobre o caminho de produção real de `page.tsx` — confirmado por mutação. Quando `T-01.9`
   reescrever a suíte e2e (`D1.11`), ela precisa afirmar `data-fact="ui_state:..."` e
   `data-fact="error_kind:..."` contra o servidor real (API de pé/no chão via
   `scripts/e2e-env.sh`), não só bloqueio de rede no nível do browser — `02-rede-e-estados.spec.ts`
   atual mede os dois testes com o mecanismo ERRADO para esta arquitetura (pergunta se o
   BROWSER chama a API, e simula "API no chão" bloqueando requisição do browser, que nesta
   arquitetura nunca existe) e já está marcado para reescrita por `T-01.9` — não bloqueia
   `T-01.4`, mas fica registrado para não se perder.
2. [WARNING] `make verify` recusou medir `regras`/`política` (`rc=3`) neste worktree isolado —
   `.harness/mechanism` não resolve o plugin fora do checkout principal. Pré-existente,
   documentado por `T-01.1`/`T-01.2`/`T-01.5` na mesma feature; medi o substituto direto
   (`harness rules --mode sweep`) e confirmei 0 achado `block`.

## Doc delta

Verificado: `docs/INDEX.md` recebeu 1 linha ACRESCENTADA (não reescreveu nenhuma linha
existente — `git diff` confirma só `+1` no fim do arquivo); `gates/T-01.4-build.md` documenta a
rodada do builder. Nenhum padrão novo de componente/módulo não documentado: `PainelClient.tsx`/
`SourceState`/`error.tsx` como rede de última instância já estavam descritos em `ADR-028/D1`/`D4`
antes do código existir — implementação bate com a decisão já registrada, não decisão nova.
`SourceNoneMarker`/5 blocos `source:none` batem com `ADR-028/D5`. Nenhuma rota nova, nenhuma
mudança de contrato, nenhuma decisão estrutural sem ADR.

## Veredito

**APPROVED.**

Justificativa: os DoD de comportamento (D1.1, D1.3-D1.7) foram medidos AO VIVO nesta sessão, não
só aceitos do gate do builder — incluindo um teste mais forte que o dele (D1.4 via mutação de
dado no store, sem rebuild, provando fetch dinâmico de verdade). `harness rules` dá 0 achado
`block` no repositório inteiro. Lint/typecheck/test:s1/test:s3/test:app batem com o que o
builder relatou. A mutação do falsificador da fase foi reproduzida e prova que só e2e (fora de
escopo desta task, `T-01.9`) discrimina o defeito — gap estrutural do toolchain (`.tsx` não
testável hoje), disclosurado, não omissão desta task, registrado como achado `WARNING` para
`T-01.9` herdar. `make verify` completo: `test`/`lint-backend`/`lint-frontend`/`boundaries`
verdes; `regras`/`política` recusaram medir por gap de resolução do plugin neste worktree
isolado (pré-existente, medido o substituto direto com 0 achado `block`) — mesmo padrão já
aceito em `T-01.1`/`T-01.2`/`T-01.5` nesta feature.
