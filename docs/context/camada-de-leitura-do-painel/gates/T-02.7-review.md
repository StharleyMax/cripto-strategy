# Review arquitetural — Fase 02 [infra] — T-02.7

Branch `tasks/T-02.7-caddyfile-um-host`, commits `ad87e2c` (build) + `992e70f` (teste do QA).
QA: `docs/context/camada-de-leitura-do-painel/gates/T-02.7-qa.md` → APPROVED.

## Universo varrido

`git diff origin/master...HEAD --name-status` → 2 arquivos, ambos `A` (adição, nenhuma
modificação de arquivo existente):
- `deploy/Caddyfile` (45 linhas, commit `ad87e2c`)
- `backend/tests/api/test_api_prefix_moves_caddyfile_and_openapi_together.py` (148 linhas,
  commit `992e70f`)

`git diff origin/master...HEAD -- backend/src` → vazio: **confirmado, o QA não tocou código de
produção**, só o teste que o próprio DoD nomeava como faltante.

## Camada 1 — o que o runner mede

`harness rules list --severity block` → **8 regras bloqueantes em vigor** (`core.relative-import`,
`core.silent-except`, `core.print-statement`, `core.hardcoded-secret`,
`web-fullstack.browser-imports-server`, `web-fullstack.tenant-from-request`,
`web-fullstack.server-test-directory-present`, `own.compose-hardcoded-secret`).

Avaliadas as 8, sobre os 2 arquivos do diff:
- `harness rules --mode file --path deploy/Caddyfile` → vazio, `rc=0`
- `harness rules --mode file --path backend/tests/api/test_api_prefix_moves_caddyfile_and_openapi_together.py` → vazio, `rc=0`
- `harness rules --mode sweep --changed-only` (universo completo do diff) → vazio, `rc=0`

`deploy/Caddyfile` usa só `{$VAR:default}`/`{env.VAR}` (substituição do próprio Caddy) — nenhum
literal de credencial; `own.compose-hardcoded-secret` é N/A (escopo `compose.yml`, não tocado).
`web-fullstack.server-test-directory-present` OK (`backend/tests/` existe). As demais são N/A por
ausência do padrão proibido no diff. **8/8 avaliadas, 0 violação.**

## Camada 2 — arquitetura declarada (infra-architect, `ADR-029`, `SPEC-003`)

Confrontado contra `docs/adr/ADR-029-topologia-da-camada-de-leitura-caddy-proprio-mesma-origem-por-caminho-e-readiness-que-discrimina.md`:

- **D1** (Caddy próprio, estruturado, NÃO implantado): cabeçalho do `Caddyfile` declara
  explicitamente o teto (`caddy validate`) e o falsificador inverso (`docker compose ps` na VPS
  mostrando `caddy`). Nenhum comando de implantação no diff.
- **D2** (mesma origem por caminho; `API_PREFIX` é constante única; CORS não existe): `handle
  {$API_PREFIX:/api/v1}/*` → `api`, resto → `web`; `grep -rn CORSMiddleware backend/src
  --include='*.py' | wc -l` → 0 (`F-029-8`, confirmado por mim, não só citado pelo QA).
- **D6** (exposição pública reservada, decisão do owner): `basic_auth` gated por
  `@authEnabled expression {env.PANEL_BASIC_AUTH_HASH} != ""` — inerte sem valor, 0 código para
  ligar; TLS (`[Q10]`) deixado em comentário, não decidido. Ambos batem com "NÃO decide auth
  nem TLS" do DoD (`tasks.toml:338`).
- **F-029-7** (prefixo é UMA constante, não duas): é exatamente o que o teste novo prova — via
  `caddy adapt` dockerizado comparado contra `_api_prefix_from_environment()`, MAIS um controle
  de mutação (`Caddyfile` embutido que hardcoda `/api/v1` ignorando `API_PREFIX`) que prova que a
  asserção morde de verdade, não compara um número consigo mesmo (`ADR-005/D6`). Sem o controle
  negativo o teste seria vácuo — ele existe.

Nenhuma violação de fronteira de responsabilidade do `infra-architect`
(`.claude/agents/infra-architect.md`): a mudança fica inteira em `deploy/` + `backend/tests/`,
não entra em `backend/src/modules/*` (bounded context), não decide auth/TLS/CORS (que são do
owner ou já fechados por `ADR-028`).

## O que o QA escreveu — confirmado, é só teste

`git diff origin/master...HEAD --name-status` sobre `992e70f` isolado → `A
backend/tests/api/test_api_prefix_moves_caddyfile_and_openapi_together.py`, único arquivo. Leitura
integral do arquivo: 3 testes, importa `_api_prefix_from_environment` de `src.main` (leitura, não
escrita), nenhuma linha em `backend/src/`, nenhuma linha em `deploy/`. `pytest.mark.skipif` quando
`docker` ausente — anomalia declarada, não bypass.

## Denominador

- Regras bloqueantes existentes: **8** (`harness rules list --severity block`)
- Avaliadas: **8/8**, sobre **2 arquivos** varridos (`harness rules --mode sweep --changed-only`
  + 2× `--mode file`)
- Camada 2: **3 decisões da ADR-029** (D1, D2, D6) + **1 falsificador nomeado** (F-029-7)
  confrontados contra o diff, citação de arquivo:linha acima — **0 violação**

## Veredito: COMPLIANT

Nenhuma regra bloqueante violada, nenhuma violação de arquitetura declarada. O QA cumpriu a
restrição de escopo (só teste). Implementação e teste batem com `ADR-029`/`SPEC-003` e com o DoD
literal de `T-02.7` (`tasks.toml:337`).
