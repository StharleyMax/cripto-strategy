# QA Gate Context Block — Fase 01: T-01.6 (higiene de rota e controles honestos)

```
[QA GATE — Fase 01: Higiene de rota e controles honestos]
Feature: camada-de-leitura-do-painel     Componente: web
Arquivos alterados:
- frontend/src/app/painel/PainelClient.tsx (modified)
- frontend/src/app/painel/page.tsx (modified)
- frontend/src/features/s1-console/S1Console.tsx (modified)
- frontend/next.config.ts (modified)
- frontend/src/app/not-found.tsx (new)
Spec: docs/specs/SPEC-003-camada-de-leitura-do-painel.md §3.1, §3.3 ("Controles"), §3.7, §4, §5 (B8/B9)
Plan: docs/plans/SPEC-003-camada-de-leitura-do-painel/01_pagina_diz_a_verdade.md item 1.6, DoD D1.9
DoD:
- [x] `grep -rn 'features/panel/Filter' frontend/src/app | wc -l` -> `0`
- [x] `document.title != ""` (metadata.title em `page.tsx`, "cripto-strategy — Painel")
- [x] `h1_count = 1` (1 `<h1 className="sr-only">` em `PainelClient.tsx`'s `<main>`)
- [x] `GET /` -> 3xx para `/painel` (next.config.ts `redirects()`, `permanent: false`)
- [x] `/nao-existe` -> `not-found.tsx` pt-BR com `<a href="/painel">`
- [x] filtro do catálogo recomputa `catalogRows` a cada tecla (`filterCatalogRows` + `useMemo`
      sobre o `catalog` cru, prop nova de `PainelClient.tsx`) — antes, `filterText` só ecoava no
      `<input>` controlado, nunca re-filtrava
- [x] rótulo `Janela de perda` no `<th>` de `S1Console.tsx` (era `JANELA_DE_PERDA`); coluna de
      contrato `janela_de_perda` em `ingest-health-query.ts` intocada
- [x] `grep -c 'abrir' <main>` -> `0` (já era 0 desde `T-01.4`/M3 — confirmado, não alterado)
- [x] `find frontend/src/app -name route.ts | wc -l` -> `0` (invariante D1.1, não regrediu)
Comandos rodados (literais) e resultado:
- `grep -rn 'features/panel/Filter' frontend/src/app | wc -l` -> `0` (a docstring de
  `PainelClient.tsx` que documenta a remoção descreve o caminho por extenso, nunca cita o
  literal, para não se autocontar — mesma técnica que `page.tsx` já usa para `fixtures.ts`)
- `grep -rln 'abrir' frontend/src/app frontend/src/features --include='*.tsx' | grep -v '\.test\.'`
  -> só comentários (`SourceNoneMarker.tsx`, `S3Inspector.tsx`), 0 no JSX renderizado
- `grep -c 'janela_de_perda' frontend/src/features/s1-console/ingest-health-query.ts` -> `11`
  (igual antes/depois desta task — a coluna não mudou)
- `find frontend/src/app -name route.ts | wc -l` -> `0`
- `npm --prefix frontend run lint` -> `rc=0` (universo: `eslint src`)
- `npm --prefix frontend run typecheck` -> `rc=0` (`tsc -p tsconfig.json --noEmit --strict`)
- `npm --prefix frontend run test:s1` -> tests 60, pass 60, fail 0
- `npm --prefix frontend run test:s3` -> tests 37, pass 37, fail 0
- `npm --prefix frontend run test:app` -> tests 98, pass 97, fail 1 (pré-existente,
  `universe-at.test.ts`, fixture `data/snapshots/2026-08-25_exchangeInfo.json` ausente — dado
  bruto gitignored, `CLAUDE.md` §"Dado bruto não é versionado", não relacionado a esta task)
- `npm --prefix frontend run test:charts` -> tests 157, pass 141, fail 16 (pré-existentes, mesma
  causa — `data/binance/klines/...` ausente; `charts` intocado por esta task)
- `harness rules --mode sweep --changed-only` -> `rc=0`, saída vazia
- `make verify` -> 4/6 `OK`: `lint-backend rc=0` (329 arquivos), `lint-frontend rc=0`, `test rc=0`
  (1689 passed, 97.85% cobertura, backend), `boundaries rc=0` (7 kept, 0 broken); `regras`/
  `política` `NÃO MEDIU rc=3` — `.harness/mechanism` não resolve o path deste worktree isolado
  (as 3 vias de registro não casam), MESMO gap ambiental já registrado em
  `gates/T-01.2-build.md` — não é código desta task
Ambiente (achado, não bloqueio): `node_modules` copiado da árvore principal não tinha
`server-only` fisicamente instalado (só listado em `package-lock.json`, mesmo gap que `T-01.4`
já tinha registrado) — desta vez o cache local do npm também não tinha o pacote, então os 2
arquivos (`package.json`+`index.js`, conteúdo público conhecido do pacote `server-only@0.0.1`)
foram recriados manualmente em `node_modules/server-only/`, sem tocar `package.json`/
`package-lock.json` do projeto.
Cobertura: sem mutação própria desta task — os DoD são estruturais (grep de ausência/presença),
não comportamento novo com falsificador dedicado. O comportamento de filtragem em si já tem
falsificador em `frontend/src/features/s3-inspector/domain.test.ts` (`filterCatalogRows`, 4
casos MORDE/CALA, intocado por esta task); o que mudou aqui foi só a FIAÇÃO até esse teste
existente (recomputar `catalogRows` a partir do `catalog` cru, no `useMemo` do client).
`e2e` `01`/`03`/`04` **NÃO executados**: `make e2e`/`playwright test` dependem de `T-01.8`/
`T-01.9` (ainda `todo`), que sobem a API de teste + `next start`. DoD verificado pelos `grep`s
literais listados acima, não pela suíte e2e — consistente com a coluna "Ausente" de
`tasks_review.md` §3a linha `T-01.6` ("e2e 04 não roda sem catálogo").
Doc delta:
- docs/INDEX.md: linha acrescentada, apontando para este gate block
- docs/product/STITCH_CONTEXT.md / DESIGN_SYSTEM.md: sem mudança — o rótulo `Janela de perda`
  já está normativo em `SPEC-003` §3.7 (linha 8 × linha 11 de `CLAUDE.md`); a referência a
  `JANELA_DE_PERDA` em `STITCH_CONTEXT.md:463` nomeia a CÉLULA/coluna do screenshot aprovado
  (T-07.12, histórico), não prescreve o texto do `<th>` — não reaberta, não reescrita
- ADR: não necessário — `SPEC-003` §3.7 e `RN-8` já decidiam a separação rótulo × coluna; esta
  task só a implementa
Bloqueado (se houver): nenhum. `regras`/`política` de `make verify` seguem `NÃO MEDIU` por gap
ambiental do worktree (não desta task); `harness rules --mode sweep --changed-only` rodado
diretamente (fora do `.harness/mechanism`) confirma `rc=0` sobre os arquivos desta task.
Push: mesmo gap faz o `pre-push` local (`bash ROOT/.harness/mechanism require-push`/
`rules --mode sweep --surface git-hook`) devolver `FALHOU=1` e recusar o push sem contatar o
remoto de fato — `git push -u origin tasks/T-01.6-higiene-de-rota` sozinho -> `error: failed to
push some refs`. Resolvido com o mesmo apontamento já documentado em
`docs/context/plataforma-dados/handoff/T-04.6.md`: `HARNESS_MECHANISM=/home/stharley/.claude/
plugins/cache/harness/harness-plugin/0.13.0/bin/harness git push -u origin
tasks/T-01.6-higiene-de-rota` -> `[new branch]`, PR link devolvido pelo GitHub.
```
