# `T-01.9` — QA gate report — suíte e2e reescrita para a arquitetura (`CST-123`)

**Feature:** `camada-de-leitura-do-painel` · **Fase:** `01` · **Componente:** `web` · **Data:** 2026-09-06
**Branch:** `tasks/T-01.9-suite-e2e-arquitetura` (base `d697bd6`, já em cima do master atual).
**Worktree:** `.claude/worktrees/wf_3060558d-d67-27` — `frontend/node_modules` (1,4G) e
`backend/.venv` (163M) já presentes; `df -h /` no início desta sessão: **27G livres** (o build
report de `T-01.9` (`docs/context/camada-de-leitura-do-painel/gates/T-01.9-build.md`) relatou disco
cheio e **nenhuma execução real** — este gate é a primeira execução de fato.

## 1. O que foi executado

| comando | rc | resultado |
|---|---|---|
| `make -n e2e` | 0 | imprime `playwright test --config=frontend/playwright.config.ts` (`D1.10` ok) |
| `grep -o 'test(' frontend/e2e/*.spec.ts \| wc -l` | — | **17** (`[Q11]`: 17 call-sites estáticos, runner reporta 19 — 2 dentro de loop em `06-viewport` e `02-rede-e-estados`, como o build report documenta) |
| `harness rules --mode sweep --changed-only` | 0 | saída vazia — `frontend/e2e/`/`Makefile` fora de `[code_paths] include_prefixes`, confirmado |
| `make lint-frontend` | 0 | `eslint src` + `tsc --noEmit --strict` limpos |
| `make e2e` (API **de pé**, default) | **2** | **17 passed, 2 failed** de 19 |
| `make e2e E2E_API_UP=0` (API **no chão**) | **2** | **15 passed, 4 failed** de 19 |
| `grep e2e scripts/verify.sh` | — | vazio — `e2e` confirmado FORA de `verify` (M5) |

Logs brutos: `/tmp/claude-.../scratchpad/e2e-up.log`, `e2e-down.log`, `lint-frontend.log` (sessão QA,
não versionado).

## 2. `D1.11` — a suíte discrimina? Parcialmente, e o gap está provado, não suposto

**De pé:** `01`,`02` (todos os `test(` de `B1`-`B6`,`B8`), `03`,`04`,`07` ✓. **Falham dois testes que
NÃO fazem parte de `SPEC-003` §5 `B1`-`B9`** (confirmado por
`grep -n 'B1\|B2\|...\|B9' docs/specs/SPEC-003-camada-de-leitura-do-painel.md:224-232` — nenhuma linha
menciona label/caption/viewport) nem de nenhuma célula da tabela `D1.1`-`D1.11` do plano de fase
(`grep -n 'a11y\|viewport\|caption\|label' docs/plans/.../01_pagina_diz_a_verdade.md` → **vazio**):

- `05-a11y.spec.ts:31` — `expect.soft(labelledInputs).toBe(inputs)` **0 de 1** (filtro só tem
  `placeholder`, sem `aria-label`/`id`+`label`); `expect.soft(tablesWithCaption).toBe(tables)`
  **0 de 2** (nenhuma `<table>` tem `caption`/`aria-label`).
- `06-viewport.spec.ts:34` — a 390px, `tablesWiderThanViewport = 2` (esperado `0`).

Isso **reprova o requisito literal de `D1.11`** ("`make e2e` com API de pé ⇒ **todos** os specs de
`F1` ✓") — não é falha do teste, é gap real de acessibilidade/responsividade em `PainelClient`
(fora do escopo de `T-01.9`, que só toca `frontend/e2e/*` + 1 linha de `Makefile`), mas o DoD da fase
não declara essas duas linhas como exceção conhecida (ao contrário de `D1.7`/`D1.9`, que dizem
"declarado"). `axe-core` (`05-a11y.spec.ts:12`) passa nos dois modos — a violação é mais estrita que
WCAG A/AA, é o padrão que o teste escreveu.

**No chão**, `D1.3`(`B1`)/`D1.4`(`B2`) reprovam como esperado, `D1.5(b)` passa como esperado — o par
que a linha 41 do plano nomeia como falsificador central **não repete `05_fatia_visivel.md:225`**
(vereditos diferentes, confirmado). Mas `D1.6`(`B6`, loading) **passa nos dois modos** — o texto
literal da tabela `D1.11` diz `"no chão ⇒ D1.3/D1.4/D1.6 ✘"`; medido: `D1.6` ✓ em ambos. O build
report declara essa divergência como leitura deliberada de `SPEC-003` §5 canônica (B6 se prova
sozinho via par de stubs, independente do toggle `E2E_API_UP`) — mas **não editou o texto do plano de
fase** para refletir essa leitura, então o documento normativo (`01_pagina_diz_a_verdade.md:36`) e o
comportamento medido divergem, sem reconciliação registrada.

## 3. Regras bloqueantes (8 em vigor)

Nenhuma toca `frontend/e2e/`/`Makefile` diretamente (fora de `code_paths`, confirmado pelo sweep vazio
acima) — `core.print-statement` não se aplica ao `console.log` de `helpers.ts:28` (é o próprio
instrumento `E2E-FACT`, fora do escopo varrido). `web-fullstack.server-test-directory-present`: n/a
(componente `web`, servidor é `backend/`, já com `backend/tests/`).

## 4. Veredito

`D1.11` — o falsificador central da fase ("a suíte discrimina de pé × no chão") **passa** para
`B1`/`B2`. Mas o DoD literal exige **todos** os 19 casos verdes de pé, e **2 reprovam** por gap real
de produção não declarado como exceção; e `D1.6` não discrimina como o texto da tabela promete. Isso é
`NEEDS_FIX` — não porque a arquitetura da suíte esteja errada (o desenho de duas famílias é sólido e
bem documentado), mas porque o estado medido não bate com o DoD escrito, e nenhum dos dois documentos
(plano de fase, build report) foi atualizado para fechar a lacuna.
