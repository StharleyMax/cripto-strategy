# QA Gate — Fase 03: T-03.2 (`list_series_catalog()` + rota `GET /series-catalog`, `CST-133`)

**Feature:** `camada-de-leitura-do-painel` · **Fase:** `03` · **Componente:** `sentimento` + `infra`
**Commit avaliado:** `5bb1acb` (branch `origin/tasks/T-03.2-series-catalog-use-case`), checkout via
`git checkout -b qa-review-T-03.2-19 origin/tasks/T-03.2-series-catalog-use-case` — a branch local
`tasks/T-03.2-series-catalog-use-case` já estava travada noutra worktree (`wf_6fa5da7f-97b-1`),
revisão feita em branch local próprio apontando pro mesmo commit remoto. Ciclo 2 (rodada 1 sobre
`53ab28d` recebeu `QA NEEDS_FIX` — divergência de contagem `n_entries`; ver
`docs/context/camada-de-leitura-do-painel/gates/T-03.2-build.md` §3).

Refs: `docs/context/camada-de-leitura-do-painel/tasks.toml` id="T-03.2"; plano
`docs/plans/SPEC-003-camada-de-leitura-do-painel/03_recursos_baratos.md` item 3.1 / DoD `D3.1`.

## Regras bloqueantes (8 em vigor, `harness rules list --severity block`)

- [OK] `core.relative-import`, `core.silent-except`, `core.print-statement`,
  `core.hardcoded-secret`, `web-fullstack.browser-imports-server`,
  `web-fullstack.tenant-from-request`, `web-fullstack.server-test-directory-present`,
  `own.compose-hardcoded-secret` — `harness rules --mode file --path backend/src/api/__init__.py
  --path backend/src/api/dependencies.py --path backend/src/api/routes/series_catalog.py --path
  backend/src/main/__init__.py --path backend/src/modules/sentimento/use_cases/series_catalog.py`
  → `rc=2` (modo `file` sempre sai != 0 quando há qualquer achado), `reason` = **5x
  `[AVISO] core.module-docstring-single-line`** (docstring multi-linha, achado não-bloqueante já
  aceito no repositório — mesmo padrão de `T-03.6-qa.md`), **0 `[BLOQUEIO]`**. `web-fullstack.*` é
  N/A por ausência de superfície de front/tenant nesta task.

Regras bloqueantes avaliadas: 8 de 8.

## DoD (`tasks.toml` T-03.2 + plano `03` D3.1), comando a comando

- [OK] 0 SQL no handler — `grep -rn 'SELECT' backend/src/api | wc -l` → **0**.
- [OK] `n_entries = len(entries)`, servidor real — teste
  `test_get_series_catalog_serves_the_real_ten_row_catalog_when_the_process_is_up` sobe
  `create_app()` num socket loopback real (`uvicorn.Server` em thread, porta 0) e mede
  `GET /api/v1/series-catalog` → `200`, `n_entries == len(entries) == 10` — **[OK]**. Confirmado
  em execução direta: `backend/.venv/bin/python -m pytest
  tests/api/test_series_catalog_route.py tests/sentimento/test_series_catalog_use_case.py
  --no-cov -q` → **15 passed** (3 rota + 12 use case).
- [OK] contagem de call-sites (distinta de linha, `CA-F2-17`) —
  `grep -rn 'SeriesCatalogEntry(' backend/src --include='*.py' | grep -v test | wc -l` → **7**
  (não 10 — bate com o DoD reconciliado: 7 é call-site, 10 é linha real).
- [OK] `make boundaries` verde — `bash backend/scripts/boundaries.sh` → `Contracts: 7 kept, 0
  broken` (`Analyzed 216 files, 944 dependencies`).
- [OK] DI real (não decorativa) — `test_the_dependency_override_is_genuinely_substitutable`
  injeta catálogo de 1 linha via `app.dependency_overrides` e mede `n_entries == 1` na resposta.
- [OK] camelCase = `series-catalog.ts:134`/`:67` — `test_envelope_entry_field_names_are_camelcase_matching_series_catalog_ts_134`
  e `test_envelope_key_field_names_are_camelcase_matching_series_catalog_ts_67`.
- [OK] `Completeness` fora do fio — `test_envelope_never_carries_a_completeness_field`;
  `grep -n 'Completeness' backend/src/modules/sentimento/use_cases/series_catalog.py` só no
  docstring (texto), não em lógica.
- [OK] `harness tasks validate camada-de-leitura-do-painel` → `OK: 25 task(s), 0 ERROR, 0 WARN`.

## Testes existem e passam

- [OK] `bash backend/scripts/test.sh` (suíte completa) → **`1749 passed, 3 warnings in 398.40s`**,
  `rc=0`. `use_cases/series_catalog.py`: **28/28 linhas, 2/2 branches, 100%**.
- [OK] `bash backend/scripts/lint.sh` → `ruff check` / `ruff format --check` / `mypy --strict`
  limpos: `345 files already formatted`, `Success: no issues found in 345 source files`.

## Cobertura `97.91%` contra alvo `70.0%` (piso, `ADR-009/D1`)

- [OK] Por camada: `domain 99.8%` (meta 90%, 3671/3678), `use_cases 100.0%` (meta 80%, 794/794),
  `infra 95.2%` (meta 70%, 2327/2444) — 3/3 camadas medidas, todas acima do alvo declarado.

## Controle negativo — mutação prova que a suíte MORDE

- [OK] `sed -i 's/entry.native_grid/entry.native_grid_MUTATED/'
  backend/src/modules/sentimento/use_cases/series_catalog.py`, depois `.venv/bin/python -m pytest
  tests/sentimento/test_series_catalog_use_case.py tests/api/test_series_catalog_route.py --no-cov
  -q` → **11 failed** (`AttributeError: 'SeriesCatalogEntry' object has no attribute
  'native_grid_MUTATED'`). Revertido com `git checkout --
  backend/src/modules/sentimento/use_cases/series_catalog.py`; `git status --short` confirma sem
  resíduo.

## DoD da fase — item a item

- [OK] Use case `list_series_catalog()` concatena as 3 fontes reais (`cvd_source_catalog`,
  `price_source_catalog`, `open_interest_catalog`) sem colapsar linhas de OI (`CA-F2-17`
  preservado — 5 linhas, não 3).
- [OK] Rota `GET {API_PREFIX}/series-catalog` registrada, DI via `get_series_catalog_source`
  seguindo o padrão `get_ingest_record_source`/`get_store_readiness_source`.
- [OK] `NAO calcula Completeness (4c, NG-3)` — confirmado, sem lógica de staleness no módulo.
- [OK] Divergência da rodada 1 (`n_entries` 7 vs 10) reconciliada em `SPEC-003`, plano `03`,
  `PRD-003`, `tasks_review.md` e `tasks.toml` — todos com `[MEDIDO 2026-09-07]` e comando citado
  (verificado por leitura direta dos 5 arquivos de doc no diff `origin/master..HEAD`).

## Anomalia

Nenhuma.

Veredito: **APPROVED**

Ações: nenhuma.
