# Gate — QA — `T-03.8` (`CST-139`)

Feature: camada-de-leitura-do-painel

**Feature:** `camada-de-leitura-do-painel` · **Fase:** `03` · **Task:** `T-03.8` — Formatador
único `Intl.NumberFormat("pt-BR")` na apresentação, 0 `toLocaleString` espalhado, fio com ponto
decimal, cabeçalho "Janela de perda" sem tocar a coluna — e2e `07` de locale reescrito.
**Componente:** `web`. **Branch:** `tasks/T-03.8-formatador-unico-pt-br` (commit `2e80219`,
checkout detached neste worktree — a branch já estava aberta em outro worktree em paralelo).

## 1. O que foi verificado (execução real, não releitura do build report)

Setup: `df -h /` → 23G livres antes da cópia (>3G, prosseguiu). `node_modules`/`.venv` copiados de
`cripto-strategy` (não reinstalados) — `diff` de `frontend/package-lock.json` e
`backend/pyproject.toml`/`poetry.lock` contra o checkout principal → 0 linhas nos três. Ao fim,
`df -h /` → 20G livres.

1. **Testes rodados de verdade:**
   - `npm --prefix frontend run test:s1` → **105/105 passed** (bate com o build report, +1 sobre
     `T-03.7`).
   - `npm --prefix frontend run test:s3` → **84/84 passed**, sem regressão.
   - `npm --prefix frontend run lint` / `run typecheck` (`tsc -p tsconfig.json --noEmit --strict`)
     → limpos, sem saída.
2. **Mutação/falsificador aplicado por mim**: reverti `statusCellText`'s `uptimeText` de
   `formatPtBrNumber(row.uptimePercent, 1)` para `row.uptimePercent.toFixed(1)` — o bug EXATO que
   esta task fecha (ponto decimal em uptime%). `npm run test:s1` continuou **105/105 verde** — a
   suíte unitária **não morde** essa regressão porque `statusCellText`/`uptimeText` não tem teste
   unitário direto (achado, ver §3). `make e2e E2E_API_UP=0` (só `07-locale` interessa aqui, não
   depende de API) **morde**: `D3.4` reprovou exatamente como esperado — `"the stubbed 99.8 never
   rendered as a comma decimal at all"`, `commaDecimal.length` `0` (esperado `>0`). Arquivo
   restaurado byte a byte (`git status`/`git diff` → limpos) e suíte voltou a 105/105 e e2e verde.
   Prova que o par morde/cala é real na camada e2e, mas **não** na unitária para este call-site.
3. **`e2e 07-locale` isolado, ao vivo, sem mutação**: `make e2e E2E_API_UP=0` (D3.4/D3.5 declarados
   "servidor ausente: n/a") → 3/3 passed nesse arquivo (outras 3 falhas são `B1`/`B2`/
   `04-interacoes`, esperadas sem API, não relacionadas). `stub_comma_decimal_hits=["9,8"]`,
   `stub_dot_decimal_hits=[]`, `th_janela_de_perda_count=1`, `th_shouting_column_name_count=0`.
4. **`make e2e` completo (API de pé)**, rodado por mim do zero: **21/21 passed** — bate com o
   build report, inclui as 3 asserções de `07-locale` (D3.5 ao vivo, D3.4 via stub próprio,
   ambiente como registro soft).
5. **`make verify`**, com `HARNESS_MECHANISM` exportado (não resolvia sozinho neste worktree):
   **VERDE, 6/6** — `lint-backend` rc=0 (345 arquivos), `lint-frontend` rc=0, `test` rc=0 (1749
   passed, cobertura 97,91%), `boundaries` rc=0 (7 kept/0 broken), `regras` rc=0 (0 bloqueio, 48
   aviso), `política` rc=0. Idêntico ao número do build report.
6. **`harness rules --mode sweep --changed-only`** → `rc=0`, saída vazia. Controle manual sobre o
   diff real (`git diff HEAD~1 HEAD` nos 4 arquivos de código): grep por import relativo,
   `console.log`, `except:` nu, credencial/token — **0 ocorrências**, então o `rc=0` não é vácuo
   aqui (o diff é pequeno e realmente não toca nenhum dos 8 padrões de bloqueio listados por
   `rules list --severity block`).
7. **Grep de contrato, rodado por mim:**
   - `grep -rn toLocaleString frontend/src --include='*.tsx' | grep -v test | wc -l` → `0`.
   - `grep -rn toLocaleString frontend/src` (sem filtro) → 2 ocorrências, ambas em COMENTÁRIO de
     docstring (`view-model.ts:19-20`), não em código executável.
   - `grep -rn 'formatPtBrThousands\|formatPtBrDecimal\|formatDotDecimal'` → 1 ocorrência, também
     docstring (nome antigo citado como histórico).
   - `grep -n "Janela de perda\|JANELA_DE_PERDA" S1Console.tsx` → 1/`0` — rótulo presente, coluna
     nunca gritada.
   - `grep -c '"janela_de_perda"' backend/.../ingest_record.py` → `2`, e `git diff HEAD~1 HEAD
     --stat -- backend/` → vazio (backend não tocado por este diff, então o número não podia ter
     mudado por construção, não só por sorte).
   - Fio (`/collector-status`/`/series-catalog`) com ponto decimal: backend serializa com `json`
     padrão do Python (dot decimal estrutural), e `grep -n 'locale\|format('` nos dois módulos de
     uso do caso `collector_status` não mostra formatação alternativa — a garantia é estrutural,
     não uma medição que este diff frontend pudesse ter quebrado ou consertado.
8. **Leitura de código**: `formatPtBrNumber` é a única função de formatação nova, usada nos 4
   call-sites (`retentionCellText`, `resilienceCellText`, `statusCellText`,
   `buildStorageBudgetView`) — confirmado por grep, nenhum call-site de `S1` ficou no formatador
   antigo. `S3Inspector`/`series-catalog-query.ts` não têm número decimal formatado (só inteiros em
   `completenessText`) — confirmado, nada para migrar lá.
9. **Doc delta**: `docs/INDEX.md` — a linha nova é a última do arquivo, nenhuma linha anterior
   reescrita (`git show HEAD -- docs/INDEX.md` confirma diff só de adição). Nenhuma mudança de
   estrutura/rota/contrato (é reformatação de número, mesma posição/layout) ⇒
   `arquitetura-fluxos.md`/ADR não se aplicam. `STITCH_CONTEXT.md`/`DESIGN_SYSTEM.md`: sem classe
   ou token novo — confirmado por leitura do diff (só `view-model.ts`/`.test.ts`/2 specs e2e
   tocados, nenhum `.tsx` de estrutura).

## 2. DoD (`T-03.8` + plano `03` `D3.4`/`D3.5`) — item a item

- [OK] e2e `07`: `comma_decimal_hits = 0` **ou** `dot_decimal_hits = 0` — provado nos dois
  sentidos (ambiente: ambos 0; stub: comma>0, dot=0) e a mutação de controle confirma que o stub
  reprova quando o formador único é revertido.
- [OK] `grep -rn toLocaleString frontend/src --include='*.tsx' | grep -v test | wc -l` → `0`.
- [OK] `<th>` "Janela de perda", nunca "JANELA_DE_PERDA" — 1/0, medido ao vivo por mim.
- [OK] `janela_de_perda` em `ingest_record.py` inalterado — `2`, backend fora do diff.
- [OK] Fio com ponto decimal — estrutural (serialização Python), não regredido.

## 3. Achados

`WARNING` (não bloqueia `APPROVED`, mas registra lacuna real): a suíte unitária
(`view-model.test.ts`) **não cobre `statusCellText`/`uptimeText` diretamente** — nenhum teste
chama `statusCellText`/`buildCollectorRowView` afirmando o texto exato de `uptimeText` (o teste
novo do formatador único testa `formatPtBrNumber` isoladamente, não o call-site). Minha mutação de
controle no call-site exato (`row.uptimePercent.toFixed(1)` em vez de
`formatPtBrNumber(row.uptimePercent, 1)`) passou despercebida por `test:s1` (105/105, sem falha) e
só foi pega pelo e2e (`07-locale.spec.ts` `D3.4`). Isso funciona hoje porque o e2e existe e é
específico o bastante — mas é o único falsificador para esse call-site; se o e2e for pulado
(`playwright` indisponível, CI sem browser) a regressão exata que esta task fecha volta sem
nenhum teste unitário para pegá-la. Recomendação: um teste unitário de
`statusCellText`/`buildCollectorRowView` afirmando `uptimeText === "99,8%"` para um `uptimePercent`
fracionário fecharia essa lacuna sem depender de browser.

Nenhum `BLOCKER`.

## 4. Veredito

**APPROVED.** DoD fechado item a item com comando próprio, rodado por mim (não só citado do build
report); `make verify` verde (6/6, 1749 passed, 97,91%); `make e2e` verde de pé (21/21) e o
falsificador de `07-locale` confirmado tanto ao vivo (D3.4/D3.5 passam) quanto por mutação de
controle (D3.4 reprova quando revertido); `harness rules --mode sweep --changed-only` rc=0
corroborado por grep manual sobre o diff real; doc delta correto (`docs/INDEX.md` só acrescido,
nenhum padrão novo sem doc). Um `WARNING` registrado: lacuna de cobertura unitária para
`statusCellText`/`uptimeText`, hoje coberta só pelo e2e — não bloqueia porque o e2e existe, roda e
morde de fato (provado por mutação), mas é ponto de atenção para o coordenador/próxima task de
manutenção deste módulo.
