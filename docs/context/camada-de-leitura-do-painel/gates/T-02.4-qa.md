# QA Gate — Fase 02: T-02.4 (`ETag` = `IngestHealthReport.fingerprint()` no handler de `/ingest-health`, `304` sem corpo)

Commit avaliado: `26483c9` (branch `tasks/T-02.4-etag-fingerprint-304`, checkout detached — a
branch já estava sob checkout em outro worktree; `git diff a832856..26483c9 --stat -- backend/`
→ 2 arquivos: `backend/src/api/routes/ingest_health.py` (+33/-6) e
`backend/tests/api/test_ingest_health_etag_and_304.py` (novo, 172 linhas)).

## DoD (`tasks.toml` T-02.4 + plano `02_api_alcancavel_e_honesta.md`, item 2.3, `D2.4`, `D2.5`)

- [OK] `ETag` presente exatamente 1 vez — servido `create_app` ao vivo em
  `127.0.0.1:8811` sobre store SQLite efêmero (`build_run(0)`):
  `curl -sD - -o /dev/null $API/ingest-health | grep -ci '^etag:'` → **1**.
- [OK] `ETag` é validador FORTE (`"<hex>"`, nunca `W/"..."`) e IGUAL a
  `IngestHealthReport.fingerprint()` medido do lado Python **independente** (segunda leitura do
  MESMO store, nunca o número comparado consigo mesmo — `ADR-005/D6`): valor observado
  `"db90e1884c086dc2b0631c02b015990662e501dd0e231d51c4f2588ee47ac1b4"`, igual ao teste
  `test_etag_is_the_strong_fingerprint_and_the_200_body_is_untouched`.
- [OK] CONTROLE NEGATIVO: gravei uma run a mais (`build_run(1)`) no MESMO store, MESMO processo
  servido, sem reiniciar — `ETag` mudou de `"db90e18…"` para `"9ac1063…"`. Prova que o header não
  é constante (o próprio falsificador que a task existe para satisfazer).
- [OK] `If-None-Match` igual ao `ETag` corrente → `304`, corpo vazio (`curl -w '%{size_download}'`
  → `0`), `ETag` repetido no header da resposta.
- [OK] `If-None-Match` com valor errado (`"not-the-real-fingerprint"`) → `200`, corpo cheio,
  `n_runs` inalterado.
- [OK] NG-9 (não acrescenta campo em `runs[]`, corpo do 200 intocado) — `git diff a832856..26483c9`
  sobre `domain/ingest_record.py` e `use_cases/ingest_health.py`: **0 linhas**. Nenhum código de
  domínio novo; o handler só lê `report.fingerprint()`, já existente (`ingest_record.py:177`).
- [OK] `ADR-005/D6.3` (header fora da região hasheada) — `fingerprint()` hasheia
  `canonical_projection()` (`ingest_record.py:173-179`), que não inclui o header; o handler
  computa o header DEPOIS da projeção, nunca antes.

## Testes existem e passam

- `bash backend/scripts/test.sh` (suíte inteira, política `test_cmd.sentimento`, que também
  cobre `backend/src/api/` e `backend/tests/api/` — não há `test_cmd` separado para `infra`) →
  **1700 passed, 3 warnings in 394.02s**. Cobertura total 97.86% (piso declarado 70%); por
  camada (`ADR-009/D1`): domain 99.8% (meta 90%), use_cases 100.0% (meta 80%), infra 95.2% (meta
  70%) — as 3 camadas medidas, as 3 acima do piso.
- `bash backend/scripts/test.sh -k ingest_health` → **39 passed** (subconjunto, só para
  localizar; o veredito de cobertura usa a rodada completa acima, não esta).
- `.venv/bin/python -m pytest tests/api/test_ingest_health_etag_and_304.py -v --no-cov` →
  **5 passed** isolado (arquivo novo desta task).
- `bash backend/scripts/lint.sh` → `ruff` "All checks passed!", `black` "333 files already
  formatted", `mypy` "Success: no issues found in 333 source files".

## Mutação (prova de que o teste morde, não só passa)

Mutei `backend/src/api/routes/ingest_health.py` temporariamente: troquei
`etag = f'"{report.fingerprint()}"'` por uma string constante
(`'"mutated-constant-etag-for-mutation-testing"'`) — o defeito clássico que `ADR-005/D6` nomeia
("ETag que nunca muda passaria por acidente"). Rodei
`pytest tests/api/test_ingest_health_etag_and_304.py -v --no-cov`:

```
FAILED test_etag_is_the_strong_fingerprint_and_the_200_body_is_untouched
FAILED test_etag_changes_when_one_more_run_is_recorded_into_the_same_store
2 failed, 3 passed in 3.20s
```

Restaurei o arquivo original a seguir (`md5sum` antes/depois idêntico:
`c358752c50bec5c161b59a84c12a2280`; `git diff backend/src/api/routes/ingest_health.py` → vazio
após a restauração — nenhuma alteração de produção ficou desta sessão de QA). A suíte volta a
5/5 depois da restauração. Isso prova que o teste novo MORDE um `ETag` constante — não é um
verde vazio.

## Regras bloqueantes

- `harness rules --mode file --path backend/src/api/routes/ingest_health.py` → `decision:
  block`, mas o achado citado é `[core.module-docstring-single-line]` — que é **AVISO**, não
  `BLOQUEIO` (confirmado em `harness rules list --severity warn`), e já existia ANTES desta task
  (`git show a832856:backend/src/api/routes/ingest_health.py` tem o mesmo docstring multi-linha
  no commit-base, antes de `T-02.4` tocar o arquivo). Não é regressão desta task.
- `harness rules --mode file --path backend/tests/api/test_ingest_health_etag_and_304.py` →
  `rc=0`, vazio.
- Nenhuma das 8 regras de severidade `block` (`harness rules list --severity block`) tem escopo
  que esta task toca de forma nova: sem import relativo, sem `except` silencioso, sem `print`,
  sem segredo literal, sem import do servidor pelo browser, sem tenant do request, diretório de
  testes do servidor presente, sem compose.

## DoD da fase (item a item, escopo desta task)

- [OK] `ETag: "<fingerprint>"` no handler — presente, valor confirmado.
- [OK] `If-None-Match` igual ⇒ `304` sem corpo — confirmado.
- [OK] header fora da região hasheada — confirmado por inspeção do `fingerprint()`.
- [OK] não acrescenta campo em `runs[]` (NG-9) — confirmado, 0 diff em domain/use_cases.
- [FALSIFICADOR DA TASK] "igualdade só de um lado" — evitado: o teste novo lê o fingerprint de
  um `SqliteIngestRecordStore` **independente**, nunca comparando o header com o valor que o
  próprio handler calculou em memória. O lado TS é `T-02.5`, fora de escopo aqui.

Regras bloqueantes avaliadas: 8 de 8 listadas por `harness rules list --severity block`

Veredito: **APPROVED**
