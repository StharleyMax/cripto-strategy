# QA Gate — Fase 02: T-02.2 (`API_PREFIX` lido UMA vez em `src.main`)

Commit avaliado: `57475f7` (branch `tasks/T-02.2-api-prefix-uma-vez`), checkout via
`origin/tasks/T-02.2-api-prefix-uma-vez` (a branch já estava travada noutra worktree; revisão
feita em branch local `qa/T-02.2-review-in-wf4` apontando pro mesmo commit — `git log --oneline
-1` = `57475f7`, idêntico).

Refs: `docs/context/camada-de-leitura-do-painel/tasks.toml` (T-02.2, `CST-126`); plano
`docs/plans/SPEC-003-camada-de-leitura-do-painel/02_api_alcancavel_e_honesta.md` item 2.4 / DoD
`D2.7` (e falsificador da fase, sobre `D2.4`, não aplicável a esta task).

## DoD (item 2.4 / `D2.7`)

- [OK] `grep -rn 'API_PREFIX' backend/src/main | wc -l` → **5**; `.env.example` (raiz do repo,
  não `backend/`) → **1** (linha 10, `API_PREFIX=/api/v1`, via `git show HEAD:.env.example`,
  leitura direta bloqueada pela política de permissão do agente para arquivos `.env*`) — total
  **6** ≥ 2.
- [OK] `API_PREFIX=/x` move todo path de `openapi.json` — provado ao vivo (socket real, sem
  `TestClient`): app servido via `uvicorn.Server` em `127.0.0.1:0`, `GET /openapi.json` →
  `paths == {"/x/ingest-health"}`, também coberto por
  `test_api_prefix_env_var_moves_every_openapi_path`.
- [OK] ausência de `API_PREFIX` → default `/api/v1`, nunca raiz — provado ao vivo: `GET
  /api/v1/ingest-health` → `200`; `GET /ingest-health` (sem prefixo) → `404`; `GET /` (raiz) →
  `404` — nunca `200` na raiz nem no bare path.
- [OK] `openapi.json` reflete o prefixo default: `paths == {"/api/v1/ingest-health"}` medido ao
  vivo.
- [OK] Escopo respeitado: não decide `[Q2]` (segmento definitivo), não toca `Caddyfile`
  (`T-02.7`) — confirmado por `git show --stat HEAD` (3 arquivos: `src/main/__init__.py`,
  `tests/api/test_api_prefix.py` novo, `tests/api/test_ingest_health_route_over_the_network.py`
  ajustado para chamar `/api/v1/ingest-health`).

## Controle negativo (mutação) — prova de que a suite NÃO é vazia

Revertido `app.include_router(api_router, prefix=api_prefix)` → `app.include_router(api_router)`
(sem prefixo, regressão a "monta na raiz"). Resultado: **3 testes reprovam**
(`test_default_prefix_is_api_v1_and_the_bare_path_404s`,
`test_api_prefix_env_var_moves_every_openapi_path`,
`test_get_ingest_health_serves_the_fixed_envelope_when_the_process_is_up` — este último porque
passou a chamar `/api/v1/ingest-health` e a rota "sem prefixo" deixa de existir sob a mutação).
Arquivo restaurado a seguir; `git diff --stat` limpo confirmado.

## Regras bloqueantes (`harness rules list --severity block`, 8 regras em vigor)

- [OK] `core.relative-import` — `harness rules --mode file --path backend/src/main/__init__.py`
  e os dois arquivos de teste: 0 achado de import relativo.
- [OK] `core.silent-except` — 0 achado nos 3 arquivos do diff.
- [OK] `core.print-statement` — 0 achado.
- [OK] `core.hardcoded-secret` — 0 achado (nenhum literal de credencial nos 3 arquivos).
- [N/A] `web-fullstack.browser-imports-server` / `web-fullstack.tenant-from-request` —
  escopo `frontend`, task é só backend/infra.
- [OK] `web-fullstack.server-test-directory-present` — `backend/tests/` existe (não regressão).
- [N/A] `own.compose-hardcoded-secret` — task não toca `deploy/compose.yml` (é `T-02.6`).
- [anomalia, não-bloqueante] `harness rules --mode file --path backend/src/main/__init__.py`
  devolve `decision: block` + `[AVISO] core.module-docstring-single-line` (docstring de módulo
  multi-linha) — severidade confirmada como `AVISO` em `harness rules list` (não está na lista
  de 8 regras bloqueantes) e **pré-existente**: idêntico ao rodar a mesma checagem sobre
  `git show 355f816:backend/src/main/__init__.py` (base antes de T-02.2), então não é regressão
  desta task nem motivo de `NEEDS_FIX`.

Regras bloqueantes avaliadas: 8 de 8 listadas por `harness rules list --severity block`
(6 aplicáveis ao escopo do diff, 0 achados; 2 fora de escopo/N-A).

## Testes existem e passam

- `bash backend/scripts/lint.sh` → `ruff` + `mypy --strict`: **"All checks passed!"** /
  **"Success: no issues found in 330 source files"**.
- `cd backend && .venv/bin/python -m pytest tests/api/ -v` → **6 passed** (3 pré-existentes +
  3 novos de `test_api_prefix.py`); universo declarado no plano ("hoje 3") confirmado por
  `grep -c 'def test_' backend/tests/api/*.py` → 1+3+0+2 = 6.
- `bash backend/scripts/test.sh` (test_cmd declarado por `harness policy --key test_cmd` para o
  componente `sentimento`, único runner Python do repo) → **1692 passed, 0 failed, 458.60s**.
- `bash backend/scripts/boundaries.sh` → **7 contratos kept, 0 broken** (inclui D2.11 primeira
  metade: camadas `infra > use_cases > domain`).
- `grep -rn 'SELECT' backend/src/api | wc -l` → **0** (D2.11 segunda metade).
- `grep -rn 'CORSMiddleware' backend/src --include='*.py' | wc -l` → **0** (D2.10).

## Cobertura

Medida por `bash backend/scripts/test.sh` (piso por camada, `ADR-009/D1`):
`[OK] domain 99.8% (meta 90%)`, `[OK] use_cases 100.0% (meta 80%)`, `[OK] infra 95.2% (meta
70%)`, `universo: 3 camada(s) medida(s) de 3 declarada(s)`. Total do arquivo:
`TOTAL 6784 132 1374 37 98%`, piso global `Required test coverage of 70.0% reached. Total
coverage: 97.85%`. Acima do alvo declarado nas três camadas — não há regressão de piso
atribuível a este diff.

## DoD da fase (plano `02`, item 2.4 e `D2.7`) — item a item

- [OK] `API_PREFIX` lido uma vez, em `_api_prefix_from_environment` (única função que toca
  `os.environ` para essa chave).
- [OK] Todas as rotas montadas sob o prefixo via `include_router(api_router, prefix=api_prefix)`
  — ponto único.
- [OK] `openapi.json` reflete o prefixo — provado com default e com `API_PREFIX=/x`.
- [OK] Sem prefixo → 404, nunca 200 na raiz.
- [OK] Escopo: não decide `[Q2]`, não toca `Caddyfile`.

## Anomalia

Nenhuma anomalia bloqueante. A única observação (`core.module-docstring-single-line`, `AVISO`,
pré-existente) está registrada acima e não impede o fechamento — é convenção de estilo, não
regra bloqueante, e não foi introduzida por este diff.

Regras bloqueantes avaliadas: 8 de 8
Veredito: APPROVED
