# `T-03.6` — build report — agregação por série `collector-status` (`CST-137`)

**Feature:** `camada-de-leitura-do-painel` · **Fase:** `03` · **Componente:** `sentimento` + `infra` · **Data:** 2026-09-07
**Branch:** `tasks/T-03.6-agregacao-por-serie-collector-status`, a partir de `origin/master@8e59688`.
**Depends_on:** `T-03.1` (`ADR-030`, done) e `T-02.2` (`API_PREFIX`, done) — ambas já mergeadas.

## 1. O que mudou

Três arquivos novos, um arquivo tocado em 2 linhas, e um teste pré-existente atualizado (o mesmo
padrão que `T-02.3` já usou para `/ready`):

- `backend/src/modules/sentimento/domain/collector_status.py` (novo) — a FORMA do envelope:
  `CollectorStatusRow`/`CollectorStatusReport`, os quatro pares fechados de variantes
  (`LivenessJudged`/`LivenessNotJudged`, `RetentionUnmeasured`/`RetentionNotApplicable`,
  `ResilienceNotScored`/`ResilienceUnavailable`), `MalformedRunTimestampError`, e as constantes
  `ADR-030` nomeia (`STALE_AFTER_MULTIPLIER=3`, `MIN_RUNS_FOR_LIVENESS=3`,
  `MAX_RUNS_FOR_PERIOD=20`, `UPTIME_WINDOW_HOURS=24`).
- `backend/src/modules/sentimento/use_cases/collector_status.py` (novo) — `collector_status_query(source, now)`:
  agrupa `source.runs()` por `(source, endpoint)` (porta `IngestRecordSource` EXISTENTE, zero
  método novo) e aplica `ADR-030` D1 (status/liveness), D2 (uptimePercent na janela trailing 24h),
  D3/D4 (retention/resilience gated só por status).
- `backend/src/api/routes/collector_status.py` (novo) — `GET {API_PREFIX}/collector-status`,
  zero SQL, resolve `now` uma vez (`datetime.now(UTC)`) e repassa como parâmetro — a única
  responsabilidade da rota.
- `backend/src/api/__init__.py` (+2 linhas) — registra o router novo, ao lado dos dois existentes.
- `backend/tests/api/test_api_prefix.py` (editado) — os dois testes que fixam o CONJUNTO de
  paths do `openapi.json` agora incluem `/collector-status` (mesmo ajuste que `T-02.3` fez para
  `/ready`; sem isto os dois quebravam — achado ao rodar a suíte completa, não a suíte isolada).

`to_envelope()`/as 15 colunas de `/ingest-health` **não foram tocados** — nenhum import de
`domain/ingest_record.py` além de `IngestRun` (para o type hint) no use case novo.

## 2. DoD (`tasks.toml` T-03.6 + plano `03` D3.3), comando a comando

- **F-9/NG-9 (sha256 de `/ingest-health` inalterado):** `backend/tests/api/test_collector_status_route.py::test_f9_falsifier_ingest_health_sha256_is_unchanged_by_the_new_route`
  — chama `/ingest-health`, depois `/collector-status`, depois `/ingest-health` de novo sobre o
  MESMO store real (loopback socket, `uvicorn.Server` em thread) e compara `sha256` do corpo —
  **[OK]**, idêntico.
- **Envelope separado, `query == "collector_status"`:** `test_get_collector_status_serves_the_adr_030_envelope_over_the_wire`
  — `200`, `query="collector_status"`, `window_hours=24`, 1 linha por série
  (`build_run` escreve 1 par `(source,endpoint)`), `started_at`/`ended_at` NUNCA vazam na linha.
- **0 SQL no handler:** `grep -rn 'SELECT' backend/src/api | wc -l` → **0**.
- **`make boundaries` verde:** `Contracts: 7 kept, 0 broken` (`Analyzed 214 files, 927 dependencies`).
- **`ARQUIVO`/`PENDENTE` nunca emitidos (F-4):** `grep -rnE '"ARQUIVO"|"PENDENTE"' backend/src | wc -l` → **0**.
- **`computed_uniform`/`measured_sparse`/`declared_constant` nunca emitidos (F-7):** `grep -rnE 'computed_uniform|measured_sparse|declared_constant' backend/src | wc -l` → **0**.
- **`slo_multiplier`/`4,7×` nunca ecoados (F-8):** `grep -rnE 'slo_multiplier|4\.7' backend/src/modules/sentimento` → 9 ocorrências, TODAS pré-existentes em `provenance.py`/`cvd.py` referindo `T-04.7`/`D4.7` (falso-positivo do padrão `4\.7` casando com o identificador de task, confirmado por leitura de cada linha) — **0** no código desta task.
- **openapi.json reflete a rota nova sob o prefixo:** medido ao vivo — `python -c "from src.main import app; print(sorted(app.openapi()['paths']))"` com `INGEST_HEALTH_STORE_PATH` apontando para um diretório existente → `['/api/v1/collector-status', '/api/v1/ingest-health', '/api/v1/ready']`.

## 3. Falsifiers de `ADR-030` D1-D4, um teste por falsificador (`test_collector_status_query.py`, 17 testes)

D1 exemplos (i)/(ii)/(iii) da própria ADR reproduzidos literalmente (24 runs horários, cadência
3600s ⇒ `stale_after_s=10800`); F-1 (regra A: `REJECTED` sempre `PARADO`, mesmo fresquíssimo);
F-2 (nunca `judged`+stale+`ATIVO` juntos); F-4 (fechamento em `{ATIVO,PARADO}`); D2 exemplo
(23 `ACCEPTED` + 1 `REJECTED` em 24h ⇒ `95.83`, não `100`); F-6 (uptime ≠ razão do último run
isolado); sem clamp acima de 100 (defeito do store visível); D3/D4 gated só por status; F-7/F-8
(nenhuma constante de plano ecoada); F-10 (`ended_at` malformado ⇒ `MalformedRunTimestampError`,
nenhuma linha produzida); agrupamento/ordenação por `(source, endpoint)`; mediana usa só os
últimos 20 runs (outlier mais antigo ignorado).

## 4. Mutação (verde não prova nada até uma mutação reprovar)

Mutei `use_cases/collector_status.py`: troquei `if last_run.verdict == "REJECTED":` (regra A) por
`if False:` — desligando a única regra que força `PARADO` por veredito. Rodei
`pytest tests/sentimento/test_collector_status_query.py -v --no-cov`:

```
FAILED test_f1_falsifier_rejected_last_verdict_is_always_stopped
    assert 'ATIVO' == 'PARADO'
FAILED test_d3_d4_stopped_series_is_not_applicable_and_unavailable
    assert 'ATIVO' == 'PARADO'
2 failed, 15 passed
```

A mutação foi mordida exatamente pelos dois testes que deveriam mordê-la. Revertido (`cp` do
arquivo original salvo antes da mutação); `git diff` no arquivo confirmado vazio; suíte isolada
re-executada e voltou a `20 passed` (17 + 3 da rota).

## 5. Comandos rodados (literais) e resultado

- `df -h /` no início: **28G livre** (> 3GB, prossegui). `cp -r … node_modules`/`.venv` do
  checkout principal — `diff` de `backend/pyproject.toml`/`backend/poetry.lock` entre os dois
  checkouts: **vazio** (`rc=0`), reuso seguro, sem reinstalar.
- `bash backend/scripts/test.sh` (suíte completa, com cobertura): **1733 passed, 3 warnings in
  421.75s** (0 failed). Cobertura por camada (`ADR-009/D1`): `domain 99.8%` (meta 90%),
  `use_cases 100.0%` (meta 80%), `infra 95.2%` (meta 70%) — **3 de 3 camadas medidas, todas OK**.
  Cobertura total `97.90%` ≥ piso `70.0%`. `collector_status.py` (domain): 66 stmts, 0 missed,
  **100%**. `collector_status.py` (use_cases): 52 stmts, 0 missed, 8 branches, 0 missed, **100%**.
- `bash backend/scripts/lint.sh`: `ruff check` / `ruff format --check` / `mypy --strict` —
  todos limpos (`341 files already formatted`, `Success: no issues found in 341 source files`).
- `bash backend/scripts/boundaries.sh`: `Contracts: 7 kept, 0 broken` (`Analyzed 214 files, 927
  dependencies`).
- `harness rules --mode sweep --changed-only`: **1 `[AVISO]`** (`core.module-docstring-single-line`
  em `backend/src/api/__init__.py:1`, docstring multi-linha PRÉ-EXISTENTE, severidade `warn` — não
  bloqueia; `harness rules list --severity block` lista 8 regras, nenhuma acesa).
- Isolado, os arquivos desta task: `pytest backend/tests/sentimento/test_collector_status_query.py
  backend/tests/api/test_collector_status_route.py backend/tests/api/test_api_prefix.py -v --no-cov`
  → **23 passed** (17 + 3 + 3).

## 6. Doc delta

**Sem mudança em `docs/INDEX.md`** — motivo explícito: esta task não cria artefato de doc novo
(a ADR já foi registrada por `T-03.1`, `docs/INDEX.md:204`); o mesmo padrão vale para as tasks
`feat` anteriores desta fase (`T-01.1`..`T-02.7`), que também não tocaram o registro. Nenhum ADR
novo necessário — `ADR-030` já decide as fórmulas; esta task apenas as constrói.

## 7. Bloqueado

Nenhum item bloqueado. `D3.0` (pré-condição) já estava satisfeita antes desta task (`ADR-030`
existe, citada em `plano 03` linhas 4/14/38, `T-03.1` `done`).
