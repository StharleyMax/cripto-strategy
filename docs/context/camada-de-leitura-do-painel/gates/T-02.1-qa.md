# `T-02.1` — QA gate report — `create_app` recusa subir com diretório-pai do store ausente (`CST-125`)

**Feature:** `camada-de-leitura-do-painel` · **Fase:** `02` · **Componente:** `infra` · **Data:** 2026-09-06
**Commit avaliado:** `d084ccb` (branch `tasks/T-02.1-create-app-recusa-store-ausente`, base `355f816`).
**Worktree:** `.claude/worktrees/wf_0cfc5e74-c0d-8` — `frontend/node_modules` e `backend/.venv` copiados
de `/home/stharley/Documentos/projects/cripto-strategy` (`poetry.lock`/`package-lock.json` sem diff,
`diff <origem> <worktree>` rc=0 nos dois — reinstalação do zero não foi necessária). `df -h /` no
início: **19G livres**, acima do piso de 3G.

## 1. O que foi executado

| comando | rc | resultado |
|---|---|---|
| `bash backend/scripts/lint.sh` | 0 | `ruff` + `mypy --strict` limpos, 331 arquivos |
| `bash backend/scripts/test.sh` | 0 | **1692 passed**, cobertura total 97,85% (piso 70%); por camada: domain 99,8%/90%, use_cases 100%/80%, infra 95,2%/70% — 3/3 camadas `[OK]` |
| `pytest tests/api/test_create_app_refuses_missing_store_parent.py -v` | 0 | 3/3 (isolado, `INGEST_HEALTH_STORE_PATH` não setado ⇒ `conftest.py` aponta pro tmpdir) |
| `INGEST_HEALTH_STORE_PATH=<scratch>/nao-existe/ih.sqlite3 backend/.venv/bin/python -m src.main; echo $?` | **1** | stderr: `src.main.StoreParentDirectoryMissingError: ingest health store parent directory does not exist: <scratch>/nao-existe` — DoD literal de `T-02.1` e `D2.1` da fase |
| `cd frontend && node --conditions=react-server --test src/features/s1-console/ingest-health-query-http.test.ts` | 0 | **8/8** — DoD literal de `T-02.1` e metade de `D2.3` |
| `harness rules --mode sweep --path backend/src/main/__init__.py` | 0 | 1 `[AVISO]` (`core.module-docstring-single-line`, não-bloqueante); 0 bloqueio |
| `harness rules --mode sweep --path backend/tests/api/test_create_app_refuses_missing_store_parent.py` / `conftest.py` / TS test | 0 | saída vazia nos três — 0 achado |

## 2. Prova do defeito (mutação/controle negativo)

`if not parent.is_dir():` trocado por `if False and not parent.is_dir():` em
`backend/src/main/__init__.py` (só nesta sessão de QA, revertido antes de terminar — `git status`/
`git diff` limpos ao final, confirmado). Com a guarda desligada:

```
tests/api/test_create_app_refuses_missing_store_parent.py::test_create_app_raises_when_the_store_parent_directory_does_not_exist FAILED
tests/api/test_create_app_refuses_missing_store_parent.py::test_running_the_process_exits_non_zero_and_names_the_path_when_the_parent_is_missing FAILED
2 failed, 1 passed in 10.44s
```

O segundo teste (o que roda `python -m src.main` de verdade) trava servindo em vez de sair — a mutação
prova que a suíte nova morde a regressão exata que `ADR-029/D3` proíbe (subir e responder `200
{"n_runs":0}` em vez de recusar). Restaurado o arquivo original (`cp` do backup + `git diff` vazio),
suíte volta a 3/3.

## 3. Regras bloqueantes (8 listadas por `harness rules list --severity block`)

- [OK] `core.relative-import` — `grep -n '^from \.\|^import \.' backend/src/main/__init__.py backend/tests/api/test_create_app_refuses_missing_store_parent.py backend/tests/conftest.py` → vazio
- [OK] `core.silent-except` — nenhum `except` nos arquivos tocados; o `raise` é direto de um `if`, não engole nada
- [OK] `core.print-statement` — `grep -n 'print('` nos três arquivos → vazio; usa `logger.error("ingest_health_store_parent_missing", extra={"path": ...})` (evento novo em inglês, linha 10 da tabela de fronteira do `CLAUDE.md`)
- [OK] `core.hardcoded-secret` — n/a, sem literal de credencial
- [OK] `web-fullstack.browser-imports-server` — n/a, mudança não toca código de navegador
- [OK] `web-fullstack.tenant-from-request` — n/a
- [OK] `web-fullstack.server-test-directory-present` — `backend/tests/` já existia, inalterado
- [OK] `own.compose-hardcoded-secret` — n/a, nenhum arquivo de compose tocado

## 4. DoD (bloco `T-02.1` em `tasks.toml:222-237`)

- [OK] `create_app` lança `StoreParentDirectoryMissingError`, mensagem em inglês nomeando o caminho — `backend/src/main/__init__.py:67-72`
- [OK] `python -m src.main` com pai ausente ⇒ `rc != 0`, stderr nomeia o caminho — comando acima
- [OK] controle negativo: pai existe / arquivo ausente ⇒ `create_app` NÃO lança —
  `test_create_app_accepts_a_store_whose_parent_exists_even_if_the_file_is_absent`, passou
- [OK] `/ingest-health` sobre arquivo ausente continua `200 {"n_runs":0}` (`ADR-005/D6.1` intocado) —
  coberto pela suíte completa (1692 passed inclui `test_ingest_health_route_over_the_network.py` e os
  testes de `_fetch` em `backend/tests/sentimento/`, nenhum regrediu)
- [OK] `node --test .../ingest-health-query-http.test.ts` → 8/8
- [OK] `core.silent-except` / `core.print-statement` — ver seção 3
- [OK] "NAO muda `_fetch` do store nem o 200 vazio de `/ingest-health`" — `git diff --stat 355f816
  d084ccb` toca só `backend/src/main/__init__.py`, `backend/tests/api/...`, `backend/tests/conftest.py`
  e o TS de teste; nenhum arquivo de `sqlite_ingest_record_store.py` no diff

## 5. Nota fora de escopo (não bloqueia `T-02.1`)

`backend/src/main/__init__.py` está sob `omit = ["*/__init__.py"]` em `[tool.coverage.run]`
(`backend/pyproject.toml:108`) — pré-existente, não introduzido por esta task. O arquivo (e portanto a
lógica nova) não aparece na tabela de cobertura por linha, mas os 3 testes de
`test_create_app_refuses_missing_store_parent.py` exercitam a lógica diretamente (unit + subprocesso
real) e a mutação da seção 2 prova que mordem. Mudar o `omit` é decisão de política do repositório, fora
do escopo desta task.

## Veredito

**APPROVED**
