# Review arquitetural — T-02.2 (`API_PREFIX` lido uma vez)

**Branch:** `tasks/T-02.2-api-prefix-uma-vez` (57475f7, sobre `master` 355f816)
**Componente:** `infra` — arquiteto: `.claude/agents/infra-architect.md` (`harness policy --key agents.by_component`)
**Escopo:** 3 arquivos no diff (`git diff --name-only master...tasks/T-02.2-api-prefix-uma-vez`):
`backend/src/main/__init__.py`, `backend/tests/api/test_api_prefix.py` (novo),
`backend/tests/api/test_ingest_health_route_over_the_network.py`.

## Camada 1 — regras mecânicas (`harness rules --mode file --path <arquivo>`)

Rodado nos 3 arquivos do diff (denominador: 8 regras bloqueantes em vigor, `harness rules list
--severity block`, avaliadas contra 3 arquivos):

- `backend/src/main/__init__.py`: 1 achado — `[AVISO] core.module-docstring-single-line`
  (linha 1: docstring de módulo não abre/fecha na primeira linha). **Severidade AVISO, não
  BLOQUEIO** — não está na lista das 8 regras bloqueantes. **Pré-existente**: o docstring
  multi-linha já estava idêntico em `master` (`git show master:backend/src/main/__init__.py`),
  T-02.2 não o tocou — não é dívida introduzida por esta task.
- `backend/tests/api/test_api_prefix.py`: nenhum achado.
- `backend/tests/api/test_ingest_health_route_over_the_network.py`: nenhum achado.

**Zero regras bloqueantes violadas nos 3 arquivos avaliados.**

## Camada 2 — arquitetura declarada

Confrontado contra `ADR-029/D2` (mesma origem por caminho; `API_PREFIX` é constante única, lida
uma vez no composition root) e `SPEC-003` §3.4/M2, e o DoD de `T-02.2` em
`docs/context/camada-de-leitura-do-painel/tasks_review.md:77`:

- **Leitura única no composition root**: `_api_prefix_from_environment()` é a ÚNICA leitura de
  `os.environ["API_PREFIX"]`, chamada uma vez em `app = create_app(..., _api_prefix_from_environment())`
  em `backend/src/main/__init__.py` — confere com `ADR-029/D2` ("lido uma vez, no composition
  root").
- **Rotas montadas sob o prefixo**: `app.include_router(api_router, prefix=api_prefix)` — todas
  as rotas de `api_router` movem juntas.
- **`openapi.json` reflete**: provado em `test_default_prefix_is_api_v1_and_the_bare_path_404s`
  e `test_api_prefix_env_var_moves_every_openapi_path`, sobre socket real (mesmo idioma de
  `test_ingest_health_route_over_the_network.py`, nunca `TestClient`/subprocess — `httpx` não é
  dependência declarada).
- **Ausente ⇒ default, nunca raiz**: `_DEFAULT_API_PREFIX = "/api/v1"`; bare path sem prefixo
  → `404` provado explicitamente (nunca cai na raiz).
- **`.env.example` já tem a chave**: `API_PREFIX=/api/v1` presente (linha 10 do arquivo),
  comentário cita `ADR-029/D2` e menciona o Caddy seguir o mesmo valor.
- **Falsificador `D2.7`** (`tasks_review.md:77`): `grep -rn 'API_PREFIX' backend/src/main
  .env.example | wc -l` = **6** (≥ 2 exigido) `[MEDIDO 2026-09-06]`.
- **`ADR-029/D2` — CORS**: `grep -rn 'CORSMiddleware' backend/src` = 0 ocorrências na árvore
  atual — consistente com "CORS não entra" (nota: esse DoD pertence a `T-02.7`, aqui só
  confirmado como não regredido).
- Contrato de camadas `ADR-009/D6.3` (`src.main` acima de `src.api`) não é tocado por este
  diff — `create_app` continua a única função de composição, parâmetro `api_prefix` segue o
  mesmo padrão de `store_path` (injeção por parâmetro, não leitura de ambiente dentro da
  função).

Nenhuma violação de camada, direção de dependência ou contrato encontrada nas duas camadas.

## Veredito

**COMPLIANT**

**Denominador:** 8 regras bloqueantes em vigor (`harness rules list --severity block`); 3
arquivos no diff avaliados com `harness rules --mode file --path <arquivo>` (todos os 3 do
`git diff --name-only master...tasks/T-02.2-api-prefix-uma-vez`); 0 violações bloqueantes; 1
achado de severidade AVISO (`core.module-docstring-single-line`), pré-existente, não introduzido
por esta task. Camada 2 (arquitetura declarada) confrontada contra `ADR-029/D2`, `SPEC-003`
§3.4/M2 e o DoD de `T-02.2` em `tasks_review.md:77` — sem divergência.
