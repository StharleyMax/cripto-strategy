# QA Gate — Fase 02: T-02.3 (`GET {API_PREFIX}/ready` discrimina três estados do store)

Commit avaliado: `3dbbe94` (branch `tasks/T-02.3-ready-tres-estados`, checkout detached neste
worktree por já estar em uso em outro worktree paralelo — `git log --oneline -1` confirma
`3dbbe94 feat(infra): GET /ready discrimina tres estados do store (T-02.3)`).

Nota: o título de `T-02.3` em `tasks.toml:258` é o item `2.2` do plano `02` (`/ready`), **não**
o item `2.3` (`ETag`/`304`, que é `T-02.4`) — confirmado cruzando `tasks.toml:257-271` com
`docs/plans/SPEC-003-camada-de-leitura-do-painel/02_api_alcancavel_e_honesta.md:11-12`.

## Setup

`df -h /` no início: **8-9,5G livre** (> 3GB, prossegui). `cp -r … node_modules`/`.venv` do
checkout principal (`md5sum` de `poetry.lock`/`package-lock.json` idêntico entre os dois
checkouts, então reuso é seguro, sem reinstalar).

## DoD (`tasks.toml` T-02.3 + plano `02_api_alcancavel_e_honesta.md`, D2.2)

- [OK] **D2.2 três estados, medido AO VIVO nesta sessão** (não só lido do teste): subi
  `python -m src.main` real 3 vezes com `INGEST_HEALTH_STORE_PATH` apontando para (1) arquivo
  ausente, (2) arquivo 0 byte, (3) store inicializado — `curl -s -w '%{http_code}'
  $API/api/v1/ready`:
  - ausente → `HTTP:503`, `{"store":{"path":"…","exists":false,"schema_present":false}}`
  - 0 byte → `HTTP:503`, `{"store":{"path":"…","exists":true,"schema_present":false}}`
  - válido → `HTTP:200`, `{"store":{"path":"…","exists":true,"schema_present":true}}`
  Nenhum outro campo no corpo em nenhum dos três — confirmado por leitura direta do JSON.
- [OK] **D2.2 "no chão"**: depois de `kill` no processo válido, `curl -m2` no mesmo socket →
  `HTTP:000`, `curl` exit `7` (conexão recusada) — nunca um `503` lido como falha de conexão.
  `pytest`: `test_ready_refuses_the_connection_when_the_process_is_down` prova o mesmo por
  `pytest.raises(ConnectionRefusedError)`.
- [OK] **D6.1 intocado**: `curl $API/api/v1/ingest-health` sobre o MESMO store ausente →
  `HTTP:200 {"query":"ingest_health_query","n_runs":0,"n_gaps":0,"runs":[],"gaps":[]}` —
  comportamento pré-`T-02.3` preservado, medido ao vivo.
- [OK] **D2.11 camadas**: `grep -rn 'SELECT' backend/src/api` → 0 linhas (nenhum SQL na camada
  de API, `describe_readiness()` mora em `infra`); `make boundaries` → `Contracts: 7 kept, 0
  broken`.
- [OK] **core.silent-except**: `describe_readiness()` não captura `sqlite3.DatabaseError`;
  `test_ready_propagates_a_database_error_as_500_over_a_corrupted_store` e
  `test_describe_readiness_over_a_corrupted_store_keeps_failing_loudly` prova propagação (500 /
  `pytest.raises`), reusando a MESMA técnica de corrupção de
  `test_ingest_record_crash_borders.py`.
- [OK] `test_api_prefix.py` atualizado: os dois testes que fixam o conjunto de paths do
  `openapi.json` agora incluem `/ready` — único ponto que a rota nova atinge (confirmado por
  `git show 3dbbe94` no arquivo).

## Regras bloqueantes — evidência

`harness rules list --severity block` → 8 regras em vigor. `harness rules --mode sweep` (repo
inteiro) → **0 `BLOQUEIO`** (`grep -c BLOQUEIO` → 0 sobre 86 linhas de saída, todas `[AVISO]`
pré-existentes e alheias a esta task — ex.: `core.module-docstring-single-line` em módulos que
não fazem parte deste diff). `harness rules --mode file --path
backend/src/api/routes/ready.py` e `--path backend/src/api/dependencies.py` só acusam o mesmo
`AVISO` de docstring multi-linha (severidade `warn`, não bloqueia); `--path
backend/src/modules/sentimento/infra/sqlite_ingest_record_store.py` e `--path
backend/tests/api/test_ready_route.py` → vazio.

Regras bloqueantes avaliadas: 8 de 8 listadas por `harness rules list --severity block`.

## Testes existem e passam

- `bash backend/scripts/test.sh` (suite completa, com cobertura): **1705 passed, 3 warnings in
  402.05s**. Cobertura por camada (`ADR-009/D1`): `domain 99.8%` (meta 90%), `use_cases 100.0%`
  (meta 80%), `infra 95.2%` (meta 70%) — **3 de 3 camadas medidas, todas OK**. Cobertura total
  `97.86%` ≥ piso declarado `70.0%`.
- `bash backend/scripts/lint.sh`: `ruff check`/`ruff format --check`/`mypy --strict` — todos
  limpos (`334 files already formatted`, `no issues found in 334 source files`).
- Isolado, os arquivos tocados por `T-02.3`: `pytest backend/tests/api/test_ready_route.py
  backend/tests/api/test_dependencies.py backend/tests/sentimento/test_ingest_record_crash_borders.py
  backend/tests/api/test_api_prefix.py -v --no-cov` → **18 passed** (5 + 2 + 8 + 3).

## Mutação (a exigência "verde não prova nada até uma mutação reprovar")

Mutei `backend/src/api/routes/ready.py:42` trocando `exists and schema_present` por `exists or
schema_present` (o defeito exato que o D2.2 existe para proibir: 200 sobre um store 0-byte
"half-born"). Rodei `pytest backend/tests/api/test_ready_route.py -v --no-cov`:

```
backend/tests/api/test_ready_route.py .F...    [100%]
FAILED test_ready_is_503_with_schema_present_false_over_a_zero_byte_store
    assert status == 503
    E   assert 200 == 503
```

A mutação foi mordida por exatamente o teste que deveria mordê-la — `1 failed, 4 passed`.
Revertido logo em seguida (`cp` do arquivo original salvo antes da mutação); `git diff` no
arquivo confirmado vazio; suíte re-executada e voltou a `5 passed`.

## Achados

Nenhum `NEEDS_FIX`. 1 observação não-bloqueante: os dois `AVISO`
`core.module-docstring-single-line` nos arquivos novos (`ready.py`, `dependencies.py`) são o
mesmo padrão de docstring multi-linha que já existe em ~16 outros módulos do repositório
(medido por `harness rules --mode sweep`) — não é regra bloqueante, não introduzido
seletivamente por esta task, e portanto não motivo de `NEEDS_FIX`.

## Doc delta

Nenhuma mudança em `docs/` além deste gate — `T-02.3` é puramente código+teste; `tasks.toml`
não foi editado à mão (fica para o `harness tasks resolve` no fechamento da fase, não neste QA
individual de task).

## Veredito

**APPROVED.**

Justificativa: os 3 estados de `/ready` foram medidos AO VIVO (curl real sobre processo real,
não só lidos do pytest) e batem com D2.2; `/ingest-health` (D6.1) confirmado intocado sobre o
mesmo store; camadas (`make boundaries`) e regras bloqueantes (`harness rules --mode sweep`, 0
BLOQUEIO) verdes; suíte completa (1705 testes) e lint (`ruff`+`mypy --strict`) verdes; cobertura
por camada acima da meta nas 3 camadas declaradas. A mutação do falsificador do D2.2 (trocar
`and` por `or` na condição do status code) foi reproduzida e mordida por um teste já existente
na suíte, provando que o verde não é vacuidade.
