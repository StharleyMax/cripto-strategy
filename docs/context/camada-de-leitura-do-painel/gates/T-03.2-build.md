# `T-03.2` — build report — use case `list_series_catalog()` + rota `GET /series-catalog` (`CST-133`)

**Feature:** `camada-de-leitura-do-painel` · **Fase:** `03` · **Componente:** `sentimento` + `infra` · **Data:** 2026-09-07
**Branch:** `tasks/T-03.2-series-catalog-use-case`, reconstruída por **merge** de `origin/master@b566a68`
para dentro do commit remoto existente (`53ab28d`) — não por rebase/cherry-pick — para que o push
seja fast-forward (`git merge-base --is-ancestor origin/tasks/T-03.2-series-catalog-use-case HEAD`
→ verdadeiro). Motivo: um rebase local ficaria com histórico divergente do que já está em
`origin/tasks/T-03.2-series-catalog-use-case`, e o `pre-push` deste repositório (`.harness/mechanism`
→ `harness require-push`) recebe stdin **vazio** do git para esse caso específico nesta sessão
(`[push] RECUSADO — nenhuma ref no protocolo do pre-push`) — reproduzido de forma determinística com
`GIT_TRACE2` (o hook roda, `make lint`/`boundaries`/`natureza` executam de verdade, mas o `ENTRADA=$(cat)`
inicial do hook chega vazio), confirmado independente de transporte (SSH/HTTPS), de redirecionamento de
I/O e com `dangerouslyDisableSandbox`. Replay manual do hook com stdin correta (`bash .git/hooks/pre-push
origin <url> < <refs-reais>`) **passa, rc=0** — a suíte de portões em si está íntegra; é a entrega do
protocolo do pre-push pelo `git push` desta sessão que falha nesta condição (histórico não-descendente
do ref remoto). `--force`/`--force-with-lease` e `gh api ... DELETE` (a via que este mesmo achado já
documentou para apagar branch, `docs/context/plataforma-dados/gates/T-05.5-builder.md:39`) estão
bloqueados pelo classificador de auto mode desta sessão — não usados. A saída sem nenhum destes dois é
reconstruir o branch como descendente direto de `53ab28d` (este merge), o que faz o `git push` normal
virar fast-forward e não depender do caminho de código que falha.
**Ciclo:** 2 (rodada 1 recebeu `QA NEEDS_FIX` sobre `53ab28d` — ver §3).

## 1. O que mudou

`git merge origin/master` sobre `53ab28d` (1 conflito textual em `test_api_prefix.py`, resolvido
unindo as duas listas de paths — `T-03.6` chegou a `master` entre as duas rodadas e tocava as mesmas
duas asserções de conjunto). Nenhuma linha de
código de produção mudou de comportamento nesta rodada — a rodada 1 já estava correta no código
(ver §3); o que mudou foram os **três documentos** que a rodada 1 deixou divergentes de propósito
para o QA reconciliar.

- `backend/src/modules/sentimento/use_cases/series_catalog.py` (novo) — `list_series_catalog()`
  concatena as linhas reais de `build_cvd_source_catalog_entries` (3) + `build_price_series_entries`
  (2) + `open_interest_catalog_entries` (5) = **10**; `series_catalog_envelope()` projeta para o
  fio (`n_entries = len(entries)`, computado, nunca literal).
- `backend/src/api/routes/series_catalog.py` (novo) — `GET /series-catalog`, 0 SQL.
- `backend/src/api/dependencies.py` (+`get_series_catalog_source`, stub que levanta
  `NotImplementedError` até `src.main` sobrescrever).
- `backend/src/main/__init__.py` — `create_app` constrói `list_series_catalog()` uma vez e
  registra o override.
- `backend/src/api/__init__.py` — registra o router novo.
- `backend/tests/api/test_api_prefix.py` — as duas asserções de conjunto de paths agora incluem
  `/collector-status` (`T-03.6`) **e** `/series-catalog` (esta task) juntas.
- `backend/tests/api/test_dependencies.py`, `backend/tests/api/test_series_catalog_route.py` (novo,
  3 testes), `backend/tests/sentimento/test_series_catalog_use_case.py` (novo, 12 testes).

**Doc delta (o que esta rodada corrigiu, não o código):**
`docs/specs/SPEC-003-camada-de-leitura-do-painel.md` (§0.1#7 nota, §3.6 linha "catálogo", §5 B14),
`docs/plans/SPEC-003-camada-de-leitura-do-painel/03_recursos_baratos.md` (item 3.1, 3.2, D3.1),
`docs/context/camada-de-leitura-do-painel/tasks.toml` (T-03.2 title+refs+DoD, T-03.3 title+DoD —
T-03.3 ainda não implementada, só consistência textual). Ver §3.

## 2. DoD (`tasks.toml` T-03.2 + plano `03` D3.1), comando a comando

- **`n_entries = len(entries)`, servidor real:** `test_get_series_catalog_serves_the_real_ten_row_catalog_when_the_process_is_up`
  serve `create_app()` num socket loopback real (`uvicorn.Server` em thread) e mede
  `GET /api/v1/series-catalog` → `200`, `body["n_entries"] == 10 == len(body["entries"])` —
  **[OK]**, mas **10, não 7** (ver §3 — o achado que motivou `NEEDS_FIX` na rodada 1).
- **0 SQL no handler:** `grep -rn 'SELECT' backend/src/api | wc -l` → **0**.
- **DI real, não decorativa:** `test_the_dependency_override_is_genuinely_substitutable` injeta
  um catálogo de 1 linha via `app.dependency_overrides` e mede `n_entries == 1` na resposta —
  prova que a rota lê o valor injetado, não um `list_series_catalog()` chamado direto.
- **camelCase = `series-catalog.ts:134`/`:67`:** `test_envelope_entry_field_names_are_camelcase_matching_series_catalog_ts_134`
  e `test_envelope_key_field_names_are_camelcase_matching_series_catalog_ts_67` comparam o
  conjunto de chaves exato.
- **`Completeness` fora do fio:** `test_envelope_never_carries_a_completeness_field`.
- **`make boundaries` verde:** `Contracts: 7 kept, 0 broken` (`src.api` não importa
  `src.modules.sentimento.infra` direto — só `src.main`, via `ADR-009/D6.3`).
- **Não calcula `Completeness` (NG-3):** nenhuma chamada a lógica de medição de staleness neste
  módulo — `grep -n 'Completeness' backend/src/modules/sentimento/use_cases/series_catalog.py`
  não aparece (só no docstring, como texto).

## 3. O achado da rodada 1 — reconciliado, não silenciado

`git show 53ab28d` já continha o código correto e o QA da rodada 1 (`NEEDS_FIX`) mediu, ao vivo:

```
curl -s $API$PREFIX/series-catalog | python3 -c 'import json,sys;d=json.load(sys.stdin);print(d["n_entries"],len(d["entries"]))'
# -> 10 10   (nao "7 7", como tres documentos escreviam)
grep -rn 'SeriesCatalogEntry(' backend/src --include='*.py' | grep -v test | wc -l
# -> 7   (contagem de CALL-SITES, nao de linhas do catalogo)
```

`open_interest_catalog.py` builda 4 das 5 linhas de OI a partir de **1** list-comprehension sobre
`Reduction.OPEN/HIGH/LOW/CLOSE` (`CA-F2-17`, herdado de `plataforma-dados/T-06.5`: "quatro linhas
de catálogo, não três" para a Coinalyze, mais o `POINT` da Binance = 5). `SPEC-003`/plano/
`tasks.toml` escreveram a equação `n_entries = grep -c 'SeriesCatalogEntry('`, assumindo 1 call-site
= 1 linha — verdade para `cvd_source_catalog` (3=3) e `price_source_catalog` (2=2), falsa para
`open_interest_catalog` (2 call-sites, 5 linhas). `3+2+5=10 ≠ 7`.

O QA da rodada 1 recomendou explicitamente **não** colapsar o código para bater com `7` (isso
descartaria 3 séries reais de Open Interest, a falha exata que `CA-F2-17` proíbe) e pediu
reconciliação dos três documentos **ou** justificativa explícita para `7`. Nesta rodada:

- **Reconciliados** (`[MEDIDO 2026-09-07]`, comando citado em cada local): `SPEC-003` §0.1#7 (nota
  que distingue call-site de linha, mantendo o **7** original — essa medição continua verdadeira),
  §3.6 (linha "catálogo": `n_entries:7`→`n_entries:10`, com a fórmula `3+2+5`), §5 B14 (`n_entries
  = 7 = grep -c(...)` → `n_entries = len(entries) = 10`, com o `curl` real); plano `03` item 3.1/3.2
  e `D3.1`; `tasks.toml` T-03.2 (title, refs, DoD) e T-03.3 (title, DoD — consistência textual, essa
  task segue `status = "todo"`, não implementada aqui).
- **Não alterado:** nenhum `status`, `depends_on` ou `tracker` de nenhuma task; nenhuma outra SPEC;
  nenhum código de produção além do já presente em `53ab28d`.
- `harness tasks validate docs/context/camada-de-leitura-do-painel/tasks.toml` → `OK: 25 task(s),
  0 ERROR, 0 WARN` depois da edição.

## 4. Comandos rodados (literais) e resultado

- `bash backend/scripts/test.sh` → **`1749 passed, 3 warnings in 412.45s`**, `rc=0`. Cobertura por
  camada (`ADR-009/D1`): `domain 99.8%` (meta 90%, 3671/3678 linhas), `use_cases 100.0%` (meta 80%,
  794/794 linhas), `infra 95.2%` (meta 70%, 2327/2444 linhas) — 3/3 camadas medidas, todas acima do
  alvo. Cobertura total `97.91%` ≥ piso `70.0%`. `use_cases/series_catalog.py`: **28/28 linhas,
  2/2 branches, 100%**.
- `bash backend/scripts/lint.sh` → `ruff check`/`ruff format --check`/`mypy --strict` limpos — `345
  files already formatted`, `Success: no issues found in 345 source files`.
- `bash backend/scripts/boundaries.sh` → `Contracts: 7 kept, 0 broken` (`Analyzed 216 files, 944
  dependencies`).
- `harness rules --mode sweep --changed-only` → `rc=0`; **0** `[BLOQUEIO]`, **5** `[AVISO]`
  (`core.module-docstring-single-line` nos 5 arquivos de produção tocados — mesmo achado
  não-bloqueante já registrado em `T-03.6-qa.md`, docstring multi-linha é o padrão deste módulo).
- **Mutação de controle negativo** (prova que a suíte MORDE, não só que passa): `sed -i
  's/entry.native_grid/entry.native_grid_MUTATED/'
  backend/src/modules/sentimento/use_cases/series_catalog.py`, depois
  `.venv/bin/python -m pytest backend/tests/sentimento/test_series_catalog_use_case.py
  backend/tests/api/test_series_catalog_route.py --no-cov -q` → **11 failed** (`AttributeError:
  'SeriesCatalogEntry' object has no attribute 'native_grid_MUTATED'`), depois `git checkout --
  backend/src/modules/sentimento/use_cases/series_catalog.py` (revertido; `git status --short`
  confirma o arquivo de volta a `A` sem diff extra).
- `grep -rn 'SeriesCatalogEntry(' backend/src --include='*.py' | grep -v test | wc -l` → **7**
  (call-sites, universo = 3 arquivos: `cvd_source_catalog.py`, `price_source_catalog.py`,
  `open_interest_catalog.py`).

## 5. Regras bloqueantes (8 em vigor, `harness rules list --severity block`)

Avaliadas sobre o diff desta task (`sweep --changed-only`, §4): `core.relative-import`,
`core.silent-except`, `core.print-statement`, `core.hardcoded-secret`,
`web-fullstack.browser-imports-server`, `web-fullstack.tenant-from-request`,
`web-fullstack.server-test-directory-present`, `own.compose-hardcoded-secret` — **8/8 avaliadas**,
0 disparo. As 4 de `web-fullstack` são N/A por ausência de superfície (nenhum frontend tocado nesta
task, componente = `sentimento`+`infra`).

## 6. O que esta task NÃO faz (herdado do plano `03`)

Não calcula `Completeness` (`4c`, `NG-3`) — confirmado por ausência de lógica de medição de
staleness no use case. Não decide `[Q5]` (paginação) nem toca as 15 colunas de `ingest_health`
(nenhum import de `domain/ingest_record.py` neste módulo).
