# QA Gate (Front) — Fase 02: A API é alcançável e honesta — T-02.5

Branch `tasks/T-02.5-server-component-if-none-match`, commit original `42a6be3` (origin), rebaseado
sobre `origin/master` sem conflito ⇒ `77a8bf1` (worktree `wf_0cfc5e74-c0d-21`). Merge-base era
`ff8ef4a` (T-02.4, mergeada); origin/master trouxe `T-02.6`/`T-02.3` (infra, arquivos distintos —
`deploy/compose.yml`, `.env.example`, `backend/src/api/...`), zero conflito no rebase
`[MEDIDO: git rebase origin/master → "Successfully rebased", 0 conflito]`.

## DoD da fase (item 2.5 + metade TS de 2.9), comando a comando

- **D2.4 (lado TS + paridade dois lados):** `etag` (dequotado) `==` `fingerprint(parseIngestHealthEnvelope(body))`
  **e** `== IngestHealthReport.fingerprint()` do lado Python, medido no MESMO teste
  (`ingest-health-query-http.test.ts:573-607`, `CALA T-02.5`) — **[OK]**. Controle negativo já
  coberto pelos testes `MORDE F-D6-1`/`MORDE F-D6-2(i)` (coluna reordenada / campo extra em
  `runs[]` movem o hash do servidor; TS fica ancorado no original ⇒ paridade reprova de propósito).
- **D2.5 (304 real):** mesmo teste, 2ª chamada com `If-None-Match` conhecido recebe `304` REAL do
  servidor (espiado via `fetchImpl` que envolve o `fetch` global, nunca mockado) — **[OK]**.
- **D2.6 (Next reaproveita no processo; cache nunca mascara erro de transporte):** mesma
  chamada reutiliza `projection`/`fingerprint` idênticos; teste separado
  (`MORDE D2.6`) mata o servidor DEPOIS de esquentar o cache — 2ª chamada rejeita com
  `connection_refused`, nunca serve a projeção velha — **[OK]**.
- **Nuance sobre a letra do DoD ("2º GET /painel ⇒ access log mostra 304 na 2ª linha"):**
  `src/app/painel/page.tsx:56` chama `fetchIngestHealthProjectionViaHttp()` sem argumentos — a
  MESMA função exercitada pelo teste, sem lógica intermediária. Não existe hoje um teste que suba
  `next dev`/`next start` de verdade e leia o access log do backend; a prova ficou no nível da
  função de transporte, que é o único código entre o clique e a rede. Não bloqueia — registro
  para não confundir "não testado" com "não provado por este teste".

## Comandos rodados

```
npm --prefix frontend run test:s1     → 66/66 pass, 0 fail   [MEDIDO]
npm --prefix frontend run lint        → rc=0, saída vazia    [MEDIDO]
npm --prefix frontend run typecheck   → rc=0, saída vazia    [MEDIDO] (tsc -p tsconfig.json --noEmit --strict)
```

Rodei TAMBÉM `test:app` e `test:charts` (fora do escopo de `s1-console`, mas parte da suíte de
front): `test:app` 97/98 (1 falha, `universe-at.test.ts`), `test:charts` falha em
`s2-panels.test.ts` — **ambas dependentes da data corrente do sistema (2026-09-07) contra
snapshot/fixture com datas fixas**, confirmadas PRÉ-EXISTENTES: reproduzi a mesma falha após
`git checkout dba4572 -- .` (T-02.4, antes do diff de T-02.5) nos mesmos 2 arquivos, depois
restaurei (`git checkout HEAD -- frontend/src/features/s1-console/`, `git status --short` limpo).
Não são regressão de T-02.5; não bloqueiam este gate (escopo = `s1-console`).

## Mutação / controle negativo exigido nesta tarefa

Editei `dequoteStrongEtag` em `ingest-health-query.ts` (produção) para acrescentar um sufixo ao
hex dequotado, quebrando deliberadamente `etag == fingerprint(...)`. Rodei
`node --conditions=react-server --test src/features/s1-console/ingest-health-query-http.test.ts`:
**9 pass, 1 fail** — o teste `CALA T-02.5 (D2.4+D2.5+D2.6)` morde exatamente na asserção de
paridade (`AssertionError: D2.4: etag ... tem de bater com fingerprint()`, valores
`...568b1MUTATED-QA-T02.5` vs `...568b1`). Revertido; `git status --short` limpo; suíte voltou a
66/66.

## `harness rules --mode sweep --changed-only`

`rc=0`, saída vazia (ambíguo por si só — regra deste repo). Desambiguado: `harness rules --mode
file --path <cada um dos 3 arquivos do diff>` também `rc=0`/vazio, E confirmei que o instrumento
MORDE de verdade com uma sonda descartável (`frontend/src/app/qa-probe-tmp/probe.ts`, import de
`../../../backend/...`) → `rc=2`, `{"decision":"block","reason":"[web-fullstack.browser-imports-server]..."}`.
Sonda removida antes de qualquer commit; `git status --short` limpo depois. `core.hardcoded-secret`
só varre `**/*.py` (não alcança TS) — não é regra aplicável aqui; não há segredo literal nos 3
arquivos (confirmado por leitura).

## Doc delta

Nenhum padrão novo: o mecanismo (ETag = fingerprint, header fora da região hasheada,
`If-None-Match`/`304`, cache de processo que nunca mascara erro de transporte) já está
integralmente descrito em `ADR-029/D4` (`docs/adr/ADR-029-...md:41-43`) e em `SPEC-003` §3.2, ambos
anteriores a este código — implementação bate com decisão já registrada, não decisão nova. Nenhuma
rota nova, nenhuma mudança de contrato visível, nenhuma UI. `docs/INDEX.md` não tem linha para
T-02.5 ainda (owner ainda não avançou o ledger) — não é um "Doc delta" ausente do PR, é o registro
de artefato que corre à parte, no `harness pipeline`/ledger.

## Achados

1. [WARNING] O DoD literal de `D2.6` ("access log da API mostra 304 na 2ª linha, GET /painel")
   não tem prova end-to-end via servidor Next real — só a função de transporte, que é o único
   código entre `page.tsx` e a rede (ver nuance acima). Sugestão: se/quando `T-02.9`/e2e cobrir
   `/painel` de ponta a ponta, adicionar essa checagem lá; não é motivo de `NEEDS_FIX` aqui porque
   a função testada é literalmente a mesma que `page.tsx` chama, sem lógica intermediária.

## Veredito: APPROVED

Justificativa: os 3 pares MORDE/CALA da paridade `etag == fingerprint()` (original, coluna
reordenada, campo extra) mais o 304 real (não mockado) mais o "cache nunca mascara erro de
transporte" cobrem exatamente o que `ADR-029/D4`/plano 02 (D2.4-D2.6) exigem, incluindo o
controle negativo que `ADR-005/D6` proíbe omitir. Mutação ao vivo confirmou que a suíte reprova
quando a igualdade quebra de verdade. `lint`/`typecheck`/`test:s1` verdes; `harness rules`
desambiguado com sonda própria. Único achado é WARNING, sem impacto em comportamento.
