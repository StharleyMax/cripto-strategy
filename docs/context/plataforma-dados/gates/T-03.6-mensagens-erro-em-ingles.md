# Relatório — T-03.6: mensagens de `raise`/`Error`/`Exception` em inglês

Mesma chore aplicada em `chore/mensagens-erro-em-ingles` (PR #76), estendida aos 8 arquivos que
`T-03.6` introduz e que `master` ainda não tem — universo isolado por
`git diff --name-only master...HEAD -- backend/src`:

```
backend/src/modules/sentimento/domain/availability_lag.py
backend/src/modules/sentimento/domain/availability_lag_stats.py
backend/src/modules/sentimento/domain/availability_poll.py
backend/src/modules/sentimento/domain/availability_probe_set.py
backend/src/modules/sentimento/infra/availability_http_client.py       (sem raise/Error)
backend/src/modules/sentimento/infra/availability_probe_cli.py         (só SystemExit)
backend/src/modules/sentimento/infra/system_probe_clock.py
backend/src/modules/sentimento/use_cases/run_availability_probe.py
```

**Deliberadamente NÃO tocados aqui:** as 50 mensagens que já existiam em `master@76c4d65` (ex.:
`coinalyze_daily_series.py`, `liquidation_reconciliation.py`) — essas são escopo de
`chore/mensagens-erro-em-ingles` (PR #76) e continuam PT nesta branch até o merge trazê-las.
`scan_raise_pt.py` copiado para cá roda sobre o repo inteiro e por isso ainda reporta `PT=54`
(50 herdadas de `master` + eventuais falso-positivos) — o universo desta chore é só os 8 arquivos
acima, e todos os 10 `raise` de mensagem que eles continham (incluindo 3 que o próprio heurístico
do scanner não alcançava, achados por leitura manual) estão traduzidos.

## Testes ajustados

10 `pytest.raises(..., match="...")` em 4 arquivos de teste apontavam a string antiga em
português e foram corrigidos para a nova substring em inglês, sem alterar o tipo de exceção
verificado.

## Comandos rodados e resultado

- `bash backend/scripts/test.sh` → **1029 passed**, cobertura **97,82%** (domain 99,9%, use_cases
  100%, infra 95,1%).
- `bash backend/scripts/lint.sh` → limpo, 180 arquivos.
- `bash backend/scripts/natureza.sh` → universo 55 arquivos, 0 leituras de relógio.
- `bash backend/scripts/boundaries.sh` → **3 kept, 0 broken**.
- `harness rules --mode sweep --changed-only --format ndjson` → 1 achado `[WARN]` (não
  bloqueante, pré-existente — docstring de módulo multi-linha em `run_availability_probe.py`,
  linha que esta chore não tocou), `rc=0`.
