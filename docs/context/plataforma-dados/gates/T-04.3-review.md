# T-04.3 (CST-30) — Auditoria arquitetural — PR #51

**Veredito: COMPLIANT**

## Denominador
- Regras bloqueantes em vigor: 7 (`harness rules list --severity block`). Nenhuma tocada pelo diff.
- `harness rules --mode file --path <arquivo>` em cada um dos 6 arquivos do `gh pr diff 51`
  (2 produção + 3 teste + `backend/README.md`): 0 achado em todos.
- Contrato `[tool.importlinter]` (3 contratos, incluindo "Natureza"):
  `bash backend/scripts/boundaries.sh` → `Contracts: 3 kept, 0 broken` (a dívida de
  `dump_window.py`/`retention_probe.py` que o builder registrou contra `master@a3c63c5` já não
  existe na base atual — `T-03.12` mesclou).
- `make natureza` (scanner de USO, `T-03.12`): `18 arquivo(s) em domain`, `7 em use_cases`,
  `universo de 25 arquivo(s), 0 leitura(s) de relogio` — `aggtrade_contiguity.py` não lê relógio.
- Direção de import: `aggtrade_contiguity.py` (domain) não importa nada do repositório;
  `aggtrade_csv_reader.py` (infra) importa `domain.aggtrade_contiguity` — única direção
  permitida por "Camadas por contexto: infra > use_cases > domain".
- Cobertura por camada (`bash backend/scripts/check-coverage-layers.sh`): domain 100%
  (1057/1057, piso 90%), use_cases 100% (220/220, piso 80%), infra 98,3% (832/846, piso 70%) —
  todos acima do piso; números que batem com os do builder.
- Falsificador do vocabulário de componentes (`CLAUDE.md`, tabela de fronteira, linha 9):
  `git ls-tree -r --name-only HEAD | ... | grep -vxE 'sentimento|charts|convergencia|backtest|web|docs'`
  → 14 segmentos, 0 em português (nenhum segmento novo introduzido por este PR).

## Achados
Nenhum BLOCKER, nenhum WARNING com id de regra ou citação de documento que reprove.

Nota não-bloqueante (dívida já documentada em `CLAUDE.md`, não portão): `aggtrade_contiguity.py:52-55`
introduz uma nova mensagem de `raise DuplicateAggIdError(...)` em português ("agg_id ... repetido —
a unicidade e por agg_id..."). É produção, não teste — diferente da nota já registrada em
`T-04.1-review.md`. O comando de medição citado em `CLAUDE.md` (linha única, string na mesma linha do
`raise`) não a captura por ela ser multi-linha, então a contagem "3 PT / 2 EN" ali escrita já está
desatualizada. A superfície "mensagem de exceção" segue `⏸ NÃO DECIDIDO`, dono `/architect` — registro
para o gatilho de reabertura, não para o veredito.

## Comandos usados
`harness rules list --severity block`; `harness rules --mode file --path <arquivo>` (x6);
`gh pr diff 51 --name-only`; `bash backend/scripts/boundaries.sh`; `make natureza`;
`bash backend/scripts/check-coverage-layers.sh`;
`grep -rnoE '(raise|Error|Exception)\w*\(\s*f?"[^"]{4,90}"' backend/src --include='*.py'`;
`git ls-tree -r --name-only HEAD | grep -E '^(backend/src|backend/tests|frontend/src)/' | awk -F/ '{for(i=1;i<NF;i++) print $i}' | sort -u | grep -vxE 'sentimento|charts|convergencia|backtest|web|docs'`.

## Doc-vs-ledger
Este veredito cobre só `T-04.3`. A fase 04 do pipeline não fecha aqui — `T-04.6` ainda está em
andamento (`harness pipeline state plataforma-dados` → `BUILD_AUTHORIZED`, sem gate de fase 04
fechado). Não usei pipeline override.
