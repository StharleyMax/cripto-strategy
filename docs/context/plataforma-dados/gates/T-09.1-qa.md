[QA GATE — Fase 09] Feature: plataforma-dados · Task: T-09.1 (CST-83) · Componente: docs

Arquivos no escopo do GATE (confirmados por `git status --porcelain` no worktree):
- docs/adr/README.md (novo)
- docs/context/plataforma-dados/tasks.toml (modificado)
- docs/INDEX.md (modificado)
- docs/context/plataforma-dados/gates/T-09.1-builder.md (novo, relatório do builder)

## Reprodução independente

- `ls docs/adr/ADR-*.md | wc -l` → **26** — confere a alegação "26 ADRs" (universo real, o
  padrão que o builder usou no falsificador de D9.2 e no `comm` de D9.1).
- `ls docs/adr/*.md | wc -l` → **27**, não 26. Este é o comando literal citado nas linhas
  15/18 do relatório do builder ("Contagem real registrada... → 26") **e é o próprio
  falsificador que `docs/adr/README.md:138-140` declara para si mesmo** ("se `ls
  docs/adr/*.md | wc -l` divergir de 26 (...) este índice está desatualizado"). Ele diverge
  **hoje, no commit que o cria** — porque `docs/adr/README.md` está dentro de `docs/adr/` e
  casa com o próprio glob `*.md` que o mede. A explicação escrita na linha 27-28 do README
  ("excluído pelo glob `*.md`" referindo-se a `docs/adr/bancadas/`) é irrelevante para o
  problema real: `bancadas/` é subdiretório e `*.md` não-recursivo nunca o alcançaria de
  qualquer forma; quem contamina o glob é o próprio `README.md`, criado por esta mesma task,
  no mesmo diretório. **Isto é o padrão exato de defeito que este repositório existe para
  achar** (nenhum número sem o comando que o produziu — e aqui o comando citado não reproduz
  o número citado).
- D9.2 (falsificador ≥1 por ADR): `grep -c "[Ff]alsificador" docs/adr/ADR-*.md | awk -F: '$2==0'`
  → vazio. **26/26 OK**, confirmado por reprodução independente (contagem por arquivo
  auditada manualmente, mínimo 2 ocorrências, máximo 26).
- D9.1 parte a (zero referência órfã): `comm -23 <(grep -rohE 'ADR-[0-9]{3}' docs/plans
  docs/context/*/tasks.toml docs/specs | sort -u) <(ls docs/adr/ADR-*.md | sed -E
  's#.*/(ADR-[0-9]{3}).*#\1#' | sort -u)` → vazio. **OK**, reproduzido.
- D9.1 parte b (toda ADR de plataforma-dados referenciada, exceto ADR-013/ADR-015 que são de
  `codigo-em-ingles`): `comm -13 ... | grep -vxE 'ADR-013|ADR-015'` → vazio. **OK**,
  reproduzido.
- As 9 refs novas em `tasks.toml` (T-05.11→ADR-018, T-05.14→ADR-019, T-08.4→ADR-021,
  T-08.6→ADR-020, T-08.7→ADR-022, T-08.8→ADR-023, T-08.12→ADR-026, T-08.13→ADR-024,
  T-08.14→ADR-025): checadas uma a uma contra `git log --grep` e o cabeçalho de cada ADR
  (`Feature:`/task de origem). Todas batem — inclusive o par menos óbvio pelo título
  (T-05.14 → ADR-019, "cliente HTTP de ingest_health"), confirmado em
  `docs/adr/ADR-019-...md:4` (`**Feature:** plataforma-dados (T-05.14, CST-105)`).
- `git diff docs/context/plataforma-dados/tasks.toml`: só linhas `refs = [...]` reescritas
  para apensar 1 item cada; nenhum conteúdo anterior removido. **OK**.
- `git diff --stat -- docs/plans` → vazio. `docs/plans/` genuinamente intocado. **OK**.
- `harness rules --mode sweep --changed-only` → saída vazia, rc=0. Reproduzido. **OK**
  (8 regras bloqueantes em vigor via `harness rules list --severity block`, todas de escopo
  `code`/`production`; não se aplicam a `docs/`, e o sweep confirma isso empiricamente em vez
  de por suposição).

## Veredito

- [FAIL] Evidência de D9.1 "contagem real" (`docs/adr/README.md` linhas 22-28 + builder
  gate linhas 15/18) — comando citado `ls docs/adr/*.md | wc -l` não reproduz 26; reproduz
  27, porque conta o próprio `README.md` criado por esta task. O README declara este exato
  comando como seu próprio falsificador (`README.md:138-140`) e ele dispara imediatamente.
- [OK] D9.2 (falsificador ≥1 por ADR) — 26/26, `grep -c` reproduzido.
- [OK] D9.1 parte a (zero referência órfã) e parte b (toda ADR referenciada) — `comm` vazio,
  reproduzido.
- [OK] 9 refs de tasks.toml mapeadas corretamente (task → ADR conferido no histórico git e no
  cabeçalho de cada ADR).
- [OK] docs/plans/ intocado — `git diff --stat -- docs/plans` vazio.
- [OK] `harness rules --mode sweep --changed-only` vazio, rc=0.
- Testes existem e passam: N/A — componente `docs`, sem suite de código nesta trilha
  (`harness policy --key test_cmd` só declara `sentimento`); nenhuma anomalia, é ausência
  esperada.
- Cobertura: N/A pelo mesmo motivo.

Regras bloqueantes avaliadas: 8 de 8 listadas por `harness rules list --severity block`
(nenhuma de escopo `docs`; confirmadas via sweep vazio, não por suposição).

Veredito: **NEEDS_FIX**

Ações:
1. Corrigir `docs/adr/README.md` (linhas 22-28 e 138-140) e
   `docs/context/plataforma-dados/gates/T-09.1-builder.md` (linhas 15, 18) para que o comando
   de "contagem real" exclua o próprio índice — ex.: `ls docs/adr/ADR-*.md | wc -l` (que já dá
   26 corretamente, é o mesmo padrão usado no `comm` de D9.1/D9.2) — ou explicitamente somar
   "-1 (README.md)" com a justificativa certa, em vez da menção a `bancadas/` que não é a causa
   real da divergência.
2. Após a correção, confirmar que `ls docs/adr/*.md | wc -l` (se mantido como o comando citado)
   reproduz o número declarado, ou trocar o comando citado por um que reproduza.
