# T-09.3 — review gate (read-only)

**Task:** `[docs] 09 · Registro consolidado de ADR-004 e do finalista do motor` (`CST-85`).
**Componente:** `docs`. Diff: `docs/adr/ADR-004-*.md` (+74/0), `docs/adr/ADR-002-*.md` (+36/0),
`docs/INDEX.md` (+1/0) — 3 arquivos, append puro, zero linha removida (`git diff --numstat`
reproduzido: `74 0`, `36 0`, `1 0`). `docs/plans/` e `docs/specs/` — **não tocados**
(`git status --porcelain | grep -E 'docs/plans/|docs/specs/'` → 0 linhas).

## Bootstrap
`harness rules list --severity block` → 8 regras (todas escopo `code`/`production`, nenhuma
sobre `docs/*.md`). `harness policy --key docs.bootstrap_docs` → `docs/proposta-discovery.md`.
`harness policy --key glossary_doc` → vazio (dívida conhecida, CLAUDE.md §Idioma, não relevante
aqui). `harness policy --key code_paths` → `include_prefixes` não cobre `docs/`.

## Camada 1 — o que o runner mede
`harness rules --mode sweep --changed-only` → `rc=0`, saída vazia. Reproduzido, bate com QA.
Denominador: 8 regras bloqueantes existentes, 8 avaliadas pelo sweep, 3 arquivos varridos.

## Camada 2 — disciplina de rótulo do CLAUDE.md ("Nenhum número sem o comando")
Toda afirmação quantitativa nas duas emendas carrega comando + universo + rótulo
(`[MEDIDO 2026-09-04]`, `[DOC: backend/README.md:2234-2238]`, `[NÃO MEDIDO]`). Reproduzi
independentemente as 6 contagens da emenda de ADR-004 — todas batem: `grep -c "ADR-004"
test_force_order_reconnection.py`→5, `grep -c "def test_"` mesmo arquivo→25, `test_aggtrade_contiguity*.py`→18/9,
`grep -c "ADR-004"` em `oi_history_paginator.py`/`binance_oi_history_client.py`→0/0,
`grep -rniE "reconnect.*aggtrade|..."`→1 (o mesmo falso positivo já nomeado como comentário, não
reconexão real), `grep -n "Classe A" tasks.toml`→0. A tabela de `D9.4` em ADR-002 reproduz números
já existentes em `ADR-002:150-151` (não remedidos aqui, e a emenda diz isso: "não decide nada de
novo") — mesmo padrão de tabela sem colchete inline que o próprio ADR-002 pré-existente já usa
para comparação de critérios (ex. linha "espaço ≤ 2× zipado" original também só usa ✅), portanto
não é desvio introduzido por esta task. Nenhuma citação `[PREMISSA-OWNER]`/`[DECISÃO-OWNER]`
inventada nas duas emendas.

## Camada 2 — limites arquiteturais declarados
- `docs/plans/SPEC-001-plataforma-dados/09_consolidacao_de_fronteira.md` (o plano da fase) —
  intocado, conforme regra de que builder de `docs` não altera plano aprovado.
- Cabeçalho das duas ADRs (`Status: proposto`) intocado — só houve blocos `+++` no fim de cada
  arquivo, confirmando o padrão "append-only por convenção" que o próprio texto novo cita.
- `docs/INDEX.md` — só uma linha nova ao final; nenhuma linha existente reescrita
  (`git diff -- docs/INDEX.md | grep -E '^-[^-]'` → vazio), conforme "Registro de artefatos é
  append-only" do CLAUDE.md.
- Gap A1/A3 de Classe A e região da VPS (`[GAP G7]`) nomeados em seção própria com título, não
  em rodapé — não é ato de abrir task nova (fora de escopo de `docs`), e o texto diz isso.

## Veredito

**COMPLIANT.**

Denominador: 8 regras bloqueantes em vigor no repositório, 8 avaliadas via
`harness rules --mode sweep --changed-only` (rc=0, vazio) sobre os 3 arquivos alterados nesta
task. Nenhuma violação de regra mecânica nem de arquitetura declarada encontrada — nenhum achado
sem id de regra ou citação de documento a reportar.

```
harness gate-record plataforma-dados 09 REVIEW COMPLIANT "T-09.3: 3 arquivos append-only, sweep rc=0 vazio, 6 greps reproduzidos, docs/plans e docs/specs intocados"
```
