# T-09.3 — QA gate

**Task:** `[docs] 09 · Registro consolidado de ADR-004 e do finalista do motor` (`CST-85`).
**Componente:** `docs`. Sem código de produção alterado (nenhum arquivo em `backend/`/`frontend/`
tocado) — regras `code`/`production` do sweep não se aplicam a este diff; verificadas mesmo assim.

## 1. Append puro

```
git diff --numstat -- docs/adr/ADR-004-reconexao-de-stream-sem-sequencia.md   # 74  0
git diff --numstat -- docs/adr/ADR-002-motor-de-armazenamento.md              # 36  0
git diff --numstat -- docs/INDEX.md                                            #  1  0
git diff -- docs/adr/ADR-004*.md docs/adr/ADR-002*.md docs/INDEX.md | grep -E '^-[^-]'
  -> rc=1, zero linhas — nenhuma deleção nos três arquivos
```
Headers das duas ADRs (`Status: proposto`) intocados — confirmado por leitura do diff completo
(`git diff`), só há blocos `+++` no final de cada arquivo.

## 2. Greps reproduzidos (não confiei nos números do builder)

```
grep -c "ADR-004" backend/tests/sentimento/test_force_order_reconnection.py          -> 5
grep -c "def test_" backend/tests/sentimento/test_force_order_reconnection.py        -> 25
grep -c "def test_" .../test_aggtrade_contiguity.py .../test_aggtrade_contiguity_fixtures.py
                                                                                       -> 18 / 9
grep -c "ADR-004" .../oi_history_paginator.py .../binance_oi_history_client.py       -> 0 / 0
grep -rniE "reconnect.*aggtrade|aggtrade.*reconnect|overlap.*agg_id" backend/src backend/tests
  -> 1: test_aggtrade_contiguity_fixtures.py:86 "# does not raise: no file overlaps another
     in `agg_id`" — comentário sobre unicidade ENTRE ARQUIVOS de dump, não reconexão ao vivo
grep -n "Classe A" docs/context/plataforma-dados/tasks.toml -> rc=1, 0 ocorrências
```
Todos os 6 números batem exatamente com o relatório do builder. `backend/README.md:2236`
confirma "897 passed", "cobertura total 98,06%" citados na emenda de ADR-004.

## 3. A1/A3 nomeados, não escondidos — e a decisão de não abrir task

Seção "Classe A (`aggTrade`) — parcial" e "O que fica bloqueado, nomeado" na emenda de ADR-004
nomeiam A1/A3 explicitamente como não implementados e sem task no plano `03`–`08`, com o grep que
prova a ausência. Não está enterrado em rodapé nem em nota lateral — é seção própria com título.

Julgamento: builder decidiu **certo em só nomear**. Componente da task é `docs`, fase `09`
declara "não escreve código de produção… não decide política de coleta" — abrir task nova é ato
de planejamento (padrão já visto em `T-09.4`, que nomeia pendências como *"fora de escopo,
dono X"* em vez de criar item). Criar item de fase é histórico ato de `/architect` neste repo
(ex.: dispatch 2026-09-03 citado em `T-09.4`). QA concorda que abrir a task é chamada de
`/tech-lead`/`/architect`, não do builder nem desta invocação de QA — não bloqueio.

## 4. Números de ADR-002 vs. fonte de `T-08.1`

Comparados byte a byte contra `docs/spike/T-08.1-motor-armazenamento/README.md:82` e
`docs/adr/ADR-002-motor-de-armazenamento.md:150-151` (texto já existente, não desta emenda):
153,8 MiB RSS, 5.219 MB RAM livre, ~900 KB delta, 75 GB disco livre, 1,506×, 0,05–0,09s — **todos
idênticos, nenhum arredondado/alterado**. Região da VPS corretamente mantida `[NÃO MEDIDO]`.

## 5. Sweep de regras

```
harness rules --mode sweep --changed-only   -> rc=0, saída vazia
```

## Veredito

## QA Gate — Fase 09 [docs] T-09.3
- [OK] Append puro — `git diff --numstat` nos 3 arquivos (74/0, 36/0, 1/0), zero deleção
- [OK] Greps do relatório reproduzidos independentemente — 6/6 números batem
- [OK] Lacuna A1/A3 nomeada em seção própria, não escondida; decisão de só nomear (não abrir
  task) é correta para escopo `docs`/fase `09` — abertura de task é ato de `/architect`
- [OK] Números de ADR-002 conferem exatamente com `T-08.1` (spike README + ADR-002:150-151)
- [OK] `harness rules --mode sweep --changed-only` — rc=0, vazio
- [OK] DoD D9.4: `free -m`, `df -h`, região, 5 critérios do spike — todos com valor (região
  `[NÃO MEDIDO]` por desenho declarado, não bloqueia D9.4 conforme texto da própria ADR)
- Sem anomalia
Regras bloqueantes avaliadas: 8 de 8 listadas por `harness rules list --severity block`
(nenhuma incide sobre `docs/*.md`; sweep confirma vazio)
Veredito: APPROVED
