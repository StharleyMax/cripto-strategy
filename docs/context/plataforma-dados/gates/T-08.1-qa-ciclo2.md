# Gate report — T-08.1 (QA), ciclo 2, CST-69

**Fase 08, componente `docs`. Modo: completo (invocado direto).**

## Escopo

Verificação independente da resposta do builder ao `NEEDS_FIX` do ciclo 1
(`docs/context/plataforma-dados/gates/T-08.1-qa.md`): 2 achados — `compression_level=19`
ausente/inefetivo e `OUT_DIR` divergente entre `write_parquet.py`/`verify_duckdb.py`/README.
Resposta do builder: `docs/context/plataforma-dados/gates/T-08.1-builder-ciclo2.md`. Não
confiei no relatório — rodei os scripts do zero, em container Docker próprio (`spike-ts-qa2`,
porta 15433, removido ao final).

## Checagem 1 — `compression_level=19` presente e efetivo

`OK`. `grep -n compression_level write_parquet.py` → linha 43, `compression_level=19`
literal, dentro da chamada `pq.write_to_dataset`. Reexecutei o pipeline completo do zero
(refetch real da Binance, 34.496 + 30 + 30 + 863 = 35.419 linhas): `python3 write_parquet.py
&& du -sb built/parquet_z19` → **1.199.837 B = 1,987×** o zipado da fonte (603.725 B) — bate
com o 1,989×/PASS da emenda de `ADR-002` (3ª casa varia por refetch, mesma direção que o
builder já havia documentado). Repeti o mesmo dataset **sem** `compression_level` (zstd
default): 1.231.453 B = **2,040×** — reprova ≤2×, confirmando que o defeito do ciclo 1 era
real e que a correção o fecha.

## Checagem 2 — `OUT_DIR` consistente

`OK`. `write_parquet.py:21` → `OUT_DIR = "built/parquet_z19"`; `verify_duckdb.py:13` →
`GLOB = "built/parquet_z19/**/*.parquet"`; README usa `built/parquet_z19` nos 3 lugares
(`du -sb`, passo 3b, seção de correção). `python3 verify_duckdb.py` → `OVERALL: PASS` sem
qualquer edição prévia dos consumidores — prova a consistência por execução, não só por leitura.

## Checagem 3 — número final bate com a emenda de `ADR-002`

`OK`. Candidato 4 (TimescaleDB): `hypertable_size` = **909.312 B = 1,506×**, byte a byte
idêntico ao citado na emenda e ao medido no ciclo 1; `verify_timescale.py` → `OVERALL: PASS`.
Candidato 5: 1,987× (acima), dentro da mesma faixa/direção do 1,989× citado. Nenhum critério
de `D8.21` muda de veredito.

## Checagem 4 — nada além do prometido mudou

`OK`. `git diff --stat` (working tree vs `HEAD`, commit `194df2e`): `docs/INDEX.md` (+1
linha, append), `docs/spike/T-08.1-motor-armazenamento/README.md` (+49 linhas, seção nova
"Correção de reprodutibilidade — ciclo 2", append), `write_parquet.py` (2 mudanças:
`OUT_DIR` e `compression_level=19`, + comentário). `ADR-002` **não tocado** — confirmado (não
aparece no diff). Untracked: os 2 gates (`T-08.1-qa.md` do ciclo 1, `T-08.1-builder-ciclo2.md`
do ciclo 2). Exatamente o escopo declarado pelo builder, nada a mais.

## Regras bloqueantes

`harness rules --mode sweep --changed-only` → `rc=0`, 0 achado. `docs/` fora de `code_paths`;
as 8 regras de `harness rules list --severity block` (core + web-fullstack + own) não se
aplicam a este universo (spike Python fora de `backend/src`/`frontend/src`, sem segredo
literal, sem import relativo/`except` silencioso/`print` observado em `write_parquet.py`).

## Testes / cobertura

Não aplicável — `docs` não tem `test_cmd` declarado (`harness policy --key test_cmd` só lista
`sentimento`). O "teste" aqui é a reexecução do pipeline reprodutível, que é o próprio
artefato da task.

## DoD (`D8.21`, os 5 critérios) — item a item

1. espaço ≤2× zipado — candidato 4 1,506× ✅, candidato 5 1,987× ✅ (medido nesta sessão).
2. backtest `as_of` ≤60s — candidato 4 0,049s ✅, candidato 5 0,010s ✅ (medido nesta sessão).
3. fixture 3 classes bit-idêntica — `verify_timescale.py`/`verify_duckdb.py` ambos `PASS`
   (medido nesta sessão).
4. RAM/disco com folga — não remedido nesta sessão (não fazia parte do achado do ciclo 1;
   já constava `[MEDIDO 2026-09-04]` no README, sem contradição encontrada).
5. latência de rede (candidato 5) — segue `[NÃO MEDIDO]`, rotulado, sem crédito indevido;
   não é o que este ciclo reabriu.

## Veredito

Os 2 achados do ciclo 1 estão fechados, com evidência própria (não apenas o relatório do
builder): `compression_level=19` presente e efetivo, `OUT_DIR` consistente nos 3 arquivos e
comprovado por execução, número final (1,987×/1,506×) bate com a emenda de `ADR-002`, e o
diff do ciclo 2 não extrapola o escopo prometido.

**Veredito: APPROVED**
