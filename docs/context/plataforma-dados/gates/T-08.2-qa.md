# T-08.2 (`CST-70`) — Gate de QA · spike do eixo do Lightweight Charts (`D8.19`)

**Feature:** `plataforma-dados` · **Fase:** `08` · **Componente:** `charts`
**PR** #41 · **commit** `22541a9` · **branch** `tasks/T-08.2-spike-eixo-charts`
**Worktree:** `/tmp/claude-1002/wt/T-08.2` · **Base:** `master@aac3442`

> **Veredito: `NEEDS_FIX`.** `D8.19` PASSA e o instrumento é sólido: os dois falsificadores
> **matam** as mutações que plantei, e o achado (eixo ordinal) sobrevive a todas as sementes
> que testei. O que reprova são **dois números publicados que não reproduzem como estão
> escritos** e **a guarda anti-falso-verde sem teste nenhum**. Nada disso derruba o achado;
> os três são correções de baixo custo no relatório e uma função exportada.

---

## 1. O que reproduziu, byte a byte

```bash
cd /tmp/claude-1002/wt/T-08.2/frontend && node src/charts/axis-spike.ts   # rc=0
```

| grandeza | builder | **medido por mim** |
|---|---|---|
| Cenário A — pior caso | `2,27e-13 px` | **`2.27e-13 px`** ✅ |
| Cenário A — universo | 1.728 itens · 1.440 timestamps | **1728 itens (1440+288) · 1440 distintos** ✅ |
| pane / vão entre âncoras | 1.138 px / 1.137,210 px | **1138 px / 1137.210 px** ✅ |
| Cenário B — pior caso | `36,390 px` (73×) | **`36.390 px` (73×)** ✅ |
| Cenário B — onde | `2025-01-01T13:47:00Z`, 689,245 vs 652,855 | **idêntico** ✅ |
| Cenário B — universo | 602 itens (314+288) · 535 distintos | **idêntico** ✅ |
| `rc` do runner na árvore limpa | 0 | **0** ✅ |

`[MEDIDO 2026-08-29, n=1.728 (A) e n=602 (B), rc capturado sem pipe]`.

**`D8.19` ao pé da letra** (`docs/plans/SPEC-001-plataforma-dados/08_superficie_e_reprodutibilidade.md:44`
— *"288 pontos + 1.440 candles no mesmo eixo, tolerância 0,5 px"*): **288 pontos + 1.440 candles,
um único `timeScale`, `unplacedCount = 0` exigido pelo próprio runner, pior caso
`2,27e-13 px` ≤ `0,5 px`. PASSA.**

---

## 2. As três coisas que o despacho mandou decidir

### 2.1 O falso verde foi corrigido? — **SIM, e a guarda é quem corrige.** Mutação MATA.

Plantei o defeito de volta em duas camadas, porque uma só não separa as causas.

**Mutação 1 — só `flushFrames()` removido** (as duas chamadas `await flushFrames(dom, 3)`
viraram comentário):

```bash
sed -i 's/^  await flushFrames(dom, 3);$/  \/\/ MUTANT/' src/charts/headless-chart.ts
node src/charts/axis-spike.ts    # rc=3
# RECUSA: nao consegui medir — o eixo NAO coube na viewport: barSpacing = 6 px para 1440
# slots em 1200 px de pane (esperado ~0.8333 px). ...
```

`[MEDIDO 2026-08-29: rc=3]` — e `rc=3` é *"não mediu"*, distinto de `rc=1` (*"mediu e reprovou"*).

**Mutação 1b — `flushFrames()` removido E `assertViewportFitted()` desligada**
(`if (true) { return; }` no topo da guarda):

```bash
node src/charts/axis-spike.ts    # rc=0  ← O FALSO VERDE VOLTA
#   pane do eixo : 8634.000 px entre as ancoras (timeScale.width() = 1200 px)
#   PIOR CASO    : 9.09e-13 px  (tolerancia D8.19 = 0.500 px)
#   veredito     : DENTRO da tolerancia
```

**Este é o resultado decisivo do gate.** Com a guarda desligada, o spike publica **verde** sobre
um gráfico de **8.634 px de vão numa pane de 1.200 px** — os mesmos `~8.634 px` que o builder
descreve em §4.2, reproduzidos ao pixel. **`assertViewportFitted()` é o que transforma esse verde
em `rc=3`, e não `flushFrames()`:** com o `flush` ausente e a guarda presente, RECUSA; com os dois
ausentes, verde mentiroso. A afirmação do builder *"`flushFrames()` sozinho não basta; a guarda é
a parte que dura"* está **medida, não alegada**.

*(Nota de precisão histórica: sob o formatador atual `px()`, o falso verde imprime `9.09e-13 px`,
não o `0,000 px` que §4.2 cita — aquele número é de uma versão anterior do formatador. A
geometria é idêntica; a citação é histórica e não reproduz literalmente. Sem consequência.)*

### 2.2 O cenário B é falsificador não-vácuo? — **SIM. Mutação MATA.**

```bash
sed -i 's/^const MEASURED_ONE_MINUTE_COVERAGE = 0.2;$/const MEASURED_ONE_MINUTE_COVERAGE = 1.0;/' \
  src/charts/axis-spike.ts
node src/charts/axis-spike.ts    # rc=1
# REPROVA O PROPRIO INSTRUMENTO: o cenario B ficou DENTRO da tolerancia, entao esta
# medicao nao tem poder de rejeitar e o verde do cenario A nao prova nada.
```

`[MEDIDO 2026-08-29: rc=1]`. A saída de rejeição **existe e dispara**. O runner não é capaz de
devolver verde a partir de um instrumento sem poder de rejeitar.

### 2.3 Os 20,0% são de `D8.11`? — **SIM como PARÂMETRO; NÃO como cobertura realizada.**

```bash
grep -n 'D8.11' docs/plans/SPEC-001-plataforma-dados/08_superficie_e_reprodutibilidade.md
# 36:| **D8.11** | ... Cobertura: 1m **20,0%** · 5m 100% · 15m 100% · 60m 99,9% · ...
```

O `0.2` de `MEASURED_ONE_MINUTE_COVERAGE` **é** o número que o plano 08 mediu para 1 m, na
linha 36. Não foi escolhido para produzir o achado. ✅ — **mas ver `F-1` abaixo.**

---

## 3. Os defeitos — provados, não opinados

### `F-1` · **`314/1.440` NÃO é 20,0%; é 21,81%.** A tabela do relatório publica um número que
a própria divisão desmente.

`docs/context/plataforma-dados/gates/T-08.2-builder.md`, §1, célula da linha **B**:

> `| **B** — mutação, cobertura medida em D8.11 | **20,0%** (314/1.440) | ... |`

```bash
python3 -c "print(314/1440*100)"    # 21.805555555555557
```

`20,0%` é o **parâmetro alvo** passado a `buildSparseGridWorkload`; `21,81%` é a **cobertura
realizada** pelo LCG com `seed = 20260829`. A célula apresenta a realizada **como se fosse** a
alvo. Sob a regra do repositório (*"nenhum número sem o comando que o produziu"*), um par
`percentual (numerador/denominador)` que não fecha é defeito — e este fecha errado por
**1,81 pp**, para **cima**, na direção que aumenta o achado.

### `F-2` · **A frase *"e por quanto"* extrapola o limite que o próprio relatório declarou.**
O pior caso varia **2,7×** com a semente, à mesma cobertura alvo.

O relatório escreve, em §3, logo depois de declarar que a *forma* dos buracos é sintética:

> *"Ele afirma que **naquela cobertura** o eixo ordinal deixa de concordar com `event_time`,
> **e por quanto**."*

**O "por quanto" não é propriedade da cobertura — é propriedade da semente.** Medido com o
mesmo `coverage = 0.2` e cinco sementes, pelo mesmo caminho de código do runner:

```bash
node /tmp/claude-1002/seed-probe.mjs   # importa synthetic-series.ts, headless-chart.ts, axis-fidelity.ts
```

| semente | candles | cobertura realizada | **pior caso** | média |
|---|---|---|---|---|
| **20260829** (publicada) | 314 | **21,81%** | **36,390 px** | 19,216 px |
| 1 | 294 | 20,42% | 27,333 px | 7,664 px |
| 42 | 258 | 17,92% | **13,664 px** | 6,122 px |
| 987654 | 276 | 19,17% | 27,753 px | 11,300 px |
| 20260830 | 286 | 19,86% | 27,254 px | 12,088 px |

`[MEDIDO 2026-08-29, n=5 sementes × 602±36 itens]`. Faixa: **13,664 – 36,390 px**, razão **2,66×**.
**A semente publicada é o MÁXIMO das cinco** — e também a de maior cobertura realizada.

**O que isto NÃO derruba, e a distinção é o ponto:** o **achado qualitativo é robusto** — as
cinco sementes ficam **fora** da tolerância, a pior delas em **27×** e nenhuma abaixo de
`13,664 px`. O eixo ordinal diverge de `event_time` em toda semente testada. **O que cai é o
`36,390 px` como se fosse "o" número daquela cobertura**, e o `73×` que dele deriva: eles são o
número **daquela semente**, e o relatório não declara que é uma amostra — muito menos a maior de
uma família que ele não publicou. Isso é exatamente a extrapolação que a ressalva de §3 existia
para impedir, feita na frase seguinte à ressalva.

### `F-3` · **A guarda anti-falso-verde não tem teste.** 2 dos 4 módulos novos: cobertura zero.

```bash
node --test --experimental-test-coverage src/charts/axis-fidelity.test.ts
#   axis-fidelity.ts    |  98.48 | 92.86 | 100.00 | 119-120
#   synthetic-series.ts | 100.00 | 100.00 | 100.00 |
#   all files           |  99.22 | 96.88 | 100.00 |
```

`[MEDIDO 2026-08-29, n=9 testes]`. **`headless-chart.ts` (272 linhas) e `axis-spike.ts` (172
linhas) não aparecem no relatório porque nenhum teste os carrega.** `assertViewportFitted()` — a
única coisa que separa este spike do falso verde que ele mesmo produziu, e a que a §2.1 acima
prova ser load-bearing — **não é exercida por teste nenhum.** O `99,22%` acima é sobre os 2
módulos que o teste importa, não sobre os 4 que a task entrega.

Consequência concreta: quem remover a guarda amanhã **não é reprovado por nada** — os 9 testes
continuam verdes (eles não a tocam), o `eslint` passa, e o `make verify` não roda
`test:charts` (ver `A-2`). A mutação que a matou foi **minha, manual, uma vez**; não há
regressão que a repita. `assertViewportFitted` é função pura de três números
(`paneWidthPx`, `barSpacingPx`, `barCount`) — o teste custa exportá-la e escrever dois casos.

---

## 4. Portões e regras

```bash
export HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness
make verify
```

```
[OK       ] lint-backend    rc=0  52 source files
[OK       ] lint-frontend   rc=0  ESLint do projeto sobre frontend/src
[OK       ] test            rc=0  325 passed · Total coverage: 99.16%
[OK       ] boundaries      rc=0  3 kept, 0 broken
[OK       ] regras          rc=0  0 bloqueio(s), 0 aviso(s)
[OK       ] política        rc=0
veredito: VERDE — 6 portões mediram e passaram
```

`[MEDIDO 2026-08-29T19:00:34Z, log em /tmp/verify-T-08.2-20260829T190034Z.log]`.

```bash
npm --prefix frontend run test:charts    # tests 9 · pass 9 · fail 0 · rc=0
```

### As 7 regras bloqueantes, uma a uma — com o universo de cada uma

`harness rules list --severity block` ⇒ **7 regras**.

| regra | veredito | evidência |
|---|---|---|
| `core.relative-import` | **OK** | `paths = ["**/*.py"]` (`packs/core/rules.toml:20`) — **fora do universo** do diff `.ts`; sweep sobre o Python: `rc=0` |
| `core.silent-except` | **OK** | `paths = ["**/*.py"]` (`:30`) — fora do universo; sweep `rc=0`. **Ver `O-1`** |
| `core.print-statement` | **OK** | `paths = ["**/*.py"]` (`:40`) — fora do universo; sweep `rc=0` |
| `core.hardcoded-secret` | **OK** | sweep `rc=0`, 0 achados; nenhuma chave nos 5 arquivos novos |
| `web-fullstack.browser-imports-server` | **OK, e NÃO-VÁCUO** | ver bloco abaixo |
| `web-fullstack.tenant-from-request` | **OK** | não há manipulação de requisição nesta task (spike offline, zero rede) |
| `web-fullstack.server-test-directory-present` | **OK** | `ls -d backend/tests` ⇒ existe (`helpers/`, `sentimento/`, `__init__.py`) |

**Não-vacuidade do motor sobre estes arquivos** — porque `rc=0` com saída vazia é ambíguo
(`ADR-012`), plantei a violação e ela **bloqueia**:

```bash
for f in frontend/src/charts/*.ts; do harness rules --mode file --path "$f"; done   # 5 arquivos, rc=0
printf '\nimport { thing } from "backend/src/whatever";\n' >> frontend/src/charts/synthetic-series.ts
harness rules --mode file --path frontend/src/charts/synthetic-series.ts            # rc=2
# {"decision": "block", "reason": "[BLOQUEIO] [web-fullstack.browser-imports-server]
#  frontend/src/charts/synthetic-series.ts:125: ... 1 ocorrencia(s)"}
```

`[MEDIDO 2026-08-29, n=5 arquivos]` — o motor **lê** e **bloqueia** neles. Arquivo restaurado
(`git status --short` ⇒ vazio).

**Não-vacuidade do ESLint** (`make verify` diz "sobre `frontend/src`", mas isso não prova que
enxerga o diretório novo):

```bash
npx eslint src/charts --format json | python3 -c "...len(d)..."   # 5 arquivos analisados
```

`[MEDIDO 2026-08-29, n=5]` — os cinco arquivos novos estão no universo do lint.

### `O-1` · Observação, não reprovação — `catch {}` vazio fora do alcance da regra

`frontend/src/charts/headless-chart.ts:43-46` engole a exceção de `Object.defineProperty` com
comentário e sem registro. O texto de `core.silent-except` (*"exceção engolida em silêncio"*)
descreve exatamente isto, mas a **regex da regra é `^\s*except Exception:` sobre `**/*.py`** —
estruturalmente incapaz de alcançar TypeScript. **Não é FAIL de regra** (está fora do universo
declarado) e o `catch` é justificado por escrito e de falha benigna. Fica **registrado** porque
é uma superfície que o portão não cobre, não uma que ele aprovou.

---

## 5. Escopo — o que o despacho mandou confirmar que NÃO mudou

```bash
git diff --stat aac3442 HEAD -- docs/context/plataforma-dados/tasks.toml    # saída VAZIA
```

**O grafo não foi tocado.** `D-2` continua `NÃO APLICADA` (`tasks.toml:17` — *"D-2 · NAO
decidida. Continua MARCADA e NAO APLICADA nas 2 tasks cujas arestas mudariam"*), nenhuma aresta
`T-08.2 → T-05.2` foi acrescentada, nenhuma migração de Epic. ✅

`docs/INDEX.md`: **+1 linha, 0 linhas removidas** (`git diff` mostra um único `+`). Append-only
respeitado. ✅

A decisão de arquitetura (grade completa com *whitespace* vs. X não legível como tempo) **não
foi tomada aqui** e continua com o `/architect`, como o relatório do builder declara em §3. ✅

---

## 6. Anomalias — checagens que não produziram veredito

**`A-1` · Alvo de cobertura para `charts`: não existe.**

```bash
harness policy --key coverage_target    # campo ausente na politica: coverage_target
```

`[MEDIDO 2026-08-29]`. **Não é `OK` nem `FAIL`:** sem alvo declarado não há piso contra o qual
reprovar. O `99,16%` do `make verify` é do **backend**, universo que esta task não tocou. O
`99,22%` que eu medi é dos **2 módulos** que o teste carrega. `B-2` do builder está correto.

**`A-2` · Os 9 testes desta task não são cobrados por portão nenhum.**

```bash
harness policy --key test_cmd    # {"sentimento": {"lint": ..., "test": ...}}
```

`[MEDIDO 2026-08-29, n=1 de 6 componentes declarados]`. Não há `test_cmd.charts`, então
`make verify` **não** roda `npm run test:charts`. Declarar a chave é **ato do owner** sobre
política — nem do builder, nem meu. Enquanto não existir, `F-3` não tem como ser cobrada por
máquina, o que **aumenta** o custo de deixá-la aberta. `B-1` do builder está correto.

**`A-3` · Ambiente de worktree** — `backend/.venv` não existe em worktree novo; sem symlink para
o `.venv` do checkout principal, `lint-backend`/`test`/`boundaries` devolvem `rc=3`
(`INDETERMINADO`, que **não** é passar). Symlink criado para medir e **apagado antes de
qualquer `git add`**; `git status --short` vazio antes e depois. Confere com §5 do builder.

---

## 7. Veredito e ações

**`NEEDS_FIX`** — três ações, todas de baixo custo, nenhuma delas toca a conclusão da task.

1. **`F-1`** — corrigir a célula de §1: a cobertura **realizada** com `seed = 20260829` é
   **21,81% (314/1.440)**; `20,0%` é o **parâmetro alvo** de `D8.11`. Escrever os dois, não um
   no lugar do outro.
2. **`F-2`** — capar a frase *"e por quanto"*: publicar `36,390 px` **como o valor de uma
   semente**, com a faixa medida (`13,664 – 36,390 px`, `n=5` sementes, mesma cobertura alvo) e
   com o que de fato é robusto — *"toda semente testada ficou fora da tolerância, a menos ruim
   em 27×"*. O achado não encolhe; ele passa a ser afirmado no tamanho em que foi medido.
3. **`F-3`** — exportar `assertViewportFitted` e cobrir os dois casos que importam: **aceita**
   `barSpacing ≈ paneWidth/barCount` e **recusa** `barSpacing = 6 px` com `barCount = 1440` em
   `paneWidth = 1200 px` (a geometria literal do falso verde). Sem isso, a única prova de que a
   guarda funciona é a mutação manual registrada em §2.1 deste relatório, que não se repete
   sozinha.

**O que já está aprovado e não deve ser refeito:** `D8.19` passa (`2,27e-13 px`, `n = 1.728`),
os dois falsificadores matam suas mutações, o achado do eixo ordinal é robusto às 5 sementes, o
grafo está intocado e os 6 portões do `make verify` estão verdes.

---

## Apêndice — a sonda de sementes de `F-2`, para o número reproduzir

Rodada de `frontend/`, com `node`. Ela usa o **mesmo caminho de código do runner** (`buildSparseGridWorkload` -> `collectAxisCoordinates` -> `measureAxisFidelity`); não é um modelo do gráfico, é o gráfico.

```js
const base = "/tmp/claude-1002/wt/T-08.2/frontend/src/charts/";
const { buildSparseGridWorkload, CANDLE_COUNT } = await import(base + "synthetic-series.ts");
const { collectAxisCoordinates } = await import(base + "headless-chart.ts");
const { measureAxisFidelity } = await import(base + "axis-fidelity.ts");
for (const seed of [20260829, 1, 42, 987654, 20260830]) {
  const w = buildSparseGridWorkload(0.2, seed);
  const { samples } = await collectAxisCoordinates(w);
  const r = measureAxisFidelity(samples);
  const cov = (w.candles.length / CANDLE_COUNT * 100).toFixed(2);
  console.log(`seed=${seed} candles=${w.candles.length} cobertura=${cov}% pior=${r.worstErrorPx.toFixed(3)}px media=${r.meanErrorPx.toFixed(3)}px`);
}
```

---

**`A-4` · O veredito NAO PODE SER GRAVADO — o portao do `gate-record` recusou, e a divida nao e desta task.**

```bash
harness gate-record plataforma-dados 08 QA NEEDS_FIX "..."     # rc=4
# VEREDITO QA NEEDS_FIX da fase 08 RECUSADO PELO DOCUMENTO DE TAREFAS, NAO PELA APROVACAO:
#   - fase 02: 3 de 5 tarefa(s) da fase 02 em estado nao-terminal: T-02.1, T-02.2, T-02.4b
#     — contradiz o veredito QA APPROVED de 2026-08-29T13:08:52Z
```

`[MEDIDO 2026-08-29: rc=4]`. `harness tasks list plataforma-dados` confirma: `T-02.1` e `T-02.2`
em `blocked`, `T-02.4b` em `todo`. **Divida herdada da fase 02**, anterior a `T-08.2` e sem
relacao com ela — e exatamente o portao que `agents/qa.md` descreve: *"RECUSA (exit 4) o proximo
veredito enquanto fase ja aprovada tiver task aberta"*. Ele funcionou.

**Nao fechei essas tasks e nao usei `harness pipeline override`:** fechar desfecho de tarefa de
outra fase nao e ato meu (escritor unico, `ADR-005`), e o `override` e escape do **owner**. O
veredito deste documento vale como esta escrito; **falta o append no ledger**, e ele so entra
depois que a fase 02 tiver as tres tarefas com desfecho declarado.

---

**Regras bloqueantes avaliadas: 7 de 7** listadas por `harness rules list --severity block`.
