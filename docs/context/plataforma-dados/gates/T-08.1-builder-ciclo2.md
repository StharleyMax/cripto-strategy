# Gate report — T-08.1, builder ciclo 2 (resposta ao `NEEDS_FIX` do QA), `CST-69`

**Fase 08, componente `docs`.** QA original: `docs/context/plataforma-dados/gates/T-08.1-qa.md`
(veredito `NEEDS_FIX`, 3 achados). Builder ciclo 1: `docs/context/plataforma-dados/gates/T-08.1-builder.md`.

## Achados fechados

**1. `write_parquet.py` sem `compression_level=19`.** Adicionado explícito na chamada de
`pq.write_to_dataset` (linha 37). Antes do fix, `du -sb built/parquet` (sem o parâmetro) =
1.236.601 B, confirmado reproduzindo o script tal como estava, antes de editar — bate com o
achado do QA (2,048×, reprova ≤2×).

**2. `OUT_DIR` (`write_parquet.py:16`) divergia de `verify_duckdb.py:13`
(`built/parquet_z19`).** Corrigido alinhando `OUT_DIR = "built/parquet_z19"` — não tocado
`verify_duckdb.py` nem o README, que já citavam o nome certo. `verify_duckdb.py` rodado sem
qualquer edição, aponta pro diretório real agora.

**3. Reexecução completa do pipeline, do zero (`raw/`, `built/`, container `spike-ts`
recriados), comandos literais:**

```bash
python3 fetch_data.py --symbols BTCUSDT,ETHUSDT,SOLUSDT,BNBUSDT --days 30 --out-dir ./raw
# -> 8.624 linhas × 4 símbolos = 34.496 [MEDIDO 2026-09-04]
tar -czf raw_source.tar.gz raw/*.csv && du -sb raw_source.tar.gz    # -> 603.709 B
python3 build_dataset.py --raw-dir ./raw --out-dir ./built          # -> 35.419 linhas

docker run -d --name spike-ts -e POSTGRES_PASSWORD=spike -e POSTGRES_USER=postgres \
  -e POSTGRES_DB=spike -p 15433:5432 timescale/timescaledb:2.17.2-pg15
psql ... -f schema.sql
psql ... -c "\copy market_series FROM 'built/dataset.csv' WITH (FORMAT csv, HEADER true)"
psql ... -c "SELECT compress_chunk(c) FROM show_chunks('market_series') c;"
psql ... -c "VACUUM ANALYZE market_series;"
psql ... -c "SELECT hypertable_size('market_series');"
# -> 909.312 B  (idêntico byte a byte ao número já citado na emenda de ADR-002)
python3 verify_timescale.py
# -> OVERALL: PASS, scan 0,0479s, n=34496

python3 write_parquet.py && du -sb built/parquet_z19
# -> 1.200.765 B = 1,989× (ratio calculado: 1200765/603709) [MEDIDO 2026-09-04, ciclo 2]
python3 verify_duckdb.py
# -> OVERALL: PASS, scan 0,0119s, n=34496 -- roda sem editar nada além do OUT_DIR

# contraste com zstd default (sem compression_level), medido à parte para confirmar a direção
# do achado do QA:
python3 -c "... compression='zstd' sem nivel ..." && du -sb built/parquet_default
# -> 1.233.261 B = 2,043× (reprova ≤2×; mesma direção do 2,061× já na emenda)
```

`hypertable_size` bate byte a byte com o número já na emenda (909.312 B). O ratio de espaço
do Parquet z19 (1,989×) bate com o já citado — o byte absoluto varia na 3ª casa (1.200.765 B
vs. 1.200.986 B da emenda) porque o dado é um **refetch real da Binance** (`--days 30` a
partir de "agora"), não o mesmo CSV bit a bit; o comportamento do motor (razão de compressão)
é o que o critério mede, e ele se confirma.

## Efeito na decisão / `ADR-002`

**Números batem com a emenda existente → `ADR-002` NÃO foi tocado**, conforme a instrução
de despacho ("se os números baterem, não toque no ADR — só reprodutibilidade"). O defeito era
só o script não reproduzir o número já documentado; corrigido o script, ele reproduz.

## Regras bloqueantes

`harness rules --mode sweep --changed-only` → `rc=0`, saída vazia. Universo: `write_parquet.py`
(modificado) + `README.md` do spike (modificado) — nenhum sob `code_paths` (mesma conclusão do
QA original para este diretório).

## Doc delta

- `docs/spike/T-08.1-motor-armazenamento/README.md`: nova seção **"Correção de
  reprodutibilidade — ciclo 2"**, **append** ao fim do arquivo — nenhuma linha/número
  pré-existente reescrito, os dois bugs e a reexecução completa citados com os comandos
  acima.
- `docs/adr/ADR-002-motor-de-armazenamento.md`: **sem mudança** — números confirmados reais,
  nenhuma emenda nova necessária (ver "Efeito na decisão" acima).
- `docs/INDEX.md`: 1 linha nova, append (`git diff --numstat` → `1 0`).

## Artefatos gerados, não versionados

`raw/`, `built/`, `raw_source.tar.gz` (dado de terceiro/derivado, `CLAUDE.md` "Dado bruto não
é versionado") e o container `spike-ts` — todos removidos após a medição
(`rm -rf raw built raw_source.tar.gz`; `docker rm -f spike-ts`). `git status --porcelain`
final: só `README.md` e `write_parquet.py` modificados.

## Bloqueado

Nenhum. Os 3 achados do QA (compression_level ausente, diretório divergente, número não
reproduzível) estão fechados e reconfirmados por reexecução do zero.
