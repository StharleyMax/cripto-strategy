[QA GATE — Fase 06: As QUATRO séries de L/S com colunas próprias; `ls_ratio` genérico PROIBIDO]
Feature: `plataforma-dados`     Componente: `sentimento`     Task: `T-06.3` (`CST-47`)
PR: https://github.com/StharleyMax/cripto-strategy/pull/103, branch `tasks/T-06.3-fase06`, worktree `/tmp/claude-1002/wt/T-06.3`
Base no momento da validação: `206d211` (`T-06.3`) sobre `935eb97` (`T-06.2`) sobre `fdb22aa` (`T-06.5`).

## Escopo verificado
- `backend/src/modules/sentimento/domain/series_key.py` (editado)
- `backend/src/modules/sentimento/domain/long_short_ratio_series.py` (novo)
- `backend/tests/sentimento/test_long_short_ratio_series.py` (novo)
- `backend/tests/sentimento/test_series_identity.py` (editado)

## Rebase — verificado, NÃO executado (fora do escopo de QA, sem conflito em produção)
`git fetch origin` mostrou `origin/master` em `0670c52` (T-05.2) e `f238c15` (T-06.9), ambos
posteriores ao ponto de partida desta branch (`935eb97`). `git merge-tree $(git merge-base HEAD
origin/master) HEAD origin/master`: **1 único conflito**, em `docs/INDEX.md` (duas linhas
append-only inseridas no mesmo ponto — o padrão já nomeado no handoff, "mantenha AMBAS as
seções"). **Nenhum conflito em código de produção** — os arquivos que `T-06.9`/`T-05.2` tocam
(`cvd_source_catalog.py`, `fee_schedule.py`, `price_source_catalog.py`, `frontend/src/charts/*`)
não se sobrepõem aos dois arquivos desta task. `backend/README.md`, apesar de tocado por ambos
os lados, resolve limpo (sem `<<<<<<<`) no merge-tree. **Ação necessária antes do merge da PR**:
rebase trivial resolvendo o append de `docs/INDEX.md` — não fiz esse rebase (é ação de
`/build`/orquestrador, não de QA; QA só edita teste), e ele não muda nenhum dos números abaixo,
medidos sobre o código de produção desta task, intocado pelas duas branches concorrentes.

## Testes existentes rodados antes de qualquer teste novo ser escrito
`bash backend/scripts/test.sh` (suite completa, sem filtro) → **1335 passed**, 0 failed, 3
warnings, 366s. Cobertura total **97,65%**. `bash scripts/check-coverage-layers.sh`: domain
**99,8%** (2590/2594, meta 90%) `[OK]`, use_cases **100,0%** (585/585, meta 80%) `[OK]`, infra
**95,1%** (2208/2321, meta 70%) `[OK]` — 3/3 camadas medidas de 3 declaradas.
(Nota: builder relatou 1307/97,63%; a diferença de contagem — 1335 aqui — não vem desta task:
o corpo de produção/teste é byte-idêntico ao commit `206d211` avaliado, `git status` limpo
durante toda a sessão. Não investiguei a fundo por não afetar nenhum veredito abaixo — mas
registro o número que MEDI, não o do relatório do builder, por disciplina de "nenhum número sem
o comando".)

`bash scripts/lint.sh` → `ruff check` + `ruff format --check` + `mypy --strict`: sem achado, 239
arquivos. `bash scripts/boundaries.sh` → 3 kept, 0 broken (158 arquivos, 716 dependências).
`bash scripts/natureza.sh` → 73 arquivo(s) (54 domain + 19 use_cases), 0 leitura(s) de relógio.
`harness rules --mode sweep --changed-only` (rodado no worktree, na raiz do repo) → **rc=0**.

## D6.4 — autocorrelação, MUTAÇÃO PLANTADA E REVERTIDA
Reproduzi os números do builder rodando `_series_by_metric`/`_autocorrelation_lag1` diretamente:
BTCUSDT `{count_lsr: 0,9974, count_toptrader: 0,9974, sum_toptrader: 0,9989}` taker `0,0899`;
COTIUSDT `{0,9966/0,9968/0,9940}` taker `-0,0083`; DOGEUSDT `{0,9970/0,9962/0,9986}` taker
`-0,0022`; SLXUSDT `{0,9909/0,9941/0,9972}` taker `0,0365` — batendo exatamente com os 4 valores
de taker citados no relatório do builder e com `PRD-001-plataforma-dados.md:288`. As 3 séries de
posicionamento ficam entre 0,9909 e 0,9989 (todas ≥0,99, dentro do falsificador do DoD; os
dígitos exatos `0,9999/0,9999/0,9996` do DoD vêm de outra janela, disclaimer explícito no
docstring do teste — a janela desta task é `2026-08-17..23`, não a janela original da SPEC).
**Mutação**: editei `metrics_csv_reader.py` para alimentar `sum_taker_long_short_vol_ratio` com
a coluna `count_long_short_ratio` (série de posicionamento) em vez da coluna real do taker.
Resultado: `test_d6_4_the_taker_series_has_near_zero_lag1_autocorrelation` reprovou nos 4
símbolos (`0,9908...` em vez de `<0,10`) e `test_d6_4_taker_orthogonality_is_below_0_10_in_all_
12_pairs` reprovou no primeiro par (`r=1,0000`). **Morde.** `git checkout --` revertido,
`git status --porcelain` vazio, `diff -q` contra backup confirma byte-identidade.

## D6.5 — `delta()` é erro de TIPO, MEDIDO À MÃO
Reproduzi o comando exato do builder num scratch file fora do commit:
`delta(TakerRatioComponents(buy_vol=Decimal("1"), sell_vol=Decimal("1")),
PositionRatioObservation(metric="count_long_short_ratio", value=Decimal("1")))` sob
`.venv/bin/python -m mypy --strict` → `error: Argument 1 to "delta" has incompatible type
"TakerRatioComponents"; expected "PositionRatioObservation"  [arg-type]`, `Found 1 error in 1
file`. Controle positivo: a mesma chamada com dois `PositionRatioObservation` → `Success: no
issues found in 1 source file`. Confere com `PositionRatioMetric` excluindo o taker por membro
(`Literal` de 3) e `TakerRatioComponents` sem campo `.value` — nenhum `isinstance`/`nature ==`
no corpo de `delta()` (confirmado por leitura + pelo teste `test_structural_falsifier_delta_
body_cannot_spell_the_taker_metric`, que faz o mesmo scan por `ast` que `test_universe_at.py`
usa para `DecisiveUniverseSource`). Scratch files removidos, `git status --porcelain` vazio.

## D6.6 — TF 15min sobre taker RECUSA, MUTAÇÃO PLANTADA E REVERTIDA
**Mutação**: editei `resample_bare_taker_ratio_refuses` para, quando `values` não for vazio,
`return sum(values, start=Decimal(0))` em vez de levantar `NonAggregableFlowRatioError` —
exatamente a agregação ingênua que o DoD proíbe. Resultado: `test_d6_6_naive_resample_of_the_
real_taker_series_refuses` (dado real, `binance/metrics/btcusdt/2026-08-23.csv`) reprovou com
`DID NOT RAISE NonAggregableFlowRatioError`; `test_resample_bare_taker_ratio_refuses_never_
touches_its_argument` reprovou igual. **Morde nas duas frentes** — no dado real e no teste de
"nunca toca o argumento". A função nunca retorna `3,1809`: no código real (não mutado) ela
sempre recusa antes de qualquer aritmética, confirmado pela mutação não ter conseguido produzir
UM número sequer sem que os dois testes acusassem. `git checkout --` revertido, `diff -q` contra
backup confirma byte-identidade.

## `ls_ratio` genérico PROIBIDO — confirmado por grep
`grep -rn "ls_ratio" backend/src --include='*.py'`: 4 ocorrências, todas dentro de
`FORBIDDEN_METRIC_NAMES`/`_FORBIDDEN_METRIC_REASONS`/comentários que EXPLICAM a proibição —
nenhuma como nome de série, coluna, variável ou constante ativa. `LONG_SHORT_METRICS` (produção)
lista exatamente as 4 séries nomeadas, e `test_the_four_metrics_are_named_and_ls_ratio_is_not_
one_of_them` fixa isso com `assert "ls_ratio" not in LONG_SHORT_METRICS`.

## Item 6.10 (buyVol/sellVol REST) — só a FORMA, nomeado no relatório
Confirmado no relatório do builder (`gates/T-06.3-builder.md`, seção final "Bloqueado"):
"Nenhuma ingestão REST de `takerlongshortRatio` existe ainda — `TakerRatioComponents` é a forma
que uma REST client futura preenche". Não está escondido — está na seção de escopo não fechado,
nomeada, e o módulo (`long_short_ratio_series.py:37-44`) repete o mesmo disclaimer no cabeçalho.
`TakerRatioComponents` de fato carrega `buy_vol`/`sell_vol` como campos (não descarta), e
`resample_taker_ratio_to_timeframe` só opera sobre eles — confirmado por leitura direta.

## Contrato `Natureza` (`ADR-016`) — domain puro
`grep -n "^import\|^from" long_short_ratio_series.py`: só `__future__`, `collections.abc`,
`dataclasses`, `decimal`, `typing` — zero I/O, zero relógio. `bash scripts/natureza.sh`: 0
leituras de relógio no universo de 73 arquivos. `bash scripts/boundaries.sh`: 3 kept, 0 broken.

## Idioma
Identificadores, mensagens de exceção e docstrings dos 2 arquivos de produção e 2 de teste: só
uma ocorrência de acento encontrada por `grep -nP '[áàâãéêíóôõúçÁÀÂÃÉÊÍÓÔÕÚÇ]'`, em
`test_long_short_ratio_series.py:278`, e é uma CITAÇÃO literal entre aspas de `SPEC-001` §5.11
("pedir TF 15m na série taker recusa; nunca devolve 3,1809") dentro de um docstring em inglês —
mesmo padrão já em uso em `series_key.py` (cita "RATIO de estoque" da SPEC). Não é identificador
novo em português; é citação de fonte, precedente já aceito neste repositório.

## Regras bloqueantes (7 listadas por `harness rules list --severity block`)
- [OK] `core.relative-import` — `grep -n "^from \.\|^import \."` nos 4 arquivos: 0 ocorrências.
- [OK] `core.silent-except` — nenhum `except:`/`except ... pass` nos arquivos de produção.
- [OK] `core.print-statement` — nenhum `print(` nos arquivos de produção.
- [OK] `core.hardcoded-secret` — nenhuma credencial nos arquivos (grep manual + `lint.sh` limpo).
- [N/A] `web-fullstack.browser-imports-server` — task é 100% `backend`/`sentimento`.
- [N/A] `web-fullstack.tenant-from-request` — sem rota HTTP nesta task.
- [N/A] `web-fullstack.server-test-directory-present` — regra sobre AUSÊNCIA de diretório de
  testes do servidor `web`; não se aplica a `sentimento`.
- Confirmação agregada: `harness rules --mode sweep --changed-only` → rc=0, 0 achados.

## DoD (bloco `T-06.3`, `docs/context/plataforma-dados/tasks.toml`)
- [OK] 4 séries com colunas próprias, `ls_ratio` proibido (grep + teste).
- [OK] D6.4 (autocorrelação real, mutação morde).
- [OK] D6.5 (erro de tipo, `mypy --strict` reproduzido à mão).
- [OK] D6.6 (recusa por construção, mutação morde nas 2 frentes).
- [OK] Item 6.10 (buyVol/sellVol carregados, escopo futuro nomeado, não escondido).
- [OK] `Natureza`/`ADR-016` (domain puro, sem I/O).
- [OK] Idioma inglês (1 citação literal de SPEC, precedente aceito).
- [anomalia] Rebase pendente contra `origin/master` — só conflito em `docs/INDEX.md`
  (append-only, trivial), zero conflito em código de produção. Não bloqueia este veredito
  porque não muda nenhum arquivo avaliado; é ação para antes do merge da PR, não para o QA.

Regras bloqueantes avaliadas: 7 de 7
Veredito: APPROVED
