# QA Gate — `T-09.4` / item `9.6` / DoD `D9.6` · componente `infra` adotado, vocabulário 6 → 7

**Objeto validado:** commit `dd0e703` (merge `f63fb5b`, PR #116), já aterrado em `master`.
**Modo:** gate de escopo explícito. **Veredito: `APPROVED`, com 1 aviso não bloqueante provado por comando.**

**⚠️ Nota de método:** a árvore de trabalho estava suja com a `T-01.8` em curso (`harness.toml`,
`.claude/agents/*`, `docs/gate-de-design.md`). Para não medir trabalho alheio, **todas as sondas de
política e de enum rodaram em worktrees detached** criadas para este gate:
`git worktree add --detach <scratch>/wt-after dd0e703` e `<scratch>/wt-before dd0e703^`.

---

## 1 · O que o commit tocou — e o que ele NÃO tocou

```
git show dd0e703 --numstat
  1   0  docs/INDEX.md
 57   0  docs/adr/ADR-009-reuso-da-forma-do-anything.md
 42   1  harness.toml
```

**3 arquivos, zero código.** `tasks.toml` intocado, ledger intocado, `.claude/agents/` intocado.

### `ADR-009` emendada por APPEND — provado por identidade de bytes, não por `numstat`

`57 0` prova ausência de deleção, **não** prova que a inserção foi no fim. Verificado direto:

```bash
git show dd0e703^:docs/adr/ADR-009-...md | wc -l                       # 293
git show dd0e703:docs/adr/ADR-009-...md  | wc -l                       # 350
diff <(git show dd0e703^:...) <(git show dd0e703:... | head -293)      # sem saída  => IDENTICO
```

⇒ **`D1`–`D6.7` byte-idênticos**; `D6.8` é append puro. `D6.5` **não** foi reescrita — a frase
*"nada foi escrito em `harness.toml` por esta emenda"* continua lá, e mantê-la é correto: era
medição datada, e apagá-la esconderia que o documento esteve decidido antes da política.
`docs/INDEX.md`: `1 0`, linha acrescentada no fim ⇒ append-only respeitado.

---

## 2 · DoD `D9.6` — *"a decisão está escrita nas duas direções possíveis — adotado **ou** recusado, **com o motivo** — nunca ausente"*

**`OK`.** As três direções do menu estão escritas **com o custo**, em três superfícies concordantes:

| direção | motivo escrito | onde |
|---|---|---|
| **ADOTADO `infra`** | *"põe o schema HTTP e o TLS/compose sob o mesmo juiz — duas classes de risco diferentes com um só dono"* | `harness.toml:38-40,45-47` · `ADR-009` §`D6.8` · `docs/decisoes-do-owner.md:818` |
| **RECUSADO `api`** | *"nome preciso para a API, mas **mente para o worker** (`jobs/` não é API) e deixa `ADR-009/D5` aberto para `deploy/`/backup ⇒ duas decisões em vez de uma"* | idem |
| **RECUSADO nenhum rótulo** | *"zero ato de política, mas cai em `ADR-003:11-13` (componente sem dono de julgamento) e faria `sentimento` rotular código que não é dele — falso por construção"* | idem |

A recusada não é enfeite: ela carrega o custo, que é o que `D9.6` pede. **Uma direção só teria
reprovado**, e não é o caso.

### O rótulo de owner — `OK`, e foi conferido no diff, não no texto renderizado

```bash
git show dd0e703 | grep '^+' | grep -c 'PREMISSA-OWNER'                    # 0
git show dd0e703 | grep '^+' | grep -c 'DECIS.O-OWNER\|DECISAO-OWNER'      # 3
```

**Zero `[PREMISSA-OWNER]` nas linhas adicionadas.** As 3 ocorrências são
`[DECISÃO-OWNER: 2026-09-03, escolha entre alternativas apresentadas]`, e as três dizem
explicitamente *"NÃO é fala do owner"*. É a distinção que `CLAUDE.md` §*"os dois rótulos de owner
não são o mesmo ato"* exige: o owner **escolheu** de um menu redigido por agente; ele não **disse**
a frase. Rotular como premissa teria inventado uma fala — não aconteceu.

---

## 3 · Verde não prova nada — a mutação, REFEITA por mim, com controle diferencial

O builder alegou a sonda. **Não aceitei a alegação: refiz em arquivo avulso próprio**
(`<scratch>/qa-mutation-t094.toml`, 2 tasks: `T-99.1 components=["infra"]`, `T-99.2 components=["foo"]`)
e rodei o **mesmo arquivo** contra os **dois commits**, que é o que a alegação do builder não tinha:

```bash
# DEPOIS  (worktree em dd0e703)
harness tasks validate <scratch>/qa-mutation-t094.toml
  ERROR ...:T-99.2 V-16 componente fora do enum: foo
        (validos: sentimento, charts, convergencia, backtest, web, docs, infra)
  # T-99.1 (infra): NENHUM V-16  => CALA

# ANTES   (worktree em dd0e703^)
  ERROR ...:T-99.1 V-16 componente fora do enum: infra (validos: ... docs)
  ERROR ...:T-99.2 V-16 componente fora do enum: foo   (validos: ... docs)
```

`[MEDIDO 2026-09-04, n=2 tasks × 2 commits]` ⇒ **reproduz**. E o par diferencial prova mais do que a
sonda do builder: o enum **não** virou permissivo — o mesmo byte que antes reprovava `infra` agora o
aceita, **e `foo` continua reprovado**. Exatamente **um** rótulo a mais.

**Declarado para a saída não parecer mais limpa do que é:** a sonda também emite `V-06`, `V-07`,
`V-18` e `V-24` (feature sem ledger, `local_only` sem `local_reason`, `phase 99` sem arquivo de
plano) — `5 ERROR, 2 WARN` no total. **Nenhum deles é `V-16`**, e nenhum interfere na mutação.

### Efeito no arquivo real, medido nos dois lados

```bash
harness tasks validate plataforma-dados
# ANTES  (dd0e703^): ERROR V-16 em T-05.12 e T-05.13 — FALHOU: 2 ERROR, 4 WARN
# DEPOIS (dd0e703) : OK: 93 task(s), 0 ERROR, 4 WARN   rc=0
harness validate --strict   # rc=0, "politica valida: cripto-strategy (schema_version=1)"
harness policy --key components
# ["sentimento","charts","convergencia","backtest","web","docs","infra"]   (7)
harness doctor              # veredito: CONFORME (12 checagens)
```

As **4 `WARN`** são `V-09`/`blocked_reason` em `T-02.4b`/`T-03.9`/`T-05.10`/`T-07.11`,
**pré-existentes**, e **continuam** — o ato não as tocou e não se creditou a limpeza.

---

## 4 · `F-D6-5b` — avaliado no marco dele, e a contagem é minha

Critério: *"ao fechar `9.6`/`T-09.4`, contar as naturezas distintas que declaram `infra`: **≥ 2** —
a camada consumidora **e** ao menos uma de `deploy/`/backup/topologia"*.

| natureza | evidência **em `dd0e703`** | comando |
|---|---|---|
| **camada consumidora** (`src/api/`, `src/jobs/`) | `T-05.12` e `T-05.13`, `components = ["docs","infra"]` (linhas 805 e 833) + item `5.13` do plano `05` com componente-alvo **`infra`**, sem marca de forçamento | `grep -n 'components = .*"infra"' docs/context/plataforma-dados/tasks.toml` → **2 tasks** (a 3ª ocorrência, `:774`, é linha de COMENTÁRIO em `T-05.10`, não declaração) |
| **`deploy/`** (compose, reverse proxy, TLS) | item `1.14` do plano `01`, componente-alvo **`infra`** | `grep -c '^| 1.14 |' docs/plans/.../01_governanca_gateante.md` → **1** |

⇒ **2 naturezas distintas ⇒ `F-D6-5b` NÃO dispara** `[MEDIDO 2026-09-04 em dd0e703]`. A alegação do
builder confere. **E o falsificador não está gasto:** se o item `1.14` for retirado ou nunca virar
task, `infra` volta a rotular só a camada de API e `api` (opção `B`) era o nome certo. *(Fora do
escopo deste gate, mas registrado: em `master` hoje há **3** tasks declarando `infra` — a terceira é
`T-01.10`, criada por `2f2a733` para o item `1.14`, o que **fortalece** a segunda natureza.)*

## `F-D6-6` — DISPARADO, e isso está CORRETO

```bash
harness policy --key agents.by_component   # em dd0e703
# donos: backtest, charts, convergencia, sentimento, web  —  `infra` AUSENTE (grep -c infra -> 0)
```

`infra` no enum e ausente da tabela de juízes é literalmente o que `F-D6-6` existe para acusar. **Não
é defeito desta task:** é a ordem que `V-16` impõe (`lib/policy.py:539-543` reprova chave de
`agents.by_component` fora do enum ⇒ enum primeiro, juiz depois), a condição está **nomeada, datada e
com dono** (`T-01.8`/`CST-100`) em `ADR-009` §`D6.8` item 1 e em `harness.toml:67-71`. **Nomeado, não
silenciado** — que é o desfecho certo. *(Confirmado: `T-01.8` está fechando essa janela agora, em
trabalho não commitado que este gate ignorou por construção.)*

---

## 5 · O que a task NÃO devia fazer — 3 de 3 respeitados

| não devia | medição | resultado |
|---|---|---|
| declarar o juiz de `infra` | `harness policy --key agents.by_component` em `dd0e703` → `infra` ausente | **não fez** |
| fechar a cobertura de `deploy/` | `harness policy --key code_paths` → `include_prefixes` com **3** entradas (`backend/src/`, `backend/tests/`, `frontend/src/`); `ls -d deploy` → **inexistente** | **não fez** — e diz por quê: *"rótulo não é cobertura"* |
| `gate-record` / `approve` / `advance` | `harness pipeline show plataforma-dados \| grep 'T-09.4'` → **nenhum evento** até `dd0e703`; `harness pipeline state` → `BUILD_AUTHORIZED` | **não fez** |

---

## 6 · Regras bloqueantes — 7 de 7 avaliadas

`harness rules list --severity block` → **7 regras** (`core.relative-import`, `core.silent-except`,
`core.print-statement`, `core.hardcoded-secret`, `web-fullstack.browser-imports-server`,
`web-fullstack.tenant-from-request`, `web-fullstack.server-test-directory-present`).

Os 3 arquivos do commit foram avaliados **um a um**, e classificados:

```bash
harness rules --mode file --path <arq> --severity block    # rc=0, zero linha, para os 3
harness code-paths classify harness.toml                   # nao-producao
harness code-paths classify docs/adr/ADR-009-....md        # nao-producao
harness code-paths classify docs/INDEX.md                  # nao-producao
```

As 6 primeiras têm escopo `code`/`production` e **nenhum dos 3 arquivos casa `include_prefixes`** ⇒
`OK` por não-aplicabilidade, o que é veredito e não omissão. A 7ª
(`server-test-directory-present`, `path-presence`) é propriedade do repositório e é medida pelo
portão `regras` do `make verify`, abaixo: **0 bloqueio(s)**.

---

## 7 · Testes e cobertura

O commit tem **zero linha de código** ⇒ nenhum teste novo é devido, e criar um seria medir outra
coisa. Rodei a suíte que existe **antes** de qualquer outra conclusão:

```bash
bash scripts/verify.sh    # === verify · cripto-strategy · 20260904T000740Z ===
[OK] lint-backend    rc=0  256 source files
[OK] lint-frontend   rc=0  ESLint do projeto sobre frontend/src
[OK] test            rc=0  1434 passed · Total coverage: 97.74%
[OK] boundaries      rc=0  3 kept, 0 broken
[OK] regras          rc=0  0 bloqueio(s), 16 aviso(s)
[OK] política        rc=0
veredito: VERDE — 6 portões mediram e passaram        (rc=0)
```

**Cobertura medida `97,74%` contra alvo declarado `fail_under = 70`** (`backend/pyproject.toml:103`)
⇒ `OK`, com folga de 27,74 pontos.

**⚠️ Universo declarado, para o número não valer mais do que mede:** o `verify` rodou na árvore de
trabalho (`master` + a `T-01.8` não commitada), **não** em `dd0e703`. É legítimo e é dito: nem
`dd0e703` nem a sujeira da `T-01.8` tocam código (`git status --porcelain -- backend frontend` →
**vazio**). `git diff --stat dd0e703 HEAD -- backend frontend` mostra **7 arquivos** de diferença,
mas eles vêm dos PRs **#113/#114/#115** trazidos pelo merge `2f9467d`, **alheios a esta task**.

---

## 8 · ⚠️ AVISO — um número `[MEDIDO]` que NÃO reproduz, e ele foi escrito por esta task

**Não é bloqueante e não muda decisão nenhuma. É reportado porque omiti-lo seria o defeito.**

`harness.toml:61` (linha **acrescentada** por `dd0e703`):

```
# [MEDIDO 2026-09-03: `harness code-paths classify backend/src/api/...` já devolve
# `producao` sem rótulo nenhum; `backend/src` é 1 dos 19 prefixos].
```

Medido agora, com o instrumento:

```bash
harness pipeline scope plataforma-dados list | grep -c '^'      # 21, nao 19
harness pipeline require-code backend/src/api/routes.py
# [pipeline] código permitido (backend/src/api/routes.py) — feature 'plataforma-dados' (scope)
harness code-paths classify backend/src/api/routes.py
# producao: ... — include_prefixes + include_globs casam e nada exclui
```

**A conclusão da frase é VERDADEIRA e está confirmada** — `backend/src` está no escopo, `classify`
devolve `producao` sem rótulo, e o rótulo de componente de fato **não** governa `[code_paths]` nem
`require-code`. **O que não reproduz é a cardinalidade:** são **21** prefixos, não 19. O escopo foi
ampliado de 19 para 21 **no mesmo dia e ANTES do commit** — eventos `scope` do ledger em
`2026-09-03T22:51:21Z`, contra o commit em `2026-09-03T23:48:29Z` (`Sep 3 20:48:29 -0300`), e a
ampliação está escrita em `docs/decisoes-do-owner.md:967` (*"Escopo de escrita ampliado de 19 para 21
prefixos"*). O `19` foi **copiado de uma medição anterior** (`docs/decisoes-do-owner.md:813`, correta
para o momento dela) e recarimbado com `[MEDIDO 2026-09-03]` quando já valia 21.

**Por que aviso e não `NEEDS_FIX`:** não é `D9.6`, não é regra bloqueante, e a proposição que o
número sustenta é verdadeira e foi verificada de forma independente. **Precedente do próprio
repositório:** o `INDEX.md:159` corrige um número do coordenador *ao lado* dele, sem reverter o ato.
**Conserto de uma linha, e há um dono à mão:** a `T-01.8` está editando `harness.toml` neste momento
— trocar `19` por `21` (ou remover a cardinalidade, que não carrega peso de argumento) fecha isto sem
commit dedicado. **Não editei: QA não altera produção.**

---

## Veredito

```
## QA Gate — Fase 09 [docs] · T-09.4 · item 9.6 / DoD D9.6
- [OK] core.relative-import / core.silent-except / core.print-statement /
       core.hardcoded-secret / web-fullstack.browser-imports-server /
       web-fullstack.tenant-from-request — `harness rules --mode file --path <arq> --severity block`
       rc=0, zero linha, nos 3 arquivos; `code-paths classify` -> `nao-producao` nos 3
- [OK] web-fullstack.server-test-directory-present — `scripts/verify.sh` portão `regras`: 0 bloqueio(s)
- [OK] Testes existem e passam — `bash scripts/verify.sh` VERDE, 6 portões, 1434 passed (rc=0)
- [OK] Cobertura 97,74% contra alvo 70 (`backend/pyproject.toml:103 fail_under = 70`)
- [OK] D9.6 as duas direções COM motivo — ADOTADO `infra` + RECUSADO `api` + RECUSADO nenhum rótulo,
       com o custo de cada uma, em 3 superfícies concordantes
- [OK] Rótulo — `git show dd0e703 | grep '^+' | grep -c PREMISSA-OWNER` -> 0 · DECISÃO-OWNER -> 3
- [OK] `ADR-009` append puro — `diff` do arquivo anterior contra as 293 primeiras linhas do novo: IDÊNTICO
- [OK] Mutação REFEITA por QA, com controle diferencial — `infra` MORDE em dd0e703^ e CALA em dd0e703;
       `foo` MORDE nos dois  [MEDIDO, n=2 tasks × 2 commits]
- [OK] F-D6-5b não dispara — 2 naturezas (T-05.12/T-05.13 + item 1.14)
- [OK] F-D6-6 disparado, mas NOMEADO/datado/com dono (T-01.8) — desfecho correto, não defeito
- [OK] Fronteiras não cruzadas — juiz não declarado, `deploy/` não fechado, ledger não tocado
- [aviso, não bloqueante] `harness.toml:61` — `[MEDIDO]` diz "1 dos 19 prefixos"; o instrumento
       devolve 21. A conclusão da frase é verdadeira; só a cardinalidade está velha. Conserto de
       uma linha, dono à mão em `T-01.8`.
Regras bloqueantes avaliadas: 7 de 7 listadas por `harness rules list --severity block`
Veredito: APPROVED
```
