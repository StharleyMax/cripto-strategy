# T-08.13 — gate `QA`

**Veredito: APPROVED.** A alegação do arquiteto/`builder` foi verificada por leitura direta do
código de produção (não da ADR) e por **mutação plantada e revertida** em cada um dos 5
invariantes — nenhum deles é opinião não-falsificável.

## Verificação por DoD

- **D8.15/D1** (idade fora da borda) — leitura de `observation.py`/`histogram.py`/`scan.py`:
  nenhum de `Observation`, `Fired`, `NotFired`, `Insufficient`, `PointMass`, `Bin`, `Overflow`,
  `HistogramResult`, `ScanResult` tem campo de idade. **Mutação:** acrescentei `age_ms: int = 0`
  a `ScanResult` → `test_d1_no_pooled_or_aggregate_type_carries_an_age_field` reprovou
  (`AssertionError: {'ScanResult': {'age_ms'}}`). Revertido, `git diff` limpo.
- **D8.16/D2** (invariante por série, não constante global) — `as_of_accessor.py:343-369`
  (`reject_delay_threshold_above_staleness`) pré-existe, mensagem cita `series_key_id`,
  `delay_threshold_ms`, `asof_ms` — nunca um número global; coberto por
  `tests/sentimento/test_as_of_accessor.py`. **Mutação:** acrescentei
  `ASOF_MAX_STALENESS_MS = 5000` em `firing_rate.py` (dentro de `charts/`) →
  `test_d2_no_staleness_literal_inside_charts` reprovou. Revertido.
- **D8.17/D5** (retrospectiva por assinatura) — `compute_distribution`/`run_scan`/
  `compute_firing_rate` exigem `window`/`knowledge_time_ms` sem default; `Window.end_ms: int`
  sem `| None`. **Mutação 1:** `live: bool = False` em `run_scan` →
  `test_d5_no_use_case_accepts_a_live_flag_or_an_optional_end_ms` reprovou. **Mutação 2:**
  `while False: pass` em `compute_firing_rate.py` → `test_d5_no_polling_construct_inside_use_cases`
  reprovou. Ambas revertidas.
- **D8.18/D3** (`denom` verbatim) — `FieldIdentity` é `@dataclass(frozen=True)` com
  `(metric, unit, denom)`; `__eq__`/`__hash__` gerados cobrem os 3 termos. **Verificação direta
  (não mutação):** `FieldIdentity(..., denom="base_contracts") != FieldIdentity(..., denom="notional_usd")`,
  hashes diferentes, `dict` os distingue como chaves separadas — nunca comparação cross-denom
  silenciosa (`.venv/bin/python -c "..."` rodado nesta sessão). **Mutação:** acrescentei
  `_infer_denom` com `re.match` em `field_identity.py` →
  `test_d3_no_denom_heuristic_inside_charts` reprovou. Revertido. `compute_histogram`/
  `evaluate_scan` confirmados com `field: FieldIdentity` único (não coleção).
- **D8.20/D4** (zero seleção) — `ObservationSource.observed_values` assinatura
  `(field, nature, universe, window, knowledge_time_ms)`, sem `spec`. **Mutação:** acrescentei
  `spec: object = None` à assinatura → `test_d4_the_read_port_never_accepts_a_threshold_or_spec_parameter`
  reprovou. Revertido. Nenhum `use_case` chama outro por nome (`ast.walk` sobre os 3 módulos).

## Comandos rodados nesta sessão (literais)

```
bash backend/scripts/lint.sh
→ ruff check: All checks passed! · ruff format --check: 310 files já formatados · mypy: Success

bash backend/scripts/test.sh
→ 1581 passed, 3 warnings in 375.69s (0:06:15); cobertura TOTAL 97,80%; por camada
  domain 99,9%/use_cases 100,0%/infra 95,2% — 3/3 metas OK (bate a alegação do builder)

.venv/bin/python -m pytest tests/charts --no-cov
→ 105 passed in 0.20s (bate a alegação do builder: 89 pré-existentes + 16 novos)

harness rules --mode sweep --changed-only
→ rc=0, saída vazia — nenhuma das 8 regras `block` disparou
```

## DoD (D8.15/D8.16/D8.17/D8.18/D8.20)

- [x] D8.15 — apoiado por D1, confirmado por leitura + mutação. Texto do chip é `web`/`T-08.12`,
  fora de escopo desta task (componente `charts` apenas) — consistente com `ADR-024`.
- [x] D8.16 — pré-existente em `sentimento`, confirmado por leitura direta e teste já verde.
- [x] D8.17 — confirmado por leitura + 2 mutações (live flag, while loop).
- [x] D8.18 — confirmado por leitura + verificação direta de hash/eq + mutação.
- [x] D8.20 — confirmado por leitura + mutação.

Regras bloqueantes avaliadas: 8 de 8 listadas por `harness rules list --severity block`.

## Nota de escopo (não bloqueia)

`D8.15`/`D8.17` na tabela do plano `08_superficie_e_reprodutibilidade.md` incluem uma cláusula de
UI ("chip substituído", "escrito na própria tela") que o `ADR-024` explicitamente marca como
"Não fecha" (dono é `web`/`T-08.12`). Como `T-08.13.components = ["charts"]`, isto é escopo
correto, não lacuna — mas o `T-08.12`/rota HTTP terá de reabrir essas duas cláusulas quando
existir.
