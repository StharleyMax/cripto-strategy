[REVIEW — Fase 06: Quarentena pelo predicado de TRÊS termos, D6.2 falsificador da fase]
Feature: `plataforma-dados`   Componente: `sentimento`   PR: #104 (`tasks/T-06.6-fase06` → `master`, OPEN)
Comando de diff usado: `git diff master...HEAD` (three-dot, merge-base `6d3f284`) após `git fetch`;
`master` local == `origin/master` == `6ecb9ae` no momento da revisão.
Arquivos varridos (7, via `git diff master...HEAD --name-only`): `backend/README.md`,
`backend/src/modules/sentimento/domain/live_availability_write.py` (novo),
`backend/src/modules/sentimento/domain/quarantine_terms.py` (estendido),
`backend/tests/sentimento/test_live_availability_write.py` (novo),
`backend/tests/sentimento/test_quarantine_terms.py`, `docs/INDEX.md`,
`docs/context/plataforma-dados/gates/T-06.6-builder.md`.

## Camada 1 — o que o runner mede
`harness rules list --severity block` → 7 regras em vigor. `harness rules --mode sweep --changed-only`
(reproduzido nesta revisão) → 0 achados, rc=0. 7 de 7 regras bloqueantes avaliadas, 0 violadas.
`git diff master...HEAD -- backend/README.md docs/INDEX.md` — reproduzido: as duas seções são
puramente ADITIVAS (só linhas `+`), sem nenhum `-` de linha existente. Append-only confirmado.

Independentemente do QA (não apenas relido, reexecutado nesta revisão):
- `bash backend/scripts/natureza.sh` → 76 arquivos (57 domain + 19 use_cases), 0 leituras de relógio.
- `bash backend/scripts/boundaries.sh` → 162 arquivos, 734 dependências, 3 contratos KEPT (camadas,
  fronteira de contexto, `Natureza`/socket-ssl), 0 broken.

## Camada 2 — arquitetura declarada

**Contrato `Natureza` (`ADR-016`)** — CONFORME. Leitura de código (não apenas o `boundaries.sh`):
`quarantine_terms.py` importa só `series_catalog` (domain); `live_availability_write.py` importa só
`availability_lag_stats` e `provenance` (domain). Nenhum dos dois faz I/O; `resolve_unmeasured_
endpoint_availability` DECIDE (`tuple[None, AvailabilitySource]`) e não persiste — exatamente a
divisão que o handoff exige ("a persistência de `available_at`/`availability_source` em
`md.ingest_run` é `infra`", que este módulo corretamente NÃO implementa).

**Predicado de 3 termos, recalculado, não OR-de-2-com-bônus** — CONFORME. `QuarantineTerms.
is_quarantined` (`quarantine_terms.py:52-54`) é `not (label_shift_present and unit_present and
available_at_present)` — os três lidos e recombinados a cada chamada, nunca cache parcial nem
"bônus" de dois termos abrindo sozinho. Teste exaustivo sobre as 8 combinações
(`test_quarantine_is_the_negation_of_all_three_terms_present`, `itertools.product`) fecha o espaço.

**D6.2 sobre dado REAL da Coinalyze** — CONFORME. `_coinalyze_close_entry()` lê
`open_interest_catalog_entries().entry_for(coinalyze_open_interest_key(Reduction.CLOSE))` — linha de
produção de `T-06.5`, não fixture sintética. Confirmado em código: `open_interest_catalog.py:53,61`
define `_INTERVAL_MS = 300_000` / `OPEN_INTEREST_LABEL_SHIFT_MS = _INTERVAL_MS`, batendo com o
`label_shift=300_000` que o teste lê via `entry.key` (nunca hardcoded). Com os dois primeiros termos
presentes e `available_at_present=False`, `is_quarantined is True` — a Coinalyze permanece isolada,
como o handoff exige ("não force uma abertura prematura").

**Resíduo de `label_shift` divergente** — NENHUM encontrado. `grep -n "label_shift"` nos 4 arquivos
do diff mostra só leitura via `entry.key`/comentários — nenhum literal numérico hardcoded no teste
(o `label_shift=0` do ciclo anterior, citado pelo QA como corrigido, não aparece em nenhum lugar).

**Idioma — identificador de produção/teste (CLAUDE.md, tabela §"fronteira", linha 1/2)**
⚠️ **WARNING real, não pego por regra mecânica** (o repositório declara, por doutrina, que não mede
idioma de identificador — `ADR-013/D2e` — então isto não é achado de `harness rules`, é leitura da
arquitetura declarada): `quarantine_gaveta` (função de PRODUÇÃO, `quarantine_terms.py:103`) e a
variável local `gaveta` (`quarantine_terms.py:145`, e replicada em `test_quarantine_terms.py:146,164,
170,174` como nome de função de teste e variável) misturam o termo de domínio em português "gaveta"
— que É vocabulário legítimo de `SPEC-001`/`PRD-001`/`ADR-014` em PROSA (`count(gaveta)`, "a gaveta
não recebe...") — dentro de um identificador de código. A tabela de fronteira do `CLAUDE.md` (12
linhas, `CA-F1-1`) manda produção e teste para inglês (linhas 1 e 2) e só abre exceção para o
vocabulário FECHADO de componentes (linha 9: `sentimento`/`charts`/`convergencia`/`backtest`/`web`/
`docs`) e para nome de coluna de contrato já declarado (linha 11, com citação de código e ADR dona).
"Gaveta" não está em nenhuma das duas listas — não é um dos 6 componentes, não é coluna de contrato.
O precedente da própria tabela (linha 11) mostra o padrão correto para um termo de spec que precisa
sobreviver em código: exceção DECLARADA, com citação de linha e ADR dona. Isso não existe aqui.
**Severidade: WARNING (convenção, não portão — `CLAUDE.md` §"Idioma de identificador é convenção,
não portão" é explícito em recusar qualquer instrumento mecânico para isto), não BLOCKER.** Não
muda o veredito binário (que é function de regra bloqueante), mas é achado real e citável.
Correção concreta: renomear `quarantine_gaveta` → algo como `quarantine_drawer` ou
`quarantined_series_ids`, e a variável local `gaveta` → `quarantined`/`drawer`; manter "gaveta" só
em docstring/comentário citando a SPEC literalmente (linha 5/7 da tabela, já em conformidade).

Resto do idioma (docstrings, mensagens de exceção, nomes de teste) — CONFORME: `grep` para
identificadores portugueses nos dois arquivos de produção não achou outro caso além de "gaveta";
`MeasuredLagCannotUseUnmeasuredPathError` e sua mensagem estão em inglês; sem import relativo
(`core.relative-import`), sem `except` silencioso, sem `print`.

## Denominador
7 regras bloqueantes existiam (`harness rules list --severity block`), 7 avaliadas (via sweep
changed-only + leitura manual de cada uma contra os arquivos do diff), 0 violadas. 7 arquivos
varridos via `git diff master...HEAD --name-only`. 1 achado de arquitetura declarada (WARNING,
idioma de identificador, não coberto por regra mecânica, citado contra `CLAUDE.md` tabela linha 1/2
+ precedente da linha 11).

## Veredito
**COMPLIANT** — nenhuma regra bloqueante violada (0/7). O achado de idioma é WARNING de convenção,
explicitamente não-portão por doutrina deste repositório, e não bloqueia `T-06.7`/`T-06.8`. Recomendo
o rename de `gaveta`→inglês como follow-up de baixo custo (não bloqueante) antes que `T-06.7`/`T-06.8`
importem `quarantine_gaveta` e propaguem o nome para mais chamadores.
