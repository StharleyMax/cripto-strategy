# QA Gate — T-03.12 · Portao de natureza por USO (scanner de AST) + estreitamento atomico do contrato 3

**Autor:** `/qa` · **Data:** 2026-09-01 · **Feature:** `plataforma-dados` · **Fase:** `03` · **Componente:** `sentimento`
**PR:** https://github.com/StharleyMax/cripto-strategy/pull/50 · **Branch:** `tasks/T-03.12-natureza-por-uso`
**Base:** `master@a3c63c5` · **Commit unico avaliado:** `c7df9fd`
**Worktree:** `/tmp/claude-1002/wt/T-03.12`

Fontes lidas por inteiro: `docs/context/plataforma-dados/handoff/T-03.12.md`,
`docs/context/plataforma-dados/gates/T-03.12-builder.md`,
`docs/adr/ADR-016-relogio-e-capacidade-tipo-de-data-e-valor.md`, o bloco `id = "T-03.12"` de
`docs/context/plataforma-dados/tasks.toml` (linhas 487-513), `backend/scripts/natureza.py`,
`backend/scripts/natureza.sh`, e o diff completo do commit contra `master@a3c63c5`.

## Mentalidade destrutiva — o que foi reproduzido, nao apenas lido

### Atomicidade (DoD-1/D5)

`git show --stat c7df9fd` mostra um UNICO commit contendo `backend/scripts/natureza.py` (novo),
`backend/scripts/natureza.sh` (novo), alvo `natureza` no `Makefile`, a chamada em
`scripts/hooks/pre-push.pre-harness`, E a edicao de `backend/pyproject.toml` que estreita
`forbidden_modules` de `["socket","ssl","time","datetime"]` para `["socket","ssl"]`. Confirmado
por `git diff a3c63c5 c7df9fd` — as duas metades (scanner + estreitamento) estao no mesmo diff.
**PASS.**

### DoD-2 — arvore limpa

`bash backend/scripts/natureza.sh` → `rc=0`: "natureza: 17 arquivo(s) em .../domain",
"natureza: 7 arquivo(s) em .../use_cases", "natureza: universo de 24 arquivo(s), 0 leitura(s) de
relogio". **PASS.**

### DoD-3 — bateria de 6 mutantes formais + 2 achados extras, UM POR VEZ, arvore reconferida entre cada

Reproduzido em copia isolada (`cp -r backend backend-mutscratch`), original nunca tocado.
Cada mutante plantado sozinho, scanner rodado, mutante removido, arvore reconferida limpa antes
do proximo:

| mutante | expressao | resultado |
|---|---|---|
| M1 | `datetime.now()` | `rc=1`, `datetime.now` na linha certa |
| M2 | `date.today()` sem import novo | `rc=1`, `date.today` |
| M3 | `import time` + `time.monotonic()` | `rc=1`, `time.monotonic` |
| M4 | `f(datetime.now)` sem chamar | `rc=1`, `datetime.now` |
| M5 (achado extra 1) | `from time import monotonic` chamado NU | `rc=1`, `monotonic (=time.monotonic)` |
| M6 | `time.ctime()` | `rc=1`, `time.ctime` |
| achado extra 1b | `from time import monotonic as m; m()` | `rc=1`, `m (=time.monotonic)` |
| achado extra 2 | `getattr(date, "today")()` | `rc=0` — **ponto cego confirmado, declarado no cabecalho do script (item 3) e no DoD-9** |

6/6 mutantes formais MORDEM, os dois achados extra da ERRATA 1 (from-time-import-monotonic
nu/alias) TAMBEM MORDEM, e o ponto cego declarado (`getattr`) de fato escapa exatamente como
documentado — nao e bug escondido, e limite runtime declarado com dono (`/architect`). Arvore
restaurada a `rc=0`, 24 arquivos, 0 leituras apos remover todos os mutantes. **PASS.**

### DoD-4 — zero falso positivo, n=9, conferido linha a linha

`grep -n 'date\.\|timedelta'` em `dump_window.py` (129,171,174,194,196,214,230) e
`retention_probe.py` (131,132): todas sao anotacao de tipo (`period: date`, `end_date: date`,
`old_period: date`) ou aritmetica de calendario (`timedelta(days=...)`) — nenhuma alcanca os
nomes de relogio da taxonomia. `natureza.sh` sobre a arvore real confirma silencio (`0
leitura(s)`). **PASS.**

### DoD-5 — particao exaustiva e disjunta

`_confere_particao()` chamada isoladamente nao dispara `RECUSA`. Recontagem independente:
`dir(time)` em Python 3.13 = 38 publicos; `L(14) + E(3) + A(6) + ARIDADE(5) + P(10) = 38`, sem
sobreposicao. **PASS.**

### DoD-6 — contrato estreitado ainda morde

`bash backend/scripts/boundaries.sh` → `Contracts: 3 kept, 0 broken` (nome do contrato:
"Natureza: domain e use_cases nao falam com socket nem ssl"). Com `import ssl` plantado ao fim
de `dump_window.py` (real, revertido com `git checkout --` em seguida — `git status --short`
confirma arvore limpa): `rc=1`, `src.modules.sentimento.domain.dump_window -> ssl (l.252)`.
**PASS.**

### DoD-7 — sweep

`git add -A && harness rules --mode sweep` → `rc=0`, zero `[BLOQUEIO]`. **PASS.**

### DoD-8 — o bloqueio some

`git ls-remote https://github.com/StharleyMax/cripto-strategy.git
refs/heads/tasks/T-03.12-natureza-por-uso` devolve `c7df9fd...`, identico ao `HEAD` local — o
push real ja aconteceu e esta ao vivo (PR #50, `state=OPEN`, `mergeable=MERGEABLE`). Confirmado
tambem que `master` atual (`origin/master@840c500`) AINDA tem `forbidden_modules =
["socket","ssl","time","datetime"]` — ou seja, o "antes" quebrado e real, nao hipotetico. Dentro
do pipeline local de `git push --dry-run` (reproduzido nesta sessao), `make boundaries` e `make
natureza` executam e passam LIMPOS (`3 kept, 0 broken`; `0 leitura(s)`); a tentativa de dry-run
nesta sessao esbarrou num problema **nao relacionado a esta task** — o protocolo `require-push`
do harness le stdin vazio nesta reproducao manual em worktree (mecanismo de ledger, nao o
scanner/contrato desta task) — anomalia de ambiente, nao falha do artefato sob teste. A evidencia
mais forte (SHA remoto == HEAD local, PR aberta e mergeable) sustenta PASS no que e responsabilidade
desta task. **PASS** (com anomalia de ambiente anotada, fora do escopo do que T-03.12 modifica).

### DoD-9 — ponto cego documentado no proprio script

Reproduzido acima (achado extra 2): `getattr(date, "today")()` → `0`. Cabecalho de
`natureza.py`, item (3), declara exatamente este caso. **PASS.**

### DoD-10 — falsificador contavel

Formato `RELOGIO EM CAMADA PURA: <arquivo>:<linha> -> <expressao>` confirmado estavel nas
reproducoes acima. Nenhum `rc=1` sobre arvore real (so em copias mutadas) — placar desta task
0x0, placar de referencia da ADR continua 0x3. **PASS.**

### DoD-11 — nao reabre o que nao e seu

`git diff --stat a3c63c5 c7df9fd -- docs/adr` → vazio. `docs/adr/bancadas/ADR-016-natureza.py`
intocado. Status `proposto` da ADR-016 nao tocado. **PASS.**

## Gate do componente

- `bash backend/scripts/lint.sh` → `rc=0`, "83 files already formatted", "no issues found in 83
  source files".
- `bash backend/scripts/test.sh` → `rc=0`, **560 passed**, cobertura domain 100%/use_cases
  100%/infra 98.3% (3/3 camadas medidas, meta 90%/80%/70%).

## Regras bloqueantes (`harness rules list --severity block`, 7 regras)

Nenhuma delas se aplica a Python de scanner/CLI de forma que exigisse achado especifico alem do
sweep (DoD-7), que ja cobre as 7 em `code`/`production`. Sweep: `rc=0`, 0 bloqueios.

## Anomalia registrada, e por que nao vira FAIL nem OK

`harness gate-record plataforma-dados 03 QA APPROVED ...` foi **RECUSADO pelo portao de
consistencia doc-vs-ledger**, mas por INCONSISTENCIA EM OUTRAS FASES, nao na fase 03:

```
- fase 02: 3 de 5 tarefa(s) em estado nao-terminal: T-02.1, T-02.2, T-02.4b
  — contradiz o veredito QA APPROVED de 2026-08-29T13:08:52Z
- fase 04: 6 de 7 tarefa(s) em estado nao-terminal: T-04.1, T-04.3, T-04.4, T-04.5, T-04.6, T-04.7
  — contradiz o veredito QA APPROVED de 2026-09-01T18:20:13Z
```

Executado a partir do repo principal (`/home/stharley/Documentos/projects/cripto-strategy`),
como a instrucao de despacho manda para este caso — mesmo assim recusou. **Isto e uma
divida de outras fases (02 e 04), preexistente a T-03.12, e nao e desta task fechar
(`tasks.toml` e escrito pelo escritor unico, nunca a mao, e a task T-03.12 nao tem autoridade
sobre o desfecho de T-02.x/T-04.x).** Sem `pipeline override`, por instrucao explicita do
despacho. O gate NAO foi gravado no ledger; este documento e a evidencia tecnica completa do
merito da task, para ser gravado assim que a divida das fases 02/04 for resolvida por quem tem
essa autoridade.

## Veredito

**Pelo merito tecnico da T-03.12 (os 11 itens do DoD): APPROVED — 11/11 PASS**, incluindo os 2
achados extras nao declarados pela ADR original, reproduzidos independentemente pelo QA.

**Gravacao no ledger: BLOQUEADA por divida de outras fases**, nao pelo conteudo desta PR. Acao
necessaria antes de `gate-record` aceitar: fechar o desfecho de T-02.1/T-02.2/T-02.4b (fase 02)
e de T-04.1/T-04.3/T-04.4/T-04.5/T-04.6/T-04.7 (fase 04) via `harness tasks resolve`, ou emitir
`harness pipeline override` com motivo — decisao que nao cabe a este gate de T-03.12.
