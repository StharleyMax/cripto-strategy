# Review Gate — T-05.7 (`/review`, read-only, independente de builder/QA) · **Veredito:** COMPLIANT

# T-05.7 — review gate (`/review`, read-only, independente de builder/QA)

**Escopo:** `frontend/src/charts/color-tokens.ts` (novo), `frontend/src/charts/color-tokens.test.ts`
(novo), `frontend/src/charts/s2-headless-run.ts` (diff aditivo, campo `style`/`appliedCandlestickColors`),
`frontend/src/charts/s2-axis-integration.test.ts` (diff, +1 assert de cor), `frontend/README.md` §13,
`docs/INDEX.md` (+1 linha), `docs/context/plataforma-dados/gates/T-05.7-builder.md`,
`docs/adr/ADR-003-fronteira-charts-web.md`, `docs/adr/ADR-010-governanca-de-cor-por-tipo-de-marca.md`,
`frontend/eslint.config.mjs` (regra `D5.12`), `scripts/validate_palette.js`,
`docs/context/plataforma-dados/tasks.toml:670-685` (`T-05.7`/`CST-41`).

**Método:** branch `tasks/T-05.7-cor-token` (commit `5844bd4`) já estava checked out num worktree
irmão (`wf_2a5f5015-5f1-5`); em vez de reutilizá-lo, criei um worktree independente e descartável
(`git worktree add --detach .../scratchpad/review-T-05.7 tasks/T-05.7-cor-token`), removido ao final
(`git worktree remove --force`) — nenhum efeito no repositório principal nem nos demais worktrees.

## Camada 1 — o que o runner mede

`harness rules list --severity block` → **n=7** regras bloqueantes (`core.relative-import`,
`core.silent-except`, `core.print-statement`, `core.hardcoded-secret`,
`web-fullstack.browser-imports-server`, `web-fullstack.tenant-from-request`,
`web-fullstack.server-test-directory-present`). **Avaliadas 7 de 7 — nenhuma disparou.**

`harness rules --mode file --path <f>` rodado individualmente sobre os 4 arquivos tocados
(`color-tokens.ts`, `color-tokens.test.ts`, `s2-axis-integration.test.ts`, `s2-headless-run.ts`) →
**rc=0, 0 achados em cada um.** `harness rules --mode sweep --changed-only` → **rc=0, 0 achados**
(mesmo comando e mesmo resultado que o builder relatou — confirmado, não só copiado).

**Reproduzido de forma independente, sem confiar no relato do builder/QA** (não havia gate de QA
para `T-05.7` no momento desta revisão — só o builder self-attested):

```
npm --prefix frontend install
  -> added 128 packages, audited 129 packages, 0 vulnerabilities
npm --prefix frontend run lint          (eslint src)
  -> rc=0, 0 erro/aviso
npm --prefix frontend run test:charts   (node --test 'src/charts/*.test.ts', universo: 14 arquivos *.test.ts)
  -> 76 pass, 0 fail, 0 cancelled, 0 skipped
node scripts/validate_palette.js
  -> rc=0, "medicoes executadas nesta rodada: 361"
```

Os 76/0 e o `exit 0`/361 batem exatamente com o que `gates/T-05.7-builder.md` e a entrada de
`docs/INDEX.md` (2026-09-03) declaram. **Pré-condição de ambiente confirmada de forma independente:**
`data/` (gitignored, ~850MB) não existe em worktree novo (`ls data` → `ENOENT`); criei o mesmo
symlink que o builder descreve (`ln -s <repo-principal>/data data`) e `git status --porcelain`
confirma que ele fica `??` (não rastreado) — a alegação de que "nada além de `frontend/README.md`
foi `git add`ado" é consistente com o estado observável.

## Camada 2 — arquitetura declarada

### `ADR-003` FR-1 (`charts` não faz I/O) — sem violação nesta task

`docs/adr/ADR-003-fronteira-charts-web.md:36`: *"`charts` não faz I/O. Zero `fetch`, zero rota,
zero `localStorage`. Toda entrada é argumento."* `color-tokens.ts` (produção) é módulo puro — hex
literais + 2 funções, zero `import` de `node:fs`/`node:path`/rede. `s2-headless-run.ts` só ganhou
um campo opcional `style?: Record<string, unknown>` passado adiante para `chart.addSeries(...)` —
não lê nada do disco. **Só `color-tokens.test.ts`** (arquivo de teste, não produção) chama
`readFileSync` para ler `scripts/validate_palette.js` como texto — o mesmo padrão já estabelecido e
julgado aceitável em `s2-axis-integration.test.ts` (que já lia klines/OI/aggTrades reais antes desta
task) e distinto da classe de violação que `T-05.2-review.md` (rodada 1) apontou como WARNING
(I/O em módulos `.ts` de produção, não `.test.ts`) — aquele WARNING já estava fechado antes de
`T-05.7` começar (rodada 2 do mesmo gate). **Nenhuma regressão introduzida.**

### `D5.12` (fronteira executável `charts`⇄`web`, `eslint.config.mjs`) — intacta

`grep -n "^import\|from \"" frontend/src/charts/color-tokens.ts frontend/src/charts/color-tokens.test.ts`
→ só `node:*` e imports relativos dentro de `src/charts/`. Nenhum import de `**/app/**` ou
`**/features/**`. A suíte `test:charts` inclui os próprios testes MORDE+CALA de `D5.12`
(`eslint-boundary.test.ts`), que passaram (76/76) contra o código real após esta task — a fronteira
não foi tocada nem quebrada.

### `ADR-010/D-5` (`critical` fora do canal de cor, `CA-F4-10`) — a alegação central da task, verificada

`ColorRole` é união fechada de 6 membros (`directionUpFill`, `directionDownFill`, `directionOn`,
`dataBrokenInk`, `provenanceStrong`, `provenanceWeak`) — **sem** `critical`/`severidade`/`severity`
em nenhuma forma (`color-tokens.ts:47-53`). Guarda dupla, como declarado: estática (o union não tem
o membro) e runtime (`assertNoForbiddenColorRoles` + `FORBIDDEN_COLOR_ROLE_SUBSTRINGS`), exercitada
em `color-tokens.test.ts` **rejeitando** 4 violadores reais (`criticalFill`, `severityFill`,
`severidadeFill`, `CriticalFill`) — não só "typecheck limpo". `scripts/validate_palette.js` (fonte
única da aritmética, `docs`-owned) também não usa `critical`/`severidade` como papel — consistente
(`grep` confirma as únicas ocorrências de "severidade" no script são comentário citando `ADR-010`
§9). Os 6 hex por modo batem, hex a hex, com a tabela `PAPEIS` do script (2 dos 76 testes fazem
esse cross-check e passaram).

### Escopo — `acao-*`/`foco` corretamente excluídos

`color-tokens.ts:15-21` declara explicitamente por que os papéis de chrome DOM (`acao-*`, `foco`)
ficam fora — citando `ADR-003` FR-1 + o boundary `D5.12`. `grep -rn "acao-\|foco\b"` sobre os
arquivos `.ts` de produção só encontra essas duas linhas de docstring explicando a exclusão — nenhum
código implementa esses papéis. Escopo respeitado.

### `CLAUDE.md` — fronteira de idioma

`grep -noE '\b[a-zA-Z_][a-zA-Z0-9_]*\b'` sobre `color-tokens.ts`/`color-tokens.test.ts` filtrado por
palavras PT comuns (`nao`, `janela`, `preco`, `dados`, `serie`, `erro`, `falha`, `arquivo`, `codigo`)
→ só falsos positivos por substring (`Error`, `precondition`) — nenhum identificador, nome de
arquivo ou mensagem de `throw new Error(...)` em português. As duas mensagens de erro do módulo
(`color-tokens.ts:78-81`) estão em inglês.

### `docs/INDEX.md` — append-only

`git diff <pai>..<tip> -- docs/INDEX.md` → `+1` linha (a entrada de `2026-09-03`, builder
self-attested), zero linha existente reescrita.

### `tasks.toml` — estado não avançado por agente

`status = "todo"` (linha 673) inalterado pela task, consistente com "veredito pende de `/qa`+`/review`
independentes" — nenhum `gate-record`/`approve`/`advance` executado pelo builder.

## Nota de processo (não entra no veredito)

Não encontrei `docs/context/plataforma-dados/gates/T-05.7-qa.md` no worktree revisado — só o
builder report existia no momento desta revisão (esta é uma auditoria de arquitetura/regras
mecânicas, independente da camada de QA; não bloqueia o veredito de `/review`, mas o denominador de
"builder+QA+review" desta task está com a perna de QA ainda pendente, fora do meu escopo verificar).

## Veredito

```
Regras bloqueantes avaliadas: 7 de 7 (harness rules list --severity block)
Arquivos varridos: 4 arquivos tocados via `harness rules --mode file --path <f>` (rc=0 nos 4)
  + `harness rules --mode sweep --changed-only` (rc=0) + leitura integral dos 4 diffs
  + git diff de docs/INDEX.md, tasks.toml, eslint.config.mjs, ADR-003, ADR-010
Comandos reproduzidos nesta sessão, de forma independente: npm --prefix frontend install,
  npm --prefix frontend run lint (rc=0), npm --prefix frontend run test:charts (76 pass, 0 fail,
  universo: 14 arquivos frontend/src/charts/*.test.ts), node scripts/validate_palette.js
  (rc=0, 361 medições)
Veredito: COMPLIANT
Achados: 0 bloqueantes, 0 WARNING arquitetural. `ADR-003` FR-1 e `D5.12` intactos (produção sem
  I/O; único I/O é em .test.ts, padrão já estabelecido). `ADR-010/D-5` (critical fora do canal
  de cor) implementado com guarda dupla, testada rejeitando 4 violadores reais. Escopo respeitado
  (acao-*/foco corretamente fora). Fronteira de idioma sem violação. docs/INDEX.md append-only.
```

