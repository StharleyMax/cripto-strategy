# QA — T-03.4 · Agregado de bucket `q`/`nq` do stream `aggTrade` (CL-5)

**Veredito técnico: APPROVED** (ledger não gravado — dívida pré-existente da fase 02, ver nota abaixo)

- `bash backend/scripts/test.sh` → **652 passed**, rc=0. Cobertura: domain 100,0% (piso 90%),
  use_cases 100,0% (piso 80%), infra 97,8% (piso 70%).
- `bash backend/scripts/boundaries.sh` → 3 kept, 0 broken. `bash backend/scripts/natureza.sh` → 0
  leituras de relógio. `bash backend/scripts/lint.sh` → limpo (100 arquivos). `harness rules
  --mode sweep --changed-only` → rc=0, 0 achados.
- **D3.5**: confirmado por leitura que `require_contiguous` delega em
  `aggtrade_contiguity.require_unique_agg_ids`/`detect_agg_id_gaps`
  (`aggtrade_bucket_aggregate.py:153-175`), sem reimplementar a invariante. Mutação (remover o
  `if nq_trade_count else None` no fold) quebra 3 testes estruturais.
- **D3.7**: reproduz byte a byte a evidência real de `ADR-001` sobre
  `data/binance/rest/nq_{BTC,ETH,SOL,XRP,DOGE}USDT.json` — DOGEUSDT 16/1000, 80,56bp; demais
  0/1000. Mutação (inverter sinal em `deficit_bp`) quebra 2 testes, incluindo o de DOGEUSDT.
- **D3.8**: confirmado que leitura sob `quantity_field=nq` antes da 1ª captura retorna
  `Absence.NO_SOURCE` (`"SEM_FONTE"`), reutilizando o mecanismo já provado por `T-04.4`
  (`as_of_accessor.py`), não reimplementado.

**Ledger:** `harness gate-record plataforma-dados 03 QA APPROVED ...` → recusado (`rc=4`) pelo
portão de tasks.toml: fase 02 tem `T-02.1`/`T-02.2` em `todo` (não-terminal), contradizendo o
veredito QA já aprovado daquela fase. Dívida pré-existente, fora do escopo de `T-03.4` — sem
`pipeline override`. Regrava assim que a fase 02 fechar.
