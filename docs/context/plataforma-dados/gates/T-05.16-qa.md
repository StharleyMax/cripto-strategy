# T-05.16 — QA (Front) — DoD D5.17(b): a fronteira de importação que mantém `fingerprint()` síncrona

`CST-107`. Escopo do diff: `frontend/eslint.config.mjs` (+64), `ingest-health-query.ts` (+12,
docstring only), `fingerprint-sync-boundary.test.ts` (novo, 160 linhas).

## Checklist

- **[OK] DoD D5.17(b)** — `05_fatia_visivel.md:91` exige: (i) importador de VALOR de
  `ingest-health-query.ts` plantado fora do módulo ⇒ portão reprova nomeando o arquivo; (ii)
  árvore real hoje = 3/3 `import type`, zero aresta de runtime. Ambos reproduzidos por mim,
  independentemente do teste do builder — ver "Mutação manual" abaixo.
- **[OK] `fingerprint()` permanece síncrona** — `grep -n "^export function fingerprint"
  ingest-health-query.ts:305-307` → `export function fingerprint(...): string`, retorno direto
  de `sha256Hex(...)`, sem `Promise`, sem `async`. Diff do arquivo é só docstring
  (`git diff --stat` → `+12 -0`, dentro de um bloco `/**...*/` já existente).
- **[OK] Instrumento é o herdado de `D5.12`, não redecidido** — `@typescript-eslint/
  no-restricted-imports` (variante TS-aware, único com `allowTypeImports`), mesmo padrão dos 2
  blocos simétricos já em produção para `charts`↔`web`. `ADR-003:46` não alcança este arquivo
  (não alimenta `harness.toml`/`[agents.by_component]`) — herdado, correto.
- **[OK] Testes existem, passam e têm o par morde/cala** — `npm --prefix frontend run test:s1`
  → **54/54 pass** `[MEDIDO, universo: 6 arquivos `*.test.ts` sob `s1-console/`]`. O teste novo
  planta 2 violadores DE VALOR (sibling + same-dir) + 1 controle `import type`, MORDE asserted
  ANTES de CALA (falsificador de vacuidade `ADR-012` aplicado), probes removidos em `finally`.
- **[OK] Mutação manual independente, fora do teste do builder** — plantei
  `src/features/s3-inspector/_qa-manual-value-probe.ts` importando `fingerprint` POR VALOR de
  `../s1-console/ingest-health-query.ts`: `eslint src --format json` → **`rc=1`**, mensagem
  nomeando exatamente `_qa-manual-value-probe.ts` com `ruleId:
  "@typescript-eslint/no-restricted-imports"`. Revertido, replantei com `import type` apenas →
  **`rc=0`**, mensagens `[]` para o arquivo. `git status --porcelain` depois → nenhuma sonda
  restante.
- **[OK] CALA da árvore real** — os 3 importadores reais (`s3-inspector/{fixtures,domain,
  view-model}.ts`) são todos `import type` `[MEDIDO por `grep -n "from .*ingest-health-query"
  frontend/src --include='*.ts' --include='*.tsx' | grep -v '\.test\.'` → 3 linhas, todas
  `import type`]`; `S1Console.tsx` importa só `S1ViewModel` de `./view-model.ts`, zero aresta
  direta — confirmado por leitura de import, não presumido.
- **[OK] `make lint-frontend`** → `rc=0` (ESLint + `tsc -p tsconfig.json --noEmit --strict`).
- **[OK] `npm --prefix frontend run test:s3`** → 37/37 pass (regressão, não tocado).
- **[OK] `harness rules --mode sweep --changed-only`** → `rc=0`, saída vazia (nenhum
  bloqueante sobre o diff).
- **[OK] `harness code-paths classify` do teste novo** → `producao` (confirmado por mim).
- **[PARCIAL] `make verify`** — travei o próprio `scripts/verify.sh` duas vezes (180s e depois
  ~6min) e ele nunca passou de `lint-frontend`: o gate `test` (backend, `pytest --cov`, 1437
  casos) estava rodando de fato — processo `python -m pytest` vivo, consumindo CPU — não é o
  "mecanismo NÃO RESOLVÍVEL" de `regras`/`política` que o builder citou (não cheguei nem perto
  desse gate). Não é regressão desta task: `lint-backend` e `lint-frontend`, os dois que
  rodaram, saíram `OK`/`rc=0`. `harness rules --mode sweep --changed-only` (substituto já aceito
  em `T-05.11-qa`/`T-05.14-qa` para o gap de `regras`/`política` nesta worktree) rodou limpo. Não
  bloqueante, mas o motivo que o builder deu ("mecanismo NAO RESOLVIVEL") não é o que eu
  observei — registro a divergência, não a suprimo.
- **[OK] Sem segredo no cliente** — `grep -rniE 'api[_-]?key|secret|token|password'` sobre os 3
  arquivos do diff → 0 ocorrência.
- **[OK] Contrato na borda / ADR-005** — nada de novo na borda de rede; este item é só o portão
  de importação, não toca `fetch`/envelope.
- **[N/A] Acessibilidade** — 0 arquivo `.tsx`/UI tocado.
- **[OK] Doc delta — `docs/INDEX.md`** — "sem mudança" do builder tem motivo e **precedente
  reproduzido por mim**: `T-05.15-builder.md:65` usa a MESMA frase ("sem mudança... padrão já
  usado pelos módulos vizinhos"), e `docs/INDEX.md` **recebeu** entrada para `T-05.15` mesmo
  assim — mas escrita DEPOIS, na linha de fechamento de build+qa+review
  (`docs/INDEX.md:174`, 2026-09-04T16:20Z), não pelo builder. Mesmo padrão em `T-05.1`
  (`docs/INDEX.md:132`). Não é achado — é como este repo já fecha o ciclo: quem faz a linha é
  quem fecha a task (coordenador/QA), não o builder isolado. **Ação de acompanhamento, não
  bloqueante**: a entrada de `docs/INDEX.md` para `T-05.16` deve ser escrita no fechamento do
  ciclo, mesmo formato das duas anteriores.
- **[OK] `docs/product/STITCH_CONTEXT.md` sem mudança** — confirmado, 0 `.tsx` no diff
  (`git status --porcelain | grep -c '\.tsx$'` → 0).
- **[OK] Rótulos de força e números com comando** — os `[MEDIDO 2026-09-04]` do gate report do
  builder são re-verificáveis e eu os reproduzi de forma independente (comandos próprios, não
  apenas relidos).

## Achados

Nenhum `BLOCKER`. Um `WARNING` não bloqueante:

1. **[WARNING]** `make verify` não completou nesta sessão de QA (travou no gate `test`
   backend por tempo de execução, não por erro) — o veredito de `make verify` fica com o
   substituto (`lint-backend` OK + `lint-frontend` OK + `harness rules sweep` rc=0), igual ao
   precedente de `T-05.11-qa`/`T-05.14-qa`, mas a CAUSA da não-conclusão observada aqui
   (pytest lento) diverge da causa citada pelo builder (regras/política "NAO RESOLVIVEL"). Não
   reprova a task; registrado para quem revisitar `make verify` nesta worktree.

## Veredito: APPROVED

Ações: nenhuma bloqueante. Recomendado (não bloqueante): a linha de `docs/INDEX.md` para
`T-05.16` deve ser escrita no fechamento do ciclo (padrão `T-05.1`/`T-05.15`), não antes.
