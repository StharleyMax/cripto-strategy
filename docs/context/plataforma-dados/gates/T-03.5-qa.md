[QA GATE — Fase 03: Captura contínua]
Feature: plataforma-dados     Componente: sentimento     Task: T-03.5 (CST-21)
PR: https://github.com/StharleyMax/cripto-strategy/pull/58 (branch tasks/T-03.5-premiumindex,
base master@634833b) · Worktree: /tmp/claude-1002/wt/T-03.5

## Escopo verificado
- backend/src/modules/sentimento/domain/premium_index_batch.py
- backend/src/modules/sentimento/use_cases/collect_premium_index.py
- backend/src/modules/sentimento/infra/premium_index_http_client.py
- backend/src/modules/sentimento/infra/premium_index_jsonl_sink.py
- backend/src/modules/sentimento/infra/premium_index_probe_cli.py
- backend/tests/sentimento/{test_premium_index_batch,test_collect_premium_index,test_premium_index_infra}.py
- backend/tests/sentimento/test_infrastructure_durability.py (modificado, +1 teste de fsync)
- backend/README.md, .gitignore

## Reprodução independente (não apenas citação do builder)

1. `bash backend/scripts/lint.sh` → `All checks passed!`, mypy `Success: no issues found in 101
   source files`. `[MEDIDO 2026-09-01]`
2. `bash backend/scripts/test.sh` → `655 passed in 608.34s`, `rc=0`. Cobertura por camada
   (`check-coverage-layers.sh`, ADR-009/D1): domain **100.0%** (meta 90%, 1158/1158) · use_cases
   **100.0%** (meta 80%, 289/289) · infra **98.4%** (meta 70%, 967/983) · TOTAL 99.31%. Números
   idênticos aos do relatório do builder. `[MEDIDO 2026-09-01]`
3. `bash backend/scripts/boundaries.sh` → `Contracts: 3 kept, 0 broken` (77 arquivos, 279
   dependências). `[MEDIDO]`
4. `harness rules --mode sweep --changed-only --format ndjson` (rodado no worktree) → saída
   vazia, rc=0. `[MEDIDO]`

## Estrutura: chamada SEM filtro de símbolo (confirmação destrutiva pedida)

- `domain/premium_index_batch.py:46`: `PREMIUM_INDEX_ENDPOINT = "/fapi/v1/premiumIndex"` —
  string literal sem `?`, nenhum parâmetro.
- `grep -rn "urlencode|params|query|\"symbol\"" infra/premium_index_http_client.py
  use_cases/collect_premium_index.py infra/premium_index_probe_cli.py` → 0 ocorrências de
  construção de query string.
- `PremiumIndexHttpClient.fetch()` faz **uma** `connection.request("GET",
  PREMIUM_INDEX_ENDPOINT, …)` por chamada; `collect_premium_index_once()` chama `fetcher.fetch()`
  **uma vez** por ciclo, sem laço sobre lista de símbolos. `run()` do CLI itera só sobre
  `--cycles` (ciclos no tempo), nunca sobre símbolo.
- Confirmado: **não há** iteração símbolo a símbolo que multiplicaria o peso — o único jeito de
  pedir mais de um símbolo aqui seria reintroduzir `symbol=` no path, e nenhuma função aceita
  esse argumento.
- Discrepância 875 (tasks.toml/handoff) vs 888 (probe do builder) símbolos: **não é defeito** —
  `domain/premium_index_batch.py:17-30` documenta que o universo é fato do dia, não invariante, e
  cita o mesmo padrão de drift já registrado em `docs/decisoes-do-owner.md:364` (875 vs 872 entre
  `exchangeInfo`/`premiumIndex` no mesmo instante). O parser nunca fixa contagem de símbolo como
  regra.

## Falsificador — os 2 mutantes reproduzidos ao vivo (não apenas aceitos por citação)

(a) **domain** — `parse_premium_index_batch`: troquei `if reading.symbol in seen:` por `if False
and reading.symbol in seen:` → `pytest tests/sentimento/test_premium_index_batch.py::test_rejects_a_repeated_symbol`
→ `Failed: DID NOT RAISE InvalidPremiumIndexPayloadError`. Revertido (`git checkout --`).
`[MEDIDO 2026-09-01]`

(b) **infra** — `premium_index_probe_cli.main()`: troquei `if not all(delta == …):` por `if
False and not all(delta == …):` → `pytest
tests/sentimento/test_premium_index_infra.py::test_probe_cli_main_returns_one_when_delta_diverges`
→ `assert 0 == 1`. Revertido (`git checkout --`). `[MEDIDO 2026-09-01]`

Árvore confirmada limpa após os dois reverts (`git status --short` só mostra
`frontend/package-lock.json`, ruído fora de escopo já sinalizado pelo despacho).

## Colisão com T-02.1 (segunda testemunha)

- `git ls-tree -r --name-only tasks/T-02.1-snapshot-diario` → cliente é
  `infra/binance_futures_snapshot_client.py` (módulo distinto de
  `infra/premium_index_http_client.py`). T-02.1 redeclara seu próprio Protocol de conexão
  ("Same connection shape as `https_quota_probe.py`"); T-03.5 **reusa** diretamente
  `ConnectionFactory`/`HttpConnection`/`open_https_connection`/`flatten_headers` de
  `https_quota_probe.py` em vez de redeclarar. Nenhuma classe duplicada entre as duas branches —
  só a constante de path `/fapi/v1/premiumIndex` aparece nos dois módulos (literal, não client).
  Sem colisão de código; consolidação de cliente HTTP é assunto de `/review` futuro, não
  bloqueante aqui (conforme o handoff já previa).

## DoD (tasks_review.md:269, tasks.toml id="T-03.5", plano 03 item 3.3)

- [OK] série nasce no primeiro poll, nunca antes — estrutural (sem parâmetro de tempo em nenhuma
  camada)
- [OK] batch sem `symbol`, peso 10 confirmado — estrutural + probe do builder citado com número e
  comando; discrepância de universo (875→888) documentada como drift esperado, não erro
- [OK] código + probe curto, não deploy contínuo — nenhum cron/systemd; CLI de composição manual
- [OK] zero API key — `grep` por `API_KEY`/`X-MBX-APIKEY` nos 3 arquivos: 0 ocorrências
- [OK] Natureza/boundaries preservados — `boundaries.sh` 3 kept, 0 broken

## Veredito
```
## QA Gate — Fase 03 [sentimento]
- [OK] core.silent-except/print-statement/relative-import/hardcoded-secret — harness rules --mode sweep --changed-only: saída vazia, rc=0
- [OK] Testes existem e passam — bash backend/scripts/test.sh (655 testes, rc=0, 608.34s)
- [OK] Cobertura medida domain 100.0%/use_cases 100.0%/infra 98.4% contra alvo 90/80/70
- [OK] DoD da fase, item a item — ver seção acima
- [OK] Falsificador — 2 mutantes plantados ao vivo, ambos derrubam o teste correspondente e revertidos
- [OK] SEM filtro de symbol confirmado estruturalmente (nenhum código itera símbolo a símbolo)
- [OK] Sem colisão de cliente HTTP com T-02.1 (módulos distintos)
Regras bloqueantes avaliadas: 7 de 7
Veredito: APPROVED
```
