# `T-04.7` (`CST-34`) — relatório de gate do `/review`

**Feature:** `plataforma-dados` · **Fase:** `04` · **Componente:** `sentimento`
**PR:** #49 (`tasks/T-04.7-principal-id-dimensao`) · **Base:** `master@840c500`
**Escopo:** `gh pr diff 49` + worktree `/tmp/claude-1002/wt/T-04.7`

## Veredito: COMPLIANT

**Denominador:** 7 regras bloqueantes em vigor (`harness rules list --severity block`), 7 avaliadas
via `harness rules --mode file` sobre os 2 arquivos de código tocados (rc=0, zero achado em ambos) +
`bash backend/scripts/boundaries.sh` (3 contratos import-linter) + `bash backend/scripts/test.sh`
(cobertura por camada). 4 arquivos tocados no PR, todos revisados: `provenance.py`,
`test_provenance_columns.py`, `backend/README.md`, `docs/INDEX.md`.

## Checklist

1. **Contrato "Natureza" (import-linter)** — `bash backend/scripts/boundaries.sh` →
   `Camadas por contexto: infra > use_cases > domain KEPT`, `Fronteira de contexto: sentimento
   nao importa outro contexto KEPT`, `Natureza: domain e use_cases nao falam com socket, ssl nem
   relogio KEPT`. **3 kept, 0 broken.** `[MEDIDO 2026-09-01]`. O diff não toca import nenhum —
   só adiciona um campo `str | None` e uma constante `str`.

2. **Idioma de identificador (CLAUDE.md, produção+teste em inglês)** — todo identificador novo
   (`PRINCIPAL_COLUMN`, `principal_id`, 6 nomes de função de teste) está em inglês. As únicas
   palavras em português no diff (`coluna`, `dimensao`, `identidade`, `linha`) são citação literal
   de `SPEC-001` §4.4 dentro de comentário em inglês (`provenance.py:9-10`), o mesmo padrão já
   estabelecido na linha 7 da tabela de fronteira do `CLAUDE.md`. Convenção, não portão — nenhum
   detector de idioma existe neste repo (`ADR-013/D2e`), então isto é achado por leitura, não por
   `rc`.

3. **Pisos de cobertura** — `bash backend/scripts/test.sh`: domain 100,0% (meta 90), use_cases
   100,0% (meta 80), infra 97,7% (meta 70). `452 passed` (era 444 em `840c500`, +8 = exatamente os
   testes novos). `[MEDIDO 2026-09-01]`, reproduzido nesta sessão, bate com o relatório do builder.

4. **ADR-009/D2 (recusa `organization_id`)** — `grep -rn organization_id backend/` no worktree →
   1 ocorrência, dentro de comentário em `provenance.py:45` explicando por que a coluna NÃO foi
   introduzida. Zero identificador, campo, coluna ou default com esse nome. O campo novo
   (`principal_id: str | None = None`) é por-linha, exigido só quando `provenance is
   Provenance.HUMAN`, nunca uma constante de tenant — a forma que `ADR-009/D2` recusa não foi
   reintroduzida.

5. **`docs/INDEX.md` append-only** — `git diff 840c500 -- docs/INDEX.md` mostra só `+1` linha no
   fim da tabela (`2026-09-01T18:20Z`); as 3 linhas anteriores (`T-04.4` build/qa/build-ciclo2)
   permanecem byte a byte idênticas. Sem reescrita.

## Achados fora do checklist prescrito (nenhum bloqueante)

- Nenhum. O escopo de `principal_id` foi corretamente limitado a `sentimento`/`SeriesRow`;
  `<Anotacao>`/`run_registry` (que também carregam a dimensão) ficaram fora, com a justificativa
  de fronteira de componente citada no próprio código (`provenance.py:41-43`) — condiz com
  `harness policy --key components`.
- `D5.10` (falsificador formal do ator autenticado) foi corretamente declarado fora de escopo,
  reservado para a fase `05` — não é uma lacuna desta task, é a fronteira dela.

## Comandos executados nesta sessão

```
harness rules list --severity block
harness policy --key docs.bootstrap_docs / glossary_doc / code_paths
gh pr diff 49
cd /tmp/claude-1002/wt/T-04.7 && git status / git log
harness rules --mode file --path backend/src/modules/sentimento/domain/provenance.py   # rc=0
harness rules --mode file --path backend/tests/sentimento/test_provenance_columns.py   # vazio
bash backend/scripts/boundaries.sh                                                     # 3 kept, 0 broken
bash backend/scripts/test.sh                                                           # 452 passed
grep -rn organization_id backend/
git diff 840c500 -- docs/INDEX.md
```

Read-only: nenhum arquivo de código foi editado, nenhum teste criado.
