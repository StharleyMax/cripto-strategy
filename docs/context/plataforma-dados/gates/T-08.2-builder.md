# T-08.2 (`CST-70`) — Spike do eixo do Lightweight Charts sob carga cheia · relatório do builder

**Feature:** `plataforma-dados` · **Componente:** `charts` · **DoD:** `D8.19`
**Branch:** `tasks/T-08.2-spike-eixo-charts` · **Base:** `master@aac3442`
**Plano:** `docs/plans/SPEC-001-plataforma-dados/08_superficie_e_reprodutibilidade.md:44`

---

## 1. Veredito, em uma linha

**`D8.19` PASSA no caso que ele descreve, e o número é `2,27e-13 px` de pior caso sobre
`n = 1.728` itens** — mas o verde vale sob uma condição que o DoD não escreve, e a condição
é o achado desta task: **o eixo do Lightweight Charts é ORDINAL, não temporal.**

| cenário | grade de 1 m | pior caso em X | tolerância | veredito |
|---|---|---|---|---|
| **A** — o DoD ao pé da letra | **completa** (1.440/1.440) | **2,27e-13 px** | 0,5 px | **DENTRO** |
| **B** — mutação, cobertura medida em `D8.11` | **20,0%** (314/1.440) | **36,390 px** | 0,5 px | **FORA — 73×** |

`[MEDIDO 2026-08-29]`, com o comando literal:

```bash
npm --prefix frontend run spike:axis     # => node frontend/src/charts/axis-spike.ts
```

Universo de A: **1.728 itens** = 1.440 candles de 1 m + 288 pontos de 5 m, **1.440 timestamps
distintos** num único eixo, pane de **1.138 px** (`timeScale.width()`), **1.137,210 px** entre
as âncoras ⇒ **0,7903 px por slot**. Universo de B: **602 itens** = 314 candles + 288 pontos,
**535 timestamps distintos**, mesma pane.

---

## 2. O que exatamente foi medido, porque uma definição vaga tornaria o número infalsificável

Para cada item renderizado, o erro é `|x_real − x_esperado|`, onde `x_esperado` é o mapa
**afim** de tempo para pixel calibrado nas **duas amostras extremas do próprio gráfico**:

```
esperado(t) = x(t₀) + (t − t₀) / (t_n − t₀) · (x(t_n) − x(t₀))
```

**Ancorar nos extremos do próprio gráfico é a escolha conservadora, e é deliberada:** cancela
todo offset, padding e margem direita que a biblioteca aplica, de modo que um erro não-nulo só
pode vir da **não-linearidade** do mapa tempo→pixel. Uma definição ancorada numa origem externa
reportaria o padding da biblioteca como erro — isso seria espantalho, porque padding não faz o
eixo mentir sobre `event_time`.

**Ressalva que limita a afirmação, escrita porque ela limita:** por construção os dois extremos
têm erro exatamente 0, então o pior caso é sempre encontrado **estritamente dentro** da janela,
e o número publicado é um **limite inferior** do que uma definição ancorada externamente
reportaria — nunca inflado.

Publicado é o **pior caso**. A média aparece como contexto (`5,71e-14 px` em A, `19,216 px` em
B) e **não é o critério** — há teste de unidade que pinta exatamente esse caso: média abaixo da
tolerância com veredito FORA.

---

## 3. O achado, e ele é maior que o veredito

**O eixo do Lightweight Charts indexa por posição ordinal do slot, não por `event_time`.**
Sobre uma grade de 1 m completa, "ordinal" e "linear no tempo" são **a mesma função**, e por
isso o cenário A dá zero-de-float. Assim que a grade tem buracos, as duas funções divergem, e
divergem muito: **36,390 px, 73× a tolerância**, no pior ponto (`2025-01-01T13:47:00Z`, série
candle, `x` real `689,245 px` contra `652,855 px` esperado).

**Por que isso não é hipotético:** a cobertura de 20,0% do cenário B não foi inventada — é o
número que o **próprio plano 08 já mediu** para o timeframe de 1 m (`D8.11`: *"Cobertura: 1m
**20,0%** · 5m 100% · 15m 100% · 60m 99,9% · 240m 99,4% · 1440m 100%"*).

**O que o cenário B NÃO afirma, e a distinção importa:** a **forma** dos buracos é sintética e
semeada (LCG determinístico, `seed = 20260829`), e este spike **não** afirma que ela reproduz a
distribuição real de lacunas. Ele afirma que **naquela cobertura** o eixo ordinal deixa de
concordar com `event_time`, e por quanto.

**Consequência para as 16 tasks de `charts` construídas sobre esta premissa** — e ela é de
arquitetura, não minha: em qualquer painel de `charts`, ou a grade entregue ao gráfico é
**completa e uniforme** (com buracos preenchidos por *whitespace*), ou a posição X **não pode
ser lida como tempo**. Sem uma das duas, o gráfico inventa geometria que não está no dado — que
é a mesma classe de defeito que `D8.14` nomeia para o eixo Y duplo.

> **Dono da decisão: `/architect`.** `T-08.2` mede risco; não decide arquitetura, e **não
> alterei o grafo de dependências** (`D-2` continua `NÃO APLICADA`, como o handoff manda).

---

## 4. Verde não prova nada até uma mutação reprovar — as TRÊS mutações rodadas

### 4.1 Mutação nos DADOS — o cenário B é o falsificador embutido

O runner **sai com `rc≠0` se o cenário B ficar DENTRO da tolerância**, mesmo com A verde: um
instrumento sem poder de rejeitar não produz evidência. Hoje B dá `36,390 px` ⇒ o instrumento
rejeita.

### 4.2 Mutação no INSTRUMENTO — e ela pegou um FALSO VERDE REAL, produzido aqui

**A primeira versão desta medição publicou `0,000 px` e estava errada.** `fitContent()` e
`setVisibleLogicalRange()` **não mudam nada de forma síncrona** — enfileiram uma invalidação
aplicada dentro do ciclo de desenho movido a `requestAnimationFrame`. Lendo as coordenadas sem
deixar o ciclo rodar, o gráfico responde de um modelo ainda com os **defaults**: `barSpacing = 6
px`, faixa alinhada à direita, **1.440 barras espalhadas por ~8.634 px numa pane de 1.200 px**.
Nessa geometria toda barra fica a um número **inteiro** de pixels da vizinha, o erro sai
`0,000 px`, e o DoD parece satisfeito — **avaliando um gráfico 7× mais largo que a viewport,
num espaçamento onde arredondamento sub-pixel não pode ocorrer.** Ou seja: mediu-se exatamente
o que `D8.19` existe para não deixar passar.

A correção tem duas partes, e a segunda é a que dura: (a) `flushFrames()` deixa o ciclo rodar;
(b) `assertViewportFitted()` **RECUSA a corrida** se o fit não aterrissou.

```bash
# mutação: remover as chamadas de flushFrames() de headless-chart.ts
npm --prefix frontend run spike:axis   # rc=3
# RECUSA: nao consegui medir — o eixo NAO coube na viewport: barSpacing = 6 px para 1440
# slots em 1200 px de pane (esperado ~0.8333 px). O ciclo de desenho nao aplicou o fit, e
# medir aqui devolveria o numero de um grafico com ~8640 px de largura, nao de 1200 px.
```

`[MEDIDO 2026-08-29: rc=3 com a mutação, rc=0 sem ela]`. `rc=3` é *"não mediu"* e é **diferente**
de `rc=1` (*"mediu e reprovou"*) — a mesma semântica de `backend/scripts/lint.sh` e de
`scripts/verify.sh`.

### 4.3 Mutação no PORTÃO DE REGRAS — porque `rc=0` vazio é ambíguo

`harness rules --mode sweep --changed-only` devolveu **`rc=0` com saída vazia**, e num branch
sem commit isso é o modo de falha que `ADR-012` nomeia: o universo de `--changed-only` é
`master...HEAD`, que estava **vazio**. Verde de universo vazio. Disambiguado por arquivo:

```bash
for f in frontend/src/charts/*.ts; do harness rules --mode file --path "$f"; done   # 5 arquivos, rc=0, 0 achados
# mutação: `import { thing } from "backend/src/whatever";` em synthetic-series.ts
harness rules --mode file --path frontend/src/charts/synthetic-series.ts            # rc=2
# {"decision": "block", "reason": "[BLOQUEIO] [web-fullstack.browser-imports-server] ...:126"}
```

`[MEDIDO 2026-08-29, n=5 arquivos]` — o motor **lê** estes arquivos e **bloqueia** neles; o
`rc=0` da árvore limpa é verde de verdade, não universo vazio.

---

## 5. Portões — `make verify`, uma chamada

```bash
export HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/.harness/mechanism
bash scripts/verify.sh
```

```
[OK       ] lint-backend    rc=0  52 source files
[OK       ] lint-frontend   rc=0  ESLint do projeto sobre frontend/src
[OK       ] test            rc=0  325 passed · Total coverage: 99.16%
[OK       ] boundaries      rc=0  3 kept, 0 broken
```

```bash
npm --prefix frontend run test:charts     # tests 9 · pass 9 · fail 0
```

**Duas RECUSAS de ambiente tiveram de ser resolvidas para os portões MEDIREM, e elas são
propriedade do worktree, não do trabalho** `[MEDIDO 2026-08-29]`:

1. `backend/.venv` não existe num worktree novo (é gitignored) ⇒ `lint-backend`, `test` e
   `boundaries` devolviam `rc=3`. Resolvido por symlink para o `.venv` do checkout principal,
   **não versionado** (entrou em `$(git rev-parse --git-common-dir)/info/exclude`).
2. `harness` não resolve o mecanismo a partir do caminho do worktree ⇒ `regras` e `política`
   devolviam `rc=3` (e `rc=126` com a variável apontando para o diretório em vez do executável).
   Resolvido com `HARNESS_MECHANISM` apontando para o **arquivo** `.harness/mechanism`.

**Sem esses dois passos, `verify.sh` num worktree devolve `INDETERMINADO` — que é honesto, e
NÃO é passar.** Fica registrado porque toda task futura em worktree vai bater nisto.

---

## 6. O que ficou BLOQUEADO — nomeado, não silenciado

**B-1 · Não há portão declarado para o componente `charts`, e portanto os 9 testes de unidade
desta task NÃO são cobrados por nada.**

```bash
harness policy --key test_cmd
# {"sentimento": {"lint": "bash backend/scripts/lint.sh", "test": "bash backend/scripts/test.sh"}}
```

`[MEDIDO 2026-08-29, n=1 componente declarado de 6]`. `make verify` roda `eslint src` (que
**vê** os arquivos novos e passa) mas **não** roda `npm run test:charts`. Os testes existem, são
determinísticos e passam — e **nenhum portão reprova se alguém os quebrar**. Declarar
`test_cmd.charts` é **ato do owner** sobre política, não do builder.

**B-2 · Cobertura do `charts` não é medida — não existe instrumento.** O `99,16%` acima é do
**backend**, universo que esta task não tocou. Não há `c8`/`nyc` no `frontend`, e introduzir um
sem `test_cmd.charts` declarado seria instrumento sem portão. **Alvo de cobertura para `charts`:
`[NÃO SEI]` — não está declarado em lugar nenhum que eu tenha lido.**

**B-3 · Não há `tsconfig.json` nem script de typecheck** — o próprio `frontend/README.md` §5-bis
(iv) já registra que `any` IMPLÍCITO não é pego por nada. Vale para o código desta task: ele é
**lintado**, não **type-checkado**. Herdado, não criado aqui.

---

## 7. Escopo — o que esta task NÃO fez

- **Não** acrescentou a aresta `T-08.2 → T-05.2` em `depends_on` (`D-2` segue `NÃO APLICADA`; a
  decisão é do owner porque migraria a task de Epic `CST-6` → `CST-1`).
- **Não** mexeu em atribuição de Epic, nem no tracker, nem no ledger.
- **Não** criou ADR: o achado da §3 é decisão de arquitetura e o dono é `/architect`.
- **Não** desenhou UI, não fez varredura ao vivo, não tocou em rede nem em `$COINALYZE_API_KEY`.

---

## 8. Arquivos

| arquivo | estado |
|---|---|
| `frontend/src/charts/synthetic-series.ts` | novo — as duas cargas sintéticas, determinísticas |
| `frontend/src/charts/axis-fidelity.ts` | novo — a definição do erro e o veredito |
| `frontend/src/charts/headless-chart.ts` | novo — o gráfico real sem navegador, e a guarda anti-falso-verde |
| `frontend/src/charts/axis-spike.ts` | novo — o runner; um comando, um número |
| `frontend/src/charts/axis-fidelity.test.ts` | novo — 9 testes, incluindo o caso que REJEITA |
| `frontend/package.json` | modificado — `spike:axis`, `test:charts`, 2 devDependencies |
| `frontend/package-lock.json` | modificado — `lightweight-charts@5.2.1`, `jsdom` |
| `frontend/README.md` | modificado — §7, o diretório `src/charts/` e os dois comandos |
| `docs/INDEX.md` | append de 1 linha |

**Idioma:** todo identificador em inglês (`CLAUDE.md`, linhas 1–4 da tabela de fronteira);
comentário e docstring em inglês (linha 5); saída de terminal para o operador em pt-BR
(linha 8, *"microcopy de operador"*), pelo mesmo precedente de `scripts/verify.sh` e
`backend/scripts/lint.sh`. O segmento de diretório `charts` casa por igualdade com
`harness policy --key components`, então o falsificador de erosão do `CLAUDE.md` continua em
**1 segmento português (`painel`)**, inalterado por esta task.
