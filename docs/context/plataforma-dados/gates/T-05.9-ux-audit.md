# UX Audit — `T-05.9` (esquema de chave de requisição HTTP, não uma tela)

**Escopo:** `frontend/src/app/history-transport.ts`. Mesmo formato de `T-05.8-ux-audit.md`,
10 heurísticos de Nielsen, `ux-ui-mastery:ux-audit`. Sem tela nesta task — não avalia
layout/cor/tipografia.

## Veredito: **APROVADO**, sem condição

## O que foi checado, e por quê — a recorrência que importa

`T-05.8` teve 1 achado (H2/H6): o param `t` não era reconhecível. O `ui-designer` recomendou,
em `T-05.9-design.md`, reusar vocabulário já fixado em vez de abreviação nova. **Checado: a
recomendação foi seguida.** `PARAM_ORDER` desta task é `seriesKeyId, symbol, interval, from,
to, knowledgeTime, barPolicy` — sete nomes completos, zero abreviação de 1 caractere. H2/H6
não reincidem.

| # | heurística | verificação | resultado |
|---|---|---|---|
| H2/H6 | correspondência sistema↔mundo real, reconhecimento > memorização | todo nome de param é a palavra completa do domínio (`knowledgeTime`, não `kt`; `barPolicy`, não `bp`) | ✅ passa |
| H4 | consistência | `barPolicy` usa os mesmos dois valores verbatim de `BarPolicy` em `as_of_accessor.py:54-70` (`final_only`/`intrabar`) — zero segundo vocabulário para a mesma coisa | ✅ passa |
| H5 | prevenção de erro | `assertValidHistoryRequestKey` recusa ANTES de virar URL/chave de cache — mesmo padrão de `assertValidBundle` | ✅ passa |
| H9 | diagnóstico de erro | toda mensagem nomeia o campo, a ADR/DoD violado, e por que (ex.: `barPolicy` ausente cita `ADR-005/D4` explicitamente) — útil para quem debuga, mesmo sendo interno (não há UI ainda que exponha isto a um operador) | ✅ passa |
| H1 | visibilidade de status | `mode`/`barPolicy` sempre explícitos na chave — nenhum estado "assumido" existe para confundir | ✅ passa (mesmo argumento de `T-05.8`) |

## O que este audit NÃO julga

Fidelidade do dado (fora de `web`) e qualquer decisão de UI/UX nova — não houve nenhuma
(`ui-designer` já confirmou em `T-05.9-design.md`); este audit confirma que a ausência de
decisão nova também não escondia regressão de usabilidade no contrato, e que o achado de
`T-05.8` não se repetiu por acidente — foi seguido deliberadamente.

**Merge:** sem impedimento.
