# T-05.6 — builder report (self-attested, NOT final QA/review)

[QA GATE — Fase 05: S2-mínima — a primeira fatia de valor visível]
Feature: plataforma-dados     Componente: charts
Arquivos alterados:
- frontend/src/charts/s2-pointer-mode.ts (new)
- frontend/src/charts/s2-pointer-mode.test.ts (new)
- frontend/README.md (modified — §13 nova + correção de números)
Spec: docs/specs/SPEC-001-plataforma-dados.md (§3.6, linha 284)
Plan: docs/plans/SPEC-001-plataforma-dados/05_fatia_visivel.md (item 5.8)
DoD:
- [x] `pointer_mode ∈ {read, annotate}` declarado como tipo fechado + enumeração em runtime (`assertPointerMode` recusa valor fora do conjunto, inclusive variação de maiúscula) — `node --test 'src/charts/s2-pointer-mode.test.ts'`
- [x] overlay reservado acima do plot e abaixo do crosshair, invariante checado por POSIÇÃO (não por nome) via `assertOverlayIsSandwiched` — mesmo comando
- [x] clique/Espaço só travam o crosshair em `read`; em `annotate` nunca devolvem `lock_crosshair`, só o placeholder `annotate_reserved` — mesmo comando
- [x] zero I/O, zero DOM (`ADR-003` FR-1) — módulo é geometria/estado puro, sem `fetch`/listener/`node:fs`
- [x] compõe sobre `T-05.2` sem reimplementar (`s2-annotation-identity.ts` já citava esta task pelo número como dona de `pointer_mode`)
Comandos rodados (literais) e resultado:
- `npm --prefix frontend install` → `up to date, audited 129 packages` (node_modules gitignored, worktree novo)
- `npm --prefix frontend run lint` → `eslint src`, 0 erro/0 aviso
- `node --test 'src/charts/s2-pointer-mode.test.ts'` → `tests 14 / pass 14 / fail 0` (universo: 14 testes no arquivo, todos com um caso positivo e ao menos um MORDE por proteção — 3 recusas distintas em `assertOverlayIsSandwiched`, `notDeepEqual` contra `lock_crosshair` em `annotate`, 2 valores contornando o tipo estático via `as unknown as`)
- `npm --prefix frontend run test:charts` (glob `src/charts/*.test.ts`) → `tests 75 / pass 64 / fail 11` — as 11 falhas são PRÉ-EXISTENTES de `T-05.2` (já em master antes desta task: `canonical-grid-sha256-proof.test.ts`, `s2-axis-integration.test.ts`, `s2-cvd.test.ts`, `s2-klines-loader.test.ts`, `s2-oi-loader.test.ts`, `s2-panels.test.ts`), todas ENOENT/asserção sobre `data/binance/**`, ausente neste worktree (dado bruto não é versionado, `.gitignore:51`; confirmado `ls data` → "Arquivo ou diretório inexistente"). Nenhuma delas toca `s2-pointer-mode.ts`.
- `harness rules --mode sweep --changed-only` → rc=0, saída vazia (0 achado bloqueante sobre os arquivos alterados)
- `git push -u origin tasks/T-05.6-pointer-mode` → `e36d688..e743cd6 tasks/T-05.6-pointer-mode -> tasks/T-05.6-pointer-mode` (pre-push: `make boundaries` 3 kept/0 broken, `make natureza` 0 leituras de relógio, `make lint` backend+frontend limpos, `harness rules --mode sweep --surface git-hook` só AVISO pré-existente em arquivos não tocados por esta task)
Cobertura: sem piso declarado para `charts` (`harness policy --key test_cmd` só cobre `sentimento`, componente `charts` sem `[test_cmd.charts]` — silêncio deliberado, `harness.toml:368-371`). Medição qualitativa: 14 testes cobrem 100% das 7 funções/constantes exportadas do módulo.
Doc delta:
- frontend/README.md: atualizado — §13 nova (append-only) documentando o módulo, os falsificadores e os comandos rodados. Correção adicional nesta mesma task: o §13 original (do commit e36d688) tinha dois números errados sem o comando que os produziu — "15/15 nos 15 testes" (real: 14/14) e "23 falhas PRE-EXISTENTES" (real: 11) — corrigidos no commit e743cd6 após remedição.
- docs/specs/SPEC-001-plataforma-dados.md: sem mudança — §3.6 já fixa a frase que este módulo torna executável; a task consome, não emenda a SPEC.
- Gate de design: não aplicável — zero elemento visual novo (zero DOM, zero componente React); mesma classe de `s2-panels.ts`, que também não passou por gate de design.
- ADR: não necessário — nenhuma decisão de arquitetura nova, só torna executável uma frase já fixada em SPEC-001 §3.6.
Bloqueado (se houver): nenhum. Nota de ambiente (não bloqueio desta task): o resolvedor `.harness/mechanism` não reivindica o path do worktree isolado (registro do Claude Code aponta para o checkout principal) — push só resolveu com `$HARNESS_MECHANISM` apontado explicitamente para o binário instalado (via 1, precedência máxima, sem tocar em nada versionado). É gap de infra do ambiente de worktree, não desta task.
