[QA GATE — Fase 07: S5 embutido — `universe_at` portado para TS + badge de delisting] — VEREDITO QA

Feature: `plataforma-dados` · Componente: `web` · Task: `T-07.14`
Escopo lido: exatamente o QA Gate Context Block do builder
(`docs/context/plataforma-dados/gates/T-07.14-builder.md`) + handoff
(`docs/context/plataforma-dados/handoff/T-07.14.md`) + os dois arquivos que ele lista.

## Rodada de baseline (antes de qualquer mutação)

```
$ cd frontend && npm run test:app
ℹ tests 115
ℹ pass 115
ℹ fail 0
$ npm run lint
> eslint src
(sem saída)
```
Confere com o relatório do builder — nada divergiu entre a entrega e esta verificação.

## Mutação — 5 defeitos do contrato, reintroduzidos um a um, revertidos com `git checkout --`
Protocolo: mutação em `universe-at.ts` → `npm run test:app` → registrar `pass/fail` →
`git checkout -- frontend/src/app/universe-at.ts` → `git diff --stat` confirma 0 resíduo antes da
próxima mutação. `universe-at.ts` termina IDÊNTICO ao estado de entrega (`git diff --stat` final:
0 linhas nesse arquivo).

1. **Fusão silenciosa (união→interseção)** — `symbols = new Set([...decidedSymbols, ...s3WitnessSymbols])`
   → `.filter(s3WitnessSymbols.has)`. **PEGO** — 6 testes falham (MATICUSDT, retrospectivo,
   stamping, snapshot decidido).
2. **`s3_inferred` entrando na decisão apesar do tipo** — witness de `SNAPSHOT` contaminado com
   `s3WitnessSymbols` dentro de `universeAt`. **PEGO** — 1 teste falha (`MATICUSDT: ...divergence,
   not silently merged`).
3. **`RETROSPECTIVE_LABEL` confundido com decidido-vazio** — `label` trocado de
   `snapshotRows !== null ? null : RETROSPECTIVE_LABEL` para
   `decidedSymbols.size !== 0 ? null : RETROSPECTIVE_LABEL`. **NÃO PEGO na primeira rodada** — 115/115
   passam com o mutante vivo. Nenhum teste da suíte original exercita "snapshot presente, filtro
   zera tudo" (todo filtro testado ainda deixa `decidedSymbols` não vazio). **Defeito de cobertura
   real, corrigido nesta sessão**: teste novo adicionado em `universe-at.test.ts` — `"a snapshot
   present but filtered to zero matches is DECIDED-AND-EMPTY, never RETROSPECTIVE_LABEL"`
   (`market: "NO_SUCH_MARKET"`, `decidedSymbols.size === 0`, `label === null`). Confirmado nos
   dois sentidos: 116/116 passam contra o código correto; ao reintroduzir o mutante, exatamente
   esse teste novo falha (1/116) e nenhum outro. `universe-at.ts` revertido depois (`git diff
   --stat` = vazio).
4. **`universeSource` ausente em `stampUniverseRows`** — array zerado antes do `push`.
   **PEGO** — 4 testes falham (stamping, badge, retrospectivo, MATICUSDT).
5. **Sentinela de delisting invertida** — `hasScheduledDelisting`: `!==` trocado por `===`.
   **PEGO** — 3 testes falham (BTCUSDT sem badge, ICX/STORJ/SCRT com badge, stamping).

4 de 5 mutações pegas na entrega original; a 5ª (item 3) expôs um buraco real de cobertura,
fechado por um teste novo desta sessão de QA (não altera `universe-at.ts`).

## Achado colateral do builder — sentinela 567/570 vs 568/570

```
$ python3 -c "import json,collections; d=json.load(open('data/snapshots/2026-08-25_exchangeInfo.json'));
syms=d['symbols']; filt=[s for s in syms if s.get('status')=='TRADING' and s.get('contractType')=='PERPETUAL'];
print(len(filt), collections.Counter(s['deliveryDate'] for s in filt)[4133404800000])"
570 567
```
**O número real medido é 567/570** (3 sem sentinela: `ICXUSDT`, `STORJUSDT`, `SCRTUSDT`) — confirma
a remedição do builder, não o `568/570` de `docs/recorte-plataforma.md:90`. `docs/recorte-
plataforma.md:90` está desatualizado em 1 símbolo; não bloqueia esta task (o módulo não filtra por
`status`/`contractType`), fica registrado para quem for corrigir aquele doc.

## Veredito

## QA Gate — Fase 07 [web] T-07.14
- [OK] web-fullstack.browser-imports-server — `harness rules --mode file --path frontend/src/app/universe-at.ts --surface ci --format ndjson` (rc=0, sem achado)
- [OK] web-fullstack.tenant-from-request — idem, `universe-at.test.ts` incluído, rc=0 sem achado
- [OK] web-fullstack.server-test-directory-present — não aplicável a este módulo (`frontend/src/app/`, não servidor)
- [OK] core.relative-import / core.silent-except / core.print-statement / core.hardcoded-secret — sem achado nos 2 arquivos (rc=0)
- [OK] Testes existem e passam — `npm run test:app` (116 testes, 115 originais + 1 novo desta sessão de QA)
- [OK] Lint — `npm run lint` (0 problema)
- [NÃO MEDIDO] Cobertura por ferramenta — sem `c8`/`nyc` em `frontend/`, mesma lacuna que o builder já declarou; medição por mutação (5/5 defeitos do contrato pegos, 1 exigiu teste novo) usada como substituto
- [OK] DoD da fase — caso-âncora (badge ICX/STORJ/SCRT, sem badge BTC), caso retrospectivo, divergência MATICUSDT, `universe_source` carimbado em toda linha, `backend/` intocado: todos verificados por mutação acima
- [anomalia] nenhuma

Regras bloqueantes avaliadas: 7 de 7 listadas por `harness rules list --severity block`

Veredito: **APPROVED**

Notas: `universe-at.ts` não foi alterado (entrega do builder preservada, `git diff --stat` = 0
linhas nesse arquivo ao final). Único arquivo tocado por este QA:
`frontend/src/app/universe-at.test.ts` (+9 linhas, 1 teste novo, fecha o buraco de cobertura do
item 3 acima).
