# Gate — T-03.12 · Portao de natureza por USO (scanner de AST) + estreitamento atomico do contrato 3

**Autor:** `/build` · **Data:** 2026-09-01 · **Feature:** `plataforma-dados` · **Componente:** `sentimento`
**Worktree:** `/tmp/claude-1002/wt/T-03.12` · **Branch:** `tasks/T-03.12-natureza-por-uso`
**Base:** `master@a3c63c5` (fetch via `https://github.com/StharleyMax/cripto-strategy.git`)
**Commit unico:** `c7df9fd` · **PR:** https://github.com/StharleyMax/cripto-strategy/pull/50

Fontes lidas por inteiro antes de escrever qualquer linha: `docs/adr/ADR-016-relogio-e-capacidade-tipo-de-data-e-valor.md`
(incluindo ERRATA 1), `docs/adr/bancadas/ADR-016-natureza.py`, `docs/context/plataforma-dados/tasks_review-T-03.12.md`,
e o bloco `id = "T-03.12"` de `docs/context/plataforma-dados/tasks.toml` (linhas 487-513), que e a fonte normativa do
DoD — a narrativa do dispatch citava "11 itens"; o bloco real numera `DoD-2` a `DoD-11` mais `DoD-1` (atomicidade,
implicito no texto ATOMICO) e `DoD-8` (git push), totalizando 11 criterios distintos.

## Achado relevante antes de medir: a bancada JA vinha com os dois furos fechados

Ao ler `docs/adr/bancadas/ADR-016-natureza.py`, o arquivo ja continha a ERRATA 1 aplicada — SITIO 2
(`ast.Name`) cobrindo `from time import monotonic` nu, e a taxonomia L/E/A/ARIDADE/P com 38 nomes de
`time` particionados e `_confere_particao()`. Os "dois achados nao declarados pela ADR" citados no
despacho sao os da ERRATA 1 (achados do `/tech-lead`, ja registrados na propria ADR). Reproduzi os dois
diretamente (ver DoD-3 e DoD-9 abaixo) em vez de assumir a bancada correta por leitura — comando e
resultado abaixo.

## DoD — 11 itens, comando e resultado de cada

1. **Atomicidade (D5).** `git show --stat c7df9fd` — PASS. Um commit contem juntos: `backend/scripts/natureza.py`
   (novo), `backend/scripts/natureza.sh` (novo), alvo `natureza` no `Makefile`, chamada em
   `scripts/hooks/pre-push.pre-harness`, e a edicao de `backend/pyproject.toml` (contrato 3 estreitado).

2. **Arvore limpa.** `bash backend/scripts/natureza.sh` → PASS. `rc=0`, "universo de 24 arquivo(s)
   (17 domain + 7 use_cases), 0 leitura(s)". `[MEDIDO 2026-09-01, a3c63c5 + este commit]` — diverge do `n=20`
   medido em `6b8f441` porque `domain/` cresceu de 13 para 17 arquivos entre commits (T-04.x mergeados
   depois); o script imprime o universo real a cada chamada, que e o requisito.

3. **Bateria de 6 mutantes, um por vez, arvore reconferida entre cada.** Plantados em copia (`cp -r` +
   mutacao + `natureza.py` direto na copia, original nunca tocado): `datetime.now()`,
   `date.today()` sem import novo, `import time`+`time.monotonic()`, `f(datetime.now)` sem chamar,
   `from time import monotonic` chamado NU, `time.ctime()` — **6/6 rc=1**, cada um nomeando
   `arquivo:linha -> expressao`. PASS.

4. **Zero falso positivo, n=9.** `bash backend/scripts/natureza.sh` sobre a arvore real → `0 leitura(s)`,
   confirmando silencio sobre `dump_window.py:129,171,174,194,196,214,230` e `retention_probe.py:131,132`
   (conferidos linha a linha via `grep -n 'date\|timedelta'` antes de rodar). PASS.

5. **Conjunto de nomes derivado e justificado.** Cabecalho de `natureza.py` documenta a particao
   L(14)/E(3)/A(6)/ARIDADE(5)/P(10) = 38 nomes de `time`, com `_confere_particao()` recusando alto se
   `dir(time)` mudar. PASS.

6. **Contrato estreitado ainda morde.** `bash backend/scripts/boundaries.sh` → `3 kept, 0 broken` (PASS).
   Com `import ssl` plantado em `dump_window.py` (real, revertido em seguida com
   `git restore --source=HEAD`) → `2 kept, 1 broken`, nomeando `dump_window -> ssl (l.252)`. PASS.

7. **`git add` antes do sweep.** `git add -A` seguido de `harness rules --mode sweep` (completo, sem
   `--changed-only`) → `rc=0`, nenhuma linha de bloqueio. Sanidade do instrumento confirmada plantando
   `print("sonda-sweep")` em arquivo staged → `[BLOQUEIO] core.print-statement`, `rc=1`; revertido com
   `git restore --source=HEAD --staged --worktree`. PASS.

8. **O bloqueio some.** `git push --dry-run` (via `$HARNESS_MECHANISM`, URL HTTPS explicita) → `rc=0`,
   `[new branch] tasks/T-03.12-natureza-por-uso`. Antes do fix, o mesmo comando reprovava em
   `make boundaries` (`2 kept, 1 broken`, medido nesta sessao antes de qualquer edicao). Push real feito
   e PR #50 aberta. PASS.

9. **Ponto cego documentado no script.** Cabecalho de `natureza.py`, item (3), declara
   `getattr(date, "today")()` → `0`. Reproduzido nesta sessao em copia isolada: `rc=0`,
   "0 leitura(s)". PASS.

10. **Falsificador contavel.** Cada acusacao imprime `RELOGIO EM CAMADA PURA: <arquivo>:<linha> ->
    <expressao>`, linha estavel e grepavel — o mecanismo que o DoD pede para contar FP x VP ao fim da
    fase 03. Nao ha `rc=1` nesta task sobre arvore real (so em copias mutadas), entao o placar desta
    task e 0 x 0; o placar de referencia da ADR (E3) continua 0 x 3. PASS.

11. **Nao reabre o que nao e seu.** `docs/adr/bancadas/ADR-016-natureza.py` nao foi tocado
    (`git diff --stat -- docs/adr` → vazio). `ADR-016` status `proposto` nao tocado. PASS.

## Comandos de gate do componente

- `bash backend/scripts/lint.sh` → `rc=0`, "83 files already formatted", "no issues found in 83 source files".
- `bash backend/scripts/test.sh` → `rc=0`, `560 passed`, cobertura domain 100%/use_cases 100%/infra 98.3%
  (universo 3/3 camadas medidas) — suite nao tocada por esta task, verde herdado.

## Doc delta

- Sem mudanca em `docs/`. `docs/adr/bancadas/ADR-016-natureza.py` permanece intocado (DoD-11).
  `docs/INDEX.md` nao recebeu linha nova por esta task — nenhuma decisao arquitetural nova foi tomada,
  apenas a implementacao da que ja estava aprovada em `ADR-016/D5`; o proprio `INDEX.md` e append-only e
  este relatorio de gate cumpre o papel de registro tecnico da fase.
- ADR: nao necessario — `ADR-016` ja existe e cobre a decisao; nenhuma decisao nova foi tomada aqui.

## Bloqueado

Nenhum item ficou bloqueado. `frontend/package-lock.json` foi tocado transitoriamente por
`npm --prefix frontend install` (necessario para o `lint-frontend` do `pre-push` rodar) e revertido
antes do commit — nao faz parte do escopo desta task.
