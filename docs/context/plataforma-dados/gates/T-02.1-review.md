# Review — T-02.1 (CST-12) · fase 02 · componente sentimento · PR #60

**Veredito: COMPLIANT**

## Denominador
- Regras bloqueantes em vigor: **7** (`harness rules list --severity block`), todas avaliadas.
- Arquivos do PR: **12** (`gh pr diff 60 --name-only`) — 6 produção + 6 teste, todos sob
  `backend/src/modules/sentimento/` e `backend/tests/sentimento/`.
- `harness rules --mode sweep --changed-only` → rc=0, nenhum achado.
- `harness rules --mode file --path <arquivo>` rodado individualmente nos 6 arquivos de
  produção novos → rc=0 em todos, nenhum achado.
- Worktree `/tmp/claude-1002/wt/T-02.1` confirmado no commit do PR (`9f552ab`).

## Contrato import-linter "Natureza"
`bash backend/scripts/boundaries.sh` → **3 contracts kept, 0 broken** (79 files, 288
dependencies), incluindo `Natureza: domain e use_cases nao falam com socket nem ssl`.
Confirmado por leitura: `domain/instrument_universe_snapshot.py` e
`use_cases/capture_instrument_universe_snapshot.py` não importam `socket`/`ssl`/`http.client`
— só `infra/binance_futures_snapshot_client.py` (o `http.client.HTTPSConnection`) e
`infra/daily_instrument_universe_snapshot_cli.py` (`datetime.now(UTC)`) tocam rede/relógio,
e ambos vivem em `infra`. COMPLIANT.

## Idioma de identificador
Varredura AST dos 6 módulos de produção + 6 de teste novos: nenhum identificador
(função/classe/variável/parâmetro) em português. Convenção (não portão, `CLAUDE.md`
"Idioma de identificador é convenção, não portão"). Sem violação.

## Cobertura por camada
Reexecutei a suíte inteira (`bash backend/scripts/test.sh`, 658 passed in 713.33s) — **igual**
ao relatório do builder: domain **100.0%** (1186/1186), use_cases **100.0%** (234/234), infra
**95.2%** (991/1041). Os 3 pisos (90/80/70) batidos. `[MEDIDO 2026-09-01]` — confirma o número
do builder, não apenas o cita.

## Achado não-bloqueante (WARNING, sem id de regra — cita documento)
O PR introduz **2 novas mensagens de exceção em português**:
`binance_futures_snapshot_client.py:116` (`UnexpectedStatusError`) e
`capture_instrument_universe_snapshot.py:55-57` (`UnstableExchangeInfoReadError`). O universo
que `CLAUDE.md` § "lacuna conhecida … mensagem de exceção" declarou em 2026-08-29 era **3 PT /
2 EN** (n=5) com gatilho explícito: *"se o número de mensagens em português subir, a lacuna
deixou de ser inércia e virou rampa — e volta à mesa"*. Com este PR o número sobe para **5 PT /
2 EN** (n=7) `[MEDIDO 2026-09-01, mesmo grep de CLAUDE.md aplicado a backend/src]`. Não é
regra bloqueante (nenhum `[[rules.own]]` cobre idioma de mensagem) — é o gatilho do próprio
`CLAUDE.md` disparando. Correção: não é deste PR corrigir; é `/architect` decidir se a 13ª
superfície entra na tabela, como o próprio documento já prevê.

## Comandos usados
`harness rules list --severity block`; `harness rules --mode sweep --changed-only`;
`harness rules --mode file --path <arquivo>` × 6; `bash backend/scripts/boundaries.sh`;
`bash backend/scripts/test.sh`; leitura direta dos 6 módulos de produção via `gh pr diff 60`
e worktree.
