[BUILDER GATE — Fase 08: Primitivo `swing_point` em `<Anotacao>` — zero algoritmo, zero limiar, zero "nível"]
Feature: `plataforma-dados`     Componente: `charts`     Task: `T-08.10` (`CST-78`)
Worktree: `/tmp/claude-1002/wt/T-08.10`, branch `tasks/T-08.10-swing-point-anotacao`, base `fa4db73`

⚠️ **RECONSTRUÍDO PELO `/qa`, não escrito pelo builder.** O builder deixou o worktree com as
mudanças **staged** (`git status` → `Changes to be committed`, zero commit na branch além do
merge-base) e nunca gravou `gates/T-08.10-builder.md`. Este arquivo existe porque o `/qa` o
recriou a partir da evidência disponível no worktree — `frontend/README.md` §17 (a narrativa que
o builder de fato escreveu, íntegra, incluindo a seção "Conflito registrado com `ADR-017`") e do
próprio diff. **Não é o relato original do builder; é a reconstrução do que ele entregou,** citada
com a evidência que a sustenta. A ausência do artefato em si é uma anomalia de processo, registrada
no veredito de QA (`T-08.10-qa.md`), não escondida aqui.

## Arquivos alterados

```
git diff --cached --stat
 frontend/README.md                         |  99 ++++++++++++++++++++
 frontend/src/charts/s2-swing-point.test.ts | 122 ++++++++++++++++++++++++
 frontend/src/charts/s2-swing-point.ts      | 145 +++++++++++++++++++++++++++++
```

Zero arquivo fora de `frontend/`; `backend/` intocado (confirmado por `git status --short`, sem
entrada fora de `frontend/`).

## Refs e escopo (`tasks.toml:1090-1097`, plano `08` item 8.7)

`refs = ["SPEC-001 §3.6", "plano 08 item 8.7", "Q20 NAO bloqueia: os dois vocabularios se apoiam
no mesmo primitivo", "zone (OB/FVG/Fib) depende de Q20 e NAO entra aqui"]`. `depends_on =
["T-05.6"]` — mergeado (`fa4db73`'s ancestry inclui `#111 tasks/T-05.6-pointer-mode`).

`SPEC-001:282`, literal: **"O primeiro primitivo de `<Anotacao>` é `swing_point`, não `zone`...
Esta SPEC entrega o primitivo e a chave, e ZERO ALGORITMO, ZERO LIMIAR, ZERO 'NÍVEL'."**

## O que foi entregue (transcrito de `frontend/README.md` §17)

- `SwingKind = "high" | "low"` + `SWING_KINDS` (runtime) + `assertSwingKind` — recusa qualquer
  valor fora do par (`InvalidSwingKindError`), mesmo padrão de `assertPointerMode` (`s2-pointer-mode.ts`).
- `SwingPoint extends PriceBoundAnnotation` (composição, não reimplementação, de
  `s2-annotation-price-binding.ts`/`T-05.5` sobre `s2-annotation-identity.ts`/`T-05.2`/`D5.10`) —
  acrescenta exatamente `kind`, `eventTimeMs`, `price`, `primitive: "swing_point"` (discriminante
  literal para uma futura união `SwingPoint | Zone`).
- `createSwingPoint` recusa independentemente: `kind` inválido, `eventTimeMs` não finito/negativo
  (`NonFiniteSwingEventTimeError`), `price` não finito ou `<= 0` (`NonFiniteSwingPriceError`).
- Falsificador estrutural (`s2-swing-point.test.ts`, último teste): lista os 8 campos esperados e
  falha se um campo fora dessa lista aparecer no objeto devolvido.

## Conflito nomeado com `ADR-017` (RASCUNHO) — decisão do builder, transcrita

`docs/adr/ADR-017-deteccao-autonoma-com-auditoria-por-excecao.md` (`Status: RASCUNHO`, sem evento
`approve` no ledger) nomeia `T-08.10` por número em sua seção de consequências, pedindo
`provenance ∈ {HUMANO, DETECTOR}`, `detector_key`, `review_verdict`, `structure_definition`. O
builder **não construiu** esses campos, com o argumento (README §17): os `refs` desta task citam
só `SPEC-001` §3.6 + plano `8.7`, nenhum cita `ADR-017`; construir sobre um rascunho não ratificado
seria "amplie escopo"; `provenance = HUMANO` (a única linha que `SPEC-001` §3.6 já fixa,
incondicional) já é coberta pela `AnnotationIdentity` composta. O próprio módulo (`s2-swing-point.ts:38-50`)
registra o mesmo conflito no código.

## Comandos citados pelo builder (README §17, "Comandos rodados, literais")

```bash
npm --prefix frontend run lint   # eslint src -> 0 erro, 0 aviso
npm --prefix frontend run test:charts
  # node --test 'src/charts/*.test.ts' -> universo total 147 testes, 114 pass / 33 fail
  # (re-executado isolado: node --test src/charts/s2-swing-point.test.ts -> 12/12 pass, 0 fail —
  # as 33 falhas do universo total são PRE-EXISTENTES, ENOENT sobre data/binance/**, ausente
  # neste worktree)
harness rules --mode sweep --changed-only   # rc=0, nenhuma saída — 0 achado
```

⚠️ **O `/qa` NÃO reproduziu "147 testes / 33 fail" nesta mesma árvore** — ver `T-08.10-qa.md` para
a contagem medida de novo, com o comando, e a explicação da divergência (não afeta o veredito: a
causa — `data/` ausente, ENOENT — é a mesma em ambas as contagens).

## Cobertura (citada do README §17)

Qualitativa (sem piso declarado para `charts`): 12 testes cobrem 100% das funções/constantes
exportadas, um caso positivo por `kind`, um `MORDE` por recusa (6), a propagação das duas recusas
de `s2-annotation-price-binding.ts`/`s2-annotation-identity.ts`, e o falsificador estrutural.

## Doc delta (citado do README §17)

`frontend/README.md` §17 nova (append-only). `SPEC-001` — sem mudança (§3.6 já fixa o texto que
este módulo executa). `ADR-017` — sem mudança (RASCUNHO, gate do owner). `docs/INDEX.md` — sem
mudança, motivo explícito no README (linha só é escrita depois do ciclo build+QA+review completo).

## Estado do ledger

`tasks.toml:1093` — `status = "todo"` (não alterado pelo builder). Nenhum `gate-record`,
`approve` ou `advance` no worktree.
