# UX Audit — `T-07.14` (porta de `universe_at` + badge de delisting, não uma tela)

**Gate independente `ux-ui-mastery`, rodado pelo `/workflow` (não pelo builder) — R9.**
**Escopo:** `frontend/src/app/universe-at.ts`. Mesmo formato de `T-08.5-ux-audit.md`/
`T-08.11-ux-audit.md`. Sem tela nesta task — `S5` não é tela (confirmado em
`T-07.14-design.md`), não avalia layout/cor/tipografia.

## Veredito: **APROVADO, sem condição**

## O que foi checado

| # | heurística | verificação | resultado |
|---|---|---|---|
| H2/H6 | correspondência sistema↔mundo real | `symbol`, `universeSource`, `deliveryDate`, `delistingBadge`, `decidedSymbols`, `s3WitnessSymbols` — todo nome é a palavra completa do domínio, mesma disciplina de `HistoryRequestKey`/`ThresholdSpecBundle`; nenhuma abreviação nova | ✅ passa |
| H4 | consistência de vocabulário | `UniverseSource`/`DecisiveUniverseSource` traduzem 1:1 os literais de `universe_at.py` (`"snapshot"`, `"s3_inferred"`, `"premium_index_witness"`) — zero segunda grafia para o mesmo conceito; `RETROSPECTIVE_LABEL` é a MESMA string do Python (`"retrospective_before_first_snapshot"`), não uma tradução livre | ✅ passa |
| H1 | visibilidade de status | `universeSource` é campo **obrigatório** (não opcional) em `UniverseAtRow` — nenhuma saída de `stampUniverseRows` pode omiti-lo, que é literalmente o requisito do título da task ("carimbado em TODA saída") | ✅ passa |
| H5 | prevenção de erro | não se aplica da mesma forma que em `T-08.5`/`T-08.11`: este módulo não faz parsing de string externa (URL, payload de rede) — os inputs já chegam tipados. A "prevenção" aqui é por TIPO: `DecisiveUniverseSource` não aceita `"s3_inferred"` estruturalmente, verificado com `tsc --noEmit --strict` (documentado no cabeçalho do arquivo) | ✅ passa, por via diferente |
| H9 | diagnóstico de erro | **não se aplica** — o módulo não lança nenhum erro (não há estado de entrada inválida a rejeitar; é derivação pura sobre dados já tipados). Diferente de `T-08.5`/`T-08.11`, que tinham um `assertValid*` porque decodificavam string de URL | ⚪ N/A, registrado por que não |

## Achado observado, não é Should-Fix — precisão da sentinela

O comentário de `DELISTING_SENTINEL_DELIVERY_DATE` documenta **dois** números medidos (877 símbolos
totais / 743 com sentinela SEM filtro; 570/567 COM o filtro `status=TRADING`+`contractType=PERPETUAL`
que é o recorte da task) — e é honesto sobre qual filtro cada número usa. Isso é exatamente a
disciplina de "nenhum número sem o comando que o produziu" do `CLAUDE.md`, não uma falha de UX; menciono
porque é o tipo de comentário que outros módulos desta sessão (`threshold-spec-bundle.ts`) fariam bem em
copiar.

## O que este audit NÃO julga

Fidelidade do dado (fora de `web`) e qualquer decisão de UI/UX nova — não há nenhuma possível ainda
(`S5` não é tela; nenhuma tela tem seletor de símbolo construído, `T-07.14-design.md` já confirmou).

**Merge:** sem impedimento.
