# T-03.1 · `/build` — QA Gate Context Block

**Feature:** `plataforma-dados` · **Componente:** `sentimento` · **Fase:** 03 · **Tracker:** `CST-17`
**Branch:** `task/T-03.1-ws-aggtrade-nq` · **Base:** `master@7c3599b` · **Commit:** `adffcf5`
**Data:** 2026-08-29T16:18Z

---

## Veredito

**A pergunta de `D3.9` continua `[NÃO MEDIDO]` — com motivo, universo e comando.**

O WS de **futuros** `<symbol>@aggTrade`, deste ponto de observação, **aceita a assinatura,
responde `PING` e não entrega evento nenhum**. Sem payload não há campo a inspecionar.
`D3.9` pede "1 símbolo, 1 mensagem"; **o universo obtido foi 0 mensagens.**

**Isto não é "o WS não tem `nq`".** Os controles abaixo mostram que o instrumento distingue
ausência de campo de ausência de mensagem.

### As três camadas que a task mandou separar

| camada | estado |
|---|---|
| **presença do campo** no WS de futuros | **`[NÃO MEDIDO]`** — 0 mensagens |
| **presença de valor** | **`[NÃO MEDIDO]`** — depende da anterior |
| **concordância de semântica com o dump** | **`[NÃO MEDIDO]`** — depende das duas anteriores |

**Só a primeira era alcançável nesta rodada, e nem ela fechou.** As outras duas não foram
tentadas por dependência, não por esquecimento.

---

## Arquivos alterados

- `backend/src/modules/sentimento/domain/binance_aggtrade_payload.py` (new)
- `backend/src/modules/sentimento/domain/stream_probe_outcome.py` (new)
- `backend/src/modules/sentimento/use_cases/probe_stream_quantity_fields.py` (new)
- `backend/src/modules/sentimento/infra/rfc6455_client.py` (new)
- `backend/src/modules/sentimento/infra/binance_stream_probe.py` (new)
- `backend/src/modules/sentimento/infra/aggtrade_nq_probe_cli.py` (new)
- `backend/tests/sentimento/test_aggtrade_nq_probe.py` (new)
- `docs/medicao-ws-aggtrade-nq.md` (new)
- `docs/INDEX.md` (modified — linha acrescentada, nenhuma reescrita)

**Spec:** `docs/specs/SPEC-001-plataforma-dados.md` · **Plano:** `docs/plans/SPEC-001-plataforma-dados/03_captura_continua.md` (item 3.6)

---

## DoD

- [x] **`D3.9` — o método existe, roda e discrimina.** `python -m src.modules.sentimento.infra.aggtrade_nq_probe_cli --symbols … --evidence …`
- [ ] **`D3.9` — "1 símbolo, 1 mensagem".** **NÃO FECHADO:** 0 mensagens. Nomeado como bloqueio.
- [x] **Ausência de mensagem ≠ ausência de campo.** 4 vereditos distintos, separação estrutural.
- [x] **Controle negativo antes do positivo.** 4 controles, 2 vereditos opostos.
- [x] **Evidência crua re-lida, com carimbo.** `data/binance/ws/*.jsonl` (JSONL, UTC por linha).

---

## Comandos rodados (literais) e resultado

| comando | resultado | universo |
|---|---|---|
| `bash backend/scripts/lint.sh` | `rc=0` — ruff + format + mypy strict | 36 arquivos |
| `bash backend/scripts/boundaries.sh` | `rc=0` — `2 kept, 0 broken` | 23 arquivos, 19 deps |
| `bash backend/scripts/test.sh` | `rc=0` — **166 passed** | 166 testes |
| `harness rules --mode sweep --changed-only` | `rc=0` — **0 bloqueantes** | 1 aviso, em `frontend/` (não tocado) |

### A medição

| # | comando | universo | resultado |
|---|---|---|---|
| 1 | `curl -s '…/fapi/v1/aggTrades?symbol=BTCUSDT&limit=1'` | 1 símbolo, 1 trade | `['T','a','f','l','m','nq','p','q']` — **`nq` presente no REST**, bate com `ADR-001` `[MEDIDO]` |
| 2 | `… aggtrade_nq_probe_cli --symbols BTCUSDT,ETHUSDT,SOLUSDT,XRPUSDT,DOGEUSDT --seconds 120 --max-messages 300` | **5 símbolos, 120 s, teto 300, 1 conexão** | `NOT_MEASURED` / `FRAME` / `rc=3` · **0 eventos** `[MEDIDO]` |
| 3 | REST na **mesma** janela `[1788016790957,1788016917197]` | 2 símbolos, 120 s | BTCUSDT **≥1000** (teto do `limit`), DOGEUSDT **121** `[MEDIDO]` |
| 4 | WS + REST **simultâneos**, `/ws/btcusdt@aggTrade` | 1 símbolo, 60 s | WS **0** eventos + **1** `PING`; REST **698** `[MEDIDO]` |
| 5 | 8 streams, 1 conexão cada, 10 s | 8 streams | `trade` 0,1 s · `bookTicker` 0,07 s · `depth5` 0,06 s · **`aggTrade` silêncio** (BTC/ETH/DOGE) · `markPrice` e `kline_1m` silêncio `[MEDIDO]` |
| 6 | `ssl.getpeercert()` em 3 hosts | 3 hosts | **DigiCert Inc**, CN `*.binance.com`, sem proxy ⇒ **sem interceptação** `[MEDIDO]` |

⇒ **≥ 1121 aggTrades ocorreram enquanto o WS entregava 0.** A janela não estava vazia.

### Os controles (o mesmo caminho de código, vereditos opostos)

| controle | universo | resultado | `rc` |
|---|---|---|---|
| host que não resolve | 1 símbolo | `NOT_MEASURED` · **`DNS`** | 3 |
| host real que recusa upgrade (`fapi`) | 1 símbolo | `NOT_MEASURED` · **`HTTP_UPGRADE`** (403) | 3 |
| **conectado, stream sem `nq`** (`bookTicker`) | 2 símbolos, 15 s | **`ABSENT_IN_ALL`**, n=**40** | 0 |
| **conectado, `aggTrade` de SPOT** | 2 símbolos, 25 s | **`ABSENT_IN_ALL`**, n=**60** | 0 |

Os dois últimos fecham a saída *"o instrumento não sabe ler `aggTrade`"*.

**Controle que NÃO discrimina, registrado como tal:** o ack de `SUBSCRIBE` devolve
`{"result":null,"id":99}` para `btcusdt@naoExisteStream` **igual** ao de um stream válido
(3 streams, 1 conexão cada). ⇒ **Assinatura aceita não é evidência de stream existente.**

### Falsificação por mutação (`PYTHONDONTWRITEBYTECODE=1`, `sha256` conferido no retorno)

| mutação | `rc` | veredito |
|---|---|---|
| `NULL` colapsado em `ABSENT` | 1 | **reprovou** |
| janela vazia vira medição | 1 | **reprovou** |
| GUID com o `C` transposto | 1 | **reprovou** |
| sobra do handshake descartada | 1 | **reprovou** |

**4 de 4 reprovaram; 4 de 4 arquivos restaurados com `sha256` idêntico.**

---

## Cobertura

`bash backend/scripts/test.sh` — **domain 100,0%** (meta 90) · **use_cases 100,0%** (meta 80) ·
**infra 96,8%** (meta 70). Global 98,4%.

**Superfície não coberta, nomeada:** `connect_tls` (14 linhas, `binance_stream_probe.py:69-82`)
— abre socket TLS real e a suíte é **ZERO REDE** por construção. Não foi marcada `no cover`:
aparece no relatório como o que é.

---

## Doc delta

- `docs/medicao-ws-aggtrade-nq.md`: **criado** — a medição, os controles, o que `T-03.4` pode e
  não pode assumir, e os 4 defeitos que os controles pegaram.
- `docs/INDEX.md`: **atualizado** — 1 linha acrescentada; nenhuma linha existente reescrita.
- `data/MANIFEST.md`: **sem mudança** — o arquivo é **gitignored** (`.gitignore:47 data/`), não
  entra em PR. A evidência vive em `data/binance/ws/` e o documento cita o caminho.
- `ADR-001`: **sem mudança** — a dependência que ele declara **continua aberta**. Fechá-la ou
  reescrevê-la exige a medição que esta rodada não obteve, e é ato de arquitetura.
- **ADR novo: não necessário** — nenhuma decisão arquitetural foi tomada. O contrato
  `QF-1..QF-6` sobrevive intacto, como `ADR-001` e o plano `03` já previam para as duas
  respostas.

---

## Bloqueado

1. **`D3.9` não fecha.** O WS de futuros entregou **0** mensagens em 120 s / 5 símbolos. O DoD
   pede 1 mensagem. **Não fechou, e não está sendo apresentado como fechado.**
2. **Não sei se o silêncio é global ou deste observador.** Uma origem de rede, uma faixa de
   horário (≈14h50–15h15 UTC de 2026-08-29). **Nada aqui autoriza dizer "a Binance parou de
   servir `aggTrade`".** Repetir de outra origem custa 1 comando e 2 min.

---

## O que `T-03.4` pode assumir

**PODE:**
- O contrato `QF-1..QF-6` de `ADR-001` está **intacto** — o próprio `ADR-001` e o plano `03`
  declaram que o desenho do item 3.5 muda, o contrato não.
- O **REST de futuros carrega `nq` hoje** (8 campos, reproduzido nesta rodada).
- O instrumento e os controles estão **prontos e em portão**; remedir é um comando.

**NÃO PODE:**
- **Assumir que o WS entrega `aggTrade`.** Um coletor desenhado sobre esse stream não recebe
  nada e, sem veredito por estágio, **pareceria estar funcionando gravando buckets vazios** —
  e `nq` é `CL-5`, *capture-or-lose*: relógio consumido sem captura.
- **Assumir que o WS não tem `nq`.** Não foi observado. Desenhar a queda para REST como se
  fosse fato medido herda uma conclusão que esta rodada **não** produziu.

---

## Decisões de domínio (do `quant-architect`, não minhas)

1. Se um `nq` nulo por trade vira zero, ausência, ou herda `q` no bucket.
2. Se a série `nq` do WS, quando existir, é a **mesma** série do REST para efeito de
   `quantity_field` como termo de identidade.
3. Se a queda para REST (peso, janela de 48 h) preserva `CL-5` ou aceita perda declarada.
