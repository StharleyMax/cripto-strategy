# QA Gate — T-07.7 (plataforma-dados, fase 07, sentimento) — PR #82

## Veredito

## QA Gate — Fase 07 [sentimento]
- [OK] core.relative-import / core.silent-except / core.print-statement / core.hardcoded-secret — `bash backend/scripts/lint.sh` (worktree /tmp/claude-1002/wt/T-07.7): "All checks passed! / 186 files already formatted / Success: no issues found in 186 source files" (ruff + mypy --strict)
- [OK] Natureza (ADR-016) — `bash backend/scripts/natureza.sh`: "universo de 58 arquivo(s), 0 leitura(s) de relogio" sob domain/use_cases de sentimento; `bash backend/scripts/boundaries.sh`: "Contracts: 3 kept, 0 broken" (129 arquivos, 583 dependencias) — jitter.py e circuit_breaker.py confirmados domain puro (random permitido por natureza.py, so time/socket/ssl guardados; `domain/jitter.py:19-26` e `domain/circuit_breaker.py:37-43` justificam)
- [OK] Testes existem e passam — `bash backend/scripts/test.sh` (reproduzido independentemente, worktree /tmp/claude-1002/wt/T-07.7, 464.99s): "1064 passed, 3 warnings" rc=0
- [OK] Cobertura medida 97,84% total contra alvo global 70% — por camada: domain 99,8% (meta 90%, 2111/2115), use_cases 100,0% (meta 80%, 537/537), infra 95,1% (meta 70%, 1786/1878) — todas acima do alvo
- [OK] DoD da fase (T-07.7, CA-F3-9, plano 07 item 7.9):
  - Broker de cota com jitter e circuit breaker, contagem local, por composicao sobre local_quota_broker.py/quota_bucket.py sem duplicar logica — confirmado por leitura de quota_circuit_broker.py (compoe LocalQuotaBroker + JitterPolicy + circuit_breaker via decide_call/record_failure/record_success)
  - Ciclo completo CLOSED->OPEN->HALF_OPEN->CLOSED testado (test_circuit_breaker.py::test_the_full_cycle_closed_to_open_to_half_open_to_closed_again) e reproduzido via mutacao (ver abaixo)
  - Jitter varia de fato, nao e mock fixo — reproduzido via mutacao (ver abaixo)

## Mutacoes plantadas de forma independente (nao as do relatorio do builder) e resultado

1. **Circuit breaker nao abre apos falhas consecutivas** — `circuit_breaker.py:254`, troquei
   `consecutive_failures >= policy.failure_threshold` por `False` (mantendo `kind.trips_immediately`
   intacto, para isolar so o gatilho por limiar generico). Comando:
   `bash backend/scripts/test.sh --no-cov tests/sentimento/test_circuit_breaker.py` ->
   `1 failed, 16 passed` — `test_the_full_cycle_closed_to_open_to_half_open_to_closed_again` falha em
   `assert state.phase is CircuitPhase.OPEN` (ficou CLOSED apos 3 falhas TRANSPORT_ERROR). Revertido;
   `git diff` sobre `circuit_breaker.py` = 0 linhas apos reversao.
2. **Circuit breaker nao fecha depois do cooldown/half-open** — `circuit_breaker.py:217`, troquei
   `if remaining > 0:` por `if True:` (OPEN nunca transiciona para HALF_OPEN). Comando:
   `bash backend/scripts/test.sh --no-cov tests/sentimento/test_circuit_breaker.py
   tests/sentimento/test_quota_circuit_broker.py` -> `2 failed, 20 passed` —
   `test_the_full_cycle_closed_to_open_to_half_open_to_closed_again` (`assert False is True` na
   transicao apos cooldown) e `test_the_circuit_reopens_from_the_broker_after_a_failed_probe`
   (mesma causa, via a fachada do broker). Revertido; `git diff` = 0 linhas.
3. **Jitter deterministico disfarcado de aleatorio** — `jitter.py:79`, troquei
   `return random.random()` por `return 0.37` fixo. Comando:
   `bash backend/scripts/test.sh --no-cov tests/sentimento/test_jitter.py
   tests/sentimento/test_quota_circuit_broker.py` -> `2 failed, 16 passed` —
   `test_the_real_random_source_is_not_hardcoded_to_the_same_value`
   (`assert 1 > 1`, onde `1 = len({0.37})`) e
   `test_the_real_random_source_feeds_a_broker_pause_that_also_varies`
   (`assert 1 > 1`, onde `1 = len({1.383})`). Revertido; `git diff` = 0 linhas.

`git status --short` e `git diff | wc -l` = 0 apos as tres reversoes (worktree /tmp/claude-1002/wt/T-07.7,
confirmado antes de gravar este veredito).

Regras bloqueantes avaliadas: 7 de 7 listadas por `harness rules list --severity block` (4 core code,
2 web-fullstack sem escopo neste PR — sem browser import, sem tenant-from-request, componente e
`sentimento`/backend — e 1 path-presence de server-test-directory, N/A a este PR pois nao toca
frontend/server).

Veredito: APPROVED

Notas: as 2 mutacoes proprias reportadas pelo builder (jitter hardcoded; remocao de
`kind.trips_immediately`) foram reproduzidas de forma equivalente e independente acima com pontos de
mutacao proprios (limiar generico via `False` em vez de threshold absurdo; cooldown via `if True:` em
vez de subtracao de tempo) — o falsificador do DoD segura sob ataque direto aos tres pontos centrais:
abertura por limiar, fechamento por cooldown/half-open, e nao-determinismo do jitter.

## [ANOMALIA] `harness gate-record` recusou o registro do veredito

Comando `harness gate-record plataforma-dados 07 QA APPROVED "..."` -> recusado (exit != 0), NAO pelo
merito de T-07.7, mas por uma inconsistencia PRE-EXISTENTE e ALHEIA a esta task: a fase **08** ja tem
um veredito QA APPROVED registrado (ts `2026-09-02T18:30:01Z`) que hoje contradiz `tasks.toml` — 13 de
14 tasks da fase 08 (`T-08.1..T-08.14`, exceto `T-08.11=done`) seguem em estado nao-terminal
(`todo`/`blocked`). O portao de `gate-record` recusa QUALQUER novo registro enquanto essa contradicao
existir em qualquer fase do ledger, nao so na fase que se esta gravando.

Isto e fora do escopo de T-07.7 (fase 07, componente sentimento) — nao e desta QA fechar tasks da fase
08, e o unico escape (`harness pipeline override`) exige motivo auditado que so o owner/orquestrador
pode declarar. **Nao usei o override.** Consequencia: o veredito tecnico acima (APPROVED, com evidencia
citavel) fica registrado NESTE arquivo, mas NAO no ledger via `gate-record`, e por isso **nao resolvi
`T-07.7` em `tasks.toml`** — a evidencia que `harness tasks resolve` exige (ts+veredito+commit do gate
copiados pela maquina) nao existe enquanto o `gate-record` nao aceitar o registro. Quem despachou esta
QA precisa investigar a fase 08 (provavelmente outra sessao paralela) antes que T-07.7 possa ser
fechada oficialmente no ledger.

`harness tasks list plataforma-dados` confirma hoje: `T-07.7  07  todo  linked  sentimento` — ainda nao
resolvida no documento de tasks, exatamente por essa razao.
