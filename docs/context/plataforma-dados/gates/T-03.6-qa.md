# `T-03.6` (`CST-22`) — relatório de gate do `/qa`

**Feature:** `plataforma-dados` · **Componente:** `sentimento` · **Fase:** `03`
**Base:** `master@fba23f9` · **Branch:** `tasks/T-03.6-availability-probe-set` · **PR:** #73

## Veredito

**APPROVED.** Suíte real rodada do zero (não confiei no relatório do builder): `bash
backend/scripts/test.sh` → `1029 passed, 3 warnings in 324.83s`, cobertura total `97.82%`
(domain/use_cases 100%, infra com os 3 arquivos novos entre 75–100%), batendo exatamente os
números do relatório do builder. `lint.sh` limpo (180 arquivos), `natureza.sh` → `universo de
55 arquivo(s), 0 leitura(s) de relogio` em domain/use_cases, `boundaries.sh` → `3 kept, 0
broken`, `harness rules --mode sweep --changed-only` → `rc=0`, saída vazia.

## Mutação — 5 mutantes plantados nos 3 pontos centrais do DoD, todos mortos, todos revertidos

1. **D3.3 (cálculo de período/cota):** `availability_probe_set.py:182`, `>=` → `>` no limiar
   `MAX_INFORMATIVE_PERIOD_SECONDS` (deixaria período == 60s passar). Morto por
   `test_availability_probe_set.py::test_a_period_of_sixty_seconds_is_refused_regardless_of_budget`
   (`DID NOT RAISE` sem a mutação revertida).
2. **D3.2 (tabela de defasagem):** `availability_lag_stats.py:124`, `lag_resolution_s` trocado
   por constante `0.0` em vez do valor lido do probe. Morto por
   `test_availability_lag_stats.py::test_lag_resolution_and_window_are_read_off_the_probe_not_derived_from_samples`
   (`0.0 == 10.0` falhou).
3. **D3.4 (razão OBSERVED/total):** `availability_lag_stats.py:82`, `observed_ratio` trocado de
   `lag_n/total_polls` para uma fórmula que estima (`lag_n/(total_polls-1)` truncada em 1.0).
   Morto por `test_observed_ratio_divides_transitions_by_every_polled_line` (`0.222… != 0.2`).
4. **D3.4 (linha a linha, não amostra):** `availability_probe_cli.py:117`, `for attempt in
   attempts` → `attempts[::2]` (amostragem 1-a-cada-2 em vez de toda linha). Morto por
   `test_availability_probe_cli.py::test_run_emits_a_header_every_attempt_line_and_a_summary_row`
   (`len(attempt_lines) == footer["n_attempts"]` falhou, `2 == 3`).
5. **Extra (baseline nunca é amostra):** `availability_lag.py:70`, removida a guarda `previous
   is not None` — a 1ª leitura passaria a virar amostra fabricada. Morto por 9 testes de
   `test_availability_lag.py` de uma vez (inclusive `ValueError` de `available_at < event_time`
   nos fixtures existentes).

Todas as 5 mutações foram revertidas uma a uma; `git diff --stat` limpo confirmado após cada
reversão e ao final.

## Achado não bloqueante (relatado, não corrigido — regra "só testes")

Plantei um 6º mutante fora dos 3 pontos pedidos: margem de 10% no limiar de cota Binance
(`BINANCE_FUTURES_DATA_REQUESTS_PER_MINUTE * 1.10` em vez do valor exato) — **sobreviveu**, 17
testes verdes. O único teste de estouro de cota Binance usa 20 símbolos (`600/min`, 3x o
limite), não há teste de fronteira perto de 200–220/min. Não é regressão desta task nem
bloqueia o DoD declarado (`D3.3` como escrito exige `<= 200`, e o código usa `>`, correto) — é
lacuna de cobertura de fronteira, registrada para consideração futura, não fundamento de
`NEEDS_FIX`.

## `observer_region` — confirmado como NÃO implementado de verdade

`git diff fba23f9..HEAD` mostra `observer_region` só como string plumada (chave de
agrupamento, coluna de validação, parâmetro de CLI com default `"unknown"`
`infra/availability_probe_cli.py:154`) — nenhuma chamada de geolocalização/IP. Consistente com
`T-03.9` seguir `blocked` (VPS), fora de escopo desta task.

## DoD, item a item

- **D3.3** — `período >= 60s` reprova sozinho (mutante #1 morto); orçamento Binance
  `5×4×(60/10)=120/200` e Coinalyze `2×4×(60/30)=16/40`, ambos dentro do balde, validados em
  `__post_init__`.
- **D3.2** — tabela com `p99`/`lag_n`/`lag_resolution_s`/`lag_window_s` como colunas
  (`LagSummaryRow`), chaveada por `(endpoint, observer_region)`, `total_endpoint_count == 7 >=
  5` testado contra a instância declarada.
- **D3.4** — `observed_ratio` exibido por linha (mutante #3 morto), toda linha de `attempts`
  emitida (mutante #4 morto).
- Rodada ao vivo do builder (9m20s, `n_attempts=1270`, `n_samples=76`) não foi re-executada por
  mim (contra API externa, fora do gate) — aceita como evidência do relatório do builder, não
  como parte da suíte determinística.

Regras bloqueantes avaliadas: 7 de 7 listadas por `harness rules list --severity block`.
- `core.relative-import` — OK, `grep -nE '^\s*from \.|^\s*import \.'` nos 8 arquivos novos → 0.
- `core.silent-except` — OK, nenhum `except: pass`/`except Exception: pass` nos arquivos novos.
- `core.print-statement` — OK, nenhum `print(` nos arquivos novos (usa `logger`/`emit`).
- `core.hardcoded-secret` — OK, `AvailabilityHttpClient` lê `$COINALYZE_API_KEY` de
  `os.environ`.
- `web-fullstack.browser-imports-server` / `tenant-from-request` / `server-test-directory` —
  fora de escopo (mudança é só backend `sentimento`).
