# T-05.16 — builder — a fronteira que mantém `fingerprint()` SÍNCRONA (D5.17)

`CST-107`. Escopo: `frontend/eslint.config.mjs` (novo bloco) +
`frontend/src/features/s1-console/fingerprint-sync-boundary.test.ts` (novo) +
`ingest-health-query.ts` (docstring, +12 linhas, sem mudança de comportamento).
`ADR-005/D6.3`/`D6.4` já fixam que `fingerprint()` fica síncrona — não redecidida aqui.

## D5.17(a) — paridade F-D6-1 fora do render, JÁ COBERTA (herdada de `T-05.15`)

`ingest-health-query-http.test.ts` já roda "CALA F-D6-1"/"MORDE F-D6-1" (Python × Node, via
processo ASGI real em loopback, fora de qualquer caminho de render — `S1Console.tsx` não é
tocado por nenhum teste desta suíte). Confirmado vivo nesta rodada, não presumido:
`npm --prefix frontend run test:s1` → **54/54 pass**, incluindo os dois testes de paridade e
os 2 de `F-D6-2` `[MEDIDO 2026-09-04]`. Nada foi alterado nesse arquivo.

## D5.17(b) — o portão de importação, instrumento HERDADO de `D5.12` (não redecidido)

`05_fatia_visivel.md:53` deixa a pergunta do instrumento em aberto para `D5.12`; `T-05.1` já a
respondeu com medição (as 3 alternativas nomeadas exigem infra inexistente;
`no-restricted-imports`/`group` lê caminho mas não alimenta `harness.toml`/
`[agents.by_component]`, então `ADR-003:46` não alcança este arquivo). `T-05.16` HERDA essa
resposta: `@typescript-eslint/no-restricted-imports` — a variante TS-aware, porque só ela tem
`allowTypeImports` (`node_modules/@typescript-eslint/eslint-plugin/dist/rules/
no-restricted-imports.js:23-26,169-212`, lido antes de escolher) — com `group:
["**/ingest-health-query.ts"]`, aplicado a `src/**/*.{ts,tsx,mts,cts}` menos `*.test.ts`
(mesmo recorte do `grep -v '\.test\.'` do DoD).

**Verificado que o glob casa import relativo com `../`**, não presumido: `ignore` (a lib que o
plugin usa por baixo, com `allowRelativePaths: true`) contra `./ingest-health-query.ts` e
`../s1-console/ingest-health-query.ts` → **`true` nos dois** `[MEDIDO 2026-09-04, node -e
contra node_modules/ignore]`.

## Prova morde+cala — `fingerprint-sync-boundary.test.ts`, mesmo padrão de `eslint-boundary.test.ts` (`D5.12`/`T-05.1`)

```
✔ D5.17(b) MORDE+CALA: a VALUE import of ingest-health-query.ts is refused from both a
  sibling feature and the same directory, an `import type` probe stays clean, and the real
  tree (3/3 import type) stays green
```
`[MEDIDO 2026-09-04]`. Dentro do teste, na MESMA passada:
- **MORDE (i):** `import { fingerprint } from "../s1-console/ingest-health-query.ts"` plantado
  em `s3-inspector/` → `eslint` `rc≠0`, `ruleId: ["@typescript-eslint/no-restricted-imports"]`.
- **MORDE (ii):** `import { canonicalProjection } from "./ingest-health-query.ts"` plantado
  DENTRO de `s1-console/` (a outra metade do `grep` do DoD) → mesmo veredito.
- **CONTROLE positivo:** `import type { IngestHealthProjection } from "../s1-console/
  ingest-health-query.ts"` sobre o MESMO alvo → `ruleId: []` — prova que o portão distingue
  tipo de valor, não que rejeita qualquer menção ao módulo.
- **CALA:** probes removidos em `finally`; árvore real (`s3-inspector/{fixtures,domain,
  view-model}.ts`, os 3 importadores reais, existência reafirmada por `existsSync`, não
  presumida) → `eslint src` limpo.

MORDE roda e é asserted ANTES de CALA — falsificador de vacuidade (`ADR-012`) aplicado antes
de aceitar o `rc=0`.

## Comandos rodados (literais)

- `make lint-frontend` → ESLint + `tsc --noEmit --strict`, ambos `rc=0`.
- `npm --prefix frontend run test:s1` → **54/54 pass** (universo: 6 arquivos `*.test.ts` sob
  `s1-console/`, incluindo o novo).
- `npm --prefix frontend run test:s3` → 37/37 pass (regressão).
- `npm --prefix frontend run test:app` → 97/98 pass, 1 falha PRÉ-EXISTENTE
  (`universe-at.test.ts`, depende de `data/`, ausente nesta worktree — mesma classe que
  `T-05.15-builder.md` já registrou).
- `npm --prefix frontend run test:charts` → falhas PRÉ-EXISTENTES nos testes "REAL FIXTURE"
  (mesma causa: `data/` não versionado). Não tocado por esta task.
- `harness rules --mode sweep --changed-only` → `rc=0`, saída vazia.
- `harness code-paths classify` sobre o teste novo → `producao`.
- `make verify` → `INDETERMINADO`: `regras`/`política` recusam medir com "mecanismo NAO
  RESOLVIVEL" para este caminho de worktree — GAP DE AMBIENTE JÁ REGISTRADO por
  `T-05.11-builder.md` e `T-05.14-builder.md`, não desta task. `lint-backend` (265 arquivos),
  `lint-frontend`, `test` (1437 pass, 97.61% cobertura) e `boundaries` (4 kept, 0 broken) —
  todos `OK`. `harness rules --mode sweep --changed-only`, rodado direto, é o substituto já
  aceito nesses dois gates anteriores.

## Doc delta

- Docstring de `ingest-health-query.ts` — seção nova "UPDATE, T-05.16/D5.17(b)", mesmo padrão
  das duas seções "UPDATE" anteriores (histórico preservado, não reescrito). Achado no
  processo: `**/` dentro de um comentário de bloco `/* */` fecha o comentário cedo (a
  substring `*/` está em `**/`) — corrigido reescrevendo a frase sem o glob literal.
- `docs/INDEX.md`: sem mudança — nenhum artefato de doc novo fora deste gate report; o código
  e o gate report carregam a justificativa (padrão já usado por `T-05.1`/`T-05.15`).
- `docs/product/STITCH_CONTEXT.md`: sem mudança — nenhuma superfície visível é tocada (0
  arquivo `.tsx` alterado).

## O que esta task NÃO decide (por registro, não por omissão)

- A metade de produto do DoD (`fingerprint` visível na tela) continua `[NÃO SEI]`, do owner —
  hoje ele não é exibido, então `D5.17` fica síncrona.
- `frontend/src/app/threshold-spec-bundle.ts:36,371` (o 2º `createHash` do front) é dona de
  `SPEC-001` §7, não desta task.

## Bloqueado

Nenhum. `D5.17(a)` confirmado coberto; `D5.17(b)` fechado com portão morde+cala.
