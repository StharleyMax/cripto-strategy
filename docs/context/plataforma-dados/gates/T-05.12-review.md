# T-05.12 — REVIEW gate (arquitetural, read-only)

Worktree: `/tmp/claude-1002/wt/T-05.12`, branch `tasks/T-05.12-porta-leitura-processo`, HEAD
`758e2ef` + trabalho não commitado (builder). Bootstrap lido: `harness rules list --severity
block` (8 regras), `docs/proposta-discovery.md` (`docs.bootstrap_docs`), `glossary_doc` (dívida
conhecida, `ADR-013/D4`, fora do escopo desta task), `code_paths`. Gates lidos: `T-05.12-infra-
architect.md`, `T-05.12-builder.md`, `T-05.12-qa.md`. Handoff lido: `handoff/T-05.12.md`.

## Camada 1 — o que o runner mede

`harness rules --mode file --path <arquivo>` rodado em cada um dos 11 arquivos do delta
(`src/api/{__init__,dependencies}.py`, `src/api/routes/{__init__,ingest_health}.py`,
`src/main/{__init__,__main__}.py`, `domain/ingest_record.py`, `tests/api/{__init__,
test_dependencies,test_ingest_health_route_over_the_network}.py`, `pyproject.toml`):

- **[WARNING] `core.module-docstring-single-line`** — 5 achados: `src/api/__init__.py:1`,
  `src/api/dependencies.py:1`, `src/api/routes/ingest_health.py:1`, `src/main/__init__.py:1`,
  `src/main/__main__.py:1`. Regra é AVISO, não BLOQUEIO (confirmado contra `harness rules list
  --severity block`, que não a lista — 8 regras, nenhuma é esta). Os 5 módulos abrem o docstring
  com uma frase-resumo na linha 1 e continuam em parágrafos abaixo (estilo PEP-257 padrão do
  resto do repositório, ex. `domain/ingest_record.py` não pega a regra porque seu docstring é
  literalmente 1 linha). Não decide o veredito (WARNING, não BLOQUEIO) mas fica citado: 5/5
  arquivos novos de `src/api`+`src/main` batem nela, e é o único achado mecânico do delta inteiro.
  Correção: se o repositório quiser estilo multi-parágrafo, ou os módulos passam a ter um
  resumo estritamente monolinha, ou a regra ganha exceção — decisão de convenção, não desta
  review.
- `harness rules --mode sweep --changed-only` → `exit=0`, nenhum achado (confirmado
  independentemente, mesmo resultado do builder/QA).
- As 8 regras BLOQUEANTES: nenhuma mordeu nenhum dos 11 arquivos (`core.relative-import`,
  `core.silent-except`, `core.print-statement`, `core.hardcoded-secret`,
  `web-fullstack.browser-imports-server`, `web-fullstack.tenant-from-request`,
  `web-fullstack.server-test-directory-present`, `own.compose-hardcoded-secret`) — confirmado
  via `harness rules --mode file` arquivo a arquivo, não só via grep manual do QA.

Denominador desta camada: **8 regras bloqueantes em vigor, 8 avaliadas, 11 arquivos varridos**
com `harness rules --mode file --path <arquivo>` + `harness rules --mode sweep --changed-only`.

## Camada 2 — arquitetura declarada, confirmada por leitura direta (não por citação do gate)

**(1) `src.main` é o ÚNICO ponto que importa infra de sentimento:**
```
grep -rn "infra" backend/src/api/ --include='*.py'
# -> só 2 linhas de PROSA em docstring (`__init__.py:6`, `dependencies.py:7,9`), zero import
grep -n "SqliteIngestRecordStore" backend/src/api/dependencies.py
# -> só menção em docstring, não `from ... import`
grep -rln "SqliteIngestRecordStore" backend/src/ --include='*.py'
# -> único hit fora de `src/modules/sentimento/infra` é `backend/src/main/__init__.py`
```
Confirmado: `src/api` nunca importa infra diretamente. Cadeia real de import é `src/api/routes/
ingest_health.py` → `src.api.dependencies.get_ingest_record_source` (STUB, `IngestRecordSource`
do porto) → override só acontece em `src/main/__init__.py:28`, via `app.dependency_overrides`.

**(2) Handler chama `ingest_health_query(source)`, zero SQL próprio:**
`backend/src/api/routes/ingest_health.py:35-36` — corpo inteiro é `report =
ingest_health_query(source)` + `return report.to_envelope()`. `grep -niE 'SELECT|FROM
|psycopg|sqlite3'` só casa com as 4 linhas de `from X import Y` do próprio Python (falso
positivo do padrão, já nomeado pelo builder §4) — zero SQL real. Lacuna real e já documentada
por QA (§3 do gate QA): nenhuma das 8 regras bloqueantes nem `import-linter` morde uma string
SQL arbitrária plantada dentro do handler — só o grep literal do DoD morde. **Confirmado, não
é achado novo desta review** — está no `tasks.toml` como `[NAO SEI]` com dona `ADR-008/D4`.

**(3) Extração em `domain/ingest_record.py` é puramente estrutural:**
`git diff` lido linha a linha. `_project_run`/`_project_gap` (que já montavam um dict interno
antes de `canonical_json(...)`) foram divididos em `_project_run_dict`/`_project_gap_dict`
(retornam o dict, corpo idêntico ao que já existia) + as funções originais, que passam a chamar
`canonical_json(_project_..._dict(...))` — bit a bit o mesmo resultado. `to_envelope()` é
código NOVO, mas só compõe os dois dict-builders existentes num objeto aninhado — nenhuma
coluna de `INGEST_HEALTH_RUN_COLUMNS`/`INGEST_HEALTH_GAP_COLUMNS` foi acrescentada, removida
ou reordenada; `janela_de_perda`→`LOSS_WINDOW_NOT_COMPUTED_IN_F0` e `class`↔`gap_class`
continuam exatamente como antes. Rodei `tests/sentimento/test_ingest_health_query.py`
`tests/sentimento/test_ingest_health_contract_guards.py` → 32 passed (mesmo número citado pelo
builder). O ponto que o infra-architect sinalizou como possível turf do `quant-architect` (§5
do gate) não foi cruzado — é extração de forma, não decisão de contrato de coluna.

**(4) Pin exato, sem faixa:**
`backend/pyproject.toml`: `dependencies = ["pyyaml (==6.0.3)", "fastapi (==0.141.1)", "uvicorn
(==0.52.4)"]` — `==` nas 3 entradas, nenhum `^`/`>=`/`~=`. Confirmado sem `[standard]`:
`.venv/bin/python -c "import uvloop"` / `"import httptools"` → `ModuleNotFoundError` (rodado
por mim).

**(5) Rota não exposta à rede pública:**
`backend/src/main/__main__.py:27` — `uvicorn.run(app, host="127.0.0.1", ...)`.
`backend/tests/api/test_ingest_health_route_over_the_network.py:71` — `uvicorn.Config(app,
host="127.0.0.1", port=0, ...)`. `grep -rn "0\.0\.0\.0" backend/src/ backend/tests/` → vazio.
`deploy/compose.yml` (existente, de `T-01.10`) não referencia `src.main`/a API — só Postgres;
nenhum conflito de bind. Confirmado: loopback nos dois lugares que sobem o processo (launcher
e teste), sem exceção.

## Testes — reproduzidos, não citados

```
backend/scripts/lint.sh        -> All checks passed! / 265 files formatted / mypy 0 issues
backend/scripts/boundaries.sh  -> 3 contratos import-linter KEPT (nenhum contrato novo de
                                   T-05.13 existe ainda — fora de escopo, confirmado pelo pyproject)
.venv/bin/pytest tests/api -v  -> 3 passed (test_dependencies + 2 de rede)
backend/scripts/test.sh        -> 1437 passed, 3 warnings, 286.96s — bate exatamente com QA
                                   Cobertura: domain 99.8%, use_cases 100.0%, infra 95.1%,
                                   TOTAL 97.61% (piso 70%)
harness rules --mode sweep --changed-only -> exit=0
```

## Veredito

**COMPLIANT.**

Denominador: 8 regras bloqueantes em vigor (`harness rules list --severity block`), 8
avaliadas contra 11 arquivos do delta (`harness rules --mode file` arquivo a arquivo +
`--mode sweep --changed-only`), 0 violação bloqueante. 1 achado de camada 1 não-bloqueante
(WARNING `core.module-docstring-single-line`, 5/11 arquivos) citado acima, não decide o
veredito. As 5 confirmações estruturais pedidas pelo despacho (import único de infra,
zero-SQL no handler, extração estrutural em domain, pin exato, bind loopback) foram
verificadas por leitura/grep/execução direta, não por citação dos gates de builder/QA — todas
se sustentam.
