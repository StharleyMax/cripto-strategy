# QA Gate — `T-05.9` (`plataforma-dados`, fase 05, componente `web`)

**Executor:** `qa` · **Data:** 2026-09-02 · **Worktree:** isolada
(`.claude/worktrees/agent-a04d38e15fb9e42cf`, commit `f3236c3`)

## 1. Escopo lido

`docs/context/plataforma-dados/gates/T-05.9-builder.md`, `docs/adr/ADR-005-transporte-de-
leitura.md` (inteira), `docs/plans/SPEC-001-plataforma-dados/05_fatia_visivel.md` (item 5.12,
`DoD D5.8`), `docs/context/plataforma-dados/tasks.toml:694-701` (bloco `T-05.9`),
`frontend/src/app/history-transport.ts`, `frontend/src/app/history-transport.test.ts`.

## 2. Baseline — testes que já existem, rodados antes de tocar em qualquer coisa

```
cd frontend && npm run test:app
-> tests 37, pass 37, fail 0 (23 de T-05.9 + 14 pré-existentes de T-05.8)
npm run lint
-> eslint src, 0 erro, 0 aviso
```

## 3. Regras bloqueantes — evidência por arquivo, não por lista

`harness rules list --severity block` → 7 regras em vigor. Aplicadas por sweep de arquivo:

```
harness rules --mode file --path frontend/src/app/history-transport.ts --format ndjson
-> rc=0, 0 achado

harness rules --mode file --path frontend/src/app/history-transport.test.ts --format ndjson
-> rc=2, 1 achado: web-fullstack.hardcoded-url, severidade WARN (linha 21, `TEST_BASE_URL`
   sintético, nenhum host contatado — mesmo padrão já aceito em T-05.8), FORA do conjunto de
   severidade block
```

Nenhuma das 7 regras bloqueantes (`core.relative-import`, `core.silent-except`,
`core.print-statement`, `core.hardcoded-secret`, `web-fullstack.browser-imports-server`,
`web-fullstack.tenant-from-request`, `web-fullstack.server-test-directory-present`) tem
achado nestes dois arquivos. `web-fullstack.server-test-directory-present` é sobre o
servidor Python (`backend/tests/`), fora do escopo desta task (nenhum arquivo de `backend/`
tocado — confirmado por `git show --stat f3236c3` só listar `frontend/` e `docs/`).

## 4. Validação destrutiva por MUTAÇÃO — o item central deste gate

Working tree confirmado limpo antes de começar (`git status --short
frontend/src/app/history-transport.ts` → vazio). Todas as mutações abaixo foram revertidas
imediatamente após medidas; `git diff --stat` e `npm run test:app`/`npm run lint` reconferidos
limpos ao final (linha "5. Estado final").

### 4.1 Payload de teste independente (fora da suíte do builder) — os dois falsificadores

Script isolado em `/tmp/.../scratchpad/mutation-check.mjs`, importando as funções reais e
plantando os dois defeitos que `ADR-005`/Falsificador proíbe, SEM reusar os payloads do
builder:

```
node --experimental-strip-types mutation-check.mjs
MUTATION 1 (tick field) -> threw: 'ADR-005 violada: campo de nivel de tick "agg_id"
  encontrado em $.trades[0].agg_id — nenhum tick chega ao browser (falsificador da ADR, DoD D5.8)'
MUTATION 2 (rate) -> threw: 'ADR-005 violada: espacamento de 250ms entre buckets [1, 2] e
  mais fino que o intervalo pedido (60000ms) — taxa acima de max(1 Hz, 1/TF), sintoma de tick
  chegando disfarcado de bucket (falsificador da ADR, DoD D5.8)'
```

`assertNoTickLevelFields` e `assertBucketSpacingWithinInterval` REPROVAM os dois defeitos
plantados, com payload novo (não o do arquivo de teste do builder). **OK.**

### 4.2 Mutação no CÓDIGO DE PRODUÇÃO — mata (ou não) a suíte real

Três mutações aplicadas a `frontend/src/app/history-transport.ts`, cada uma revertida antes
da próxima, `npm run test:app` rodado após cada uma:

| # | mutação | resultado esperado | resultado medido |
|---|---|---|---|
| A | remover `"is_buyer_maker"` de `TICK_LEVEL_FIELD_NAMES` | suíte reprova (mata o mutante) | **MORREU** — `tests 37, pass 36, fail 1` |
| B | `spacing < intervalMs` → `spacing < intervalMs / 2` (portão de taxa aceita até 2× o limite de `D5.8`) | suíte reprova | **SOBREVIVEU** — `tests 37, pass 37, fail 0` |
| C | remover `"first_trade_id"` de `TICK_LEVEL_FIELD_NAMES` | suíte reprova | **SOBREVIVEU** — `tests 37, pass 37, fail 0` |

**Mutação A morre** — prova que o teste de mutação já escrito pelo builder
(`assertNoTickLevelFields e MUTAÇÃO`, linha 159 do `.test.ts`) protege especificamente
`is_buyer_maker`.

**Mutação B sobrevive — DEFEITO REAL, não opinião.** Nenhum dos 9 testes de
`assertBucketSpacingWithinInterval` usa um espaçamento entre `intervalMs/2` e `intervalMs`
(os dois casos de violação testados usam 400ms/300000ms e 500ms/60000ms — fatores de
~750× e ~120× menores que o intervalo, não perto do limiar). Uma regressão de produção que
frouxasse o limiar de `D5.8` para o dobro do permitido (aceitando taxa até `2×max(1Hz,1/TF)`)
passaria pelos 37 testes sem reprovar nenhum. Isto é exatamente o "verde não prova nada até
uma mutação reprovar" — a suíte atual cala sobre essa mutação específica.

**Mutação C sobrevive — segundo defeito independente.** `TICK_LEVEL_FIELD_NAMES` tem 8
nomes; só 4 têm teste que planta EXATAMENTE aquele nome e prova reprovação individual
(`agg_id`, `price`, `quantity`, `is_buyer_maker` — linhas 144-167 do `.test.ts`). Os outros 4
(`agg_trade_id`, `first_trade_id`, `last_trade_id`, `transact_time`) não têm nenhum teste que
os planta isoladamente; uma remoção acidental de qualquer um deles do conjunto não é
detectada pelos 37 testes.

## 5. Estado final — reversão confirmada

```
git status --short frontend/src/app/history-transport.ts   -> (vazio)
git diff --stat frontend/src/app/history-transport.ts       -> (vazio)
npm run test:app  -> tests 37, pass 37, fail 0
npm run lint      -> 0 erro, 0 aviso
```

Nenhum arquivo de produção foi alterado por este gate (as três mutações da seção 4.2 foram
aplicadas e revertidas na mesma sessão, evidenciado acima).

## 6. Cobertura

Sem piso declarado para `web` em `harness policy --key test_cmd` (só cobre `sentimento`).
23 testes novos cobrem 100% das funções exportadas — mas a seção 4.2 mostra que "cobrir a
função" não é o mesmo que "matar toda mutação plausível": os dois falsificadores da ADR têm
teste de caminho feliz + 1 caso de violação cada, mas o caso de violação escolhido está longe
do limiar em ambos os portões (mutações B e C), deixando uma classe de regressão sutil sem
detecção.

## 7. DoD `D5.8`, item a item

- "taxa ≤ `max(1Hz, 1/TF)` por série" — mecanismo presente e correto no código atual
  (`spacing < intervalMs`, verificado por leitura E por mutação A/testes de linha 4 e 5), mas
  a suíte não prova resiliência a uma regressão de limiar (mutação B sobrevive) — **DoD
  tecnicamente satisfeito pelo código, mas o teste que deveria protegê-lo permanentemente não
  o faz**.
- "zero campo de nível de tick" — mecanismo presente e correto (conjunto de 8 nomes,
  percurso recursivo), mas 4 dos 8 nomes não têm teste individual (mutação C sobrevive).

## Veredito (rodada 1)

`NEEDS_FIX`. O código de produção fecha `D5.8` hoje (nenhuma mutação alterou o comportamento
atual — as mutações foram aplicadas E revertidas só para medir a suíte). O defeito é na
COBERTURA DE MUTAÇÃO do falsificador, não no comportamento: um regressor futuro nos dois
portões pode passar pelos 37 testes verdes sem ser pego.

### Ações
1. Adicionar a `history-transport.test.ts` um caso de violação de
   `assertBucketSpacingWithinInterval` com espaçamento entre `0,5×intervalMs` e `1×intervalMs`
   (ex.: `0,7×intervalMs`) para matar a mutação B.
2. Adicionar 4 testes de `assertNoTickLevelFields`, um por nome ainda não coberto
   individualmente (`agg_trade_id`, `first_trade_id`, `last_trade_id`, `transact_time`), para
   matar a mutação C.

## 8. Rodada 2 — reverificação por MUTAÇÃO (commit `e824d1a`)

**Diff em `frontend/src/app/history-transport.test.ts` desde `f3236c3`:** 4 testes novos de
`assertNoTickLevelFields` (um por nome ainda não coberto: `agg_trade_id`, `first_trade_id`,
`last_trade_id`, `transact_time`, linhas ~159-181) + 1 teste novo de
`assertBucketSpacingWithinInterval` a `0,7×intervalMs` (linha 223) — exatamente as duas ações
pedidas na rodada 1, nenhuma a mais.

Reaplicação das mutações exatas B e C sobre `frontend/src/app/history-transport.ts` (working
tree confirmado limpo antes: `git status --short` vazio), uma de cada vez, revertida antes da
próxima:

| # | mutação reaplicada | comando | resultado |
|---|---|---|---|
| B | `spacing < intervalMs` → `spacing < intervalMs / 2` (linha 329) | `npm --prefix frontend run test:app` | **MORREU** — `tests 42, pass 41, fail 1`; falha exatamente em `assertBucketSpacingWithinInterval RECUSA espaçamento a 0,7×intervalMs … (mutacao B)` (`history-transport.test.ts:223`), `Missing expected exception` |
| C | remover `"first_trade_id"` de `TICK_LEVEL_FIELD_NAMES` (linha 263) | `npm --prefix frontend run test:app` | **MORREU** — `tests 42, pass 41, fail 1`; falha exatamente em `assertNoTickLevelFields RECUSA first_trade_id sozinho … (falsificador literal da ADR)` (`history-transport.test.ts:169`), `Missing expected exception` |

Cada mutação foi revertida (`git diff --stat frontend/src/app/history-transport.ts` → vazio
após cada reversão) antes de aplicar a seguinte. Estado final: `git status --short
frontend/src/app/history-transport.ts` → vazio; `npm --prefix frontend run test:app` → `tests
42, pass 42, fail 0`; `npm --prefix frontend run lint` → 0 erro, 0 aviso.

Os dois mutantes específicos apontados na seção 4.2 (rodada 1) agora são capturados
individualmente pela suíte real, não por payload isolado. Nenhum arquivo de produção
permaneceu alterado.

## Veredito (rodada 2, final)

`APPROVED`. Mutação B e mutação C — os dois mutantes sobreviventes registrados na rodada 1 —
morrem agora sob a suíte real (`npm --prefix frontend run test:app`), cada uma isoladamente,
com o teste específico apontado como causa da falha. Nenhuma regra bloqueante com achado nos
dois arquivos do escopo (seção 3, inalterada). `DoD D5.8` fechado tanto no código quanto na
proteção de mutação.

Regras bloqueantes avaliadas: 7 de 7 listadas por `harness rules list --severity block`.
