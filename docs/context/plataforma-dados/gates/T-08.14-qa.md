# `T-08.14` — QA (`CST-82`)

**Papel:** `qa` · **Data:** 2026-09-04 · **Task:** `T-08.14` · **Componente:** `charts` (task) /
`backtest` (código tocado, mesma nota de escopo do builder)
**Insumos julgados:** [`ADR-025`](../../../adr/ADR-025-grade-canonica-versionada-com-o-dado-derivado.md)
D1-D6/H1-H4, emenda em [`ADR-021`](../../../adr/ADR-021-run-registry-reprodutibilidade-de-backtest.md),
[`gates/T-08.14-builder.md`](T-08.14-builder.md), diff de `run_registry_entry.py`,
`postgres_run_registry_store.py`, `record_run.py` e os 4 arquivos de teste tocados.

## Reprodução independente (fabricada, fora do arquivo de teste do builder)

Script próprio em `/tmp/.../scratchpad/qa_grid_version_probe.py`, 4 sondas contra `record_run`
real (não mockado), fixtures fabricadas distintas das do builder:

- **probe1/H1**: mesma tripla + `partitions_content_hash` igual, `grid_version` `3`→`9` ⇒
  `GridVersionDivergenceError`, mensagem cita `run-A`, `3` e `9`, store com 1 linha só. PASS.
- **probe2**: `grid_version` repete (`5`==`5`), hash igual ⇒ não recusa (2 linhas). PASS.
- **probe3/H4**: hash E `grid_version` divergem juntos ⇒ `RunRegistryDivergenceError` (G1) —
  nunca `GridVersionDivergenceError` (G6). PASS — confirma a ordem de checagem em
  `record_run.py:107` (G1) antes de `record_run.py:116` (G6).
- **probe4**: mensagem de G6 cita "ADR-025"/"G6"/`grid_version`, nunca a mensagem genérica de
  G1 — `grid_version` não é confundido com `bundle_hash`/`commit` em nenhum caminho.

Todas as 4 sondas: `python .../qa_grid_version_probe.py` → 4 `PASS`, 0 `FAIL`.

## Obrigação prospectiva de `charts` — confirmada como NÃO implementada, e é isso que a ADR pede

`grep -rn "CANONICAL_GRID_VERSION" frontend/src backend/src` → 0 ocorrências. `find
backend/src/modules/charts -iname '*store*' -o -iname '*persist*'` → 0 arquivos. O relatório do
builder e a ADR (`D5`) são explícitos que isso fica de fora desta invocação — nenhuma alegação
falsa de que `charts` já persiste ou já versiona algo.

## Comandos rodados por mim (não pelo builder)

```
bash backend/scripts/lint.sh     # rc=0, ruff+ruff format+mypy --strict limpos, 316 arquivos
bash backend/scripts/test.sh     # rc=0, 1609 passed, 3 warnings pre-existentes (sqlite3, nao relacionados)
                                  # TOTAL 97.78% · domain 99.8% (meta 90) · use_cases 100.0% (meta 80) · infra 95.2% (meta 70)
harness rules --mode sweep --changed-only   # 3 [AVISO] core.module-docstring-single-line, 0 bloqueio
```
Números batem exatamente com o relatado pelo builder (`gates/T-08.14-builder.md`).

## Veredito

## QA Gate — Fase 08 [charts/backtest] T-08.14
- [OK] core.relative-import — 0 ocorrência nos 3 arquivos de produção tocados (grep próprio)
- [OK] core.silent-except — 0 ocorrência
- [OK] core.print-statement — 0 ocorrência
- [OK] core.hardcoded-secret — n/a (nenhuma credencial no diff)
- [OK] web-fullstack.* (3 regras) — n/a, nenhum código de navegador/tenant/servidor-teste tocado
- [OK] own.compose-hardcoded-secret — n/a, nenhum compose tocado
- [OK] Testes existem e passam — `bash backend/scripts/test.sh` (1609 testes, rc=0)
- [OK] Cobertura 97.78% contra alvo global 70% (`ADR-009/D1` por camada: domain 99.8%/90%,
  use_cases 100.0%/80%, infra 95.2%/70% — 3/3 OK)
- [OK] H1 provado — teste do builder + reprodução independente minha (probe1)
- [OK] H4 provado — teste do builder + reprodução independente minha (probe3): G1 tem precedência
  sobre G6 quando ambos divergem, `record_run.py:107` antes de `:116`
- [OK] `grid_version` não confundido com `bundle_hash`/`commit` — mensagem de G6 é distinta e
  cita "ADR-025"/G6 (probe4); nenhuma comparação de `commit` foi introduzida
- [OK] Obrigação prospectiva de `charts` é só nomeada (ADR D5) — confirmado sem código/persistência
  em `backend/src/modules/charts` nem `CANONICAL_GRID_VERSION` em `frontend/src`
- [OK] DoD/escopo da ADR-025 (D1-D4) implementado 1:1; D5/D6/H2/H3 fora de escopo, nomeados, não
  bloqueados (H2 depende do port TS ainda não pedido; H3 depende de cache futuro inexistente)
Regras bloqueantes avaliadas: 8 de 8
Veredito: APPROVED
