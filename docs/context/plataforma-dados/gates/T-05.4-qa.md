# QA Gate — T-05.4 (plataforma-dados, fase 05, charts)

## QA Gate — Fase 05 [charts] — T-05.4

**Branch validada:** `origin/tasks/T-05.4-ausencia-guia` @ `17872a3` (checkout via `git checkout -B qa-review-T-05.4 origin/tasks/T-05.4-ausencia-guia`, pois a branch original já estava aberta em outro worktree). Diff contra `origin/master`: `git diff --name-only origin/master...HEAD` → `frontend/README.md`, `frontend/package-lock.json`, `frontend/src/charts/s2-absence-policy.ts`, `frontend/src/charts/s2-absence-policy.test.ts`.

- [OK] `core.relative-import`/`core.silent-except`/`core.print-statement`/`core.hardcoded-secret` — inspeção de `packs/core/rules.toml` (`paths = ["**/*.py"]`): as 4 regras `core` de escopo `code`/`production` só miram `*.py`. Universo desta task é `*.ts`/`*.md` — as regras não têm arquivo para avaliar. Confirmado também por `harness rules --mode file --path frontend/src/charts/s2-absence-policy.ts --surface ci --format ndjson` → 0 linhas (0 achado).
- [OK] `web-fullstack.browser-imports-server` — `grep -n "backend/" frontend/src/charts/s2-absence-policy.ts frontend/src/charts/s2-absence-policy.test.ts` → 0 ocorrência (só importa `./canonical-grid.ts`/`./s2-scalar-grid.ts`, mesmo diretório).
- [OK] `web-fullstack.tenant-from-request` — escopo `backend/**/*.py`, fora do universo do diff.
- [OK] `web-fullstack.server-test-directory-present` — `ls backend/tests` → presente (`helpers`, `__init__.py`, `sentimento`), inalterado por este diff.
- [OK] Testes existem e passam — `npm --prefix frontend install` (128 pacotes) seguido de `npm --prefix frontend run test:charts` (`node --test 'src/charts/*.test.ts'`) → **79 pass, 0 fail** (10 dos 79 são novos de `s2-absence-policy.test.ts`, 4 contra fixture real `data/binance/metrics/BTCUSDT-metrics-2026-08-{22,23}.csv`). Dado bruto (`data/`, gitignored) não existia neste worktree isolado — linkei `data/` do checkout principal (`ln -s .../cripto-strategy/data data`) só para reproduzir; sem ele, 8 testes de OUTROS arquivos (não deste diff) também dão `ENOENT`, confirmando que a dependência de dado bruto é pré-existente ao componente `charts`, não introduzida por esta task.
- [OK] `npm --prefix frontend run lint` (`eslint src`) → saída vazia, 0 erro/aviso.
- [FAIL→OK após correção provada por mutação] Mutação independente 1 — larguei o teto de "trilho de vigência ≤ grade nativa" em `resolveStockReading` de 1 para 2 buckets nativos (`D5.2`, a metade PROIBIDA da regra §5.11). Teste `D5.2 — REAL FIXTURE: trilho de vigencia caps at ONE native bucket...` MORREU (`'held' !== 'absent'`). Revertido, `git diff --stat` limpo, suíte voltou a 79/79 verde.
- Mutação independente 2 — troquei `closeTimeMs` para devolver o bucket-start cru em vez do FECHO (`D5.1`, o defeito nomeado literalmente pelo plano). 3 testes MORRERAM: `D5.1 — REAL FIXTURE...` (`'00:00:00Z' !== '00:05:00Z'`), `closeTimeMs is bucket-start + timeframe...` (`0 !== 300000`), e o `held` do `D5.2`. Revertido, suíte voltou a verde.
- Mutação independente 3 — reintroduzi `LOCF` em `resolveFlowReading` (olhar 1 bucket para trás quando o slot exato é nulo), a proibição explícita de `D5.3`/§5.11 ("`LOCF`, sempre" sob "proibido"). Teste `D5.3 — a present FLOW bucket...` MORREU (`'present' !== 'absent'`). Revertido, suíte voltou a verde.
- [OK] Cobertura — sem piso numérico declarado para `charts` (`harness policy --key coverage` → `campo ausente na politica`; `harness policy --key test_cmd` só declara `sentimento`). Medida qualitativa citável no `frontend/README.md` §13 (10 testes cobrindo as 6 funções exportadas, os 3 ramos de `StockReading` e os 2 de `FlowReading`) — coerente com o silêncio já registrado nas outras seções do mesmo README para `charts`/`web`.
- [OK] DoD `D5.1` (SPEC `05_fatia_visivel.md:29`) — carimbo é o FECHO na tela: `formatCloseStamp`/`closeTimeMs`, provado no fixture real (`00:05:00Z` para o primeiro ponto de `08-23`, teste `s2-absence-policy.test.ts:59`).
- [OK] DoD `D5.2` (`05_fatia_visivel.md:30`) — ausência de OI não lida como leitura da barra: `resolveStockReading`, tinta secundária via `kind: "held"`, texto `"de HH:MM:SSZ (−Xm)"` (`formatHeldStockLabel`), linha-guia `{fromMs, toMs}` presente só em `held`, teto de UM bucket nativo provado contra o dia-buraco real (`2026-08-22`).
- [OK] DoD `D5.3` (`05_fatia_visivel.md:31`) — `FLOW` ausente é `—`: `resolveFlowReading`/`formatFlowValue`, provado contra o mesmo dia-buraco real e contra zero medido (nunca confundido com ausência).
- [Nota de escopo, não achado] `§5.11` lista 6 `nature`s; este módulo resolve só `STOCK`/`FLOW` — as únicas com painel real em `T-05.2` (item 5.1 do plano). `RATIO`/`EVENT`/quarentena não têm consumidor nesta fase (`T-05.5`..`T-05.7` ainda `todo`); declarado no cabeçalho do arquivo e no README §13, consistente com a proibição de "amplia escopo" do protocolo de despacho. Módulo é puro (zero `node:fs`, zero leitura de relógio) e ainda não tem consumidor de UI wired — coerente com `T-05.3` (selo/crosshair visível) permanecer `todo`; nenhuma outra task da fase reivindica `D5.1`/`D5.2`/`D5.3` (`grep -n "D5\.1\|D5\.2\|D5\.3" tasks.toml` → só `T-05.4`).
- [OK] `harness tasks validate plataforma-dados` → `OK: 85 task(s), 0 ERROR, 4 WARN` (os 4 avisos são de outras tasks — `blocked_reason`, chave desconhecida — pré-existentes, não relacionados a `T-05.4`).

Regras bloqueantes avaliadas: 7 de 7 listadas por `harness rules list --severity block`.

Veredito: **APPROVED**

