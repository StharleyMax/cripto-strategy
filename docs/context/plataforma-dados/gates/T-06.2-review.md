[REVIEW GATE — Fase 06: Tabela de shift POR ENDPOINT: dump = REST −5 min]
Feature: `plataforma-dados`     Componente: `sentimento`     Task: `T-06.2` (`CST-46`)
PR: https://github.com/StharleyMax/cripto-strategy/pull/101 (branch `tasks/T-06.2-fase06`)
Worktree: `/tmp/claude-1002/wt/T-06.2`

## Escopo revisado

`git diff master...HEAD --name-only` (three-dot, contra o merge-base — `master` avançou via
PR #99/T-06.5 desde a criação da branch):
- `backend/src/modules/sentimento/domain/endpoint_shift_table.py` (novo)
- `backend/tests/sentimento/test_endpoint_shift_table.py` (novo)
- `backend/README.md` (append)
- `docs/INDEX.md` (append)
- `docs/context/plataforma-dados/gates/T-06.2-builder.md`

Lidos por inteiro: handoff `T-06.2.md`, gate QA `T-06.2-qa.md` (veredito APPROVED), o módulo de
produção completo (178 linhas), o teste completo (262 linhas).

## Camada 1 — o que o runner mede

`harness rules list --severity block` → **7 regras bloqueantes em vigor** (`core`×4,
`web-fullstack`×3). Avaliadas as 7 sobre os 4 arquivos do diff, individualmente
(`harness rules --mode file --path <arquivo>`) e por sweep completo (`harness rules --mode
sweep`, não só `--changed-only`, porque `--changed-only` mede diff **não commitado** — nesta
branch tudo já está commitado, então `--changed-only` sempre devolve vazio aqui e o `rc=0` do
QA não prova ausência de achado no diff da task; é o `--mode sweep` completo, filtrado ao
arquivo, que prova):

- `core.relative-import`, `core.silent-except`, `core.print-statement`,
  `core.hardcoded-secret`: **sem achado** nos 2 arquivos novos — confirmado por grep direto e
  por `harness rules --mode file`.
- `web-fullstack.*` (3 regras): não aplicável — mudança é só `backend` Python, domain puro.
- **1 achado real, mas severidade `[AVISO]` (não `[BLOQUEIO]`)**: `core.module-docstring-single-line`
  em `endpoint_shift_table.py:1` — o docstring do módulo abre e fecha em várias linhas (é o
  padrão de docstring-longo-com-contexto usado em quase todo o `domain/` deste componente:
  o mesmo sweep completo, sem filtro de arquivo, mostra o MESMO padrão em outros **11**
  arquivos pré-existentes de `sentimento/domain`+`use_cases`+`infra`, então não é regressão
  introduzida por esta task, é convenção já estabelecida no componente). **Não muda o
  veredito** — regra é `warn`, não `block`, confirmado em `harness rules list` (sem filtro):
  `[AVISO   ] core.module-docstring-single-line`.

Denominador da camada 1: **7 regras bloqueantes existiam, 7 foram avaliadas, 4 arquivos do
diff varridos** com `harness rules --mode file --path <arquivo>` + `harness rules --mode
sweep` (completo). **0 violação bloqueante.**

## Camada 2 — arquitetura declarada (julgamento, com citação)

**Contrato `Natureza` (`ADR-016`) — módulo em `domain`, sem I/O:**
`endpoint_shift_table.py:63-67` importa só `collections.abc.Sequence`, `decimal.Decimal`,
`typing.Final` — confirmado por leitura direta do arquivo (não só relato do QA).
`bash backend/scripts/natureza.sh` reproduzido por mim: `71 arquivo(s), 0 leitura(s) de
relogio`. `bash backend/scripts/boundaries.sh` reproduzido por mim: `3 kept, 0 broken` (156
arquivos, 707 dependências), incluindo o contrato "domain e use_cases nao falam com socket nem
ssl". A leitura de arquivo real (fixture) só existe no teste
(`test_endpoint_shift_table.py:45-53`, comentário explícito: "this repository has no runtime
need to re-read a historical REST snapshot, only this test does") — em conformidade com
`ADR-016`.

**Não confundir com o shift canônico de `T-04.1`:** o módulo distingue explicitamente as duas
superfícies no próprio docstring (`endpoint_shift_table.py:7-16`): `metrics_shift.LABEL_SHIFT_MS`
(`event_time = create_time + 300_000`, oito colunas do stream) é citado por nome como "a
DIFFERENT shift on a DIFFERENT axis", nunca reimportado nem redefinido.
`git diff master...HEAD --stat -- backend/src/modules/sentimento/domain/metrics_shift.py
backend/src/modules/sentimento/domain/series_key.py
backend/src/modules/sentimento/domain/series_catalog.py` → **vazio, os três arquivos não
foram tocados por esta branch**. Confirma que `T-06.2` não reabriu nem duplicou o shift
canônico de `T-04.1`, nem os contratos de `T-06.1`.

**Exceção do `takerlongshortRatio` é POR ENDPOINT, não condicional espalhada:**
`ENDPOINT_LABEL_SHIFT_MS: Final[dict[str, int]]` (linha 74-80) é um único dicionário
chave→valor; `label_shift_for_endpoint` faz lookup direto (`ENDPOINT_LABEL_SHIFT_MS[endpoint]`,
sem `.get(..., default)`) e relança `UnknownEndpointShiftError` para qualquer nome fora da
tabela. Nenhum `if endpoint == "takerlongshortRatio"` em lugar nenhum do módulo — confirmado
por leitura completa (178 linhas) e por `grep -n 'if endpoint' backend/src/modules/sentimento/
domain/endpoint_shift_table.py` → sem achado. `test_an_unmeasured_endpoint_is_refused_not_
defaulted` e `test_the_table_has_exactly_the_five_measured_endpoints` fecham o falsificador
dessa forma (um 6º endpoint nunca herda nenhum dos dois valores).

**Idioma inglês em produção e teste, inclusive mensagem de exceção:** as 2 classes
(`UnknownEndpointShiftError`, `MismatchedSeriesLengthError`) e as 2 chamadas `raise` (linhas
107-111, 167-169, 172-173) têm mensagem em inglês — em conformidade com a decisão de
2026-09-02 (`CLAUDE.md` §"Mensagem de exceção"). Os únicos trechos em português no arquivo são
**citações literais entre aspas** de `SPEC-001`/handoff/`docs/recorte-plataforma.md` (linhas
56-57, 70, 136 do módulo; 6, 49 do teste) — não são identificador, comentário original nem
mensagem de exceção; são a mesma prática de citar fonte em português dentro de docstring em
inglês já usada alhures no repositório. Não fere a linha 1/2/5 da tabela de fronteira.

**Append-only:** `git diff master...HEAD -- docs/INDEX.md backend/README.md | grep -c
'^-[^-]'` → **0** linhas removidas em nenhum dos dois arquivos.

## DoD / falsificador (reproduzido, não só relatado)

- `bash backend/scripts/natureza.sh`, `bash backend/scripts/boundaries.sh` rodados por mim
  nesta sessão de review, mesmo resultado do QA (71 arquivos/0 relógio; 3 kept/0 broken).
- `.venv/bin/python -m pytest tests/sentimento/test_endpoint_shift_table.py --no-cov -q` →
  **16 passed**, reproduzido por mim.
- Não rodei de novo o falsificador central (288/288, MAE=0) nem a mutação plantada — o QA já
  os reproduziu de forma independente com dado real e resultado idêntico ao builder
  (`T-06.2-qa.md`), e minha auditoria é de arquitetura declarada, não de re-medição do mesmo
  número um terceira vez.

## Achados

- `[WARNING]` `core.module-docstring-single-line` — docstring de módulo aberto/fechado em
  várias linhas — `backend/src/modules/sentimento/domain/endpoint_shift_table.py:1` — regra
  `core.module-docstring-single-line` (severidade `warn`, não bloqueante). Padrão idêntico já
  presente em 11 outros arquivos do componente `sentimento` antes desta task; não é regressão
  introduzida por `T-06.2`, mas o achado é real e citável. Correção concreta, se o time decidir
  pagá-lo: comprimir a linha de abertura do docstring para uma linha (`"""One-line summary.` na
  própria linha 1, corpo depois), que é o formato que a regra exige.

Nenhum achado `[BLOCKER]`.

## Denominador

7 regras bloqueantes existiam (`harness rules list --severity block`), 7 avaliadas, 4 arquivos
do diff varridos (`git diff master...HEAD --name-only`) com `harness rules --mode file` +
`harness rules --mode sweep` completo (não `--changed-only`, que mede diff não commitado e
sempre devolve vazio nesta branch já commitada — usado pelo QA, mas não é a medida certa para
"achados no diff desta task" numa branch sem staged changes).

## Veredito

**COMPLIANT** — 0 de 7 regras bloqueantes violadas. 1 achado WARNING não-bloqueante, pré-existente
como padrão no componente, registrado acima para o ledger.
