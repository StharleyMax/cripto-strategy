# REVIEW GATE — Fase 06: T-06.8 · Campo aditivo → quarentena+alarme; ausente/renomeado → reprova

Feature: plataforma-dados   Componente: sentimento   PR: https://github.com/StharleyMax/cripto-strategy/pull/106
Branch: `tasks/T-06.8-fase06`   Worktree: `/tmp/claude-1002/wt/T-06.8`

## Denominador

- `harness rules list --severity block` → **7 regras bloqueantes** em vigor (core: relative-import,
  silent-except, print-statement, hardcoded-secret; web-fullstack: browser-imports-server,
  tenant-from-request, server-test-directory-present).
- Universo do diff: `git diff master...HEAD --stat` (three-dot, `master` local == `origin/master` ==
  merge-base `0ff5339`, confirmado por `git fetch` + `git log -1`) → **7 arquivos, 517 inserções, 0
  deleção**: `schema_change.py` (novo), `classify_schema_change.py` (novo), 2 arquivos de teste
  (novos), `backend/README.md` (+67), `docs/INDEX.md` (+1), `gates/T-06.8-builder.md` (novo).
- Rodei `harness rules --mode sweep --path <arquivo>` nos **4 arquivos de código/teste** do escopo,
  individualmente → **0 achados** nos 4. Nenhuma das 7 regras bloqueantes se aplica.
- Avaliei as 7 regras contra os 4 arquivos: 7/7 avaliadas, 0/7 violadas.

## Achados de arquitetura declarada (camada de julgamento, cada um citando documento)

Nenhum achado BLOCKER nem WARNING. Um INFO.

**[INFO] Nenhuma ADR nova para uma decisão que já cabia inteiramente em `SPEC-001` §5.5** —
`backend/README.md` (linha "ADR: não necessário") declara isso explicitamente; conferido: a task
não introduz regra de arquitetura nova, só implementa uma já especificada. Não é violação, é
verificação de que a ausência de ADR está correta e foi justificada, não omitida.

## Verificação ponto a ponto do pedido

**1. Contrato `Natureza` (`ADR-016`) — divisão de camada `domain`/`use_cases`.**
`backend/src/modules/sentimento/domain/schema_change.py` importa só `dataclasses` e
`provenance.Absence` — nenhum `logging`, nenhum I/O, nenhuma leitura de relógio. A decisão inteira
(dois testes independentes, "rejeição vence") vive em `classify_schema_change()` e nas properties de
`SchemaChangeVerdict`, ambas em `domain/`.
`backend/src/modules/sentimento/use_cases/classify_schema_change.py::classify_and_alarm` **chama**
`classify_schema_change` e só adiciona a decisão de **quando** logar (`if verdict.should_alarm`) —
não reimplementa o predicado nem recalcula `unknown_fields`/`missing_fields`. `should_alarm` é
property do domain; o use_case só a lê. `bash backend/scripts/natureza.sh` → `80 arquivo(s), 0
leitura(s) de relogio` (confirmado independentemente por QA); `boundaries.sh` → `3 kept, 0 broken`,
contrato `pyproject.toml:277` = `"Natureza: domain e use_cases nao falam com socket nem ssl"`
(`ADR-016` linha 90) — nome e universo batem com a ADR vigente. **Divisão correta.**

**2. `Absence.QUARANTINE` reusado, sem enum paralelo.**
`schema_change.py:34` importa `from src.modules.sentimento.domain.provenance import Absence`;
`SchemaChangeVerdict.absence` (linha 71-79) retorna `Absence.QUARANTINE if self.is_additive else
None`. `grep -c "class.*Enum"` nos 2 arquivos novos = 0. **Confirmado, nenhum enum paralelo.**

**3. Caso real do `nq` (`ADR-001`) reproduzido fielmente.**
`docs/adr/ADR-001-quantity-field-na-identidade.md:15-16`: "campos REST `['T','a','f','l','m','nq','p','q']`
— oito" / "colunas do dump — sete, sem `nq`". `test_schema_change.py:21,25`:
`AGGTRADE_DUMP_CONTRACT = frozenset({"T","a","f","l","m","p","q"})` (7) e
`AGGTRADE_REST_WITH_NQ = frozenset({"T","a","f","l","m","nq","p","q"})` (8). **Idêntico ao medido na
ADR, campo a campo — não é fixture sintética.**

**4. Idioma inglês em produção e teste.**
Leitura integral dos 4 arquivos: identificadores, docstrings, comentários e mensagens de exceção em
inglês. As únicas strings em português são citações literais entre aspas do texto de `SPEC-001` §5.5
dentro de um comentário (`schema_change.py:12-13`, ex.: `"campo ADITIVO desconhecido"`) — citação de
fonte (tabela de fronteira, linha 7: docs em português são citáveis), não identificador nem mensagem
nova. `raise SchemaChangeRejectedError(...)` (linhas 109-113) é 100% inglês — em conformidade com o
fechamento da lacuna de 2026-09-02 (`raise`/`Error`/`Exception` nasce em inglês desde essa data).
Nenhuma ocorrência de português em `raise`/`Error(`/`Exception(` nos 2 arquivos de produção
(`grep -c` = 0). **Conforme.**

**5. `docs/INDEX.md`/`backend/README.md` append-only.**
`git diff master...HEAD -- docs/INDEX.md` mostra só `+1` linha ao final do arquivo, nenhuma linha
existente tocada (diff sem `-`). `git diff master...HEAD -- backend/README.md` mostra só `+67`
linhas ao final (`## 📎 2026-09-03 por T-06.8`), a seção anterior (`T-05.10`/outra) intocada — sem
`-` no diff. **Append-only respeitado nos dois arquivos.**

**6. Escopo não fechado — nomeado, não escondido.**
Três lacunas nomeadas explicitamente em 3 lugares (docstring de `classify_schema_change.py:8-13`,
`backend/README.md` seção "Escopo que esta task NÃO fecha" e `gates/T-06.8-builder.md` seção
"Escopo NÃO fechado"): (a) nenhum canal de alarme real fora do log — dono é `T-07.11`, `blocked` em
`Q3`; (b) nenhum caller de produção liga `classify_and_alarm` a um coletor real; (c) `expected_fields`
não deriva de nenhum schema declarado, chega como argumento do caller. As três são consistentes com
o handoff (`docs/context/plataforma-dados/handoff/T-06.8.md:22`, que já proibia construir canal
novo) e não aparecem só em um documento isolado — estão no código, no README e no gate do builder.
**Nomeado, não escondido.**

## QA já rodou mutação real (não repetida aqui, só verificada)

`docs/context/plataforma-dados/gates/T-06.8-qa.md`: 2 mutações reais (inversão completa de lógica →
6/9 falham; inversão de ordem "aditivo antes de ausente" → 2/9 falham, exatamente o caso combinado
renomeado) — ambas revertidas e confirmadas limpas (`git diff --stat` vazio). Consistente com o que
este review leu no código: a ordem "missing primeiro" em `classify_schema_change` (linhas 107-113
antes do `return` de verdict) é a mesma que a mutação 2 do QA quebrou.

## Comandos rodados nesta auditoria

`harness rules list --severity block`; `harness policy --key {docs.bootstrap_docs,glossary_doc,
code_paths}`; `git fetch origin`; `git branch --show-current`; `git merge-base HEAD origin/master`;
`git diff master...HEAD --stat`; `harness rules --mode sweep --path <arquivo>` × 4; leitura integral
de `schema_change.py`, `classify_schema_change.py`, `test_schema_change.py`,
`test_classify_schema_change.py`, `provenance.py` (grep), `ADR-016` (grep), `ADR-001` (grep),
`docs/context/plataforma-dados/handoff/T-06.8.md`, `gates/T-06.8-qa.md`, `gates/T-06.8-builder.md`.

## Veredito

**COMPLIANT** — 7/7 regras bloqueantes avaliadas contra os 4 arquivos de escopo, 0 violadas; 4/4
arquivos varridos com `harness rules --mode sweep --path`, 0 achados. Camada de julgamento (divisão
`domain`/`use_cases` sob `ADR-016`, reuso de `Absence`, fidelidade ao caso `nq` de `ADR-001`, idioma,
append-only, escopo nomeado): 6/6 pontos verificados, 0 violações citáveis, 1 INFO não-bloqueante.
