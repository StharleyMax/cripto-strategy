# QA Gate (Front) — `T-05.11` / `CST-102` — Scaffold Next real sob `frontend/src/app`, `tsconfig.json` estrito, `tsc --noEmit --strict` em `make lint-frontend`

**Papel:** `frontend-qa` (porte de `frontend_qa.agent.md`) · **Data:** 2026-09-04
**Ancoragem:** worktree `/tmp/claude-1002/wt/T-05.11`, branch `tasks/T-05.11-scaffold-next`,
`master@758e2ef`. Nenhum commit feito nesta sessão; toda mutação plantada foi revertida em disco
antes de terminar (`git status --short` no fim = idêntico ao do builder).

Fontes: `docs/adr/ADR-018-...md`, `docs/context/plataforma-dados/gates/T-05.11-architect.md`,
`docs/context/plataforma-dados/tasks.toml` (`T-05.11`, DoD `D5.16`/`D5.16b`), `frontend/README.md`
§20 e `docs/INDEX.md` (as duas entradas novas — `architect` + `/build` — funcionaram como o QA Gate
Context Block, já que não existe `T-05.11-builder.md` separado).

## O que rodei, ao vivo, nesta sessão

1. **`make lint-frontend` sobre a árvore como o builder a deixou** → `rc=0` (`eslint` + `tsc
   --noEmit --strict`), silencioso.
2. **Erro de tipo plantado em `painel/page.tsx`** (`const qaPlantedTypeError: number =
   filterText;`) → `make lint-frontend` nomeia **`src/app/painel/page.tsx(38,9): error TS2322`**,
   `rc≠0`. Removido → `rc=0` de novo. Independente do erro que o `architect`/`/build` já tinham
   testado (`useState<string>(1)`), mesmo resultado.
3. **`npm run build` + `npm run start -p <porta>` + `curl`** — os 3 marcadores (`"Filtro: any
   resultado serve"`, `"Monitoramento de Coletores e Ingestão"`, `"Catálogo de Séries"`) presentes,
   contagem 1 cada. **MORDE (i)**: `<S3Inspector .../>` comentado → `next build` continua
   `✓ Compiled successfully`, mas o marcador `"Catálogo de Séries"` cai a **0** (os outros dois
   seguem em 1) — compilar ≠ renderizar, provado de novo, independentemente do log do `architect`.
   **MORDE (ii)**: processo `next start` morto → `curl --max-time 2` → `http_code=000`, `rc=7`
   (conexão recusada), não payload vazio.
4. **`frontend/app/` (sem `src/`) confirmado ausente**: `ls frontend/app` → "arquivo ou diretório
   inexistente"; `find frontend -maxdepth 2 -type d` só mostra `frontend/src/{app,features,charts,
   components}` — a restrição dura da task não foi violada.
5. **As 3 falhas pré-existentes** (`src/app/universe-at.test.ts`, `src/charts/s2-panels.test.ts`,
   `src/features/s1-console/ingest-health-query.test.ts`) reproduzidas **idênticas** com `git stash
   -u` (árvore sem nenhuma mudança de `T-05.11`, `node_modules/` preservado por ser gitignored): as
   duas primeiras falham por `ENOENT` em `data/` (dado bruto não versionado, `CLAUDE.md`), a terceira
   por `AssertionError` num `withTmpDir`/subprocesso — **as 3 causas são idênticas com e sem o
   scaffold**, confirmando "pré-existente, não introduzida".

## Achado novo — não estava no gate do `architect` nem no de `/build`

**`make verify` (o comando que `CLAUDE.md` chama de "Verificação é `make verify`") NÃO exercita o
`tsc --noEmit --strict` que esta task cabeou.** `scripts/verify.sh:79` chama `npm --prefix frontend
run lint` **diretamente**, não `make lint-frontend` — é uma reimplementação paralela do alvo do
Makefile, e só uma das duas cópias foi atualizada por `ADR-018`/`T-05.11`.

Provado ao vivo: com o mesmo erro de tipo plantado do item 2 acima,

```
$ npm --prefix frontend run lint        # o que scripts/verify.sh:79 chama
rc=0
$ npm --prefix frontend run typecheck   # o que make lint-frontend TAMBÉM chama, verify.sh não
src/app/painel/page.tsx(53,7): error TS2322: Type 'string' is not assignable to type 'number'.
rc=2
$ bash scripts/verify.sh
[OK       ] lint-frontend   rc=0  ESLint do projeto sobre frontend/src
```

`make verify` reporta `lint-frontend` **`[OK] rc=0`** com o erro de tipo presente. `ADR-018`'s
próprio falsificador geral só validou `npm run typecheck` direto e `git push --dry-run` (via
`pre-push.pre-harness`, que roda `make lint` → `make lint-frontend`, a via que FUNCIONA) — nunca
`make verify`, que é uma terceira via com uma terceira definição de "lint-frontend". O portão de
`D5.16` protege `make lint`/pre-push, mas **não protege `make verify`**, e são dois caminhos que o
próprio `Makefile` deixa divergir (`lint: lint-agents lint-corpus lint-backend lint-frontend` usa o
alvo real; `scripts/verify.sh` reimplementa em vez de chamar `make lint-frontend`). Removido o
plantado, o quadro volta a `[OK] rc=0` legitimamente — não é falso positivo, é a mesma lacuna dos
outros 5 portões de `verify.sh` que retornaram `rc=3` (backend não medido neste ambiente, fora de
escopo desta task).

**Achado B (não-bloqueante, registrado por completude):** `harness rules --mode sweep
--changed-only` e `--mode file --path frontend/src/app/painel/page.tsx` devolvem `rc=0` com saída
vazia mesmo com uma credencial literal plantada (`AWS_SECRET_ACCESS_KEY = "AKIA...`) no arquivo —
porque `core.hardcoded-secret` declara `paths = ["**/*.py"]` (`packs/core/rules.toml:50`) e
`web-fullstack.hardcoded-url` só casa URL literal, não credencial. **Nenhuma das 11 regras em vigor
alcança segredo em `frontend/src/**`.** Não é defeito desta task (gap estrutural pré-existente do
pack), mas significa que o item "Sem segredo no cliente" do checklist abaixo foi verificado por
`grep` manual (0 ocorrências nos arquivos novos), não pelo `harness rules` — `rc=0`/vazio aqui não é
prova, é a mesma ambiguidade que `CLAUDE.md` nomeia.

## QA Gate (Front) — Fase 05: `T-05.11`

- [OK] DoD `D5.16` (par morde/cala do `tsc`) — `make lint-frontend` nomeia arquivo+linha com erro
  plantado, `rc=0` limpo; verificado 2x com erros diferentes do que o `architect`/`/build` usaram
- [OK] DoD `D5.16b` (renderiza, não só compila) — 3 marcadores presentes; MORDE (i)/(ii) reproduzidos
  ao vivo, independentes do log do `architect`
- [OK] Restrição dura `frontend/app/` (sem `src/`) — confirmado ausente
- [OK] Lógica fora do componente — `painel/page.tsx` só compõe `.tsx` já testados via
  fixture/view-model publicados; nenhuma lógica nova
- [N/A] Contrato tipado na borda de rede — fora do escopo (`T-05.12`+ traz o transporte real)
- [OK] Sem segredo no cliente — `grep` manual sobre os arquivos novos, 0 ocorrências (ver Achado B:
  `harness rules` não cobre esta superfície, não use o `rc=0` dele como prova)
- [N/A] Acessibilidade nos interativos — nenhum elemento interativo NOVO nesta task (`input`/`button`
  já existiam em `S3Inspector.tsx`, não tocado aqui; fora do diff)
- [OK] Testes existem, passam — 34 arquivos de teste pré-existentes intactos; os 3 que falham são
  pré-existentes, confirmado por `git stash -u` (mesma causa, com e sem o scaffold). `D5.16`/`D5.16b`
  são prova manual declarada (sem Vitest/Playwright ainda, `ADR-018` §`D5.16b` explícito)
- [N/A] Cobertura — task não traz suíte automatizada nova; alvo declarado é "abrir caminho", não medir %
- [FAIL] `make verify` verde para o que `D5.16` promete — `lint-frontend` reporta `[OK] rc=0` mesmo
  com erro de tipo plantado, porque `scripts/verify.sh:79` chama `npm run lint` direto, não `make
  lint-frontend`/`npm run typecheck` (ver achado acima)
- [OK] Doc delta — `docs/INDEX.md` só com 2 linhas ACRESCENTADAS (confirmado: `git diff
  origin/master -- docs/INDEX.md` não mostra remoção); `frontend/README.md` §20 nova, consistente
- [OK] Rótulos de força — números do `architect`/`/build` carregam comando; os que reproduzi aqui
  também

## Achados

1. **[BLOCKER]** `scripts/verify.sh:79` chama `npm --prefix frontend run lint` em vez de `make
   lint-frontend` — `make verify` não exercita `tsc --noEmit --strict`, deixando o portão de `D5.16`
   ausente do comando que `CLAUDE.md` declara canônico ("Verificação é `make verify`"). Provado por
   mutação: erro de tipo plantado → `make verify` reporta `lint-frontend` `[OK] rc=0`.
2. **[WARNING]** Nenhuma das 11 regras de `harness rules` alcança `frontend/src/**` para segredo —
   `core.hardcoded-secret` é `paths=["**/*.py"]`. Não é defeito desta task, mas registre para não
   tratar `rc=0` do `harness rules` como prova de "sem segredo" em código de front daqui em diante.
3. **[WARNING]** `ADR-018` está `Status: RASCUNHO` — decisão estrutural ainda sem aprovação de owner
   registrada no ledger. Não bloqueia o merge do código (a implementação já reflete a decisão), mas
   o gate de aprovação da ADR é ato de owner, separado deste QA.

## Veredito: NEEDS_FIX

### Ações

1. Em `scripts/verify.sh`, trocar a chamada de `lint-frontend` (linha ~79) de `npm --prefix frontend
   run lint` para `make lint-frontend` (ou, equivalente, `npm --prefix frontend run lint && npm
   --prefix frontend run typecheck` nas duas linhas que o padrão do próprio Makefile usa) — sem essa
   troca, `D5.16` só protege o `pre-push`/`make lint` direto, não o comando que a doutrina do
   repositório chama de verificação canônica.
2. Depois do fix, repetir o morde/cala do item 1 (`useState`/atribuição inválida em
   `painel/page.tsx` → `make verify` tem de reportar `lint-frontend` `FALHA`/`rc≠0`; removido, `OK`).
3. Opcional, fora do escopo de bloqueio desta task, mas registrar como dívida: nenhuma regra de
   `harness rules` cobre segredo em `frontend/src/**` — mesma classe de lacuna que `T-01.10` já
   fechou para `deploy/*.yml` via `[[rules.own]]`.

Relatório completo: `docs/context/plataforma-dados/gates/T-05.11-qa.md`

---

## Rodada 2 — reverificação por MUTAÇÃO do fix de `scripts/verify.sh`

**Data:** 2026-09-04. **Ancoragem:** mesmo worktree `/tmp/claude-1002/wt/T-05.11`, branch
`tasks/T-05.11-scaffold-next`. Correção do builder: `scripts/verify.sh` agora chama, em
`lint-frontend`, DOIS `portao()` sequenciais — `npm --prefix frontend run lint` e, só se `rc=0`,
`npm --prefix frontend run typecheck` — espelhando o alvo `lint-frontend` do `Makefile` em vez de
reimplementar 1/2 dele. Nenhum commit feito; árvore devolvida idêntica ao início.

### 1. Mutante novo, diferente dos 3 já usados (rodada 1 usou `useState<string>(1)` e duas
variações de `const x: number = filterText`)

Tentativa A — `filterText={42}` na prop de `<S3Inspector>` (tipo incompatível em JSX): **mascarou o
teste** — `filterText` local ficou sem outro uso e o ESLint (`@typescript-eslint/no-unused-vars`)
reprovou ANTES do `tsc` rodar (o script só chama `typecheck` `if RC_LF -eq 0`), então a rodada não
provava que o `tsc` funciona, só que o ESLint funciona (já provado na rodada 1). Descartada.

Tentativa B, a usada — `onOpenSeries={(seriesId: number) => setOpenedSeriesId(seriesId)}`
(`painel/page.tsx:47`): ESLint limpo (`0 problems`), e SÓ o `tsc` reprova —
`src/app/painel/page.tsx(47,9): error TS2322` + `TS2345`, dois erros de incompatibilidade de tipo
de parâmetro/argumento. Classe de erro distinta das 3 anteriores (mismatch de assinatura de
callback via JSX, não atribuição direta de variável).

```
$ npm --prefix frontend run lint         # rc=0, "0 problems"
$ npm --prefix frontend run typecheck    # rc=2
src/app/painel/page.tsx(47,9): error TS2322: Type '(seriesId: number) => void' is not
  assignable to type '(seriesKeyId: string) => void'. ...
src/app/painel/page.tsx(47,63): error TS2345: Argument of type 'number' is not assignable
  to parameter of type 'SetStateAction<string | null>'.
$ bash scripts/verify.sh
[FALHA    ] lint-frontend   rc=1  ESLint + tsc --noEmit --strict do projeto sobre frontend/src
```

Confirmado no log bruto (`grep` sobre `/tmp/verify-T-05.11-20260904T114214Z.log`): a seção
`########## lint-frontend :: npm --prefix frontend run lint ##########` mostra `0 problems`; a
seção seguinte, `########## lint-frontend-typecheck :: npm --prefix frontend run typecheck
##########`, mostra os dois `error TS…` acima. **O `BLOCKER` da rodada 1 está corrigido**: agora é
o `tsc` — não só o ESLint — que reprova `make verify`.

### 2. Os outros 5 portões não quebraram com o fix

Com o mutante plantado, `bash scripts/verify.sh` reportou `lint-backend`/`test`/`boundaries`/
`regras`/`política` em `rc=3` ("NÃO MEDIU"). Confirmado por `git stash -u` (árvore igual a
`master@758e2ef`, zero mudança de `T-05.11`) que os MESMOS 5 portões já eram `rc=3` **sem nenhuma
mudança desta task** — `backend/.venv` ausente e mecanismo do harness "NAO RESOLVIVEL" neste
worktree (registro de plugin não alcança este diretório). Pré-existente, não introduzido pelo fix
de `verify.sh`; fora do escopo desta task (que é só `frontend/`).

### 3. Reversão e volta ao verde

`onOpenSeries={(seriesId: number) => setOpenedSeriesId(seriesId)}` → revertido para
`onOpenSeries={setOpenedSeriesId}`. `git diff --stat frontend/src/app/painel/page.tsx` vazio
(idêntico ao estado do builder). `bash scripts/verify.sh` de novo:

```
[OK       ] lint-frontend   rc=0  ESLint + tsc --noEmit --strict do projeto sobre frontend/src
```

`git status --short` no fim = idêntico ao do início da rodada (mesmos 8 modificados + 8
untracked do builder, nada a mais, nada a menos).

### 4. Reavaliação dos 2 `[WARNING]` da rodada 1

**Achado B (regras não alcançam segredo em `frontend/src/**`) — reconfirmado ao vivo, ainda
`WARNING`, não escalou.** Plantei `const QA_ROUND2_PLANTED_SECRET = "AKIAABCDEFGHIJKLMNOP";` em
`painel/page.tsx`, rodei `harness rules --mode sweep --changed-only` e `--mode file --path
frontend/src/app/painel/page.tsx`: ambos `rc=0`, saída vazia — não capturado. Confirmado na fonte
do pack (`.../harness-plugin/0.13.0/packs/core/rules.toml:50`): `core.hardcoded-secret` continua
`paths = ["**/*.py"]`, inalterado desde a rodada 1. Revertido o plantado. **Continua gap
estrutural pré-existente do pack, não desta task** — não bloqueia `T-05.11`, mas segue como dívida
a registrar (mesma classe que `T-01.10` fechou para `deploy/*.yml`).

**Achado 3 (`ADR-018` `RASCUNHO`) — reconfirmado, ainda `WARNING`, não escalou.**
`grep -n '^\*\*Status' docs/adr/ADR-018-*.md` → `**Status:** \`RASCUNHO\` (aprovar é gate do
owner)`, sem mudança desde a rodada 1. `harness pipeline show plataforma-dados | grep -i adr-018`
→ nenhuma linha: nenhum evento `approve` da ADR no ledger. Não bloqueia o merge do CÓDIGO (a
implementação já reflete a decisão e o portão de `D5.16` está provado); a aprovação da ADR
continua sendo ato de owner, separado deste QA — e nada nesta rodada indica que ela precise
bloquear `T-05.11` especificamente (não é uma decisão estrutural nova introduzida pelo fix desta
rodada).

## Veredito (rodada 2, final): APPROVED

O `BLOCKER` da rodada 1 (`make verify` não exercitava `tsc --noEmit --strict`) está corrigido e
provado por mutação nova, com par morde (erro plantado → `rc=1`, arquivo+linha nomeados)/cala
(revertido → `rc=0`). Os 5 outros portões de `verify.sh` seguem `rc=3` por motivo pré-existente e
fora de escopo (ambiente sem `backend/.venv` e sem registro do harness neste worktree — mesmo sem
nenhuma mudança de `T-05.11`, confirmado por `git stash -u`). Os 2 `WARNING`s seguem
não-bloqueantes, reconfirmados ao vivo nesta rodada, sem evidência de terem piorado.

Relatório completo: `docs/context/plataforma-dados/gates/T-05.11-qa.md`
