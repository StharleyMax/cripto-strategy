# `T-04.7` (`CST-34`) — relatório de gate do `/qa`

**Feature:** `plataforma-dados` · **Componente:** `sentimento` · **Fase:** `04`
**Base:** `master@840c500` · **Branch:** `tasks/T-04.7-principal-id-dimensao` · **Commit avaliado:** `9c43ed5`
**Worktree:** `/tmp/claude-1002/wt/T-04.7` · **PR:** #49

## Veredito: APPROVED

## 1. Testes existentes rodados antes de qualquer teste novo

```
cd backend && bash scripts/test.sh
# 452 passed in 7.27s · Total coverage: 99.29%
# domain 100.0% (meta 90%) [829/829] · use_cases 100.0% (meta 80%) [220/220] · infra 97.7% (meta 70%) [588/602]
bash scripts/lint.sh
# All checks passed! · 60 files already formatted
```
`[MEDIDO 2026-09-01]`, confere exatamente com o número do builder (era 444 em `840c500`, +8 novos).
Cobertura por camada acima do alvo declarado por `ADR-009/D1` nas 3 camadas medidas de 3 declaradas.

## 2. Mutação — prova de que a recusa é real, não default silencioso

4 mutantes aplicados em `backend/src/modules/sentimento/domain/provenance.py`, cada um revertido
(`git diff --stat` vazio ao final) antes do próximo, suite completa (452) confirmada limpa depois:

| # | mutante | resultado |
|---|---|---|
| A | `principal_id: str \| None = None` → `= "owner"` (default silencioso, exatamente o risco citado no despacho) | **morre** — 5 testes falham, incl. `test_a_non_human_row_does_not_require_principal_id[*]` e o falsificador do builder |
| B | corpo do `if provenance is HUMAN and blank: raise` substituído por `pass` (recusa removida) | **morre** — 3 falhas, incl. `test_a_human_row_with_no_principal_id_is_refused` |
| C | `not (self.principal_id or "").strip()` → `not (self.principal_id)` (aceita string em branco) | **morre** — `test_a_human_row_with_blank_principal_id_is_refused` falha |
| D | `provenance is Provenance.HUMAN` → `is not Provenance.HUMAN` (inverte quem é obrigado) | **morre** — 7 falhas, incl. os 3 casos parametrizados não-humanos |

`[MEDIDO 2026-09-01, executado nesta sessão]`. Mutante A é o que prova o requisito central do
despacho: ausência de `principal_id` é recusa (`InvalidSeriesRowError`), não um valor implícito —
se o default fosse silencioso, o próprio teste do builder (`test_a_human_row_with_no_principal_id_is_refused`)
e mais 4 outros já detectam. 4 de 4 mutantes mortos; nenhum sobrevivente.

## 3. `ADR-009/D2` — recusa a `organization_id`

```
grep -rn 'organization_id\|org_id\|tenant_id' backend/src backend/tests
# 3 ocorrências, todas em COMENTÁRIO explicando a recusa (provenance.py:45, README.md:674,689) — 0 em código executável
```
`[MEDIDO 2026-09-01]`. Nenhum campo, coluna, parâmetro ou constante `organization_id`/equivalente
foi introduzido por analogia com sistema multi-tenant.

## 4. Regras bloqueantes (`harness rules list --severity block`, 7 no total)

- `core.relative-import` — OK, 0 ocorrência no diff (`git diff 840c500 9c43ed5 -- backend/src backend/tests`)
- `core.silent-except` — OK, 0 `except` no diff
- `core.print-statement` — OK, 0 `print(` no diff
- `core.hardcoded-secret` — OK, 0 ocorrência
- `web-fullstack.browser-imports-server` — N/A, diff não toca frontend
- `web-fullstack.tenant-from-request` — OK, reforça item 3 acima
- `web-fullstack.server-test-directory-present` — OK, `backend/tests/` presente

## 5. `make verify` e a anomalia de plugin em worktree — confirmada, não contornada às cegas

```
make verify
# lint-backend rc=0 · lint-frontend rc=0 · test rc=0 · boundaries rc=0 (3 kept, 0 broken)
# regras rc=3 · política rc=3 → veredito INDETERMINADO (anomalia de mecanismo em worktree)
```
Rodado direto com o mecanismo do plugin, como o handoff prescreve:
```
export HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness
$HARNESS_MECHANISM rules --mode sweep --changed-only --format ndjson   # rc=0, 0 achados
$HARNESS_MECHANISM validate --strict                                  # "politica valida" rc=0
```
`[MEDIDO 2026-09-01]` — confirma a mesma anomalia e a mesma resolução já medidas pelo builder.

## 6. DoD (SPEC-001 §4.4, ADR-009/D2, plano 04 item 4.11) — item a item

- [x] `SPEC-001` §4.4 — "identidade é dimensão desde a primeira linha — nunca constante
      implícita, nunca `NULL`" e "`principal_id` é coluna em toda linha que registre ato
      humano" (`SPEC-001-plataforma-dados.md:370`) → implementado em `SeriesRow`/`Provenance.HUMAN`,
      recusa provada por mutação (§2 acima)
- [x] plano `04` item 4.11 / `tasks_review.md:290` — mesma leitura do escopo (fase `04` constrói a
      dimensão; `D5.10` fase `05` é quem consome via `<Anotacao>`) confirmada linha a linha
- [x] `ADR-009/D2` — recusa `organization_id`, confirmado por grep (§3)
- [x] Handoff (`docs/context/plataforma-dados/handoff/T-04.7.md`, existente só no branch
      `docs/gates-e-handoffs-fase-04`, lido via `git show 3c7da3d:...`): correto que esta task não
      cita `D4.x` — falsificador formal é `D5.10`/fase `05`, fora deste escopo. Não é DoD ausente
      por descuido.
- [x] Cobertura acima do alvo em 3/3 camadas declaradas (§1)
- [x] Nenhuma regra bloqueante violada (§4)

## Anomalia registrada, não bloqueante

O handoff da task não existe na branch `tasks/T-04.7-principal-id-dimensao` nem em `master` —
só na branch paralela `docs/gates-e-handoffs-fase-04` (commit `3c7da3d`, ainda não mesclada). Lido
via `git show` para esta avaliação; não impede o veredito porque o conteúdo é consistente com
`tasks.toml`, `tasks_review.md` e o relatório do builder — mas o merge dessa branch é pendência de
rastreabilidade, não desta task.

Regras bloqueantes avaliadas: 7 de 7.
