# QA Gate Context Block — `T-03.10` (fase 03, `/build`)

**Feature:** `plataforma-dados` · **Componente:** `sentimento` · **Task:** `T-03.10` / `CST-26`
**Branch:** `task/T-03.10-fila-etl-retomavel` · **Base:** `master@7c3599b` (+ merge de `origin/master@c7df90c`)
**Refs:** `CA-F0-5` · `SPEC-001` §5.8 · plano `03` itens 3.11+3.14 · DoD **`D3.1`** · `Q18` não bloqueia (§7/D-5)

## Arquivos alterados

| arquivo | estado |
|---|---|
| `backend/src/modules/sentimento/domain/dump_window.py` | new |
| `backend/src/modules/sentimento/domain/retention_probe.py` | new |
| `backend/src/modules/sentimento/infra/dump_ingest_worker.py` | new |
| `backend/src/modules/sentimento/infra/head_probe_log.py` | new |
| `backend/src/modules/sentimento/infra/dump_etl_cli.py` | new (**raiz de composição**) |
| `backend/src/modules/sentimento/domain/etl_backlog.py` | modified (`Counter`) |
| `backend/src/modules/sentimento/infra/jsonl_checkpoint.py` | modified (validação de forma) |
| `backend/tests/helpers/dump_queue_driver.py` | new |
| `backend/tests/sentimento/test_dump_window.py` | new |
| `backend/tests/sentimento/test_retention_probe.py` | new |
| `backend/tests/sentimento/test_dump_etl_queue_resumable.py` | new (**`D3.1`**) |
| `backend/tests/sentimento/test_dump_ingest_edge.py` | new |
| `backend/tests/sentimento/test_head_probe_log.py` | new |
| `backend/tests/sentimento/test_dump_etl_cli_surface.py` | new |
| `backend/tests/sentimento/test_verified_edge_call_sites.py` | new (**gatilho de AST virou teste**) |
| `backend/README.md` | modified — **+145/−0, puramente aditivo** |
| `docs/INDEX.md` | modified — **+1/−0, append-only** |

## DoD

- [x] **`D3.1`** — fila retomável não duplica nem perde, matando o processo no meio · universo **≥ 100 arquivos**
      → `bash backend/scripts/test.sh` · `test_killing_the_process_halfway_and_resuming_neither_duplicates_nor_loses`, **120 objetos**, `SIGKILL` real em subprocesso do **`dump_etl_cli.run`**.
- [x] item **3.11** — profundidade como parâmetro, default **30 d** (`Q18`(d)) → `test_the_declared_default_depth_is_thirty_days`, `test_extending_the_depth_drains_only_what_the_deeper_window_added`
- [x] item **3.14** — `curl -sI` mensal em `aggTrades` **e** `bookDepth`, prefixo antigo **e** recente → `domain/retention_probe.py`, `test_the_probe_targets_both_an_old_and_a_recent_prefix_for_both_datasets`
- [x] `bookDepth` **não tem prefixo `monthly`** (`SPEC-001` §5.8, `[MEDIDO, CST-5]`) → `test_book_depth_has_no_monthly_prefix_and_the_window_refuses_to_pretend_it_does`

## Comandos rodados (literais) e resultado

| comando | resultado | universo |
|---|---|---|
| `make lint` (`ruff check` + `ruff format --check` + `mypy --strict`) | **rc=0** — `All checks passed!` · `42 files already formatted` · `Success: no issues found` | **42 arquivos** |
| `make test` (`pytest --cov` + piso por camada) | **rc=0** — **183 passed** | 183 testes (**linha de base: 107**) |
| `make boundaries` (`import-linter`) | **rc=0** — **2 kept, 0 broken** · `Analyzed 22 files, 20 dependencies` | base: **17 arquivos, 7 dependências** |
| `harness rules --mode sweep` | **rc=0** — **0 BLOQUEIO**, 1 AVISO | AVISO é `browser-test-file-present` no `frontend/`, **pré-existente e fora do componente** |
| gatilho de AST do `README` (contagem de call sites de `lines()`) | **`1`, rc=0** `[CALA]` | `src/**/*.py` — a task **não** acrescentou call site |
| bancada de mutação (`python -B`, `PYTHONDONTWRITEBYTECODE=1`, `__pycache__` apagado) | **n=12 · 12 morderam · 0 sobreviveram · 0 inertes · 0 ambíguos** | âncora conferida antes; reversão por `sha256` |

**Cobertura: `domain` 100,0% · `use_cases` 100,0% · `infra` 100,0%** (alvos declarados 90/80/70;
linha de base 100/100/100 **mantida**) — `[MEDIDO: bash backend/scripts/test.sh, 244/244 · 52/52 · 382/382 linhas]`.

## As quatro dívidas herdadas — item a item

1. **Raiz de composição (achado `H`)** — **FECHADA.** `infra/dump_etl_cli.py`. O falsificador
   escrito no `README` tem duas metades e elas divergem: um **módulo novo FOI** necessário (logo o
   achado procede), mas a **direção NÃO se inverteu** — o achado supunha que `infra` orquestrar
   `use_cases` inverteria a camada, e `[tool.importlinter]` declara `layers = ["infra",
   "use_cases", "domain"]` com **a primeira como a mais alta**. `make boundaries` → `2 kept, 0
   broken`. Precedente seguido: `infra/ingest_health_cli.py`.
2. **`T-02.1`/`T-02.2` fora desta borda** — **FECHADA, e por TIPO em vez de por política.** A raiz
   só aceita um `DumpDataset` (vocabulário fechado de 2 membros); nenhuma fonte REST tem um, logo
   não há linha de política que alguém possa apagar. `ADR-014/D3a` está **`proposto`**, então o
   registro geral de testemunha por fonte **não** foi construído — construí só o roteamento que
   esta task de fato possui. Pinado por `test_a_rest_shaped_key_is_refused_and_the_refusal_is_the_missing_witness`.
3. **Os dois pontos cegos do gatilho de AST** — **FECHADOS, e o gatilho saiu do comentário.**
   Ele vivia digitado dentro de um comentário, e *ferramenta que ninguém roda não é portão*
   (`ADR-011:268`). Agora é `tests/sentimento/test_verified_edge_call_sites.py`, executado por
   `make test`. Medi as duas evasões, cada uma sozinha numa árvore isolada `[MEDIDO, python -B +
   PYTHONDONTWRITEBYTECODE=1, __pycache__ apagado]`: `pull = payload.lines` + `pull()` → **0
   `[CEGO]`**; `getattr(payload, "lines")()` → **0 `[CEGO]`**; `payload.lines()` → **1**. O
   scanner novo vê as três e **é falsificado pela própria suíte**. Continua invisível e está
   escrito: `getattr(payload, nome)()` com `nome` calculado em runtime — o mesmo limite que
   `[tool.importlinter]` já declara para si.
4. **`curl -sI` mensal** — **FECHADA na metade decidível offline.** `domain/retention_probe.py`
   enumera e classifica; a rede fica com o cron e entra por `infra/head_probe_log.py`. Implementa
   o achado **`A7`**: *o último período antes de um `404` é sistematicamente suspeito* — a sonda
   pega o `404` de maio e **não** pega o abril parcial, que é o mês que envenena a série.
   **A fila NÃO aceita um mês curto por o checksum ter batido:** o portão P2 (`ADR-014/D3b`)
   avisa e registra durável **antes** da drenagem, e **nunca recusa** — recusar as 6,781 h reais
   plantaria o survivorship que `SPEC-001` §5.6 existe para impedir.

**Dívidas extras que o próprio código nomeava como minhas, também fechadas:**
`EtlBacklog` O(n²) → `Counter` (**37.227,38 ms → 5,40 ms** em `n=43.660`, as duas formas na mesma
rodada, mínimo de 5 repetições, saída conferida idêntica); e a validação de forma do
`JsonlCheckpoint` — `{"key": null}` deixava de levantar e virava a chave `'None'` **em silêncio**
`[MEDIDO, n=4 payloads: os quatro agora caem em `CorruptedCheckpointError`]`.

## Doc delta

- `backend/README.md`: **atualizado** — nova seção `📦 A fila de ETL do dump…` + 4 ponteiros de
  fechamento nas seções existentes (achado `H`, os dois bullets de `T-02.4a`, o gatilho de AST).
  **+145/−0**: nada reescrito.
- `docs/INDEX.md`: **atualizado** — 1 linha acrescentada. **Append-only respeitado** (+1/−0), e o
  conflito com `origin/master` foi resolvido **mantendo as 3 linhas de master E a minha** (84 + 1 = 85).
- **ADR: não necessário.** A colocação da raiz de composição segue precedente existente
  (`ingest_health_cli.py`) e o contrato de camadas já declarado; a testemunha por fonte já tem
  foro aberto em `ADR-014`, que está **`proposto`** — e o gate de SPEC/ADR é do owner.

## ⚠️ Achado sobre o INSTRUMENTO do portão de regras

**`harness rules --mode sweep --changed-only` é CEGO a arquivo NÃO RASTREADO.** Medido plantando
`print(...)` (viola `core.print-statement`, severidade `block`) e variando **só** o estado do
arquivo no git `[MEDIDO, n=3 estados, mutante revertido e conferido por sha256]`:

| estado | comando | resultado |
|---|---|---|
| untracked, com o `print` | `--mode sweep --changed-only` | **0 achados, rc=0** — `[CEGO]` |
| untracked, com o `print` | `--mode sweep` (completo) | 1 achado, **rc=1** — `[MORDE]` |
| **staged** (`git add`), mesmo `print` | `--mode sweep --changed-only` | **rc=1**, nomeia `dump_etl_cli.py:165` |

Um builder que **cria arquivos** e roda `--changed-only` antes do `git add` recebe **verde falso**
— foi o que esta task recebeu na primeira execução, e só apareceu porque a bancada foi rodada
**contra o próprio portão**. Os `rc=0` desta seção são de **depois** do `git add`.
**NÃO consertado:** é o plugin `harness`, fora desta árvore e fora do escopo desta task.

## Bloqueado / não entregue (nomeado)

- **Não baixa nada.** O espelho em `<workdir>/mirror/` é alimentado por quem busca. **`T-07.1`**
  é dona do paginador e da listagem S3 por `NextContinuationToken`.
- **Ordem `T-07.1` → `T-03.10` (§7/D-7): NÃO muda o que foi entregue, e o argumento é este** — o
  que `T-07.1` exige é janela **fechada e enumerada A PRIORI, nunca por cursor**, e é exatamente
  o que `enumerate_window` faz (aritmética de data, nenhuma resposta decide a próxima unidade de
  trabalho). A costura é o `EtlBacklog`: quando `T-07.1` chegar, ela alimenta **a mesma** fila.
  **Se o `/architect` discordar, o ponto de discórdia é este parágrafo.**
- **Não escreve `md.ingest_run`/`md.ingest_gap`.** A casa durável do achado de classe O **é**
  `md.ingest_gap`; o escritor de produção chega com **`T-03.8`**. Até lá o achado é durável em
  `findings.jsonl` — segundo-melhor **escrito como tal**.
- **Não lê o conteúdo do objeto.** Cobertura contra os *timestamps* de dentro do CSV exige unzip +
  parse. Na resolução de `HEAD` a testemunha de classe O é a regra do vizinho. **O `n_missing`
  real continua `[NÃO MEDIDO]`**, e a razão de tamanho é **alarme e não contagem** (177,8× contra
  déficit real de 106,2×, `[MEDIDO, ADR-014/D3d]`).
- **Nenhum `verdict` foi escrito.** `verdict` é o conjunto fechado de `md.ingest_run`, a SPEC é
  dona (`ADR-014/D2a`), e **`ADR-014` está `proposto`**. O vocabulário de retenção é próprio.
- **Pergunta de domínio em aberto, para o `quant-architect`:** `ADR-014/D3c` deixa a linha do
  `!forceOrder@arr` como **`[NÃO SEI]`** (sem testemunha de integridade). Não inventei uma.

---

# Ciclo 2 — rodada de correção (`/qa` → `NEEDS_FIX`, `/review` → `COMPLIANT` com 6 WARNING)

**Veredito do `/qa`:** *"DoD cumprido; reprovam 4 invariantes que a task declara em docstring e não
sustenta na execução"*. **As quatro eram verdadeiras.** Os 4 `xfail(strict=True)` que o `/qa`
escreveu passaram a **XPASS pelo conserto** e viraram guardas de regressão simples.

| gate | ciclo 1 | ciclo 2 |
|---|---|---|
| `make test` | 183 passed | **197 passed**, 0 xfailed |
| cobertura | 100/100/100 | **100/100/100** (252/252 · 52/52 · 388/388) |
| `make lint` | rc=0 | **rc=0** (45 arquivos) |
| `make boundaries` | 2 kept, 0 broken | **2 kept, 0 broken** |
| `harness rules --mode sweep` | 0 BLOQUEIO | **0 BLOQUEIO** |
| bancada dos consertos | — | **n=7 · 7 morderam · 0 sobreviveram** |

## 1 [BLOQUEIA] · O `G1` acontecendo — **CORRIGIDO**

Duas causas, e cada metade estava certa sozinha; a **composição** é que errava.

* **Adjacência agora é de CALENDÁRIO.** `DumpPartition.successor()` nasce, e `classify` procura
  `probed.get(outcome.partition.successor())` em vez de `outcomes[index + 1]`. Um buraco no log
  de sonda deixa de fabricar vizinhança: o período cujo sucessor de calendário **não foi sondado**
  não é suspeito **e não é atestado como são** — a `reason` diz que a fronteira `A7` não pôde ser
  avaliada, *ausência de evidência, não evidência de saúde*.
* **O sucessor fora da janela passa a ser lido.** `assess_window` resolve
  `probe_targets_for_window(partitions)` = a janela **+ o sucessor do mais novo**. É a linha que
  fecha o caso que motivou `G1`: com a janela terminando em 2024-04, o `404` de 2024-05 estava no
  log e ninguém o consultava, e abril era gravado **`PRESENT`**.
* **[C] alcance nulo — corrigido por desenho.** As duas perguntas da sonda estavam confundidas, e
  não são a mesma: `probe_targets` responde **retenção do balde** (1 antigo + 1 recente);
  `probe_targets_for_window` responde **classe O da drenagem corrente**. A interseção 0 medida
  pelo `/qa` era a consequência de usar a primeira para a segunda.

## 2 [BLOQUEIA] · "barrado POR TIPO" era falso — **AFIRMAÇÃO CORRIGIDA + LINHA COBERTA**

`process(self, key: str)` recebe `str`, e nenhum portão estático morde uma fonte REST plantada
nessa borda. Corrigido **onde foi escrito**: docstring de `DumpIngestWorker` e `backend/README.md`
(erratum explícito). O que sobrevive e é verdade: **pela `run()` não há caminho**. A barreira real
é `DATASETS_BY_NAME`, e ela agora tem teste (mutante `C2-5` **morde**).

## 3 [BLOQUEIA] · O fluxo de produto — **CORRIGIDO, e as duas causas eram distintas**

* **`product = logging.getLogger("dump_etl_cli.keys")`**, deliberadamente **fora da hierarquia
  `src`**, carrega as chaves; `logger` volta a ser só diagnóstico. `dump_window_enumerated` sai do
  `stdout`.
* **`_APPLICATION_LOGGER` deixa de colapsar:** vem de `DumpIngestWorker.__module__.split(".")[0]`,
  que é `"src"` **como script ou importado**. E `route_diagnostics_…` passa a ajustar também o
  logger do próprio módulo — como script ele é `__main__`, fora de `src`, e cairia em
  `logging.lastResort`, cujo nível é WARNING: um diagnóstico INFO **sumiria em silêncio**.

**⚠️ E aqui a bancada pegou o meu próprio conserto pela metade.** Com as duas metades no lugar,
reverter só o `_APPLICATION_LOGGER` **não quebrava teste nenhum** (`C2-4` **SOBREVIVEU**) — a
segunda metade sozinha mantinha os fluxos disjuntos. Um conserto que nada afirma é um conserto
esperando ser desfeito. O risco real dele é o de `T-02.3`: um host que chama
`logging.basicConfig(stream=sys.stdout)` antes de invocar. Fechado por
`test_a_host_that_puts_the_root_logger_on_stdout_does_not_contaminate_the_product`, que roda a
raiz sob `runpy` com o root em `stdout`. `C2-4` **morde** agora.

## 4 · Os três mutantes vivos — **FECHADOS**

`M08` e `M25` pelos testes do `/qa`; `M32` pelo teste de fluxo. E
`test_the_sink_counts_exactly_what_the_edge_says_it_delivered` foi **renomeado** para
`test_the_published_object_is_byte_identical_to_the_verified_source`, que é o que o corpo dele de
fato verifica — *um teste cujo nome afirma o que ele não verifica é pior que ausência*. A
comparação que o nome antigo prometia existe, escrita pelo `/qa`.

## 5 · Os seis WARNING do `/review`

1. **`[PREMISSA-OWNER]` sobre texto que o owner não disse — CORRIGIDO, e o `/review` está certo.**
   Conferi: `docs/decisoes-do-owner.md:51` lista **`| Q18 | … | ABERTA | NÃO |`** e a seção da
   linha 435 é **`### Q18 · ABERTA`**. Os itens `(a)/(b)/(c)/(d)` são **campos do formulário do
   próprio registro**, preenchidos por quem o mantém; `(d) RELÓGIO` anota se a pergunta aberta tem
   relógio. **Não é o owner falando** — compare `Q16` (`RESPONDIDA`) e `Q17` (`RESPONDIDA COM
   RESÍDUO`): pergunta respondida diz isso no status. Rótulo trocado para
   **`[DOC: docs/decisoes-do-owner.md, Q18(d)]`**. **O desenho sobrevive intacto** (profundidade é
   parâmetro; o default 30 é declarado pela DECOMPOSIÇÃO, `tasks_review.md` §7/D-5). **O que não
   sobrevive é a autoridade que o rótulo alegava: `Q18` continua ABERTA.**
2. **Vizinho falso em `A7`** — corrigido pelo item 1 (adjacência de calendário).
3. **Janela predita ≠ balde observado** — **NÃO reparado, e a recusa é fundamentada.** Pular objeto
   ausente faria a fila seguir por cima de um buraco que ela não registra — a série curta e
   silenciosa que esta task existe para impedir; e mudar `drain` alteraria um contrato
   compartilhado com a outra fila, cujo `D3.1` se apoia nele. **O custo fica PINADO**
   (`test_a_hole_in_the_bucket_stops_the_drain_and_the_cost_is_named`: 0 de 3) e **o remédio
   também** (`test_probing_the_window_turns_that_hole_into_a_drain_that_finishes`) — e foi o
   ciclo 2 que tornou o remédio alcançável, porque `probe_targets_for_window` enumera exatamente
   a janela drenada.
4. **15 de 17 cabeçalhos em português — CORRIGIDO.** Os **14** cabeçalhos `# ──` dos 5 módulos
   novos estão em inglês, e a prosa deles junto. **Preservadas por decisão:** as **citações
   literais marcadas** (`*"…"*`) de `README`, `ADR-014` e `decisoes-do-owner.md` — mesma regra que
   `T-02.3` aplicou hoje de manhã.
5. **`numero` — CORRIGIDO.** O parâmetro novo em `JsonlCheckpoint._key_of` virou `number`. O
   `numero` das linhas 80/86/87 é do laço **pré-existente** de `entries()`, dívida da feature
   `codigo-em-ingles`, e não foi tocado.
6. **O gatilho cego de `ingest_verified_payload.py` — PONTEIRO ACRESCENTADO.** O bloco continua
   dizendo "OBSERVABLE REOPENING TRIGGER" porque é o que o registro de reabertura cita; abaixo
   dele entrou a medição das duas evasões (`0 [BLIND]` cada) e o ponteiro para o teste que roda a
   versão consertada.

## 6 · O enquadramento da cegueira do `--changed-only` — **CORRIGIDO**

**Não é achado novo.** Já está escrito em `harness.toml:470-472`: *"NÃO cito `--changed-only`: ele
é `git diff --name-only HEAD` (`lib/runner.py:88`) e portanto CEGO a arquivo novo não rastreado"*.
Apresentá-la como descoberta foi erro meu de leitura. **É dívida documentada**, e o que esta task
acrescenta é uma **forma nova**: não "task cujo universo é todo novo", mas **arquivo não rastreado
convivendo com rastreados** — o portão varre os rastreados, devolve `rc=0`, e isso **parece**
medição. Registrado assim no `backend/README.md`.

---

## 🚧 BLOQUEIO ABERTO (ciclo 2) — colisão de merge com o contrato 3 que `T-03.1` acrescentou hoje

**Não é regressão desta task, e não foi resolvido por mim de propósito: a resolução é decisão de
arquitetura, não de builder.**

`origin/master` passou a declarar um terceiro contrato de `import-linter`:

```toml
name = "Natureza: domain e use_cases nao falam com socket, ssl nem relogio"
source_modules = ["src.modules.sentimento.domain", "src.modules.sentimento.use_cases"]
forbidden_modules = ["socket", "ssl", "time", "datetime"]
```

Ele **não existia** na minha base (`7c3599b`) nem no meu HEAD pré-merge (`5523167`)
`[MEDIDO: `git show <ref>:backend/pyproject.toml | grep -c "Natureza:"` → **0** nos dois]`. Ao
mesclar `origin/master`:

```
Natureza: domain e use_cases nao falam com socket, ssl nem relogio BROKEN
  src.modules.sentimento.domain is not allowed to import datetime:
  -   src.modules.sentimento.domain.dump_window    -> datetime (l.6)
  -   src.modules.sentimento.domain.retention_probe -> datetime (l.6)
Contracts: 2 kept, 1 broken.
```

**O `pre-push` REFUSA, e está certo em refusar** `[MEDIDO: `git push --dry-run` → rc=1]`.

### O ponto exato da questão, e ele é de mérito

| | |
|---|---|
| **o que o contrato PROÍBE** | o **módulo** `datetime`, inteiro |
| **o que o nome do contrato DECLARA** | *"não falam com … **relógio**"* |
| **o que meu `domain` de fato usa** | `date` e `timedelta` — **tipos de valor**, sem autoridade ambiente |
| **o que meu `domain` NUNCA faz** | ler o relógio: `grep -rn "\.now()\|\.today()\|\.utcnow()\|time\.time()\|monotonic()" backend/src/modules/sentimento/domain/` → **nenhuma ocorrência** `[MEDIDO]` |

`dump_window.py` já dizia, antes deste contrato existir: *"This module is `domain`: no file, no
socket, no clock. `date` arrives as an argument precisely so that nothing here reads one."*
⇒ **o desenho honra a INTENÇÃO do contrato e viola a LETRA dele.** `date.today()` é o relógio;
`date` como tipo não é. **`import-linter` lê o grafo de imports e não consegue separar os dois** —
a distinção não é expressável no instrumento.

### As duas saídas, e por que NÃO escolhi nenhuma

1. **Estreitar o contrato** (tirar `datetime`, manter `socket`/`ssl`/`time`) e passar a guardar o
   relógio por um scanner de AST sobre as CHAMADAS (`.now()`, `.today()`, …) — o mesmo instrumento
   que esta task já usa para os call sites de `lines()`, e que **vê o que o grafo de imports não
   vê**. ⇒ Mexe num contrato que **`T-03.1` acrescentou hoje**, e alterar fronteira declarada não é
   ato de builder.
2. **Tirar `datetime` do meu `domain`**, com um `CalendarPeriod(year, month, day)` próprio e
   aritmética de calendário à mão. ⇒ Satisfaz a letra e **piora o desenho**: aritmética de
   calendário escrita à mão é exatamente onde mora o off-by-one, e esta task acabou de gastar um
   ciclo inteiro consertando um defeito de **adjacência de período**.

**Recomendação (1), com o argumento acima — mas a decisão é do `/architect`.** Enquanto ela não
vem, o remoto fica em `5523167`, que está **verde nos quatro portões**; o merge de `origin/master`
existe apenas localmente e **não é empurrável**. O conflito de `docs/INDEX.md` é trivial
(append-only, as duas metades convivem) e **não é o bloqueio**.
