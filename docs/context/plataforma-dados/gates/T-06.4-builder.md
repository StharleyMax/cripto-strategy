[QA GATE — Fase 06: `funding_settled` ≠ `funding_estimado`, séries distintas]
Feature: `plataforma-dados`     Componente: `sentimento`
Arquivos alterados:
- `backend/src/modules/sentimento/domain/funding_settlement.py` (new)
- `backend/tests/sentimento/test_funding_settlement.py` (new)
- `backend/tests/sentimento/test_funding_next_funding_time_grid.py` (new)
- `backend/tests/sentimento/test_as_of_is_the_single_reader.py` (modified — `DECLARED_TOUCHERS` +1 entrada, docstring corrigido por adendo)
- `backend/README.md` (modified — seção `## 📎 2026-09-03 por T-06.4` acrescentada)
- `docs/INDEX.md` (modified — linha acrescentada, append-only)
Spec: docs/specs/SPEC-001-plataforma-dados.md §3.4; docs/specs/PRD-001-plataforma-dados.md §5.6 (correção R1·D-10)
Plan: docs/plans/SPEC-001-plataforma-dados/06_semantica_declarada.md (item 6.4)
DoD:
- [x] `funding_settled`/`funding_estimado` nunca a mesma `SeriesKey` — `FundingSource.SETTLED`/`ESTIMATED`, provado por `test_funding_settled_and_funding_estimado_are_two_catalog_entries` (duas `SeriesCatalogEntry` coexistindo em UM `SeriesCatalog`)
- [x] `interval_hours_declared` por linha, nunca constante — `FundingRecord.interval_hours_declared` é campo de instância; `compute_settlement_slot` recebe o intervalo como argumento, nunca lê um valor global
- [x] PK inclui `source` — `FundingRecord.primary_key()` = `(instrument_id, settle_bucket, source, observed_at)`, literal de `PRD-001` §5.6
- [x] D6.11 (settlement_slot correto, por linha) — `test_d6_11_every_row_of_the_real_fixture_lands_in_a_small_non_negative_residual` + `test_d6_11_the_old_fixed_interval_formula_misplaces_most_rows_the_per_line_one_does_not`
- [x] D6.12 (dupla ingestão não duplica, sobrevive à transição 8h→1h→4h) — `test_d6_12_ingesting_the_fixture_twice_still_yields_321_rows` + `test_d6_12_dedupe_survives_the_8h_to_1h_to_4h_transition`
- [x] D6.16 (nextFundingTime na grade, com data do snapshot ao lado do número) — `test_d6_16_nextfundingtime_divides_evenly_by_its_own_interval_for_every_symbol` + `test_d6_16_4h_is_the_rule_not_8h_with_the_snapshot_date_attached`

Comandos rodados (literais) e resultado:
- `bash backend/scripts/test.sh` → `1296 passed, 3 warnings in 445.93s` (universo: suíte inteira do backend; era 1277 em `T-06.1`, +19 novos: 16 em `test_funding_settlement.py`, 3 em `test_funding_next_funding_time_grid.py`). Cobertura total **97,64%**; por camada (piso `ADR-009/D1`): domain **99,8%** (2558/2562, meta 90%) `[OK]`, use_cases **100,0%** (585/585, meta 80%) `[OK]`, infra **95,1%** (2208/2321, meta 70%) `[OK]`. `funding_settlement.py` isolado: **100%** (59/59 stmts, 16/16 branches) — `.venv/bin/python -m pytest tests/sentimento/test_funding_settlement.py tests/sentimento/test_funding_next_funding_time_grid.py tests/sentimento/test_as_of_is_the_single_reader.py --cov=src --cov-report=term-missing -q | grep funding_settlement`.
- `bash backend/scripts/lint.sh` → `ruff check`/`ruff format --check`/`mypy --strict` sem achado, 236 arquivos.
- `bash backend/scripts/boundaries.sh` → `Contracts: 3 kept, 0 broken` (156 arquivos, 709 dependências) — `funding_settlement.py` não importa `infra`/`use_cases`, contrato `Camadas por contexto` mantido.
- `bash backend/scripts/natureza.sh` → `universo de 71 arquivo(s), 0 leitura(s) de relogio` — `funding_settlement.py` não lê relógio (`ADR-016`/`Natureza`, `compute_settlement_slot`/`deduplicate_funding_records` são domain puro).
- `harness rules --mode sweep --changed-only --format ndjson` → saída vazia, `rc=0` (0 achados sobre os 3 arquivos novos + 1 modificado).
Cobertura: 97,64% total / domain 99,8% / use_cases 100,0% / infra 95,1% (piso 90/80/70, todos `[OK]`) — comando acima, universo = suíte completa (1296 testes, 5468 stmts em `src`).

Falsificadores medidos (não só citados do plano):
- D6.11: `1000XECUSDT-fundingRate-2026-07.zip` (md5 `40e35f60a065aac30f2d08d7a47139bc`, `data/MANIFEST.md`), 321 linhas — residual por linha (intervalo próprio) em `[0,12]` ms, nunca negativo; a MESMA base sob intervalo fixo assumido `8h` erra **228/321 = 71,0%** fora de `[0,20]` ms.
- D6.12: as 321 linhas ingeridas 2× (642 registros) colapsam a **321** via `deduplicate_funding_records`; transições reais `8h→1h` (índice 38) e `1h→4h` (índice 267, delta exata 3,0h) sobrevivem distintas. PK igual + conteúdo diferente → `ConflictingFundingRecordError` (mutação: 2 records com mesma PK e `funding_rate` diferente, testado explicitamente).
- D6.16: divergência de fixture DECLARADA — `data/MANIFEST.md:144` registra que `2026-08-25_fundingInfo.json` não tem `premiumIndex` companheiro, então a tripla `exchangeInfo`+`fundingInfo`+`premiumIndex` que `D6.16` exige não existe para `2026-08-25` (só `2026-09-01`). Teste RE-MEDE em `2026-09-01` com a data ao lado do número: `[MEDIDO 2026-09-01]` **569** símbolos `TRADING PERPETUAL` comuns às 3 fontes, `{4h:430, 8h:136, 1h:3}` (4h continua sendo a regra, nunca 8h), `nextFundingTime % (interval_hours·3600000) == 0` em **569/569**, zero exceção.

Achado durante o desenvolvimento, corrigido antes do commit:
`FundingRecord.observed_at` colidia com `READ_PATH_COLUMNS` do scan AST de `test_as_of_is_the_single_reader.py` (proteção contra segundo leitor de série). Corrigido adicionando `funding_settlement.py` a `DECLARED_TOUCHERS` com parágrafo justificando que `__post_init__`/`settlement_residual_ms`/`primary_key` são uma AGREGAÇÃO DIFERENTE (PK de funding), nunca comparam contra um instante de decisão `t`. Docstring do módulo corrigido por adendo (123 módulos, 5 touchers) sem reescrever a frase anterior.

Doc delta:
- `backend/README.md`: atualizado — seção `## 📎 2026-09-03 por T-06.4` acrescentada ao final (peça, falsificadores, achado do `as_of` guard, comandos, escopo não fechado).
- `docs/INDEX.md`: linha `2026-09-03T08:30Z` acrescentada (append-only, nenhuma linha existente reescrita).
- ADR: não necessário — nenhuma decisão arquitetural nova; forma segue `PRD-001` §5.6 (PK já decidida) e `T-06.1` (`SeriesCatalog` reusado sem alteração).

Escopo NÃO fechado, nomeado:
- Zero persistência/ingestão de produção — `domain` puro (`ADR-016`); `T-07.x` constrói a ingestão real do `monthly/fundingRate`.
- `FundingSource.ESTIMATED` não tem produtor real (`premiumIndex.lastFundingRate` polling é trabalho futuro) — o tipo/PK aceitam `observed_at ≠ settle_bucket`, mas nada popula esse caso hoje.
- `unit`/`denom`/`native_grid` do catálogo de funding usados no teste de coexistência são `[INFERRED]` placeholders (`"rate"`/`"notional"`/`"event"`) — os valores reais do catálogo de funding são escopo de `T-06.9`.
- D6.16 não reproduz o número `570`/`433` de `2026-08-25` citado no plano — a fixture que permitiria isso (tripla casada naquela data) não existe (`data/MANIFEST.md:144`); divergência declarada acima, não escondida.
Bloqueado (se houver): nenhum.
