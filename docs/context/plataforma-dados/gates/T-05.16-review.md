# T-05.16 — REVIEW (arquitetural, read-only)

`CST-107`. Escopo: `frontend/eslint.config.mjs` (+64), `ingest-health-query.ts` (docstring, +12),
`fingerprint-sync-boundary.test.ts` (novo, 160 linhas).

## Camada 1 — o que o runner mede

- `harness rules list --severity block` → 8 regras bloqueantes em vigor (denominador).
- `harness rules --mode sweep --changed-only` → **rc=0, saída vazia** — reproduzido
  independentemente (não apenas relido do QA).
- `harness rules --mode file --path <cada um dos 3 arquivos do diff>` → saída vazia nos três.
- Nenhuma das 8 regras bloqueantes toca este diff (nenhum import relativo, nenhum `except`
  silencioso, nenhum `print`, nenhum segredo, nenhuma diretiva de tenant, sem diretório
  `backend/tests` ausente, sem import navegador→servidor).

## Camada 2 — arquitetura declarada

1. **`fingerprint()` permanece síncrona (`ADR-005/D6.4`)** — `ingest-health-query.ts:305-307`:
   `export function fingerprint(...): string`, retorno direto de `sha256Hex(...)`, sem
   `Promise`/`async`. Confirmado por leitura direta, não citação do gate do builder.
2. **`grep -rn 'from "\(\.\./s1-console/\|\./\)ingest-health-query\.ts"' frontend/src ... | grep
   -v '\.test\.'`** reproduzido: 3 linhas, as 3 `import type` (`s3-inspector/{fixtures,domain,
   view-model}.ts`) — casa exatamente o CALA declarado, `n=3`.
3. **`npm --prefix frontend run test:s1`** reproduzido → 54/54 pass; `npx eslint src --format
   json` → `rc=0`, 0 mensagens; `git status --porcelain` após — nenhuma sonda efêmera
   remanescente.
4. **Tensão ADR-003:46 vs. instrumento `no-restricted-imports`** — real, não fabricada: ESLint
   só sabe casar especificador de import, que é caminho. Mas a task **não redecide** — herda a
   resposta que `T-05.1`/`D5.12` já mediu (as 3 alternativas nomeadas exigem infra inexistente),
   e o próprio bloco novo casa por **nome de arquivo** (`**/ingest-health-query.ts`), não por
   diretório de componente — não alimenta `harness.toml`/`[agents.by_component]`, não redefine
   fronteira `charts`/`web`. `ADR-003:46` recusa **caminho decidindo posse de componente**; este
   bloco não decide posse, aplica um invariante de módulo único já fixado por `ADR-005/D6.4`.
   Tensão real em abstrato, **já resolvida** pelo precedente `D5.12`, corretamente citada como
   herdada e não redecidida.

## Achados

Nenhum. Nenhuma violação citável (id de regra ou documento+trecho) encontrada.

## Denominador

8 regras bloqueantes existentes, 8 avaliadas (`rules list` + `sweep --changed-only` + `file` ×3),
5 arquivos no diff (`git status --porcelain`), varredura via `harness rules --mode sweep
--changed-only` (rc=0) e `harness rules --mode file` por arquivo.

## Veredito: COMPLIANT
