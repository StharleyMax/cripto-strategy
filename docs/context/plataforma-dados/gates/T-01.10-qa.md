# QA Gate — T-01.10 (fase 01, [docs][infra], plataforma-dados)

Escopo do delta: `harness.toml` (`include_prefixes += "deploy/"`, `include_globs += "*.yml"/"*.yaml"`,
`[[rules.own]] own.compose-hardcoded-secret`), `deploy/compose.yml`, `corpus/manifest.toml`,
`corpus/policy/harness.toml`, `corpus/reference/compose_secret_reference.sh`,
`corpus/cases/own.compose-hardcoded-secret/{conforming,violating}/*.yml`. Branch
`tasks/T-01.8-juizes-e-deploy`, não commitado. Nenhum arquivo de outra task tocado (confirmado por
`git diff --name-only HEAD` antes de iniciar: só `harness.toml` some no diff de tracked, o resto é
`??` sob `deploy/` e `corpus/`).

## Ataque 1 — `classify` (parte a+b do item 1.14)

```
$ harness code-paths classify deploy/compose.yml
producao: deploy/compose.yml — include_prefixes + include_globs casam e nada exclui
$ echo $?
0
```
Antes desta task (comentário do próprio `harness.toml:353-356`, re-medido): `nao-producao`, `rc=1`.
Confirmado: o arquivo virou `producao`.

## Ataque 2 — `rules --mode file` sobre o compose real, e sobre violador efêmero (D1.14)

**Cala** (arquivo real, versionado):
```
$ harness rules --mode file --path deploy/compose.yml
$ echo $?
0
```
Saída vazia, `exit=0` — confirmado, sem falso positivo no arquivo legítimo.

**Morde** (violador efêmero, criado fora da árvore versionada, apagado logo em seguida — nunca
esteve em `git status`):
```
$ printf 'services:\n  postgres:\n    environment:\n      POSTGRES_PASSWORD: hunter2_prod_master\n' > deploy/.qa_violator_ephemeral.yml
$ harness rules --mode file --path deploy/.qa_violator_ephemeral.yml
{"decision": "block", "reason": "[BLOQUEIO] [own.compose-hardcoded-secret] deploy/.qa_violator_ephemeral.yml:4: segredo literal em arquivo de compose: leia-o do ambiente (`${VAR}`) ou de `/run/secrets` — credencial versionada nao volta atras\n    1 ocorrencia(s) fora da allowlist"}
$ echo $?
2
$ rm -f deploy/.qa_violator_ephemeral.yml   # removido; git status confirma deploy/ sem sujeira
```
`D1.14` fechado pelo critério certo (não por `classify`, que o próprio plano nomeia como
falsificador se usado como fecho): morde `exit=2` nomeando a regra, cala `exit=0`.

## Ataque 3 — corpus (D1.14b, e reativação do item 1.8)

```
$ harness corpus verify --corpus corpus --reference corpus/reference/compose_secret_reference.sh
...
casos: 7 · regras: 1 · divergentes: 0
violadores em caminho de teste: 1 (sem eles, estreitar o escopo para producao passaria verde)
corpus dourado VERDE: veredito identico, arquivo a arquivo
$ echo $?
0
```
7 casos (3 `violating` + 4 `conforming`, incluindo o par `deploy/tests/` que dá sujeito ao mutante e
o caso `backend/config/instrument_alias.yml` que separa "escopo nunca alcançou" de "julgou e
absolveu") — igualdade de veredito com a referência AWK independente, 0 divergência.

```
$ harness corpus mutate --corpus corpus --reference corpus/reference/compose_secret_reference.sh --only-rule own.compose-hardcoded-secret
baseline sem mutacao: VERDE
  [morto] own.compose-hardcoded-secret — estreitar o escopo reprovou os casos DESTA regra
1/1 mutantes mortos
$ echo $?
0
```
O corpus DEFENDE a regra — não é decoração. `D1.14b` fechado.

## Ataque 4 — tentativa de achar segredo que escapa à regex nova

Testado e **calou corretamente** (não é achado): variação de capitalização de chave (`(?i:...)`
cobre), aspas simples/duplas ao redor do valor, `Dockerfile` sem extensão — este último continua
**`[NÃO MEDIDO]`, declarado em aberto pelo próprio `harness.toml:437-442`** (não é defeito novo, é
lacuna já confessada).

**Três escapes REAIS, não documentados, com segredo literal ficando fora do bloqueio** — cada um
criado como arquivo efêmero em `deploy/`, rodado e apagado na hora, nunca entrou em `git status`:

**(A) decoy `${...}` colado depois de um segredo literal real** — contradiz o próprio propósito que
o comentário do regex declara (`harness.toml`: "separar senha no arquivo de senha lida do
ambiente"):
```
$ printf 'services:\n  postgres:\n    environment:\n      POSTGRES_PASSWORD: hunter2_prod_master${UNUSED}\n' > deploy/.qa_atk_a.yml
$ harness rules --mode file --path deploy/.qa_atk_a.yml; echo $?

0
```
`hunter2_prod_master` é literal (não vem de `${VAR}` nenhum — `${UNUSED}` é apêndice morto), e a
linha cala porque a allowlist `\$\{` é verificada em **qualquer posição da linha**, não no valor
capturado.

**(B) segredo real + comentário de fim de linha com palavra da allowlist** — contradiz o propósito
declarado da allowlist ("valor de amostra declarado como amostra": aqui o VALOR não é amostra, só o
comentário menciona uma palavra da lista):
```
$ printf 'services:\n  postgres:\n    environment:\n      POSTGRES_PASSWORD: hunter2_prod_master  # changeme depois\n' > deploy/.qa_atk_b.yml
$ harness rules --mode file --path deploy/.qa_atk_b.yml; echo $?

0
```

**(C) YAML flow-style** (`environment: {POSTGRES_PASSWORD: hunter2_prod_master}`) — a chave não
começa a linha, e o regex ancora em `^` (avaliação linha a linha); qualquer segredo dentro de um
mapeamento *flow* nunca é avaliado:
```
$ printf 'services:\n  postgres:\n    environment: {POSTGRES_PASSWORD: hunter2_prod_master}\n' > deploy/.qa_atk_c.yml
$ harness rules --mode file --path deploy/.qa_atk_c.yml; echo $?

0
```

Os três são segredo literal, sem indireção de ambiente real, e a regra devolve silêncio (`rc=0`)
para os três. (A) e (B) não são só "regex imperfeita" genérica — cada um derruba, com um caso
concreto, a garantia que o próprio comentário da regra em `harness.toml` alega ter.

## Veredito

```
## QA Gate — Fase 01 [docs][infra] T-01.10
- [OK] own.compose-hardcoded-secret existe, alcança `deploy/compose.yml`, cala no arquivo real — harness.toml:576-651 (rules.own) + harness rules --mode file --path deploy/compose.yml → vazio, exit=0
- [FAIL] own.compose-hardcoded-secret morde todo segredo literal em compose — 3 escapes reproduzidos (decoy `${...}`, comentário com palavra da allowlist, YAML flow-style), cada um com segredo literal e `rc=0`; (A) e (B) contradizem o propósito que o próprio comentário do regex declara
- [OK] D1.14 (morde/cala, não por classify) — harness rules --mode file: violador efêmero → BLOQUEIO exit=2 citando a regra; deploy/compose.yml real → vazio exit=0 (2 casos)
- [OK] D1.14b (corpus com rede) — harness corpus verify --corpus corpus --reference corpus/reference/compose_secret_reference.sh (7 casos, 0 divergentes, exit=0); harness corpus mutate ... --only-rule own.compose-hardcoded-secret (1/1 mutante morto, exit=0)
- [OK] Testes existem e passam — harness corpus verify --corpus corpus --reference corpus/reference/compose_secret_reference.sh (7 testes/casos)
- N/A Cobertura de linha — task [docs][infra], sem alvo de `test_cmd`/coverage declarado para este componente (`harness policy --key test_cmd` só lista `sentimento`); o instrumento de fechamento do próprio plano é o corpus verify/mutate acima, não %
- [FAIL] DoD 1.14/1.14b item a item — as duas metades morde/cala e o corpus com rede fecham; a robustez da regex contra bypass adversarial NÃO fecha (achado do Ataque 4)
- [anomalia] nenhuma
Regras bloqueantes avaliadas: 8 de 8 (`harness rules list --severity block`) — só `own.compose-hardcoded-secret` é alcançável pelos arquivos deste delta; as outras 7 não têm arquivo `.py`/`.tsx` no diff para avaliar
Veredito: NEEDS_FIX
Ações (se NEEDS_FIX):
1. Restringir a allowlist `\$\{` a casar só dentro/adjacente ao VALOR capturado (não a linha inteira) — ou exigir que o valor comece por `${` para contar como indireção, fechando o escape (A).
2. Restringir `example|placeholder|changeme|dummy|redacted` a casar dentro do próprio valor capturado, não em qualquer trecho da linha (comentário incluso) — fechando o escape (B).
3. Documentar (C) como `[NÃO MEDIDO]` explícito no comentário do `harness.toml`, no mesmo padrão já usado para `Dockerfile`, se a correção de forma YAML *flow-style* for adiada para outra task — não deixar a lacuna implícita.
4. Após ajuste, acrescentar aos 3 escapes como novos casos `violating/` no corpus (com `repo_path` sob `deploy/`) para que `corpus mutate` também os defenda — hoje eles não têm caso que os defenda.
```

Grave o veredito: `harness gate-record plataforma-dados 01 QA NEEDS_FIX "T-01.10: cobertura de deploy/ fechada (D1.14/D1.14b), mas regex de own.compose-hardcoded-secret tem 2 escapes que contrariam o propósito documentado (decoy \${...}, comentário com palavra de allowlist) + 1 não documentado (YAML flow-style)"`

## Rodada 2 — commits `02149f4`/`e80645e`, branch `tasks/T-01.8-juizes-e-deploy`, HEAD limpo

### Reprodução dos ataques 4(A) e 4(B) — devem morder agora

Arquivo efêmero sob `deploy/`, criado e apagado na hora, nunca em `git status`:
```
$ printf '...\n      POSTGRES_PASSWORD: hunter2_prod_master${UNUSED}\n' > deploy/.qa2_atk_a.yml
$ harness rules --mode file --path deploy/.qa2_atk_a.yml; echo $?
{"decision": "block", "reason": "[BLOQUEIO] [own.compose-hardcoded-secret] ... 1 ocorrencia(s) fora da allowlist"}
2
$ printf '...\n      POSTGRES_PASSWORD: hunter2_prod_master  # changeme depois\n' > deploy/.qa2_atk_b.yml
$ harness rules --mode file --path deploy/.qa2_atk_b.yml; echo $?
{"decision": "block", "reason": "[BLOQUEIO] [own.compose-hardcoded-secret] ... 1 ocorrencia(s) fora da allowlist"}
2
```
Ambos mordem `exit=2` nomeando a regra. (A) e (B) fechados — confirmado, não só lido no diff.

### Uma 4ª variação — e ela AINDA escapa

O comentário do `harness.toml:667-668` ("a correção ancora cada allowlist de valor ao MESMO ponto de
partida do valor que o regex principal usa") não é o que o motor faz. Lido em
`lib/rules.py:205-216` (`0.13.0`, `/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/lib/rules.py`):

```python
hits = [(i + 1, ln) for i, ln in enumerate(lines)
        if rgx.search(ln) and not any(a.search(ln) for a in allow)]
```

`a.search(ln)` roda cada `allowlist_regex` sobre a **linha inteira**, sem nenhum vínculo de posição
com onde o `regex` principal capturou o valor. O padrão de allowlist exige um `[:=]` **em algum
lugar da linha**, imediatamente seguido (com aspa/espaço opcional) do termo de exceção — não
necessariamente o MESMO `:`/`=` que separa a chave de credencial do valor literal. Basta um segundo
`:` ou `=` em outro lugar da linha (ex.: dentro do comentário) seguido de `${` ou de uma palavra de
amostra, e a allowlist casa mesmo sem tocar o valor real:

```
$ printf '...\n      POSTGRES_PASSWORD: hunter2_prod_master  # ref:changeme\n' > deploy/.qa2_atk_d.yml
$ harness rules --mode file --path deploy/.qa2_atk_d.yml; echo $?

0
$ printf '...\n      POSTGRES_PASSWORD: hunter2_prod_master  # nota:${UNUSED}\n' > deploy/.qa2_atk_e.yml
$ harness rules --mode file --path deploy/.qa2_atk_e.yml; echo $?

0
$ printf '...\n      - POSTGRES_PASSWORD=hunter2_prod_master  # x=changeme\n' > deploy/.qa2_atk_f.yml
$ harness rules --mode file --path deploy/.qa2_atk_f.yml; echo $?

0
```
Três variações (D: `:changeme` em comentário, E: `:${...}` em comentário, F: `=changeme` em
comentário com chave em forma `KEY=valor`), segredo literal real, `rc=0` nas três — mesma classe do
ataque 4(B) original (palavra de allowlist fora do valor), só que sobrevivendo à correção porque a
âncora é "existe algum `:`/`=` na linha", não "é o `:`/`=` desta chave". Os 3 são efêmeros sob
`deploy/`, apagados na hora, `git status --short deploy/` confirma limpo depois.

Isto **contradiz o texto do próprio `harness.toml` que descreve a correção** (linha "ancora cada
allowlist de valor ao MESMO ponto de partida do valor") — a alegação está errada para qualquer linha
com mais de um `:`/`=`, e nenhum caso do corpus tem dois separadores na mesma linha, então
`corpus verify`/`mutate` não veem o problema.

### Regressão — casos conformantes antigos continuam calando

```
$ harness rules --mode file --path deploy/compose.yml; echo $?
0
```
Inspeção dos 4 `conforming/*.yml` do corpus (`${VAR}`, `${VAR:?...}`, `_FILE:`, fora de escopo) —
nenhum mudou de veredito. Sem novo falso-positivo introduzido pela ancoragem.

### `harness corpus verify` / `harness corpus mutate`

```
$ harness corpus verify --corpus corpus --reference corpus/reference/compose_secret_reference.sh
casos: 9 · regras: 1 · divergentes: 0
corpus dourado VERDE: veredito identico, arquivo a arquivo
$ echo $?
0
$ harness corpus mutate --corpus corpus --reference corpus/reference/compose_secret_reference.sh --only-rule own.compose-hardcoded-secret
baseline sem mutacao: VERDE
  [morto] own.compose-hardcoded-secret
1/1 mutantes mortos
$ echo $?
0
```
9 casos (5 `violating` + 4 `conforming`), 0 divergentes — os 2 casos novos (`04_dollar_decoy_suffix`,
`05_allowlist_word_in_comment`, mapeados via `corpus/manifest.toml:83-94` para `deploy/compose.decoy-dollar.yml`
e `deploy/compose.decoy-comment.yml`) defendem exatamente (A) e (B). Nenhum caso cobre D/E/F —
por isso o corpus não acusa a lacuna nova.

### Ações 1–4 do `NEEDS_FIX` da rodada 1 — status individual

1. Escape (A) — **fechado**, confirmado acima.
2. Escape (B) — **fechado**, confirmado acima.
3. (C) YAML flow-style documentado `[NÃO MEDIDO]` — **confirmado**, `harness.toml` (bloco logo após
   `allowlist_regex`, mesmo padrão do gap de `Dockerfile`).
4. Casos novos no corpus para os escapes fechados — **confirmado**, `corpus/manifest.toml:83-94`,
   `corpus verify` os inclui (9 casos) e `mutate` os usa para matar o mutante.

Nenhuma das 4 ações cobria a variação D/E/F — ela não existia na rodada 1. É achado novo desta rodada,
não ação pendente reaberta.

## Veredito (rodada 2)

```
## QA Gate — Fase 01 [docs][infra] T-01.10 (rodada 2)
- [OK] own.compose-hardcoded-secret existe, alcança deploy/, cala no arquivo real — harness rules --mode file --path deploy/compose.yml → vazio, exit=0
- [OK] Escape (A) decoy ${...} fechado — harness rules --mode file --path deploy/.qa2_atk_a.yml → BLOQUEIO, exit=2 (efêmero, apagado)
- [OK] Escape (B) palavra de allowlist em comentário fechado — harness rules --mode file --path deploy/.qa2_atk_b.yml → BLOQUEIO, exit=2 (efêmero, apagado)
- [FAIL] 4ª variação (D/E/F): separador `:`/`=` extra em comentário casa a allowlist sem tocar o valor real — harness rules --mode file --path deploy/.qa2_atk_{d,e,f}.yml → vazio, exit=0 nas 3, com segredo literal real na linha; causa raiz em lib/rules.py:205-216 (`a.search(ln)` roda sobre a linha inteira, não sobre a posição do valor) — contradiz o texto do harness.toml (linha "ancora ao MESMO ponto de partida do valor")
- [OK] Regressão dos conformantes — deploy/compose.yml e os 4 conforming/*.yml do corpus continuam calando, exit=0
- [OK] harness corpus verify --corpus corpus --reference corpus/reference/compose_secret_reference.sh — 9 casos, 0 divergentes, exit=0
- [OK] harness corpus mutate ... --only-rule own.compose-hardcoded-secret — 1/1 mutante morto, exit=0
- [OK] Testes existem e passam — harness corpus verify (9 testes/casos)
- N/A Cobertura de linha — mesmo motivo da rodada 1: sem alvo de test_cmd/coverage para [docs][infra]
- [FAIL] DoD 1.14/1.14b item a item — robustez da regex contra bypass adversarial continua não fechando: (A) e (B) fechados, mas D/E/F reabrem a mesma classe de defeito (palavra/padrão de allowlist fora do valor real) por uma via que o fix da rodada 2 não cobriu
- [anomalia] nenhuma
Regras bloqueantes avaliadas: 8 de 8 (`harness rules list --severity block`) — só own.compose-hardcoded-secret é alcançável pelos arquivos deste delta
Veredito: NEEDS_FIX
Ações (se NEEDS_FIX):
1. Ligar a posição da allowlist à posição do VALOR capturado pelo `regex` principal, não a "existe um `[:=]` em algum lugar da linha" — ex.: usar o span do match do `regex` principal e testar `allow.search(ln, pos=<início do valor capturado>)`/`match` a partir daí, ou reescrever a allowlist para checar apenas o trecho da linha após a ÚLTIMA ocorrência do `[:=]` que o `regex` principal usou como separador da chave.
2. Se a mudança de engine (item 1) não for possível nesta task, ao menos restringir cada `allowlist_regex` para não casar dentro de comentário (`#.*$`) explicitamente — reduz a superfície sem resolver o caso de dois `:`/`=` fora de comentário na mesma linha (que também é possível, ex. duas chaves na mesma linha por outro mecanismo), então é mitigação parcial, não fechamento.
3. Acrescentar casos `violating/` no corpus para D/E/F (linha com segredo literal real + segundo separador `:`/`=` seguido de termo de exceção) para que `corpus mutate` passe a defender contra regressão desta classe específica.
4. Atualizar o comentário do harness.toml que descreve a correção — a frase "ancora ao MESMO ponto de partida do valor" está factualmente errada para o comportamento do motor (`lib/rules.py:205-216` usa `.search()` de linha inteira); documentar com precisão ou apontar a lacuna como `[NÃO MEDIDO]`, no mesmo padrão já usado para (C).
```

Grave o veredito: `harness gate-record plataforma-dados 01 QA NEEDS_FIX "T-01.10 rodada 2: escapes (A) e (B) fechados e confirmados, mas allowlist_regex ainda casa por presenca de QUALQUER ':'/'=' na linha (lib/rules.py:205-216 usa .search() de linha inteira) — 4a variacao (comentario com separador extra + termo de excecao) reabre a mesma classe de defeito com segredo literal real escapando"`

## Rodada 3 — commit `bc43358` (registrada aqui por completude, resumida do builder — não reexecutada nesta rodada, ver Rodada 4 abaixo para a reexecução)

Ação 1 do `NEEDS_FIX` da rodada 2 aplicada por mitigação (não pela reescrita do motor, que é
vendorizado/fora de escopo): prefixo `^(?:(?!#).)*` em cada `allowlist_regex` de
`own.compose-hardcoded-secret`, exigindo que nenhum `#` apareça entre o início da linha e o ponto
onde o padrão de exceção casa — fecha D/E/F (o decoy nos três está dentro do comentário, ou seja,
depois do `#`). O caso residual (decoy antes de qualquer `#`, ou linha sem `#` nenhum) continua
escapando por limite estrutural do motor (`lib/rules.py:205-216` não recebe o span do match
principal) e foi declarado `[LIMITE CONHECIDO]` em `harness.toml` em vez de apresentado como
fechado. Dois casos novos de corpus (`06_extra_colon_placeholder_in_comment`,
`07_extra_colon_dollar_in_comment`) defendem o que foi fechado. Ações 3 (documentação honesta) e 4
(sincronizar `corpus/policy/harness.toml`) também tocadas neste commit.

## Rodada 4 — verificação independente do zero, HEAD `008be7b`

Checkout `/home/stharley/Documentos/projects/cripto-strategy`, branch `tasks/T-01.8-juizes-e-deploy`.
`git status --porcelain` antes de começar: só este arquivo de gate (modificado, sou eu escrevendo) e
`gates/fase-01-qa-2026-09-03.md` (`??`, gate da sessão paralela) — nenhum arquivo de outra task.

### (1) `make lint-corpus` e `make lint`, do zero

```
$ make lint-corpus
... 11 casos: 3 violating novos (decoy-comment-colon, decoy-comment-dollar, + os 5 antigos) + 4 conforming
casos: 11 · regras: 1 · divergentes: 0
violadores em caminho de teste: 1
corpus dourado VERDE: veredito identico, arquivo a arquivo
  [morto] own.compose-hardcoded-secret — estreitar o escopo reprovou os casos DESTA regra
1/1 mutantes mortos
$ echo $?
0

$ make lint
bash scripts/check-agents-by-component.sh  -> "bate com o dourado — 6 componente(s), 10 papel(is)."
harness corpus verify + mutate            -> mesma saída acima, rc=0
bash backend/scripts/lint.sh              -> "All checks passed!", 256 arquivos formatados, mypy 0 issues
npm --prefix frontend run lint            -> ESLint sem erro
$ echo $?
0
```
`lint-corpus` está de fato antes de `lint-backend`/`lint-frontend` (`Makefile:116`, confirmado por
leitura: `lint: lint-agents lint-corpus lint-backend lint-frontend`).

### (2) `make lint-corpus` reproduz a divergência real do commit `02149f4`

Cópia de segurança de `corpus/policy/harness.toml` salva no scratchpad. Editada a linha
`allowlist_regex` DESTE arquivo (só o fixture do corpus, não a política raiz) para a versão
desatualizada de `02149f4` (`['\$\{', '(?i)_file\s*[:=]', '/run/secrets/',
'(?i)(example|placeholder|changeme|dummy|redacted)']` — sem âncora, sem prefixo anti-comentário):

```
$ make lint-corpus
  [DIFERE] violating own.compose-hardcoded-secret  caminho: deploy/compose.decoy-dollar.yml         referencia: [own.compose-hardcoded-secret]  runner: -
  [DIFERE] violating own.compose-hardcoded-secret  caminho: deploy/compose.decoy-comment.yml        referencia: [own.compose-hardcoded-secret]  runner: -
  [DIFERE] violating own.compose-hardcoded-secret  caminho: deploy/compose.decoy-comment-colon.yml  referencia: [own.compose-hardcoded-secret]  runner: -
  [DIFERE] violating own.compose-hardcoded-secret  caminho: deploy/compose.decoy-comment-dollar.yml referencia: [own.compose-hardcoded-secret]  runner: -
EXPECTATIVA DECLARADA NAO CUMPRIDA (passe degenerado): 4 violadores
make: *** [Makefile:134: lint-corpus] Erro 1
```
`rc≠0` (make devolve `Erro 1`), citando a regra e os 4 arquivos exatamente da classe que a raiz já
fecha e o fixture desatualizado não — **é o mesmo formato de divergência do `02149f4`, só que agora
com um portão que a acusa em vez de silêncio**. Arquivo restaurado da cópia de segurança logo em
seguida; `diff` contra a cópia original → vazio; `make lint-corpus` voltou a `rc=0`;
`git status --porcelain corpus/` → vazio durante todo o experimento (arquivo já era tracked e sujo
não aparece porque a edição foi desfeita antes do commit desta rodada).

### (3) Reprodução dos ataques das rodadas 1–3 — arquivo efêmero em `deploy/`, nunca em `git status`

Sete casos, cada um criado como `deploy/.qa_r4_<nome>.yml`, avaliado com
`harness rules --mode file --path <arquivo>` e apagado na sequência:

| ataque | conteúdo (resumo) | esperado | obtido |
|---|---|---|---|
| A — decoy `${...}` sufixo | `PASSWORD: hunter2_prod_master${UNUSED}` | morder (fechado r1) | `rc=2`, BLOQUEIO |
| B — palavra de allowlist em comentário | `PASSWORD: hunter2_prod_master  # changeme depois` | morder (fechado r1) | `rc=2`, BLOQUEIO |
| D — `:changeme` em comentário (2º separador) | `PASSWORD: hunter2_prod_master  # ref:changeme` | morder (fechado r3) | `rc=2`, BLOQUEIO |
| E — `:${...}` em comentário | `PASSWORD: hunter2_prod_master  # nota:${UNUSED}` | morder (fechado r3) | `rc=2`, BLOQUEIO |
| F — `=changeme` em comentário, chave `KEY=valor` | `- PASSWORD=hunter2_prod_master  # x=changeme` | morder (fechado r3) | `rc=2`, BLOQUEIO |
| C — YAML flow-style | `environment: {PASSWORD: hunter2_prod_master}` | escapar (`[NÃO MEDIDO]` declarado) | `rc=0`, silêncio |
| RESIDUAL — decoy antes de qualquer `#`, sem comentário | `PASSWORD: hunter2_prod_master extra_ref:${UNUSED}` | escapar (`[LIMITE CONHECIDO]` declarado r3) | `rc=0`, silêncio |

5 de 5 fechados mordem (`rc=2`, `{"decision": "block", ...own.compose-hardcoded-secret...}`); os 2
casos que o próprio `harness.toml` declara em aberto (C e o residual D/E/F) escapam exatamente como
documentado — nenhuma surpresa em nenhuma direção. `find deploy -type f` → só `deploy/compose.yml`
antes, durante e depois; `git status --porcelain deploy/` → vazio.

### (4) `harness.toml` documenta o `[LIMITE CONHECIDO]` honestamente

```
$ grep -n "NÃO MEDIDO\|LIMITE CONHECIDO\|flow-style\|Dockerfile" harness.toml
442-446   [NÃO MEDIDO] Dockerfile sem extensão em include_globs — comando de fechamento citado
704,710-724  [LIMITE CONHECIDO] decoy antes de # / linha sem # — causa raiz citada
              (lib/rules.py:205-216, a.search(ln) não recebe o span do match principal),
              exemplo que ainda escapa citado literal, e endereço de quem decide
              (mantenedor do plugin vendorizado, não esta task)
726-745   [NÃO MEDIDO] YAML flow-style — comando de fechamento citado, custo de subconto
              com ${VAR:?msg} explicado
```
Nenhum dos três blocos afirma cobertura que não existe; cada um nomeia o comando que fecharia a
lacuna e por que não foi rodado nesta task. `harness.toml:697-708` também substituiu a frase
factualmente errada da rodada 2 ("ancora ao MESMO ponto de partida do valor") pela descrição correta
do que o prefixo `^(?:(?!#).)*` faz e não faz — ação 4 do `NEEDS_FIX` da rodada 2, fechada.

### (5) `harness rules --mode sweep --changed-only` sobre o diff completo desde `53801a9`

`--changed-only` usa `git diff --name-only HEAD` (árvore de trabalho vs `HEAD`) — leitura do
código-fonte do plugin, `lib/runner.py:87-90`. Como a árvore está limpa em `008be7b`, essa flag
sozinha varre 0 arquivos e não testa o diff pedido. Para cobrir o diff completo desde `53801a9`,
rodei `--mode file` com todos os 28 arquivos de `git diff --name-only 53801a9 HEAD`:

```
$ git diff --name-only 53801a9 HEAD | wc -l
28
$ harness rules --mode file --path <cada um dos 28> --format ndjson | wc -l
0
$ harness rules --mode file --path <cada um dos 28>; echo $?
0
```
0 achados (nenhuma linha em `ndjson`, saída humana vazia, `rc=0`) sobre os 28 arquivos do delta
inteiro da fase (`Makefile`, `corpus/**`, `deploy/compose.yml`, `harness.toml`,
`docs/context/plataforma-dados/**`, `scripts/check_agents_by_component.py` etc.) — consistente com
`make verify` (`[OK] regras rc=0 · 0 bloqueio(s), 16 aviso(s)` sobre a árvore inteira, medido acima
em `make lint`/`make verify`).

## Veredito (rodada 4, final)

```
## QA Gate — Fase 01 [docs][infra] T-01.10 (rodada 4)
- [OK] own.compose-hardcoded-secret morde os 5 escapes fechados (A/B/D/E/F) e cala os 2 declarados em aberto (C flow-style, residual sem #) — 7 arquivos efêmeros em deploy/, harness rules --mode file, rc=2 nos 5/rc=0 nos 2, git status limpo depois
- [OK] make lint-corpus rc=0 (11 casos, 0 divergentes, 1/1 mutante morto) e make lint rc=0 (lint-agents+lint-corpus+lint-backend+lint-frontend)
- [OK] make lint-corpus PEGA a divergência real do 02149f4 — allowlist desatualizada plantada só em corpus/policy/harness.toml → 4 [DIFERE], make: *** [Makefile:134: lint-corpus] Erro 1 (rc≠0); arquivo restaurado, diff vazio, make lint-corpus voltou a rc=0
- [OK] harness.toml documenta [LIMITE CONHECIDO] (residual D/E/F, harness.toml:704-724) e [NÃO MEDIDO] (Dockerfile:442-446, YAML flow-style:726-745) com causa raiz, exemplo citável e comando de fechamento — nenhuma cobertura fingida
- [OK] Testes existem e passam — harness corpus verify --corpus corpus --reference corpus/reference/compose_secret_reference.sh (11 casos) + harness corpus mutate --only-rule own.compose-hardcoded-secret (1/1 morto)
- [OK] harness rules --mode sweep --changed-only trivialmente vazio (git diff --name-only HEAD vazio, lib/runner.py:87-90); cobertura real do diff desde 53801a9 obtida via --mode file sobre os 28 arquivos (git diff --name-only 53801a9 HEAD) — 0 achados, rc=0, consistente com make verify (0 bloqueio(s))
- [OK] Regras bloqueantes — 8/8 listadas por harness rules list --severity block; own.compose-hardcoded-secret é a única alcançável pelos arquivos do delta desta fase, medida à parte acima; as outras 7 cobertas por make verify (regras rc=0, 0 bloqueio(s))
- N/A Cobertura de linha — [docs][infra], sem alvo de test_cmd/coverage declarado (harness policy --key test_cmd só lista sentimento); make verify mede 97.74% no backend, fora do escopo desta task
- [OK] DoD 1.14/1.14b item a item — D1.14 (morde/cala sem classify): confirmado; D1.14b (corpus com rede E enforcement repetível em make lint-corpus, não só medição manual): confirmado, incl. reprodução da divergência real que o DEFEITO 1 da sessão paralela apontou
- [anomalia] nenhuma
Regras bloqueantes avaliadas: 8 de 8 (harness rules list --severity block)
Veredito: APPROVED
```

Grave o veredito: `harness gate-record plataforma-dados 01 QA APPROVED "T-01.10 rodada 4 (final): own.compose-hardcoded-secret morde os 5 escapes das rodadas 1-3 (A/B/D/E/F) e cala os 2 limites declarados (YAML flow-style, decoy residual antes de #); make lint-corpus (rc=0) reproduz E pega a divergencia real do 02149f4 quando plantada de novo; harness.toml documenta LIMITE CONHECIDO/NAO MEDIDO com causa raiz e comando de fechamento; sweep --changed-only trivialmente vazio em arvore limpa, cobertura real do diff desde 53801a9 (28 arquivos, --mode file) sem achados"`
