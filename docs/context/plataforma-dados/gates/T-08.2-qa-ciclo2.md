# `T-08.2` · `/qa` ciclo 2 — veredito `APPROVED`

> **Transcrito pelo coordenador do loop**, não escrito pelo `/qa`. **Por que existe:** o `/review`
> apontou (`[INFO]`) que *"o `APPROVED` do ciclo 2 não é citável do repo"* — `gates/` só tinha
> `T-08.2-qa.md`, que é o **`NEEDS_FIX` do ciclo 1**, e o `gate-record` **recusa** gravar
> (`rc=4`, dívida da fase 02). Sem este arquivo a aprovação existia **só na conversa**, que é
> exatamente a classe de estado que o `CLAUDE.md` recusa: *"o ledger é a identidade do estado, não o
> texto do documento"* — e quando o ledger não pode receber, o mínimo é o texto **existir**.
>
> **Autoria e força:** veredito do `/qa` sobre `d149f74`; números reproduzidos por ele com os
> comandos abaixo. O que este arquivo acrescenta é **durabilidade**, não julgamento.

**Commit auditado:** `d149f74` · **Fase:** 08 · **Componente:** `charts` · **PR:** #41

## Veredito: `APPROVED`

As três ações do `NEEDS_FIX` do ciclo 1 estão feitas e **verificadas por comando**.

| ação | comando | medido |
|---|---|---|
| `F-1` | `python3 -c "print(f'{314/1440*100:.2f}%')"` | **`21.81%`** — a célula agora traz `20,0% alvo ⇒ 21,81% realizada` |
| `F-2` | bloco de §3.1 extraído verbatim (`awk 'NR>=104&&NR<=117'` → `bash`) | `rc=0`, reproduz **byte a byte**: `36,390 / 27,333 / 13,664 / 27,753 / 27,254 px` |
| `F-3` | mutação `if (drift > 0.02)` → `if (false)` | `tests 13 · pass 11 · fail 2`; restaurado `13/13`, arquivo **idêntico** |

**Faixa `n=5`: `13,664–36,390 px`, razão `2,66×`, semente publicada = MÁXIMO. 5 de 5 FORA da
tolerância, a menos ruim a `27,3×`.** O comando **mora no repositório** — a versão anterior o citava
como `node /tmp/…mjs`, e número cujo comando vive fora do repo não reproduz.

## O mutante que o `/qa` acrescentou sem ser pedido, e ele fecha um buraco real

```
if (paneWidthPx <= 0) → if (false)      tests 13 · pass 12 · fail 1
```

**O caso da pane zero NÃO é vácuo, e a razão é sutil:** a guarda de `drift` **não o pega**, porque
com largura zero o cálculo dá `NaN`, e **`NaN > 0.02` é falso**. Sem a guarda explícita, uma pane de
largura zero passaria em silêncio pelo teste que existe para rejeitá-la. Dois caminhos de falha,
duas guardas, e agora cada uma com um mutante que a mata.

## `docs/INDEX.md` — append-only medido POR CONJUNTO

```bash
comm -23 <(git show 22541a9:docs/INDEX.md | sort) <(git show d149f74:docs/INDEX.md | sort) | wc -l
# 0        ← nenhuma linha perdida
# novas: 1 · total 94 -> 95 · pipes não escapados da linha nova: 5, igual à linha 94
```

**Medir por conjunto e não por `git diff | grep '^-'` não é preciosismo:** linha movida conta como
removida num diff e não foi removida. Este repositório já cometeu esse erro três vezes.

## Não-vacuidade no arquivo NOVO, e por que ela não se herda

```bash
# import de backend/src plantado em headless-chart.test.ts
harness rules --mode file …   ->  rc=2  [BLOQUEIO] web-fullstack.browser-imports-server
# restaurado
                              ->  rc=0
```

O `/qa` do ciclo 1 provou não-vacuidade sobre os **5** arquivos daquele diff. `headless-chart.test.ts`
**nasceu no ciclo 2**, fora daquele universo — **não herdou a prova, e por isso ela foi refeita**.

## Portões

```
make verify -> VERDE, 6 portões
  lint-backend rc=0 (52 arquivos) · lint-frontend rc=0
  test rc=0  325 passed · 99,16%
  boundaries rc=0  3 kept, 0 broken
  regras rc=0  0 bloqueio(s), 1 aviso(s)   (browser-test-file-present, pré-existente)
  política rc=0
```

`7 de 7` regras bloqueantes avaliadas (`harness rules list --severity block`).

## Bloqueado, e é ato do owner — sobrevive à aprovação

```bash
harness policy --key test_cmd.charts    # rc=1, campo ausente
harness policy --key coverage_target    # rc=1, campo ausente
```

**Os 13 testes de `charts` não são cobrados por portão nenhum: quem apagar o arquivo de teste passa
no `make verify`.** O conserto do `F-3` criou a rede; **nada obriga a rede a existir amanhã.**

```bash
harness gate-record plataforma-dados 08 QA APPROVED
# rc=4 — "3 de 5 tarefa(s) da fase 02 em estado nao-terminal: T-02.1, T-02.2, T-02.4b"
```

Dívida da fase 02, **sem relação com `T-08.2`**: `T-02.1` e `T-02.2` estão `blocked` na `Q1`, que é
pergunta do owner. **`pipeline override` não foi usado** — é escape do owner, e usá-lo por
conveniência apagaria a diferença entre *"a fase 02 está fechada"* e *"alguém contornou o portão"*.
