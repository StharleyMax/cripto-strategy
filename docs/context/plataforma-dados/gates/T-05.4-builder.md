# T-05.4 — builder report (self-attested, NOT final QA/review)

[QA GATE — Fase 05: S2-mínima, item 5.5 — Política de ausência por `nature`]
Feature: plataforma-dados     Componente: charts
Arquivos alterados:
- frontend/src/charts/s2-absence-policy.ts (new)
- frontend/src/charts/s2-absence-policy.test.ts (new)
- frontend/README.md (modified — §13, append-only)
- frontend/package-lock.json (modified — sync com package.json, sem dependência nova)
Spec: docs/specs/SPEC-001-plataforma-dados.md §5.11
Plan: docs/plans/SPEC-001-plataforma-dados/05_fatia_visivel.md, item 5.5, DoD D5.1/D5.2/D5.3
Jira: CST-38

DoD:
- [x] D5.1 — carimbo de idade é o FECHO na tela, nunca o rótulo cru (`formatCloseStamp`/`closeTimeMs`) — verificado com `npm --prefix frontend run test:charts`
- [x] D5.2 — ausência de OI (`STOCK`) não é lida como leitura da barra: valor mantido em tinta secundária + `de hh:mm:ssZ (−Xm)` + linha-guia apontando para trás até a marca real (`resolveStockReading`), com teto de UM bucket nativo ("trilho maior que grade nativa" = proibido, SPEC-001 §5.11)
- [x] D5.3 — `FLOW` (CVD delta) ausente é ausência: `resolveFlowReading` nunca olha slot vizinho, `formatFlowValue` imprime `—`

Comandos rodados (literais) e resultado:
- `npm --prefix frontend install` → `up to date, audited 129 packages` (universo: lockfile já instalado nesta worktree)
- `npm --prefix frontend run lint` (= `eslint src`) → 0 erro, 0 aviso (universo: todo `frontend/src`)
- `npm --prefix frontend run test:charts` (= `node --test 'src/charts/*.test.ts'`) → **79 pass, 0 fail** (universo: todos os `.test.ts` de `frontend/src/charts/`, 10 deles novos desta task)
- `harness rules --mode sweep --changed-only` → rc=0, 0 achado (universo: os 4 arquivos alterados desta task)
- `git push -u origin tasks/T-05.4-ausencia-guia` (via `pre-push.pre-harness`) → `make boundaries` (3 contratos, 0 broken, universo 164 arquivos/745 deps), `make natureza` (78 arquivos, 0 leitura de relógio), `make lint` backend (`ruff` + `mypy`: 0 achado, 250 arquivos) e frontend (`eslint`: 0 achado) — push aceito. Únicos [AVISO] emitidos são pré-existentes, fora dos 4 arquivos desta task (docstring de módulo em `backend/.../sentimento/*` e `hardcoded-url` em `frontend/src/app/*.test.ts`)

Falsificador (mostrando o que a proteção REJEITA):
- `s2-absence-policy.test.ts`: "trilho de vigencia caps at ONE native bucket — the SECOND missing bucket is absent, not held for the whole gap day" — contra o gap real de dia inteiro (`2026-08-22`, zero arquivo `metrics`): o PRIMEIRO bucket ausente (5 min do último ponto real, 08-21 23:55) resolve `held`; o SEGUNDO (10 min de distância) a mesma função REJEITA para `absent` — prova de que o teto de "trilho maior que grade nativa" (proibido por §5.11) é imposto, não só afirmado em prosa.

Cobertura: 10 testes novos cobrindo as 6 funções exportadas, os 3 ramos de `StockReading` (exact/held/absent, inclusive o teto), os 2 ramos de `FlowReading` (present/absent, zero medido ≠ ausência) e 2 erros de chamador (grade vazia; query FLOW desalinhada) — 4 dos 10 contra fixture real (`data/binance/metrics/BTCUSDT-metrics-2026-08-{22,23}.csv`). Alvo: nenhum piso declarado para `charts` em `harness policy --key test_cmd` (só cobre `sentimento`) — mesmo silêncio já registrado nas seções anteriores do README para `charts`/`web`.

Doc delta:
- frontend/README.md: atualizado — §13 nova (append-only), documenta escopo (2 de 6 `nature`s: só STOCK/FLOW têm painel real nesta fase), as 3 decisões de leitura, geometria-vs-texto (guide-line em bucket-start, label em FECHO), comandos rodados e cobertura.
- docs/specs/SPEC-001-plataforma-dados.md §5.11: sem mudança — a tabela já fixa a política; esta task a implementa, não a emenda.
- docs/adr/ADR-003-fronteira-charts-web.md: sem mudança — módulo puro, mesma disciplina FR-1, nenhuma superfície nova cruza a fronteira charts⇄web.
- ADR: não necessário — nenhuma decisão de arquitetura nova (composição sobre T-05.2, sem reimplementar grade/painéis).

Bloqueado (se houver): nenhum. Nota operacional (não bloqueia o DoD): `git push` nesta worktree exigiu `$HARNESS_MECHANISM` apontado manualmente para o binário já resolvido do projeto principal (`~/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness`) — o registro `~/.claude/plugins/installed_plugins.json` reivindica o caminho do checkout principal, não o da worktree isolada; sem isso o `pre-push` recusa com "mecanismo NAO RESOLVIVEL" antes mesmo de rodar `make boundaries`/`make lint`. Infra do ambiente, fora do escopo desta fase — registrado aqui só para o próximo push nesta mesma worktree não reabrir a investigação.
