# T-08.13 — gate `review`

**Veredito: COMPLIANT.** Regras bloqueantes: 8 de 8 listadas por `harness rules list --severity
block` avaliadas. `harness rules --mode sweep --changed-only` → rc=0, saída vazia (5 arquivos
tocados: `docs/INDEX.md`, `test_reading_honesty.py`, `ADR-024`, 3 gates). `make boundaries` → 7
contratos, **7 kept / 0 broken**. Confirmado: zero linha de `backend/src/` mudou desde `b8953b6`
(`git diff HEAD --stat -- backend/src/` vazio) — a alegação "zero código de produção novo" é real.
16 testes novos confirmados (`grep -c '^def test_' test_reading_honesty.py` = 16), todos verdes
isolados (`pytest tests/charts/test_reading_honesty.py` → 16 passed).

## A leitura "já era estrutural" — honesta em 3/5, parcial e mal-endereçada em 2/5

Mapeamento DoD→decisão confirmado por leitura de `08_superficie_e_reprodutibilidade.md:40-45`:
D8.16→D2, D8.18→D3, D8.20→D4 são **integralmente estruturais** — cobertos pelos 16 testes +
mutação plantada/revertida (QA), sem lacuna. D8.15→D1 e D8.17→D5 têm, no próprio texto do DoD
(`plano 08:40,42`), uma cláusula de **comportamento visível na tela** ("chip de idade
substituído pelo rótulo absoluto da janela"; "escrito na própria tela") que o `ADR-024` mesmo
lista em "Não fecha" (`ADR-024:11,238`) e a QA replica em "Nota de escopo" — **isto está
divulgado, não escondido**, e por isso não é o defeito que a task pediu para auditar.

**O que É um achado:** o endereço do gatilho de reabertura que a ADR dá para essas duas cláusulas
está **errado**. `ADR-024:11,238` cita `T-08.12`/"web" duas vezes como o dono futuro do texto do
chip/legenda. Mas `tasks.toml:1335-1342` mostra `T-08.12` com `components = ["charts"]` (não
`web`) e `refs` = `DoD D8.11-D8.14` (regras de renderização de painel: colisão de disco, eixo Y
duplo) — **nada relacionado a chip de idade ou a "declara-se retrospectiva"**. Busquei por
qualquer task `web`/`S4`/chip em `tasks.toml` (`grep -n "S4\b"`, `grep -n "chip\|idade"`) e não há
candidato: as duas tasks `web` da fase 08 são `T-08.5` (bundle de parâmetros/URL) e `T-08.11`
(SSE) — nenhuma cobre o texto da tela de S4.

**Por que isto importa:** este repositório tem doutrina própria contra exatamente esta forma de
falha — `CLAUDE.md` linha 11 da tabela de fronteira: "exceção sem gatilho é exceção permanente
por omissão", e o gatilho "tem que ter endereço". Um gatilho que aponta para uma task existente,
de componente errado, com DoD sem relação, é pior que nenhum gatilho: ele passa a impressão de
que o trabalho tem dono e data, quando não tem — quebra silenciosa do mesmo tipo que `ADR-012`
nomeia para `rc=0` ambíguo.

**Não é violação de regra bloqueante** (nenhum id de `harness rules` cobre rastreabilidade entre
ADR e `tasks.toml`) — é achado de arquitetura declarada, citável, e por isso **WARNING**, não
reprova o gate.

## Achados

- **[WARNING]** Gatilho de reabertura de `D8.15`/`D8.17` (chip de idade e texto "retrospectiva")
  aponta para `T-08.12`, que não é `web` e não trata dessas DoD — `docs/adr/ADR-024-...md:11,238`
  vs `docs/context/plataforma-dados/tasks.toml:1335-1342`. Correção: `ADR-024` deveria dizer
  `[NÃO SEI: task ainda não nomeada]`, não citar um id existente e incorreto.
- **[WARNING]** Comentários de cabeçalho de seção em português em código sob `backend/tests/`
  (`# ── D1 — idade nunca é campo de tipo pooled/agregado ──` e equivalentes D2-D5) —
  `backend/tests/charts/test_reading_honesty.py:70,115,148,193,270` — contra `CLAUDE.md`, tabela
  de fronteira linha 5 (`docstring/comentário` → inglês, `[DOC]`, força normativa, não a doutrina
  de "convenção não portão" que só cobre IDENTIFICADOR). Frase completa em PT, não token curto
  ambíguo — não é o caso que `ADR-013/D2` recusou medir. Correção: traduzir os 5 cabeçalhos.

## Denominador

8 regras bloqueantes existentes, 8 avaliadas (`harness rules list --severity block`); sweep sobre
5 arquivos alterados nesta worktree; `make boundaries` sobre 199 arquivos/853 dependências, 7
contratos avaliados.
