# Review — T-03.3 (CST-19), fase 03, componente `sentimento` — PR #64

**Veredito: COMPLIANT.**

## Denominador
- Regras bloqueantes em vigor: 7 (`harness rules list --severity block`, packs `core`+`web-fullstack`).
  Nenhuma teve achado nos arquivos deste PR (sem import relativo, sem `except` silencioso, sem
  `print`, sem segredo hardcoded, sem import navegador→servidor, sem tenant do request; regra de
  ausência de diretório de teste do servidor não se aplica — `backend/tests/` existe e recebeu o
  novo `test_force_order_reconnection.py`).
- `harness rules --mode sweep --changed-only` na worktree `/tmp/claude-1002/wt/T-03.3`: saída vazia
  = 0 achados. Builder relatou o mesmo número.
- Arquivos varridos: 6 novos de código-fonte (`domain`×3, `use_cases`×1, `infra`×1) + 1 suíte de
  teste, via `gh pr diff 64` (1244 linhas) e leitura direta na worktree.

## Camada mecânica (runner)
- `bash backend/scripts/boundaries.sh`: **3 kept, 0 broken**, incluindo o contrato
  `Natureza: domain e use_cases nao falam com socket nem ssl` — KEPT. Confere com o builder.
- `bash backend/scripts/natureza.sh`: universo 48 arquivos, **0 leituras de relógio** em
  `domain`/`use_cases`. Confere.
- `bash backend/scripts/test.sh` (rodado de ponta a ponta por este review, não só citado):
  **897 passed**; cobertura por camada (`ADR-009/D1`) — domain **99,8%** (piso 90), use_cases
  **100,0%** (piso 80), infra **95,8%** (piso 70) — os três dentro do piso. Pequena divergência de
  arredondamento com o builder (99,9% vs 99,8% em domain) não muda o veredito: ambos acima do piso.

## Camada de julgamento (ADR-004)
- **B1** (sobreposição obrigatória): `force_order_reconnection_overlap.require_overlap` levanta
  `ReconnectionGapError` se `old_source_closed_at < new_first_message_at`; `perform_overlap_handoff`
  chama `new_source.open()` → lê a 1ª mensagem → só então `old_source.close()`. Nenhum caminho de
  reconexão sem overlap foi reintroduzido — a alternativa recusada em `ADR-004`
  ("Reconexão sem sobreposição") não aparece no diff.
- **B2** (chave natural, não hash): `force_order_natural_key.py` declara
  `(symbol, side, price, orig_qty, trade_time)`, lida do payload, nunca `hash()`/`sha256`/`md5`
  sobre o payload cru — `grep -rniE 'hash|md5|sha256'` só encontra a palavra em um comentário que
  explica por que o hash foi RECUSADO, não uma implementação. Alternativa recusada ("dedupe por
  hash do payload cru") não foi reintroduzida.
- **B3** (taxa publicada + viés): `count_daily_collisions` nunca soma uma colisão a `total_events`
  (subcontagem, nunca supercontagem, mecânico); `COLLISION_BIAS_DIRECTION` carrega a frase literal
  da ADR e é emitida em toda saída do CLI (`_report_lines`, `_summary`).
- Idioma de identificador: `git ls-tree` filtrado por segmento de diretório não introduz nenhum
  segmento em português novo sob `backend/src`/`backend/tests`/`frontend/src` além dos já
  documentados. Mensagens de exceção em português em `force_order_collision_report_cli.py:74`
  (`"linha {number} ilegivel..."`) repetem o padrão pré-existente de `jsonl_checkpoint.py:84` — já
  registrado em `CLAUDE.md` como lacuna `⏸ NÃO DECIDIDO`, convenção e não portão; não é achado novo.

## O que NÃO entrou no veredito
Nenhum achado de opinião sem regra/citação foi incluído — nada a reportar como INFO/WARNING além do
já registrado acima (que não é bloqueante).
