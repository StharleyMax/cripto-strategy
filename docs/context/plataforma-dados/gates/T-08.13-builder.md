# T-08.13 — gate `builder`

**Veredito:** regressão escrita, zero código de produção novo — exatamente o que `ADR-024`
("Consequência") previu: os cinco pontos (`D1`-`D5`) já estavam satisfeitos pelo desenho
existente (`ADR-020`, `ADR-022`) mais o reuso explícito de `sentimento.domain.as_of_accessor`. O
trabalho desta fase foi transformar cada falsificador da ADR em asserção de CI.

**Artefato novo:**
[`backend/tests/charts/test_reading_honesty.py`](../../../../backend/tests/charts/test_reading_honesty.py)
(new) — 16 testes, um bloco por decisão (`D1`-`D5`), cada gate pareado com uma prova de
disparo positivo (planta o defeito exato que a ADR nomeia e confere que o scanner o vê — nenhum
verde sem uma mutação que reprove antes).

## Mapeamento falsificador → teste

| Decisão | Falsificador da ADR | Teste |
|---|---|---|
| `D1` | campo `age`/`age_ms`/`idade` em tipo pooled/agregado | `test_d1_no_pooled_or_aggregate_type_carries_an_age_field` + prova positiva `test_d1_falsifier_catches_a_planted_age_field` |
| `D2` | literal `staleness`/`atraso` dentro de `charts/` | `test_d2_no_staleness_literal_inside_charts` + `test_d2_falsifier_catches_a_planted_staleness_constant` |
| `D3` | heurística `baseAsset`/`re.match` produzindo `denom` dentro de `charts/`; `scan`/`histogram` teriam que aceitar mais de um `FieldIdentity` por chamada para poder misturar símbolos | `test_d3_no_denom_heuristic_inside_charts` + `test_d3_falsifier_catches_a_planted_denom_heuristic` + `test_d3_scan_and_histogram_key_on_exactly_one_field_identity` |
| `D4` | `ObservationSource.observed_values` ganhando parâmetro `spec`/`ThresholdSpec`; um `use_case` chamando outro em cadeia | `test_d4_the_read_port_never_accepts_a_threshold_or_spec_parameter` + `test_d4_falsifier_catches_a_planted_spec_parameter`; `test_d4_no_use_case_calls_another_use_case_by_name` + `test_d4_falsifier_catches_a_planted_use_case_chain` |
| `D5` | `use_case` de `S4` aceitando `live`/`end_ms` opcional ("agora" implícito); laço de polling dentro de `charts/use_cases/` | `test_d5_no_use_case_accepts_a_live_flag_or_an_optional_end_ms`, `test_d5_window_end_ms_is_a_required_int_never_optional`; `test_d5_no_polling_construct_inside_use_cases` + `test_d5_falsifier_catches_a_planted_while_loop` |

Mais `test_the_charts_source_tree_the_scanners_below_walk_is_not_empty` (`ADR-012`'s `rc=0`
doctrine: universo varrido, não silêncio ambíguo).

## Comandos rodados (literais) e resultado

```
bash backend/scripts/lint.sh
→ ruff check src tests: All checks passed!
→ ruff format --check src tests: 310 files already formatted
→ mypy src tests: Success: no issues found in 310 source files

bash backend/scripts/test.sh                      # universo: suíte inteira do backend
→ 1581 passed, 3 warnings in 365.47s (0:06:05)
→ cobertura TOTAL 97,80% (piso global 70%)
→ por camada (ADR-009/D1): domain 99,9% (meta 90%) [3332/3337], use_cases 100,0% (meta 80%)
  [649/649], infra 95,2% (meta 70%) [2252/2366] — 3/3 metas OK
→ backend/src/modules/charts/{domain,use_cases}/*.py: 100% linha em todos os 10 arquivos
  (coverage.xml, seção "Cobertura por camada" acima)

.venv/bin/python -m pytest tests/charts --no-cov  # universo: pacote charts isolado
→ 105 passed in 0.15s (89 pré-existentes + 16 novos de test_reading_honesty.py)

harness rules --mode sweep --changed-only         # universo: arquivos alterados desta task (3)
→ rc=0, saída vazia — nenhuma das 8 regras `block` em vigor disparou
```

`bash backend/scripts/lint.sh`/`test.sh` chamados a partir da raiz do worktree
(`tasks/T-08.13-honestidade-leitura-s4`, base `fe8a466`), com `backend/.venv` symlinkado para o
venv do repositório principal (mesmo precedente já em uso na worktree `T-08.8`) — nenhuma
instalação de dependência foi necessária.

## DoD (`D8.15`, `D8.16`, `D8.17`, `D8.18`, `D8.20`)

Todos os cinco já estavam satisfeitos pelo desenho existente ANTES desta task tocar código —
julgamento do `quant-architect` em `ADR-024` ("Consequência": "este ADR mostra que os quatro já
estão satisfeitos pelo desenho existente"), e o trabalho desta fase foi fixar essa alegação como
asserção que reprova sozinha se algum PR futuro reabrir uma das cinco portas:

- [x] `D8.15` (chip de idade substituído fora da borda direita) — apoiado por `D1`: nenhum tipo
  pooled/agregado de `S4` pode carregar idade, então nenhum consumidor futuro tem de onde ler um
  número de idade fora da borda. **Texto exato do chip na tela é `web`/`T-08.12`, fora de
  escopo** (`ADR-024`, "Não fecha").
- [x] `D8.16` (teste falha exibindo os dois números DA SÉRIE, nunca constante global) — já
  implementado e testado em `sentimento.domain.as_of_accessor.reject_delay_threshold_above_staleness`
  (`as_of_accessor.py:343-369`); `D2` pina que `charts` nunca duplica essa constante
  (`test_d2_no_staleness_literal_inside_charts`).
- [x] `D8.17` (`S4` retrospectiva, `[NÃO SUSTENTADO hoje]`) — `D5`: nenhum `use_case` aceita
  "agora" implícito nem contém laço de polling (`test_d5_*`).
- [x] `D8.18` (`denom` verbatim, recusa cross-símbolo) — `D3`: sem heurística de `baseAsset`
  dentro de `charts/`, e `scan`/`histogram` só aceitam um `FieldIdentity` por chamada
  (`test_d3_*`).
- [x] `D8.20` (zero nudge para baixar o limiar) — `D4`: porta de leitura nunca recebe
  `ThresholdSpec`, nenhum `use_case` encadeia no próprio resultado (`test_d4_*`).

## Doc delta

- `docs/adr/ADR-024-*.md`: sem mudança — já escrito e fechado pelo `/architect` desta mesma
  task, antes deste gate.
- `docs/context/plataforma-dados/gates/T-08.13-architect.md`: sem mudança — pré-existente.
- `docs/INDEX.md`: linha nova acrescentada (append-only) registrando este gate.
- ADR novo: não necessário — `ADR-024` já é o ADR desta task, escrito na fase anterior
  (`/architect`), e nenhuma decisão nova de arquitetura surgiu ao escrever a regressão.

## Bloqueado

Nada. `harness rules --mode sweep --changed-only` → `rc=0`; lint limpo; suíte inteira verde;
cobertura por camada nas três metas.
