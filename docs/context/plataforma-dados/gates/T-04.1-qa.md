# T-04.1 (CST-28) — QA Gate — PR #45

**Veredito: APPROVED**

## O que foi medido (independente do relatório do builder)

- `cd backend && bash scripts/test.sh` → `470 passed`; cobertura domain 100,0% (877/877, piso 90%),
  use_cases 100,0% (220/220, piso 80%), infra 97,8% (614/628, piso 70%) — `[MEDIDO 2026-09-01]`,
  todos acima do piso declarado (`ADR-009/D1`).
- `bash scripts/lint.sh` → ruff + `mypy --strict` limpos, 67 arquivos. `bash scripts/boundaries.sh`
  → `3 kept, 0 broken` (inclui o contrato "Natureza": domain/use_cases não importam datetime).
- `harness rules --mode sweep` (worktree, árvore completa) → rc=0, 0 achado. `harness rules
  --mode sweep --changed-only` → rc=0, 0 achado. `harness validate --strict` → rc=0.
- `harness rules list --severity block` → 7 regras; nenhuma tocada pelo diff (grep manual por
  `print(`, `except.*:$`, import relativo `^from \.`, segredo — 0 ocorrência nos 8 arquivos novos).
- **Anomalia registrada, não reprova**: `make verify` devolveu `rc=3` em "regras" e "política"
  porque `.harness/mechanism` não resolve o plugin dentro do worktree `/tmp/.../wt/T-04.1` (registro
  do Claude Code é escopado ao path do repo principal). Contornado rodando os mesmos dois portões
  via `harness rules --mode sweep` e `harness validate --strict` diretamente — ambos rc=0. Anomalia
  de ambiente, não de código; não usar `make verify` como prova única em worktree.

## Mutação própria (não copiada do builder) — os quatro DoD nomeados

- **D4.1** (bypass do sort reprova): troquei `return tuple(sorted(...))` por `return
  tuple(labeled)` em `label_and_sort_metrics_rows`. `pytest -k "d4_1 or d4_3 or monotonic or
  order"` → **3 failed** (`test_d4_1_labeling_through_the_mandatory_path_is_monotonic`,
  `test_d4_3_the_first_event_time_of_2026_08_23_is_the_bucket_close`,
  `test_label_and_sort_returns_rows_in_event_time_order`), nomeando o índice exato da discordância.
  Restaurado; `git diff` vazio depois.
- **D4.2/D4.4** (gap contado, nunca interpolado): troquei `n_missing=delta // grid_ms - 1` por
  `n_missing=1` fixo em `detect_gaps`. `pytest -k "d4_2 or gap"` → **4 failed**
  (`test_d4_2_exactly_one_gap_with_n_missing_3...`, `test_d4_2_zero_points_interpolated...`,
  `test_three_consecutive_missing_buckets_are_one_gap_not_three`,
  `test_two_separate_gaps_stay_separate`). Restaurado; suíte completa reconferida verde (470
  passed) depois da reversão.
- **D4.3** morre junto do mutante de D4.1 acima (mesmo bypass do sort expõe o primeiro bucket
  errado). Coberto.
- **D4.10**: não mutado (mudaria `LABEL_SHIFT_MS`, usado por toda a suíte); inspecionado o teste
  — Pearson calculado manualmente sobre fixtures reais (`data/binance/metrics/.../2026-08-{21,22,23}`
  + klines), com falsificador explícito no docstring (correção da coluna colapsaria `r_future`
  para perto de zero). Números batem a MESMA assinatura do PRD (`r_future` uma ordem de grandeza
  acima de passado/futuro+1), com `n` e corpus diferentes e declarados como tal — não é cópia.

## CA-F1-14 ("19 disparos vs 0" não reproduzido) — achado real, mas não bloqueia esta task

`tasks.toml id="T-04.1"` lista o DoD literal como **D4.1, D4.2, D4.3, D4.10** — CA-F1-14 aparece
só como `refs`, ao lado de CA-F1-1, sem prefixo `DoD`. No plano (`04_contrato_temporal.md`), o item
4.2 ("ordenação obrigatória") cita `CA-F1-1` e `CA-F1-14` juntas como origem/motivação, mas a ÚNICA
linha da tabela de DoD que opera esse item é `D4.1`, cujo alvo numérico (13/30 dias fora de ordem,
deslocamento máx 275/288, salto ~1435min) **bate exato** com o medido pelo builder e reconferido
aqui via mutação. CA-F1-14 (`pct_change(3)>5%`: 19 disparos vs 0) é uma articulação alternativa do
MESMO defeito (CSV desordenado corrompe leitura), não um número que D4.1 precisa reproduzir
literalmente — nenhuma linha do DoD desta task pede o `19`. **Não é NEEDS_FIX**: a exigência literal
do plano para 4.2 (monotonia + bypass reprova) está coberta e comprovada por mutação. Registrado
como achado aberto para quem cardar/tocar `CA-F1-14` como critério autônomo depois — não é dívida
desta task.

## DoD do plano, item a item

- **4.1** `event_time = create_time+300000`, uma vez, 8 colunas, `src_label_raw` ao lado —
  `metrics_shift.py::label_metrics_row`, coberto por 100% domain + mutação acima.
- **4.2** ordenação obrigatória, bypass reprova — `label_and_sort_metrics_rows`, único ponto de
  saída, mutação mata 3 testes.
- **4.4** `md.ingest_gap` sem interpolação — `detect_gaps` + `SqliteIngestRecordStore` existente,
  mutação mata 4 testes; strings ISO do gap batem byte a byte com dois arquivos de teste
  pré-existentes (`test_ingest_health_query.py`, `test_ingest_health_contract_guards.py`).
- **D4.3** carimbo do FECHO, nunca do início — confirmado (`00:05:00Z` vs `00:00:00Z`), morre no
  mesmo mutante de D4.1.

## Nota herdada de `/review` (T-04.1-review.md, COMPLIANT)

`backend/tests/helpers/data_fixtures.py:39-48` tem 2 mensagens de `raise` em português — superfície
"mensagem de exceção" é lacuna `⏸ NÃO DECIDIDO` explícita em `CLAUDE.md` (dono `/architect`), não
regra bloqueante. Confirmado aqui: são só as 2, em teste, não em produção nova (`metrics_shift.py`
está em inglês).

## Comandos e universo — todos rodados neste ciclo, worktree `/tmp/claude-1002/wt/T-04.1`

```
cd backend && bash scripts/test.sh        # 470 passed, cobertura acima do piso
bash scripts/lint.sh                      # ruff + mypy --strict limpos
bash scripts/boundaries.sh                # 3 kept, 0 broken
harness rules --mode sweep                # rc=0, 0 achado
harness rules --mode sweep --changed-only # rc=0, 0 achado
harness validate --strict                 # rc=0
git diff --stat (mutantes revertidos)     # vazio antes e depois de cada mutação
```
