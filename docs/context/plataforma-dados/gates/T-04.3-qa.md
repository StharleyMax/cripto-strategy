# QA Gate — T-04.3 (CST-30) · PR #51 · Fase 04 · sentimento

**Veredito: APPROVED**

## 0. Anomalia herdada, resolvida antes de auditar

Ao iniciar, `git status --short` acusava um mutante NÃO revertido em
`aggtrade_contiguity.py` (`if delta != 1:` → `if delta != 1 and delta < 1_000_000:`),
diferente do mutante que o relato da sessão anterior dizia ter revertido
(`tick.agg_id` → `tick.transact_time_ms`). Revertido com
`git checkout -- backend/src/modules/sentimento/domain/aggtrade_contiguity.py` antes de
qualquer medição — confirmado limpo depois (`git status --short` vazio,
`git diff --stat` vazio).

## 1. Testes existentes — rodados antes de escrever qualquer teste novo

`bash backend/scripts/test.sh -q` (repetido 2x para confirmar, ambas `rc=0`):
**591 testes** (`poetry run pytest --collect-only -q`, somado por arquivo), 0 falhas.
Cobertura por camada (`ADR-009/D1`): domain **100,0%** (1057/1057, piso 90%), use_cases
**100,0%** (220/220, piso 80%), infra **98,3%** (832/846, piso 70%) — total 99,45%. Números
idênticos aos do builder (`T-04.3-builder.md` §4). Nenhum teste novo foi necessário: a
suíte já espelha o DoD (18 testes sintéticos + 9 sobre fixture real + 4 de borda do
reader).

## 2. Mutação — três mutantes próprios, todos revertidos

Nenhum teste novo criado; em vez disso, ataquei a suíte existente com mutantes que a
sessão anterior não tinha coberto:

- **Mutante A** — `infra/aggtrade_csv_reader.py`: `_AGG_TRADE_ID_INDEX` apontando para
  `"first_trade_id"` em vez de `"agg_trade_id"` (prova "nunca por trade_id" no ponto de
  leitura, não só na forma do tipo). `poetry run pytest tests/sentimento/test_aggtrade_csv_reader.py -q`
  → **3 failed** de 4. Revertido; `git diff` vazio depois.
- **Mutante B** — `domain/aggtrade_contiguity.py`: `require_unique_agg_ids` checando
  `tick.transact_time_ms` em vez de `tick.agg_id` (o mesmo mutante que a sessão anterior
  já tinha provado, reconstruído independentemente). `poetry run pytest
  tests/sentimento/test_aggtrade_contiguity.py -q` → **2 failed**
  (`test_require_unique_agg_ids_raises_on_a_repeated_agg_id`,
  `test_two_ticks_sharing_a_timestamp_are_not_a_duplicate`) — nem precisou da fixture real.
  Revertido; `git diff` vazio depois.
- **Mutante C** — `domain/aggtrade_contiguity.py`: `if delta != 1:` →
  `if delta != 1 and delta < 1_000_000:` (o mutante herdado da sessão anterior,
  reaplicado deliberadamente para medir). **Sobrevive aos 18 testes sintéticos**
  (`poetry run pytest tests/sentimento/test_aggtrade_contiguity.py -q` → 18 passed) — nenhum
  buraco sintético da suíte passa de 1M. **Morre só no fixture real**: `poetry run pytest
  tests/sentimento/test_aggtrade_contiguity_fixtures.py -k d4_4 -q` → **1 failed**
  (`test_d4_4_the_2026_08_22_hole_is_the_only_gap`, `assert 0 == 1`) — é exatamente o
  buraco de 1.620.908 que `D4.4` cita. Achado registrado como risco (não bloqueio): a
  proteção contra "costurar buracos grandes" depende inteiramente do teste de fixture
  real, não há teste sintético equivalente. Revertido; `git diff` vazio depois.

Todos os três mutantes foram revertidos antes de qualquer commit; `git status --short`
confirmado vazio ao final de cada um e ao final da sessão.

## 3. Regras bloqueantes (7 em vigor)

`harness rules list --severity block` → 7 regras. `harness rules --mode sweep` e
`harness rules --mode sweep --changed-only` → `rc=0`, 0 achados em ambos, sobre a árvore
completa e sobre o diff do PR. Nenhuma das 7 regras (`relative-import`, `silent-except`,
`print-statement`, `hardcoded-secret`, `browser-imports-server`, `tenant-from-request`,
`server-test-directory-present`) toca os arquivos deste PR — confirma o já apurado por
`T-04.3-review.md` (auditoria arquitetural, COMPLIANT).

## 4. Contratos de arquitetura

`bash backend/scripts/boundaries.sh` → `Contracts: 3 kept, 0 broken` (a dívida de
`dump_window.py`/`retention_probe.py` contra `master@a3c63c5` que o builder registrou já
não existe na base atual — `T-03.12` mergeou, confirmado também pelo `review`).

## 5. DoD — item a item

- **D4.4** (0 saltos, 0 ts decrescente, 8.873.078 linhas, buraco de 08-22 =
  1.620.908 entre `agg_id` 3420055157 e 3421676065, não costurado): provado por
  `test_d4_4_*` (5 testes) sobre fixture real, e pela morte do Mutante C acima —
  **PROVADO, com ressalva de dependência de fixture** (item 2).
- **D4.5** (até 184 aggTrades/ms, 25,6% dos ms com colisão, `agg_id` único no mesmo
  arquivo que colide por tempo): provado por `test_d4_5_*` (4 testes), incluindo o
  falsificador que roda dedup ingênuo por tempo e mede 1.796.568 trades descartados.
- **"Nunca por tempo, nunca first/last trade_id"**: garantia estrutural (`AggTradeTick`
  não tem campo para `trade_id`) + comportamental (Mutantes A e B acima, ambos mortos).

## 6. Achado não-bloqueante herdado do review

`T-04.3-review.md` já registrou (não-bloqueante, dívida `⏸ NÃO DECIDIDO` de
`CLAUDE.md`/mensagem de exceção): a mensagem de `DuplicateAggIdError` em
`aggtrade_contiguity.py:52-55` está em português. Não é regra bloqueante hoje; QA
concorda com o registro, sem ação adicional.

## Comandos usados

`git status --short`; `git diff --stat`; `git checkout -- <arquivo>` (reversão da
anomalia); `bash backend/scripts/test.sh -q` (x2); `poetry run pytest --collect-only -q`;
`poetry run pytest tests/sentimento/test_aggtrade_csv_reader.py -q`; `poetry run pytest
tests/sentimento/test_aggtrade_contiguity.py -q`; `poetry run pytest
tests/sentimento/test_aggtrade_contiguity_fixtures.py -k d4_4 -q`; `bash
backend/scripts/boundaries.sh`; `harness rules list --severity block`; `harness rules
--mode sweep`; `harness rules --mode sweep --changed-only`.

## Doc-vs-ledger

Este veredito cobre só `T-04.3`. A fase 04 **não fecha aqui**: `tasks.toml` mostra
`status = "todo"` para `T-04.1`, `T-04.3`, `T-04.4`, `T-04.5`, `T-04.6`, `T-04.7` (só
`T-04.2` está `done`), mesmo com `T-04.1`/`T-04.4` já mergeadas (`gh pr list`) — o
desfecho não foi escrito no `tasks.toml` para elas. `T-04.5` (#46), `T-04.6` (#52) e
`T-04.7` (#49) seguem com PR **OPEN**, sem evidência de QA+Review no repositório que eu
tenha encontrado nesta sessão. Não resolvi essas tasks nem fechei a fase — resolvo aqui
somente `T-04.3=done`, que é o escopo deste gate.
