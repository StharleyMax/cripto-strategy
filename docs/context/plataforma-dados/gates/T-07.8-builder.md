[QA GATE — Fase 07: `universe_at(ts, filtro)` com `universe_source` inadmissível POR TIPO]
Feature: `plataforma-dados`     Componente: `sentimento`
Arquivos alterados:
- `backend/src/modules/sentimento/domain/universe_at.py` (new)
- `backend/tests/sentimento/test_universe_at.py` (new — 9 testes)
- `backend/README.md` (modified — seção `## 📎 2026-09-02 por T-07.8`)
- `docs/INDEX.md` (modified — linha append-only)

Spec: `docs/specs/SPEC-001-plataforma-dados.md` §3.7 (tipos-soma, `universe_source`)
Plan: `docs/plans/SPEC-001-plataforma-dados/07_aquisicao_em_regime.md:19,36` (item 7.10, `D7.7`)
Handoff: `docs/context/plataforma-dados/handoff/T-07.8.md`
Referências: `CA-F3-4` (`docs/specs/PRD-001-plataforma-dados.md:623`), `CA-F0-1`
(`docs/specs/PRD-001-plataforma-dados.md:510`, prova que nenhum `ts` anterior a `2026-08-25` tem
snapshot real), `tasks_review.md:341` (linha completa do DoD)

DoD:
- [x] `universe_at(ts, filtro)` implementado como função pura de `domain` — nenhum socket,
  relógio ou arquivo tocado (`ADR-016`, contrato `Natureza`) — `bash backend/scripts/test.sh`
- [x] `s3_inferred` INADMISSÍVEL POR TIPO no caminho de decisão, não por convenção nem
  validação em runtime — `DecisiveUniverseSource = Literal["snapshot", "premium_index_witness"]`
  é o único tipo na assinatura de `decide_universe_membership`; não existe `if fonte ==
  "s3_inferred": raise` em lugar nenhum do módulo — medido diretamente com `mypy --strict`
  (ver "Falsificador exigido pelo handoff" abaixo)
- [x] `D7.7`: `universe_at('2025-08-01')` inclui `ICXUSDT` e exclui `DOSUSDT` (onboard
  `2026-08-11`) — `test_d7_7_includes_icxusdt_and_excludes_dosusdt_with_no_snapshot_available`
- [x] Devolve a UNIÃO das duas testemunhas (snapshot de `exchangeInfo`/`T-02.1` e o witness
  S3-derivado/`T-07.2`) com a divergência marcada — `UniverseAtResult.symbols` é
  `decided_symbols | s3_witness_symbols`, nunca um merge silencioso; `divergence` reusa
  `compare_symbol_sets` (`instrument_universe_snapshot.py`) — nenhuma terceira representação do
  universo foi inventada, por instrução explícita do handoff —
  `test_maticusdt_absent_from_snapshot_but_present_in_s3_witness_is_a_marked_divergence`
- [x] Reuso de `T-02.1`/`T-07.2` sem terceira representação — `InstrumentRow`
  (`instrument_universe_snapshot.py`) é o formato de entrada do witness "snapshot";
  `compare_symbol_sets` é reusado, não reimplementado

Comandos rodados (literais) e resultado:

- `bash backend/scripts/lint.sh` → "All checks passed!" / "216 files already formatted" /
  "Success: no issues found in 216 source files" (universo: `ruff check`+`format --check`+
  `mypy --strict` sobre `src`+`tests`)
- `bash backend/scripts/test.sh` → **rc=0, 1162 passed, 3 warnings, 296,46s** (universo: suíte
  inteira; era 1147 na última medição de `T-07.3`, +15 — 9 são `test_universe_at.py`, as demais
  vieram de `master` no rebase). Cobertura total **97,47%**, piso por camada (`ADR-009/D1`):
  `[OK] domain 99,8% (2304/2308, meta 90%)` · `[OK] use_cases 100,0% (549/549, meta 80%)` ·
  `[OK] infra 95,0% (2166/2279, meta 70%)`, universo 3 de 3 camadas medidas
- `bash backend/.venv/bin/python -m coverage report` (linha de `universe_at.py` isolada, dentro
  do `coverage.xml` da rodada acima) → **100% (40 statements, 6 branches, 0 missing)**
- `harness rules --mode sweep --changed-only` → saída vazia, `rc=0` (nenhuma das 7 regras
  `[BLOQUEIO]` em vigor se aplica: sem import relativo, sem `except` silencioso, sem `print`,
  sem credencial — os dois arquivos desta task são domínio puro sem I/O nenhum)
- `harness rules --mode file --path backend/src/modules/sentimento/domain/universe_at.py` e
  `--path backend/tests/sentimento/test_universe_at.py` → ambos saída vazia, `rc=0`

Falsificador exigido pelo handoff, medido diretamente — `mypy --strict` recusa a atribuição
ANTES de o código rodar:

```
$ cd backend && .venv/bin/python -m mypy --strict <arquivo escrito e apagado nesta sessão,
  fora da árvore versionada, contendo:
    from src.modules.sentimento.domain.universe_at import DecisiveUniverseSource
    bad: DecisiveUniverseSource = "s3_inferred"
    good: DecisiveUniverseSource = "snapshot">
src/.../_scratch_universe_at_typecheck.py:5: error: Incompatible types in assignment
(expression has type "Literal['s3_inferred']", variable has type "Literal['snapshot',
'premium_index_witness']")  [assignment]
Found 1 error in 1 file
```

`[MEDIDO 2026-09-02]`. O arquivo-escrutínio foi criado e apagado na mesma sessão (nunca commitado
— não é fixture, é a prova pontual de que o tipo REJEITA); `good: DecisiveUniverseSource =
"snapshot"` na mesma rodada não gerou erro, confirmando que a recusa é específica ao membro
excluído e não um erro de configuração do `mypy`. A suíte automatizada NÃO invoca `mypy` em
runtime (mesma divisão que `test_provenance_columns.py` já usa para sua própria alegação
`comparison-overlap`: `bash backend/scripts/lint.sh` é o portão que roda isso a cada push); o que
`test_universe_at.py` prova em toda execução é a FORMA de que esse erro depende —
`test_decisive_universe_source_excludes_s3_inferred_by_member_set` (`get_args`) e
`test_structural_falsifier_decide_universe_membership_cannot_spell_s3_inferred` (AST sobre o
corpo executável, com controle: o scanner MORDE um mutante manual que insere a string excluída).

Cobertura: **97,47%** total; `universe_at.py` **100%** (40 linhas, 6 branches) — comando acima.
Meta declarada pela política: domain 90% / use_cases 80% / infra 70% (`harness policy --key
test_cmd`); as três folgam.

Doc delta:
- `backend/README.md`: atualizado — seção nova `## 📎 2026-09-02 por T-07.8`, com os dois tipos
  (`UniverseSource`/`DecisiveUniverseSource`), o comando `mypy` medido, o `D7.7` explicado (por
  que a testemunha S3 é construída e não lida de um arquivo que não existe) e o escopo não
  fechado
- `docs/INDEX.md`: linha append-only acrescentada (nenhuma linha existente reescrita)
- ADR: não necessário — a técnica (Literal estreito excluindo um membro na assinatura da função
  de decisão) é a MESMA que `SurvivorshipVerdict`/`ClosedWindow` já fixam neste componente; não
  há decisão arquitetural nova, só a aplicação do precedente a um terceiro eixo

Bloqueado: nenhum item do DoD desta task ficou pendente. Escopo nomeado como NÃO fechado por
esta task (ver `backend/README.md` §"Escopo que esta task NÃO fecha"):
- não escreve o use_case/CLI que lê um snapshot real do disco e chama `universe_at` — fica para
  quem consumir esta função (`T-07.14`/`T-07.15`, componente `web`, per `tasks_review.md:347`)
- não persiste `onboard_ts`/`deliveryDate` por símbolo em `InstrumentRow` (`T-02.1`) — sem esses
  campos, um `ts` posterior a `2026-08-25` só é decidível com um snapshot real daquele dia
  exato, nunca por interpolação a partir do snapshot de hoje; extensão pertence a `SPEC-001`
  §3.4/`T-02.1`, não a esta task
- não implementa `Q2`/S5 embutido (seletor de símbolo, badge de delisting lido de
  `deliveryDate`, `universe_source` carimbado em toda saída de UI) — plano `07` item 7.15,
  componente `web`

`tasks.toml`, ledger e Jira INTOCADOS; nenhum `gate-record`, `approve` ou `advance` — veredito é
do `/qa`.
