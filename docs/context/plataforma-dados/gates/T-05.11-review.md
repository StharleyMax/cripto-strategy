# REVIEW Gate (Arquitetural) — `T-05.11` / `CST-102` — Scaffold Next real, fase 05, componente `web`

**Papel:** revisor arquitetural (read-only) · **Data:** 2026-09-04
**Ancoragem:** worktree `/tmp/claude-1002/wt/T-05.11`, branch `tasks/T-05.11-scaffold-next`.
Nenhum arquivo de código editado por esta revisão.

## Denominador

- **Regras bloqueantes em vigor:** 8 (`harness rules list --severity block`, packs `core` +
  `web-fullstack` + `own`).
- **Avaliadas:** 8/8, sobre os 16 caminhos do delta (`git status --short | wc -l` = 16: 8
  modificados + 8 novos) via `harness rules --mode sweep --changed-only` (`rc=0`, saída vazia) e
  confirmado arquivo a arquivo com `harness rules --mode file --path <arquivo>` nos 8 arquivos de
  código/config tocados (`painel/page.tsx`, `layout.tsx`, `headless-chart.ts`,
  `s2-axis-integration.test.ts`, `next.config.ts`, `tsconfig.json`, `Makefile`, `scripts/verify.sh`)
  — todos `rc=0`, saída vazia.
- **Camada de arquitetura declarada** (fronteira `charts`↔`web` de `ADR-003`/`D5.12`,
  `frontend/eslint.config.mjs:73-113`): verificada por leitura direta do arquivo e do diff, não só
  citação de gate.

## Achados (checklist do despacho)

1. **[OK] App vive em `frontend/src/app/`, não `frontend/app/`.**
   `find frontend/src/app -type f` lista `layout.tsx`/`painel/page.tsx` entre os arquivos
   pré-existentes; `ls frontend/app` → "Arquivo ou diretório inexistente" (confirmado ao vivo,
   reproduz o item 4 do gate de QA).

2. **[OK] `frontend/src/app/painel/page.tsx` não monta `charts/`.**
   Lido o arquivo inteiro: imports são só `../../features/panel/Filter.tsx`,
   `../../features/s1-console/{S1Console,fixtures,view-model}.ts(x)`,
   `../../features/s3-inspector/{S3Inspector,fixtures,view-model}.ts(x)` — nenhum `charts/`.
   A fronteira segue viva no mecanismo: `frontend/eslint.config.mjs:102-113` tem
   `no-restricted-imports` com `group: ["**/app/**", "**/features/**"]` sobre `files:
   ["src/charts/**/*.{ts,tsx,mts,cts}"]`, citando `ADR-003 FR-1`/`D5.12` no próprio texto da regra.
   `tsc --noEmit --strict` roda limpo (`rc=0`) sobre a árvore como está.

3. **[OK] O fix de `scripts/verify.sh` não introduziu regressão nos outros 5 portões.**
   `git diff scripts/verify.sh` mostra a mudança confinada ao bloco `lint-frontend` (troca de uma
   chamada por duas `portao()` sequenciais); nenhuma linha dos blocos `lint-backend`/`test`/
   `boundaries`/`regras`/`política` foi tocada. Rodado ao vivo (`bash scripts/verify.sh`): os 5
   seguem `[NÃO MEDIU] rc=3` — mesmo padrão que a rodada 2 do QA reproduziu por `git stash -u`
   contra `master@758e2ef` (ambiente sem `backend/.venv`/registro do harness neste worktree,
   pré-existente e fora do escopo desta task, que é só `frontend/`).

4. **[OK] As 7 correções em `headless-chart.ts`/`s2-axis-integration.test.ts` são casts mecânicos.**
   Confirmado lendo `git diff` (não só o gate): `headless-chart.ts` — `(window as unknown as
   Record<string, unknown>).devicePixelRatio = 1` (era atribuição direta, `TS2540` read-only) e
   `candleSeries.setData(workload.candles as unknown as Parameters<typeof
   candleSeries.setData>[0])` / idem para `pointSeries` — os valores passados ao `.setData` não
   mudam, só o tipo estático que o TS enxerga (`number` vs. `Time` de marca). `s2-axis-
   integration.test.ts` — os 4 itens do array passado a `runHeadlessChart` ganham `as unknown as
   readonly Record<string, unknown>[]`, mesmo padrão de cast que `s2-headless-run.ts:129` já usa
   internamente (`spec.items as never`) para o mesmo choque de tipo. Nenhuma linha de lógica,
   valor ou branch mudou nos dois arquivos — só anotação de tipo em posições de chamada.

5. **[WARNING, não bloqueante] `ADR-018` segue `Status: RASCUNHO`.**
   `grep -n '^\*\*Status' docs/adr/ADR-018-*.md` → `RASCUNHO (aprovar é gate do owner)`;
   `harness pipeline show plataforma-dados | grep -i adr-018` → nenhuma linha, sem evento
   `approve` no ledger. **Não encontrei regra id nem trecho de documento que condicione o fechamento
   do código de uma task ao evento `approve` da ADR que a fundamenta** — `CLAUDE.md` reserva a
   ato-de-owner os gates de pipeline (`spec`, `build`, `advance DONE`), não a aprovação de ADR
   isolada, e há precedente vivo no próprio repositório: `ADR-017` está no mesmo estado
   (`RASCUNHO (aprovar é gate do owner)`, `grep -n '^\*\*Status' docs/adr/ADR-017-*.md`) enquanto
   trabalho segue avançando noutras tasks. `ADR-018` não afirma estar aprovada — o rótulo é
   honesto, o que difere do caso que `CLAUDE.md` nomeia ("documento marcado aprovado sem o evento
   no ledger"). **Portanto: não entra como violação no veredito** (sem id de regra, sem citação de
   documento que a proíba) — mas fica registrado, porque é exatamente o tipo de pergunta que só o
   owner responde, e a resposta objetiva é "não precisa de approve prévio da ADR para esta task
   fechar, salvo declaração em contrário do owner".

## Veredito: **COMPLIANT**

0/8 regras bloqueantes violadas, avaliadas 8/8 sobre 16 caminhos do delta
(`harness rules --mode sweep --changed-only`, `rc=0`, saída vazia; confirmado arquivo a arquivo).
Nenhuma violação de fronteira arquitetural declarada (`ADR-003`/`D5.12`) encontrada — confirmado
por leitura do código, não por citação do gate de QA. O único achado não-BLOCKER (`ADR-018`
`RASCUNHO`) é `[WARNING]` sem id de regra nem citação de documento que o proíba; tem precedente
igual em `ADR-017`, vivo no mesmo repositório hoje.

