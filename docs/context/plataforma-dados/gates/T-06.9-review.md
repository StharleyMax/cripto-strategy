# Review Gate — T-06.9 · as quatro séries de preço, `implied_avg_price` PROIBIDO, `fee_schedule` datada, `cvd_source` com erro medido

**Fase:** 06 · **Componente:** `sentimento` · **PR:** https://github.com/StharleyMax/cripto-strategy/pull/100
**Branch:** `tasks/T-06.9-fase06` · **Worktree:** `/tmp/claude-1002/wt/T-06.9` · **Diff:** `git diff master...HEAD` (three-dot, master avançou com T-06.2/T-06.5)

## Escopo lido

`ADR-007` inteiro, handoff `T-06.9.md` inteiro, `docs/context/plataforma-dados/gates/T-06.9-qa.md` inteiro, os 3 módulos `domain` novos (`price_source_catalog.py`, `fee_schedule.py`, `cvd_source_catalog.py`), as 3 suítes de teste, `series_catalog.py`/`series_key.py` (mecanismos reutilizados), diffs de `docs/INDEX.md`/`backend/README.md`.

## Camada 1 — o runner mede

`harness rules list --severity block` → **7 regras em vigor** (4 `core.*` + 3 `web-fullstack.*`).
`harness rules --mode file --path <arquivo>` rodado individualmente nos 6 arquivos novos (3 domain + 3 teste): **rc=0, saída vazia em todos os 6** — reproduzido nesta sessão, não só citado do QA. Sanity check do próprio comando: um arquivo-lixo com `print("x")` sob `backend/src/...` foi detectado (`rc=2`, `core.print-statement`), confirmando que ausência de achado é sinal real, não silêncio do instrumento.
As 3 regras `web-fullstack.*` são `N/A` — componente é `sentimento`, não `web`. **Regras bloqueantes avaliadas: 4 de 7** (universo do componente).

`bash backend/scripts/natureza.sh` → `73 arquivo(s), 0 leitura(s) de relogio` `[MEDIDO, reproduzido nesta sessão]`.
`bash backend/scripts/boundaries.sh` → `Contracts: 3 kept, 0 broken` (159 arquivos, 721 dependências) `[MEDIDO, reproduzido nesta sessão]`, incluindo o contrato "domain e use_cases nao falam com socket nem ssl".
Suite dos 3 módulos novos isolada (`--no-cov`): `42 passed in 0.16s` `[MEDIDO, reproduzido nesta sessão]` — bate com os `+42` do builder/QA.

## Camada 2 — arquitetura declarada

- **`Natureza`/`ADR-016` (domain sem I/O):** `grep -nE "^import|^from"` nos 3 módulos → só stdlib (`types`, `typing`, `dataclasses`, `datetime`, `decimal`, `enum`) + `series_catalog.py`/`series_key.py` (irmãos de `domain`). Nenhum `socket`/`ssl`/cliente HTTP/arquivo. `date` usado só como tipo-valor (`effective_from`, `measured_on`), nunca `.today()`/`.now()`. **Conforme.**
- **`implied_avg_price` PROIBIDO — grep real, não só relato do QA:** `grep -rn "implied_avg_price" backend/` → 8 ocorrências, todas em `FORBIDDEN_METRIC_NAMES` (`series_key.py:60`), no guard (`series_key.py:187`), e em comentário/docstring/teste explicando a proibição. **Nenhuma** instancia `SeriesKey(metric="implied_avg_price", ...)` fora de um `pytest.raises`. `price_mark_close` é o nome real cadastrado para o conceito `mark_price`. **Conforme com `ADR-007`/PS-2.**
- **`price_use` reusa `PRICE_USES`/`InvalidPriceUseError`:** `price_source_catalog.py` importa os dois de `series_catalog.py` — `grep -rln "class InvalidPriceUseError\|class PublishedError\b" backend/src/` devolve **um único arquivo** (`series_catalog.py`). Nenhuma validação paralela. **Conforme.**
- **`PublishedError`/guard `reconstructed_from` reusado:** `cvd_source_catalog.py` importa `PublishedError` de `series_catalog.py`; a recusa de reconstrução sem erro publicado é o `__post_init__` de `SeriesCatalogEntry` (`series_catalog.py`), não reimplementado. QA já provou por mutação que as duas suítes (`test_series_catalog.py` de `T-06.1` e `test_cvd_source_catalog.py` desta task) caem JUNTAS quando o guard único é neutralizado — evidência de mecanismo único, não duplicado. **Conforme.**
- **`coinalyze_bv` valores e `TailCause`:** `median_bp=0.0000`, `p99_bp=29.34`, `max_bp=1955.80`, `n=699`, `measured_on=2026-08-24` batem literalmente com o handoff. `TailCause` é `Enum` com **exatamente 1 membro** (`NOT_DIAGNOSED`) — a hipótese "maker" refutada a `2584.87` bp é registrada em `RefutedTailHypothesis`, separada do `tail_cause`, e **não existe membro do enum para afirmá-la como causa** — impossibilidade estrutural, não só convenção. **Conforme.**
- **Idioma inglês (produção e teste):** nomes de módulo, classe, função, todos em inglês. Varredura AST das chamadas `*Error(...)`/`*Exception(...)` nos 3 módulos domain não encontrou nenhuma mensagem com marcador de português. Nomes de teste (`def test_...`) nas 3 suítes sem termo em português. Comentários/docstrings citam termos de `ADR-007`/`SPEC-001` em português entre crases (citação literal de documento, não identificador) — mesmo padrão já usado em `series_catalog.py`. **Conforme com a fronteira da tabela (linha 1/2/5) e com a decisão de 2026-09-02 sobre mensagens de exceção.**
- **`docs/INDEX.md`/`backend/README.md` append-only:** `git diff master...HEAD` mostra, nos dois arquivos, **apenas linhas adicionadas ao final** — nenhuma linha pré-existente alterada ou removida. **Conforme.**

## Achados

Nenhum achado bloqueante, de convenção ou informativo digno de nota. Escopo não fechado nesta task (`index_price`/`premium_index` sem linha de catálogo, `QF-5` `[NÃO MEDIDO]` para `aggtrade_q`/`aggtrade_nq`, `fee_schedule` sem entrada real, `PS-3`/`<Anotacao>` fora do componente `sentimento`) está nomeado no próprio código/README, com justificativa que confere com `ADR-007` e o handoff — não é lacuna silenciosa.

## Denominador

- Regras bloqueantes existentes: **7** (`harness rules list --severity block`).
- Avaliadas como aplicáveis ao componente `sentimento`: **4** (as 3 `web-fullstack.*` são `N/A`).
- Arquivos varridos com `harness rules --mode file`: **6** (3 domain + 3 teste), individualmente, `rc=0` em todos.
- Comandos de arquitetura reproduzidos nesta sessão (não só citados do QA): `natureza.sh`, `boundaries.sh`, suite isolada dos 3 módulos novos (42 testes), greps estruturais de `implied_avg_price`/reuso de classes/mensagens de erro.

## Veredito

**COMPLIANT** — nenhuma regra bloqueante violada, nenhuma violação de arquitetura declarada citável.
