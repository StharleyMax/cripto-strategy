# `T-05.14` — decisão estrutural do `architect` (`web`), antes do `/build`

**Papel:** `architect` (`web`, `A6`) · **Data:** 2026-09-04 · **Task:** `T-05.14` / `CST-105`
**Decisão normativa:** [`ADR-019`](../../../adr/ADR-019-cliente-http-de-ingest-health-e-paridade-de-fingerprint.md)
— este arquivo é o ponteiro, não uma segunda decisão.

**Ancoragem:** `master@56b866c` (inclui `T-05.12`/`T-05.11`), worktree isolado
`/tmp/claude-1002/wt/T-05.14`, branch `tasks/T-05.14-cliente-rota-real`. Só leitura nesta sessão —
nenhum commit, nenhum arquivo de produção tocado. Li: `frontend/src/app/history-transport.ts` (338
linhas), `frontend/src/features/s1-console/ingest-health-query.ts` (501 linhas) e seu teste (256+
linhas), `docs/adr/ADR-005-transporte-de-leitura.md` §D6 completo (linhas 124-260),
`backend/src/modules/sentimento/domain/ingest_record.py` (236 linhas, `IngestHealthReport`/
`_project_run_dict`/`_project_gap_dict`), `backend/src/api/routes/ingest_health.py`, e
`backend/tests/api/test_ingest_health_route_over_the_network.py` (o idioma de servidor real —
`uvicorn.Server` numa thread `daemon`, porta `0` — que `ADR-019/D5` reusa para o lado TS).

## O que decidi, em uma frase por eixo

1. **Mesmo arquivo, sem módulo novo**: `ingest-health-query.ts` ganha `parseIngestHealthEnvelope` +
   `fetchIngestHealthProjectionViaHttp`; perde `SectionMarker`/`isHeaderLine`/
   `parseCanonicalProjection` (NDJSON morreu com `D6.1`) e `fetchIngestHealthProjectionViaCli`/
   `IngestHealthQueryResult` (única chamadora do parser morto). `runIngestHealthCli` + o transporte
   por subprocesso ficam intocados — remoção forçada é `T-05.15`, não esta task.
2. **`fingerprint(): string` continua síncrona** (`ADR-005/D6.4`) — só a borda de I/O
   (`fetchIngestHealthProjectionViaHttp`) é `async`; canonicalização e hash, inalteradas.
3. **URL base**: `process.env.INGEST_HEALTH_API_BASE_URL` (nunca `NEXT_PUBLIC_*`, nunca literal) —
   confirmei que `harness rules --mode file` continua sem `[AVISO]` (regra é sobre string literal
   `http(s)://`, não sobre `process.env`).
4. **Paridade** (`F-D6-1`/`F-D6-2`): arquivo de teste novo,
   `ingest-health-query-http.test.ts`, subindo `uvicorn` real via `spawn` (não `spawnSync` — precisa
   ficar vivo), com variantes monkeypatchadas (`INGEST_HEALTH_RUN_COLUMNS` reordenada; `_project_run_dict`
   com campo extra; `to_envelope()` com campo extra na raiz) só no processo de teste — nunca edita
   `backend/src/`. Forma exata dos quatro cenários (CALA + 3 controles) em `ADR-019/D5`.
5. **Fora do escopo desta task, registrado, não decidido**: `DoD-3` sobre HTTP; wiring em
   `painel/page.tsx`/`S1Console.tsx` (restrição que vincula quem o fizer: nunca `"use client"`).

## Falsificador

Tabela G1-G4 em `ADR-019` — cada um nomeia o comando e o par CALA/MORDE. Nenhum comando foi
executado nesta sessão (decisão de estrutura, não implementação); `/build` é quem os roda pela
primeira vez.
