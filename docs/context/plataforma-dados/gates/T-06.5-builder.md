# Gate — `T-06.5` (`CST-49`, componente `sentimento`, fase `06`, plano item 6.11)

## Escopo

`CA-F2-17`: popular `reduction` para Open Interest — 4 linhas de `series_catalog` para a
Coinalyze (`OPEN`/`HIGH`/`LOW`/`CLOSE`, `OHLC_OVER_BUCKET`) + 1 para a Binance (`POINT`,
`POINT_AT_BUCKET_END`). `T-06.1` (mesclada) já havia construído o contrato — o termo
`reduction` na `SeriesKey` e o enum `Reduction` com os seis membros. Esta task não toca em
nenhum dos dois; ela adiciona a POPULAÇÃO de produção que faltava.

## Arquivos alterados

- `backend/src/modules/sentimento/domain/open_interest_catalog.py` (novo) — três funções de
  produção: `coinalyze_open_interest_key(reduction, *, instrument_id=...)` (parâmetro
  `reduction` posicional obrigatório, sem default — o falsificador `D6.7` na própria
  assinatura), `binance_open_interest_key(*, instrument_id=...)` (sempre `POINT`), e
  `open_interest_catalog_entries(instrument_id=...)` (as cinco linhas, via
  `build_series_catalog` de `T-06.1`).
- `backend/tests/sentimento/test_open_interest_catalog.py` (novo, 10 testes) — exercita a
  população de PRODUÇÃO diretamente, não as fixtures locais de `test_series_identity.py`/
  `test_series_catalog.py` (essas continuam intocadas e continuam passando).
- `backend/README.md` (modificado) — seção `## 📎 2026-09-03 por T-06.5`.
- `docs/INDEX.md` (modificado) — uma linha, append-only.

## DoD

- [x] `D6.7` — pedir a chave da Coinalyze sem `reduction` é erro, nunca default silencioso.
      Verificado por `test_asking_for_the_coinalyze_open_interest_key_without_reduction_is_refused`
      (`pytest.raises(TypeError, match="reduction")` sobre
      `coinalyze_open_interest_key()` chamada sem argumento).
- [x] `D6.8` — citado como medição já feita em `CST-4`/`SPEC-001` §2.1 (1,86 bp mediana / 9,46
      bp p99, n=1.706), não recalculado por esta task (fora do "Não faz" do plano `06`: "não
      reconcilia automaticamente"). Usado para justificar `label_shift = +interval` (e não
      zero) e para confirmar que são 4 identidades, não 3 — verificado por
      `test_the_four_coinalyze_keys_and_the_one_binance_key_are_five_distinct_identities`.
- [x] As 4 linhas Coinalyze + 1 linha Binance existem como `SeriesCatalogEntry` de produção,
      validadas via `build_series_catalog` (invariante "UMA linha por `SeriesKey`" de
      `SPEC-001` §3.3 aplicada, não assumida) — `test_the_catalog_has_five_rows_...`.
- [x] Reuso de `Reduction`/`series_catalog.py` de `T-06.1` — nenhum enum novo, nenhum membro
      novo (confirmado por leitura de `series_key.py` antes de escrever: os 6 membros já
      existiam com o docstring citando esta mesma medição).

## Comandos rodados (literais) e resultado

- `bash backend/scripts/test.sh` → **1289 passed, 3 warnings in 670.16s**; `Total coverage:
  97.62%`; piso por camada (`ADR-009/D1`): domain **99.8%** (2517/2521, meta 90%), use_cases
  **100.0%** (585/585, meta 80%), infra **95.1%** (2208/2321, meta 70%) — 3 de 3 `[OK]`.
  Universo: 1289 testes coletados (era 1277 em `T-06.1`; +12 novos — 10 no arquivo novo, 2 a
  mais do que os testes do próprio módulo por conta de parametrize). Rodado 2× (antes e depois
  do ajuste de docstring de módulo); ambas as rodadas verdes, números idênticos.
- Universo do módulo novo isolado: `python3 -c "..." backend/coverage.xml` →
  `modules/sentimento/domain/open_interest_catalog.py line-rate=1 branch-rate=1` (100%/100%).
- `bash backend/scripts/lint.sh` → `All checks passed!` / `235 files already formatted` /
  `Success: no issues found in 235 source files` (`ruff check` + `ruff format --check` +
  `mypy --strict`).
- `bash backend/scripts/boundaries.sh` → `Contracts: 3 kept, 0 broken.` (156 arquivos, 707
  dependências analisadas).
- `bash backend/scripts/natureza.sh` → `natureza: universo de 71 arquivo(s), 0 leitura(s) de
  relogio` (era 70 em `T-06.1`; +1 pelo módulo novo).
- `harness rules --mode sweep --changed-only --format ndjson` (arquivos staged) → **1ª
  passada: 1 achado WARN** (`core.module-docstring-single-line`, docstring de módulo
  multi-linha em `open_interest_catalog.py`). Corrigido (docstring de uma linha + comentário
  `#`, mesmo estilo de `series_catalog.py`/`quarantine_terms.py`). **2ª passada: saída vazia,
  `rc=0`** — 0 achados, nenhum de severidade `block`.

## Cobertura

97,62% total (`bash backend/scripts/test.sh` acima); alvo declarado 70% global + piso por
camada 90/80/70 (`ADR-009/D1`) — todas as 3 camadas `[OK]`. Módulo novo: 100%/100% linha e
branch.

## Doc delta

- `backend/README.md`: seção nova `## 📎 2026-09-03 por T-06.5 — reduction populado: OI da
  Coinalyze = 4 linhas, Binance = 1` — peça, falsificador e comandos citados acima, mais o
  escopo que a task não fecha (persistência, quarentena, reconciliação automática).
- `docs/INDEX.md`: uma linha nova, append-only, apontando para a seção acima.
- ADR: não necessário — nenhuma decisão arquitetural nova; reuso integral do contrato de
  `T-06.1` (`ADR-016`/`Natureza` já cobre a camada `domain` deste módulo).

## Bloqueado

Nenhum. `D6.7` e `D6.8` fechados dentro do escopo desta task (a medição de `D6.8` é citação de
`CST-4`, não recálculo — o "Não faz" do plano `06` exclui reconciliação automática). Fora de
escopo, nomeado, não bloqueante: persistência de `SeriesCatalog` (nenhuma task da fase 06 a
cobre ainda) e a quarentena pelo predicado de três termos (`T-06.6`, em paralelo).
