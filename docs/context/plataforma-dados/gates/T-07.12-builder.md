# T-07.12 — relatório do builder

**Componente:** `web` · **Fase:** 07 · **Data:** 2026-09-02 · **Worktree:**
`.claude/worktrees/agent-add747ce422d4dfa7` (branch `worktree-agent-add747ce422d4dfa7`)

## 0. Handoff citado no despacho não existe — mesmo achado que o `ui-designer` já registrou

`docs/context/plataforma-dados/handoff/T-07.12.md` **não existe** no worktree
(`find docs/context/plataforma-dados/handoff -iname '*T-07.12*'` → 0 resultado). O gate de
design (`T-07.12-design.md` §0) já registrou o mesmo achado antes de mim. Segui o despacho
mesmo assim, reconstruindo contexto de: `T-07.12-design.md`, `T-07.12-ux-critique.md`,
`STITCH_CONTEXT.md` §4.2, `tasks.toml:945-952`, `plano 07` (`07_aquisicao_em_regime.md`), e
os precedentes `T-05.8`/`T-05.9` (handoffs + `frontend/README.md` §8/§9). `[NÃO SEI]` sobre
por que o handoff não foi escrito — registro, não resolvo.

## 1. Decisão de escopo: sem página React — a infraestrutura não existe, e não é desta task

`frontend/README.md` §1 e `frontend/src/app/routes.ts` já registravam, ANTES desta task, que
o pacote `frontend/` não é a aplicação Next.js — isso pertence a "outra task"
`[DOC: frontend/package.json:5, frontend/README.md linha 3, routes.ts linhas 11-12]`.
Confirmado: `grep -c '"react"' frontend/package-lock.json` → **0**; nenhum `tsconfig.json`
no pacote. Implementei a `S1` como o mesmo padrão que `T-05.8`/`T-05.9` estabeleceram —
módulo TS puro, testado por mutação, mais um componente TSX presentacional no MESMO nível
lint-only que `src/features/panel/Filter.tsx` já ocupava (linta, não roda) — em vez de
escalar escopo para scaffold de uma aplicação inteira.

## 2. O que foi implementado

4 arquivos novos em `frontend/src/features/s1-console/` + 2 arquivos de teste + 1 linha em
`package.json` + `frontend/README.md` §10 (doc delta):

- **`domain.ts`** — `RetentionWindow` (union de 6 variantes: `computed_uniform`,
  `measured_sparse`, `doc_only`, `declared_constant`, `unmeasured`, `not_applicable`),
  `computeUniformWindowDays` (fórmula `D7.12`), `DECLARED_SLO_TRAIL_MULTIPLIER` +
  `resilienceMultiplierFromWindows` (`D7.13`), `CollectorStatus` +
  `NEUTRAL_STATUS_BADGE_CLASS` + `badgeClassForStatus` + `orderRowsBySeverity` (`D17`),
  `StorageBudgetLine` + `totalStorageBudgetGbPerDay`, `ReconnectionEvent` (`D7.15`, sem
  campo de severidade por TIPO).
- **`fixtures.ts`** — dado sintético com proveniência comentada linha a linha (ver §4, GAP).
- **`view-model.ts`** — formatação pt-BR (`formatPtBrThousands`/`formatPtBrDecimal`,
  determinísticos, sem `Intl`/locale de runtime — `Q14`), `retentionCellText`,
  `resilienceCellText`, `buildS1ViewModel`.
- **`S1Console.tsx`** — tradução estrutural do HTML/Tailwind da tela canônica (mesmas
  classes, mesma hierarquia), tipada contra `S1ViewModel`.

## 3. Falsificadores rodados — não só "os testes passam"

- **`D7.14`**: `computeUniformWindowDays(3052, 1)` dá ≈2,1 dias — o teste PROVA que a
  fórmula uniforme NÃO reproduz os 8 dias publicados para a série esparsa de liquidação,
  que é exatamente por que `measured_sparse` existe como variante separada de
  `computed_uniform` em vez de forçar uma fórmula sobre uma série sem cadência regular.
- **`D17`**: `badgeClassForStatus` sobre as 4 statuses converge para 1 classe (`Set.size ===
  1`) — uma segunda classe ali seria cor-por-severidade. `orderRowsBySeverity` sobre um
  fixture EMBARALHADO (o `PARADO` não é o primeiro elemento) prova que a posição no topo é
  produzida por código, não por acidente de ordenação do fixture.
- **orçamento aritmético**: `totalStorageBudgetGbPerDay` sobre as 3 linhas canônicas bate
  com `"1.6 GB"` (soma, não número solto); um segundo teste muda uma linha e prova que o
  total muda junto (não está hard-coded).

## 4. GAP registrado: dado é fixture, `ingest_health_query` é `T-07.13`

Nenhum valor vem de banco. `S1` lê pela consulta nomeada `ingest_health_query`
(`ADR-008/D3`, `DoD D7.17`) — wiring é `T-07.13`, task dependente e SEPARADA
(`tasks.toml:954-962`). Duas notas registradas em `fixtures.ts` e `README.md` §10:
`OI · grade 5m` usa `2016` pontos (não o `"~2.000"` textual do plano) para que a fórmula
bata EXATAMENTE em `7,0` dias — divergência deliberada da microcopy literal, documentada,
não escondida. E a tela aprovada mistura separador decimal (vírgula na coluna de janela,
ponto em uptime%/GB) — reproduzido fielmente, registrado como achado de design system, fora
do `DoD` desta task.

## 5. Comandos rodados (literais) e resultado

```
npm --prefix frontend run test:s1
  -> 26 pass, 0 fail  [universo: 26 testes, 2 arquivos: domain.test.ts + view-model.test.ts]
npm --prefix frontend run lint
  -> eslint src, 0 erro, 0 aviso  [universo: todo frontend/src, hoje 15 arquivos]
npm --prefix frontend run test:app
  -> 42 pass, 0 fail  [regressão pré-existente, T-05.8/T-05.9]
npm --prefix frontend run test:charts
  -> 13 pass, 0 fail  [regressão pré-existente, T-08.2]
make lint-frontend
  -> rc=0
harness rules --mode sweep --changed-only --format ndjson
  -> saída vazia, rc=0  [universo: 8 arquivos staged desta task]
harness rules --mode sweep --format ndjson
  -> 2 achados pré-existentes (severidade warn, web-fullstack.hardcoded-url, em
     history-transport.test.ts:21 e knowledge-time-bundle.test.ts:22, de T-05.8/T-05.9),
     0 achado novo, 0 achado de severidade block
harness code-paths classify frontend/src/features/s1-console/domain.ts       -> producao
harness code-paths classify frontend/src/features/s1-console/S1Console.tsx   -> producao
make boundaries
  -> rc=3, "RECUSA: backend/.venv nao existe" — portão de backend/, fora da fronteira desta
     task (⛔ não criar/editar backend/), nada em frontend/ depende dele
```

**`harness policy --key test_cmd`** só declara `sentimento` — `[test_cmd.web]` continua não
declarado, mesmo motivo já registrado em `frontend/README.md` §4/§7/§8/§9: `test_cmd` não é
lido por portão nenhum, é ato do dono do componente. Os comandos acima são os que
`builder`/`qa` rodam hoje, precedente de `T-05.8`/`T-05.9`.

## 6. Cobertura

Sem piso declarado para `web`. Qualitativa: 26 testes cobrem 100% das funções exportadas de
`domain.ts` e `view-model.ts`.

## 7. Doc delta

`frontend/README.md` — **atualizado**, `§10` nova (append-only, nenhuma linha reescrita).
`docs/plans/SPEC-001-plataforma-dados/07_aquisicao_em_regime.md` — sem mudança (normativo).
`docs/product/STITCH_CONTEXT.md` — sem mudança (edição ali é ato do `ui-designer`, `R6`).

## 8. Bloqueado

Nenhum item bloqueado. `make boundaries` não rodou (rc=3, backend/.venv ausente) mas está
fora da fronteira desta task por instrução explícita do despacho.
