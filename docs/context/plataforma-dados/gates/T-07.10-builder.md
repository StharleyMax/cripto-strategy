# Gate — `T-07.10` (builder) · `clock_skew_tolerance_ms` CALIBRADO, RECUSADO hoje

`CA-F3-13`, plano `07` item 7.12, DoD `D7.18`. Card `CST-64`, componente `sentimento`. Handoff:
[`docs/context/plataforma-dados/handoff/T-07.10.md`](../handoff/T-07.10.md).

## O que o DoD pedia, e por que "recusar" é a entrega

`D7.18` pede a distribuição de `clock_skew_ms` por `ingest_run` sobre `>= 7 dias de runs`. Isso
não existe ainda: `T-03.8` mediu 5 corridas de probe curto (~6,6 s de span total), não 7 dias de
captura contínua (owner roda local/probe-curto por decisão, `docs/decisoes-do-owner.md` §Q19). O
handoff é explícito que o padrão certo (`T-03.3`/`D3.6`, `T-03.6`) é construir a MECÂNICA agora,
prová-la com distribuição SIMULADA rotulada como tal, e fazer o mecanismo RECUSAR calcular sobre
dado insuficiente — nunca fabricar os 7 dias.

## Arquivos alterados

- `backend/src/modules/sentimento/domain/clock_skew_tolerance.py` (new)
- `backend/src/modules/sentimento/use_cases/calibrate_clock_skew_tolerance.py` (new)
- `backend/src/modules/sentimento/infra/clock_skew_tolerance_reader.py` (new)
- `backend/src/modules/sentimento/infra/clock_skew_tolerance_cli.py` (new)
- `backend/tests/sentimento/test_clock_skew_tolerance.py` (new, 9 testes)
- `backend/tests/sentimento/test_calibrate_clock_skew_tolerance.py` (new, 3 testes)
- `backend/tests/sentimento/test_clock_skew_tolerance_reader.py` (new, 4 testes)
- `backend/tests/sentimento/test_clock_skew_tolerance_cli.py` (new, 5 testes)
- `backend/README.md` (doc delta: seção `## 📎 2026-09-02 por T-07.10`)
- `docs/INDEX.md` (append)

## DoD

- [x] Reusa `domain/clock_skew.py`/`use_cases/persist_ntp_skew_run.py` sem duplicar
      `ClockSkewSample` — `ClockSkewObservation` é um tipo distinto (valor já computado +
      timestamp), não uma reimplementação de `.skew_ms()`.
- [x] Tolerância CALCULADA por percentil (`p99` de `|clock_skew_ms|`, reusando
      `availability_lag_stats.p99`, nunca constante hardcoded).
- [x] Mecanismo RECUSA quando `n`/span é insuficiente, com mensagem explícita em inglês
      (`InsufficientClockSkewCalibrationDataError`).
- [x] Falsificador: alimentado com os 5 `clock_skew_ms` REAIS de `T-03.8`
      (`-69,-69,-73,-66,-23`, `started_at` reais de
      `docs/context/plataforma-dados/medicoes/T-03.8-ntp-skew/01_ingest_health_query.jsonl`),
      REPROVA — `test_refuses_on_the_5_real_t038_probe_runs` e
      `test_report_on_the_real_5_run_t038_store_refuses`.
- [x] Distribuições usadas para provar a fórmula são SIMULADAS e rotuladas como tal no teste
      (`_simulated(...)`, docstrings com `SIMULATED:` explícito) — nunca 7 dias inventados.
- [x] `Natureza` (`ADR-016`): domain/use_cases não leem relógio; o parse ISO→epoch-ms mora em
      `infra/clock_skew_tolerance_reader.py`.

## Comandos rodados (literais) e resultado

- `bash backend/scripts/lint.sh` → `All checks passed!` / `188 files already formatted` /
  `Success: no issues found in 188 source files` (ruff + mypy --strict, universo: todo
  `backend/src` + `backend/tests`).
- `bash backend/scripts/test.sh` → **1049 passed**, 0 failed, 3 warnings pré-existentes
  (`ResourceWarning` em `test_sqlite_series_quarantine_store.py`, não desta task), `468.62s`.
  Cobertura por camada (`ADR-009/D1`): **domain 99,9%** (meta 90%, 2007/2009 linhas) ·
  **use_cases 100,0%** (meta 80%, 549/549) · **infra 95,2%** (meta 70%, 1833/1925). Universo:
  suíte completa do componente `sentimento`, não só os arquivos novos.
- `grep -c '^def test_' backend/tests/sentimento/test_clock_skew_tolerance.py
  backend/tests/sentimento/test_calibrate_clock_skew_tolerance.py
  backend/tests/sentimento/test_clock_skew_tolerance_reader.py
  backend/tests/sentimento/test_clock_skew_tolerance_cli.py` → `8`, `3`, `4`, `5` (**20 testes
  novos** desta task; a suíte-base de `master@031e007` não foi rerodada em separado para uma
  comparação direta de delta).
- `bash backend/scripts/boundaries.sh` → `Contracts: 3 kept, 0 broken` (import-linter, 129
  arquivos, 597 dependências) — as duas camadas + a fronteira de contexto + `Natureza`
  socket/ssl.
- `bash backend/scripts/natureza.sh` → `natureza: universo de 57 arquivo(s), 0 leitura(s) de
  relogio` (domain: 40 arquivos, use_cases: 17 arquivos).
- `harness rules --mode sweep --changed-only` → `rc=0`, saída vazia (0 achados bloqueantes).

## Cobertura

99,9% domain / 100,0% use_cases / 95,2% infra sobre a suíte completa (universo: 1049 testes,
188 arquivos fonte) — acima dos alvos 90/80/70 declarados em `test_cmd`/`ADR-009/D1`.

## Doc delta

- `backend/README.md`: seção nova `## 📎 2026-09-02 por T-07.10 — clock_skew_tolerance_ms
  CALIBRADO, RECUSADO hoje por falta de 7 dias`, com as peças por camada, o falsificador sobre
  dado real e o escopo que a task não fecha (calibração real, decisão de alarme/UI).
- `docs/INDEX.md`: linha append-only para `T-07.10`.
- ADR: não necessário — nenhuma decisão arquitetural nova, só aplicação de `ADR-008/D3` (schema
  `md.ingest_run`) e `ADR-016` (Natureza) já existentes.

## Bloqueado

Nenhum item do DoD desta task ficou bloqueado. **Fora do escopo desta task, nomeado
explicitamente**: a calibração REAL de `clock_skew_tolerance_ms` depende de `>= 7 dias` de
captura contínua que ainda não existe (`Q19`, VPS fora por decisão do owner) — quando existir,
`infra/clock_skew_tolerance_cli.py` roda sobre o mesmo store sem mudança de código.

**`tasks.toml`, ledger e Jira INTOCADOS; nenhum `gate-record`, `approve` ou `advance`** — veredito
é do `/qa`.
