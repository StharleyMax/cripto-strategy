# QA Gate — T-01.8 (fase 01, componente docs) — CST-100

Checkout: `/home/stharley/Documentos/projects/cripto-strategy`, branch
`tasks/T-01.8-juizes-e-deploy`, commit `53801a9`. Verificação executada em `2026-09-04` sobre o
checkout real (não repetição do texto do commit).

## 0. Reconfirmação da auditoria independente citada

`docs/context/plataforma-dados/gates/fase-01-qa-2026-09-03.md`, Eixo 1 (`T-01.9`, 6 mutações
sobre bancada isolada em `.../scratchpad/bench`, HEAD `e80645e`, árvore real `git status
--porcelain` vazia antes/depois). **M6 — só o VALOR de `infra.architect` trocado, a chave fica —
morde**: `check-agents-by-component.sh` → `rc=1`, `agents.by_component.infra.architect —
esperado '.claude/agents/infra-architect.md', obtido '.claude/agents/quant-architect.md'`; e
`harness validate --strict` fica em `rc=0`, silêncio (não é o instrumento que protege isto).
Reconfirmo o achado por leitura direta do arquivo — não repeti a bancada isolada, mas rodei o
comparador real desta sessão (item 4 abaixo) sobre o estado atual e ele bate com o dourado, o que
é consistente com M6 ter sido corrigida no commit em avaliação.

## 1. Frontmatter dos 4 arquivos novos, e "criação vs. porte"

```
wc -l .claude/agents/{quant-architect,ui-designer,infra-architect,frontend-architect,frontend-builder,frontend-qa}.md
→ 76 / 101 / 118 / 122 / 155 / 134
```

- `infra-architect.md` e `frontend-architect.md`: frontmatter válido (`name`, `description`,
  `tools`). Comparados por diff de conjunto de linhas contra `quant-architect.md` e
  `ui-designer.md`: **150–169 linhas divergentes de 76–169 possíveis em cada par** — nenhuma
  reutilização de prosa. O único `architect.agent.md` do `anything_monorepo` (12 agents:
  3 `.claude/agents/` + 9 `.github/agents/`, confirmado por `find`) é genérico —
  `description: 'Solutions Architect Sênior. Valida PRDs (Gap Analysis)... Read-only no
  Jira/Confluence'` — e não tem nada de infra ou de front. **Criação, não porte, confirmado.**
- `frontend-builder.md` / `frontend-qa.md`: **porte declarado no próprio corpo** ("Porte de
  `frontend_builder.agent.md` (82 linhas) do `anything_monorepo`, adaptado aos portões... O que
  mudou está declarado onde mudou — não foi copiado com o nome trocado"). `diff` das primeiras 12
  linhas contra o original mostra description, tools, título e filosofia todos reescritos para o
  vocabulário/portões daqui (ex.: `node --test` em vez de Vitest/Playwright fixo, "Devolve o QA
  Gate Context Block — não despacha QA, não aprova e não avança estado" em vez do original). Não é
  cópia cega.

## 2. `harness validate --strict`

```
$ harness validate --strict
politica valida: cripto-strategy (schema_version=1)
```
Limpo, `rc=0`, sem os novos ponteiros gerando aviso nem erro.

## 3. Nenhum agent novo aponta para caminho inexistente

```
test -f .claude/agents/frontend-architect.md   → OK
test -f .claude/agents/frontend-builder.md     → OK
test -f .claude/agents/frontend-qa.md          → OK
test -f .claude/agents/infra-architect.md      → OK
test -f docs/gate-de-design.md                 → OK
```
`harness policy --key agents.by_component` devolve os 4 ponteiros novos com os mesmos caminhos
testados acima — nenhum ponteiro pendurado.

## 4. `design_gate` do `ui-designer` para `web` — mantido

```
$ harness policy --key agents.by_component
{"...": ..., "web": {"architect": ".claude/agents/frontend-architect.md",
 "builder": ".claude/agents/frontend-builder.md",
 "design_gate": "docs/gate-de-design.md",
 "qa": ".claude/agents/frontend-qa.md"}}
```
`web.design_gate = "docs/gate-de-design.md"` presente e inalterado; `charts.design_gate` também
presente. `docs/gate-de-design.md:30` documenta o `ui-designer` como dono do `design_gate` de
`web`, com tarja datada 2026-09-03 registrando que a coluna `architect` mudou e o que dizia antes
(`ui-designer`) fica registrado, não apagado. `frontend-architect.md:23-26` declara
explicitamente "Você não herda o `design_gate`... quem decide de UI/UX é o `ui-designer`".

Reprodução independente do comparador dourado (`T-01.9`), rodado agora, não só citado:
```
$ bash scripts/check-agents-by-component.sh
agents.by_component: bate com o dourado — 6 componente(s), 10 papel(is).
$ echo $?
0
```
6 componentes (`docs` continua ausente, conforme as refs de `T-01.9` previam) e 10 papéis batem
com a contagem manual: `backtest.architect`, `charts.{architect,design_gate}`,
`convergencia.architect`, `infra.architect`, `sentimento.architect`,
`web.{architect,builder,qa,design_gate}` = 10.

## Regras bloqueantes (`harness rules list --severity block`, 8 regras em vigor)

Nenhuma das 8 regras tem escopo sobre `.claude/agents/*.md` (todas são `escopo code` ou
`escopo production`/`compose`, isto é, `backend/src`, `frontend/src`, `docker-compose`). A task é
`[docs]`, não-produção — confirmado no próprio `tasks.toml:286-287`
(`harness code-paths classify .claude/agents/infra-architect.md` → `nao-producao`). Não se
aplica regra bloqueante de código a este delta; a proteção real da task é o comparador dourado do
item 4, que é o instrumento certo por desenho (`T-01.9`).

`harness rules --mode sweep` rodado sobre a árvore inteira: só `[AVISO]`, nenhum `[BLOQUEIO]`.

## Veredito

## QA Gate — Fase 01 [docs] T-01.8
- [OK] Frontmatter válido nos 4 arquivos novos — `.claude/agents/{infra-architect,frontend-architect,frontend-builder,frontend-qa}.md`
- [OK] `infra-architect.md`/`frontend-architect.md` são criação, não cópia de `quant-architect.md`/`ui-designer.md` — 150–169 linhas divergentes por par; único `architect.agent.md` do vizinho é genérico (Jira/Confluence)
- [OK] `frontend-builder.md`/`frontend-qa.md` são porte adaptado, declarado no corpo, não cópia cega — diff das primeiras linhas mostra reescrita de description/tools/filosofia
- [OK] `harness validate --strict` — `politica valida: cripto-strategy (schema_version=1)`, rc=0
- [OK] Nenhum ponteiro pendurado — `test -f` nos 5 arquivos-alvo (4 novos + `docs/gate-de-design.md`) todos OK
- [OK] `web.design_gate` mantido = `docs/gate-de-design.md`; `ui-designer` deixou de ser `architect` mas segue dono do gate — `docs/gate-de-design.md:30` + `frontend-architect.md:23-26`
- [OK] Testes existem e passam — `bash scripts/check-agents-by-component.sh` (comparador dourado de `T-01.9`, reexecutado agora, 1 teste de igualdade estrutural): "bate com o dourado — 6 componente(s), 10 papel(is)", rc=0
- [OK] Auditoria independente citada (fase-01-qa-2026-09-03.md, Eixo 1, M6) reconfirmada por leitura — mutação morde no comparador, cala em `validate --strict`
- Cobertura: não aplicável no sentido de %-linhas (task é `[docs]`, sem `test_cmd` associado); o alvo declarado pela própria task (`D1.12`/refs) é o comparador dourado de `T-01.9`, que roda e passa — não há alvo numérico de cobertura para este componente
- DoD da task (refs de `tasks.toml:284-315`): `agents.by_component` com `infra.architect` presente e apontando para arquivo existente (F-D6-6 fechado); `web.architect != ui-designer.md`; `web.builder`/`web.qa` presentes; `web.design_gate` presente; `harness validate --strict` rc=0 — **todos os itens confirmados por comando nesta sessão**
- [anomalia] nenhuma

Regras bloqueantes avaliadas: 8 de 8 listadas por `harness rules list --severity block` (nenhuma tem escopo sobre este delta — task `[docs]`/não-produção)
Veredito: APPROVED
