# T-05.12 — QA Gate

Worktree: `/tmp/claude-1002/wt/T-05.12`, branch `tasks/T-05.12-porta-leitura-processo`,
base `758e2ef` (merge #117). Todo comando abaixo foi rodado por mim, não copiado do gate do
builder — inclusive as duas mutações (falsificador do envelope e SQL plantado), com reversão
byte-a-byte verificada por `diff` antes do veredito.

## 1. `backend/tests/api/` roda e passa (comando literal meu)

```
.venv/bin/python -m pytest tests/api/ -v
# -> tests/api/test_dependencies.py .                                         [ 33%]
#    tests/api/test_ingest_health_route_over_the_network.py ..                [100%]
#    3 passed in 1.27s
```

## 2. Falsificador do envelope — mutação `class`→`gap_class` em `_project_gap_dict`

Backup do arquivo original tirado ANTES da mutação (`.../scratchpad/ingest_record.py.bak`),
mutação aplicada por script Python (renomeia a chave `"class"` para `"gap_class"` no `dict` de
retorno), suíte rodada, resultado, reversão e reverificação:

```
.venv/bin/python -m pytest tests/api/ -q
# -> FAILED test_ingest_health_route_over_the_network.py::
#    test_get_ingest_health_serves_the_fixed_envelope_when_the_process_is_up
# AssertionError: assert {'detected_at', ..., 'gap_class', ...} == {'class', ...}
#   Extra items in the left set: 'gap_class'
#   Extra items in the right set: 'class'
```

Reversão: `diff ingest_record.py.bak ingest_record.py` → vazio (arquivo idêntico); suíte
`tests/api/ tests/sentimento/test_ingest_health_query.py
tests/sentimento/test_ingest_health_contract_guards.py` → 35 passed. Falsificador do builder
**confirmado independentemente**, não só repetido.

⚠️ Incidente de processo, registrado por transparência: o primeiro `git checkout --
<arquivo>` que usei para reverter apagou TODO o trabalho do builder naquele arquivo (voltou ao
`HEAD` pré-task, não à versão mutada→original). Detectado na hora pelo próprio teste seguinte
(`AttributeError: 'IngestHealthReport' object has no attribute 'to_envelope'`) e corrigido
copiando o backup por cima — `diff` confirmou bytes idênticos ao estado do builder depois. Nenhum
código de produção do builder ficou alterado no resultado final; documentado aqui porque um QA
que "conserta e segue" sem registrar seria exatamente o tipo de ato que este papel proíbe.

## 3. SQL falso plantado no handler — D5.13c, "prova por sítio de chamada, não por regra"

Plantei `_sql_debug = "SELECT * FROM md.ingest_run"` (código morto, nunca executado) dentro de
`get_ingest_health`, backup tirado antes:

```
grep -niE 'SELECT|FROM |psycopg|sqlite3' src/api/routes/ingest_health.py   # o grep do DoD
# -> 36:    _sql_debug = "SELECT * FROM md.ingest_run"  # QA PLANTED SQL...   MORDE (grep manual do DoD)

harness rules --mode sweep --changed-only ; echo exit=$?      # -> exit=0, nenhum achado — NÃO MORDE
bash backend/scripts/lint.sh ; echo exit=$?                   # -> "All checks passed!" / "Success: no issues found" — NÃO MORDE
bash backend/scripts/boundaries.sh                             # -> Contracts: 3 kept, 0 broken — NÃO MORDE (grafo de import, não conteúdo)
harness rules --mode file --path backend/src/api/routes/ingest_health.py
# -> [AVISO] core.module-docstring-single-line — MESMO achado com E SEM a mutação (confirmado
#    rodando a mesma checagem no arquivo original): não é o SQL que dispara, é um aviso
#    pré-existente e não-bloqueante sobre formatação de docstring, nada a ver com SQL.
```

Revertido, `diff` contra o backup → vazio; `tests/api/` volta a 3 passed.

**Lacuna confirmada real, não escondida**: nenhuma regra automatizada (`harness rules`,
`ruff`/`mypy` via `lint.sh`, `import-linter` via `boundaries.sh`) morde uma string SQL arbitrária
dentro do handler. O único instrumento que morde é o `grep` literal do próprio DoD, rodado à mão
no gate. Isso bate exatamente com o que `tasks.toml` (T-05.12, bloco de refs) já declara: `[NAO
SEI] se a regra própria + corpus de ADR-008/DoD-1 alcançam um route handler Python … DONA DA
PERGUNTA: ADR-008/D4, NAO esta task`, e com `harness rules list --severity block` (8 regras, 0
sobre conteúdo SQL). O builder não inventou a lacuna nem a escondeu atrás de "regra passou" — ela
está nomeada no próprio corpo do gate (`gates/T-05.12-builder.md` §4) e no `tasks.toml`.

## 4. MORDE pela rede — processo derrubado, conexão recusada (não timeout, não mock)

```
.venv/bin/python -m pytest tests/api/test_ingest_health_route_over_the_network.py::test_get_ingest_health_refuses_the_connection_when_the_process_is_down -v
# -> 1 passed in 0.54s
```

Reprodução isolada, fora do pytest, com instrumentação de tipo/errno/tempo (script próprio, não
o teste do builder):

```
exception type: ConnectionRefusedError module: builtins
errno: 111 strerror: Connection refused
elapsed seconds: 0.0018
thread alive after join: False
```

`0.0018s` — não é o timeout de 5s do `HTTPConnection`; é recusa imediata do SO num socket
verdadeiramente fechado (`errno=111`), depois de `server.should_exit=True` + `thread.join()`
confirmar a thread morta. `find tests/api -name conftest.py` → nenhum arquivo: sem monkeypatch,
sem mock, sem fixture escondida.

## 5. `lint.sh` e `boundaries.sh`, árvore limpa (sem mutação)

```
bash backend/scripts/lint.sh
# -> All checks passed! / 265 files already formatted / Success: no issues found in 265 source files
bash backend/scripts/boundaries.sh
# -> Camadas por contexto: infra > use_cases > domain KEPT
#    Fronteira de contexto: sentimento nao importa outro contexto KEPT
#    Natureza: domain e use_cases nao falam com socket nem ssl KEPT
#    Contracts: 3 kept, 0 broken.
```

Greps manuais complementares (nenhuma das 8 regras bloqueantes tem sítio nos arquivos novos):

```
grep -rnE '^from \.|^import \.' backend/src/api backend/src/main backend/tests/api   # -> vazio (core.relative-import)
grep -rnE 'except.*:\s*(pass|\.\.\.)\s*$' backend/src/api backend/src/main            # -> vazio (core.silent-except)
grep -rn 'print(' backend/src/api backend/src/main                                    # -> vazio (core.print-statement)
grep -rniE 'password|secret|token|api_key' backend/src/api backend/src/main           # -> vazio (core.hardcoded-secret)
harness rules --mode sweep --changed-only ; echo exit=$?                              # -> exit=0, nenhum achado
```

## 6. Pin exato — sem faixa

```
grep -nE '^dependencies' -A4 backend/pyproject.toml
# -> dependencies = ["pyyaml (==6.0.3)", "fastapi (==0.141.1)", "uvicorn (==0.52.4)"]
```
`==` nas três entradas, nenhum `^`/`>=`.

## 7. Suíte completa + cobertura (independente, não citação do gate do builder)

```
bash backend/scripts/test.sh
# -> 1437 passed, 3 warnings in 274.94s (0:04:34)
# -> Required test coverage of 70.0% reached. Total coverage: 97.61%
# === Cobertura por camada (ADR-009/D1) ===
#   [OK] domain      99.8% (meta 90%)
#   [OK] use_cases   100.0% (meta 80%)
#   [OK] infra       95.1% (meta 70%)
#   universo: 3 camada(s) medida(s) de 3 declarada(s)
```
Bate exatamente com os números do gate do builder — medido de novo, não copiado.

## 8. Pré-requisitos e governança

```
harness tasks validate plataforma-dados   # -> 0 ERROR, 8 WARN (pré-existentes, não relacionados a T-05.12)
```
`T-09.4`, `T-01.8`, `T-05.9` (depends_on) → `status = "done"` nos três, confirmado por leitura de
`tasks.toml`.

---

## QA Gate — Fase 05 [infra] T-05.12
- [OK] core.relative-import — `grep -rnE '^from \.|^import \.' backend/src/api backend/src/main` → vazio
- [OK] core.silent-except — `grep -rnE 'except.*:\s*(pass|\.\.\.)\s*$' backend/src/api backend/src/main` → vazio
- [OK] core.print-statement — `grep -rn 'print(' backend/src/api backend/src/main` → vazio
- [OK] core.hardcoded-secret — `grep -rniE 'password|secret|token|api_key' backend/src/api backend/src/main` → vazio
- [OK] web-fullstack.browser-imports-server — fora de escopo (nenhum arquivo de `frontend/` tocado)
- [OK] web-fullstack.tenant-from-request — fora de escopo (rota sem multi-tenant)
- [OK] web-fullstack.server-test-directory-present — `backend/tests/` já existe e ganhou `backend/tests/api/`
- [OK] own.compose-hardcoded-secret — fora de escopo (nenhum `compose*.yml` tocado)
- [OK] Testes existem e passam — `.venv/bin/python -m pytest backend/tests/api/ -v` (3 testes); suíte completa `bash backend/scripts/test.sh` (1437 testes)
- [OK] Cobertura 97.61% (global) / domain 99.8%, use_cases 100.0%, infra 95.1% contra alvo 70%/90%/80%/70% — todos acima
- [OK] DoD D5.13 (prova pela rede) — MORDE confirmado independentemente: `ConnectionRefusedError`, errno 111, 0.0018s, sem mock (`docs/context/plataforma-dados/tasks.toml:850`)
- [OK] DoD D5.13b (pin exato) — `backend/pyproject.toml:54`, `==` nas 3 entradas
- [OK] DoD D5.13c (zero SQL no handler) — grep do DoD confirma zero SQL hoje; falsificador plantado confirma que só o grep morde, não uma regra automatizada — lacuna real e já documentada em `tasks.toml:854` e `gates/T-05.12-builder.md` §4, não escondida por este builder
- [OK] Falsificador do envelope (`class`→`gap_class`) — reprova como declarado (`AssertionError`), revertido byte a byte
- [anomalia] Nenhuma — todas as checagens produziram veredito; o incidente do §2 (checkout destrutivo próprio) foi detectado e corrigido dentro desta sessão, sem impacto no estado final do worktree

Regras bloqueantes avaliadas: 8 de 8
Veredito: APPROVED
