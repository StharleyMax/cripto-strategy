# Review Gate — Fase 07 · `T-07.6` (`CST-60`) — Particionamento dimensionado contra a vazão MEDIDA

**Feature:** `plataforma-dados` · **Componente:** `sentimento` · **PR:** #93 (`tasks/T-07.6-fase07` → `master`, MERGEABLE, OPEN)
**Worktree auditada:** `/tmp/claude-1002/wt/T-07.6` (HEAD = `cc057a6`, base `master@bf15df6`, com `origin/master@b550a47`/`d195f29` já mesclados)
**Handoff:** `docs/context/plataforma-dados/handoff/T-07.6.md` · **Builder:** `gates/T-07.6-builder.md` · **QA:** `gates/T-07.6-qa.md` (APPROVED)

## Denominador

- `harness rules list --severity block`: **7 regras bloqueantes** em vigor no repo (`core.relative-import`,
  `core.silent-except`, `core.print-statement`, `core.hardcoded-secret`,
  `web-fullstack.browser-imports-server`, `web-fullstack.tenant-from-request`,
  `web-fullstack.server-test-directory-present`). Avaliadas as 7 sobre o diff da task.
- `harness rules --mode file --path <arquivo>` rodado individualmente sobre os 4 arquivos da task
  (2 produção + 2 teste) → **saída vazia nos 4** (0 achados, 0 bloqueios), reproduzindo
  independentemente o que o QA já havia relatado.
- `harness rules --mode sweep --changed-only` no HEAD do PR → `rc=0`, sem achados (working tree
  limpo, só `frontend/node_modules` untracked, não relacionado).
- Arquivos lidos por inteiro: `stream_partitioning.py` (149 linhas), `stream_partition_plan.py` (40
  linhas), `test_stream_partitioning.py` (159 linhas), `test_stream_partition_plan.py` (72 linhas),
  handoff, builder, QA gate, `docs/INDEX.md`/`backend/README.md` (diff contra merge-base).
- Camada de julgamento arquitetural avaliada contra 3 documentos citáveis: `ADR-016` (relógio/tipo,
  natureza), `ADR-011/D3a` (contrato de camadas `infra > use_cases > domain`, `import-linter`),
  `CLAUDE.md` §"Idioma de identificador" (mensagem de exceção em inglês).

## Achados

### 1. Contrato `Natureza` (`ADR-016`/`ADR-011/D3a`) — CONFORME, verificado de 3 formas independentes

- Leitura direta de `stream_partitioning.py:25-30`: imports são só `from __future__ import
  annotations`, `math`, `collections.abc.Sequence`, `dataclasses.dataclass`, `typing.Final` — zero
  I/O, socket, ssl ou relógio.
- `bash backend/scripts/boundaries.sh` (rodado por mim, não só citado do QA): `3 kept, 0 broken`,
  incluindo `Natureza: domain e use_cases nao falam com socket nem ssl KEPT` e `Camadas por
  contexto: infra > use_cases > domain KEPT`.
- `bash backend/scripts/natureza.sh` (rodado por mim): `universo de 69 arquivo(s), 0 leitura(s) de
  relogio`.
- `stream_partition_plan.py` (infra) só importa `domain/stream_partitioning.py` +
  `infra/binance_stream_probe.py` (já existente, `T-03.1`) — nenhuma leitura de socket/arquivo
  própria, é composição pura de strings de path. Direção de import respeita `infra > use_cases >
  domain`.

### 2. D7.11 — fórmula usa só `max`, sem ramo alternativo — CONFIRMADO por leitura linha a linha

`max_symbols_per_partition` (`stream_partitioning.py:88-114`) tem exatamente um `return
partition_capacity_msg_per_second // throughput.max`, precedido só do guard de
`InfeasiblePartitionCapacityError`. Nenhum `if`/branch/default que aplique `p50`/`p95`/`p99`.
`SingleSymbolThroughput` carrega os 4 percentis extras, mas `test_only_max_feeds_the_sizing_arithmetic`
prova por construção (duas réguas que só concordam em `max` dimensionam igual) que eles não entram na
aritmética. Não há código morto da fórmula antiga ("1 quente + resto p50") no arquivo — a citação dela
existe só em comentário/docstring explicando a rejeição, e em `test_p50_...` como valor calculado
isoladamente para a asserção de overflow, nunca chamando uma função de produção com essa fórmula.

### 3. Merge com `T-07.9` — `docs/INDEX.md`/`backend/README.md` íntegros, nenhuma entrada perdida

`git diff $(git merge-base HEAD origin/master)..HEAD -- docs/INDEX.md`: só uma linha `+`, entrada
`T-07.6`, no fim do arquivo — as entradas de `T-07.5`, `T-07.14` e `T-07.9` (mescladas em paralelo)
seguem intactas antes dela, nenhuma reescrita. Mesma checagem em `backend/README.md`: seção nova
`## 📎 2026-09-02 por T-07.6` só adicionada no fim, entradas de `T-07.9`/`T-07.10`/outras intocadas.
Verificado por mim, não só citado do relato do builder/QA.

### 4. Reuso de `T-07.4`/`T-07.5` — não há estrutura equivalente para reusar

`redis_stream_bus.py`/`redis_series_write_queue.py` implementam `RedisStreamConsumerGroup` — o
primitivo do **Redis Streams** (`XADD`/`XREADGROUP`/`XACK` sobre UM stream), que resolve
durabilidade/ack de mensagens já publicadas. `T-07.6` resolve um problema diferente: quantas
conexões WebSocket abrir e quais símbolos cada uma carrega, ANTES de qualquer publicação em Redis
— não existe, nesses dois arquivos, nenhum conceito de "partição de símbolos" ou "quantas
conexões". Não há segunda estrutura de fila inventada; os vocabulários não se sobrepõem.
`[INFO]` — não é achado de violação, é confirmação de que a ressalva do handoff (verificar reuso)
foi atendida sem necessidade de reuso.

### 5. Idioma — inglês em produção e teste, inclusive mensagens de exceção

`grep -noP '[áéíóúãõâêôçÁÉÍÓÚÃÕÂÊÔÇ]'` sobre os 4 arquivos → **zero ocorrências** (nenhum diacrítico).
As 3 mensagens de exceção (`InvalidThroughputError` ×2, `InfeasiblePartitionCapacityError`) são
100% inglês. Há uma citação literal em português dentro de comentário em inglês
(`stream_partitioning.py:13`, `"nao o p50 — dimensionar pelo p50 sub-provisiona..."`, sem
diacrítico) — é citação verbatim do handoff para rastreabilidade, mesmo padrão já usado por outras
ADRs deste repositório para citar fonte; não é identificador nem mensagem nova, e não há regra
(bloqueante ou `[[rules.own]]`) que meça idioma — `CLAUDE.md` é explícito: idioma de identificador
é convenção, não portão (`ADR-011/D1.10`). `[INFO]`, não entra no veredito.

## Veredito

**COMPLIANT** — 0 de 7 regras bloqueantes violadas (todas as 7 avaliadas); nenhuma violação de
arquitetura declarada citável (`ADR-016`, `ADR-011/D3a`) encontrada, confirmada por 3 comandos
independentes (`boundaries.sh`, `natureza.sh`, leitura de imports) além do já relatado por QA;
merge com `T-07.9` íntegro; nenhuma estrutura duplicada de partição/consumer-group.

Universo varrido: 4 arquivos novos da task (2 produção + 2 teste) + diff de `docs/INDEX.md` e
`backend/README.md` contra o merge-base, via `harness rules --mode file`, `harness rules --mode
sweep --changed-only`, `boundaries.sh`, `natureza.sh` e leitura integral dos arquivos.
