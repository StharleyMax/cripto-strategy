# Gate report — T-08.1 (/review), CST-69

**Fase 08, componente `docs`. Escopo dado pelo despachante** (auditoria da emenda de
`ADR-002` + `docs/INDEX.md` + rules sweep), não checklist completo de repositório.

## Denominador

- `harness rules list --severity block` → **8 regras bloqueantes em vigor** (core×4,
  web-fullstack×3, own×1). Nenhuma se aplica a este universo: os 11 arquivos tocados
  (`git diff master --stat`) estão sob `docs/`, fora de `code_paths.include_prefixes`
  (`backend/src/`, `backend/tests/`, `frontend/src/`, `deploy/`) — logo 0 de 8 avaliáveis
  como violação neste diff.
- `harness rules --mode sweep --changed-only` → **`rc=0`, 0 achado**, reproduzido nesta
  sessão (confirma o número já citado por QA e por `docs/INDEX.md`).
- Arquivos varridos: `git diff master --stat` → 11 arquivos (`docs/INDEX.md`,
  `docs/adr/ADR-002-motor-de-armazenamento.md`, 1 gate builder, 8 arquivos de
  `docs/spike/T-08.1-motor-armazenamento/`).

## Achados — camada 2 (julgamento, com citação de documento)

**[WARNING-1] Emenda de `ADR-002` não é append puro em nível de linha — contradiz a
própria declaração do documento.** `git diff master --numstat -- docs/adr/ADR-002-motor-de-armazenamento.md`
→ **`66  1`**, isto é, 1 linha DELETADA além das 66 inseridas. A linha deletada é o
**header de Status** (`docs/adr/ADR-002-motor-de-armazenamento.md:3`), reescrito de
*"proposto, com um finalista pendente de spike"* para *"`D4` decidido em 2026-09-04…"`*.
Isto contradiz a frase que a própria emenda escreve, verbatim, na linha 132:
**"Acréscimo, nada acima foi reescrito."`** — falso por este diff. Contraste com o
precedente que o mesmo arquivo já criou: a emenda anterior (commit `6e86e8a`,
2026-08-29) é `15 insertions(+), 0 deletions(-)` — append puro de fato, sem tocar o
header. **Não é BLOQUEIO**: não existe `[[rules.own]]` nem regra do runner que meça
"append puro" em ADR — é convenção autodeclarada pelo documento, não portão. Mas é
achado real, citável (arquivo + linha + `numstat`), e a correção é concreta: reverter
a linha 3 ao texto original e mover a atualização de status para dentro do bloco de
emenda (mesmo padrão que a emenda de `D1` usou, ver abaixo).

**[WARNING-2] Seção `D4` (corpo da decisão) fica STALE dentro do próprio documento —
sem o callout que o precedente de `D1` criou.** `docs/adr/ADR-002-motor-de-armazenamento.md:57`
continua, byte a byte, "**Não decidido**, e a razão é honesta: nenhum dos cinco foi
instalado nem medido, e três números faltam" — nunca reescrito, correto pela regra de
append-only. Mas ao contrário da emenda de `D1` (2026-08-29), que colocou uma tabela
"duas coisas que quem chegar aqui precisa saber antes de citar D1 ou D4" logo depois da
menção a `D1`, esta emenda **não** deixa nenhum ponteiro perto da seção `D4` em si —
só no header (linha 3, que é o WARNING-1) e no rodapé (linha 130+). Um leitor que pule
direto para `### D4` sem ler o header nem rolar até o fim lê uma decisão pendente que
já não é verdade. Correção concreta: uma linha de ponteiro logo após `### D4 · …`
apontando para a emenda de 2026-09-04, no mesmo molde do que já existe para `D1`.

## Checklist do despacho

1. **Emenda de `ADR-002` é append puro?** `NÃO`, por `numstat` (`66 1`, ver WARNING-1) —
   mas a substância das decisões `D1`–`D4` (linhas 38–83) está byte-idêntica; a única
   reescrita é o header de status (metadado, não decisão).
2. **Os 5 critérios de `D8.21` honrados?** `SIM`, os 5 endereçados item a item na tabela
   da emenda e no `README.md` do spike, com rótulo em cada célula. O critério 5 (latência
   R2) está corretamente `[NÃO MEDIDO]` em toda parte que o cita — inclusive o proxy
   informativo (64–107 ms, ambiente≠VPS) está rotulado "NÃO substitui a medição real" e
   nunca aparece como `[MEDIDO]` do critério em si. Sem disfarce encontrado.
3. **`docs/INDEX.md` append-only?** `SIM` — `git diff master --numstat -- docs/INDEX.md`
   → `2 0`, nenhuma linha existente tocada.
4. **`harness rules --mode sweep --changed-only`?** `rc=0`, 0 achado, reproduzido.

## Veredito

**COMPLIANT** — 0 de 8 regras bloqueantes violadas (universo: 11 arquivos sob `docs/`,
fora de `code_paths`). Os 2 achados acima são WARNING de camada 2 (convenção
autodeclarada do documento, não portão) e não derrubam o veredito, mas ficam citados
para correção antes do merge.
