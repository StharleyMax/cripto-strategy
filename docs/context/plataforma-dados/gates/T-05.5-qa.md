# QA Gate — T-05.5 (plataforma-dados, fase 05, charts)

## QA Gate — Fase 05 [charts] — T-05.5 "PricePanel declara price_source E price_use; marcação amarrada"

**Setup:** `tasks/T-05.5-price-source-use` já estava checked out em outra worktree; validado via `git checkout 6131ce2` (mesmo commit que a branch aponta, `git ls-remote --heads origin | grep 05.5`) em modo detached, na worktree `wf_2a5f5015-5f1-6`. `data/` (gitignored, ausente na worktree nova) foi symlinkado a partir do checkout principal só para rodar a suíte com fixture real; removido ao final (`git status` final: `working tree clean`, sem `data` untracked).

- **[OK]** `core.relative-import`/`core.silent-except`/`core.print-statement`/`core.hardcoded-secret`/`web-fullstack.browser-imports-server`/`web-fullstack.tenant-from-request` — `harness rules --mode sweep --surface ci` → `rc=0`, **zero** `[BLOQUEIO]` em todo o repo (só 12 `[AVISO]` pré-existentes, nenhum nos 6 arquivos tocados por T-05.5: `s2-price-source.ts`, `s2-price-source.test.ts`, `s2-annotation-price-binding.ts`, `s2-annotation-price-binding.test.ts`, `s2-panels.ts`, `s2-panels.test.ts`). Confirmado arquivo a arquivo: `harness rules --mode file --path <cada um dos 6>` → saída vazia em todos.
- **[OK]** `web-fullstack.server-test-directory-present` — não aplicável ao diff (backend intocado); sweep completo não achou violação.
- **[OK]** Testes existem e passam — `npm --prefix frontend run test:charts` (`node --test 'src/charts/*.test.ts'`) → **78 testes, 78 pass, 0 fail** (reproduzido, não confiei no relato do builder). `npm --prefix frontend run lint` (`eslint src`) → **0 erro, 0 aviso**.
- **[OK]** Cobertura — sem piso declarado para `charts`: `harness policy --key test_cmd` só cobre `sentimento` (`{"sentimento": {...}}`, chave `charts` ausente). Medição qualitativa: as 8 novas asserções (3 em `s2-price-source.test.ts` + 5 em `s2-annotation-price-binding.test.ts`) cobrem as 5 entradas de `PRICE_USES`, os 2 casos de recusa de `PS-1` (`null` e valor fora do conjunto), o falsificador `D5.5` + seu controle positivo, e a propagação da recusa de `D5.10` através do novo construtor.
- **[OK]** DoD item **5.7** ("o painel de Preço declara `price_source` E `price_use` na linha do painel") — `PricePanel { priceSource, priceUse, series }` em `s2-panels.ts:80-87`, `priceSource` **derivado** de `priceUse` via `resolvePriceSource` (nunca campo independente). Teste `T-05.5/5.7` em `s2-panels.test.ts:48-55` passa.
- **[OK]** DoD **D5.5** ("marcar sob `price_source = klines_last` e reabrir sob `mark_price` ⇒ a marcação NÃO é reexibida como a mesma") — `describeAnnotationOnReopen` em `s2-annotation-price-binding.ts:69-78`, label exato `"marcada sobre outra série de preço"` (`ADR-007`/`PS-3`, literal). Teste `D5.5 falsifier` em `s2-annotation-price-binding.test.ts:27-37` passa, com controle positivo irmão (mesma fonte ⇒ `label === null`).
- **[OK]** `ADR-007`/`PS-1` ("pedir preço sem `price_use` é erro, nunca default silencioso") — `priceUse: PriceUse | null` como parâmetro obrigatório (não opcional) em `resolvePriceSource`, `buildPricePanel`, `createPriceBoundAnnotation`; `MissingPriceUseError`/`InvalidPriceUseError` testados.

### Mutações plantadas — prova de que os testes MORREM (não é achado bloqueante; 3/3 mataram)

Todas revertidas ao final (`git diff --stat` vazio, `git status` → `working tree clean`); nenhum código de produção ficou alterado.

1. **`s2-annotation-price-binding.ts:73`** — `isSamePriceSeries` forçado sempre-verdadeiro (mantendo ambas variáveis referenciadas, sem novo erro de lint):
   `npm --prefix frontend run test:charts` → **77 pass, 1 fail** — só `D5.5 falsifier: mark under klines_last, reopen under mark_price` morre. Mutação isolada, teste isolado.
2. **`s2-price-source.ts:59`** — tabela de `ADR-007` alterada: `liquidation_trigger: "mark_price"` → `"klines_last"`:
   `npm --prefix frontend run test:charts` → **76 pass, 2 fail** — `ADR-007's decision table, all five price_use values...` e (por consequência, mesmo dado de teste) `D5.5 falsifier` morrem.
3. **`s2-panels.ts:107`** — `priceSource` invertido em relação ao que `resolvePriceSource` devolve (quebra "derivado, nunca independente" de `5.7`):
   `npm --prefix frontend run test:charts` → **77 pass, 1 fail** — só `T-05.5/5.7: the price panel declares price_source AND price_use on the panel row` morre.

Cada mutação no comportamento central da task matou exatamente o teste que a nomeia — nenhum verde falso.

Regras bloqueantes avaliadas: 7 de 7
Veredito: **APPROVED**

