# T-08.7 — implementação (builder), pós-decisão do `quant-architect`

Status: implementação fechada nesta sessão. **Nota de integridade:** este arquivo foi encontrado
já existente em disco no meio da sessão, com uma narrativa de "sessão anterior travou antes de
emitir o gate" e números que já estavam desatualizados frente ao estado real do código (citava
`scan.py` com 97% de cobertura; a medição desta sessão é 100%). Não foi criado por este agente
antes deste ponto e não consta no `git status` do início da sessão — o conteúdo abaixo substitui
o anterior por inteiro, com números medidos NESTA sessão, comando por comando.

Decisão que este código executa: [`ADR-022`](../../../adr/ADR-022-min-obs-por-observacao-n-obs-por-ponto-e-dispersao-do-z.md).
Decisão do arquiteto: [`gates/T-08.7-architect.md`](T-08.7-architect.md).

## Arquivos

- `backend/src/modules/charts/domain/observation.py` (**novo**) — `Observation(instrument_id,
  value, n_obs)`, `IncompleteObservationError`, `Fired`/`NotFired`/`Insufficient`,
  `ObservationVerdict`.
- `backend/tests/charts/test_observation.py` (**novo**).
- `backend/src/modules/charts/domain/scan.py` (**modificado**) — `evaluate_scan` recebe
  `Sequence[Observation]`; filtro `n_obs < min_obs` roda por observação antes de `_fires`,
  produzindo `Insufficient`; `MinObsNotMetError` agora dispara só quando NENHUMA observação
  sobrevive ao filtro (`|X| == 0`); `ScanResult` ganha `n_excluded_min_obs`, `per_observation`,
  `z_dispersion`; `ZDispersionTelemetry` novo (IQR, `MIN_SYMBOLS_FOR_Z_DISPERSION = 4`).
- `backend/src/modules/charts/use_cases/compute_distribution.py` (**modificado**) — `ObservationSource.observed_values`
  retorna `Sequence[Observation]`; a função projeta `.value` antes de `compute_histogram`.
  `histogram.py` não foi tocado (fora do diff).
- `backend/src/modules/charts/use_cases/run_scan.py` (**modificado**) — repassa `Observation`
  direto a `evaluate_scan`, sem projeção.
- `backend/tests/charts/{test_compute_distribution,test_run_scan,test_scan}.py` (**modificados**)
  — fakes convertidos para `Sequence[Observation]`; `test_scan.py` ganhou o falsificador
  obrigatório do `ADR-022` e os testes de `ZDispersionTelemetry`/exaustividade.
- `docs/adr/ADR-022-...md`, `docs/context/plataforma-dados/gates/T-08.7-architect.md` — já
  existentes, produzidos pelo `quant-architect` antes deste despacho, não tocados aqui.

## DoD (D8.4/D8.5/D8.8, `docs/context/plataforma-dados/tasks.toml:1279-1286`)

- [x] **D8.4** `n_obs` viaja por observação: `Observation.n_obs`, filtrado ANTES de `_fires`.
      Falsificador do próprio ADR reproduzido literalmente:
      `.venv/bin/python -m pytest tests/charts/test_scan.py -k test_adr_021_falsifier_mixed_n_obs_excludes_the_underobserved_symbol_not_the_aggregate -v --no-cov`
      → 1 passed; asserts: `n_total==1`, `n_excluded_min_obs==1`,
      `Insufficient(instrument_id="ALTUSDT", n_obs=300, min_obs_required=576)` em
      `per_observation`, nenhum `Fired`/`NotFired` para `ALTUSDT`.
- [x] **D8.5** dispersão do `z` (IQR) amarrada a `instrument_id`, `null`+motivo abaixo de 4
      símbolos: `pytest tests/charts/test_scan.py -k "z_dispersion" -v --no-cov` → 4 passed.
- [x] **D8.8** exclusão declarada, nunca encolhimento silencioso: `ScanResult.n_excluded_min_obs`
      testado tanto no caso de exclusão (falsificador acima) quanto no caso "nada excluído"
      (`test_adr_021_no_exclusion_when_every_observation_meets_min_obs`).
- [x] `Insufficient` sem campo de valor: `test_insufficient_verdict_carries_no_value_field` +
      `test_observation.py::test_insufficient_carries_no_value_field`.
- [x] `z_dispersion` nunca é entrada de `_fires`/`_robust_z`/`_compare`: verificado por inspeção
      de assinatura, não promessa em prosa — `test_no_decision_function_accepts_z_dispersion_as_input`.
- [x] `distribution`/`histogram.py` intocados — `git diff --stat` não lista `histogram.py`
      nem `histogram_recipe.py`.
- [x] `test_percentile_spec_below_min_obs_refuses` (herdado de `T-08.6`) continua válido —
      adaptado para `Observation` (todas com `n_obs=1 < min_obs=10`, `|X|==0`), mesmo veredito
      (`MinObsNotMetError`).

## Comandos rodados (literais) e resultado

```
$ make lint-backend
bash backend/scripts/lint.sh
All checks passed!
290 files already formatted
Success: no issues found in 290 source files
```
Universo: 290 arquivos (`ruff check`/`ruff format --check`/`mypy --strict` sobre `src` + `tests`).

```
$ bash backend/scripts/test.sh
...
TOTAL   6231   126   1238   34    98%
Coverage XML written to file coverage.xml
Required test coverage of 70.0% reached. Total coverage: 97.78%
1526 passed, 3 warnings in 322.76s (0:05:22)
=== Cobertura por camada (ADR-009/D1) ===
  [OK  ] domain      99.8% (meta 90%)  [3255/3260 linhas]
  [OK  ] use_cases  100.0% (meta 80%)  [629/629 linhas]
  [OK  ] infra       95.1% (meta 70%)  [2208/2321 linhas]
  universo: 3 camada(s) medida(s) de 3 declarada(s)
```
Universo: suíte inteira, 1526 testes, `[MEDIDO 2026-09-04]`.

```
$ .venv/bin/python -m pytest tests/charts --no-cov
89 passed in 0.15s
```
Universo: 89 testes em `tests/charts/` (8 novos em `test_observation.py`, mais testes novos
adicionados a `test_scan.py` para o falsificador do `ADR-022` e para `ZDispersionTelemetry`).

```
$ harness rules --mode sweep --changed-only
[AVISO] core.module-docstring-single-line em scan.py:1, compute_distribution.py:1, run_scan.py:1
```
rc=0. 0 `[BLOQUEIO]` — nenhuma das 8 regras de `harness rules list --severity block` disparou.
Os 3 `[AVISO]` são de docstring multi-linha, padrão já presente nesses 3 arquivos antes desta
mudança (severidade AVISO, não bloqueante).

## Cobertura por arquivo tocado (`coverage.xml`, gerado pelo comando acima)

```
modules/charts/domain/observation.py                 100%
modules/charts/domain/scan.py                         100%
modules/charts/use_cases/compute_distribution.py      100%
modules/charts/use_cases/run_scan.py                  100%
```
`[MEDIDO 2026-09-04]`, via `ElementTree` sobre `coverage.xml` filtrando `class[@filename*=charts]`.

## Doc delta

- `docs/INDEX.md`: linha nova (append-only) registrando esta implementação — a entrada existente
  cobria só a decisão do `quant-architect` (pré-código); falta a do `builder`.
- ADR: nenhum novo — `ADR-022` já cobre esta decisão por inteiro (produzida antes deste despacho).

## Bloqueado

Nenhum.
