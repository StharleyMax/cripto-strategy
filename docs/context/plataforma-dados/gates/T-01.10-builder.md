# `T-01.10` — QA Gate Context Block

```
[QA GATE — Fase 01: governanca gateante · item 1.14 · DoD D1.14 + D1.14b]
Feature: plataforma-dados     Componente: docs + infra
Arquivos alterados:
- harness.toml (modified)  — (a) include_prefixes += "deploy/"; (b) include_globs += "*.yml","*.yaml"; (c) [[rules.own]] own.compose-hardcoded-secret
- deploy/compose.yml (new) — o LEGITIMO real e versionado (a metade `cala`)
- corpus/manifest.toml (new)
- corpus/policy/harness.toml (new)
- corpus/cases/own.compose-hardcoded-secret/violating/{01_password_mapping,02_dsn_in_env_list,03_api_key_in_test_path}.yml (new)
- corpus/cases/own.compose-hardcoded-secret/conforming/{01_env_interpolation,02_docker_secrets_file,03_secret_in_test_path,04_out_of_scope_yaml}.yml (new)
- corpus/reference/compose_secret_reference.sh (new) — classificador de REFERENCIA, decide por `awk` de campo, nao pela regex da politica
- docs/INDEX.md (modified) — 1 linha APPEND, nenhuma reescrita
Spec: docs/adr/ADR-009-reuso-da-forma-do-anything.md §D6.7 (onde a especificacao nasceu)
Plan: docs/plans/SPEC-001-plataforma-dados/01_governanca_gateante.md §"Itens NOVOS — reabertura de 2026-09-03"

DoD:
- [x] D1.14 MORDE — `harness rules --mode file --path deploy/<violador efemero>` -> BLOQUEIO nomeando a regra, exit=2
- [x] D1.14 CALA  — `harness rules --mode file --path deploy/compose.yml` -> 0 byte, exit=0, sobre arquivo REAL e VERSIONADO
- [x] D1.14b      — `harness corpus verify` VERDE **e** `harness corpus mutate` com o corpus DEFENDENDO a regra
- [x] deploy/ existe com arquivo real e legitimo (nao passa por vacuidade)
- [x] violador EFEMERO e fora da arvore versionada (sweep segue rc=0; pre-push nao recusa)

Comandos rodados (literais) e resultado:

# ── O "ANTES", medido nesta arvore antes da edicao ──────────────────────────────────
- `ls -d deploy`                                            -> inexistente, rc=2
- `harness code-paths classify deploy/docker-compose.yml`   -> "nao-producao: nenhum include_prefixes casa
                                                               ['backend/src/','backend/tests/','frontend/src/']", rc=1
- `harness rules --mode file --path deploy/docker-compose.yml` -> 0 byte, rc=0
  ⇒ o rc=0 de entao era indistinguivel entre "nao ha segredo" e "o instrumento nunca olhou" (ADR-012)
- `harness rules list | tail -1`                             -> "total: 10 regra(s) em vigor" (ZERO propria)

# ── D1.14 · MORDE / CALA — o criterio NAO e `classify` ──────────────────────────────
- `harness rules --mode file --path deploy/__violador_efemero__.yml`
    -> {"decision":"block","reason":"[BLOQUEIO] [own.compose-hardcoded-secret]
        deploy/__violador_efemero__.yml:5: segredo literal em arquivo de compose: ...
        1 ocorrencia(s) fora da allowlist"}                  **exit=2**   (universo: 1 arquivo)
- `harness rules --mode file --path deploy/compose.yml`      -> 0 byte,   **exit=0**   (universo: 1 arquivo)
- segunda forma sintatica do violador (lista de ambiente, `- DATABASE_DSN=postgres://app:hunter2@db:5432/cripto`)
    -> **exit=2**, mesma regra, linha 5. Trocar a FORMA nao troca o veredito.
- violadores removidos no mesmo ciclo: `find deploy -type f` -> **1** (`deploy/compose.yml`), n=1
- `git status --porcelain deploy` -> `?? deploy/` (nenhum violador versionado)

  ⚠️ `classify` NAO e citado como criterio, e o motivo e medido: ele NAO confere existencia
  (`classify backend/src/api/routes/ingest_health.py` -> `producao` para arquivo inexistente),
  logo fecharia sobre `deploy/` vazio. Citado aqui SO como leitura de contexto:
  `harness code-paths classify deploy/compose.yml` -> "producao: include_prefixes + include_globs
  casam e nada exclui", rc=0.

# ── D1.14b · o corpus, e as DUAS metades ────────────────────────────────────────────
- `harness corpus verify --corpus corpus --reference corpus/reference/compose_secret_reference.sh`
    -> "corpus dourado VERDE: veredito identico, arquivo a arquivo", **rc=0**
       casos: 7 · regras: 1 · divergentes: 0 · violadores em caminho de teste: 1
       classificador de referencia intacto (mtime e tamanho conferidos antes e depois)
- `harness corpus mutate --corpus corpus --reference corpus/reference/compose_secret_reference.sh`
    -> "baseline sem mutacao: VERDE" · "[morto] own.compose-hardcoded-secret" ·
       "1/1 mutantes mortos: toda regra de escopo amplo tem caso que a defende", **rc=0**

# ── VERDE NAO PROVA NADA ATE UMA MUTACAO REPROVAR — o falsificador do proprio corpus ──
  Copia descartavel do corpus com o UNICO violador em caminho de teste
  (`deploy/tests/compose.fixture.yml`) removido do manifesto:
- `harness corpus verify --corpus <copia> --reference ...`  -> "corpus dourado VERDE", **rc=0**
- `harness corpus mutate --corpus <copia> --reference ...`  -> "[SOBREVIVEU] own.compose-hardcoded-secret",
    "MUTANTES SOBREVIVENTES (o corpus nao defende estas regras)", **rc=1**
  ⇒ `verify` sozinho e CEGO a metade do escopo. E exatamente a distincao que o item `1.8`
    existe para cobrar, e ela esta demonstrada e nao afirmada.

# ── PORTOES DO REPOSITORIO ──────────────────────────────────────────────────────────
- `make verify`  -> "VERDE — 6 portoes mediram e passaram"
    lint-backend rc=0 (256 source files) · lint-frontend rc=0 · test rc=0 (1434 passed,
    Total coverage 97.74%) · boundaries rc=0 (3 kept, 0 broken) · regras rc=0
    (0 bloqueio(s), 16 aviso(s)) · politica rc=0
- `make lint`    -> **rc=0**, ja incluindo o alvo `lint-agents` que a `T-01.9` acrescentou
    ("agents.by_component: bate com o dourado — 6 componente(s), 10 papel(is)")
- `harness rules --mode sweep`               -> **rc=0**, 0 bloqueio, 16 aviso (todos pre-existentes:
    12 `core.module-docstring-single-line` + 4 `web-fullstack.hardcoded-url`). Universo: repositorio
    inteiro. **Nenhum aviso novo, e `own.compose-hardcoded-secret` NAO acusa `deploy/compose.yml`.**
- `harness rules --mode sweep --changed-only` -> **rc=0** — ⚠️ e o numero e FRACO, declarado e nao
    escondido: o universo dele e `git diff --name-only HEAD` = **4 arquivos**, e ele e CEGO a
    `deploy/` e `corpus/`, que sao arquivo novo NAO RASTREADO. Quem mede de verdade nesta task e
    o sweep completo acima.
- `harness validate` -> "politica valida: cripto-strategy (schema_version=1)", **rc=0**
- `harness doctor`   -> "veredito: CONFORME (12 checagens)", **rc=0**
- `harness rules list | tail -1` -> "total: **11** regra(s) em vigor" (de 10 -> 11; a 11a e
    `[BLOQUEIO] own.compose-hardcoded-secret (own · forbidden-regex-allowlist · escopo code)`)

# ── O AVISO DO BUILDER DA `T-01.9`, atendido e PROVADO ──────────────────────────────
  O `rc=3` que ele viu era meu: a primeira redacao do `regex` usava `'''...'''` e o leitor de
  politica em `bash` RECUSOU ("cadeia multilinha em harness.toml: este leitor le uma linha por
  vez"). Achado por mim na 1a validacao e corrigido para uma UNICA linha em cadeia basica.
- `grep -c "'''\|\"\"\"" harness.toml`            -> **0** (nenhuma cadeia multilinha)
- `harness policy --key agents.by_component`      -> **rc=0**
- `harness policy --key rules`                    -> rc=1, "campo ausente na politica: rules"
- `harness policy --key rules.packs`              -> rc=1, "campo ausente na politica: rules.packs"
  ⚠️ CONTROLE, porque rc=1 sobre chave ausente e ambiguo: `rules.packs` EXISTE em `harness.toml`
  desde 2026-08-22 e mesmo assim da rc=1 ⇒ o rc=1 e propriedade do ACESSOR (lista de chaves
  servidas), **nao** efeito da minha edicao. O rc=3 — a RECUSA de verdade — nao ocorre mais.

Cobertura: 97.74% total (`make verify` -> `test`, 1434 testes) — **inalterada por esta task**,
e tinha de ser: nenhum arquivo Python foi tocado. A cobertura desta task nao e de linha, e de
REGRA: 1 regra propria, 7 casos (3 violadores / 4 conformes), 1 mutante, 1/1 morto.

Doc delta:
- docs/INDEX.md: atualizado — 1 linha APPEND ao registro (nenhuma linha existente reescrita)
- harness.toml: atualizado — 3 blocos de comentario novos (o "antes" medido, as escolhas de campo
  uma a uma, e o `[NAO MEDIDO]` do `Dockerfile`). **Nada apagado**: o bloco "SEM `[[rules.own]]`"
  de `T-01.1` FICA, com a caducidade declarada abaixo dele — ele continua verdadeiro sobre o
  universo de 2026-08-28, e apaga-lo esconderia que houve um dia sem sujeito para o corpus.
- docs/plans/.../01_governanca_gateante.md: sem mudanca — o item `1.14` e o `D1.14`/`D1.14b` ja
  estavam escritos; reescreve-los violaria o termo da autorizacao do owner ("nenhum item nem DoD
  existente e reescrito").
- docs/adr/ADR-009 §D6.7: sem mudanca — a especificacao foi TRANSPORTADA, nao reinventada.
- ADR: nao necessario — nenhuma decisao nova. `ADR-009/D6.7` ja decidiu as tres partes, o alvo e a
  fase; `ADR-011/D4` nao e contradito (ele recusou regra propria DE TYPESCRIPT, onde o ESLint ja
  era validador — nenhum validador deste repositorio le YAML) e `ADR-011/D1.10` nao e tocado (ele
  proibe `[[rules.own]]` DE IDIOMA; o alvo aqui e segredo, propriedade verificavel por conteudo).

Bloqueado (se houver):
1. **`[NAO MEDIDO]` mantido de proposito: se `include_globs` aceita nome SEM extensao
   (`Dockerfile`).** O comando que decidiria EXIGE o ato de politica, e por isso NAO foi rodado:
       acrescentar "Dockerfile" a `include_globs` e rodar `harness code-paths classify deploy/Dockerfile`
   **Enquanto nao medido, `Dockerfile` fica FORA da cobertura**, e esta task nao finge o contrario.
2. **`deploy/` tem 1 arquivo, e o falsificador nº3 do proprio item me manda dizer isto:** se
   `find deploy -type f | wc -l` continuar **1** DEPOIS da fase que construir o deploy de verdade,
   entao a cobertura foi fechada sobre um diretorio-fantasma. Hoje o 1 e correto — o item fecha
   COBERTURA e o plano proibe construir deploy aqui —, mas o relogio esta armado.
3. **Nao rodei `gate-record` / `approve` / `advance` / `tasks resolve`, e nao commitei.**
4. **Nao toquei o `Makefile`** (dono exclusivo: o builder da `T-01.9`) nem a linha `:61` do
   `harness.toml` ("19 prefixos", hoje 21) — ele declarou que a corrige depois.
```

## Nota de idioma, declarada em vez de descoberta

Duas superficies novas, duas respostas, e o criterio e `harness code-paths classify`:

- **`deploy/compose.yml` -> `producao`** ⇒ comentarios em **ingles** (linha 5 da tabela de
  fronteira do `CLAUDE.md`).
- **`corpus/**` -> fora de `code_paths`** (`corpus/` nao esta em `include_prefixes`) ⇒ comentarios
  em **portugues**, pelo precedente medido de `backend/scripts/*.sh`, que tambem esta fora e e
  portugues.
- A **mensagem da regra** e **microcopy de operador** (linha 8) ⇒ **portugues sem acento**, ao lado
  das outras 10 `[BLOQUEIO]`. Escreve-la em ingles produziria uma listagem bilingue.
