[QA GATE — Fase 07: ETL com dedupe por hash de conteúdo (byte-estável verificado)]
Feature: `plataforma-dados`     Componente: `sentimento`
Arquivos alterados:
- `backend/src/modules/sentimento/domain/content_dedupe.py` (new)
- `backend/src/modules/sentimento/infra/content_dedupe_store.py` (new)
- `backend/src/modules/sentimento/infra/content_deduping_worker.py` (new)
- `backend/src/modules/sentimento/infra/dump_etl_cli.py` (modified — `run()` envolve `DumpIngestWorker` com `ContentDedupingWorker`)
- `backend/tests/sentimento/test_content_dedupe.py` (new — 7 testes)
- `backend/tests/sentimento/test_content_dedupe_store.py` (new — 10 testes, 1 parametrizado em 4)
- `backend/tests/sentimento/test_content_deduping_worker.py` (new — 6 testes)
- `backend/tests/sentimento/test_content_dedupe_across_keys.py` (new — 2 testes, ponta a ponta pela raiz de composição)
- `backend/tests/sentimento/test_dump_etl_cli_surface.py` (modified — corpo de partição distinto por índice, sem colisão de conteúdo)
- `backend/tests/sentimento/test_dump_ingest_edge.py` (modified — idem)
- `backend/README.md` (modified — seção `## 📎 2026-09-02 por T-07.3`)
- `docs/INDEX.md` (modified — linha append-only)

Spec: `docs/context/plataforma-dados` (feature sem SPEC-NNN numerada nesta árvore de contexto — task referenciada por `CA-F3-*`)
Plan: `docs/context/plataforma-dados/tasks_review.md:336` (plano `07` item 7.5), `docs/context/plataforma-dados/tasks.toml:850` (bloco `T-07.3`)
Handoff: `docs/context/plataforma-dados/handoff/T-07.3.md`

DoD:
- [x] Dedupe por HASH DE CONTEÚDO, nunca por nome de arquivo nem timestamp de download —
  `ContentDedupeLedger.decide` (domain/content_dedupe.py) só recebe `(key, digest)`; nenhum
  campo de nome/tempo entra na decisão — `bash backend/scripts/test.sh`
- [x] "Byte-estável verificado" com falsificador: mesmo conteúdo hasheado duas vezes produz o
  MESMO digest — `test_two_different_keys_with_byte_identical_content_are_the_same_item`
  (afirma a igualdade dos dois hashes ANTES de usá-los) e, ponta a ponta pela raiz de
  composição, `test_two_keys_with_byte_identical_content_publish_only_once`
- [x] Um único byte diferente produz digest DIFERENTE, e não é duplicata —
  `test_a_single_differing_byte_produces_a_different_digest_and_is_not_a_duplicate`
- [x] Contra-exemplo rejeitado explicitamente: mesma chave, conteúdo diferente (dump
  republicado/corrigido) NÃO é duplicata —
  `test_the_counter_example_the_dod_names_same_key_different_content_is_not_a_duplicate` (nível
  domínio) e `test_the_dod_counter_example_same_key_reprocessed_with_corrected_content_is_not_hidden`
  (ponta a ponta, publica a correção em vez de escondê-la)
- [x] Reuso do primitivo de hash existente (`checksum_manifest.py`), sem duplicar lógica de
  hashing — `verified_digest_source` usa `ChecksumManifest.parse`/`.verify` e
  `ChecksummedFilePayload.digest()`, não uma segunda implementação de sha256
- [x] Contrato `Natureza`/camadas intocado — `domain/content_dedupe.py` não toca arquivo, rede
  ou relógio; leitura de bytes vive em `infra` — `boundaries.sh`/`natureza.sh`

Comandos rodados (literais) e resultado — **medidos DUAS vezes, antes e depois do merge com
`origin/master`**, e o número reportado é o da 2ª rodada (pós-merge, o que de fato foi pushado):

- ANTES do merge (base `a330e77`): `bash backend/scripts/test.sh` → **1108 passed**, cobertura
  **97,83%** (domain 99,9%, use_cases 100,0%, infra 95,4%)
- `git fetch origin master` → `origin/master@21a5f1e` (trouxe `T-07.4` Redis Streams e `T-07.7`
  circuit breaker, ambos mergeados enquanto esta task rodava); `git merge origin/master
  --no-edit` → 1 conflito, só em `backend/README.md` (duas seções `## 📎` concorrentes,
  resolvido mantendo AMBAS); `docs/INDEX.md` fundiu automaticamente, sem conflito
- DEPOIS do merge: `bash backend/scripts/test.sh` → **1147 passed**, cobertura **97,44%**
  (universo: toda a suíte, `coverage.xml` regravado na mesma rodada) — piso por camada: domain
  **99,8%** (meta 90%, `2245/2249`), use_cases **100,0%** (meta 80%, `549/549`), infra **95,0%**
  (meta 70%, `2166/2279`), `[OK]` nas 3
- `pytest --collect-only -q` sobre as 4 suítes novas → `7+2+10+6 = 25` testes novos desta task
  (universo: `test_content_dedupe.py`, `test_content_dedupe_store.py`,
  `test_content_deduping_worker.py`, `test_content_dedupe_across_keys.py`)
- `bash backend/scripts/lint.sh` (pós-merge) → `ruff check`/`format --check`/`mypy --strict`:
  "All checks passed!" / "212 files already formatted" / "Success: no issues found in 212
  source files"
- `bash backend/scripts/boundaries.sh` (pós-merge) → **3 kept, 0 broken** (universo: 144
  arquivos, 656 dependências)
- `bash backend/scripts/natureza.sh` (pós-merge) → universo 63 arquivo(s) (`domain` 46 +
  `use_cases` 17), **0 leituras de relógio**
- `harness rules --mode sweep --changed-only` (pós-merge) → saída vazia, `rc=0` (nenhum achado,
  nenhuma das 7 regras `[BLOQUEIO]` em vigor se aplica: sem import relativo, sem `except`
  silencioso, sem `print`, sem credencial, sem código de navegador/servidor neste componente)

Falsificador exigido pelo handoff, medido diretamente:
- `test_verified_digest_source_reports_a_missing_sidecar_before_opening_the_payload` prova a
  regressão que a 1ª versão desta task introduziu e o próprio `bash backend/scripts/test.sh`
  achou: ligar `digest_of` direto a `ChecksummedFilePayload(...).digest()` (sem checar o
  sidecar primeiro) faz `test_a_hole_in_the_bucket_stops_the_drain_and_the_cost_is_named`
  reprovar com `FileNotFoundError` em vez do `ChecksumRejectedError` esperado — 4 testes
  pré-existentes quebraram na 1ª rodada (`4 failed, 1102 passed`), todos corrigidos e
  re-verificados verdes na rodada final (`1108 passed`, `0 failed`).

Cobertura (pós-merge): **97,44%** total, domain **99,8%**, use_cases **100,0%**, infra **95,0%**
— piso 90/80/70 (`ADR-009/D1`) cumprido nas 3 camadas — `bash backend/scripts/test.sh`
(`check-coverage-layers.sh` embutido, saída `[OK]` nas 3 linhas).

Doc delta:
- `backend/README.md`: atualizado — seção nova `## 📎 2026-09-02 por T-07.3`, com a tabela de
  peças por camada, o ciclo de correção nomeado (ordem de verificação) e o custo nomeado (hash
  duplo por chave nova, não eliminado nesta task)
- `docs/INDEX.md`: linha append-only acrescentada (nenhuma linha existente reescrita)
- ADR: não necessário — nenhuma decisão arquitetural nova; a forma (decorator de `ItemWorker`,
  ledger imutável, store JSONL no padrão de `jsonl_checkpoint.py`) segue precedente já fixado
  neste componente

Bloqueado (se houver): nenhum item do DoD declarado nesta task. Escopo nomeado como NÃO fechado
por esta task (ver `backend/README.md` §"Escopo que esta task NÃO fecha"):
- hash duplo por chave nova (dedupe + verificação de streaming) — eliminar exigiria mudar o
  contrato de retorno de `ingest_verified_payload.py`, vigiado por asserção de ordem de chamada
  (`test_verified_edge_call_sites.py`); dono é `T-02.4a`, não `T-07.3`
- `md.ingest_run`/`md.ingest_gap` — mesma lacuna já registrada por `T-07.1`, chega com `T-03.8`
- alarme/métrica agregada sobre objetos marcados duplicata — hoje só o log
  `etl_item_duplicate_content`

`tasks.toml`, ledger e Jira INTOCADOS; nenhum `gate-record`, `approve` ou `advance` — veredito é
do `/qa`.
