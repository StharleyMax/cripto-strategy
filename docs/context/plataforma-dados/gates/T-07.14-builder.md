[QA GATE — Fase 07: S5 embutido — `universe_at` portado para TS + badge de delisting]
Feature: `plataforma-dados`     Componente: `web`
Arquivos alterados:
- `frontend/src/app/universe-at.ts` (new)
- `frontend/src/app/universe-at.test.ts` (new — 18 testes)
- `docs/INDEX.md` (modified — linha append-only)

Spec: `docs/specs/SPEC-001-plataforma-dados.md` §6 (S5 não é tela) + §3.7 (`universe_source`)
Plan: `docs/context/plataforma-dados/tasks.toml:965-972` (`T-07.14`, plano `07` item 7.15, `CST-68`)
Handoff: `docs/context/plataforma-dados/handoff/T-07.14.md`
Design gate: `docs/context/plataforma-dados/gates/T-07.14-design.md` — veredito "SEM DECISÃO DE
UI/UX PENDENTE. Nenhuma ação no Stitch"; nenhuma tela tem seletor de símbolo construído hoje
(só um `<span>` estático em `S2`), então este gate não tem imagem/UX a auditar.
Fonte normativa (lida, não editada — `backend/` intocável):
`backend/src/modules/sentimento/domain/universe_at.py` (`T-07.8`, PR já mergeado, QA APPROVED).

## O que foi portado (1:1 com `universe_at.py`)

- `UniverseSource = "snapshot" | "s3_inferred" | "premium_index_witness"`.
- `DecisiveUniverseSource = "snapshot" | "premium_index_witness"` — `s3_inferred` excluído POR
  TIPO do parâmetro de `decideUniverseMembership` (nenhum `if fonte === "s3_inferred"` em
  lugar nenhum).
- `UniverseFilter{market?, underlyingSubType?}` + `filterMatches` — `undefined` = "não filtra
  este eixo", nunca "casa linha sem o campo".
- `decideUniverseMembership(witnesses)` — união dos símbolos das fontes ADMISSÍVEIS só.
- `SymbolSetDivergence`/`compareSymbolSets` — mesma forma de duas pontas, ordenada.
- `UniverseAtResult{ts, symbols, decidedSymbols, s3WitnessSymbols, divergence, label}` —
  `symbols` é SEMPRE `decidedSymbols ∪ s3WitnessSymbols`.
- `RETROSPECTIVE_LABEL = "retrospective_before_first_snapshot"` quando `snapshotRows === null`.
- `buildSnapshotWitnessRows(exchangeInfo, fundingInfo)` — join mínimo (`market` USDS_M/COIN_M),
  plumbing necessário para alimentar `universeAt` com dado real; não é port completo de
  `InstrumentRow` (campos `fundingIntervalHours`/`interestRate` omitidos por não serem lidos
  por nenhuma função deste módulo — escopo que esta task não possui).

## Extensão NOVA desta task (sem correspondente Python)

`stampUniverseRows(result, exchangeInfo)`: para cada símbolo em `result.symbols`, carimba
`universeSource` (união não vazia de fontes que atestaram o símbolo — nunca fusão silenciosa),
`deliveryDate` (lido do `exchangeInfo` CRU, `null` se ausente) e `delistingBadge`
(`hasScheduledDelisting`). `DELISTING_SENTINEL_DELIVERY_DATE = 4133404800000`.

## DoD

- [x] `UniverseSource`/`DecisiveUniverseSource` com `s3_inferred` excluído por tipo —
  verificado por `tsc --noEmit --strict` num arquivo descartável (comando no cabeçalho do
  módulo), e pelo falsificador estrutural em runtime (`decideUniverseMembership.toString()`).
- [x] Caso-âncora: `ICXUSDT`/`STORJUSDT`/`SCRTUSDT` com badge, `BTCUSDT` sem, sobre
  `data/snapshots/2026-08-25_exchangeInfo.json` real.
- [x] Caso retrospectivo: `universeAt('2025-08-01', ...)` sem snapshot → `RETROSPECTIVE_LABEL`,
  badge nunca adivinhado sem `exchangeInfo` (mesmo quando o símbolo REALMENTE vai deslistar).
- [x] Divergência `MATICUSDT`: união sem decisão, `label=null` (snapshot disponível).
- [x] `universe_source` carimbado em toda linha de `stampUniverseRows` (testado
  explicitamente: nenhuma linha tem `universeSource` vazio).
- [x] `backend/` intocado — só leitura via `Read`.

## Comandos rodados (literais) e resultado

```
$ cd frontend && npm run test:app
ℹ tests 115
ℹ pass 115
ℹ fail 0
```
Universo: 115 testes em `frontend/src/app/*.test.ts` (2 arquivos: `threshold-spec-bundle.test.ts`
97 testes pré-existentes + `universe-at.test.ts` **18 novos** desta task —
`grep -c '^test(' frontend/src/app/universe-at.test.ts` → 18).

```
$ npm run lint
> eslint src
(sem saída — 0 problema)
```
Universo: todo `frontend/src` (arquivos `.ts`/`.tsx`/`.js`/`.jsx`/etc., `eslint.config.mjs`).

```
$ harness rules --mode sweep --changed-only --format ndjson
(sem saída, rc=0)
$ harness rules --mode file --path frontend/src/app/universe-at.ts --surface ci --format ndjson
(sem saída, rc=0)
$ harness rules --mode file --path frontend/src/app/universe-at.test.ts --surface ci --format ndjson
(sem saída, rc=0)
```
Universo: os 2 arquivos desta task, staged via `git add`. 0 achado bloqueante.

Falsificador do rule-gate exercido (não só afirmado): um `catch {}` vazio plantado
temporariamente em `universe-at.ts` foi varrido pelo mesmo comando — **não disparou**, porque
`core.silent-except` é escopado a `**/*.py` só (`packs/core/rules.toml:30`), não a TS. Achado
registrado aqui por transparência (o gate não tem regra própria para catch vazio em TS hoje),
arquivo restaurado ao estado limpo antes de seguir (`git diff` confere 0 diferença residual).

```
$ python3 -c "import json,collections; d=json.load(open('data/snapshots/2026-08-25_exchangeInfo.json'));
print(len(d['symbols']), collections.Counter(s['deliveryDate'] for s in d['symbols'])[4133404800000])"
877 743
```
Universo: os 877 símbolos do arquivo real `data/snapshots/2026-08-25_exchangeInfo.json`.

## Cobertura

Não há `test_cmd.web` declarado (`harness.toml:383-393`, deliberado — `test_cmd` não é lido por
nenhum portão, e a `web` component ainda não tem o alvo `Makefile` que `T-01.6`/`T-01.5`
fecharam para `lint-frontend`). Alvo desta task: 100% das branches do módulo novo exercidas
pelos 18 testes (âncora, retrospectivo, divergência, filtro de mercado, filtro de
`underlyingSubType`, união vazia, falsificador estrutural + mutante, sentinela de delisting) —
`[NÃO MEDIDO]` por instrumento de cobertura (nenhum `c8`/`nyc` configurado em `frontend/`); a
alegação acima é por inspeção de branch, não por ferramenta.

## Doc delta

- `docs/INDEX.md`: linha nova, append-only, `2026-09-02T22:31Z` — este artefato.
- `backend/README.md`/`frontend/README.md`: sem mudança — módulo novo que não altera contrato
  existente de nenhum dos dois; motivo explícito, não silêncio.
- ADR: não necessário — nenhuma decisão arquitetural nova (porta um contrato já decidido por
  `SPEC-001`/`ADR-016`/`T-07.8`; a extensão do badge é implementação, não decisão de arquitetura).

## Achado colateral (não corrigido — fora do escopo desta task)

`docs/recorte-plataforma.md:90` e o handoff (`T-07.14.md:61`) citam "sentinela em 568/570
símbolos TRADING+PERPETUAL". Remedição direta desta task, mesmo arquivo, mesmo filtro:
**567/570** (3 sem sentinela: `ICXUSDT`, `STORJUSDT`, `SCRTUSDT` — os mesmos 3 do caso-âncora).
`[MEDIDO 2026-09-02: python3 sobre data/snapshots/2026-08-25_exchangeInfo.json com filtro
status=='TRADING' and contractType=='PERPETUAL' → 570 símbolos, 567 com o sentinela]`.
Divergência de 1 símbolo entre os dois documentos e esta medição — não investigada, porque o
módulo desta task não filtra por `status`/`contractType` (não tem esses campos) e portanto o
número "568/570" não é um valor que este código produz ou depende de reproduzir.

## Escopo NÃO fechado, nomeado

Nenhum dropdown real de seleção de símbolo é construído — não existe em nenhuma tela hoje
(confirmado pelo gate de design). Sem app Next.js — prova por teste unitário só, mesma
fronteira de `T-05.8`/`T-08.5`/`T-08.11`. `tasks.toml`, ledger e Jira INTOCADOS; nenhum
`gate-record`, `approve` ou `advance` — veredito é do `/qa`.

Bloqueado: nenhum.
