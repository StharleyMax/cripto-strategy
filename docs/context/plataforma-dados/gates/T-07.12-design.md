# T-07.12 — design da S1 (console de coleta e retenção)

**Componente:** `web` · **Fase:** 07 · **Data:** 2026-09-02 · **Operador:** `ui-designer` (Stitch)

## 0. O que este documento NÃO tem — e por quê

`docs/context/plataforma-dados/handoff/T-07.12.md`, citado no despacho como leitura obrigatória,
**não existe no worktree** (`git status` limpo, `find` vazio). Segui o despacho mesmo assim,
reconstruindo o contexto a partir das fontes de verdade do próprio `ui-designer.md` (§9 de
`STITCH_CONTEXT.md`, plano `07`, `tasks.toml`, `SPEC-001` §6, `ADR-008`). Isto é `[NÃO SEI]` sobre
por que o handoff não foi escrito — registro, não resolvo.

**Gate `ux-ui-mastery`:** não existe, no meu conjunto de ferramentas, um mecanismo de invocação de
um agente separado rodando `/design-critique` como processo independente. Apliquei os protocolos
publicados em `~/.claude/plugins/.../ux-ui-mastery/3.0.0/commands/design-critique.md` e
`accessibility-check.md` **diretamente e de forma transparente** contra o HTML exportado — isto
**não é o mesmo ato** que um segundo agente concordando de forma independente, e o veredito abaixo
reflete essa limitação: é auto-aplicação do checklist, não um segundo julgamento. Recomendo que uma
sessão fresca rode `/design-critique` sobre o arquivo antes de promover esta tela a canônica, do
mesmo jeito que `S2 Rev. B` foi promovida com `scripts/verify_screen.py` + revisão humana.

## 1. O que foi gerado

`S1` **não existia** no Stitch (`[MEDIDO: get_project, 2026-09-02]` — 3 screenInstances, todas
`S2`). Gerei via `generate_screen_from_text`, `modelId: GEMINI_3_1_PRO`, `deviceType: DESKTOP`,
`designSystem: assets/0334450534074a98ba400e46f5b69dc7` (o mesmo tema que governou `S2 Rev. B`),
prompt = **§9 de `STITCH_CONTEXT.md` colado verbatim** (`R3`) + um adendo específico de `S1`
cobrindo `D7.12`–`D7.15` literalmente.

**Duas chamadas retornaram `"operation timed out"` no client** — mas **ambas completaram no
servidor**: `list_screens` mostrou 2 telas `S1 Console — Diagnóstico Operacional`
(`screens/8d8d58da6472459abb0aba4143d1bf82` e `screens/f950d52a464c45cbac39d8a56c39122c`), geradas
do mesmo prompt. Segui a instrução do tool ("don't retry — poll `get_screen`") por ~6 min antes da
segunda chamada; registro a segunda chamada como desvio consciente da instrução literal, não como
erro — ver seção 4.

## 2. Comparação das duas saídas — evidência, não impressão

| checagem | attempt1 (`8d8d58d…`) | attempt2 (`f950d52…`) |
|---|---|---|
| sino de notificação | **ausente**, com comentário explícito `<!-- No trailing icons as per constraints, removing settings/notifications -->` | **presente**: `<span class="material-symbols-outlined">notifications</span>` — violação literal do item 16(k) |
| microcopy | `pt-BR` em todo lugar, `lang="pt-BR"` | `>LIVE<`, `AS AT T`, `Documentation`, `API Status` — violação literal do item 16(j) |
| `direction-up`/`direction-down`/`integrity-ink` | declarados no tema, **zero aplicações no DOM** | mesmo padrão |
| status badge (`PARADO`/`ATIVO`/`PENDENTE`) | `bg-surface-border text-provenance-strong` — luminância, sem hue | mesmo padrão |

`attempt2` está **desqualificada** por dois itens do bloco DRIFT MEDIDO que o próprio §9 nomeia
como os mais fáceis de reincidir. **`attempt1` é a candidata a canônica.**
`[MEDIDO: grep -c "notifications" e grep -noE '>LIVE<|AS AT T|Documentation|API Status' contra os
dois HTML baixados, 2026-09-02]`

## 3. `attempt1` contra o DoD da fase — o que passa

- **`D7.12` (fórmula, não constante):** passa. Tabela mostra o produto, não só o resultado —
  `"2.206 pts × 1m ≈ 1,5 dia"`, `"~2.000 pts × 5m ≈ 7,0 dias"`, `[DOC-ONLY]` para `daily`,
  `"NÃO MEDIDA"` para o dump S3 — nenhum número inventado.
- **`D7.13` (trilho escrito):** passa. Coluna RESILIÊNCIA mostra `"T1m / SLO ~4.7x"` e
  `"T5m / SLO ~4.7x"` — a multiplicação está no rótulo, não só o trilho.
- **`D7.15` (reconexão é rotina):** passa, e é o ponto mais forte da tela. Painel "Reconexões e
  Rotina" é um log de terminal monoespaçado em `text-provenance-weak` (tinta fraca neutra), sem
  ícone de alerta, sem vermelho: `[10:15:00] WS drop coinalyze-oi` ao lado de
  `[10:15:02] WS resume coinalyze-oi`, tratados como o mesmo tipo de evento.
- **Item 5 / D17 (severidade sem cor), a regra que o §9 chama de "a que mais escapa":** passa. A
  linha `/futures/data/*` (parada) está **fisicamente no topo** da tabela — comentário no código
  confirma a intenção: `<!-- Stopped (TOP) -->` — e o badge `PARADO` usa o mesmo token neutro que
  `ATIVO`. Nenhum vermelho em lugar nenhum do HTML aplicado (`direction-down`/`f23645` só existem na
  declaração do tema, zero uso).

## 4. `attempt1` contra o DoD — o que NÃO passa, e fica registrado para a próxima iteração

1. **`D7.14` não está coberto.** A tabela tem 5 linhas: `/futures/data/*` (parado),
   `OI · grade 1m`, `OI · grade 5m`, `Liq · daily`, `Raw · s3`. **Falta a linha de liquidação de
   grade fina** (`Liq · 1m`, `3.052 pts ≈ 8 dias` no meu prompt) — é justamente essa linha que
   carrega o texto `"janela válida no regime atual, não garantida em cascata"` e a ideia de janela
   que ENCOLHE. `grep -n "cascata\|regime atual"` devolve **zero linhas**. Isto é BLOCKER de DoD,
   não estético.
2. **Cabeçalho reaproveita o seletor de símbolo/timeframe da `S2`** (`BTCUSDT`, `15m` como itens de
   nav ativos) **num console que não é escopado por símbolo.** `S1` monitora coletores e séries
   globalmente — o job da tela (`SPEC-001` §6: "o que está sendo gravado, o que parou") não tem
   sujeito symbol+timeframe. Isto contradiz a própria regra desta conversa
   ("Diferencie sempre: série, painel, símbolo, bucket e observação são coisas distintas") e é o
   tipo de drift que vem de reusar o chrome de `S2` sem adaptar ao escopo de `S1`.
3. **Superfície inventada, herdada — não é defeito novo do gerador.** `bg-surface`/`bg-background`
   (11 aplicações) mapeiam para `#10131a`, e `bg-surface-lowest`/`bg-surface-container-lowest`
   (8 aplicações) para `#0b0e15` — nenhum dos dois é um dos três canônicos
   (`#131722`/`#0d1017`/`#222634`). `[MEDIDO: razão de contraste `#10131a` × `#131722` = **1,038**
   — Brettel/WCAG luminância relativa, script ad-hoc]`. Este número **é o mesmo 1,038** que
   `STITCH_CONTEXT.md` linha 1067 já registra para o asset `0334…` inteiro ("todo `background` fica
   a 1,02–1,04 de `#131722`") — **não é drift novo desta geração, é o tema governante herdando o
   próprio defeito conhecido.** Registro, não resolvo — corrigir o asset é fora do escopo desta task.
4. **Selo de 4 campos (`série`/`idade`/`procedência`/`completude`): `[NÃO SEI]` se se aplica.** A
   regra do §9 item 10 fala de "numeral de MERCADO" (preço/OI/CVD/funding). Os números de `S1`
   (janela em dias, GB/dia, profundidade de fila) são operacionais/meta, não leituras de mercado —
   é uma leitura plausível que o selo estrito não se aplica aqui, mas não é uma leitura que eu
   deva decidir sozinho sem o gate concordando; fica como pergunta aberta, não como violação
   assumida.

## 5. Falsificador D7.17 / gatilho de reabertura de `ADR-008/D3` — verificado, não disparado

O prompt instruiu explicitamente a não resolver o nome de `janela_de_perda` nem desenhar o
consumidor da consulta nomeada. O HTML gerado usa `janela_de_perda` como rótulo de coluna
(`<th>...JANELA_DE_PERDA</th>`), sem propor renomeação e sem simular a query — o gatilho descrito
no despacho (`T-07.13`/`T-07.12` escrevendo o consumidor real) **não foi disparado por este
artefato**, que é HTML estático de mockup, não código de leitura. Fica registrado para quando
`T-07.13` (leitura pela `ingest_health_query`) for implementada de verdade.

## 6. Veredito (revisado 2026-09-02, após a rodada de edição — ver §8)

**Substituído pela seção 8.** `attempt1` (`screens/8d8d58da6472459abb0aba4143d1bf82`) deixou de ser
a candidata corrente: os dois pontos abertos abaixo (item 4.1 `D7.14` e item 4.2 seletor de
símbolo) foram fechados numa edição cirúrgica, gerando `screens/a0bea398e8794689b87452cb2626d3d5`
("S1 Console — Diagnóstico Operacional (Rev. A)"). O texto original desta seção fica preservado por
`R11` (erro/estado revisto não se apaga, se tarja):

~~**ITERAR, não aprovar.** `attempt1` é a base correta — passa nos itens mais difíceis do §9
(severidade sem cor, posição por gravidade, fórmula em vez de constante, reconexão como rotina,
ausência do sino, microcopy pt-BR) — mas não fecha o DoD sozinha: falta a linha/o texto de `D7.14`
(item 4.1) e o cabeçalho carrega um seletor de símbolo que não pertence a este escopo (item 4.2).~~

`attempt2` (`screens/f950d52a464c45cbac39d8a56c39122c`) continua **revogada** pelas duas violações
literais do bloco DRIFT MEDIDO (sino + microcopy EN) — não editar, não promover, inalterado por
esta rodada.

## 7. O que eu NÃO fiz, deliberadamente

Não editei `docs/product/STITCH_CONTEXT.md`. `R6` exige proposta + aprovação antes de escrever §4;
este despacho pediu geração + relatório, não atualização do documento vivo. A entrada de §4.2
proposta, para quando houver aprovação: trocar `[NÃO EXISTE] · fase 07` por
`[RASCUNHO EM ITERAÇÃO] · screens/a0bea398e8794689b87452cb2626d3d5` até o `/design-critique`
independente (§8) confirmar.

## 8. Rodada de edição — 2026-09-02, sob instrução do coordenador

O coordenador confirmou o achado da seção 0 (meu toolset não tem como invocar `/design-critique`
como agente independente) e instruiu uma edição **cirúrgica** dos dois pontos da seção 4, nada
além disso (`R5`). Rodei `edit_screens` sobre `screens/8d8d58da6472459abb0aba4143d1bf82`, mesmo
prompt-base (§9 verbatim) + adendo restrito aos dois pontos, com lista explícita do que **não**
podia mudar. Resultado: `screens/a0bea398e8794689b87452cb2626d3d5`
(`"S1 Console — Diagnóstico Operacional (Rev. A)"`).

**Verificação por `grep` no HTML baixado, não por leitura do resumo que o próprio Stitch devolveu:**

| checagem | comando | resultado |
|---|---|---|
| item 4.1 fechado — linha `Liq · grade 1m` com fórmula e texto de regime | `grep -n "cascata\|regime atual"` | **1 ocorrência**, linha completa: `"Liq · grade 1m · BTC · coinalyze"` / `"3.052 pts × 1m ≈ 8 dias"` / `"janela válida no regime atual, não garantida em cascata"` em `text-provenance-weak` (tinta neutra fraca, mesmo token do resto da tabela — sem cor de severidade) / badge `ATIVO` neutro |
| item 4.2 fechado — seletor de símbolo removido | `grep -n "BTCUSDT\|>15m<"` | **zero ocorrências** |
| chrome preservado como instruído | `grep -n 'href="#">'` | exatamente `AO VIVO`, `COMO EM T`, `MAINNET`, `v2.4`, `UTC` — nada a mais, nada a menos |
| nenhuma regressão nova (sino) | `grep -c "notifications"` | **1**, e é o mesmo comentário inócuo `<!-- No trailing icons as per constraints, removing settings/notifications -->` de `attempt1` — nenhum `<span>` novo |
| nenhuma regressão nova (microcopy EN) | `grep -noE '>LIVE<\|AS AT T\|Documentation\|API Status'` | **zero ocorrências** |
| nenhuma regressão de cor (vermelho na linha nova) | `grep -n "text-error\|f23645"` na região aplicada | **zero** — `f23645` só existe na declaração do tema, uso aplicado zero, igual a `attempt1` |
| escopo respeitado — nada mais mudou | contagem de linhas de tabela + ordem da coluna SÉRIE | **5→6 linhas**, ordem preservada (`/futures/data/*` parado ainda no topo, nova linha inserida logo após `Liq · daily` como pedido, `Raw · s3` ainda por último); contraste das superfícies `#131722`/`#0d1017`/`#222634`/`#10131a`/`#0b0e15` **idêntico** entre `attempt1` e a `Rev. A` (mesma contagem de cada hex) — o defeito herdado da seção 4.3 **não piorou nem foi tocado**, como instruído |

`[MEDIDO: greps acima contra
/tmp/claude-1002/.../scratchpad/s1_rev_a.html baixado de
projects/9264019151773162472/screens/a0bea398e8794689b87452cb2626d3d5, 2026-09-02]`

**Nada mais precisou mudar.** Os dois pontos identificados na seção 4 foram os únicos alterados;
nenhum efeito colateral apareceu na verificação por `grep`. A pergunta aberta da seção 4 item 4
(se o selo de 4 campos se aplica a numerais operacionais) **continua aberta** — não fazia parte do
escopo desta edição e não foi tocada.

**Estado corrente (revisado — ver §9):** `screens/a0bea398e8794689b87452cb2626d3d5` deixou de ser a
candidata corrente. O `/design-critique` independente do coordenador (§9) rodou sobre ela, achou 2
Must-Fix, e uma 3ª rodada de edição os fechou — ver `screens/c0fc0210272f42a1ae29b6364e68d2e4`.

## 9. Gate `ux-ui-mastery` independente — 2026-09-02, rodado pelo coordenador (não por mim)

O coordenador rodou `/design-critique` numa sessão separada (`/workflow`), olhando só o HTML e o
screenshot baixados de `screens/a0bea398e8794689b87452cb2626d3d5` — **este é o segundo agente
concordando de forma independente que a seção 0 apontava como faltante.** Relatório completo:
`docs/context/plataforma-dados/gates/T-07.12-ux-critique.md`.

**Veredito do gate: APROVADO COM CONDIÇÃO** (nota geral 5,6/10). Forças confirmadas: `D17` de
verdade (badges neutros, `PARADO` no topo por posição+glifo, não cor), `D7.12` genuíno (fórmulas
distintas por linha, não texto copiado), `D7.15` bem resolvido (log de reconexão neutro). 2
achados Must-Fix, ambos na linha `Liq · grade 1m`:

1. **Colisão semântica em `RESILIÊNCIA`**: a célula fora reaproveitada para o texto de `D7.14`,
   então a linha não mostrava dado de resiliência nenhum e quebrava o padrão de conteúdo da
   coluna para quem escaneia verticalmente.
2. **`STATUS` sem uptime%**: única linha `ATIVO` sem indicador de %, lendo como dado faltando.

Mais 1 Should-Fix (baixo esforço, não bloqueia): ícones da side nav só com `title`, sem
`aria-label` — não confiável em leitor de tela. Achado 4 (ambiguidade do multiplicador `~4.7x`
repetido em `T1m`/`T5m`) foi explicitamente marcado **não bloqueante, pergunta para depois** —
instrução do coordenador foi não tocar nele nesta rodada.

## 10. 3ª rodada de edição — fechando os achados do gate independente

`edit_screens` sobre `screens/a0bea398e8794689b87452cb2626d3d5`, mesmo prompt-base (§9 de
`STITCH_CONTEXT.md` verbatim) + adendo restrito aos 3 pontos do gate, com lista explícita do que
não podia mudar (inclusive uma instrução para **não tocar** na ambiguidade do achado 4). Resultado:
`screens/c0fc0210272f42a1ae29b6364e68d2e4` (`"S1 Console — Diagnóstico Operacional (Rev. B)"`).

**Verificação por `grep` no HTML baixado — não no resumo que o Stitch devolveu:**

| checagem | comando | resultado |
|---|---|---|
| Must-Fix 1 fechado — `RESILIÊNCIA` restaurada, texto de `D7.14` movido para `JANELA_DE_PERDA` | linha completa da `<tr>` de `Liq · grade 1m` | célula `JANELA_DE_PERDA` agora tem DOIS `<span>`: `"3.052 pts × 1m ≈ 8 dias"` + `"janela válida no regime atual, não garantida em cascata"` (em `text-provenance-weak text-data-sm`, mesma tinta neutra); célula `RESILIÊNCIA` agora mostra `"T1m / SLO ~4.7x"` |
| Must-Fix 1 — texto de regime NÃO ficou na coluna errada | `grep -n "cascata"` | **1 ocorrência**, dentro da célula `JANELA_DE_PERDA` (confirmado pelo contexto da linha), não em `RESILIÊNCIA` |
| `RESILIÊNCIA` da linha nova no mesmo formato das outras | `grep -c "T1m / SLO ~4.7x"` | **2** (a linha `OI · grade 1m` que já tinha, mais a linha `Liq · grade 1m` agora) |
| Must-Fix 2 fechado — uptime% adicionado | `grep -n "99.7%"` | **1 ocorrência**, na célula `STATUS` da linha `Liq · grade 1m`, antes do badge `ATIVO`, mesmo padrão de `99.8%`/`99.9%` das outras linhas |
| Should-Fix fechado — `aria-label` nos 6 links | `awk` no bloco `SideNavBar` + `grep -oE 'title=\|aria-label='` | **6 pares**, cada `aria-label` idêntico ao `title` correspondente: `Console`, `Symbol`, `Inspector`, `Workbench`, `Settings`, `Support` |
| achado 4 (`~4.7x` ambíguo) intocado, como instruído | inspeção da célula `RESILIÊNCIA` das 3 linhas `T*m` | as 3 linhas continuam com `~4.7x` — nada foi explicado, resolvido ou anotado, como pedido |
| nenhuma regressão (sino) | `grep -n "notifications"` | **1**, o mesmo comentário inócuo `<!-- No trailing icons as per constraints... -->` — sem `<span>` novo |
| nenhuma regressão (microcopy EN, vermelho aplicado) | `grep -noE '>LIVE<\|AS AT T\|Documentation\|API Status'` + `grep -n "text-error"` | **zero** nos dois |
| nada mais mudou — linhas/ordem/superfícies | contagem de `<td>` de SÉRIE + contagem de cada hex de superfície | **6 linhas, mesma ordem** de `Rev. A`; `#131722`/`#0d1017`/`#222634`/`#10131a`/`#0b0e15` com **contagem idêntica** entre `Rev. A` e `Rev. B` — nenhum efeito colateral |

`[MEDIDO: greps acima contra
/tmp/claude-1002/.../scratchpad/s1_rev_b.html baixado de
projects/9264019151773162472/screens/c0fc0210272f42a1ae29b6364e68d2e4, 2026-09-02]`

**Estado corrente:** `screens/c0fc0210272f42a1ae29b6364e68d2e4` (`Rev. B`) fecha os 2 Must-Fix e o
1 Should-Fix do gate independente, verificados por `grep` no HTML baixado, não por autoavaliação.
A promoção formal a canônica e a atualização de `STITCH_CONTEXT.md` §4.2 continuam pendentes de
decisão do coordenador/owner — não é ato meu (`R6`).
