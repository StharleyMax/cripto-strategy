[QA GATE — Fase 06: Tabela de shift POR ENDPOINT: dump = REST −5 min]
Feature: `plataforma-dados`     Componente: `sentimento`
Arquivos alterados:
- `backend/src/modules/sentimento/domain/endpoint_shift_table.py` (new)
- `backend/tests/sentimento/test_endpoint_shift_table.py` (new)
- `backend/README.md` (modified — nova seção "📎 2026-09-03 por `T-06.2`")
- `docs/INDEX.md` (modified — linha `/build` acrescentada, append-only)
Spec: docs/specs/SPEC-001-plataforma-dados.md (§2.2)
Plan: docs/plans/SPEC-001-plataforma-dados/06_semantica_declarada.md (item 6.2)
DoD:
- [x] Tabela de shift POR ENDPOINT (não global): `openInterestHist`/`topLongShortPositionRatio`/`topLongShortAccountRatio`/`globalLongShortAccountRatio` → `+300_000` ms; `takerlongshortRatio` → `0` (a exceção) — `ENDPOINT_LABEL_SHIFT_MS`, `bash backend/scripts/test.sh -k test_the_four_measured_endpoints_share_the_positive_five_minute_shift or test_takerlongshortratio_is_the_measured_exception_with_no_shift or test_the_table_has_exactly_the_five_measured_endpoints`
- [x] A exceção é aplicada POR ENDPOINT, nunca por default global — `label_shift_for_endpoint` é lookup de dicionário sem `.get(..., default)`; `test_an_unmeasured_endpoint_is_refused_not_defaulted` prova que um endpoint não medido levanta `UnknownEndpointShiftError` em vez de herdar `+300_000`
- [x] Falsificador medido: `openInterestHist`, dump vs REST, **288 vs 288** timestamps idênticos e **MAE = 0,000000** — `test_open_interest_hist_shift_matches_288_of_288_with_zero_mae`, sobre `data/binance/metrics/btcusdt/2026-08-23.csv` (md5 `fc8c0fba983194cf356a7d172b3bd39e`) e `data/binance/rest/rest_oi.json` (md5 `a3a941904ab9bbe27024929d157ca6d1`) — dado real, não sintético
- [x] Se a tabela estiver errada (shift no endpoint errado), o falsificador reprova — `test_wrong_shift_reprovas_the_falsifier`: aplicar o shift `0` (do taker) ao `openInterestHist` real ainda casa 287/288 timestamps por periodicidade de grade (dump e REST publicam no mesmo passo de 5 min), então o teste verifica o VALOR pareado, não só a contagem — MAE ≈ 41,9 BTC (máx ≈ 496,8) contra o `0,000000` correto
- [x] Se faltar a exceção do `takerlongshortRatio`, o falsificador reprova — `test_shifting_takerlongshortratio_by_five_minutes_reprovas`: aplicar `+300_000` na série taker real derruba a MAE de <0,001 para >0,5 (contra `data/binance/rest/r_takerlongshortRatio.json`, md5 `75821a6532a742127eb91bf2a07caddb`); `test_takerlongshortratio_matches_tightly_with_no_shift` prova o lado positivo (shift `0`, MAE <0,001, >200 pares na janela compartilhada)
- [x] `label_shift` populado com o SINAL da convenção de `SeriesKey` (`+`, somado ao timestamp do dump) e não o inverso descrito em linguagem natural pelo handoff ("REST − 5 min") — justificativa no docstring do módulo, concordando com o `+300_000` que `test_series_identity.py::test_label_shift_is_a_term_with_a_witness` já fixava para `openInterestHist` antes desta task
Comandos rodados (literais) e resultado:
- `bash backend/scripts/test.sh` (suite completa) → **1293 passed**, 0 failed, 0 error (universo: toda `backend/tests/`, incluindo os 16 testes novos de `test_endpoint_shift_table.py`; era 1277 antes desta task)
- cobertura total **97,62%**; por camada (`check-coverage-layers.sh`, `ADR-009/D1`): domain **99,8%** (meta 90%, `2524/2528` linhas), use_cases **100,0%** (meta 80%, `585/585`), infra **95,1%** (meta 70%, `2208/2321`) — 3/3 camadas `[OK]`
- `bash backend/scripts/lint.sh` → `ruff check` + `ruff format --check` + `mypy --strict`: **sem achado**, 235 arquivos
- `harness rules --mode sweep --changed-only` → `rc=0`, **0 achados**
- `bash backend/scripts/boundaries.sh` → **3 kept, 0 broken** (156 arquivos, 707 dependências) — `Camadas por contexto`, `Fronteira de contexto` e `Natureza: domain/use_cases não falam com socket/ssl`, todos `KEPT`
- `bash backend/scripts/natureza.sh` → **71 arquivo(s) (52 domain + 19 use_cases), 0 leitura(s) de relógio**
- Medição bruta da tabela (fora do pytest, prova independente do número que o teste fixa): `python3 -c "..."` comparando `create_time+300000` de `data/binance/metrics/btcusdt/2026-08-23.csv` contra `data/binance/rest/rest_oi.json` → `True` (conjuntos idênticos), `MAE=0.0`, `n=288` — comando completo no docstring de `endpoint_shift_table.py`
Cobertura: 97,62% total / domain 99,8% / use_cases 100,0% / infra 95,1% (alvo: domain 90%, use_cases 80%, infra 70% — `ADR-009/D1`, confirmado por `check-coverage-layers.sh`)
Doc delta:
- `backend/README.md`: atualizado — nova seção "📎 2026-09-03 por `T-06.2` — tabela de shift POR ENDPOINT: dump = REST −5 min, exceto o taker", mesmo padrão das seções de `T-06.1`/`T-07.x` (peça, sinal, falsificador, comandos literais, escopo não fechado nomeado)
- `docs/INDEX.md`: linha `/build` acrescentada (append-only), apontando para este gate
- ADR: não necessário — nenhuma decisão arquitetural nova; o módulo é lógica pura de `domain` (`ADR-016`/`Natureza`) consumindo `SeriesKey` já existente, sem alterar sua forma
Bloqueado (se houver): nenhum. Escopo explicitamente NÃO fechado por esta task, nomeado no doc delta: `topLongShortPositionRatio` não tem captura REST própria em disco (`data/MANIFEST.md` não cataloga uma) — seu `+300_000` é aplicado por semelhança de família de endpoint (mesmo dump de origem, mesma medição indireta que resolveu os outros três), não por medição direta desse endpoint específico; nenhuma linha real de `series_catalog` é escrita para estes 5 endpoints (composição de `T-06.3`/`T-06.5`/`T-06.9`, tasks futuras); `buyVol`/`sellVol` do REST `takerlongshortRatio` não são persistidos aqui (`T-06.10`).
