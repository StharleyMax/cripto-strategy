[QA GATE — Fase 06: `funding_settled` ≠ `funding_estimado`, séries distintas]
Feature: `plataforma-dados`     Componente: `sentimento`     Task: `T-06.4` (`CST-48`)
PR: https://github.com/StharleyMax/cripto-strategy/pull/105 (branch `tasks/T-06.4-fase06`)
Worktree: `/tmp/claude-1002/wt/T-06.4`

## Estado do branch (rebase)

`git rev-parse master origin/master HEAD` → `master`/`origin/master` = `6d3f284` (`T-06.10`),
`HEAD` = `b80df21` = `master` + 2 commits (`a514fb4` T-06.4, `b80df21` docs). `git log --oneline
HEAD..origin/master` vazio: branch já está integralmente rebaseada, nenhum merge adicional
necessário. `git diff master...HEAD --stat`: 3 arquivos de `T-06.4` (`funding_settlement.py`,
2 arquivos de teste novos, `test_as_of_is_the_single_reader.py` modificado) + docs (`README.md`,
`INDEX.md`) — o resto do stat (`S3Inspector`, `T-06.10`) é herança do merge de `T-06.10` na
base, não trabalho desta task.

## Testes existem e passam

- `bash backend/scripts/test.sh` (suíte completa) → **1366 passed, 3 warnings in 964,62s**
  (log em `/tmp/claude-1002/.../scratchpad/full_test.log`). `Required test coverage of 70.0%
  reached. Total coverage: 97.68%`.
- `.venv/bin/python -m pytest tests/sentimento/test_funding_settlement.py
  tests/sentimento/test_funding_next_funding_time_grid.py
  tests/sentimento/test_as_of_is_the_single_reader.py -v` → **24 passed** (16 + 3 + 5),
  confirma a contagem do builder isoladamente.

## Cobertura vs alvo (`ADR-009/D1`)

- domain **99,8%** (2715/2720) contra alvo 90% → `[OK]`
- use_cases **100,0%** (585/585) contra alvo 80% → `[OK]`
- infra **95,1%** (2208/2321) contra alvo 70% → `[OK]`
- total **97,68%** — `check-coverage-layers.sh` roda dentro de `test.sh`, saída acima, `rc=0`.

## Mutação real — D6.11 (settlement_slot por linha, não fixo)

**Mutação plantada:** `funding_settlement.py::compute_settlement_slot`, troquei
`grid_ms = interval_hours_declared * MILLISECONDS_PER_HOUR` por `grid_ms = 8 *
MILLISECONDS_PER_HOUR` (grade fixa de 8h, o defeito antigo que `D6.11` nomeia).

**Pega:**
```
tests/sentimento/test_funding_settlement.py::test_d6_11_every_row_of_the_real_fixture_lands_in_a_small_non_negative_residual FAILED
  assert max(residuals) == 12  ->  AssertionError: assert 25200006 == 12
tests/sentimento/test_funding_next_funding_time_grid.py::test_d6_16_nextfundingtime_divides_evenly_by_its_own_interval_for_every_symbol FAILED
  3 of 569 symbols have a nextFundingTime off their own interval grid: ['ZKCUSDT', 'SKRUSDT', 'ONGUSDT']
```
2 failed, 17 passed (dos 19 testes do módulo D6.11/D6.16). **Revertido**; `git diff --stat`
depois da reversão: vazio (nenhuma alteração de produção sobrevive).

Nota: `test_d6_11_the_old_fixed_interval_formula_misplaces_most_rows_the_per_line_one_does_not`
NÃO pega esta mutação — ele computa a fórmula fixa por conta própria (não chama
`compute_settlement_slot`), então serve como BASELINE do defeito antigo, não como sensor da
mutação em produção. Isso é esperado e não é uma lacuna: os dois testes de D6.11 que CHAMAM a
função de produção (`test_d6_11_every_row...` e o `D6.16` em cadeia via `settlement_residual_ms`)
pegaram a mutação; o terceiro é intencionalmente independente da produção (mede a régua, não o
código).

## Mutação real — D6.12 (PK inclui `source`)

**Mutação plantada:** `FundingRecord.primary_key` retornando `(instrument_id, settle_bucket,
observed_at)` — removendo `source` da tupla.

**Pega:**
```
test_funding_settled_and_funding_estimado_never_collide_at_the_same_window FAILED
  assert settled.primary_key() != estimated.primary_key()
  AssertionError: assert ('1000XECUSDT', 28800000, 28800000) != ('1000XECUSDT', 28800000, 28800000)
```
1 failed, 15 passed (dos 16 de `test_funding_settlement.py`). **Revertido**; `git status
--short` e `git diff --stat` limpos depois.

Confirma literalmente o ponto do DoD: sem `source` na PK, `funding_settled` e
`funding_estimado` da mesma janela colidiriam e `deduplicate_funding_records` colapsaria as
duas séries numa só — exatamente o "nunca a mesma linha sobrescrita" que o DoD proíbe.

## D6.12 — dedupe 642→321 e transições reais

`test_d6_12_ingesting_the_fixture_twice_still_yields_321_rows` (321×2=642 → dedupe → 321,
`deduped == once`) e `test_d6_12_dedupe_survives_the_8h_to_1h_to_4h_transition` (índice 38:
8h→1h delta ≈1,0h; índice 267: 1h→4h delta exatamente 3,0h, ambos sobreviventes distintos) —
rodados, verdes, sobre o fixture real `1000XECUSDT-fundingRate-2026-07.zip`
(md5 `40e35f60a065aac30f2d08d7a47139bc`, confirmado por `require_fixture`).
`test_a_primary_key_seen_twice_with_different_content_is_a_conflict_not_a_duplicate` prova que
PK igual + conteúdo diferente levanta `ConflictingFundingRecordError` em vez de escolher em
silêncio.

## D6.16 — divergência de data DECLARADA, não escondida

O plano/handoff citam `570/570` medido em `2026-08-25`. O fixture dessa data não tem
`premiumIndex` companheiro (`data/MANIFEST.md:144`), então a tripla completa não existe para
`2026-08-25` — só para `2026-09-01`. **A divergência está nomeada em 3 lugares, não escondida
atrás de um "570/570" falso:**
1. docstring do módulo `test_funding_next_funding_time_grid.py:1-14` — cita a lacuna do
   MANIFEST literalmente e diz "RE-MEASURES... rather than transcribing the 2026-08-25 figure
   it cannot reproduce";
2. `backend/README.md:3345-3348` (seção `T-06.4`) e a linha do `docs/INDEX.md` (`08:30Z`) —
   ambos escrevem "divergência de fixture DECLARADA" com o número real (569, não 570) ao lado
   da data `2026-09-01`;
3. relatório do builder, seção "Escopo NÃO fechado": "D6.16 não reproduz o número 570/433 de
   2026-08-25... divergência declarada acima, não escondida."

Rodei `test_d6_16_the_matched_triple_has_569_common_trading_perpetual_symbols` e
`test_d6_16_nextfundingtime_divides_evenly_by_its_own_interval_for_every_symbol` e
`test_d6_16_4h_is_the_rule_not_8h_with_the_snapshot_date_attached` — os três passam, `569/569`
sem exceção, distribuição `{4h:430, 8h:136, 1h:3}`, 4h continua sendo a pluralidade (nunca 8h).
**Nenhuma citação encontrada em código ou doc afirmando "570/570" como se fosse medição —
sempre 569, com a data ao lado.**

## Séries distintas (`SeriesKey`/`SeriesCatalogEntry`)

`test_funding_settled_and_funding_estimado_are_two_catalog_entries`: duas
`SeriesCatalogEntry` (`metric="funding_settled"` / `metric="funding_estimado"`) convivem em UM
`SeriesCatalog` via `build_series_catalog` — a mesma função que reprova colisão de
`series_key_id()` não colide aqui, prova de identidade distinta (não checado por mutação
adicional: já coberto pela mutação de PK acima, que é o mecanismo que produziria a colisão se
`source` não distinguisse as duas).

## Contrato `Natureza` (`ADR-016`) — domain puro

`bash backend/scripts/natureza.sh` → `universo de 76 arquivo(s), 0 leitura(s) de relogio`.
`bash backend/scripts/boundaries.sh` → `Contracts: 3 kept, 0 broken` (162 arquivos, 735
dependências) — `funding_settlement.py` não importa `infra`/`use_cases`. Inspeção do arquivo:
único import além de stdlib é `collections.abc.Sequence`/`dataclasses`/`decimal`/`enum`/
`typing` — nenhum I/O, nenhum relógio, nenhuma rede.

## Idioma inglês (produção e teste)

`funding_settlement.py`, `test_funding_settlement.py`, `test_funding_next_funding_time_grid.py`:
identificadores, docstrings e mensagens de exceção em inglês. `harness rules --mode sweep
--changed-only --format ndjson` → saída vazia, `rc=0` (0 achados). `bash backend/scripts/
lint.sh` → `ruff check`/`ruff format --check`/`mypy --strict` sem achado, 246 arquivos.

## Regras bloqueantes (`harness rules list --severity block`, 7 regras)

- [OK] `core.relative-import` — `funding_settlement.py` só importa `from __future__` e stdlib;
  nenhum import relativo.
- [OK] `core.silent-except` — nenhum `except` no arquivo (a única forma de recusa é `raise`
  explícito nas 3 exceções nomeadas).
- [OK] `core.print-statement` — nenhum `print(` no arquivo/testes.
- [OK] `core.hardcoded-secret` — nenhuma credencial; arquivo é lógica de grade/dedupe pura.
- [N/A] `web-fullstack.browser-imports-server` — escopo `sentimento`/backend, não toca web.
- [N/A] `web-fullstack.tenant-from-request` — sem servidor/request nesta task.
- [N/A] `web-fullstack.server-test-directory-present` — não aplicável a este componente.
- Evidência agregada: `harness rules --mode sweep --changed-only --format ndjson` → vazio,
  `rc=0`, sobre os 4 arquivos alterados (3 novos + 1 modificado).

## DoD, item a item

- [x] `funding_settled`/`funding_estimado` nunca a mesma `SeriesKey` — provado por teste +
  mutação de PK (acima).
- [x] `interval_hours_declared` por linha, nunca constante — `compute_settlement_slot` recebe
  o intervalo como parâmetro; mutação para valor fixo (8h) provou que o teste pega a regressão.
- [x] PK inclui `source` — mutação removendo `source` da PK provou colisão e falha de teste.
- [x] D6.11 — reproduzido no fixture real, 0/321 fora de `[0,20]` ms por linha vs 228/321=71,0%
  sob fórmula fixa; mutação em produção pega.
- [x] D6.12 — 642→321, transições 8h→1h/1h→4h sobrevivem distintas, conflito de conteúdo
  recusa.
- [x] D6.16 — 569/569 (não 570), divergência de data nomeada em 3 lugares, nunca escondida.
- [x] `Natureza`/`ADR-016` — domain puro, 0 leituras de relógio, boundaries mantidas.
- [x] Idioma inglês — produção e teste, `rules sweep` vazio.

Sem anomalias.

Regras bloqueantes avaliadas: 7 de 7 listadas por `harness rules list --severity block`.

Veredito: **APPROVED**
