[QA GATE — Fase 08: `ThresholdSpec` bundle versionado e hasheável]
Feature: `plataforma-dados`     Componente: `web`
Arquivos alterados:
- `frontend/src/app/threshold-spec-bundle.ts` (new)
- `frontend/src/app/threshold-spec-bundle.test.ts` (new)
- `frontend/README.md` (modified — §12 acrescentada)
Spec: docs/specs/SPEC-001-plataforma-dados.md (§3.7 linhas 292-303, §7 linhas 560-568)
Plan: docs/plans/SPEC-001-plataforma-dados/08_superficie_e_reprodutibilidade.md (item 8.4, DoD D8.3)
DoD:
- [x] `ThresholdSpec` = `Absolute{pct,op} | Percentile{q,window,scope,min_obs,interpolation,op} | RobustZ{k,window,min_obs,op}` + `spec_version`, `Custom{expr}` fora da união — `npm --prefix frontend run test:app`
- [x] nenhum eixo tem default, inclusive `op` — testes "decode REFUSES a bundle missing op, on every variant" e mutação "assertValidThresholdSpec REFUSES an op that bypassed decode"
- [x] `minObs` obrigatório por TIPO em `Percentile`/`RobustZ` (não opcional na interface) — testes "decode REFUSES a Percentile/RobustZ bundle missing minObs"
- [x] `Custom{expr}` desabilitado por padrão — teste "decode REFUSES variant=custom explicitly"
- [x] D8.3 (teste negativo obrigatório): URL sem `ThresholdSpec` → decode recusa, nunca assume — testes "D8.3: decodeBundle/decodeThresholdSpec on an EMPTY URL refuses"
- [x] `spec_version` versiona o bundle — testes de specVersion ausente/desconhecido
Comandos rodados (literais) e resultado:
- `npm --prefix frontend run test:app` → `node --test 'src/app/*.test.ts'`: **96 pass, 0 fail** (universo: 6 arquivos `*.test.ts` em `src/app/`, 28 testes novos em `threshold-spec-bundle.test.ts`)
- `npm --prefix frontend run lint` → `eslint src`: 0 erro, 0 aviso (universo: toda a árvore `frontend/src`)
- `git add frontend/src/app/threshold-spec-bundle.ts frontend/src/app/threshold-spec-bundle.test.ts frontend/README.md && harness rules --mode sweep --changed-only` → 1 achado `[AVISO]` `web-fullstack.hardcoded-url` em `threshold-spec-bundle.test.ts:22` (mesmo padrão pré-existente de `T-05.8`/`T-05.9`/`T-08.11` — `base` sintético de teste, nenhum host contatado), **0 achado de severidade block**, `rc=0`
- `harness rules list --severity block` → 7 regras em vigor (universo: packs `core` + `web-fullstack`), nenhuma aplicável a um módulo puro de encode/decode/validação sem import, `except`, `print` ou credencial
Cobertura: não medida por ferramenta de cobertura (não há `nyc`/`c8` configurado para `frontend/`, mesma lacuna que `T-05.8`/`T-05.9`/`T-08.11` já registraram) `[NÃO MEDIDO]`. Cobertura funcional por inspeção: todo axis de todo variant tem teste de presença E de mutação (op ausente/inválido em 3 variantes, minObs ausente/> window, q fora de (0,100), interpolation fora do vocabulário, k ≤ 0, window ≤ 0, scope vazio, variant=custom, variant desconhecido, specVersion ausente/desconhecido) — 28 testes novos cobrindo 1 módulo de ~330 linhas.
Doc delta:
- `frontend/README.md`: atualizado — §12 nova, "src/app/threshold-spec-bundle.ts — o bundle de ThresholdSpec de T-08.5", mesmo padrão de §8/§9/§11 (decisões de forma documentadas com a leitura que as produziu, comandos literais, achado de regra citado)
- ADR: não necessário — nenhuma decisão arquitetural nova; forma segue o precedente já fixado por `T-05.8` (`knowledge-time-bundle.ts`), decisões de vocabulário (op/interpolation/scope) são inferências documentadas em comentário no próprio código-fonte, não decisões estruturais
Bloqueado (se houver): nenhum. `S4` (a tela que um dia consumiria este bundle) não existe — confirmado pelo gate de design (`docs/context/plataforma-dados/gates/T-08.5-design.md`), fora do escopo desta task (`T-08.6`, dependente).
