# QA Gate (Front) — `T-05.14` / `CST-105` — "O cliente liga na rota real" — RODADA 2

**Papel:** `frontend_qa` · **Data:** 2026-09-04 · **Branch:** `tasks/T-05.14-cliente-rota-real`
**Worktree:** `/tmp/claude-1002/wt/T-05.14` (só leitura de produção; um mutante temporário foi
aplicado e revertido byte a byte só para falsificar a rodada 1, `grep -n "MUTATION FOR" …ts` → 0
depois). **Contra:** `gates/T-05.14-qa.md` rodada 1 (5 achados `[FAIL]`, `NEEDS_FIX`),
`gates/T-05.14-builder.md` rodada 2 (resposta ponto a ponto), `ADR-019`,
`docs/plans/SPEC-001-plataforma-dados/05_fatia_visivel.md:74,87,225-226`.

## O que ataquei nesta rodada, e o resultado

1. **`npm --prefix frontend run test:s1`, rodado por mim** → **48 pass, 0 fail**
   (universo: `src/features/s1-console/*.test.ts`, 4 arquivos) `[MEDIDO]`. Bate com o alegado.

2. **Ataquei o MORDE(i) "servidor ausente" com mutação minha, não a do builder**: troquei
   `fetchIngestHealthProjectionViaHttp` para engolir a exceção de `doFetch` num `try/catch` e
   devolver projeção vazia (`{runs:[],gaps:[]}`) em vez de rejeitar. Rodei
   `node --test ingest-health-query-http.test.ts` com a mutação viva:
   `MORDE D5.14(i): porta sem listener nenhum` **reprovou** (`Missing expected rejection`) —
   `AssertionError` real, não vazia; `7 pass / 1 fail`. O outro teste (servidor derrubado no
   meio) continuou passando porque o ponto de falha dele é `response.json()`, fora da região
   mutada — confirma que os dois testes cobrem dois estágios distintos, não é redundância.
   Revertido (`cp` do backup); suíte http voltou a **8/8**. **O par morde/cala do achado 1 da
   rodada 1 está genuinamente fechado, verificado por mutação minha, não só a do builder.**

3. **Li `ingest-health-query-http.test.ts` inteiro (540 linhas)**: o teste `CALA G1` chama
   `collectorRowsFromIngestHealthProjection` **e** `buildS1ViewModelFromIngestHealthProjection`
   sobre `result.projection` — o retorno real de `fetchIngestHealthProjectionViaHttp`, não uma
   fixture TS — com um dataset de **4 runs / 3 séries / 1 gap**, dois runs compartilhando
   `(source, endpoint)` em `started_at` diferentes. Assevera "mais recente vence"
   (`uptimePercent: 95`, não o do run mais antigo), `bybit` → `PARADO`/`uptimePercent: 0`,
   `retention: unmeasured`/`resilience: not_scored`, e `viewModel.rows[0].statusCell.status ===
   "PARADO"` (D17). **Não é caso trivial de 1 linha — é o dataset multi-run/multi-gap que a
   rodada 1 exigiu**, e resolve exatamente o "nunca chama
   `buildS1ViewModelFromIngestHealthProjection`" da rodada 1 (achado 2).

4. **Procurei um 12º MORDE que os 11 novos não cobrem — achei um**: nenhum dos 10 testes
   `MORDE` de `parseIngestHealthEnvelope` cobre um **elemento** de `runs[]`/`gaps[]` que não é
   objeto plano (`runs: [null]`, `gaps: [["não", "objeto"]]`). O código já trata isso —
   `assertIngestHealthRunRow`/`assertIngestHealthGapRow` chamam `isPlainRecord(value)` por
   elemento e lançam `"runs[i] is not a plain object"`/`"gaps[i] is not a plain object"` — confirmei
   rodando uma prova descartável (`zzz-qa-probe.test.ts`, criado e apagado nesta sessão, nunca
   commitado): as duas chamadas **rejeitam corretamente**. **O comportamento está certo; a
   cobertura versionada dessa branch específica está ausente** — é uma mutação que trocasse
   `isPlainRecord(value)` por `true` (ou apagasse o `if` por elemento) não seria pega por
   nenhum teste hoje. Achado `WARNING`, não `BLOCKER`: é uma branch estreita dentro de uma
   função já bem coberta nos outros 10 casos, não um subsistema inteiro sem prova.

5. **`make lint-frontend`, rodado por mim** → `eslint src` limpo + `tsc -p tsconfig.json
   --noEmit --strict` limpo, saída vazia nos dois. `harness rules --mode sweep --changed-only`
   → `rc=0`, saída vazia (rodei eu mesma, não só confiei no builder). `harness rules --mode
   file --path .../ingest-health-query.ts` → `rc=0` isolado também.

6. **Doc delta**: `gates/T-05.14-builder.md` existe agora, e `docs/INDEX.md` ganhou UMA linha
   ACRESCENTADA (`git diff docs/INDEX.md` → só `+`, nenhuma linha existente reescrita).
   **Ponto fraco menor**: a ação 5 da rodada 1 pedia, se a decisão fosse "sem seção nova em
   `frontend/README.md`", uma justificativa explícita — o builder não a deu por escrito. Verifiquei
   eu mesma: a seção existente do README sobre `ingest-health-query.ts` (linha ~1523, o achado
   `web-fullstack.browser-imports-server` não vendo `spawnSync`) continua válida porque
   `runIngestHealthCli`/o `spawnSync` que ela descreve **não foram tocados** nesta task — só
   `fetchIngestHealthProjectionViaCli` (a chamadora) morreu. Não achei padrão novo que devesse
   estar documentado e não está — `ADR-019` já cobre a decisão estrutural (D4: `INGEST_HEALTH_
   API_BASE_URL`, nunca `NEXT_PUBLIC_*`). `WARNING` de processo (falta a frase), não de doc
   ausente.

## Veredito

```markdown
## QA Gate (Front) — Fase 05: T-05.14 cliente na rota real — rodada 2
- [OK] DoD D5.14 MORDE(i) "servidor ausente" — 2 testes novos, falsificados por mutação MINHA
       (não só a do builder): `node --test ingest-health-query-http.test.ts`
- [OK] DoD D5.14 CALA "view-model montado" / G1 — testado sobre a rota real, dataset 4 runs/3
       séries/1 gap, "mais recente vence" e D17 confirmados
- [OK] Par morde/cala da validação de borda (`parseIngestHealthEnvelope`) — 10 MORDE + 1 CALA
       controle; 1 branch estreita (elemento não-objeto em runs[]/gaps[]) sem teste versionado
       (achado 4, WARNING)
- [OK] Lógica fora do componente / escopo D1 — inalterado desde rodada 1
- [OK] Contrato tipado na borda, agora falsificado (achado 3 da rodada 1 fechado)
- [OK] Sem segredo no cliente; URL só por `process.env`, nunca `NEXT_PUBLIC_*`
- [OK] Testes existentes passam: 48/48 (`test:s1`, rodado por mim), 8/8 isolado (http, 3x sem flake)
- [NÃO MEDIDO] Cobertura — sem instrumento de cobertura de front neste repo, comando: nenhum
- [OK] `harness rules --mode sweep --changed-only` / `--mode file` sem bloqueante, rc=0 (rodado
       por mim, não só citado)
- [OK] `make lint-frontend` verde (eslint + tsc --noEmit --strict), rodado por mim
- [OK] Doc delta — `gates/T-05.14-builder.md` + linha ACRESCENTADA em `docs/INDEX.md`; falta só
       a frase de justificativa sobre README (achado 5, WARNING, não bloqueia)

Achados:
1. [WARNING] `parseIngestHealthEnvelope`: elemento de `runs[]`/`gaps[]` que não é objeto plano
   (`null`, array, primitivo) não tem teste MORDE versionado — comportamento correto (verificado
   manualmente nesta sessão), só falta o teste. `ingest-health-query.test.ts`.
2. [WARNING] `gates/T-05.14-builder.md` não justifica por escrito por que `frontend/README.md`
   não ganhou seção nova, como a ação 5 da rodada 1 pedia explicitamente. Verifiquei que a seção
   existente continua válida (o `spawnSync` que ela descreve não mudou), mas a frase devia estar
   no gate, não só inferível por quem reabrir o arquivo.

Veredito: APPROVED
Ações (não bloqueiam, registradas para não virar dívida silenciosa):
1. Acrescentar 1-2 testes MORDE de `runs[i]`/`gaps[i]` não-objeto a `ingest-health-query.test.ts`.
2. Uma frase em `gates/T-05.14-builder.md` (ou como append em `docs/INDEX.md` na próxima task)
   registrando por que `frontend/README.md` não mudou nesta rodada.
Relatório completo: docs/context/plataforma-dados/gates/T-05.14-qa.md
```
