# `T-05.11` — decisão estrutural do `architect` (`web`), antes do `/build`

**Papel:** `architect` (`web`, `A6`) · **Data:** 2026-09-04 · **Task:** `T-05.11` / `CST-102`
**Decisão normativa:** [`ADR-018`](../../../adr/ADR-018-scaffold-next-tipo-em-make-lint-frontend.md) — este arquivo é o
log de validação que a sustenta, não uma segunda decisão.

**Ancoragem:** `master@758e2ef`, worktree isolado `/tmp/claude-1002/wt/T-05.11`, branch
`tasks/T-05.11-scaffold-next`. **Nenhum commit foi feito nesta worktree** — instalei
`next`/`react`/`react-dom`/`@types/*`, criei o scaffold mínimo, rodei os 4 experimentos abaixo, e
depois `git checkout -- frontend/package.json frontend/package-lock.json` + `rm` dos arquivos novos.
O estado em disco hoje é idêntico ao de antes desta sessão (`git status --short` → vazio); o que
sobra é este log e o `ADR-018`.

## 1 · Por que validei ao vivo em vez de só decidir no papel

`CLAUDE.md`: *"nenhum número sem o comando que o produziu"*. Duas das decisões de `ADR-018` só
existem porque rodei o comando real e ele discordou da minha primeira suposição — `jsx: "preserve"`
(convenção antiga do Next) e `moduleResolution` sem `allowImportingTsExtensions` (suposição óbvia)
**os dois falharam** contra este repositório especificamente. Documentar isso sem o log seria
`[INFERRED]` disfarçado de `[MEDIDO]`.

## 2 · Instalação — versões reais resolvidas

```
$ npm install next@16.3.4 react@19.2.8 react-dom@19.2.8 --save
$ npm install -D @types/node@^24.13.3 @types/react@^19.2.18 @types/react-dom@^19.2.7 @types/jsdom@^30.0.0
```
Ambos `rc=0`. `node --version` → `v24.10.0`; `next --version` (via `./node_modules/.bin/next`) →
`16.3.4 (Turbopack)`.

## 3 · `tsc --noEmit --strict` — baseline medido, 3 rodadas até o piso real

**Rodada 1** (`tsconfig` com `moduleResolution: "bundler"`, sem `allowImportingTsExtensions`,
`target: "ES2017"`): dezenas de `TS5097` (import com extensão `.ts` proibida) + `TS2737` (literal
`BigInt` abaixo de ES2020, `src/charts/s2-cvd.ts`/`.test.ts`) + `TS7016` (`jsdom` sem tipo).

**Rodada 2** (`allowImportingTsExtensions: true` + `target: "ES2022"`): os `TS5097`/`TS2737` somem.
Sobra só `TS7016` (`jsdom`) + os erros reais de `headless-chart.ts`/`s2-axis-integration.test.ts`.

**Rodada 3** (+ `@types/jsdom`): piso final —

```
$ ./node_modules/.bin/tsc -p tsconfig.json --noEmit --strict 2>&1 | grep -c 'error TS'
7
$ ... | grep -oE '^src/[^(]+' | sort -u
src/charts/headless-chart.ts
src/charts/s2-axis-integration.test.ts
```

7 erros, 2 arquivos, nenhum tocado pelo scaffold — inventário completo em `ADR-018` §D5.

## 4 · `D5.16b` — renderiza, não só compila (o log que sustenta `ADR-018` §D5.16b)

```
$ npm --prefix frontend run build            # com next.config.ts SEM ignoreBuildErrors
✓ Compiled successfully
Route (app): /painel  ○ (Static)

$ next start -p 4319 &
$ curl -s http://127.0.0.1:4319/painel -o painel.html
$ for m in "Filtro: any resultado serve" "Monitoramento de Coletores e Ingestão" "Catálogo de Séries"; do
    grep -c "$m" painel.html
  done
1
1
1
```

**MORDE (i) — comentei `<S3Inspector .../>` na página, deixando os imports/uso morto:**
```
$ npm --prefix frontend run build
✓ Compiled successfully        # continua VERDE — este é o vácuo que D5.16b nomeia
$ curl -s http://127.0.0.1:4320/painel | grep -c "Catálogo de Séries"
0
```

**MORDE (ii) — processo derrubado:**
```
$ pkill -f 'next start'
$ curl -s -o /dev/null -w 'http_code=%{http_code}\n' --max-time 2 http://127.0.0.1:4319/painel
http_code=000
$ echo $?     # curl
7             # "Failed to connect" — recusa de conexão, não payload vazio
```

**Limitação declarada, não escondida:** sem Vitest/Testing Library/Playwright (fora do escopo desta
task), este morde/cala não vira portão de `make` nesta rodada — é prova manual, mesmo precedente de
`T-07.14` (`docs/INDEX.md:127`). Quem instalar Playwright depois tem aqui os 3 marcadores exatos e o
padrão de porta 127.0.0.1 já provado.

## 5 · O que o `frontend-builder` implementa, item por item

1. `frontend/package.json` + `package-lock.json` — deps de `ADR-018` §D1, scripts de §D4.
2. `frontend/tsconfig.json` — conteúdo EXATO de `ADR-018` §D3 (cada linha já tem o motivo medido).
3. `frontend/next.config.ts` — mínimo, **sem** `ignoreBuildErrors`/`ignoreDuringBuilds`.
4. `frontend/src/app/layout.tsx` — Server Component, `<html lang="pt-BR"><body>{children}</body></html>`.
5. `frontend/src/app/painel/page.tsx` — Client Component, conteúdo de referência em `ADR-018` §D2
   (validado ao vivo — pode copiar quase verbatim; decidir o destino de `openedSeriesId`).
6. Corrigir os 7 erros de `ADR-018` §D5 (`headless-chart.ts` × 3, `s2-axis-integration.test.ts` × 4)
   — cast mecânico, sem mudar comportamento.
7. `Makefile` alvo `lint-frontend` — `+1` linha (`ADR-018` §D4).
8. Rodar o roteiro de §4 deste arquivo E o de `D5.16` (erro plantado → `make lint` nomeia arquivo e
   linha + `git push --dry-run` recusado com o hook instalado) e colar o output real no gate de
   `/build`.
9. Doc delta esperado: `frontend/README.md` §1 hoje diz *"frontend/ ainda não é a aplicação"* — deixa
   de ser verdade; o `/build` desta task é quem escreve a seção nova, não eu.

**Fora do escopo desta task, declarado para não ser confundido com omissão:** Tailwind/CSS pipeline
(os `className` de `S1Console.tsx`/`S3Inspector.tsx` já existem como strings, sem processador —
render funciona sem CSS, é só sem estilo, e isso não é `D5.16b`, que só exige o texto no DOM);
qualquer painel `S2`/`charts` na rota (fronteira ESLint ainda fechada, `T-05.2+`); `make build`
(alvo separado, hoje recusa por outro motivo — parte da justificativa dele fica desatualizada por
esta task, mas o alvo em si não é tocado aqui).
