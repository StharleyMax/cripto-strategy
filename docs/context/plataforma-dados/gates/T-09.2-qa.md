# T-09.2 — QA Gate

**Task:** `[docs] 09 · env em {mainnet, testnet, demo, replay} em toda linha de ordem/fill, com
chip no chrome` (`CST-84`). **Componente:** `docs`. **Fase:** `09` — item 9.3, `D9.3`.

## O que foi verificado, e como

1. **Falsificador rodado independentemente**, fora do relato do builder — script cru:
   `python3 scripts/validate_order_fill_env.py` → `APROVADO — 4 caso(s) positivo(s) aceito(s), 5
   caso(s) negativo(s) rejeitado(s) (universo: 9 casos, 4 valores validos de 'env').` exit=0.

2. **Ataquei a guarda com duas mutações próprias**, escritas do zero (não copiei o texto do
   builder), aplicadas em cópia isolada em `/tmp/.../scratchpad/`, nunca no worktree:
   - **Mutação A** — removida a checagem `if "env" not in linha: raise ...`, deixando só
     `valor = linha["env"]`. Resultado: `KeyError: 'env'` não capturado, `exit 1` — a ausência
     da guarda quebra visivelmente (mesmo achado do builder, reproduzido, não copiado).
   - **Mutação B** — corpo inteiro de `validar_env_da_linha` substituído por
     `return linha.get("env")` (aceita qualquer linha, até sem `env`). Resultado:
     `REPROVADO — 5/9 caso(s) com veredito errado`, **os 5 negativos** pegando a regressão,
     incluindo `FALSO POSITIVO: linha invalida ACEITA (sem campo env (o caso que D9.3
     nomeia))`. exit=1.
   - Tentei um terceiro ângulo — encontrar uma mutação que preservasse os 9 vereditos atuais
     mas quebrasse o contrato geral (ex.: trocar o conjunto fechado por comparação
     case-insensitive, ou aceitar tipo não-string) — não encontrei nenhuma que sobrevivesse
     aos 9 casos existentes E violasse o contrato declarado (`env` obrigatório, conjunto
     fechado). O escopo de `D9.3` é só "rejeita linha sem `env`" — não exige normalização de
     caixa nem validação de tipo, então isso não é lacuna do falsificador, é fora do que o DoD
     pede.
   - **Veredito:** o falsificador falsifica de verdade — as duas mutações revelam regressão
     real, não decoram um resultado fixo.

3. **Classificação de fronteira, reconferida:**
   `harness code-paths classify scripts/validate_order_fill_env.py` →
   `nao-producao: nenhum include_prefixes casa ['backend/src/', 'backend/tests/',
   'frontend/src/']`. Mesmo padrão de `scripts/validate_palette.js`
   (`docs/adr/ADR-010-*.md:5`, também `scripts/`, também `nao-producao`, também comentários em
   português) — não é ineditismo, é precedente já aceito no repositório.

4. **Regras bloqueantes** — `harness rules list --severity block` devolve 7 regras. Rodei
   `harness rules --mode sweep --changed-only` no worktree (universo: `docs/INDEX.md` +
   `scripts/validate_order_fill_env.py`) → **0 achado, rc=0**. Das 7, 5 têm `scope: code`
   (`core.relative-import`, `core.silent-except`, `core.print-statement`,
   `web-fullstack.browser-imports-server`, `web-fullstack.tenant-from-request`) e `code` é
   definido por `[code_paths] include_prefixes` (`harness.toml:299`) — `scripts/` e `docs/` não
   casam, confirmado no passo 3, logo estão fora do universo dessas 5 por construção, não por
   escape. `core.hardcoded-secret` tem `scope: production` — mesmo argumento, e não há
   segredo no arquivo (grep manual: nenhuma string de credencial). `web-fullstack.server-test-
   directory-present` é `path-presence` sobre `backend/tests/**`, propriedade do repositório
   não afetada por este diff.

5. **`git status --porcelain`** no worktree: só `docs/INDEX.md` (modificado, 1 linha, append),
   `scripts/validate_order_fill_env.py` (novo) e `docs/context/plataforma-dados/gates/
   T-09.2-builder.md` (novo). Nenhum arquivo de produção tocado; `tasks.toml` intocado
   (confirmado por `git status`, sem entrada para `tasks.toml`).

6. **Escopo da task**: `docs/context/plataforma-dados/tasks.toml:1155-1162` — `T-09.2` referencia
   só `CA-F5-3`, `plano 09 item 9.3`, `DoD D9.3`; `depends_on = ["T-04.2"]`, que está `status =
   "done"` (`tasks.toml:536`). Não há outro item do plano `09` no escopo desta task.

7. **"Chip no chrome"** — parte do texto do item 9.3, mas não é o critério mensurável de `D9.3`
   (que só pede o teste negativo). Já registrado em `docs/product/STITCH_CONTEXT.md:138`
   (`"Chrome global, sempre visível: chip de env ∈ {mainnet, testnet, demo, replay}..."`) e
   `:1827` (`"Chrome global mostra sempre: ambiente (mainnet/testnet/demo/replay)..."`) —
   conferido por leitura direta, não por citação do builder. Fase `09` "Não faz: não escreve
   código de produção" — não há telas nesta SPEC para hospedar o chip; registro é o suficiente.

## Observação não-bloqueante (não reprova o gate)

Identificadores e docstring do script são em português (`validar_env_da_linha`,
`LinhaOrdemFillRejeitada`, `CASOS_POSITIVOS`, comentários). A tabela de fronteira de idioma do
`CLAUDE.md` é **doutrinariamente convenção, não portão** — "Nenhuma `[[rules.own]]` de idioma,
nenhum alvo de `make` de idioma" — e o mesmo padrão já existe, aceito, em
`scripts/validate_palette.js` (comentários em português, também `nao-producao`, também citado
por 6+ documentos sem ressalva). Não há regra bloqueante que isto viole; registro por
transparência, não como pendência.

## Cobertura

Alvo declarado (`D9.3`): **≥ 1 caso negativo**. Medido: **5 casos negativos** + 4 positivos,
universo 9, via `python3 scripts/validate_order_fill_env.py`. Acima do alvo.

## Comandos rodados nesta sessão de QA, literais

```bash
python3 scripts/validate_order_fill_env.py
  -> APROVADO — 4 caso(s) positivo(s), 5 caso(s) negativo(s). exit=0

# mutação A (guarda de ausência removida), cópia isolada em scratchpad, nunca no worktree
python3 <scratch>/mut1.py
  -> KeyError: 'env' (traceback, nao capturado). exit=1

# mutação B (guarda inteira -> return linha.get("env")), cópia isolada
python3 <scratch>/mut2.py
  -> REPROVADO — 5/9 caso(s) com veredito errado (os 5 negativos, incluindo o caso que D9.3
     nomeia). exit=1

harness code-paths classify scripts/validate_order_fill_env.py
  -> nao-producao: nenhum include_prefixes casa [...]

harness rules --mode sweep --changed-only
  -> 0 achado, rc=0 (universo: docs/INDEX.md, scripts/validate_order_fill_env.py)

git status --porcelain
  -> M docs/INDEX.md ; ?? scripts/validate_order_fill_env.py ; ?? gates/T-09.2-builder.md
```

## Veredito

```
## QA Gate — Fase 09 [docs]
- [OK] core.relative-import — fora de escopo `code` (scripts/ não casa include_prefixes, harness.toml:299)
- [OK] core.silent-except — fora de escopo `code`, mesmo argumento; e o try/except do script reporta a falha, não descarta
- [OK] core.print-statement — fora de escopo `code`, confirmado por `harness rules --mode sweep --changed-only` → 0 achado
- [OK] core.hardcoded-secret — fora de escopo `production`; sem credencial no arquivo
- [OK] web-fullstack.browser-imports-server — N/A, sem código de navegador
- [OK] web-fullstack.tenant-from-request — N/A, sem request
- [OK] web-fullstack.server-test-directory-present — path-presence sobre backend/tests/**, não afetado por este diff
- [OK] Testes existem e passam — `python3 scripts/validate_order_fill_env.py` (9 casos: 4 positivos + 5 negativos, exit 0)
- [OK] Falsificador comprovado — 2 mutações próprias (guarda removida → KeyError; guarda substituída por `.get` → 5/9 reprovações, todos os negativos), reproduzidas fora do worktree, resultado idêntico ao relato do builder
- [OK] Cobertura 5 casos negativos contra alvo declarado ≥ 1 (`D9.3`)
- [OK] DoD D9.3 — "teste que rejeita linha de ordem/fill sem env" — presente e comprovado
- [OK] Escopo da task — só D9.3 (tasks.toml:1155-1162), dependência T-04.2 done
- [OK] Nenhum código de produção alterado — git status confirma só docs/scripts/gates
Regras bloqueantes avaliadas: 7 de 7 listadas por `harness rules list --severity block`
Veredito: APPROVED
```
