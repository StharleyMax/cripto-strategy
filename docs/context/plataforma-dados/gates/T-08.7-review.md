# T-08.7 — Review Gate (arquitetural, read-only)

Decisão auditada: [`ADR-022`](../../../adr/ADR-022-min-obs-por-observacao-n-obs-por-ponto-e-dispersao-do-z.md).
QA auditado: [`gates/T-08.7-qa.md`](T-08.7-qa.md). Builder: [`gates/T-08.7-builder.md`](T-08.7-builder.md).

## Denominador

- Regras bloqueantes em vigor: `harness rules list --severity block` → **8** (`core.relative-import`,
  `core.silent-except`, `core.print-statement`, `core.hardcoded-secret`, `web-fullstack.browser-imports-server`,
  `web-fullstack.tenant-from-request`, `web-fullstack.server-test-directory-present`,
  `own.compose-hardcoded-secret`).
- Avaliadas via `harness rules --mode sweep --changed-only` → **rc=0**, 0 `[BLOQUEIO]`, só 3
  `[AVISO]` `core.module-docstring-single-line` (não-bloqueante) em `scan.py`, `compute_distribution.py`,
  `run_scan.py` — mesma regra que já disparava no baseline de `T-08.6` (não é regressão nova).
- Escopo varrido: `git diff 0d02596(merge-base T-08.6) --stat` → 7 arquivos (`backend/src/modules/charts/domain/scan.py`,
  `backend/src/modules/charts/use_cases/{compute_distribution,run_scan}.py`, 3 testes, `docs/INDEX.md`) +
  `observation.py`/`test_observation.py` novos + `ADR-022` novo. **Nota:** `git diff master` traz ~28
  arquivos estranhos (módulo `backtest` inteiro sendo removido) — isso é ruído de `master` ter avançado
  com `T-08.4` (PR #126, `bc19f80`) depois do merge-base desta branch (`0d02596`), não mudança de `T-08.7`;
  a auditoria usou o merge-base, não `master`, para isolar o diff real.

## Achados

1. **ADR-022 batido linha a linha com a implementação:**
   - `Observation(instrument_id, value, n_obs)` — `observation.py:35-63`, exatamente `D1`.
   - Filtro por observação roda ANTES de `_fires`, `MinObsNotMetError` vira segunda camada sobre o
     remanescente — `scan.py:250-268` (`D2`), teste antigo `test_percentile_spec_below_min_obs_refuses`
     preservado sem alteração (confirmado por leitura).
   - União discriminada `Fired|NotFired|Insufficient`, `Insufficient` sem campo de valor —
     `observation.py:66-103` (`D3`).
   - `z_dispersion` é campo IRMÃO de `ScanResult`, calculado só depois de `n_fired`/`per_observation`
     prontos (`scan.py:306`); nenhuma assinatura de `_fires`/`_robust_z`/`_compare`/`evaluate_scan` aceita
     `z_dispersion` (`D4`) — confirmado por leitura das assinaturas, `grep -n 'z_dispersion' scan.py`
     só aparece na chamada final e no campo do dataclass.
   - `histogram.py`, `threshold_spec.py`, `histogram_recipe.py`, `compute_firing_rate.py`: `git diff
     0d02596 --stat` → **0 linhas**, confirmando `D5`/`D6` (INTOCADOS).
   - Falsificador do próprio ADR reproduzido em `test_scan.py::test_adr_021_falsifier_mixed_n_obs_excludes_the_underobserved_symbol_not_the_aggregate`
     — `n_obs=2016` vs `n_obs=300`/`min_obs=576` → `n_total=1`, `n_excluded_min_obs=1`,
     `Insufficient(instrument_id="ALTUSDT", ...)`, sem `Fired`/`NotFired` para o excluído. Lido e
     confirmado nesta sessão, não apenas aceito do relato do QA.

2. **`make boundaries`** → `Analyzed 188 files, 828 dependencies. Contracts: 5 kept, 0 broken`, incluindo
   literalmente `Fronteira de contexto: charts so importa sentimento (nunca convergencia/backtest) KEPT`
   — ADR-003 preservada.

3. **Aditividade sobre `T-08.6`:** `pytest tests/charts --no-cov` isolado → **89 passed** nesta sessão
   (reprodução independente do número do QA). `bash backend/scripts/lint.sh` → limpo, 290 arquivos.
   Nenhum arquivo do motor `T-08.6` fora do caminho `scan` foi tocado.

4. Toda mensagem nova de `raise`/`Error(...)` introduzida por este diff está em inglês (`observation.py`,
   `scan.py`) — bate com a decisão de 2026-09-02 em `CLAUDE.md` (linha de fronteira estendida a mensagens
   de exceção).

Nenhuma violação de regra bloqueante nem de fronteira arquitetural declarada foi encontrada.

## Veredito

**COMPLIANT** — 0 de 8 regras bloqueantes violadas (`rc=0` em `harness rules --mode sweep --changed-only`);
`make boundaries` confirma a fronteira `ADR-003` (`charts` só importa `sentimento`); implementação bate
com `ADR-022` em todos os pontos (`D1`-`D6`) por leitura direta do código, não por aceitação do relato do
builder/QA; mudança é aditiva sobre `T-08.6` (histogram/threshold_spec/histogram_recipe/compute_firing_rate
intocados, testes antigos preservados).
