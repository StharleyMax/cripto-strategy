# Review Gate — T-05.5 (`/review`, read-only, independente de builder/QA) · **Veredito:** COMPLIANT

# T-05.5 — review gate (`/review`, read-only, independente de builder/QA)

**Escopo:** branch `tasks/T-05.5-price-source-use` (`origin`), commit único
`6131ce268fef07604e33775bd03de345ab286baa`, checkout detached em cima do merge-base real com
`origin/master` (`6ecb9aeef0c72e817d478cf5aa53db7dfd0bec28` — o branch estava atrás de
`master`, então `git diff <ponto errado>..HEAD` inicialmente trouxe ruído de `T-06.x`;
corrigido com `git merge-base origin/master HEAD`). Diff real: **9 arquivos**, todos sob
`frontend/`:

```
frontend/README.md                                          | 72 ++
frontend/package-lock.json                                  | 47 +-
frontend/src/charts/s2-annotation-price-binding.test.ts      | 55 ++
frontend/src/charts/s2-annotation-price-binding.ts           | 78 ++
frontend/src/charts/s2-axis-integration.test.ts              |  6 +-
frontend/src/charts/s2-panels.test.ts                        | 29 +-
frontend/src/charts/s2-panels.ts                             | 57 +-
frontend/src/charts/s2-price-source.test.ts                  | 41 ++
frontend/src/charts/s2-price-source.ts                       | 99 ++
9 files changed, 468 insertions(+), 16 deletions(-)
```
`backend/` intocado (confirmado: nenhum caminho `backend/` no diff).

## Camada 1 — o que o runner mede

`harness rules list --severity block` → **7 regras em vigor** (`core.relative-import`,
`core.silent-except`, `core.print-statement`, `core.hardcoded-secret`,
`web-fullstack.browser-imports-server`, `web-fullstack.tenant-from-request`,
`web-fullstack.server-test-directory-present`).

Avaliadas uma a uma (`harness rules --mode file --path <arquivo>`) nos **7 arquivos de
código** do diff (`.ts`/`.test.ts`; `README.md` e `package-lock.json` não são código por
`code_paths.include_globs`): **saída vazia, `rc=0`, nos 7 de 7** — zero achado.

Varredura completa do repositório (`harness rules --mode sweep --format ndjson`, universo =
toda árvore rastreada): **16 achados, todos `"severity":"warn"`, zero `"severity":"block"`**,
e nenhum toca arquivo deste diff (11 são docstring-single-line em `backend/src/modules/
sentimento`, 4 são `hardcoded-url` em `frontend/src/app/*.test.ts`, pré-existentes).

**Denominador: 7 regras bloqueantes existiam, 7 foram avaliadas (por arquivo + sweep
completo), 0 disparou.**

## Camada 2 — arquitetura declarada (`ADR-003`, `ADR-007`)

### `ADR-003` FR-1 (`charts` não faz I/O) — respeitado

Os 3 arquivos de produção tocados (`s2-price-source.ts`, `s2-annotation-price-binding.ts`,
`s2-panels.ts`) não chamam `readFileSync`/`fetch`/`localStorage`/`require`
(`grep -n "readFileSync\|fetch(\|localStorage\|node:fs\|require(" <os 3 arquivos>` → 0
ocorrências de I/O real; a única linha que casa é um COMENTÁRIO em `s2-panels.ts:8`
explicando que uma versão anterior chamava `node:fs` e não chama mais). `buildPricePanel`
recebe `candles: readonly RawCandle[]` já parseado — a leitura de disco continua vivendo só
em `.test.ts` (o refactor que `T-05.2-review.md` "rodada 2" já tinha fechado como WARNING
resolvido, e este diff não o reabre).

### `ADR-003` FR-2/D5.12 (fronteira `charts`↔`web` executável) — não tocada, não regredida

`grep -n "^import" nos 3 arquivos` → todo import é relativo dentro de `./` (mesmo diretório
`frontend/src/charts/`); nenhum import de `frontend/src/app`/`frontend/src/features`.
`frontend/eslint.config.mjs` e `frontend/src/charts/eslint-boundary.test.ts` (o contrato
`no-restricted-imports`/`no-restricted-syntax` que fecha `D5.12`) **não aparecem no diff** —
a fronteira executável permanece exatamente como `T-05.1`/`ADR-003` addendum a deixou.

### `ADR-007`/PS-1/PS-3 — transcrição confere com a fonte, byte a byte na tabela

`ADR-007-price-source-por-uso.md:31-45`: `PS-1` (pedir preço sem `price_use` é erro, nunca
default silencioso) e `PS-3` (toda `<Anotacao>` carrega `price_source` E `price_use`, teste
negativo nomeado literalmente `"marcada sobre outra série de preço"`).
`backend/src/modules/sentimento/domain/price_source_catalog.py:101-106` fixa a tabela:
`structure_detection→klines_last, liquidation_trigger→mark_price, funding→mark_price,
execution→klines_last, cost→mark_price`. `frontend/src/charts/s2-price-source.ts`'s
`PRICE_SOURCE_BY_USE` transcreve as **5 de 5** entradas idênticas (verificado via `grep` nos
dois arquivos). `resolvePriceSource(priceUse: PriceUse | null)` recusa `null`
(`MissingPriceUseError`) e valor fora do conjunto fechado (`InvalidPriceUseError`) — `PS-1`
como mecanismo (parâmetro obrigatório, não opcional), não convenção documental.
`describeAnnotationOnReopen` implementa o teste negativo de `D5.5`/PS-3 com o label EXATO
`"marcada sobre outra série de preço"` (`PRICE_SERIES_MISMATCH_LABEL`).

Plano `05_fatia_visivel.md:18,33` (item `5.7`, `D5.5`) confere literalmente com o que foi
implementado.

**Nenhum achado de camada 2** — sem violação de `ADR-003` nem de `ADR-007` neste diff.

## Reprodução independente dos comandos (não confiei no relato do builder — `frontend/README.md §13`)

```
npm --prefix frontend install
  → 128 pacotes, 0 vulnerabilidades          [REPRODUZIDO nesta sessão]

npm --prefix frontend run lint
  → eslint src, rc=0, 0 erro/aviso            [REPRODUZIDO nesta sessão]
  universo: toda a árvore frontend/src (charts+app+features)

npm --prefix frontend run test:charts
  → rc=1, ℹ pass 58 / ℹ fail 12                [REPRODUZIDO nesta sessão]
```

**Os 12 fail são `ENOENT` em `data/binance/{klines,metrics}/...csv`** — dado bruto gitignored
(`CLAUDE.md` §"Dado bruto não é versionado", `.gitignore:51 data/`), ausente por padrão neste
worktree novo. **Não é regressão deste diff:**

- `git log --oneline -1 <merge-base> -- <arquivo>` confirma que **4 dos 5 arquivos que falham**
  (`canonical-grid-sha256-proof.test.ts`, `s2-axis-integration.test.ts`,
  `s2-klines-loader.test.ts`, `s2-oi-loader.test.ts`) já existiam em `T-05.1`/`T-05.2`, antes
  deste diff, e já dependiam da mesma fixture ausente.
- A ÚNICA asserção nova deste diff dentro de um arquivo que falhou
  (`s2-panels.test.ts:48`, `"T-05.5/5.7: the price panel declares price_source AND price_use
  on the panel row"`) falha no `readFileSync` do `setup` (linha 49, carregando os candles),
  **antes** de chegar às duas linhas de asserção novas — não há evidência de falha lógica.
- Os **2 arquivos de teste 100% novos deste diff** (`s2-price-source.test.ts`,
  `s2-annotation-price-binding.test.ts`, ambos sem dependência de `data/`) foram rodados
  **isoladamente**: `node --test src/charts/s2-price-source.test.ts
  src/charts/s2-annotation-price-binding.test.ts` → **8 pass, 0 fail**
  `[REPRODUZIDO nesta sessão]`.

**Nota honesta, não escondida:** não consegui reproduzir de forma independente o "78 pass, 0
fail" que `frontend/README.md §13` relata, porque o builder rodou com `data/` copiado
manualmente de outro checkout ("copiado do checkout principal só para rodar esta suíte, nunca
versionado") e `data/MANIFEST.md` — o catálogo que `CLAUDE.md` promete para reobter o dado —
**não existe no repositório** (`git show HEAD:data/MANIFEST.md` → `fatal: does not exist`,
confirmado também em `origin/master`). Isto é uma lacuna pré-existente da infraestrutura de
dado do componente `charts` (já presente em `T-05.1`/`T-05.2`), não algo introduzido por
`T-05.5` — não tem regra bloqueante que a cubra e não entra no veredito, mas fica registrado
para não virar dívida invisível: **hoje nenhum revisor consegue confirmar 100% da suíte
`test:charts` sem depender de um repasse manual de dado fora do git.**

## Veredito

```
Regras bloqueantes avaliadas: 7 de 7 (harness rules list --severity block)
Arquivos varridos: 7 arquivos de código do diff via `harness rules --mode file` (rc=0 em
  todos) + `harness rules --mode sweep --format ndjson` sobre a árvore inteira (16 achados,
  0 block) + diff integral dos 9 arquivos tocados + grep de I/O e de import nos 3 arquivos de
  produção + comparação byte-a-byte da tabela PS-1 entre s2-price-source.ts e
  price_source_catalog.py
Comandos reproduzidos nesta sessão: npm --prefix frontend install (128 pkg, 0 vuln),
  npm --prefix frontend run lint (rc=0), npm --prefix frontend run test:charts (58 pass/12
  fail, os 12 rastreados a ENOENT de dado gitignored pré-existente, não a este diff),
  node --test dos 2 arquivos 100% novos isoladamente (8 pass, 0 fail)
Veredito: COMPLIANT
Achados: 0 bloqueantes, 0 violação de ADR-003 (FR-1, FR-2, D5.12 intactos) nem de ADR-007
  (PS-1/PS-3 transcritos corretamente). 1 nota INFO fora do veredito: ausência de
  data/MANIFEST.md torna a suíte test:charts não 100% reprodutível por um revisor sem acesso
  ao dado bruto repassado manualmente — pré-existente ao componente, não introduzida por
  T-05.5, sem regra bloqueante que a cubra.
```

Arquivos citados (caminhos absolutos):
- `/home/stharley/Documentos/projects/cripto-strategy/.claude/worktrees/wf_2a5f5015-5f1-21/frontend/src/charts/s2-price-source.ts`
- `/home/stharley/Documentos/projects/cripto-strategy/.claude/worktrees/wf_2a5f5015-5f1-21/frontend/src/charts/s2-annotation-price-binding.ts`
- `/home/stharley/Documentos/projects/cripto-strategy/.claude/worktrees/wf_2a5f5015-5f1-21/frontend/src/charts/s2-panels.ts`
- `/home/stharley/Documentos/projects/cripto-strategy/.claude/worktrees/wf_2a5f5015-5f1-21/docs/adr/ADR-003-fronteira-charts-web.md`
- `/home/stharley/Documentos/projects/cripto-strategy/.claude/worktrees/wf_2a5f5015-5f1-21/docs/adr/ADR-007-price-source-por-uso.md`
- `/home/stharley/Documentos/projects/cripto-strategy/.claude/worktrees/wf_2a5f5015-5f1-21/backend/src/modules/sentimento/domain/price_source_catalog.py`

Não avancei estado nem registrei `gate-record` (restrição read-only desta função).

