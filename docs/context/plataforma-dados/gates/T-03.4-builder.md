# T-03.4 · `/build` — relatório do portão

**Card:** `CST-20` · **Componente:** `sentimento` · **Fase:** `03` · **Base:** `master@634833b`
**Branch:** `tasks/T-03.4-agregado-q-nq` · **Commit:** `8a0fb40` · **PR:** https://github.com/StharleyMax/cripto-strategy/pull/59

## Escopo

`ADR-001`/6, `SPEC-001` §1.4 (`CL-5`), plano `03` item 3.5, DoD `D3.5`+`D3.7`+`D3.8`. Declarado
pelo handoff: **código + prova de mecanismo sobre fixture/dados reais já em `data/`** — não
deploy contínuo (sem consumidor ao vivo ainda, decisão do owner). Onde o regime real (≥ 7 dias
rodando) não é medível hoje, isso está nomeado, não escondido.

## Arquivos alterados

| arquivo | status | papel |
|---|---|---|
| `backend/src/modules/sentimento/domain/aggtrade_bucket_aggregate.py` | novo | `AggTradeBucketTrade`, `aggregate_by_bucket` (fold `ADR-001`/6), `require_contiguous` (delega em `aggtrade_contiguity.py`), `detect_bucket_agg_id_gaps` |
| `backend/src/modules/sentimento/domain/qnq_divergence.py` | novo | `QnqTrade`, `measure_qnq_divergence` — `QF-6` |
| `backend/src/modules/sentimento/infra/aggtrade_csv_reader.py` | modificado | `+read_aggtrade_bucket_trades` (dump replay, `raw_nq=None` sempre) |
| `backend/src/modules/sentimento/infra/aggtrade_rest_snapshot_reader.py` | novo | lê `data/binance/rest/nq_*.json` em `QnqTrade` |
| `backend/tests/sentimento/test_aggtrade_bucket_aggregate.py` | novo | 15 testes sintéticos |
| `backend/tests/sentimento/test_aggtrade_bucket_aggregate_fixtures.py` | novo | 5 testes sobre dado real (`BTCUSDT-aggTrades-2026-08-21.csv`) |
| `backend/tests/sentimento/test_qnq_divergence.py` | novo | 12 testes; 5 sobre a evidência real de `ADR-001` |
| `backend/tests/sentimento/test_nq_bucket_capture_boundary.py` | novo | 3 testes: liga o agregado novo à borda `SEM_FONTE` de `T-04.4` |
| `backend/README.md` | modificado | seção `### 📎 2026-09-01 por T-03.4` (doc delta) |

## DoD

- [x] **D3.5** — detector de buraco é contiguidade, nunca taxa. `require_contiguous` DELEGA em
  `aggtrade_contiguity.require_unique_agg_ids`/`detect_agg_id_gaps` (`T-04.3`, já em `master`) —
  não duplica a lógica de unicidade. Comando: `bash backend/scripts/test.sh -k
  test_aggtrade_bucket_aggregate_fixtures` → falsificador "deletar 1 linha do MEIO ⇒ reprova"
  sobre os primeiros 20.000 trades reais de `BTCUSDT-aggTrades-2026-08-21.csv` (md5
  `31f5b006714d6cbc41f8a0b4e10a7aae`). A escala de milhões de linhas (8.873.078, 0 saltos) não
  foi reprocessada — já provada por `T-04.3`, reutilizada por delegação, não repetida (custaria
  ~68 s/arquivo só para ler com `Decimal`, `[MEDIDO 2026-09-01]`, por zero informação nova).
- [x] **D3.7** — `count(q≠nq)/n` e déficit em bp, por símbolo e por dia. Comando:
  `bash backend/scripts/test.sh -k test_qnq_divergence` → reproduz BYTE A BYTE a evidência real
  de `ADR-001` (`data/binance/rest/nq_{BTC,ETH,SOL,XRP,DOGE}USDT.json`): DOGEUSDT **16/1000,
  80,56 bp**; BTC/ETH/SOL/XRP **0/1000, 0,00 bp** `[MEDIDO 2026-09-01]`. **NÃO MEDIDO, nomeado:**
  ≥ 7 dias em regime real — a evidência disponível é uma janela de 251 s por símbolo, que nunca
  cruza meia-noite UTC (`test_d3_7_the_snapshot_never_crosses_a_utc_day...`). O agrupamento
  `(symbol, day)` está provado sobre timestamps SINTÉTICOS por essa razão, declarada no teste e
  no doc delta.
- [x] **D3.8** — leitura sob `quantity_field=nq` de janela anterior à 1ª captura ⇒ `SEM_FONTE`,
  nunca valor. Comando: `bash backend/scripts/test.sh -k test_nq_bucket_capture_boundary` → um
  bucket dobrado de trades "tipo dump" (`raw_nq=None` sempre) não produz observação NENHUMA sob
  `quantity_field=nq` (o weld fica impossível de CONSTRUIR); o mesmo agregado "tipo captura ao
  vivo" atravessa a borda de `first_capture_at` normalmente. Mecanismo em si (`as_of` +
  `first_capture_at` + `Absence.NO_SOURCE`) é de `T-04.4`, já exaustivamente provado por
  `test_as_of_accessor.py` — este arquivo LIGA o agregado novo a ele, não o reimplementa.

## Comandos rodados (literais) e resultado

- `bash backend/scripts/test.sh` → **652 passed** (era 617 em `master@634833b`, `[MEDIDO via git
  stash antes/depois]`), `rc=0`. Cobertura: `domain 100,0% (1250/1250)` · `use_cases 100,0%
  (220/220)` · `infra 97,8% (872/892)`, total 99,09%. Universo: suíte inteira do componente
  `sentimento`, incluindo os 35 testes novos desta task.
- `bash backend/scripts/boundaries.sh` → **3 kept, 0 broken** (os 3 contratos declarados).
- `bash backend/scripts/natureza.sh` → universo de **29** arquivos em `domain`/`use_cases`, **0**
  leituras de relógio.
- `harness rules --mode sweep --changed-only` → `rc=0`, **0 achados**, sobre os 8
  arquivos de produção+teste desta task (as 7 regras de `harness rules list --severity block`).
- `bash backend/scripts/lint.sh` (`ruff check` + `ruff format --check` + `mypy --strict`) →
  limpo, universo: `src` + `tests` inteiros (100 arquivos).
- `git push` (via `HARNESS_MECHANISM`, `pre-push.pre-harness`) → `boundaries`+`natureza`+`lint`
  (backend e frontend) rodaram de novo no hook, todos `rc=0`.

## Cobertura

`domain 100,0%` (alvo 90%) · `use_cases 100,0%` (alvo 80%) · `infra 97,8%` (alvo 70%) — as três
camadas acima do piso declarado (`ADR-009/D1`).

## Falsificador — o caso que a proteção REJEITA

**3 mutações manuais aplicadas e revertidas, as 3 morrem** `[MEDIDO 2026-09-01, `diff` byte-
idêntico ao original depois de cada reversão]`:

1. inverter `is_buyer_maker` no fold de `_fold_bucket` (troca `sum_q_buy`/`sum_q_sell`/`btx` de
   lado) → `test_a_single_bucket_folds_the_eight_terms_of_adr_001_6` reprova (`Decimal('8') ==
   20` falha).
2. inverter o sinal de `deficit_bp` em `qnq_divergence.py` (`sum_nq - sum_q` em vez de `sum_q -
   sum_nq`) → **2** testes reprovam, incluindo o que reproduz a evidência REAL de DOGEUSDT
   (`-1000.00 == 1000.00` falha).
3. afrouxar `PartialNqBucketError` para tolerar `tx - 1` → `test_a_partial_bucket_refuses_
   instead_of_guessing_a_semantics` reprova (`DID NOT RAISE`).

## Anomalia encontrada e corrigida durante a rodada

Um comentário em `aggtrade_bucket_aggregate.py` citava o nome do arquivo `as_of_accessor.py` em
prosa (não um `import`), e isso bastou para `test_as_of_is_the_single_reader.py::
test_no_production_module_imports_this_accessor_yet_and_that_is_recorded_not_claimed` reprovar —
aquele teste varre `backend/src` por ocorrência TEXTUAL da string, deliberadamente mais largo que
um import, para forçar qualquer menção a ser revista antes de virar consumidor real. Corrigido
reformulando o comentário sem citar o nome do arquivo; nenhuma linha de comportamento mudou.
`[MEDIDO: 1 failed → 0 failed, mesmo diff de produção salvo o comentário]`.

## Doc delta

- `backend/README.md`: seção `### 📎 2026-09-01 por T-03.4` **acrescentada** — descreve os
  módulos novos, os números reproduzidos, o que ficou `[NÃO MEDIDO]` e por quê, e as 3 mutações.
- `docs/plans/SPEC-001-plataforma-dados/03_captura_continua.md`: **sem mudança** — o item 3.5 e
  as linhas `D3.5`/`D3.7`/`D3.8` já citam exatamente os números que este build reproduz; nenhuma
  correção era necessária.
- `docs/context/plataforma-dados/tasks.toml` (bloco `T-03.4`) e `tasks_review.md:268`: **sem
  mudança** — já existiam, escritos pelo `/tech-lead`, e já referenciam `ADR-001`/6, `SPEC-001`
  §1.4, `D3.5`/`D3.7`/`D3.8` corretamente. Alterar status/ledger não é ato de builder.
- `docs/medicao-ws-aggtrade-nq.md`: **sem mudança** — esta task não reabriu rede (suíte é ZERO
  REDE) e não tinha mandato para remedir `D3.9`; o documento permanece a fonte correta de por que
  `D3.9` segue `[NÃO MEDIDO]`.
- `docs/INDEX.md`: **não acrescentado por este relatório** — cabe ao loop principal/QA decidir o
  ponteiro final (caminho do gate + veredito), para não duplicar linha se o `/qa` pedir ajuste.
- ADR: não necessário — nenhuma decisão nova de arquitetura; este build implementa o que
  `ADR-001`/6 já decidiu.

## Bloqueado

Nada bloqueado dentro do escopo desta fase. Nomeado, não bloqueante:

- `D3.7`/`D3.9` continuam com a metade "regime real" `[NÃO MEDIDO]` — depende de dias rodando em
  produção, fora do escopo declarado desta rodada (owner: sem consumidor ao vivo ainda).
- `D3.9` (o WS carrega `nq`?) precisa de uma 2ª tentativa independente de rede antes da regra de
  parada de `docs/medicao-ws-aggtrade-nq.md` poder tratá-lo como medido — dono: quem tiver
  mandato de rede, não esta task.
