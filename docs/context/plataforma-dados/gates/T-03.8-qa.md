# `T-03.8` (`CST-24`) — relatório de gate do `/qa`

**Feature:** `plataforma-dados` · **Componente:** `sentimento` · **Fase:** `03`
**Base:** `master@634833b` · **Branch:** `tasks/T-03.8-ntp-skew` · **PR:** #57
**Worktree:** `/tmp/claude-1002/wt/T-03.8`

## Veredito: APPROVED

## 1. Testes existentes rodaram antes de qualquer teste novo

`bash backend/scripts/test.sh` (comando literal de `harness policy --key test_cmd`), foreground,
sem sleep passivo — dois reruns concorrentes de outros builders foram detectados via `ps aux` e
um duplicado meu foi morto para reduzir contenção de CPU:

```
649 passed in 621.65s (0:10:21)
Required test coverage of 70.0% reached. Total coverage: 99.13%
[OK] domain     100.0% (meta 90%)  [1133/1133]
[OK] use_cases  100.0% (meta 80%)  [253/253]
[OK] infra       97.6% (meta 70%)  [922/945]
```

`[MEDIDO 2026-09-01, universo: 649 testes]`. Contagem de testes NOVOS confirmada por
`grep -c "^def test_" tests/sentimento/test_{binance_server_time_probe,clock_skew,measure_clock_skew,ntp_skew_probe_cli,persist_ntp_skew_run,system_wall_clock}.py`
→ `8+10+3+5+4+2 = 32`, batendo com o "+32" do relatório do builder (617→649).

## 2. Mentalidade destrutiva — dois mutantes aplicados e mortos nesta sessão

**Mutante 1** (`domain/clock_skew.py::skew_ms`): substituí o retorno pela constante `0`
(descarta a medição em silêncio). `pytest tests/sentimento/test_clock_skew.py
tests/sentimento/test_persist_ntp_skew_run.py` → **4 falhas** (`test_positive_skew_means...`,
`test_negative_skew_means...`, `test_midpoint_floors_on_an_odd_bracket_sum`,
`test_the_built_run_carries_the_measured_skew_and_a_known_verdict`). Mutante revertido e
`diff` confirmado vazio.

**Mutante 2** (`use_cases/persist_ntp_skew_run.py`): troquei o `raise MissingUsedWeightError`
por um fallback `weight_used_value = observation.weight_used if ... else 0` (fabrica peso em
vez de recusar). `pytest tests/sentimento/test_persist_ntp_skew_run.py` → **2 falhas**
(`test_a_missing_weight_header_refuses...`, `test_a_missing_weight_never_reaches_the_recorder`).
Mutante revertido e `diff` confirmado vazio.

Os dois falsificadores do relatório do builder (bracket invertido; `weight_used=None`) foram
reexecutados literalmente nesta sessão e reproduzem: `REJEITOU: local_time_after_ms (999)
precedes...` e `REJEITOU: /fapi/v1/time answered without a usable used-weight header...`.

`test_run_persists_the_measured_skew_into_a_real_sqlite_store` (em `test_ntp_skew_probe_cli.py`)
fecha exatamente a lacuna que o `IngestRunRecorder`-fake não fechava: reabre um
`SqliteIngestRecordStore` real a partir do disco e lê `clock_skew_ms=300` de volta — prova que o
skew é persistido em produção, não descartado, e não é usado para decisão binária (nenhuma
branch de aceite/rejeite depende do valor de `clock_skew_ms`, só de `weight_used`).

## 3. Ausência de limiar de tolerância — verificado, não apenas citado

```bash
git diff 634833b..HEAD -- backend/src backend/tests | grep -iE '^\+.*(MAX_SKEW|threshold|tolerance|aceitavel|acceptable)'
# +        never judged: whether a given magnitude is acceptable is `T-07.10`'s decision, made
```

Única ocorrência: um comentário de docstring (`domain/clock_skew.py:70`) que EXPLICITAMENTE
adia o julgamento para `T-07.10`. Nenhum `MAX_SKEW_MS`, nenhum `if skew_ms > N`, nenhuma branch
`ACCEPTED`/`REJECTED` condicionada ao valor de `clock_skew_ms` — o único `if` de guarda em
`persist_ntp_skew_run.py` é sobre `weight_used is None`, não sobre a magnitude do skew.
`_VERDICT = "ACCEPTED"` é constante, sempre a mesma, independente do skew medido.

## 4. Regras bloqueantes

`harness rules list --severity block` → 7 regras. As 3 `web-fullstack.*` não se aplicam (zero
arquivo `frontend/` tocado, PR é 100% `backend/src` + `backend/tests` + docs).

- [OK] `core.relative-import` — `grep -rnE "^from \.|^\s*from \.\."` nos 6 arquivos novos → 0
- [OK] `core.silent-except` — `grep -rnE "except[^:]*:\s*(pass|\.\.\.)\s*$"` → 0
- [OK] `core.print-statement` — `grep -rnE "^\s*print\("` → 0
- [OK] `core.hardcoded-secret` — builder já mediu 0 ocorrências de `API_KEY`/`api_key`; endpoint
      é público
- [n/a] `web-fullstack.*` (3 regras) — zero arquivo `frontend/`
- [OK] `bash .harness/mechanism rules --mode sweep --surface git-hook` (após `git add -A`) →
      `rc=0`, saída vazia — 0 achados
- [OK] `bash .harness/mechanism validate --strict` → `"politica valida: cripto-strategy
      (schema_version=1)"`, `rc=0`

Regras bloqueantes avaliadas: 7 de 7 listadas.

## 5. Portões do componente

- [OK] `bash backend/scripts/lint.sh` → `All checks passed! · 105 files already formatted ·
      Success: no issues found in 105 source files`
- [OK] `bash backend/scripts/boundaries.sh` → `Contracts: 3 kept, 0 broken`
- [OK] `bash backend/scripts/natureza.sh` → `universo de 30 arquivo(s), 0 leitura(s) de relogio`
      (domain 21 + use_cases 9, os dois novos `use_cases/*.py` inclusos)

## 6. DoD — item a item

- [x] `CA-F0-8`/`[GAP G6]`/plano `03` item 3.7 — NTP monitorado contra `/fapi/v1/time`,
      confirmado real por 5 corridas em `docs/context/plataforma-dados/medicoes/T-03.8-ntp-skew/`
- [x] "skew persistido por `ingest_run`" — confirmado por teste que reabre `sqlite` real (§2) E
      pelas 5 linhas reais em `01_ingest_health_query.jsonl` (`n_runs: 5`)
- [x] **D3.10** — "leitura trivial depois": `ingest_health_query` (pré-existente, `T-01.1`) lê as
      5 linhas sem script novo, confirmado no jsonl. Distribuição `>= 7 dias`: `[NÃO MEDIDO]`
      corretamente rotulado — não é exigível antes de deploy contínuo (`Q1`/`Q15` seguem
      pendentes para produção contínua)
- [x] **"a tolerância NÃO se calibra aqui"** — verificado por `git diff` completo (§3): zero
      `MAX_SKEW_MS`, zero branch de julgamento sobre a magnitude
- [x] "Zero API key" — confirmado, endpoint público, 0 ocorrência de credencial
- [x] `tasks.toml`/`tasks_review.md`/plano `03` — doc delta zero, confirmado por leitura direta
      das 3 fontes citadas no despacho; já descreviam o DoD corretamente antes desta task

## Anomalias

Nenhuma. Dois reruns concorrentes de `pytest` de outros builders (`T-03.2`, `T-03.5`, `T-02.1`,
`T-02.2`, `T-03.4`) competiam por CPU durante a medição — não afetou o resultado (`rc=0`,
649 passed), só o tempo de parede (10m21s vs os ~7,4min que o builder mediu isolado).
