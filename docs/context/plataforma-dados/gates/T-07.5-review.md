# Review arquitetural — T-07.5 · Escritor único; backfill MODELADO nunca sobrescreve OBSERVADA

**Card:** `CST-59` · **Componente:** `sentimento` · **Fase:** 07, item 7.7 · **DoD:** `D7.16`
**Branch:** `tasks/T-07.5-fase07` · **Worktree:** `/tmp/claude-1002/wt/T-07.5` · **PR:** #89

## Veredito

**COMPLIANT**, com 2 achados WARNING de convenção (não-bloqueantes) a corrigir antes do merge.

## Denominador

- `harness rules list --severity block` → **7 regras bloqueantes em vigor**; avaliadas **7 de 7**
  sobre os arquivos de produção/teste novos ou alterados desta task (4 `core.*` aplicáveis a
  código Python; 3 `web-fullstack.*` fora de escopo — task é 100% backend, N/A, não disparam).
- Arquivos varridos, por comando: `git diff origin/master...HEAD --stat` → 14 arquivos (5 novos
  de produção/teste + 4 alterados + 3 doc/ledger); leitura direta de
  `domain/provenance.py`, `use_cases/write_series_row.py`, `use_cases/run_single_writer.py`,
  `infra/redis_series_write_queue.py`, `tests/sentimento/test_single_writer_call_sites.py`,
  `tests/sentimento/test_write_series_row.py`, `tests/sentimento/test_provenance_columns.py`.
- `bash backend/scripts/lint.sh`-equivalente rodado por mim, direcionado aos 5 arquivos principais:
  `ruff check` → "All checks passed!"; `mypy --strict` → "Success: no issues found in 5 source
  files".
- `./.venv/bin/lint-imports` (import-linter, `[tool.importlinter]`) → **3 contracts kept, 0
  broken** (149 arquivos, 680 dependências): `Camadas por contexto: infra > use_cases > domain`,
  `Fronteira de contexto: sentimento nao importa outro contexto`, `Natureza: domain e use_cases
  nao falam com socket nem ssl` — todos **KEPT**.
- `pytest -q` direcionado às 5 suítes de `sentimento` tocadas por esta task → **todos verdes**
  (54 testes, saída `......` 100%).

## O que a arquitetura declarada exige, e a evidência de que vale

### 1. Contrato `Natureza` (`ADR-016`) — domain puro, I/O em infra

`domain/provenance.py` só importa `dataclasses`, `enum`, `typing` — nenhum `socket`/`ssl`, nenhuma
leitura de relógio. `modeled_write_overwrites_observed(provenance, *, observed_already_present)`
recebe o resultado da leitura já pronto como `bool` — a leitura em si (`ObservedLookup.
observed_already_present`) é um `Protocol` consumido só em `use_cases/write_series_row.py`, e a
implementação concreta (Redis/store) mora em `infra/`. `import-linter` confirma mecanicamente:
contrato `Natureza` **KEPT**. **Sem violação.**

### 2. Escritor único — guarda estrutural AST cobre o `SOURCE_ROOT` inteiro

`test_single_writer_call_sites.py:22`: `SOURCE_ROOT = Path(__file__).resolve().parents[2] / "src"`
— de `backend/tests/sentimento/test_single_writer_call_sites.py`, `parents[2]` resolve para
`backend/src`, a raiz completa de produção Python (confirmado por
`test_the_source_tree_the_gate_scans_is_not_empty`, piso `>= 10` módulos; universo real medido em
outras tasks é 116+). O scanner varre `SOURCE_ROOT.rglob("*.py")` sem exclusão nenhuma — não há
subconjunto que deixaria um segundo call-site fora do radar. `test_exactly_one_production_module_
calls_write_series_row` é o gate real: `len(calls) == 1` e o único call site é
`run_single_writer.py`. Blind spot nomeado explicitamente no próprio arquivo (chamada via
`getattr` com nome computado em runtime) — mesmo limite já documentado para o próprio
`[tool.importlinter]`, não escondido. **Sem violação.**

### 3. `ADR-002/D5` — a garantia generalizada aos cinco candidatos, sink como `Protocol`

`ADR-002-motor-de-armazenamento.md` §D5, lido: "todos os caminhos de escrita convergem para um
processo escritor; os coletores 24/7 produzem para fila durável; o escritor único é o único que
toca a série. As duas invariantes vivem nele" — e §D5 parágrafo 2 explicita que a exigência de
ler-antes-de-escrever "vale nos cinco candidatos... porque a decisão de recusar sob divergência
de `knowledge_time` é da aplicação em qualquer motor". `write_series_row.py` define `SeriesSink`
e `ObservedLookup` como `Protocol` abstratos (`use_cases/write_series_row.py:21-36`), consumidos
por `run_single_writer.py` sem nomear Redis, TimescaleDB, ClickHouse, Parquet/DuckDB ou R2 —
qualquer um dos cinco candidatos de `ADR-002` pode implementá-los depois sem tocar
`write_series_row`. Builder e QA relataram isso mesmo; a leitura direta do código confirma.
**Sem violação.**

### 4. Idioma — 2 resíduos de português fora de citação literal (WARNING)

QA ciclo 2 corrigiu dois resíduos em produção (`write_series_row.py`/`run_single_writer.py`:
citação de arquivo inexistente + "escritor único" solto no docstring de produção). A varredura
desta review (busca por acento e por palavra comum PT, nos 5 arquivos de teste novos/alterados)
achou **2 resíduos remanescentes, mesma classe de defeito, no arquivo de TESTE que ficou fora do
escopo daquela correção**:

- `backend/tests/sentimento/test_write_series_row.py:1` — `"""`write_series_row` is the
  escritor único: ...` — "escritor único" em português, sem aspas de citação literal, dentro de
  docstring de módulo que é, do resto, em inglês.
- `backend/tests/sentimento/test_write_series_row.py:93` — `"""Observado sempre vence — a second
  OBSERVED...` — mesma classe: início de docstring de função em português, sem aspas, misturado a
  inglês na mesma frase.

Comparar com o padrão correto já usado no mesmo arquivo/arquivos-irmãos: toda citação literal de
`D7.16`/`ADR-002`/`SPEC-001` está **entre aspas** (ex.: `test_provenance_columns.py:331`,
`domain/provenance.py:248,262`, `run_single_writer.py:3-4`) — a doutrina deste repositório trata
citação-entre-aspas como exceção aceita, e prosa solta em português como resíduo. Estes dois não
são citação: são a mesma frase de resumo que já foi corrigida no lado de produção, esquecida no
lado de teste.

**Não é regra bloqueante** (`ADR-011/D1.10`: idioma é convenção declarada, não portão automático —
nenhuma `[[rules.own]]` de idioma existe nem pode existir por doutrina deste repositório) — por
isso **não muda o veredito para NON_COMPLIANT**, mas é achado real, citável e com correção
concreta:

**Correção concreta:** trocar `"""`write_series_row` is the escritor único: ` por `"""
`write_series_row` is the single writer: ` (linha 1) e `"""Observado sempre vence — a second` por
`"""OBSERVED always wins — a second` (linha 93), mantendo o resto de cada docstring intocado.

## Nota de contexto — não é achado de arquitetura, mas afeta a leitura do estado

`harness pipeline state T-07.5` → `INIT`; `harness status` mostra, para a feature
`plataforma-dados`: `⚠ OVERRIDE SCOPED — ... T-07.5 QA APPROVED ciclo 2 bloqueado pela mesma
anomalia` e `⚠ fase(s) com QA APPROVED e tarefa(s) em aberto no tasks.toml — o doc contradiz o
veredito: 07 (T-07.1 ... T-07.5 ...)`. No worktree, `docs/context/plataforma-dados/gates/
T-07.5-qa.md` tem uma edição **não commitada** (`git status` mostra `modified`) que é o texto do
ciclo 2 (`NEEDS_FIX` → aprovação); o `HEAD` commitado do branch só carrega o ciclo 1
(`NEEDS_FIX`, commit `39605f5`). Ou seja: a aprovação de QA ciclo 2 que o handoff desta review cita
como fato **ainda não está no ledger nem no git commitado** — está só no disco do worktree. Isto
não reprova a arquitetura (que foi auditada diretamente no código, independente do texto do QA),
mas, por doutrina deste repositório ("documento aprovado sem o evento `approve` no ledger não está
aprovado"), **o estado "QA aprovado ciclo 2" não deveria ser tratado como fato até o commit e o
`gate-record` correspondente existirem.**

## Regras bloqueantes — avaliação item a item

- `core.relative-import` — nenhum import relativo nos 5 arquivos (todos `from src.modules...`).
  **OK.**
- `core.silent-except` — nenhum `except` nos 5 arquivos. **OK** (não se aplica, não há bloco
  `try`).
- `core.print-statement` — nenhum `print(`; `logger.info(...)` nomeado por módulo em
  `write_series_row.py:18`. **OK.**
- `core.hardcoded-secret` — nenhuma credencial/connection string. **OK.**
- `web-fullstack.browser-imports-server`, `web-fullstack.tenant-from-request`,
  `web-fullstack.server-test-directory-present` — fora de escopo, task 100% backend `sentimento`.
  **N/A.**

Regras bloqueantes avaliadas: **7 de 7** listadas por `harness rules list --severity block`.

## Testes e cobertura (evidência independente, não apenas o relato do QA)

`pytest -q` direcionado às 5 suítes tocadas → 54 testes coletados, todos verdes. `ruff check` +
`mypy --strict` sobre os 5 arquivos principais → limpos. `lint-imports` → 3 contracts kept, 0
broken. Cobertura por camada e suíte completa (1184 passed, 97,50%) não foi re-executada nesta
review (custo de ~4,5 min); aceita por citação do relato do QA ciclo 2, que nomeia o comando e o
número.

## `docs/INDEX.md` / `backend/README.md` append-only

`git diff origin/master...HEAD -- docs/INDEX.md` → **+1 linha**, nenhuma linha existente
reescrita. `git diff origin/master...HEAD -- backend/README.md` → uma seção nova acrescentada ao
final (`## 📎 2026-09-02 por T-07.5`), nenhuma seção existente tocada. **OK.**
