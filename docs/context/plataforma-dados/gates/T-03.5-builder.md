[QA GATE — Fase 03: Captura contínua]
Feature: plataforma-dados     Componente: sentimento
Arquivos alterados:
- backend/src/modules/sentimento/domain/premium_index_batch.py (new)
- backend/src/modules/sentimento/use_cases/collect_premium_index.py (new)
- backend/src/modules/sentimento/infra/premium_index_http_client.py (new)
- backend/src/modules/sentimento/infra/premium_index_jsonl_sink.py (new)
- backend/src/modules/sentimento/infra/premium_index_probe_cli.py (new)
- backend/tests/sentimento/test_premium_index_batch.py (new)
- backend/tests/sentimento/test_collect_premium_index.py (new)
- backend/tests/sentimento/test_premium_index_infra.py (new)
- backend/tests/sentimento/test_infrastructure_durability.py (modified — +1 teste de fsync)
- backend/README.md (modified — nova seção "O coletor premiumIndex")
- .gitignore (modified — `backend/.probe-evidence/`)
Spec: docs/specs/SPEC-001-plataforma-dados.md
Plan: docs/plans/SPEC-001-plataforma-dados/03_captura_continua.md (item 3.3)
Tracker: CST-21 · PR: https://github.com/StharleyMax/cripto-strategy/pull/58 (branch
tasks/T-03.5-premiumindex, base master@634833b)

DoD (tasks.toml id="T-03.5", tasks_review.md:269):
- [x] série nasce no primeiro poll e nunca antes — estrutural: `PREMIUM_INDEX_ENDPOINT` é uma
  string sem `?`, nenhuma função de domain/infra aceita parâmetro de tempo
- [x] batch confirmado por medição: peso 10/chamada, independente do universo — reproduzido AO
  VIVO (não só citado), ver "Comandos rodados" abaixo
- [x] código + probe curto, não deploy contínuo — nenhum cron/systemd acrescentado; CLI chamado
  manualmente 2x

Comandos rodados (literais) e resultado:
- `bash backend/scripts/lint.sh` → `All checks passed!` + `Success: no issues found in 101
  source files` (ruff + mypy --strict; universo: todo `src`+`tests`, 101 arquivos)
- `bash backend/scripts/test.sh` → rc=0, `Required test coverage of 70.0% reached` (ver
  cobertura abaixo). Universo exato: `pytest --collect-only -q | grep -oE ': [0-9]+$' | awk
  -F': ' '{s+=$2} END{print s}'` → **655 testes coletados**, 0 reportado como FAILED/ERROR
- `bash backend/scripts/boundaries.sh` → `Contracts: 3 kept, 0 broken` (universo: 77 arquivos,
  279 dependências)
- `harness rules --mode sweep --changed-only --format ndjson` → saída vazia, rc=0 (universo: 9
  arquivos do diff)
- Probe real (rede, zero chave):
  `python -m src.modules.sentimento.infra.premium_index_probe_cli --cycles 2
  --interval-seconds 1.0 --evidence backend/.probe-evidence/premium_index_probe.jsonl --summary
  backend/.probe-evidence/premium_index_probe_summary.json`
  → `n_simbolos=888` nos dois ciclos, peso 10→20 (delta exato 10), veredito `CONFIRMADO`, rc=0
  `[MEDIDO 2026-09-01]`. Evidência bruta NÃO versionada (`.gitignore`), reproduzível pelo comando.
- Falsificador (mutação manual, plantada e revertida, `git diff` limpo depois):
  (a) domain — neutralizada a recusa de `symbol` repetido em `parse_premium_index_batch` →
  `test_rejects_a_repeated_symbol` FALHOU (`DID NOT RAISE`); revertido.
  (b) infra — neutralizado o ramo `DIVERGENTE` do veredito de peso em `main()` →
  `test_probe_cli_main_returns_one_when_delta_diverges` FALHOU (`assert 0 == 1`); revertido.

Cobertura (bash backend/scripts/test.sh, coverage.xml): domain 100,0% (meta 90%, 1158/1158
linhas) · use_cases 100,0% (meta 80%, 289/289) · infra 98,4% (meta 70%, 967/983) · TOTAL 99,31%.

Doc delta:
- backend/README.md: nova seção "📡 O coletor premiumIndex" — tabela de peças por camada, "não
  faz", comando+números do probe ao vivo. Motivo: doc de módulo do repositório é append-only por
  task, e nenhuma seção prévia cobria esta task.
- ADR: não necessário — nenhuma decisão de arquitetura nova; reusa padrões já fixados por
  `ADR-001`, `ADR-008`, `ADR-011/D3a` (Natureza) e o Protocol de `https_quota_probe.py`.

Bloqueado: nenhum.
