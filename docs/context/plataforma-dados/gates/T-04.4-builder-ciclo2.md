# `T-04.4` (`CST-31`) — conserto do `/build`, ciclo 2

**Ação única:** adotar `backend/tests/sentimento/test_qa_probe_survivors.py`, transcrito na §7 do
relatório do `/qa` (`docs/context/plataforma-dados/gates/T-04.4-qa.md`). **Zero linha de código de
produção mudou** — o `NEEDS_FIX` não apontou falha de produção, apontou **5 fronteiras não presas**.

## 1. O que mudou — um arquivo, e ele é de teste

```bash
git diff --stat 982055b..HEAD -- backend/src   # (vazio: nenhuma linha de produção)
git status --short                             # ?? backend/tests/sentimento/test_qa_probe_survivors.py
```

| arquivo | estado | linhas |
|---|---|---|
| `backend/tests/sentimento/test_qa_probe_survivors.py` | **new** | 217 |

**A fixture entregue NÃO foi tocada.** O aviso do despacho é a razão e fica registrado: o default de
`bucket_interval_ms` **coincide de propósito** com o derivável de `interval="5m"`, e a discordância
entre **injetado** e **derivado** vive em **2 testes só** (`:509` e `:526` de `test_as_of_accessor.py`).
Unificar as larguras faria `M2` **voltar a ser equivalente por construção** — o buraco que o ciclo 1
fechou. A prova de que a discordância continua viva está no placar: `M2` morre, e morre **exatamente
pelos 2 testes de direção** (§3).

### 1.1 Verbatim, e a única divergência: `ruff format`

O arquivo foi copiado **byte a byte** do `<scratchpad>/qa/test_qa_probe_survivors.py` (`cmp` limpo).
O portão `lint-backend` **reprovou** essa cópia — `ruff format --check`, e só ele:

```
[FALHA] lint-backend rc=1   unformatted: File would be reformatted
  --> tests/sentimento/test_qa_probe_survivors.py:154:21   (e 167, 178 — quebras de linha)
```

`ruff format` foi aplicado. **A mudança é whitespace, e isso foi MEDIDO, não afirmado:**

```bash
.venv/bin/python -B -c "import ast; print(ast.dump(ast.parse(open('/tmp/probe-preformat.py').read()))
                        == ast.dump(ast.parse(open('tests/sentimento/test_qa_probe_survivors.py').read())))"
# True
```

`[MEDIDO 2026-08-29]` — **AST idêntica antes e depois**. O verbatim semântico está preservado; o que
mudou foi a reflexão de linha que o formatador do repositório exige. `diff -w -B` **não** serve aqui e
não foi usado como prova: `ruff format` **move quebras de linha**, e `-B` não vê isso.

## 2. Os seis portões — `make verify`, verde

```bash
cd /tmp/claude-1002/wt/T-04.4
export HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness
make verify
```

```
=== verify · T-04.4 · 20260829T215816Z (UTC) ===
[OK] lint-backend    rc=0  60 source files
[OK] lint-frontend   rc=0  ESLint do projeto sobre frontend/src
[OK] test            rc=0  444 passed · Total coverage: 99.29%
[OK] boundaries      rc=0  3 kept, 0 broken
[OK] regras          rc=0  0 bloqueio(s), 1 aviso(s)
[OK] política        rc=0
veredito: VERDE — 6 portões mediram e passaram
```

**Rodado DUAS vezes, e a segunda é a que importa** (`20260829T220111Z`): ela é **depois** da
bancada de mutação, e é o que prova que a bancada não deixou a árvore envenenada — o modo de
falha que a §6 do gate anterior documenta. Os dois números são idênticos: `444 passed · 99,29%`.

**O delta contra o gate do `/qa` é exatamente o esperado, e por isso ele é evidência e não ruído:**

| | `/qa` em `982055b` | aqui | delta |
|---|---|---|---|
| testes | **439 passed** | **444 passed** | **+5** — os 5 probes, nem um a mais |
| arquivos de fonte no lint | 59 | 60 | +1 — o arquivo novo |
| cobertura total | 99,29% | **99,29%** | **0** — probe de teste não move cobertura de `src`, e não deve |

Alvo declarado: `domain 90 / use_cases 80 / infra 70` (`ADR-009`/D1). `as_of_accessor.py` seguia em
**100%** já no ciclo 1; os 5 probes atacam **fronteira**, não linha descoberta — é por isso que a
cobertura não se move e o placar de mutação se move.

O aviso remanescente é `[web-fullstack.browser-test-file-present]`, **severidade `warn`**, em
`frontend/src/**` — pré-existente e fora desta task:

```bash
harness rules --mode sweep --changed-only    # rc=0 · 0 achado de severidade block · 1 aviso
```

## 3. A bancada completa re-executada — `n=40`, com atribuição de matadores por mutante

Script: a bancada **do `/qa`** (`qa_bench.py`), re-executada **como está** — instrumento independente
de quem consertou. Ela restaura o módulo com `assert` de igualdade byte a byte no fim, roda com
`python -B` e limpa `__pycache__` a cada mutante (a lição da §6 do gate anterior).

```bash
backend/.venv/bin/python -B <scratchpad>/builder2/bench.py all <scratchpad>/builder2/bench-final.json
# {"total": 40, "killed": 40, "survived": 0, "not_applied": 0, "survivors": [], "not_applied_names": []}
```

`[MEDIDO 2026-08-29, n=40 mutantes × 58 testes]` — **40 mortos, 0 sobreviventes, 0 não-aplicados.**
Placar anterior: **35/40**, `13/18` nos autorais do `/qa`. **Os 5 sobreviventes viraram 5 mortos, e
nenhum dos 35 que já morriam deixou de morrer.**

### Os 5 que o `NEEDS_FIX` nomeou — um matador cada, e é o matador certo

| mutante | antes | agora | matador (`n=1` cada) |
|---|---|---|---|
| `Q2` `bucket_end <= t` → `<` | SURVIVED | **KILLED** | `test_qa_q2_a_bucket_that_closed_exactly_at_t_is_admitted_by_r2` |
| `Q3` `observed_at <= knowledge_time` → `<` | SURVIVED | **KILLED** | `test_qa_q3_an_observation_made_exactly_at_the_horizon_is_inside_it` |
| `Q8` `t < first_capture_at` → `<=` | SURVIVED | **KILLED** | `test_qa_q8_at_the_first_capture_instant_the_absence_is_no_point_not_no_source` |
| `Q11` desempate reordenado | SURVIVED | **KILLED** | `test_qa_q11_the_tiebreak_reads_source_before_ingested_at` |
| `Q18` `projection()` perde `age_ms` | SURVIVED | **KILLED** | `test_qa_q18_the_projection_carries_exactly_the_eight_declared_keys` |

**`n_killers=1` em cada um não é fraqueza — é a prova de não-equivalência.** Um probe que matasse
vários mutantes estaria medindo outra coisa; o que se afirma aqui é *"este teste passa no original e
reprova **neste** mutante"*, e é isso que transforma cada sobrevivente em **defeito** em vez de opinião.

```bash
cd backend && .venv/bin/python -B -m pytest tests/sentimento/test_qa_probe_survivors.py \
    --no-header --no-cov -p no:cacheprovider
# 5 passed in 0.03s
```

⚠️ **Nota de instrumento, e ela explica um `(número não extraído)` que já apareceu neste projeto:**
`pyproject.toml:80` já traz `-q` em `addopts`. Passar `-q` na linha de comando faz **`-qq`**, e o
pytest **suprime a linha de sumário** — a saída fica só nos pontos `.....`, sem `5 passed`. O comando
acima **não** passa `-q`. Contar pontos não é medir; a linha de sumário é.

### Os mutantes-âncora do ciclo 1 continuam mortos, e pelos mesmos matadores

| mutante | matadores | quem mata |
|---|---|---|
| `M1` guarda vira `age_ms > 0` | **6** | `..._dies_exactly_when_one_whole_bucket_has_gone_by_and_not_one_ms_later`, +5 |
| `M2` largura **DERIVADA** de `SeriesKey.interval` | **2** | `test_the_injected_width_governs_when_it_is_narrower_than_the_key_s_interval_string` · `..._wider_...` |
| `M3` fronteira `>=` → `>` | **1** | `..._dies_exactly_when_one_whole_bucket_has_gone_by_and_not_one_ms_later` |

**`M2` com exatamente 2 matadores é o falsificador do aviso da §1.** Se alguém unificar as larguras da
fixture, esse número cai para **0** e `M2` volta a sobreviver — o teste do teste está no placar, não na
prosa.

## 4. A classe de defeito que isto fecha, dita uma vez

`R-2`, o horizonte de `knowledge_time`, `D4.6(c)`/`QF-4` e `D4.13` tinham **comportamento certo** e
**fronteira solta**: trocar `<=` por `<` passava **verde**. É a classe da inversão `D-01` — a regra
anti-lookahead que estava **invertida e propagada por dois documentos**. Um teste que afirma o
comportamento sem tocar o **instante em que os dois lados discordam** não prende nada; e `Q11` é o
formato do `Q3`/`ensure_ascii` de `T-04.2` — a asserção existe, o universo sobre o qual ela discrimina
é vazio.

## 5. Doc delta

- `docs/context/plataforma-dados/gates/T-04.4-builder-ciclo2.md`: **criado** — este relatório.
- SPEC / plano / `tasks.toml`: **sem mudança** — o conserto não muda contrato, DoD nem escopo; ele
  fecha a prova de 5 critérios que já estavam escritos no DoD (`R-1 ∧ R-2`, `knowledge_time`,
  `D4.6`/`QF-4`, `D4.13`).
- ADR: **não necessário** — nenhuma decisão nova. Adotar teste que o `/qa` provou não-equivalente é
  execução de `NEEDS_FIX`, não decisão arquitetural.

## 6. Aberto, com dono — **NÃO é ação deste ciclo**

`RATIO` sem carry e `verified_by` na identidade seguem **`[NÃO SEI]` declarado no código**, dono
`/architect`. O que este ciclo fez com eles foi **pinar o lado conservador**, não decidi-lo: `Q13`
(`RATIO`), `Q14` (`EVENT`) e `Q15` (`TICK`) morrem, cada um com 1 matador
(`test_only_stock_may_be_carried_forward_today_and_ratio_is_the_conservative_side`). Se o `/architect`
mudar a decisão, **o teste reprova e a mudança fica visível** — que é o comportamento desejado para um
`[NÃO SEI]`.

## 7. Anomalia observada, registrada porque enganaria o próximo

`make verify` imprimiu `[----] diff — sem mudança não-commitada` **com o arquivo novo em disco**. O
motivo é que o arquivo estava **untracked** no momento da corrida, e a checagem de diff do
`verify.sh` olha o que o `git` **rastreia**. Não é falha do portão — os outros seis mediram o arquivo
(o lint saltou de 59 para 60 fontes, o teste de 439 para 444) —, mas a linha **sozinha** sugeriria
árvore limpa. Fica escrito: `git status --short` é quem responde essa pergunta.
