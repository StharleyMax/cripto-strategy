# /review — T-01.10 (fase 01, componentes docs+infra, plataforma-dados)

Checkout: `/home/stharley/Documentos/projects/cripto-strategy`, branch `tasks/T-01.8-juizes-e-deploy`,
HEAD `008be7b`. Delta auditado: `53801a9..008be7b`, 28 arquivos, +1807/-13
`[MEDIDO: git diff --stat 53801a9..008be7b]`. Read-only — nenhum arquivo de código de produção
foi editado nesta auditoria; um clone descartável em `/tmp` foi usado só para o teste de
injeção do item (2), fora da árvore do repositório.

## Camada 1 — o que a máquina mede

```
harness rules list --severity block   # total: 8 regra(s) em vigor
harness rules --mode sweep --surface ci   # RC=0, 0 [BLOQUEIO], 16 [AVISO] pré-existentes
```

**Denominador: 8 regras bloqueantes em vigor, 8 avaliadas, via sweep completo da árvore
versionada (`git ls-files` sob `backend/src/`, `backend/tests/`, `frontend/src/`, `deploy/` com
extensões de `code_paths` — 335 arquivos `[MEDIDO: git ls-files ... | grep -E ... | wc -l]`).
Zero `[BLOQUEIO]` no sweep inteiro; os 16 `[AVISO]` são de `core.module-docstring-single-line` e
`web-fullstack.hardcoded-url`, pré-existentes e fora do escopo desta task (não tocam
`harness.toml`/`Makefile`/`deploy/`/`corpus/`).**

## Os 4 itens do mandato

### (1) `[LIMITE CONHECIDO]` é honesto e citável

`harness.toml:710-724` (comentário acima do bloco `own.compose-hardcoded-secret`) declara, com
exemplo reproduzível, exatamente o que a rodada 3 NÃO fecha:

> `POSTGRES_PASSWORD: hunter2_prod_master extra_ref:${UNUSED}` (sem `#` na linha) — ainda
> escapa, `rc=0`, porque a allowlist não recebe o span do match do `regex` principal
> (`lib/rules.py:205-216`), e fechar isso exige mudar o motor vendorizado do plugin, fora do
> escopo deste repositório.

Não finge cobertura: nomeia a causa raiz (motor `re.search()` de linha inteira, sem
back-reference de posição), dá o exemplo que ainda morde `False`, e nomeia o dono do conserto
(quem mantém o plugin, não esta task). Mesmo padrão do gap de `Dockerfile`/YAML flow-style já
declarado logo acima (`harness.toml:726-746`), que também cita comando e `rc` medidos.
**Confirmado: honesto, sem "cobertura fingida".**

### (2) `make lint-corpus` pega divergência real, não é decorativo

Reproduzido do zero, fora do repositório:
- `harness corpus verify`/`mutate` na árvore real: `RC=0` ambos, 11 casos, 1 violador em
  caminho de teste, "corpus dourado VERDE".
- Cópia para `/tmp`, `allowlist_regex` revertida para a forma pré-rodada-3 (sem o prefixo
  `^(?:(?!#).)*`) em `corpus/policy/harness.toml`: `harness corpus verify --corpus <cópia>
  --reference corpus/reference/compose_secret_reference.sh` → **`RC=1`**, "FALHAS DE IGUALDADE
  DE VEREDITO" nos casos `06`/`07`, exatamente a classe de divergência que o comentário do
  `Makefile:119-131` cita como já tendo acontecido de verdade em `02149f4`.
- `make lint-corpus` está cabeado em `make lint` (`Makefile:117`), e `make lint` é chamado por
  `scripts/hooks/pre-push.pre-harness:84`, que o `.git/hooks/pre-push` instalado invoca como
  hook "preservado" (`.git/hooks/pre-push:39-41`) além do `rules --mode sweep` gerado pelo
  harness. **É gate repetido em todo `push`, não comando manual de uma vez só** — fecha o
  `DEFEITO 1` que a auditoria paralela (`gates/fase-01-qa-2026-09-03.md`, "DEFEITO 1") havia
  achado em `e80645e`.

### (3) Nenhuma regra bloqueante alcança os arquivos por engano

- `harness code-paths classify corpus/*` → `nao-producao: nenhum include_prefixes casa` para
  `corpus/manifest.toml`, `corpus/policy/harness.toml`, `corpus/violating/01_password_mapping.yml`
  — `corpus/` não está em `include_prefixes` (`["backend/src/", "backend/tests/",
  "frontend/src/", "deploy/"]`), logo nenhuma regra de escopo `code`/`production` a alcança.
- `harness rules --mode file --path corpus/violating/01_password_mapping.yml` → `RC=0`, 0 byte
  — confirma que o fixture (que CONTÉM segredo literal de propósito) não é avaliado pelo
  repositório real, só pelo `corpus verify/mutate` dentro do próprio harness de fixture.
- `git ls-files` não encontra nenhum `.yml`/`.yaml` sob `backend/src/`, `backend/tests/` ou
  `frontend/src/` — logo `own.compose-hardcoded-secret` (`paths = ["**/*.yml","**/*.yaha"]`,
  `scope = "code"`) só pode colidir com `deploy/**`, que é exatamente o alvo declarado.
- `harness rules --mode file --path deploy/compose.yml` → `RC=0`, 0 byte: o compose real,
  com todo valor de credencial via `${VAR:?...}`, não dispara a própria regra que a task
  introduziu — comportamento correto (caso conformante), não ausência de cobertura.
- `harness code-paths classify backend/config/instrument_alias.yml` → fora de escopo
  (`backend/config/` não está em `include_prefixes`), confirmando que não há colisão lateral.

### (4) O corpus tem caso em caminho de teste

`corpus/policy/harness.toml` declara `test_globs = ["**/tests/**", "**/test_*"]`, e o caso
`deploy/tests/compose.env.yml` materializa isso — `harness corpus verify` reporta
**"violadores em caminho de teste: 1 (sem eles, estreitar o escopo de uma regra para produção
passaria verde)"**. O comentário do próprio `corpus/policy/harness.toml` (acima de
`test_globs`) nomeia o motivo: sem esse caso, `harness corpus mutate` estreitando `scope` de
`code` para `production` não mudaria veredito nenhum e o mutante sobreviveria por vacuidade.
Confirmado empiricamente: `harness corpus mutate` → `1/1 mutantes mortos`.

## Camada 2 — arquitetura declarada

Nada a reportar além do que a Camada 1 já cobre: os quatro itens do mandato são todos
verificáveis por comando (regra id, `rc`, arquivo:linha), não por julgamento de limite de
camada/direção de dependência — não há achado de Camada 2 nesta auditoria.

## Cruzamento com o QA gate

O veredito aqui reproduz, de forma independente, o que `docs/context/plataforma-dados/gates/T-01.10-qa.md`
(rodada 4, `APPROVED`) já havia registrado, e fecha explicitamente o `DEFEITO 1` da auditoria
paralela `docs/context/plataforma-dados/gates/fase-01-qa-2026-09-03.md` (que auditou o snapshot
`e80645e`, anterior ao commit `008be7b` desta revisão).

## Veredito

**COMPLIANT** — 8/8 regras bloqueantes em vigor avaliadas via `harness rules --mode sweep
--surface ci` sobre a árvore versionada (335 arquivos em `code_paths`), `RC=0`, zero
`[BLOQUEIO]`. Os 4 itens do mandato de auditoria (limite conhecido honesto, `make lint-corpus`
como gate real, ausência de colisão de regra, cobertura de caminho de teste no corpus) foram
todos reproduzidos com comando e `rc`, nenhum aceito por citação de documento sozinha.

```
harness gate-record plataforma-dados 01 REVIEW COMPLIANT "T-01.10: 8/8 regras bloqueantes avaliadas (rules --mode sweep --surface ci, rc=0, 335 arquivos em code_paths, 0 bloqueio/16 aviso pre-existente); LIMITE CONHECIDO em harness.toml:710-724 honesto (causa raiz + exemplo que ainda escapa + dono do conserto fora do escopo); make lint-corpus reproduz divergencia real (rc=1 ao plantar allowlist antiga em copia fora da arvore) e esta cabeado em make lint -> pre-push.pre-harness -> .git/hooks/pre-push, fechando DEFEITO 1 de fase-01-qa-2026-09-03.md; nenhum .yml/.yaml sob backend/src|tests ou frontend/src, logo own.compose-hardcoded-secret so alcanca deploy/**; corpus/policy/harness.toml tem caso em deploy/tests/ que sustenta o mutation gate (1/1 mutante morto)"
```
