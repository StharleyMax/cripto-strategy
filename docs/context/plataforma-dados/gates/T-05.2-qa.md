# T-05.2 — QA gate (independent verification, overwrites the self-attested builder claim)

**Escopo:** `docs/context/plataforma-dados/handoff/T-05.2.md` (D5.10, D5.11, T-08.2 close-out) +
`docs/context/plataforma-dados/gates/T-05.2-builder.md`. Worktree `.claude/worktrees/T-05.2`,
branch `tasks/T-05.2-s2-minima`. `backend/` not touched (confirmed: `git status --short` shows
no `backend/` entries).

## Regras bloqueantes (`harness rules list --severity block`, n=7)

`for f in frontend/src/charts/s2-*.ts frontend/src/charts/headless-chart.ts; do harness rules
--mode file --path "$f" --surface ci; done` → **rc=0 on all 16 files** (15 new `s2-*` +
`headless-chart.ts`). `core.relative-import`/`silent-except`/`print-statement`,
`web-fullstack.browser-imports-server`/`tenant-from-request` all OK — no rule fired.
`web-fullstack.server-test-directory-present` and `core.hardcoded-secret` not applicable to
this file set (no server dir / no secret literal found).

## Independent reproduction of the central claim

`npm --prefix frontend run test:charts` → **66 pass, 0 fail** (matches builder's claimed
66/0). `npm --prefix frontend run lint` → `rc=0`. `node -e
"console.log(require('lightweight-charts/package.json').version)"` → `5.2.1`, confirms "v5"
claim against the ACTUAL installed library, not a version string in prose.

Re-ran the suite myself (not trusting the builder's paste) and got byte-identical numbers to
`T-05.2-builder.md`/the ADR addendum: `D5.11 combined: n=15264 distinct_t=5760 worst=0.0000px
tol=0.5px within=true`; negative controls `OI worst=189.5019px gapCoord=568.49 vs null`,
`CVD delta worst=189.6337px gapCoord=568.10 vs null`. `data/binance/{metrics,aggtrades}`
confirmed to have NO `*2026-08-22*` file (`ls` — real gap, not fabricated).

## My own destructive probes (5 tests, own file, deleted after run — never committed)

Wrote `frontend/src/charts/qa-probe.test.ts`, own scenarios (own timestamps, own synthetic
data, not reused fixtures), ran with `node --test src/charts/qa-probe.test.ts` → **5/5 pass**:
1. Whitespace on a **candlestick** series (builder's price panel has zero gaps in this window,
   so `candlestickSeriesLossless`'s gap path was previously only asserted at data-structure
   level, never against the real library axis) — gap coordinate resolved, D5.11 held.
2. Gap at the very **start** of the plotted range (builder only tested a mid-range gap).
3. Gap at the very **end** of the range.
4. A **single-slot** gap (vs. the builder's 288/1440-slot whole-day gap) — ruled out a
   size-dependent effect.
5. A **fresh synthetic** naive-drop mutant, independent of `s2-panels`/`s2-cvd` entirely — also
   broke D5.11, ruling out that the builder's negative control was an artifact of their
   specific real-data shape.

None of these broke the lossless claim. The claim generalizes beyond the builder's own fixture.

## Mutation testing on production code (temporary, reverted, verified via `git diff` = empty)

Edited `s2-lightweight-adapter.ts`'s `lineSeriesLossless` to regress to naive-drop behavior
(same bug class as `naiveDropGapsLine`), ran `node --test src/charts/s2-axis-integration.test.ts
src/charts/s2-lightweight-adapter.test.ts` → **2 failures**: `NEGATIVE CONTROL — CVD delta`
(`AssertionError: the gap day's first minute must resolve to a coordinate in the lossless
series`) and the adapter unit test. **This proves the builder's own test suite is not vacuously
green — it catches the exact regression class it claims to guard against.** Reverted
immediately (`cp` from backup); `npm --prefix frontend run test:charts` re-run after revert:
**66/66 pass** again, confirming clean restoration. `git status --short frontend/src/charts/`
after cleanup shows no diff on `s2-lightweight-adapter.ts` and `qa-probe.test.ts` removed.

## Collateral finding (`series.data()` drops whitespace) — verified

`node_modules/lightweight-charts/dist/typings.d.ts:2492` does say "Original data items provided
via setData or update methods" on `series.data()`'s JSDoc; the suite's own measured
`itemsSent`/`whitespaceItemsSent`/`dataLength` numbers (e.g. OI: sent 1152, whitespace 288,
`dataLength` 864) are internally consistent (`864 = 1152 - 288`) and match my independent
mutation-free re-run. The claim "the real proof is `timeToCoordinate`, not `.data()`" is
correct: `.data()` is a lossy accessor, `coordinateOf`/D5.11 is the load-bearing evidence, and
the code/tests already say so rather than silently relying on `.data()`.

## D5.10

`s2-annotation-identity.ts`: `createAnnotationIdentity` throws `MissingPrincipalIdError` on
empty `principalId`, no default value exists in the signature (required positional param —
`TypeError` at the call site if omitted, independent of the module's own runtime check). Own
falsifier in `s2-annotation-identity.test.ts` (two calls, two different principals → two
different identities) is a real behavioral check, not a tautology. `principalId` is stored
verbatim, never coerced to a constant.

## Scope check — no creep beyond the handoff

`git status --short` file list matches exactly the declared new-file list (9 modules + their
`.test.ts`, `headless-chart.ts` additive-only diff confirmed by `git diff` — 2 private→export
renames, zero logic lines changed, `frontend/src/charts/*.test.ts` for T-08.2 unaffected).
`grep -rn "frontend/src/app"` over the new `s2-*.ts` files → no match: no `web` mount point was
created, consistent with the handoff's "fora de escopo" list (`T-05.3`..`T-05.7`, `D5.12`
boundary untouched). `docs/INDEX.md` diff is `+1` line (append-only, respected).
`docs/context/plataforma-dados/tasks.toml` still has `T-05.2` unresolved (not self-closed by
the builder, per the handoff's own instruction) — QA does not resolve it either; that is the
orchestrator's job after this gate.

## Veredito

```
## QA Gate — Fase 05 [charts]
- [OK] core.relative-import / core.silent-except / core.print-statement / web-fullstack.browser-imports-server / web-fullstack.tenant-from-request — `harness rules --mode file --path <f> --surface ci` rc=0 on all 16 touched files
- [OK] Testes existem e passam — `npm --prefix frontend run test:charts` (66 testes, 0 fail) + `npm --prefix frontend run lint` (rc=0)
- [OK] Cobertura — D5.11 medido em escala real (n=15264, worst=0.0000px vs tolerância 0.5px); sem alvo de % de cobertura declarado para `charts` em política, não aplicável
- [OK] DoD da fase — D5.10 (principal_id nunca NULL/constante, falsificador próprio verificado) e D5.11 (0.0000px, dentro de 0.5px) ambos confirmados independentemente
- [OK] Achado T-08.2 (`docs/INDEX.md:97`) fechado com evidência: 3 controles negativos (builder's OI/CVD + minha mutação sintética própria) todos quebram D5.11 em ~189-379x tolerância quando gaps são dropados em vez de whitespace
- [OK] 5 probes adversariais próprias (candlestick whitespace, gap na borda inicial/final, gap de 1 slot, mutante sintético) — todos passaram, claim generaliza
- [OK] Mutação em produção (`lineSeriesLossless` regredido para naive-drop) prova que a suíte pega a regressão — 2 falhas reais, revertido, `test:charts` volta a 66/66
- [OK] Escopo — sem scope creep: nenhum ponto de montagem `web` criado, `headless-chart.ts` só exporta 2 símbolos (zero lógica alterada), `docs/INDEX.md` append-only (+1 linha), `tasks.toml` não autodeclarado
Regras bloqueantes avaliadas: 7 de 7
Veredito: APPROVED
```

---

# Round 2 — after builder's ADR-003 FR-1 correction (`/review` finding, disk I/O removed from production loaders)

**Escopo:** `docs/context/plataforma-dados/gates/T-05.2-builder.md` §"Correção `ADR-003` FR-1" (the
diff: `s2-klines-loader.ts`/`s2-oi-loader.ts`/`s2-cvd.ts`/`s2-panels.ts` reduced to pure functions
taking already-read string content; all `readFileSync`/`existsSync` moved into `.test.ts` files).
Round 1 (above) still stands as history for D5.11/D5.10/the negative controls it verified before the
refactor; this round re-verifies independently that the refactor didn't change behavior and closes the
FR-1 finding with my own evidence, not the builder's.

## 1. `node:fs`/`node:path` genuinely gone from production — own grep, not the builder's paste

```
$ find frontend/src/charts -name "*.ts" ! -name "*.test.ts" -print0 \
  | xargs -0 grep -nE 'node:(fs|path)|require\("fs"\)|from "fs"|readFileSync|existsSync|readdirSync'
```
→ 10 matches, **all inside `/** ... */` doc comments** (the "a prior version of this file called
`readFileSync` directly" explanatory text in `s2-cvd.ts`, `s2-panels.ts`, `s2-klines-loader.ts`,
`s2-oi-loader.ts`) — zero in executable code. Stricter pass, import/require statements only
(`^\s*(import|const|let|var).*from\s+"node:(fs|path)"|require\("(fs|path|node:fs|node:path)"\)`) over
the same 15 non-test files → **0 matches** `[MEDIDO 2026-09-03]`. `head -20 s2-panels.ts` /
`grep -n "^import" s2-panels.ts s2-klines-loader.ts s2-oi-loader.ts s2-cvd.ts` confirms all imports are
local (`./canonical-grid.ts`, `./s2-scalar-grid.ts`, `./canonical-grid-chart-consumer.ts`, `./s2-cvd.ts`)
— no `node:` builtin anywhere. `git diff -- frontend/src/charts/headless-chart.ts` confirms the only
change there is 2 `function` → `export function` (zero logic lines touched), matching the builder's claim.

## 2. Independent reproduction of D5.11 + negative controls after the refactor

`npm --prefix frontend run test:charts` → **69 pass, 0 fail** (`[MEDIDO 2026-09-03]`, matches builder's
claimed 69, up from round 1's 66 — 3 new tests: `s2-klines-loader.test.ts` didn't exist before this
refactor). `npm --prefix frontend run lint` → `rc=0`.

`node --test src/charts/s2-axis-integration.test.ts` run standalone, byte-identical to round 1's claim:
- **D5.11 combined**: `n=15264 distinct_t=5760 worst=0.0000px tol=0.5px within=true`.
- **Negative control — OI**: `worst=189.5019px gapCoord=568.4939236111111 vs null` (`379×` tolerance).
- **Negative control — CVD delta**: `worst=189.6337px gapCoord=568.0987847222223 vs null` (`379×`
  tolerance).

Numbers match round 1's claim exactly (`0.0000px`, `189.5019px`, `189.6337px`) — the refactor moved I/O
out of production code without changing a single computed value. `data/binance/{metrics,aggtrades}` gap
day is still `2026-08-22` (unchanged fixture, confirmed by the same test's own precondition assertion
`"exactly one real gap day (08-22)"` passing).

## 3. No regression elsewhere

`npm --prefix frontend run test:app` → **116 pass, 0 fail**. `npm --prefix frontend run test:s1` →
**36 pass, 0 fail**. Both unaffected by the `charts` refactor, as expected (no import from `charts` into
`app`/`s1-console`).

## 4. Own mutation on the NEW pure functions (post-refactor) — confirms the suite still bites

Backed up `s2-klines-loader.ts`, mutated the `close` column boundary in `parseKlinesCsv` (the function
`/review`'s finding forced into existence as a standalone pure export):
```diff
-      close: Number(cells[closeIndex]),
+      close: Number(cells[closeIndex + 1]),
```
Ran `node --test src/charts/s2-klines-loader.test.ts src/charts/s2-panels.test.ts
src/charts/s2-axis-integration.test.ts` → **1 failure** (8 pass): `parseKlinesCsv parses a minimal
well-formed row by column name, not position` — `AssertionError`, `close: 1000` (wrong, off-by-one into
the volume column) vs expected `close: 101`. This is `s2-klines-loader.test.ts`, a test file the
refactor itself created (it didn't exist before FR-1's correction, since the loader had no pure
CSV-parsing entry point worth unit-testing in isolation before). **The suite born from the refactor
catches a corrupted parse boundary in the refactor's own new code — the pure-function split didn't just
move code around vacuously, it's independently falsifiable.** Reverted (`cp` from backup);
`git diff --stat -- frontend/src/charts/s2-klines-loader.ts` → empty (file is untracked/new, backup
byte-identical to restored file). Re-ran `node --test src/charts/s2-klines-loader.test.ts` → **3/3 pass**
and `npm --prefix frontend run test:charts` → **69/69 pass** again, confirming clean restoration.
`git status --short frontend/src/charts/` after cleanup: only the expected new/untracked files and the
1 additive `headless-chart.ts` diff — no stray probe files.

## 5. Rule check on all 17 touched files (16 new/modified `s2-*.ts` + `headless-chart.ts`)

`for f in frontend/src/charts/s2-*.ts frontend/src/charts/headless-chart.ts; do harness rules --mode
file --path "$f" --surface ci; done` → **rc=0 on all 17 files**, no rule fired
(`core.relative-import`/`silent-except`/`print-statement`, `web-fullstack.browser-imports-server`/
`tenant-from-request` all OK). `core.hardcoded-secret` / `web-fullstack.server-test-directory-present`
not applicable (no server dir under `frontend/src/charts/`, no secret literal).

## Veredito — round 2

```
## QA Gate — Fase 05 [charts] — round 2 (post ADR-003 FR-1 correction)
- [OK] core.relative-import / core.silent-except / core.print-statement / web-fullstack.browser-imports-server / web-fullstack.tenant-from-request — `harness rules --mode file --path <f> --surface ci` rc=0 on all 17 touched files
- [OK] ADR-003 FR-1 (`charts` faz zero I/O) — own grep for `node:fs`/`node:path`/`readFileSync`/`existsSync` over every non-test `.ts` under frontend/src/charts/: 0 executable matches (10 hits, all inside doc comments explaining the fix)
- [OK] Testes existem e passam — `npm --prefix frontend run test:charts` (69 testes, 0 fail, up from round 1's 66) + `npm --prefix frontend run lint` (rc=0)
- [OK] Refactor não mudou comportamento — D5.11 re-medido standalone: n=15264, worst=0.0000px (tol 0.5px), negative controls OI worst=189.5019px / CVD delta worst=189.6337px (both ~379x tolerância) — byte-identical aos números do round 1
- [OK] Sem regressão — `npm --prefix frontend run test:app` (116/116) e `test:s1` (36/36) inalterados
- [OK] Mutação própria nas NOVAS funções puras (`parseKlinesCsv`, corrompido o limite de coluna `close`) — 1 falha real em `s2-klines-loader.test.ts` (arquivo que o próprio refactor criou), revertido, `test:charts` volta a 69/69 — prova que a suíte nascida do refactor morde, não é vácuo
- [OK] Escopo — mudança confinada a `frontend/src/charts/` + `headless-chart.ts` (2 exports aditivos) + `docs/adr/ADR-003-fronteira-charts-web.md` (addendum) + `docs/INDEX.md`; `backend/` intocado (`git status --short` sem entradas)
Regras bloqueantes avaliadas: 7 de 7
Veredito: APPROVED
```
