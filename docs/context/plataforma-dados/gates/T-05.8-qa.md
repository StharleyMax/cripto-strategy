# QA Gate — `T-05.8` (`plataforma-dados`, fase 05, componente `web`)

**Executor:** `qa` · **Data:** 2026-09-02 · **Worktree:** isolada
(`.claude/worktrees/agent-a15716f53e550998b`, `HEAD=524f803`)

## 1. Escopo (herdado do `[QA GATE]`)

Arquivos: `frontend/src/app/knowledge-time-bundle.ts` (novo) +
`frontend/src/app/knowledge-time-bundle.test.ts` (novo), `frontend/package.json`
(`+test:app`), `frontend/README.md` (`+§8`). `DoD` alvo: `D5.4` — *"`COMO EM T` sobrevive
à navegação"*, com **teste negativo obrigatório**: *"voltar para `AGORA` não tem sintoma
visível ⇒ reprova"* (`docs/plans/SPEC-001-plataforma-dados/05_fatia_visivel.md:32`).
`tasks.toml:684-691` — `refs` cita exatamente `DoD D5.4` + `teste negativo obrigatorio`.

## 2. Testes que já existiam, rodados antes de qualquer coisa nova

```
cd frontend && npm run test:app   # node --test 'src/app/*.test.ts'
-> tests 13, pass 13, fail 0
npm run lint                       # eslint src -> 0 erro, 0 aviso
```
Confere com o relatório do `/build`. Nenhum teste novo foi criado por este gate — os 13
já cobrem o contrato; a validação destrutiva (§3) é sobre os testes existentes.

## 3. Validação destrutiva do teste negativo (`D5.4`) — 2 mutações, 2 capturas

Procedimento: mutar `frontend/src/app/knowledge-time-bundle.ts` reintroduzindo o defeito
que `D5.4` proíbe, rodar `npm run test:app`, confirmar `fail ≥ 1` nomeando o teste
correto, reverter com `git checkout --` e reconfirmar `13/13` + `git diff --stat` vazio.
Produção **não foi alterada** ao final (evidência: `git diff --stat` vazio após cada
reversão, checado nas duas rodadas).

**Mutação 1 — `returnToLive` via spread (o bug literal do DoD: "voltar para AGORA sem
sintoma visível")**
```diff
-  return { mode: "live", symbol: bundle.symbol, window: bundle.window };
+  return { ...bundle, mode: "live" } as unknown as LiveBundle;
```
```
npm run test:app -> pass 12, fail 1
✖ returnToLive descarta knowledgeTime por TIPO, nao por convencao
  AssertionError: actual has extra field knowledgeTime: '2023-10-26T14:30:00Z'
```
Captura **confirmada**. Achado colateral, registrado por precisar de rigor: dos 3 testes
que tocam `returnToLive`, só este (o de `deepEqual` + `"knowledgeTime" in back`) morde —
os outros dois (URL sem `t`, navigate pós-retorno) **não** capturam esta mutação
específica, porque `encodeBundle`/`navigate` já filtram por `bundle.mode` e ignoram o
campo estranho a jusante. Não é lacuna do DoD (o teste que precisa morder morde, e a
suíte falha, `rc≠0`), mas registra que a defesa em profundidade é mais rasa do que os 3
testes sugerem à primeira leitura.

**Mutação 2 — remover a guarda de `decodeBundle` contra `mode=live` + `t` residual (a
metade "URL mente" de `D5.4`)**
```diff
-    if (knowledgeTime !== null) {
-      throw new Error('... retrocesso silencioso que D5.4 proibe ...');
-    }
     const bundle: LiveBundle = { mode: "live", symbol, window: { from, to } };
```
```
npm run test:app -> pass 12, fail 1
✖ decodeBundle RECUSA mode=live com t ainda presente — e o bug que D5.4 proibe, tornado impossivel
  AssertionError [ERR_ASSERTION]: Missing expected exception.
```
Captura **confirmada**.

Ambas as reversões confirmadas: `git diff --stat` vazio, `npm run test:app` → `13/13`,
`npm run lint` → limpo, após cada mutação.

**Conclusão da prova destrutiva:** o teste negativo não é decorativo — as duas formas
plausíveis de reintroduzir o defeito (estado zumbi por spread; URL que mente sobre
`mode`) são cada uma capturada por um teste nomeado, e a suíte fecha com `rc≠0` nos dois
casos. `NEEDS_FIX` teria evidência anexável se qualquer uma tivesse passado — nenhuma
passou.

## 4. Regras bloqueantes (`harness rules list --severity block`, 7 regras)

`harness rules --mode sweep --format ndjson` sobre a árvore completa em `524f803`:
**0 achado de severidade `block`** (script contou via `json.loads` linha a linha,
filtrando `severity=='block'`). Achado único nos dois arquivos novos:
`web-fullstack.hardcoded-url` (**`warn`**, não bloqueante),
`frontend/src/app/knowledge-time-bundle.test.ts:22` — URL sintética de teste
(`https://painel.local/simbolo`), já declarada e justificada em `README.md §8`.

| # | regra | veredito | evidência |
|---|---|---|---|
| 1 | `core.relative-import` | OK | 0 achado no sweep (bloqueante) |
| 2 | `core.silent-except` | OK | não se aplica (TS, sem `except`); 0 achado |
| 3 | `core.print-statement` | OK | 0 achado |
| 4 | `core.hardcoded-secret` | OK | 0 achado |
| 5 | `web-fullstack.browser-imports-server` | OK | `knowledge-time-bundle.ts` não importa de servidor — módulo puro, zero import externo |
| 6 | `web-fullstack.tenant-from-request` | OK | não se aplica ao módulo (sem request) |
| 7 | `web-fullstack.server-test-directory-present` | OK | não se aplica ao componente `web`/`frontend` desta task |

## 5. Cobertura

Sem piso declarado para `web` (`harness policy --key test_cmd` só cobre `sentimento`,
confirmado; `frontend/README.md §4/§7` documentam a lacuna como decisão em aberto do
dono do componente). Medição qualitativa herdada do `/build`, reconfirmada: 13 testes
cobrem as 8 funções exportadas, os dois lados de `D5.4` (positivo: 3 saltos; negativo:
retorno sem sintoma) e as 4 recusas de `decodeBundle`. Nenhum alvo numérico a reprovar
contra.

## 6. DoD `D5.4`, item a item

- "`COMO EM T` sobrevive aos três saltos" — `OK`, teste `navigate preserva mode=as_of e
  knowledgeTime atraves de tres saltos de navegacao`, 13/13.
- "Teste negativo obrigatório: voltar para AGORA não tem sintoma visível ⇒ reprova" —
  `OK`, **provado por mutação** (§3), não só presente.
- Fronteira `charts`/`web` (pixel do sintoma) — fora de escopo desta task por decisão do
  `ui-designer` (`T-05.8-design.md`, `R4`), não reavaliada aqui: este gate mede
  contrato de estado, que é o que `T-05.8` entrega.

## 7. Anomalias

Nenhuma. `harness rules`, `npm run test:app`, `npm run lint` produziram veredito
determinístico em todas as chamadas.

## Veredito

**APPROVED.**
