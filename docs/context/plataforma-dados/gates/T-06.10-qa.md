# QA — T-06.10 · S3 inspetor de série (catálogo filtrável + `src_label_raw` ao lado de `event_time`)

**Card:** `CST-54` · **Componente:** `web` · **Fase:** `06`, item 6.16, `D6.15` · **Data:** 2026-09-03
**PR:** https://github.com/StharleyMax/cripto-strategy/pull/98 · **Branch:** `tasks/T-06.10-fase06`
**Worktree:** `/tmp/claude-1002/wt/T-06.10`

## 0. Bootstrap

```
harness policy --key docs.bootstrap_docs   -> ["docs/proposta-discovery.md"]
harness policy --key test_cmd              -> {"sentimento": {...}}   # sem chave `web` — lacuna
                                               declarada em harness.toml:290-296 (frontend não tem
                                               `[test_cmd.web]`; regra `browser-test-file-present`
                                               fica como AVISO por desenho). Não é anomalia desta
                                               task — é estado documentado do repositório.
harness rules list --severity block        -> 7 regras (core×4, web-fullstack×3)
```

Nenhuma das 7 regras bloqueantes tem escopo em módulo TS sem app Next.js montado
(`browser-imports-server`, `tenant-from-request` são sobre padrões de import/request que este
módulo não usa; `server-test-directory-present` é sobre `backend/`). Verificado por leitura —
nenhuma delas casa com o diff.

## 1. Testes — rodei eu mesmo, não confiei no relato do builder

```
$ npm --prefix frontend run test:s3
ℹ tests 37
ℹ pass 37
ℹ fail 0
```

Confirmado: 37 testes, 0 falha, `node_modules` já presente na worktree (sem instalação necessária
nesta sessão). `npm --prefix frontend run lint` → saída vazia, `rc=0`.

## 2. DoD, item a item

- **Catálogo FILTRÁVEL** — `domain.ts::filterCatalogRows` (AND por texto/procedência/quarentena),
  `S3Inspector.tsx:70-77` monta o `<input>` controlado. `view-model.test.ts` e
  `domain.test.ts` não testam `filterCatalogRows` diretamente por nome, mas
  `buildS3ViewModel` (`view-model.test.ts:119-133`) o exercita via `EMPTY_CATALOG_FILTER`. **OK,
  com nota**: não há teste unitário de `filterCatalogRows` com filtro não-vazio (texto/procedência/
  quarentena) — ver Ação 1 abaixo.
- **D6.15 — `src_label_raw` NA MESMA LINHA que `event_time`** — `domain.ts:56-62`
  (`RawDataRow.eventTime` + `RawDataRow.srcLabelRaw`, mesmo objeto/linha), `view-model.ts:99-104`
  (`InspectorRowView` união discriminada, ramo `data` carrega os dois campos juntos),
  `S3Inspector.tsx:188-189` (mesma `<tr>`, colunas adjacentes). Testado por
  `view-model.test.ts:72-83` (`buildInspectorRowViews mantém src_label_raw e event_time na MESMA
  linha formatada`) — **PROVADO por mutação** (§4, Mutação 1).
- **Formato de completude, sem denominador inventado** —
  `view-model.ts:39-45::completenessText`: grade → `"N/M · k lacuna(s)"` com singular/plural
  correto (`view-model.test.ts:37-40`); tick → `"contiguidade (N saltos)"`, teste afirma
  explicitamente `!text.includes("/")` (`view-model.test.ts:42-46`). **OK.**
- **Gaveta de quarentena embutida** — `S3Inspector.tsx:221-254`, `<aside>` na MESMA árvore JSX do
  componente único (não `src/app/routes.ts`, que nem existe ainda), estado vazio explícito
  (`buildQuarantineDrawer` → `isEmpty`, `view-model.test.ts:109-115`). **OK.**
- **Divergência EXIBIDA, nunca reconciliada** — `domain.ts:98-112` (`DivergenceReading`/
  `DivergenceRow` sem campo de "valor escolhido" — falsificador estrutural, não só convenção),
  `view-model.ts:146-151::buildDivergenceRowView` mapeia TODAS as leituras sem filtrar,
  `S3Inspector.tsx:198-216` renderiza a lista inteira sob o rótulo "DIVERGÊNCIA ENTRE FONTES — NÃO
  RECONCILIADA". Teste: `view-model.test.ts:98-105`. `grep -rniE 'choose|chosen|resolved_value|
  pick(One|Source)|reconcile|winner|preferred' frontend/src/features/s3-inspector/` → 3
  ocorrências, todas em COMENTÁRIO explicando a ausência da lógica, nenhuma em código executável.
  **OK.**
- **Nenhuma ação de escrita** — `S3Inspector.tsx` só tem dois handlers: `onFilterTextChange`
  (texto do filtro) e `onOpenSeries` (abre a Camada 2, leitura). `grep -niE 'onEdit|onCorrect|
  onResolve|onClose.*[Qq]uarant|onExit' frontend/src/features/s3-inspector/*.tsx` → vazio. O botão
  "abrir" é texto sublinhado, nunca um botão de ação colorida (`S3Inspector.tsx:124-131`). **OK.**
- **`backend/` intocado** — `git diff master...HEAD --stat -- backend/` → saída vazia, `rc=0`.
  **OK.**

## 3. Cobertura

Não há alvo de cobertura declarado para `frontend/` nesta política (`test_cmd` só declara
`sentimento`) — mesma lacuna documentada em `harness.toml:290-296`. Sem alvo, não há "abaixo do
alvo" a reprovar. Medida informativa: 4 módulos de domínio/formatação (`series-catalog.ts`,
`quarantine.ts`, `domain.ts`, `view-model.ts`) com teste próprio 1:1 — nenhum arquivo de lógica
sem arquivo de teste correspondente.

## 4. Mutação real plantada (disciplina do backend aplicada aqui)

Editei os arquivos de PRODUÇÃO temporariamente, rodei a suíte, e restaurei com
`git checkout -- <arquivos>` (`git status --short` limpo ao final — confirmado abaixo). Nunca
commitei as mutações.

**Mutação 1 — remover `srcLabelRaw` do view formatado (ataca D6.15 diretamente):**
```diff
- srcLabelRaw: row.srcLabelRaw,
+ // campo removido
```
Resultado: `view-model.test.ts:72` (`buildInspectorRowViews mantém src_label_raw e event_time na
MESMA linha formatada`) quebra com `TypeError: Cannot read properties of undefined (reading
'length')`. **Pega.**

**Mutação 2 — inverter o predicado de quarentena (`isQuarantined`):**
```diff
- return !(terms.labelShiftPresent && terms.unitPresent && terms.availableAtPresent);
+ return (terms.labelShiftPresent && terms.unitPresent && terms.availableAtPresent);
```
Resultado: múltiplas falhas, incluindo `view-model.test.ts:66`
(`quarantineBadgeText de série em quarentena traz a palavra QUARENTENA...`) — esperado
`'QUARENTENA'`, obtido `null`. **Pega.**

Restauração confirmada:
```
$ git status --short                    -> (vazio)
$ npm --prefix frontend run test:s3     -> 37 pass, 0 fail
```

Não tentei mutar a troca de ramo grade/tick em `completenessText` porque a primeira formulação
(apenas reordenar os dois `if`/`return`) é semanticamente equivalente — não é mutação real, é
reescrita. As duas mutações acima são estruturais (removem um campo do contrato; invertem um
predicado booleano), classe que o backend usa como padrão de mutação.

## 5. O que NÃO valido aqui (fora do escopo desta task)

- Não repito a auditoria de UX/design — já fechada em `T-06.10-ux-audit.md` (APROVADO, 0 Must-Fix).
- Não valido paridade byte-a-byte com dado real (`fixtures.ts` é sintético, com proveniência
  citada por comentário — mesmo padrão de `s1-console/fixtures.ts`, aceito no builder).
- Não critico a ausência de app Next.js real — é a mesma fronteira de prova que `S1Console.tsx`/
  `T-05.8`/`T-08.5`/`T-08.11` usaram, e o handoff nomeia essa lacuna explicitamente (não é dívida
  silenciosa).

## Veredito

```
## QA Gate — Fase 06 [web]
- [OK] D6.15 (src_label_raw na mesma linha que event_time) — domain.ts:56-62, view-model.ts:99-104,
  S3Inspector.tsx:188-189; provado por mutação (Mutação 1, §4)
- [OK] Formato de completude sem denominador inventado — view-model.ts:39-45; testes
  view-model.test.ts:37-46
- [OK] Gaveta de quarentena embutida, não rota separada — S3Inspector.tsx:221-254
- [OK] Divergência exibida, nunca reconciliada — domain.ts:98-112 (falsificador estrutural),
  provado por mutação (Mutação 2, §4)
- [OK] Nenhuma ação de escrita — S3Inspector.tsx só tem onFilterTextChange/onOpenSeries
- [OK] backend/ intocado — git diff master...HEAD --stat -- backend/ (vazio)
- [OK] Testes existem e passam — npm --prefix frontend run test:s3 (37 testes, 37 pass, 0 fail)
- [OK] Lint — npm --prefix frontend run lint (rc=0)
- [OK] Cobertura — sem alvo declarado para `web` em test_cmd (lacuna documentada em
  harness.toml:290-296, não é anomalia desta task); 4/4 módulos de lógica com teste 1:1
- [OK] DoD da fase, item a item — §2 acima
- [nota, não-bloqueante] filterCatalogRows não tem teste unitário direto com filtro não-vazio
  (só exercitado indiretamente via buildS3ViewModel com EMPTY_CATALOG_FILTER) — ação sugerida,
  não reprova o gate porque a lógica é simples (3 AND) e o comportamento observável
  (catalogRows.length) já é coberto no caminho vazio
Regras bloqueantes avaliadas: 7 de 7 listadas por `harness rules list --severity block`
  (nenhuma casa com o diff — verificado por leitura, §0)
Veredito: APPROVED
```

### Ação sugerida (não-bloqueante, registrada para não virar dívida silenciosa)

1. Acrescentar 1-2 casos a `domain.test.ts` exercitando `filterCatalogRows` diretamente com
   `text`/`provenance`/`onlyQuarantined` não-vazios (hoje só o caminho `EMPTY_CATALOG_FILTER` é
   testado via `view-model.test.ts`). Não bloqueia esta fase — comportamento observável já coberto
   indiretamente, e a função é 3 filtros AND simples sem branch não exercitado.
