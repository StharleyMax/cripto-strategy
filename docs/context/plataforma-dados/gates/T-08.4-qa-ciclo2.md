# QA Gate — T-08.4 (`backtest`), ciclo 2

## Contexto
Ciclo 1 reprovou `bundle_hash` por não ser order-independent sob reordenação de chaves de dict
(`ADR-021`/G3, `test_bundle_hash_determinism_qa.py`). Builder corrigiu com `_canonicalize_dict`
(ordena chaves recursivamente antes de `canonical_json`) — ver
`backend/src/modules/backtest/domain/bundle_hash.py:54-70` e "Emenda 2026-09-04" em
`docs/adr/ADR-021-run-registry-reprodutibilidade-de-backtest.md:178`.

## Evidência

1. **Teste vermelho do ciclo 1, agora verde.**
   `.venv/bin/python -m pytest tests/backtest/test_bundle_hash_determinism_qa.py --no-cov -q`
   → `1 passed`.

2. **`test_bundle_hash.py` inteiro passa** — 6 testes originais + 2 novos que QA acrescentou
   neste ciclo para os casos que `_canonicalize_dict` poderia não cobrir:
   `test_dicts_nested_inside_a_list_are_still_order_independent` (dict aninhado DENTRO de uma
   lista — `_canonicalize_value` desce em cada elemento, não só no dict de topo) e
   `test_none_valued_fields_do_not_break_order_independence` (valor `None` sobrevive à
   reordenação; `None` explícito ≠ chave ausente).
   `.venv/bin/python -m pytest tests/backtest/ --no-cov -q` → `39 passed`.

3. **Mutação prova que o verde não é vácuo** — reverti `_canonicalize_dict(bundle)` para
   `bundle` puro (bypassando a correção) e rodei a suíte: **5 testes falharam**, incluindo o
   `test_g3_...` (a prova que o ciclo 1 escreveu) e os 2 testes novos deste ciclo. Reaplicado o
   fix na sequência — `39 passed` de novo. Código de produção não foi alterado (a mutação foi
   feita e revertida na mesma sessão, só para instrumentar o teste; `git status` confirma
   `bundle_hash.py` como o mesmo arquivo untracked de antes, conteúdo idêntico ao commitado
   pelo builder).

4. **Caso extra investigado e classificado como fora do domínio:** chaves de tipos mistos num
   mesmo dict (ex.: `{"a": 1, 2: "b"}`) faz `sorted(payload)` levantar `TypeError` — mas a
   assinatura de `bundle_hash`/`_canonicalize_dict` é `dict[str, object]` em toda profundidade;
   um dict com chave não-string é violação do contrato de tipo do chamador, não um bundle
   legítimo do domínio (universo, `ThresholdSpec`, `fee_schedule`, `cvd_anchor`,
   `price_source` — todos `dict[str, ...]`). Falhar alto (`TypeError`) em vez de produzir hash
   errado silenciosamente não é o modo de falha que G3 teme; não vira teste permanente.

5. **Suíte completa do backend** (`bash backend/scripts/test.sh`, sem filtro):
   `1476 passed, 3 warnings in 406.50s`, cobertura TOTAL 97.64% (piso 70%), por camada
   `ADR-009/D1`: domain 99.8%/90%, use_cases 100.0%/80%, infra 95.2%/70% — todas `[OK]`. `rc=0`.

6. **`ADR-021` — append-purity da Emenda.** O arquivo é novo nesta feature (nunca commitado
   antes — `git log --oneline -- docs/adr/ADR-021-...md` vazio), então `git diff --numstat`
   não separa "antes"/"depois" da emenda por não haver baseline commitado — **anomalia
   metodológica, não FAIL**: o comando pedido não se aplica a um arquivo ainda não commitado.
   Verificação estrutural feita em substituição: "Emenda 2026-09-04" é a ÚLTIMA seção do
   documento (linha 178, após "Consequência para quem implementar", linha 168), e o texto de
   `D3` original (linhas 72-102) permanece intacto e não contraditado — a emenda só acrescenta,
   não reescreve a decisão original. `docs/INDEX.md` (que TEM baseline) confirma append puro:
   `git diff --numstat -- docs/INDEX.md` → `2  0` (2 linhas adicionadas, 0 removidas).

7. **`harness rules --mode sweep --changed-only`** → `rc=0`, saída vazia — nenhuma das 8 regras
   bloqueantes disparou sobre os arquivos alterados/novos deste ciclo.

8. **`bash backend/scripts/lint.sh`** → ruff + mypy limpos, 284 arquivos.

## Veredito

```
## QA Gate — Fase 08 [backtest], T-08.4 ciclo 2
- [OK] G3 (ADR-021) — test_bundle_hash_determinism_qa.py::test_g3_... passa; mutação prova não-vácuo
- [OK] Testes existem e passam — `.venv/bin/python -m pytest tests/backtest/ --no-cov -q` (39 testes)
- [OK] Suíte completa — `bash backend/scripts/test.sh` (1476 passed, rc=0)
- [OK] Cobertura 97.64% contra alvo 70% global; por camada domain 99.8%/90%, use_cases 100%/80%, infra 95.2%/70% (ADR-009/D1)
- [OK] core.relative-import / core.silent-except / core.print-statement / core.hardcoded-secret / web-fullstack.* / own.compose-hardcoded-secret — `harness rules --mode sweep --changed-only` rc=0, saída vazia
- [OK] ADR-021 Emenda é append — estrutural (última seção, D3 original intacto) + docs/INDEX.md numstat 2/0
- [OK] DoD ciclo 2: teste vermelho do ciclo 1 verde; casos extra (lista-de-dicts, None) cobertos; suíte cheia sem regressão; emenda append
- [anomalia] git diff --numstat de ADR-021 não se aplica — arquivo nunca commitado antes (sem baseline); verificado estruturalmente em vez disso (item 6 acima)
Regras bloqueantes avaliadas: 8 de 8
Veredito: APPROVED
```

Relatório completo: este arquivo,
`docs/context/plataforma-dados/gates/T-08.4-qa-ciclo2.md`.
