# T-08.8 — QA: `firing_rate` walk-forward OOS (`ADR-023`)

Componente: `charts` (backend Python). Verificação independente do relatório do builder
(`gates/T-08.8-builder.md`), com mentalidade destrutiva: reprodução manual da aritmética,
mutação deliberada dos três mecanismos anti-defeito, e leitura do DoD D8.2 na fonte.

## 1. Reprodução independente da partição (n=23)

Script próprio (não o teste do builder), `partition_folds(Window(0, 30d), calib=7d, eval=1d,
step=1d)`:
```
n_folds= 23
formula floor((30-7-1)/1)+1 = 23
fold0 calib [0, 604800000) eval [604800000, 691200000)
fold22 eval end 2592000000 == window end 2592000000
```
Bate exatamente com `D8.2`/`PRD-001:649`. **OK.**

## 2. Anti-tautologia — mutação deliberada

Editado `compute_walk_forward_firing_rate.py` para recalibrar o percentil sobre `eval_values`
em vez de usar o `AbsoluteSpec` congelado (a exata armadilha de `D8.2`). Resultado:
`test_the_eval_side_is_scored_against_the_frozen_calib_threshold_not_recalibrated` **REPROVOU**
(`0.333... != 0.666...`, exatamente a distinção `1/3` vs `2/3` que a ADR previu). Restaurado e
suíte `tests/charts` voltou a 103/103. **A proteção morde de verdade — não é teste tautológico.**

## 3. Anti-lookahead (`knowledge_time` por fold) — mutação deliberada

Editado para usar `global_knowledge_time_ms = window.end_ms` em vez de `fold.calib_window.end_ms`
por fold. `test_each_fold_reads_calib_and_eval_as_of_its_own_fold_boundary` **REPROVOU**
(gravou `knowledge_time=5` — o fim da janela inteira — em vez de `2`/`3`/`4` por fold).
Restaurado, suíte voltou a verde. **Confirma D-WF2 é pinado por fold, nunca global.**

## 4. Exclusão de fold abaixo de `min_obs`

Lidos os 3 testes de `D-WF4` (`test_a_fold_below_min_obs_calib_is_excluded_and_never_reads_its_eval_side`,
`..._min_obs_eval_is_excluded_after_its_calib_was_read`, `test_every_fold_excluded_refuses...`).
O primeiro prova negativamente que o lado eval do fold excluído **nunca é consultado**
(`eval_calls == []`); o último prova que "todo fold excluído" levanta
`InsufficientWindowForWalkForwardError`, nunca devolve resultado vazio. **OK, sem mutação
necessária — os três casos já cobrem o comportamento negativo que pediria mutação.**

## 5. `compute_firing_rate` (T-08.6) intocado

`git diff --stat` do diff desta task: `firing_rate.py` (MODIFICADO — só `WalkForwardFiringRate`,
`InSampleFiringRate` sem mudança de campo), `test_firing_rate.py` (MODIFICADO), `INDEX.md`, +4
arquivos novos. `compute_firing_rate.py` **não aparece no diff** — confirmado intocado.

## 6. Ausência dos números literais de `D8.2` (1,404%/12,847%/52×) — julgamento de DoD

Li `docs/plans/SPEC-001-plataforma-dados/08_superficie_e_reprodutibilidade.md:27` (a fonte do
DoD, não a alegação do builder): `D8.2`'s "universo esperado" descreve o **produto fim-a-fim**
(a CÉLULA da UI lendo `tautológico` / os números OOS) — e o próprio `ADR-023` (`quant-architect`,
"Consequência") nomeia a legenda de UI como **fora de escopo desta ADR**, gate `ux-ui-mastery`,
tarefa distinta. `T-08.8` é metade backend de um DoD que se completa só com a UI (outra task) +
dado real (infra/`T-01.8`, fora de escopo, não construído ainda).

Investiguei a alegação do `ADR-023` de que existe fixture "BTC/30d" reaproveitável
(`docs/adr/ADR-023...md:43-46`, cita `ADR-020`/`D8.1`) — **falsa por inspeção**:
`backend/tests/helpers/charts_fixtures.py:66-79`, `synthetic_btc_like_population` é uma
população de 101 escalares SEM eixo de tempo (usada para testar `max`), incapaz de produzir 23
folds diários com uma média/máximo específicos. Reproduzir `1,404%/12,847%/52×` exigiria
fabricar uma série de 30 dias construída para bater com uma resposta pré-determinada
(curve-fitting do teste) — o próprio builder nomeou isso, e a inspeção confirma que a premissa
da ADR (fixture já reaproveitável) está errada, não é desculpa do builder.

**Veredito sobre este item: gap ACEITÁVEL para fechar o ESCOPO DE CÓDIGO de `T-08.8`,
NÃO um DoD descumprido por este builder** — mas é um **achado de arquitetura**: `ADR-023`
declara este falsificador "obrigatório...não um número novo a aceitar" sobre uma premissa de
fixture que não existe. Isso é dívida do `ADR-023`, não do builder, e fica **nomeado para o
quant-architect/owner**: ou (a) emendar `ADR-023` reconhecendo que a reprodução literal só é
possível quando o dado real + UI (`T-08.12`/`T-08.13`/`T-01.8`) existirem, ou (b) abrir task
dedicada de verificação end-to-end quando a infra existir. Não bloqueia este gate porque não há
como provar esse "defeito" com um teste limpo sem fabricar dado — fabricar seria o próprio
defeito que este projeto recusa (`CLAUDE.md` "Nenhum número sem o comando").

## Comandos rodados por mim (íntegros, não delegados)

```
bash backend/scripts/lint.sh   -> ruff+ruff format+mypy --strict: limpo, 311 arquivos
bash backend/scripts/test.sh   -> 1579 passed, 398s. domain 99,9%/use_cases 100,0%/infra 95,2%, total 97,81%
.venv/bin/python -m pytest tests/charts -q --cov  -> 103 passed, 100% linha+branch (446 stmts/130 branches, 0 miss)
.venv/bin/lint-imports --config pyproject.toml -> Contracts: 7 kept, 0 broken
harness rules --mode sweep --changed-only -> rc=0, 1 [AVISO] core.module-docstring-single-line (pré-existente), 0 [BLOQUEIO]
```

## Regras bloqueantes (`harness rules list --severity block`, 8 regras)

- `core.relative-import`: nenhum import relativo em `walk_forward.py`/`compute_walk_forward_firing_rate.py` — só `from src.modules...` absoluto. OK.
- `core.silent-except`: zero `except` nos arquivos novos/modificados (`grep -n "except" backend/src/modules/charts/domain/walk_forward.py backend/src/modules/charts/use_cases/compute_walk_forward_firing_rate.py backend/src/modules/charts/domain/firing_rate.py` → 0 ocorrências). OK.
- `core.print-statement`: 0 `print(` nos 3 arquivos. OK.
- `core.hardcoded-secret`, `own.compose-hardcoded-secret`: sem string de credencial nos arquivos (domínio puro, sem I/O). OK.
- `web-fullstack.*` (3 regras): fora de escopo — `charts` backend não é `frontend`/tenant/servidor sem testes. N/A.
- Confirmado agregado por `harness rules --mode sweep --changed-only` acima: 0 bloqueio, 8/8 regras avaliadas.

## Veredito

**APPROVED.**
