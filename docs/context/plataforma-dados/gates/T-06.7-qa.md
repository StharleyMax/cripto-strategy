# QA Gate — `T-06.7` (`CST-51`, componente `sentimento`)

**Fase:** 06 — Semântica declarada. **Item:** 6.13 (`ZL-1..ZL-3`). **PR:** #107
(`tasks/T-06.7-fase06` → `master`, `MERGEABLE`). **Worktree:**
`/tmp/claude-1002/wt/T-06.7`.

## Pré-condição — branch atualizada

`git fetch origin` + `git merge-base --is-ancestor origin/master HEAD` → confirma **UP TO DATE**
(`origin/master` em `0ff5339`, mesmo commit-base da worktree). `T-06.8` (PR #106) não interfere:
não mesclada, sem overlap de arquivo. Nenhum merge necessário.

## Testes existentes rodados antes de qualquer teste novo

- `bash backend/scripts/test.sh --no-cov -k liquidation_zero_legitimacy` → **18 passed**, 0.58s
  (reproduz exatamente o número relatado pelo builder).
- `bash backend/scripts/test.sh` (universo completo, 1425 testes coletados) → **1425 passed**, 3
  warnings, 295.01s. `Total coverage: 97.73%`. Por camada (`ADR-009/D1`): domain **99.8%**
  (2848/2853, meta 90%), use_cases **100.0%** (585/585, meta 80%), infra **95.1%** (2208/2321,
  meta 70%) — 3/3 camadas medidas, todas acima do piso.
- Cobertura isolada do arquivo (via `coverage.xml`, `ElementTree` sobre `<class
  filename=".../liquidation_zero_legitimacy.py">`): `line-rate=1`, `branch-rate=1` → **100%**,
  confirma os 66/66 stmts, 18/18 branches relatados pelo builder.

Nenhum teste novo foi criado por este QA — a suíte do builder já cobre os falsificadores exigidos
com folga; a validação abaixo é por MUTAÇÃO REAL sobre o código de produção existente, não por
teste adicional.

## Mutação real — planta, confirma pega, reverte, confirma `git diff` limpo

### Mutação 1 — ZL-2, o falsificador central

Em `classify_side_points`, o ramo `else` (zero ANTES do primeiro não-zero do lado) foi trocado de
`ClassifiedSidePoint(value=None, absence=Absence.NO_SOURCE)` para
`ClassifiedSidePoint(value=Decimal(0), absence=None)` — ou seja, tratar todo zero líder como zero
legítimo (o defeito que `D6.10` mediu em produção).

Comando: `bash backend/scripts/test.sh --no-cov -k liquidation_zero_legitimacy` com a mutação
aplicada → **5 failed, 13 passed**:
- `test_zl2_leading_zeros_before_first_nonzero_become_no_source`
- `test_zl2_no_source_points_carry_no_value`
- `test_zl1_retention_window_is_per_side_not_summed`
- `test_zl1_no_source_points_never_count_toward_the_window`
- `test_zl1_reproduces_d6_10_pattern_at_fixture_scale` (o caso `D6.10` medido: `assert 0 == 20`
  no número de pontos `NO_SOURCE` — a mutação faz o mecanismo relatar ZERO buckets suprimidos onde
  deveriam ser 20/361)

`git checkout -- backend/src/.../liquidation_zero_legitimacy.py` → revertido. `git diff HEAD --
backend/src/.../liquidation_zero_legitimacy.py | wc -l` → **0** (confirmado limpo).

### Mutação 2 — ZL-1, "por lado" via contagem bruta

Em `retention_window_per_side`, `n_points = sum(1 for p in classified_points if p.absence is
None)` trocado por `n_points = len(classified_points)` (conta buckets `NO_SOURCE` como retidos —
reintroduz o otimismo que `ZL-2` existe para remover).

Comando: mesmo `-k liquidation_zero_legitimacy` com a mutação aplicada → **3 failed, 15 passed**:
- `test_zl1_retention_window_is_per_side_not_summed`
- `test_zl1_no_source_points_never_count_toward_the_window` (`assert 3 == 0`)
- `test_zl1_reproduces_d6_10_pattern_at_fixture_scale` (`assert 23 == 3` — os 20 `NO_SOURCE`
  líderes contados como retenção, inflando a janela de 3 para 23 buckets)

`git checkout --` → revertido. `git diff HEAD --stat` → vazio, **confirmado limpo**.

Não foi necessária uma terceira mutação para "somar os dois lados antes de multiplicar" (o defeito
ZL-1 mais literal) porque o mecanismo real não expõe função alguma que aceite os dois lados juntos
— `retention_window_per_side` só recebe `Sequence[ClassifiedSidePoint]` de UM lado já
classificado; não há ponto de mutação de uma linha que produza esse defeito sem reescrever a
assinatura da função. Isso é, em si, evidência do DoD (ver ZL-1 abaixo): "por lado, não por série
inteira" é propriedade do grafo de chamada, não de uma checagem interna — não há como um chamador
montar um `n_points` que misture os lados.

## DoD, item a item

- **[OK] ZL-1** — `pontos × intervalo` é POR LADO. Evidência: `retention_window_per_side` recebe
  `Sequence[ClassifiedSidePoint]` de um único lado (assinatura,
  `liquidation_zero_legitimacy.py:214-218`); nenhuma função no módulo aceita dois lados juntos.
  `test_zl1_retention_window_is_per_side_not_summed` (linha 155) afirma que `long_window` e
  `short_window` são diferentes entre si E diferentes da janela "série somada" — mutação 2 acima
  prova que a contagem correta (só `absence is None`) é o que garante isso, não o teste sozinho.
- **[OK] ZL-2** — zero-antes-do-primeiro-não-zero-do-lado → `Absence.NO_SOURCE`. Evidência:
  `classify_side_points`, ramo `else` (linha ~195-200); mutação 1 acima planta o defeito exato
  (colapsar em zero legítimo) e 5 testes pegam, incluindo o caso `D6.10`.
- **[OK] ZL-3** — zero legítimo pós-vida é tipo distinto de ausência. Evidência:
  `ClassifiedSidePoint` é `@dataclass(frozen=True)` com `value: Decimal | None` XOR `absence:
  Absence | None`, `__post_init__` recusa as 3 combinações inválidas
  (`InvalidClassifiedSidePointError`, testado em 3 testes dedicados,
  `test_classified_side_point_refuses_*`). `test_zl3_legitimate_zero_is_never_equal_to_a_no_source_point`
  confirma que `ClassifiedSidePoint(value=Decimal("0"), absence=None) !=
  ClassifiedSidePoint(value=None, absence=Absence.NO_SOURCE)` — são estruturalmente diferentes por
  igualdade de dataclass, não por convenção de valor. Confirmado por leitura direta do tipo, sem
  necessidade de mutação (a garantia é de construção, `__post_init__`, já coberta pelos 3 testes
  de recusa).
- **[OK] D6.10** — padrão medido (361 buckets `s=0` líder, `daily` real não-zero) reproduzido em
  escala de fixture: `test_zl1_reproduces_d6_10_pattern_at_fixture_scale` (20 zeros líderes, depois
  `289.65`/`0`/`154.53`, citando os 3 valores medidos em `D6.10`) — reproduzido e confirmado pego
  pelas duas mutações acima (mutação 1: `assert 0 == 20`; mutação 2: `assert 23 == 3`).
- **[OK] Reuso de `Absence.NO_SOURCE`** — `from src.modules.sentimento.domain.provenance import
  Absence` (linha 50); `__post_init__` recusa qualquer `Absence` diferente de `NO_SOURCE`
  (`test_classified_side_point_refuses_an_absence_other_than_no_source`, usa
  `Absence.NO_POINT` como prova de recusa). Nenhum enum paralelo de ausência criado —
  `LiquidationSide` é um enum diferente (lados `l`/`s` do fio), não um vocabulário de ausência.
- **[OK] Contrato `Natureza` (`ADR-016`)** — `bash backend/scripts/natureza.sh` → universo 79
  arquivos (`domain`+`use_cases`), **0 leitura(s) de relogio**. `bash backend/scripts/boundaries.sh`
  → `Contracts: 3 kept, 0 broken` (inclui "domain e use_cases nao falam com socket nem ssl").
  Imports do módulo: só `collections.abc`, `dataclasses`, `decimal`, `enum`,
  `provenance.Absence` — nenhum `socket`/`ssl`/`time`/`datetime`.
- **[OK] Idioma inglês** — produção e teste em inglês (identificadores, docstrings, mensagens de
  `raise`). `grep` não encontrou import relativo, `print(`, nem `except:` silencioso nos dois
  arquivos.

## Regras bloqueantes (`harness rules list --severity block`, 7 listadas)

- **[OK] core.relative-import** — `grep -nE 'import\s+\.'` nos dois arquivos → 0 ocorrências.
- **[OK] core.silent-except** — único `except` do módulo (`liquidation_zero_legitimacy.py:143`)
  relança com `raise ... from failure`, nunca descarta.
- **[OK] core.print-statement** — `grep -n "print("` → 0 ocorrências.
- **[OK] core.hardcoded-secret** — módulo `domain` sem I/O, sem string de credencial/conexão.
- **[OK] web-fullstack.browser-imports-server** — não aplicável (`sentimento`, sem `frontend`).
- **[OK] web-fullstack.tenant-from-request** — não aplicável (sem HTTP request neste módulo).
- **[OK] web-fullstack.server-test-directory-present** — não aplicável (regra é sobre AUSÊNCIA de
  diretório de testes do servidor; `backend/tests/` existe e cresce nesta task).
- Confirmação agregada: `harness rules --mode sweep --changed-only` → saída vazia, `EXIT=0`.

Regras bloqueantes avaliadas: 7 de 7.

## Restrições do gate respeitadas

Nenhum arquivo de produção foi alterado permanentemente — as duas mutações foram plantadas,
confirmadas e revertidas na mesma sessão (`git diff HEAD --stat` vazio ao final). Nenhum teste
novo foi adicionado (a suíte do builder já cobre os falsificadores exigidos). Nenhum `advance`
nem `approve` executado.

## Veredito

**APPROVED.** DoD completo, os dois falsificadores centrais (`ZL-1`/`ZL-2`) comprovados por
mutação real com reversão limpa, `ZL-3` comprovado por construção de tipo + testes de recusa,
`D6.10` reproduzido em escala de fixture e pego pelas duas mutações, todas as 7 regras bloqueantes
com evidência citável, cobertura 97.73% total / domain 99.8% muito acima do piso 90%.
