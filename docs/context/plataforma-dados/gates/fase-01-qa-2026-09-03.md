# QA Gate — Fase 01 · `T-01.8` + `T-01.9` + `T-01.10`

**Data:** 2026-09-03 · **Branch:** `tasks/T-01.8-juizes-e-deploy`
**Veredito: `NEEDS_FIX`**

---

## ⚠️ ANOMALIA DE PRIMEIRA ORDEM — o objeto sob QA mudou TRÊS vezes durante o gate

O mandato nomeou `53801a9` (`T-01.8`) e `00cf499` (`T-01.9`+`T-01.10`). **`00cf499` não existe mais
na história.** Medido, com relógio:

| hora | `git rev-parse --short HEAD` | evento |
|---|---|---|
| 21:35 (abertura do gate) | `00cf499` | `git status --porcelain` → só `?? T-01.9-review.md` |
| 21:37 | `00cf499` + ` M harness.toml` (52+/6−) | árvore suja aparecendo **sob mim**, mtime `21:37:39` |
| 21:40 | `02149f4` | história REESCRITA; `d7e528c` (`chore(tasks): T-09.4`, **fase 09**) intercalado |
| 21:46 | `e80645e` | `fix(infra): T-01.10 rodada 2 - allowlist ancorada ao valor, nao a linha` |

```
$ git log --oneline -4
e80645e fix(infra): T-01.10 rodada 2 - allowlist ancorada ao valor, nao a linha
02149f4 feat(docs): T-01.9 e T-01.10 — a atribuicao vira portao e deploy/ sai da cegueira
d7e528c chore(tasks): T-09.4 em done — fase 09 resolvida com o desfecho real de cada task
53801a9 feat(docs): T-01.8 — A6: web ganha juiz de engenharia e infra ganha o seu (CST-100)
```

`ps -eo pid,etimes,cmd` durante o gate mostrou **outra sessão** rodando
`harness validate --strict; make lint-agents; harness doctor` nesta mesma árvore
(pids 2525637 / 2664248). **Não é `NEEDS_FIX` de regra e não é `OK`: é resposta desconhecida.**
Um veredito sobre `00cf499` é inverificável — o commit foi descartado. Tudo abaixo está
etiquetado com o commit em que foi medido.

---

## Eixo 1 — `T-01.9`: as 5 mutações refeitas, **mais uma que o builder não fez**

Bancada **fora da árvore**, cópia de `git show HEAD:harness.toml` em
`…/scratchpad/bench`; o **dourado resolve pelo diretório do script** (`AQUI="$(dirname "$0")"`),
então a bancada roda o **script real do repositório** contra uma **política mutada**.
Árvore real: `git status --porcelain` → **vazio** antes e depois.

`[MEDIDO 2026-09-03, HEAD `e80645e`; script `scripts/run_mut.sh` + `scripts/mutate.py` no scratchpad]`

| # | mutação | `harness policy` | `harness validate --strict` | `check-agents-by-component.sh` |
|---|---|---|---|---|
| baseline | nenhuma | `rc=0` | — | **`rc=0`** — *"bate com o dourado — 6 componente(s), 10 papel(is)"* |
| M1 | `design_gate` apagado das 2 entradas | `rc=0` | **`rc=0`, silêncio** | **`rc=1`**, 2 pontos: `charts.design_gate`, `web.design_gate` — PAPEL AUSENTE |
| M2 | conteúdo TROCADO `charts`↔`web` | `rc=0` | **`rc=0`, silêncio** | **`rc=1`**, 6 pontos; 1º: `charts.architect — esperado 'quant-architect.md', obtido 'frontend-architect.md'` |
| M3 | `[agents.by_component.charts]` esvaziada | `rc=0` | **`rc=0`, silêncio** | **`rc=1`**, 2 pontos: `architect` + `design_gate` PAPEL AUSENTE |
| M4 | `docs` ganha juiz (componente a mais) | `rc=0` | **`rc=0`, silêncio** | **`rc=1`**, 1 ponto: `agents.by_component.docs` — COMPONENTE INESPERADO |
| M5 | secção inteira apagada | `rc=0` | **`rc=0`, silêncio** | **`rc=1`**, 6 pontos: os 6 componentes — COMPONENTE AUSENTE |
| **M6** | **SÓ O VALOR de `infra.architect`; a CHAVE fica** | `rc=0` | **`rc=0`, silêncio** | **`rc=1`**, 1 ponto: `agents.by_component.infra.architect — esperado '.claude/agents/infra-architect.md', obtido '.claude/agents/quant-architect.md'` |

**M6 é a mutação que o builder NÃO fez, e é a classe exata do defeito de `D1.2`** (*"reprova se a
asserção só cobrir presença de chave"*). Ela **morde**, nomeando o caminho de chave completo e os
**dois** valores. O comparador tem o ramo explícito
(`scripts/check_agents_by_component.py`, `diff()`: `elif want[role] != got[role]`), e ele é
exercitado, não apenas escrito.

**6 de 6 mordem. 6 de 6 são SILENCIOSAS para `harness validate --strict`** ⇒ o portão novo é o
único instrumento que mede esta propriedade. `D1.12` **satisfeito**.

## Eixo 2 — `T-01.10`: `corpus mutate` é a prova, e ela foi reproduzida

**(a) A referência NÃO é a regex da regra.** `corpus/reference/compose_secret_reference.sh` decide
por `awk` separando **chave e valor por campo** (`substr`/`index`, 4 funções), sem `^`, sem
alternância e sem classe negada. Grep pelos fragmentos da regex da política e por
`allowlist_regex` dentro da referência → **0 ocorrências**. A concordância **não** é por
construção.

**(b) `verify` VERDE não prova nada; `mutate` prova.** Reproduzido em cópia do corpus com o
violador de caminho de teste (`03_api_key_in_test_path.yml`) removido do manifesto e do disco:

```
$ harness corpus verify --corpus <copia-sem-violador> --reference corpus/reference/compose_secret_reference.sh
verify rc=0
  violadores em caminho de teste: 0
  corpus dourado VERDE: veredito identico, arquivo a arquivo

$ harness corpus mutate --corpus <copia-sem-violador> --reference corpus/reference/compose_secret_reference.sh
mutate rc=1
  MUTANTES SOBREVIVENTES (o corpus nao defende estas regras):
    - own.compose-hardcoded-secret (policy/harness.toml): estreitar o escopo passou VERDE — sem caso em caminho de teste
  [SOBREVIVEU] own.compose-hardcoded-secret
```

**Corpus real, HEAD `e80645e`:** `verify` **`rc=0`** (9 casos, 1 regra, 0 divergentes) e
`mutate` **`rc=0`** — *"1/1 mutantes mortos"*, `[morto] own.compose-hardcoded-secret`.
`D1.14b` **satisfeito em `e80645e`** — e **NÃO** em `02149f4` (ver o defeito 2, abaixo).

**(c) `D1.14` morde/cala**, violador **efêmero**, criado e apagado no mesmo script:

```
morde: harness rules --mode file --path deploy/compose.qa-efemero.yml
       -> {"decision": "block", "reason": "[BLOQUEIO] [own.compose-hardcoded-secret]
           deploy/compose.qa-efemero.yml:5: segredo literal em arquivo de compose..."}   rc=2
cala : harness rules --mode file --path deploy/compose.yml   -> stdout vazio, rc=0
```

Nenhum `classify` foi usado como critério — o plano `01` (linha 184) reprova isso explicitamente.

## Eixo 3 — o violador não está versionado

```
$ find deploy -type f
deploy/compose.yml
$ find deploy -type f | wc -l
1
$ git status --porcelain          # em e80645e, depois do gate
(vazio)
```

**OK.** Os dois violadores efêmeros que eu criei foram apagados por `trap` e por `rm` explícito;
`git status` fecha vazio.

---

## Os defeitos que sobrevivem em `e80645e`

### DEFEITO 1 — `harness corpus verify`/`mutate` não estão em portão nenhum

```
$ for f in Makefile scripts/verify.sh .git/hooks/pre-push scripts/hooks/pre-push.pre-harness; do
      printf '%-40s %s\n' "$f" "$(grep -c corpus "$f")"; done
Makefile                                 0
scripts/verify.sh                        0
.git/hooks/pre-push                      0
scripts/hooks/pre-push.pre-harness       0
```

`D1.14b` exige *"enforcement **MEDIDO**, não declarado"*. A medição existe — e é **manual, de uma
vez só**. Nada a repete.

### DEFEITO 2 — a prova de que o DEFEITO 1 não é hipótese: aconteceu, medido, num commit real

A regra `own.compose-hardcoded-secret` é declarada **DUAS vezes** — `harness.toml` e
`corpus/policy/harness.toml` — e **nenhum portão mantém as duas iguais**. No commit `02149f4`
elas divergiram: raiz com a allowlist **ancorada**, fixture do corpus com a **antiga**.
Resultado, no MESMO commit:

```
$ git rev-parse --short HEAD
02149f4
$ harness corpus verify --corpus corpus --reference corpus/reference/compose_secret_reference.sh
verify rc=1
  [DIFERE] violating deploy/compose.decoy-dollar.yml   referencia: [own.compose-hardcoded-secret]  runner: -
  [DIFERE] violating deploy/compose.decoy-comment.yml  referencia: [own.compose-hardcoded-secret]  runner: -
  EXPECTATIVA DECLARADA NAO CUMPRIDA (passe degenerado):
    - 04_dollar_decoy_suffix.yml: a regra own.compose-hardcoded-secret nao disparou no violador
    - 05_allowlist_word_in_comment.yml: a regra own.compose-hardcoded-secret nao disparou no violador

$ make verify
veredito: VERDE — 6 portões mediram e passaram
```

**`make verify` VERDE sobre um corpus VERMELHO, no mesmo commit.** É o modo de falha que
`ADR-012` nomeia para o `rc=0`: sinal indistinguível entre *"nada quebrou"* e *"o instrumento
nunca foi consultado"*. A janela durou ~6 minutos porque uma sessão concorrente consertou; se o
push tivesse saído em `02149f4`, o `pre-push` teria deixado passar.

### DEFEITO 3 — a allowlist ancorada não estava no commit que me foi dado para aprovar

Em `00cf499` (o commit do mandato), a regra **escapava**, medido em bancada com
`git show 00cf499:harness.toml`:

```
POSTGRES_PASSWORD: hunter2_prod_master${UNUSED}        -> rc=0, stdout vazio   (ESCAPOU)
POSTGRES_PASSWORD: hunter2_prod_master  # changeme     -> rc=0, stdout vazio   (ESCAPOU)
```

Em `e80645e` os dois dão **`rc=2` BLOQUEIO**. O conserto é real e os casos `04`/`05` do corpus o
defendem — mas ele **não é** o commit que o mandato mandou julgar.

---

## Lacunas já declaradas — confirmadas como declaradas, NÃO tratadas como defeito novo

- **`scripts/verify.sh` não chama `lint-agents`** — ato do owner, declarado em
  `docs/gate-de-design.md:220`. `grep -c lint-agents scripts/verify.sh` → **0**. Continua declarada.
- **`[NÃO MEDIDO]` do `Dockerfile` em `include_globs`** — `harness.toml:442-448`, escrito em vez
  de suposto. Continua declarado. (Há um terceiro `[NÃO MEDIDO]` irmão em `:692`, YAML flow-style.)

## Checagens de estrutura

```
$ harness rules list | tail -1
total: 11 regra(s) em vigor          # 8 block + 3 aviso; own.compose-hardcoded-secret é a 1ª própria viva
$ grep -c "'''" harness.toml
0                                     # (também 0 em HEAD:harness.toml)
$ harness policy --key components > /dev/null; echo $?
0                                     # não sai rc=3
$ make lint-agents ; echo rc=$?
agents.by_component: bate com o dourado — 6 componente(s), 10 papel(is).
rc=0
$ make lint ; echo rc=$?
rc=0                                  # lint-agents é o 1º dos 3 (Makefile:116)
$ make verify
[OK] lint-backend rc=0  256 source files
[OK] lint-frontend rc=0
[OK] test         rc=0  1434 passed · Total coverage: 97.74%
[OK] boundaries   rc=0  3 kept, 0 broken
[OK] regras       rc=0  0 bloqueio(s), 16 aviso(s)
[OK] política     rc=0
veredito: VERDE — 6 portões mediram e passaram
```

`T-01.8`: os 6 ponteiros de `agents.by_component` existem em disco
(`infra-architect.md`, `frontend-architect.md`, `frontend-builder.md`, `frontend-qa.md`,
`quant-architect.md`, `docs/gate-de-design.md`) — nenhum AVISO de ponteiro que não resolve.

## Regras bloqueantes — 8 de 8 avaliadas

`make verify` → **`[OK] regras rc=0 · 0 bloqueio(s), 16 aviso(s)`** sobre a árvore inteira em
`e80645e`. Cobre `core.relative-import`, `core.silent-except`, `core.print-statement`,
`core.hardcoded-secret`, `web-fullstack.browser-imports-server`,
`web-fullstack.tenant-from-request`, `web-fullstack.server-test-directory-present` e
`own.compose-hardcoded-secret` (esta última também medida à parte, morde/cala, acima).

---

## Veredito

```
## QA Gate — Fase 01 [docs + infra]
- [OK]   own.compose-hardcoded-secret — morde violador efêmero rc=2, cala deploy/compose.yml rc=0 (e80645e)
- [OK]   8/8 regras bloqueantes — make verify: "regras rc=0, 0 bloqueio(s), 16 aviso(s)"
- [OK]   Testes existem e passam — make verify: 1434 passed
- [OK]   Cobertura 97.74% (alvo do repositório: sem alvo declarado para docs/infra; backend ≥ 90% [DOC])
- [OK]   D1.12 — 6/6 mutações mordem, incl. a de VALOR-SÓ; validate --strict silencioso em 6/6
- [OK]   D1.14 — morde/cala por rules --mode file, sem classify; find deploy -type f = 1; git status vazio
- [OK]   D1.14b — corpus verify rc=0 (9 casos) e mutate rc=0 (1/1 morto) EM e80645e
- [FAIL] D1.14b como enforcement REPETÍVEL — corpus verify/mutate em 0 portões (grep -c corpus = 0 em 4 arquivos)
- [FAIL] regra duplicada sem portão de igualdade — em 02149f4: corpus verify rc=1 com make verify VERDE
- [anomalia] o objeto sob QA foi reescrito 3x durante o gate; 00cf499 não existe mais
Regras bloqueantes avaliadas: 8 de 8 listadas por `harness rules list --severity block`
Veredito: NEEDS_FIX
```

### Ações

1. **Cabear `harness corpus verify` + `harness corpus mutate` num alvo de `make`** (ex.
   `make lint-corpus`), pré-requisito de `make lint` ou entrada de `scripts/verify.sh`. É o que
   o item `1.8` quer dizer com *"enforcement MEDIDO, não declarado"*, e o `02149f4` é a prova
   medida de que sem isso a duplicação da regra volta a divergir em silêncio.
2. **Reemitir o mandato de QA sobre os commits que existem** (`53801a9` … `e80645e`) — ou, se a
   intenção é aprovar `00cf499`, esse commit precisa voltar à história. QA não certifica objeto
   descartado.
3. **Explicar `d7e528c`** (`chore(tasks): T-09.4`, fase 09) intercalado numa branch de fase 01 —
   ou destacá-lo. Não julguei o mérito; ele está fora do escopo deste gate e dentro do seu range.
4. Depois de 1–3, este gate reabre sobre a nova ponta e as medições acima são re-executáveis a
   partir dos scripts citados.
