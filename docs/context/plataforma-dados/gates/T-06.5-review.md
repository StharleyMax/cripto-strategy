# Review Gate — `T-06.5` (`CST-49`, componente `sentimento`, fase `06`, plano item 6.11)

## Escopo auditado

PR #99, branch `tasks/T-06.5-fase06`, worktree `/tmp/claude-1002/wt/T-06.5`.
`git diff master...HEAD --stat`: `backend/README.md` (+70), `backend/src/modules/sentimento/domain/open_interest_catalog.py`
(**novo**, 150 linhas), `backend/tests/sentimento/test_open_interest_catalog.py` (**novo**, 136 linhas),
`docs/INDEX.md` (+1), `docs/context/plataforma-dados/gates/T-06.5-builder.md` (+86). 5 arquivos no diff,
2 de código.

Bootstrap lido: `harness rules list --severity block` (7 regras), `harness policy --key
docs.bootstrap_docs`, `harness policy --key glossary_doc` (vazio, dívida conhecida `ADR-013/D4`,
não aplicável a esta task), `harness policy --key code_paths`. Handoff completo
(`docs/context/plataforma-dados/handoff/T-06.5.md`) lido antes da auditoria. QA gate
(`docs/context/plataforma-dados/gates/T-06.5-qa.md`, mutações reais em 4 rodadas, todas confirmando
falha e revertendo limpo) lido e conferido por reprodução independente dos pontos abaixo.

## Verificação ponto a ponto

1. **Contrato `Natureza` (`ADR-016`)** — `open_interest_catalog.py` só importa de
   `series_catalog.py`/`series_key.py` (ambos `domain`). `grep -nE
   "open\(|requests|socket|os\.|datetime\.now|time\.time|Path\("` no arquivo → **vazio** (`rc=1`,
   nenhuma linha). Nenhum I/O, nenhuma leitura de relógio. `[OK]`.
2. **Enum `Reduction` (`series_key.py`) intocado** — `git diff master...HEAD --
   backend/src/modules/sentimento/domain/series_key.py` → **vazio**, arquivo nem aparece no
   `--stat` do diff. Leitura direta de `series_key.py:95-112`: 8 membros pré-existentes
   (`POINT/OPEN/HIGH/LOW/CLOSE/SUM/MEAN/LAST`); `open_interest_catalog.py` usa só
   `OPEN/HIGH/LOW/CLOSE` (Coinalyze) e `POINT` (Binance) — nenhum membro novo criado. `[OK]`.
3. **Reuso de `series_catalog.py`** — mesmo motivo do item 2: arquivo fora do diff.
   `open_interest_catalog.py` importa `SeriesCatalog`, `SeriesCatalogEntry`,
   `build_series_catalog` de `T-06.1` sem redefinir nada; `open_interest_catalog_entries()`
   constrói a lista de 5 `SeriesCatalogEntry` e a passa por `build_series_catalog`, então a
   invariante "uma linha por `SeriesKey`" (`SPEC-001` §3.3) é validada pelo tipo existente, não
   por uma segunda representação de catálogo. `[OK]`.
4. **5 `SeriesKey` distintas, `reduction` correto** —
   `coinalyze_open_interest_key(reduction, ...)` sem default (parâmetro posicional obrigatório;
   `D6.7` — chamar sem argumento é `TypeError`, testado em
   `test_asking_for_the_coinalyze_open_interest_key_without_reduction_is_refused`); 4 chamadas
   com `OPEN/HIGH/LOW/CLOSE`, `ts_convention=OHLC_OVER_BUCKET`; `binance_open_interest_key()`
   fixo em `Reduction.POINT`/`POINT_AT_BUCKET_END`. Teste
   `test_the_catalog_has_five_rows_four_coinalyze_ohlc_and_one_binance_point` confere 5 entries,
   4 `coinalyze`+1 `binance`, conjunto de `reduction` == os 5 membros esperados. `[OK]`.
5. **Idioma inglês em produção e teste** — identificadores, docstrings e comentários novos são
   em inglês. Único achado de português são citações literais entre aspas de `SPEC-001` §2.1
   (`"o label_shift da Coinalyze é +interval ... e não zero"`, linhas 21 e 56 do módulo novo) —
   mesmo padrão já usado em `series_key.py`/`series_catalog.py` para citar SPEC/ADR
   verbatim dentro de comentário em inglês; não é prosa nova em português, é citação de fonte.
   Nenhuma mensagem de `raise`/`Error`/`Exception` no diff (o único `raise` do fluxo é o
   `TypeError` nativo do Python por falta de argumento). `[OK]`.
6. **`docs/INDEX.md`/`backend/README.md` append-only** — `git diff master...HEAD -- docs/INDEX.md
   backend/README.md`: só linhas `+`, nenhuma `-`; `docs/INDEX.md` ganha 1 linha nova no fim da
   tabela, `backend/README.md` ganha uma seção `## 📎 2026-09-03 por T-06.5` ao final do
   arquivo, nenhuma seção anterior tocada. `[OK]`.
7. **Regras bloqueantes** (`harness rules list --severity block`, 7 no total) —
   `harness rules --mode file --path <cada um dos 2 arquivos novos>` → saída vazia, `rc=0` nos
   dois. Confere com o item já auditado pelo QA regra-a-regra
   (`core.relative-import`/`core.silent-except`/`core.print-statement`/`core.hardcoded-secret`
   `[OK]`; as 3 regras `web-fullstack.*` `[N/A]`, sem código de navegador/rota no diff).

## Denominador

- Regras bloqueantes existentes: **7** (`harness rules list --severity block`).
- Regras avaliadas nos 2 arquivos de código do diff: **7/7**, via `harness rules --mode file
  --path <arquivo>` em cada um — ambas as saídas vazias, `rc=0`.
- Arquivos varridos: **2 de código** (`open_interest_catalog.py`,
  `test_open_interest_catalog.py`) via `harness rules --mode file`; **5 no total** do diff
  (`git diff master...HEAD --stat`), os outros 3 sendo doc/gate (fora do universo `code_paths`).
- Pontos de arquitetura declarada verificados por citação direta: **6/6** (lista do despacho).

## Veredito

**COMPLIANT** — nenhuma das 7 regras bloqueantes violada, nenhum ponto de arquitetura declarada
(`ADR-016`, enum `Reduction`, reuso de `series_catalog`, 5 `SeriesKey`, idioma, append-only)
violado. Nenhum achado de severidade WARNING ou INFO com citação de documento restou pendente —
o único candidato (citação PT dentro de comentário EN) tem precedente já estabelecido em código
mesclado (`series_key.py`) e não é prosa nova.
