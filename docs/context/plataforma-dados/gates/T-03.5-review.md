[REVIEW GATE — Fase 03: Captura contínua]
Feature: plataforma-dados   Componente: sentimento   Task: T-03.5 (CST-21) · PR #58 (aberto,
`tasks/T-03.5-premiumindex`, base master@634833b) · worktree /tmp/claude-1002/wt/T-03.5

Veredito: **COMPLIANT**

Denominador: 7 regras bloqueantes em vigor (`harness rules list --severity block`), 5 arquivos
de produção do diff avaliados (`domain/premium_index_batch.py`,
`use_cases/collect_premium_index.py`, `infra/premium_index_http_client.py`,
`infra/premium_index_jsonl_sink.py`, `infra/premium_index_probe_cli.py`) + 4 arquivos de teste.

Checagens (comando → resultado):
- `harness rules --mode sweep --changed-only --format ndjson` → vazio, rc=0 (nenhuma regra
  bloqueante violada).
- `bash backend/scripts/boundaries.sh` → "Contracts: 3 kept, 0 broken" (77 arquivos, 279
  dependências) — inclui o contrato "Natureza: domain e use_cases nao falam com socket nem
  ssl" (`pyproject.toml:264-271`): KEPT. `domain/` e `use_cases/` desta task não tocam
  socket/ssl; só `infra/premium_index_http_client.py` fala com transporte.
- `bash backend/scripts/check-coverage-layers.sh` → domain 100,0% (meta 90) · use_cases
  100,0% (meta 80) · infra 98,4% (meta 70) — 3/3 camadas acima do piso.
- `grep -noE` por identificador com diacrítico/partícula PT nos 5 arquivos de produção novos →
  vazio. Idioma de identificador (linhas 1-4 da tabela do CLAUDE.md): conforme.
- `grep -nE '(from \.|import \.)'` → vazio (sem import relativo, `core.relative-import`).
  `grep -n "print("` → vazio. `except OSError` do cliente HTTP não descarta — retorna
  `RawPremiumIndexFetch(transport_error=...)` (sem `core.silent-except`).

Duplicação de cliente HTTP vs. T-02.1: **não verificável hoje, e por motivo estrutural, não
por omissão** — `T-02.1` (CST-12, "exchangeInfo + fundingInfo, premiumIndex como segunda
testemunha") está `status = "todo"` em `docs/context/plataforma-dados/tasks.toml:288`; nenhum
arquivo desse escopo existe no repositório (`grep -rln premiumIndex backend/src` só acha os
5 arquivos desta própria task). Não há "segundo cliente" para comparar. O que existe é
`infra/premium_index_http_client.py` reusando deliberadamente `ConnectionFactory`,
`HttpConnection`, `open_https_connection`, `flatten_headers` de
`infra/https_quota_probe.py` (citado no próprio docstring do arquivo, linhas 3-10) em vez de
redeclarar o Protocol de `http.client` — a mesma disciplina que o contrato "Natureza" existe
para proteger. Registro para o builder de `T-02.1`: reusar esse mesmo módulo compartilhado
para a chamada de `premiumIndex` ali, não reabrir `http.client` uma terceira vez — isto é
observação arquitetural (sem regra id, sem documento normativo que já a fixe), não entra
como violação no veredito.

Comando: `harness gate-record plataforma-dados 03 REVIEW COMPLIANT`
