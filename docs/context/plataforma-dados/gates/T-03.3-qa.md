# `T-03.3` (`CST-19`) — relatório de gate do `/qa`

**Feature:** `plataforma-dados` · **Componente:** `sentimento` · **Fase:** `03`
**Base:** `master@07193e6` · **Branch:** `tasks/T-03.3-reconexao-por-classe` · **PR:** #64
**Worktree:** `/tmp/claude-1002/wt/T-03.3`

## Veredito

**NEEDS_FIX.** B1 (sobreposição obrigatória) e B3 (direção do viés, subcontagem-nunca-supercontagem)
estão **provados por mutação** — sobrevivi às duas tentativas de quebrar o invariante e a suíte
reprovou nos dois casos. B2 (chave natural declarada) está **corretamente declarada em código**
(`(symbol, side, price, orig_qty, trade_time)`, igual ao literal de `ADR-004`), mas **2 dos 5 campos
declarados não têm nenhum teste que prove que eles discriminam a chave**: mutei `side` e
`trade_time` para constantes, isoladamente, e a suíte inteira (26 testes) passou sem nenhuma
falha nas duas rodadas. Isso é exatamente o padrão que o próprio módulo declara defender contra
("a chave é declarada, não presumida") — mas a prova de que os 5 campos *realmente* entram na
igualdade/desigualdade da chave cobre só 3 deles (`symbol`, `price`, `orig_qty`, e estes últimos
dois só combinados, nunca isolados).

## 1. Testes existentes rodados antes de qualquer teste novo

```
bash backend/scripts/test.sh
```
→ **897 passed**, 3 warnings, 418.57s. Cobertura total **98,06%**; por camada (`ADR-009/D1`):
`[OK] domain 99,9%` (meta 90%) · `[OK] use_cases 100,0%` (meta 80%) · `[OK] infra 95,8%` (meta 70%)
— universo 3 de 3 camadas declaradas. Números batem com o relatório do builder
(`docs/context/plataforma-dados/gates/T-03.3-builder.md`).

```
bash backend/scripts/lint.sh   → ruff check/format limpos, mypy --strict limpo (159 arquivos)
bash backend/scripts/boundaries.sh → 3 kept, 0 broken (113 arquivos, 488 dependências)
bash backend/scripts/natureza.sh   → universo 48 arquivos (domain+use_cases), 0 leituras de relógio
harness rules --mode sweep --changed-only → rc=0, sem saída (0 achados)
```

## 2. Mutação — a prova destrutiva pedida pelo despacho

Backups feitos antes de cada mutação; todo arquivo restaurado e `git status --short` limpo ao final
(confirmado).

**M1 — B1 enfraquecido** (`force_order_reconnection_overlap.py`): troquei
`old_source_closed_at < new_first_message_at` por
`old_source_closed_at <= new_first_message_at - 1` (permite buraco de até quase 1 unidade sem
levantar). Resultado: `test_require_overlap_rejects_the_old_source_closing_first` **falhou**
(`DID NOT RAISE ReconnectionGapError`) — B1 é **realmente** obrigatório, não pulável. **[OK]**

**M2 — direção do viés invertida** (`force_order_collision_accounting.py`): removi o `continue`
após registrar uma colisão, fazendo o duplicado **também** entrar em `total_events` (supercontagem
em vez de subcontagem). Resultado: **4 testes falharam**
(`test_a_repeated_key_within_one_bucket_is_a_collision_not_a_second_total`,
`test_reconnect_and_key_feeds_the_overlap_into_a_detectable_collision`,
`test_the_cli_end_to_end_detects_the_known_overlap_duplicate_and_writes_the_summary`,
`test_the_cli_skips_blank_lines_and_reads_across_multiple_evidence_files`) — a direção
"subcontagem, nunca supercontagem" de B3 está **provada por teste**, não só documentada. **[OK]**

**M3 — campo `trade_time` da chave B2 zerado** (`force_order_natural_key.py`): troquei
`trade_time=int(order["T"])` por `trade_time=0` (constante). Resultado: **26/26 passed, 0 falhas.**
`FRAME_A` (o único frame usado no teste de igualdade exata, `test_extract_reads_the_five_declared_fields`)
já tem `T=0` no JSON, então a mutação é indistinguível dele; nenhum outro teste compara
`key.trade_time` de um frame com `T` não-zero contra o valor esperado. **[FAIL — sobrevive]**

**M4 — campo `side` da chave B2 hardcoded** (`force_order_natural_key.py`): troquei
`side=str(order["S"])` por `side="SELL"` (constante — coincide com o valor de `FRAME_A`).
Resultado: **26/26 passed, 0 falhas.** Nenhum teste isola `side` como o único campo que diferencia
duas chaves — `FRAME_A` vs `FRAME_B` também mudam `symbol`; `FRAME_A` vs `FRAME_A_LATER_SAME_DAY`
têm o mesmo `side` nos dois. **[FAIL — sobrevive]**

Comando de reprodução (M3 exemplificado — M4 é análogo trocando o `sed`):
```
sed -i 's/trade_time=int(order\["T"\]),/trade_time=0,/' \
  backend/src/modules/sentimento/domain/force_order_natural_key.py
cd backend && source .venv/bin/activate && \
  python -m pytest tests/sentimento/test_force_order_reconnection.py -q
# → 26 passed (deveria ter reprovado — nenhum teste depende do valor de trade_time)
```

**Por que isto é defeito e não nitpick:** o handoff e o `ADR-004` B2 pedem a chave *"declarada, não
presumida"* — e o requisito de despacho desta rodada de QA foi explicitamente confirmar isso por
mutação, não por leitura de código. Dois dos cinco campos passam no code review (estão lá, na
`dataclass`, no `json.loads`) mas **não estão provados**: se um refactor futuro trocar `order["S"]`
por uma constante ou por `order["s"]` (o campo errado) por engano, a suíte atual continua verde. Isso
é precisamente o "verde não prova nada até uma mutação reprovar" do mandato do `/qa`.

## 3. DoD — item a item

- [x] B1 sobreposição obrigatória, verificável, **provada por mutação** (M1).
- [~] B2 chave natural declarada — **declarada corretamente em código**, mas **não integralmente
      provada por teste**: `symbol` e `price+orig_qty` (combinados) discriminam; `side` e
      `trade_time` isolados, não (M3, M4).
- [x] B3 taxa de colisão publicada + direção do viés escrita, **provada por mutação** (M2).
- [x] `D3.6` mecânica de contagem/publicação pronta sobre simulação offline com overlap conhecido —
      não medível em regime real hoje, conforme decisão do owner (`Q1`); nada a reprovar aqui.

## 4. Regras bloqueantes

Nenhuma das 7 regras de `harness rules list --severity block` incide sobre este diff:
`core.relative-import`/`core.silent-except`/`core.print-statement`/`core.hardcoded-secret` — 0
achados (`harness rules --mode sweep --changed-only`, rc=0, sem saída); as 3 de `web-fullstack` não
se aplicam a este componente (`sentimento` é backend, sem navegador/tenant/diretório de teste de
servidor envolvidos aqui).

Regras bloqueantes avaliadas: 7 de 7 listadas por `harness rules list --severity block`.

## Ações (NEEDS_FIX)

1. Adicionar teste que isola `side`: dois frames idênticos em `symbol/price/orig_qty/trade_time`,
   diferindo **só** em `side` (`BUY` vs `SELL`), devem produzir chaves diferentes e **não** colidir
   em `count_daily_collisions`.
2. Adicionar teste que isola `trade_time`: dois frames idênticos em `symbol/side/price/orig_qty`,
   diferindo **só** em `trade_time`, devem produzir chaves diferentes e **não** colidir.
3. Rerodar as mutações M3/M4 deste relatório contra os testes novos e confirmar que ambas passam a
   reprovar (`DID NOT RAISE` vira falha esperada / `passed` vira `FAILED`) antes de reenviar para QA.
