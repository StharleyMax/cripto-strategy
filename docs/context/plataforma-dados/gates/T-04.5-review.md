# Review — T-04.5 (CST-32) · `cvd_delta`/`cvd_cum(anchor)`

**PR #46**, branch `tasks/T-04.5-cvd-delta-e-cvd-cum`, diff contra `master@840c500`.
Worktree auditado: `/tmp/claude-1002/wt/T-04.5`.

## Veredito: COMPLIANT

**Denominador:** 7 regras bloqueantes em vigor (`harness rules list --severity block`), todas
avaliadas via `harness rules --mode file --path <arquivo>` sobre os 4 arquivos tocados
(`backend/src/modules/sentimento/domain/cvd.py`, `backend/tests/sentimento/test_cvd.py`,
`backend/tests/helpers/data_fixtures.py`, `backend/README.md`) — 0 achado, `rc=0` em todos.

## Camadas medidas

1. **Mecânico** (`harness rules --mode file`): 0/7 regras violadas nos 4 arquivos.
2. **Import-linter** (`backend/pyproject.toml [tool.importlinter]`, `.venv/bin/lint-imports`):
   `Contracts: 3 kept, 0 broken` — inclui o contrato "Natureza: domain e use_cases nao falam com
   socket, ssl nem relogio". `cvd.py` importa só `collections.abc`, `dataclasses`, `decimal`,
   `typing` — nenhum dos 4 módulos proibidos.
3. **Cobertura por camada** (`bash backend/scripts/test.sh`, 460 testes, 3 execuções isoladas
   consistentes): domain 100.0% (piso 90) · use_cases 100.0% (piso 80) · infra 97.7% (piso 70).
   `cvd.py` sozinho: 48/48 linhas, 100%.
   Nota: uma execução paralela a outro comando meu produziu 7 falhas espúrias por contenção de
   processo; 3 execuções isoladas subsequentes passaram limpas (460 passed) — não é defeito do
   PR.
4. **Idioma de identificador** (convenção, `CLAUDE.md`): `grep` nos 3 arquivos de código/teste
   não achou identificador em português; `ruff`/`mypy` limpos. `data_fixtures.py` carrega 2
   mensagens de `raise` em português, mas é cópia verbatim pré-existente da branch `T-04.1`
   (diff byte-a-byte confirmado) — gap já documentado como `⏸ NÃO DECIDIDO` no `CLAUDE.md`, não
   introduzido por esta task.
5. **Duplicação/deriva do fixture copiado**: `git show tasks/T-04.1-shift-canonico-event-time:
   backend/tests/helpers/data_fixtures.py` vs. o desta branch — `diff` vazio, verbatim confirmado.
6. **F-2 (falsificador global fase 04)**, `docs/plans/SPEC-001-plataforma-dados/
   04_contrato_temporal.md:50`: `cvd_delta_by_bucket`/`cvd_cum` não recebem `SeriesKey` — a
   isolação por série é responsabilidade do chamador (ainda não construído; `tasks.toml` escopa
   `T-04.5` só a D4.7/D4.8, `depends_on = ["T-04.2"]`). Não é violação desta task: é composição
   adiada, documentada no próprio handoff (`docs/context/plataforma-dados/handoff/T-04.5.md:29`).
   Fica como ponto de atenção para a task que ligar `SeriesKey` a `cvd_cum`.
7. **Autoria dos commits**: `Stharley Maxwell <stharleymax@gmail.com>` como author/committer nos
   2 commits do branch; nenhum trailer `Co-Authored-By`.

## Achados
Nenhum.
