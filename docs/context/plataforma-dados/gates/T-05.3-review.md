# Review Gate — T-05.3 (`/review`, read-only, independente de builder/QA) · **Veredito:** COMPLIANT

# T-05.3 — review gate (`/review`, read-only, independente do builder/QA)

**Escopo:** `docs/context/plataforma-dados/gates/T-05.3-builder.md` (QA ainda não rodou — nenhum
`T-05.3-qa.md` existe), `docs/adr/ADR-003-fronteira-charts-web.md`, `docs/context/plataforma-dados/tasks.toml`
(linha `T-05.3`), `docs/plans/SPEC-001-plataforma-dados/05_fatia_visivel.md`, `docs/specs/SPEC-001-plataforma-dados.md`
§6.1, `docs/adr/ADR-005-transporte-de-leitura.md`, e o diff completo `origin/master...HEAD`:
`docs/INDEX.md` (+1), `frontend/package-lock.json` (sync de lockfile), `frontend/src/charts/s2-badge.ts`
(novo), `frontend/src/charts/s2-badge.test.ts` (novo). Branch `tasks/T-05.3-selo-icamento`, commit
`393b061`, `git status` limpo, `backend/` intocado (confirmado por `git show --stat`).

## Camada 1 — o que o runner mede

`harness rules list --severity block` → **n=7** regras bloqueantes em vigor. Lidas as 7 fontes
(`packs/core/rules.toml`, `packs/web-fullstack/rules.toml`) para saber o que cada uma realmente
alcança: 4 delas (`core.relative-import`, `core.silent-except`, `core.print-statement`,
`core.hardcoded-secret`) têm `paths = ["**/*.py"]` — não avaliam TypeScript por desenho; 1
(`web-fullstack.tenant-from-request`) mira `backend/**/*.py`; 1 (`web-fullstack.server-test-directory-present`)
é `path-presence`, não olha conteúdo de arquivo; a única com alcance real sobre este diff é
`web-fullstack.browser-imports-server` (`paths = ["frontend/src/**"]`, regex contra `from "...backend/"`).

```
harness rules --mode file --path frontend/src/charts/s2-badge.ts --surface ci       → rc=0
harness rules --mode file --path frontend/src/charts/s2-badge.test.ts --surface ci  → rc=0
harness rules --mode sweep --changed-only                                           → rc=0
```

**2 de 2 arquivos novos varridos, 7 de 7 regras bloqueantes avaliadas, 0 achado.** Consistente com o
achado independente: `grep -nE "readFileSync|existsSync|node:fs|fetch\(|localStorage|Date\.now\(|new Date\(" `
sobre os 2 arquivos → **nenhuma ocorrência** (só a menção em comentário, linha 23 de `s2-badge.ts`,
narrando a regra que o módulo respeita) — `s2-badge.ts` tem **zero `import`**, é um módulo
autocontido.

**Reproduzido de forma independente nesta sessão** (não confiei no relato do builder):

```
npm --prefix frontend install       → 128 packages, 0 vulnerabilities                    [MEDIDO]
npm --prefix frontend run lint      → rc=0 ("eslint src", sem saída = 0 problemas)        [MEDIDO]
npm --prefix frontend run test:charts → tests 78, pass 67, fail 11, duration ~56s         [MEDIDO]
node --test src/charts/s2-badge.test.ts  → tests 17, pass 17, fail 0                      [MEDIDO]
```

As 11 falhas de `test:charts` são as mesmas 11 pré-existentes que `T-05.2` já carregava: confirmado
por `grep -n "^test at\|^✖ src" /tmp/test_charts_out.txt | grep -i s2-badge` → **nenhuma linha** —
zero das 11 falhas toca `s2-badge.*`; todas são `ENOENT` sob `data/binance/{klines,metrics,aggtrades}/...`
(dado bruto gitignored, ausente neste worktree) em `s2-cvd.test.ts`, `s2-oi-loader.test.ts`,
`s2-panels.test.ts`, `s2-klines-loader.test.ts`, `s2-axis-integration.test.ts`,
`canonical-grid-sha256-proof.test.ts` — nenhum desses é arquivo desta task. Os números do builder
(`67 pass, 11 fail`, `17 pass, 0 fail`) **conferem exatamente** com a reprodução independente.

## Camada 2 — arquitetura declarada

### FR-1 (`ADR-003:36`, "charts não faz I/O... entrada é argumento") — CONFORME, sem ressalva

Ao contrário do `[WARNING]` que a rodada 1 de `T-05.2-review.md` registrou (loaders com `readFileSync`
direto em produção), `s2-badge.ts` **não importa nada** — confirmado (`grep -nE "^\s*import"` → 0
linhas) — e não faz I/O, não lê relógio (`Date.now`/`new Date`/`performance.now` ausentes), não conhece
rota nem `localStorage`. `session.referenceTimeMs` (o "`T`") chega sempre como parâmetro de
`buildIdade`/`buildBadge`, nunca lido internamente — a mesma disciplina que `ADR-003:24` exige
("recebe-os como argumento").

### `D5.12` (fronteira de import `charts`↔`web`, `frontend/eslint.config.mjs`) — CONFORME, trivialmente

Zero `import`/`import()`/`require` no módulo ⇒ nenhuma superfície para a regra avaliar. `npm run lint`
(reproduzido, `rc=0`) confirma que nem o `no-restricted-imports` nem o `no-restricted-syntax` de
`D5.12` dispararam.

### Componente e escopo declarados (`tasks.toml:629-637`) — CONFORME

`components = ["charts"]`, `depends_on = ["T-05.2"]`, `refs = ["SPEC-001 §6.1", "CA-F4-14",
"ADR-005/D3", "plano 05 itens 5.3+5.4", ...]` — todos batem com o que o módulo implementa. O plano
`05_fatia_visivel.md:14-15` nomeia os itens 5.3 ("selo de quatro campos, visível sem hover") e 5.4
("içamento em três níveis") como alvo `charts`, e é exatamente o que `s2-badge.ts` fecha. `D5.1`/`D5.2`
(carimbo/linha-guia visual) e `D5.4` (sobrevivência de `COMO EM T` à navegação) estão corretamente
nomeados como fora de escopo pelo builder — nenhum dos três está nos `refs` desta task, e nenhum exige
DOM/`.tsx`, que `frontend/src/charts/` ainda não tem (`find frontend/src/charts -name '*.tsx'` →
vazio). `D5.3` (crosshair sobre bucket ausente) também está legitimamente fora — não nomeado
explicitamente pelo builder, mas pela mesma razão (não há crosshair renderizado); omissão de rótulo,
não violação.

### `docs/INDEX.md` — append-only, CONFORME

`git diff origin/master...HEAD -- docs/INDEX.md` → `+1` linha no final, zero linha existente
reescrita.

### `frontend/package-lock.json` — sync de lockfile pré-existente, não é código novo

O diff (47 linhas) apenas move `jsdom`/`lightweight-charts` de `dependencies` para `devDependencies`
no bloco raiz e marca `"dev": true` nas subdependências — `frontend/package.json` **já** declarava os
dois só em `devDependencies` antes desta task (`git show origin/master:frontend/package.json`
confirma); o lockfile estava **dessincronizado** desde antes, e `npm install` corrigiu. Não é
alteração de superfície de código, não avalio contra `code_paths`.

### Ledger — nenhum gate-record prematuro

`harness pipeline state plataforma-dados` → `BUILD_AUTHORIZED`; `harness pipeline show` não lista
nenhum evento para `T-05.3` — confirma a alegação do builder ("`tasks.toml` continua `todo`; ledger e
Jira INTOCADOS; nenhum `gate-record`/`approve`/`advance`").

### Disciplina "nenhum número sem comando" (`CLAUDE.md`) — exemplar

O módulo mede `hoisted=115B denormalized=198B ratio=1.72x` (`jsonByteLength`, próprio) e declara
explicitamente que **não** é o `519B/54B/9,6×` histórico de `ADR-005`/D3, cujo schema não está no
repositório — reproduzi o mesmo número rodando `node --test src/charts/s2-badge.test.ts`
independentemente. Sem número apresentado como outro.

### Duas observações INFO — não entram no veredito, citadas para não virarem dívida invisível

1. **Idioma dos campos do selo** (`serie`, `idade`, `procedencia`, `completude` — nomes de campo TS
   em `frontend/src/charts/s2-badge.ts`, produção) estão em português, ecoando o vocabulário literal
   de `SPEC-001` §6.1. Isso não casa com nenhuma das duas exceções válidas da tabela do `CLAUDE.md`
   (linha 9 — vocabulário de componentes; linha 11 — coluna de contrato do `sentimento`), mas o
   `CLAUDE.md` é explícito e proposital sobre esta superfície: **"idioma de identificador é
   convenção, não portão"** — nenhuma `[[rules.own]]` de idioma é permitida, e nenhum instrumento
   mede isso hoje. Não entra no veredito por desenho do próprio documento que eu audito contra;
   nomeado para quem revisar a próxima task de `charts` que expuser este mesmo vocabulário para uma
   camada de renderização.
2. **`SessionEnvelope`** (campos `mode`, `env`, `principalId`, `bundleVersion`) reside dentro de
   `frontend/src/charts/s2-badge.ts`, e `ADR-003:26-29` cita **"selo de sessão, chip de `env`"**
   como exemplo do chrome que pertence a `web`. Não classifiquei como violação porque `s2-badge.ts`
   só **recebe** `SessionEnvelope` como argumento (nunca o computa, nunca lê sessão/env/relógio) —
   exatamente a cláusula que `ADR-003:24` reserva para `charts` ("recebe-os como argumento") — e a
   própria task cita `ADR-005`/D3 como dono do desenho de três níveis (sessão/painel/célula). Fica
   como observação para o próximo arquiteto que decidir se o TIPO `SessionEnvelope` deveria morar em
   `web` e ser importado/injetado, em vez de definido dentro de `charts`.

## Veredito

```
Regras bloqueantes avaliadas: 7 de 7 (harness rules list --severity block)
Arquivos varridos: 2 de 2 novos (harness rules --mode file --path <f> --surface ci, um a um,
  rc=0 nos 2) + harness rules --mode sweep --changed-only (rc=0) + git show --stat/git diff
  integral dos 5 arquivos do commit (docs/INDEX.md, gates/T-05.3-builder.md, package-lock.json,
  s2-badge.ts, s2-badge.test.ts) + leitura integral dos 2 arquivos de produção/teste
Comandos reproduzidos nesta sessão, independentes do relato do builder:
  npm --prefix frontend install        → 128 packages, 0 vulnerabilities
  npm --prefix frontend run lint       → rc=0
  npm --prefix frontend run test:charts → 67 pass / 11 fail (11 pré-existentes de T-05.2,
    confirmado por grep que nenhuma toca s2-badge.*)
  node --test src/charts/s2-badge.test.ts → 17 pass / 0 fail
Veredito: COMPLIANT
Achados: 0 bloqueantes, 0 WARNING. 2 notas INFO (idioma dos campos do selo — explicitamente
  convenção não-portão no próprio CLAUDE.md; co-localização de SessionEnvelope com ADR-003's
  exemplo de chrome de `web` — defensável sob a cláusula "recebe-os como argumento" de FR-1,
  nomeada para o próximo arquiteto observar quando um ponto de montagem em `web` existir).
```

## Arquivos citados nesta revisão

- `/home/stharley/Documentos/projects/cripto-strategy/.claude/worktrees/wf_2a5f5015-5f1-19/frontend/src/charts/s2-badge.ts`
- `/home/stharley/Documentos/projects/cripto-strategy/.claude/worktrees/wf_2a5f5015-5f1-19/frontend/src/charts/s2-badge.test.ts`
- `/home/stharley/Documentos/projects/cripto-strategy/.claude/worktrees/wf_2a5f5015-5f1-19/docs/context/plataforma-dados/gates/T-05.3-builder.md`
- `/home/stharley/Documentos/projects/cripto-strategy/.claude/worktrees/wf_2a5f5015-5f1-19/docs/adr/ADR-003-fronteira-charts-web.md`
- `/home/stharley/Documentos/projects/cripto-strategy/.claude/worktrees/wf_2a5f5015-5f1-19/docs/context/plataforma-dados/tasks.toml` (linhas 629-637)
- `/home/stharley/Documentos/projects/cripto-strategy/.claude/worktrees/wf_2a5f5015-5f1-19/docs/plans/SPEC-001-plataforma-dados/05_fatia_visivel.md`
- `/home/stharley/Documentos/projects/cripto-strategy/.claude/worktrees/wf_2a5f5015-5f1-19/docs/INDEX.md`

