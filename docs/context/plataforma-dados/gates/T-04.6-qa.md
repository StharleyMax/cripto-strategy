# QA — T-04.6 · Serialização de numeral invariante de locale

**Veredito: APPROVED**

Escopo (bate com o builder): `git show a8b77c2 --stat` = 8 arquivos: `canonical_json.py` (novo),
`ingest_record.py`/`series_key.py` (modificados), `quota_ramp_emit_driver.py`/
`test_quota_ramp_locale_invariance.py` (novos), `README.md`/`INDEX.md`/gate do builder. `T-04.6`
referencia plano `04` item **4.12**/DoD **D4.12**, não "4.6"
(`docs/plans/SPEC-001-plataforma-dados/04_contrato_temporal.md:17,23`) — confirmado, não é erro
de leitura.

## D4.12 reproduzido de forma independente, fora do pytest

```
LANG=pt_BR.UTF-8 LC_ALL=pt_BR.UTF-8 PYTHONPATH=. .venv/bin/python tests/helpers/quota_ramp_emit_driver.py > out_ptbr.txt
LANG=C LC_ALL=C PYTHONPATH=. .venv/bin/python tests/helpers/quota_ramp_emit_driver.py > out_c.txt
sha256sum out_ptbr.txt out_c.txt
```

→ `0986dc15a417c084e7d5d2c924e6a6e8b0aa21281e30d3be85bf1c8b3ebd146f` para os dois — idêntico.

## Falsificador reproduzido (mutação temporária, restaurada)

Troquei o `float` por `locale.format_string(..., grouping=True)` com `setlocale(LC_ALL, "")`
explícito no driver → `1 failed, 1 passed` (hash diverge no caso `[C]`). Restaurado,
`git status --short` limpo.

## Demais checagens, todas reproduzidas

- `bash backend/scripts/lint.sh` → `All checks passed!`
- `bash backend/scripts/test.sh` → `562 passed`, cobertura total `99,43%`; domain 100%
  (1007/1007), use_cases 100% (220/220), infra 98,3% (808/822) — todas acima do piso.
- `bash backend/scripts/boundaries.sh` → `3 kept, 0 broken` (melhor que o `2/1` do relatório do
  builder — dívida de `T-03.10` já sanada pelo rebase pós-PR#50/`T-03.12`).
- `harness rules --mode sweep --changed-only` → `rc=0`, 0 achados; 4 regras `core.*` bloqueantes
  conferidas manualmente nos 5 arquivos tocados — 0 violações reais.

## Sobre fechar a fase 04 inteira — NÃO executado, de propósito

`gh pr list`: de `T-04.1..T-04.7`, só `T-04.1`/`T-04.2`/`T-04.4` têm PR mergeada; `T-04.3`/
`T-04.5`/`T-04.7` seguem com PR **aberta** e **sem QA gate favorável ainda registrado**
(`docs/context/plataforma-dados/gates/` não tem `T-04.3-qa.md` nem `T-04.7-qa.md`; `T-04.5` só
tem `-review.md`). A condição "todas as 7 com PR aberta/mergeada e QA/Review favoráveis" **não
está satisfeita** — `harness tasks resolve` com `=done` para as sete não foi executado.
