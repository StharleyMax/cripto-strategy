[QA GATE — Fase 06: T-06.8 · Campo aditivo desconhecido → quarentena+alarme; ausente/renomeado → reprova]
Feature: plataforma-dados     Componente: sentimento     PR: https://github.com/StharleyMax/cripto-strategy/pull/106

## Escopo revisado

- `backend/src/modules/sentimento/domain/schema_change.py` (domain, puro)
- `backend/src/modules/sentimento/use_cases/classify_schema_change.py` (use_cases)
- `backend/tests/sentimento/test_schema_change.py`
- `backend/tests/sentimento/test_classify_schema_change.py`

## 1. Suite existente, rodada antes de qualquer teste novo

`bash backend/scripts/test.sh` (universo: `backend/tests/` inteiro, 1416 testes) → **1416 passed,
3 warnings in 266.21s**, `rc=0`. Cobertura por camada (`ADR-009/D1`): domain **99,8%**
(2807/2812, meta 90%), use_cases **100,0%** (594/594, meta 80%), infra **95,1%** (2208/2321, meta
70%) — 3 de 3 `[OK]`. `schema_change.py` e `classify_schema_change.py`: **100%** cada, confirmado
no relatório `term-missing` da mesma corrida (linhas isoladas: `9 0 2 0 100%` e igual para o
outro). Bate com o número relatado pelo builder — confirmado independentemente, não copiado.

Subset isolado (`.venv/bin/python -m pytest tests/sentimento/test_schema_change.py
tests/sentimento/test_classify_schema_change.py -v --no-cov`) → **9 passed in 0.07s**, baseline
verde antes de qualquer mutação.

## 2. Mutação real (planta → confirma → reverte → confirma diff limpo)

**Mutação 1 — inversão completa da lógica (aditivo reprova, ausente vira aceito/quarentena)**,
pedida explicitamente pelo escopo do gate: troquei o subtraendo em `unknown_fields`
(`expected - received` em vez de `received - expected`) e em `missing_fields` (`received -
expected` em vez de `expected - received`) em `schema_change.py`.
Resultado: `6 failed, 3 passed` — pegou `test_additive_unknown_field_quarantines_never_rejects`
(D6.14, o caso real `nq`), `test_missing_field_rejects_the_whole_payload`,
`test_renamed_field_rejects_even_though_the_new_name_looks_additive`,
`test_missing_and_additive_together_still_rejects`, e as duas de
`test_classify_schema_change.py` que dependem da mesma classificação (`...logs_exactly_one...`,
`...rejects_without_ever_logging_an_alarm`).
`git checkout -- backend/src/modules/sentimento/domain/schema_change.py` → `git status --short
backend/` vazio, `git diff --stat` vazio. Confirmado limpo antes da mutação seguinte.

**Mutação 2 — inversão de ORDEM (checa aditivo antes de ausente)**, para provar que "renomeado é
detectado como esperado-ausente, não silenciosamente ignorado" depende da ordem, não é
coincidência: reescrevi `classify_schema_change` para computar o `SchemaChangeVerdict` primeiro e
retornar cedo (`return verdict`) se `verdict.is_additive`, só then checando `missing_fields`.
Resultado: `2 failed, 7 passed` — exatamente os dois testes que exercitam o caso "ausente E
aditivo ao mesmo tempo" pegaram: `test_renamed_field_rejects_even_though_the_new_name_looks_additive`
(`q`→`quantity`: sem a ordem certa, quarentena em vez de reprova) e
`test_missing_and_additive_together_still_rejects`. Os outros 7 (incluindo o caso `nq` puro e o
ausente puro) continuaram verdes — a mutação é cirúrgica no caso combinado, e o teste certo é o
único que reage, sem falso-negativo nos outros.
`git checkout -- backend/src/modules/sentimento/domain/schema_change.py` → `git status --short
backend/` vazio, `git diff --stat` vazio. Confirmado limpo.

Nenhuma mutação sobreviveu sem teste que a pegasse — as duas propriedades centrais do DoD (qual
reação para qual classe, e a ordem que resolve o caso combinado "renomeado") estão sob prova de
mutação real, não apenas verde por coincidência.

## 3. Reuso de `Absence.QUARANTINE`

`schema_change.py:34` importa `from src.modules.sentimento.domain.provenance import Absence`;
`SchemaChangeVerdict.absence` (linha 79) retorna `Absence.QUARANTINE if self.is_additive else
None` — mesmo enum de `provenance.py:97-107`, nenhum enum paralelo declarado neste diff (grep de
`class.*Enum` nos 2 arquivos novos: 0 ocorrências).

## 4. Contrato `Natureza` (`ADR-016`)

`bash backend/scripts/natureza.sh` → `80 arquivo(s), 0 leitura(s) de relogio` (domain 60 +
use_cases 20, universo íntegro). `bash backend/scripts/boundaries.sh` → `Analyzed 166 files, 751
dependencies. Contracts: 3 kept, 0 broken` — inclui "Camadas por contexto: infra > use_cases >
domain KEPT". `schema_change.py` está em `domain/`, importa só `dataclasses` +
`provenance.Absence` (sem `logging`, sem I/O). `classify_schema_change.py` está em `use_cases/`,
é quem importa `logging` e chama o domain — divisão respeitada estruturalmente, não só por
convenção de pasta.

## 5. Idioma

Leitura integral dos 4 arquivos: identificadores, docstrings, comentários e mensagens de exceção
em inglês. As únicas strings em português são citações literais entre aspas/backtick do texto da
SPEC-001 §5.5 (`"campo ADITIVO desconhecido"`, `"quarentena + alarme"`, `"campo AUSENTE ou
RENOMEADO"`, linhas 12-13 de `schema_change.py`) — citação de fonte, não identificador nem
mensagem nova; não há `raise`/`Error(...)` com string em português (regra da lacuna fechada em
2026-09-02).

## 6. Regras bloqueantes

`harness rules --mode sweep --path <arquivo>` rodado individualmente nos 4 arquivos do escopo →
saída vazia (`rc` sem achado) nos 4 — nenhuma das 7 regras block acusa. Nenhuma das 7 regras
(`relative-import`, `silent-except`, `print-statement`, `hardcoded-secret`,
`browser-imports-server`, `tenant-from-request`, `server-test-directory-present`) se aplica a
este diff por escopo/conteúdo — confirmado por leitura direta, não só pela ausência de achado do
sweep.

## Falsificador do gate (o que teria reprovado e não reprovou)

Se a ordem "rejeição vence" estivesse trocada, ou se os subtraendos estivessem trocados, as
mutações acima teriam passado verdes — não passaram: 6/9 e 2/9 testes reagiram, respectivamente,
e os dois conjuntos afetados são exatamente os que o DoD nomeia (D6.14 puro, ausente puro,
combinado renomeado). Nenhuma mutação sobreviveu.

## Veredito

APPROVED — DoD provado com mutação real, suite existente rodada antes de testes novos (não havia
testes novos a criar; QA usou os já entregues, verificou-os por mutação), cobertura acima do alvo
em todas as camadas, Natureza e regras bloqueantes conferidas independentemente do relatório do
builder.
