# UX Audit — T-05.7 (ux-ui-mastery, interação) · **Veredito:** APROVADO_COM_CONDICAO

# UX Audit — `T-05.7` ("cor como token nomeado por papel; critical fora do canal de cor")

Relatório completo: `docs/context/plataforma-dados/gates/T-05.7-ux-audit.md`

**Escopo do gate:** interação — usabilidade, acessibilidade, carga cognitiva, motion. Não julga fidelidade do dado. Método: leitura estrutural de código + execução real (worktree detached de `a10aa71`): `npm run lint` rc=0; `node --test src/charts/color-tokens.test.ts` → 7/7 pass; `node --test src/charts/s2-axis-integration.test.ts` (com symlink de `data/`) → 4/4 pass, incluindo round-trip real contra `lightweight-charts` (`appliedCandlestickColors` lido de `series.options()` = `{upColor:"#089981", downColor:"#f23645"}`); `node scripts/validate_palette.js` → exit 0, 361 medições.

## Veredito: APROVADO COM CONDIÇÃO

O deliverable declarado (token de cor tipado por papel, `critical`/`severity`/`severidade` fora do canal de cor com guarda dupla estática+runtime testada) está correto, bem testado, e passa no próprio DoD (`D5.6`). A condição não é sobre o que a task entregou — é que `ADR-010/D-2` (redundância de FORMA para direção — corpo vazado/cheio/cruz), já aceito pelo owner na mesma ADR que esta task consome os valores, e que o próprio `validate_palette.js` (BLOCO 6) mede como "OBRIGATÓRIO" (luminância entre os dois fills = 1,092, colapsam em cinza), não tem NENHUMA task rastreada em `tasks.toml`. `T-05.7` é a primeira vez que o token chega a uma série real (confirmado pelo round-trip acima), tornando esse gap visível na prática, não só na ADR.

**Achado principal:** WCAG SC 1.4.1 (Use of Color, nível A) — a direção da vela usa hoje um único canal (fill; `wickVisible`/`borderVisible` no default `true`, corpo sempre cheio nas duas direções). Note-se que a dicromacia comum (protan/deutan/tritan) PASSA (`min3 = 18.0` contra piso 15) — um usuário daltônico comum consegue distinguir alta de baixa pelo deslocamento de matiz; o que colapsa é o canal de luminância pura (acromatopsia total, captura em escala de cinza, impressão), que `ADR-010` escolheu tratar como piso obrigatório, mais estrito que o mínimo usual.

Não bloqueia este commit especificamente: os 2 tokens de direção batem exatamente com `ADR-010/D-1`; `candlestickSeriesColors` deriva os 6 campos de `CandlestickStyleOptions` de exatamente esses 2 tokens (testado); nenhuma garantia piora com `D-2` pendente. O escopo estreito de `T-05.7` (plano item 5.9, DoD `D5.6`) nunca pediu forma — e o código documenta essa exclusão de forma explícita, não escondida.

## O que foi checado e está limpo
- Vocabulário `ColorRole` (inglês) ↔ chaves do script (`português`) mapeado 1:1 por tabela única, cross-check hex-a-hex confirmado por execução real.
- Guarda `CA-F4-10` (exclusão de `critical`/`severity`/`severidade`) — dupla, estática e runtime, com teste negativo mostrando rejeição real (incluindo case-insensitive).
- Contraste WCAG dos 6 papéis nos dois modos, contra 3 superfícies + tinta-sobre-fill — todos PASS, confirmado por execução direta de `validate_palette.js`.
- Escopo (`acao-*`/`foco` fora deste módulo) correto — fronteira `charts`↔`web` respeitada.
- Motion e alvo de toque: não aplicável (nenhuma mudança nesta task).
- Crosshair/overlay renderizado: `[NÃO SEI]` — sem navegador nesta sessão; nada nesta task altera crosshair, então nenhum risco novo aqui.

## Ação recomendada

Mergeável como está. Condição: abrir/rastrear em `tasks.toml` uma task para `ADR-010/D-2` (redundância de forma na série de candlestick) antes de esta estilização alcançar uma tela real de `web`. Não é reabertura de `T-05.7`.
