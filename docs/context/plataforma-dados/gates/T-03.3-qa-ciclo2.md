# `T-03.3` (`CST-19`) — relatório de gate do `/qa`, CICLO 2 (re-validação de `NEEDS_FIX`)

**Feature:** `plataforma-dados` · **Componente:** `sentimento` · **Fase:** `03`
**Base:** `master@07193e6` · **Branch:** `tasks/T-03.3-reconexao-por-classe` · **PR:** #64
**Worktree:** `/tmp/claude-1002/wt/T-03.3` · **Commit avaliado:** `2da1137`
**Corrige:** `docs/context/plataforma-dados/gates/T-03.3-qa.md` (`NEEDS_FIX`, ciclo 1, commit `a01aea4`)
**Resposta do builder:** `docs/context/plataforma-dados/gates/T-03.3-builder-ciclo2.md`

## Veredito

**APPROVED.** Reproduzi eu mesmo, do zero (sem confiar no relato do builder), as duas mutações
que sobreviveram no ciclo 1 (M3 — `trade_time` hardcoded a `0`; M4 — `side` hardcoded a
`"SELL"`) contra o código do commit `2da1137`. Ambas agora **reprovam** a suíte
(`tests/sentimento/test_force_order_reconnection.py`), cada uma derrubando exatamente o teste
novo desenhado para isolá-la. Os 5 campos da chave natural B2 (`symbol`, `side`, `price`,
`orig_qty`, `trade_time`) estão agora todos provados por mutação — isolados ou em par
discriminante — não só declarados em código.

## 1. Testes existentes rodados antes de qualquer teste novo (eu não criei teste novo — só reproduzi)

```
cd /tmp/claude-1002/wt/T-03.3/backend && bash ../backend/scripts/test.sh   # (via bash backend/scripts/test.sh)
```
→ **899 passed**, 3 warnings, 262,34s (rodei eu mesmo, não copiei do builder). Cobertura total
**98,06%**; por camada (`ADR-009/D1`): `[OK] domain 99,9%` (meta 90%) · `[OK] use_cases 100,0%`
(meta 80%) · `[OK] infra 95,8%` (meta 70%) — universo 3 de 3 camadas declaradas. Números **batem
byte a byte** com `T-03.3-builder-ciclo2.md`.

```
bash backend/scripts/lint.sh       → ruff check/format limpos, mypy --strict limpo (159 arquivos)
bash backend/scripts/boundaries.sh → 3 kept, 0 broken (113 arquivos, 488 dependências)
bash backend/scripts/natureza.sh   → universo 48 arquivos (domain 34 + use_cases 14), 0 leituras de relógio
harness rules --mode sweep --changed-only → rc=0, sem saída (0 achados)
```

## 2. Reprodução independente de M3 e M4 (o pedido central deste ciclo)

Rodada isolada primeiro, contra só o arquivo do bench, para confirmar a contagem exata de testes:

```
cd backend && source .venv/bin/activate
python -m pytest tests/sentimento/test_force_order_reconnection.py
# → 28 passed in 0.07s   (26 do ciclo 1 + 2 novos: test_side_alone_discriminates_the_key,
#                          test_trade_time_alone_discriminates_the_key)
```

**M3 — `trade_time` hardcoded a `0`:**
```
cp src/modules/sentimento/domain/force_order_natural_key.py /tmp/.../force_order_natural_key.py.bak
sed -i 's/trade_time=int(order\["T"\]),/trade_time=0,/' \
  src/modules/sentimento/domain/force_order_natural_key.py
python -m pytest tests/sentimento/test_force_order_reconnection.py
```
→ **1 failed, 27 passed** — `test_trade_time_alone_discriminates_the_key`:
`AssertionError: assert 0 == 1` (`key_shifted.trade_time`). **REPROVA — confirmado.**

**M4 — `side` hardcoded a `"SELL"`** (mutante restaurado antes de aplicar este):
```
sed -i 's/side=str(order\["S"\]),/side="SELL",/' \
  src/modules/sentimento/domain/force_order_natural_key.py
python -m pytest tests/sentimento/test_force_order_reconnection.py
```
→ **1 failed, 27 passed** — `test_side_alone_discriminates_the_key`:
`AssertionError: assert 'SELL' == 'BUY'` (`key_flipped.side`). **REPROVA — confirmado.**

Mutante revertido e conferido byte a byte:
```
cp /tmp/.../force_order_natural_key.py.bak src/modules/sentimento/domain/force_order_natural_key.py
diff /tmp/.../force_order_natural_key.py.bak src/modules/sentimento/domain/force_order_natural_key.py
# → sem diferença
git status --short   # → limpo (nada a commitar), na raiz do worktree
```

**Conclusão da mutação:** M1/M2 (B1, B3) já provados no ciclo 1, sem mudança neste ciclo. M3/M4
(B2 — `trade_time`, `side`) sobreviviam no ciclo 1 e agora **morrem** — universo B2: **5 de 5
campos da chave natural provados por mutação** (`symbol` e `price+orig_qty` no ciclo 1; `side` e
`trade_time` isolados neste ciclo).

## 3. DoD — item a item

- [x] B1 sobreposição obrigatória, verificável, provada por mutação (M1, ciclo 1 — sem mudança).
- [x] B2 chave natural declarada — agora **integralmente provada por teste isolado**: `symbol`,
      `price`+`orig_qty` (ciclo 1), `side` e `trade_time` isolados (ciclo 2, M3/M4 confirmados
      reprovando por reprodução independente, não só pelo relato do builder).
- [x] B3 taxa de colisão publicada + direção do viés escrita, provada por mutação (M2, ciclo 1).
- [x] `D3.6` mecânica de contagem/publicação pronta sobre simulação offline com overlap
      conhecido — não medível em regime real hoje, conforme decisão do owner (`Q1`); nada a
      reprovar aqui (sem mudança desde o ciclo 1).

## 4. Regras bloqueantes

Nenhuma das 7 regras de `harness rules list --severity block` incide sobre este diff:
`core.relative-import`/`core.silent-except`/`core.print-statement`/`core.hardcoded-secret` — 0
achados (`harness rules --mode sweep --changed-only`, rc=0, sem saída, sobre
`git diff --name-only master...HEAD` = 6 arquivos de produção + 1 de teste, todos em
`backend/src/modules/sentimento` e `backend/tests/sentimento`); as 3 de `web-fullstack` não se
aplicam (`sentimento` é backend, sem navegador/tenant/diretório de teste de servidor envolvidos).

Regras bloqueantes avaliadas: 7 de 7 listadas por `harness rules list --severity block`.

## 5. Diferença em relação ao relatório do builder

Nenhuma. Reproduzi os mesmos dois `sed` literais, no mesmo arquivo, e obtive a mesma falha (mesmo
teste, mesma asserção) que `T-03.3-builder-ciclo2.md` §3 relata. Não encontrei nenhuma mutação
adicional que sobrevivesse nos campos B2 restantes (não testei além do escopo do `NEEDS_FIX` do
ciclo 1, que era especificamente `side`/`trade_time` — `symbol` e `price+orig_qty` já estavam
fora de escopo por já terem sido provados no ciclo 1).

## Ações (nenhuma — APPROVED)

Nenhuma. Pendências do ciclo 1 fechadas.
