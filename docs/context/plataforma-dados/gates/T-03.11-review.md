# QA Gate — Review arquitetural, T-03.11 (CST-27), fase 03, componente `sentimento`

**PR:** #67 · **Branch:** `tasks/T-03.11-reconciliacao-coinalyze` · **Commit:** `1d0711a`
**Worktree auditado:** `/tmp/claude-1002/wt/T-03.11` · **Base:** `master@07193e6`
**Builder:** `docs/context/plataforma-dados/gates/T-03.11-builder.md`

## Veredito: COMPLIANT

## Denominador

- Regras bloqueantes em vigor: **7** (`harness rules list --severity block`, packs `core` +
  `web-fullstack` + declarações do repositório). Aplicáveis a este PR (Python backend): as 4
  `core.*` + `web-fullstack.server-test-directory-present` (5 de 7; as 2 restantes são
  frontend/tenant, fora do escopo do diff).
- Arquivos varridos: 11 (`gh pr diff 67 --name-only`) — 5 fonte, 5 teste, 1 `README.md`.
- Comandos re-executados por mim, não apenas aceitos do relatório do builder:
  `harness rules --mode sweep` → rc=0; `bash backend/scripts/boundaries.sh` → `Contracts: 3
  kept, 0 broken`; `bash backend/scripts/natureza.sh` → `0 leitura(s) de relogio`, universo 46
  arquivos; `bash backend/scripts/test.sh -q` → rc=0, cobertura total 98,09%, camadas `domain
  99,9%/90 · use_cases 100%/80 · infra 95,9%/70` (todas acima do piso).

## Achados

1. **Contrato import-linter "Natureza" (domain/use_cases não falam com socket/ssl)** —
   `KEPT`, confirmado por execução própria. Nenhum import de `socket`/`ssl` nos 3 arquivos de
   domain/use_cases do diff.
2. **Idioma de identificador (linha 1/2 da tabela de fronteira)** — todo `def`/`class`/campo
   nos 3 módulos de produção e nos 3 arquivos de teste do diff é inglês
   (`grep -P '[^\x00-\x7F]'` sobre nomes de função/classe: 0 ocorrências). Strings pt-BR
   presentes (`RECONCILIATION_CAVEAT`, `HYPOTHESIS_SCREEN_LABEL`, mensagens de `raise`) são
   microcopy operador-facing, coberta pela linha 8 da tabela (`SPEC-001 §3.8`), com a
   justificativa citada em comentário no próprio módulo
   (`liquidation_reconciliation.py:52-55`).
3. **Pisos de cobertura por camada** — `[OK] domain 99,9%/90 · use_cases 100%/80 · infra
   95,9%/70`, reconferido por execução própria de `test.sh -q` (não apenas lido do relatório
   do builder). O único ponto infra não coberto (`sqlite_series_quarantine_store.py:188`) fica
   fora dos hunks do diff (`git diff master -- <arquivo>` confirma), como o builder alegou.
4. **Quarentena (T-02.2, D2.6) respeitada, não contornada** — `read_latest()` usa
   `_SELECT_LATEST_QUARANTINED`, sem filtro `available_at IS NOT NULL`; não reusa nem chama
   `read_promoted()`. Testado a contrapelo por
   `test_read_latest_returns_the_quarantined_row_even_though_available_at_is_null`. O CLI
   (`liquidation_reconciliation_cli.py:121`) chama `store.read_latest(...)`, nunca
   `read_promoted`.
5. **A ressalva está na saída de máquina, não só em prosa** — confirmado lendo o código, não
   só a doc: `DailyLiquidationReconciliation` é um dataclass com `hypothesis` (enum de 4
   membros) e `caveat` (string fixa) como campos de primeira classe — nenhum call site pode
   emitir `ratio` sem eles. `_report_line()` (`liquidation_reconciliation_cli.py:83-95`)
   projeta `hypothesis`, `screen_label` e `caveat` para o JSON emitido em `stdout` via
   `logger.info(line)` — é campo de saída de máquina, não comentário nem só o `README`.
   `classify_daily_reconciliation` nunca devolve `Decimal` solto (verificado lendo a função:
   todo `return` é uma tupla `(ratio, hypothesis)`).

Nenhuma violação de regra bloqueante nem de arquitetura declarada encontrada no escopo do diff.

## Não avaliado (fora do escopo desta auditoria)

As 2 regras bloqueantes de `web-fullstack` (frontend/tenant) — nenhum arquivo frontend no diff.
