[QA GATE — Fase 06: Quarentena pelo predicado de TRÊS termos]
Feature: `plataforma-dados`     Componente: `sentimento`
Arquivos alterados:
- `backend/src/modules/sentimento/domain/quarantine_terms.py` (modified — `COINALYZE_ONE_SHOT_TERMS` de `T-02.2` intocado; 3 funções novas: `quarantine_terms_for_catalog_entry`, `quarantine_gaveta`, `readable_by_backtest`)
- `backend/src/modules/sentimento/domain/live_availability_write.py` (new — `D6.3`: `resolve_unmeasured_endpoint_availability`, `MeasuredLagCannotUseUnmeasuredPathError`)
- `backend/tests/sentimento/test_quarantine_terms.py` (modified — 5 testes novos, incluindo o falsificador `D6.2`; 11→16 `[MEDIDO: pytest --collect-only -q]`)
- `backend/tests/sentimento/test_live_availability_write.py` (new — 6 testes, `D6.3` `[MEDIDO: pytest --collect-only -q]`)
- `backend/README.md` (modified — nova seção "📎 2026-09-03 por `T-06.6`", mesclada ao lado das seções de `T-06.5`/`T-06.2` que chegaram em paralelo)
- `docs/INDEX.md` (modified — 1 linha acrescentada, append-only, ao lado das de `T-06.5`/`T-06.2`)
Spec: docs/specs/SPEC-001-plataforma-dados.md (§5.2)
Plan: docs/plans/SPEC-001-plataforma-dados/06_semantica_declarada.md (item 6.12)
DoD:
- [x] D6.1: `count(gaveta) == count(catálogo WHERE <predicado de 3 termos>)` e `count(painéis sincronizados ∩ quarentena) == 0`, sobre TODO o catálogo — `quarantine_gaveta`/`readable_by_backtest` operam sobre `SeriesCatalog` inteiro (não hardcoded a uma fonte), exercidas sobre as 5 linhas REAIS de `open_interest_catalog_entries()` (`T-06.5`) — `test_d6_1_gaveta_count_matches_the_predicate_applied_by_hand_over_the_whole_catalog` e `test_d6_1_readable_by_backtest_never_intersects_the_gaveta` — `bash backend/scripts/test.sh -k test_d6_1`
- [x] D6.2 (falsificador da fase): série com `label_shift` E `unit` presentes mas `available_at` NULL ⇒ quarentenada, sobre a linha de PRODUÇÃO real do OI `CLOSE` da Coinalyze — `test_d6_2_the_third_term_alone_isolates_a_series_with_the_other_two_resolved` lê `open_interest_catalog_entries().entry_for(coinalyze_open_interest_key(Reduction.CLOSE))` (`unit="BTC"`, `label_shift=300_000`, ambos já resolvidos por `T-06.5`) com `available_at_present=False` e prova `is_quarantined is True`; não-vacuidade provada por `test_readable_by_backtest_is_not_vacuously_empty` — `bash backend/scripts/test.sh -k "d6_2 or not_vacuously_empty"`
- [x] D6.3: endpoint sem `lag_ms` medido grava `available_at=NULL`+`MODELED`, nunca aproxima com `event_time`/`event_time+interval` — tipo de retorno `tuple[None, AvailabilitySource]` sem ramo `int`; `test_the_output_is_never_event_time_nor_event_time_plus_interval` (parametrizado, 3 casos) + `test_a_measured_endpoint_refuses_the_unmeasured_path_instead_of_silently_taking_it` (o outro lado: endpoint MEDIDO é recusado, não cai em silêncio) — `bash backend/scripts/test.sh -k test_live_availability_write`
Comandos rodados (literais) e resultado:
- `bash backend/scripts/test.sh` (múltiplas rodadas até verde, detalhadas no ciclo de correção abaixo) → rodada final, pós-merge com `origin/master@fdb22aa` (`T-06.5`, já com `T-06.2` também mesclada em `935eb97`, que não toca nenhum arquivo desta task): **1300 passed, 0 failed** (universo: toda `backend/tests/`; +11 testes novos desta task sobre os 1289 herdados de `T-06.5`)
- cobertura total **97,63%**; por camada (`check-coverage-layers.sh`, `ADR-009/D1`): domain **99,8%** (meta 90%, 2536/2540), use_cases **100,0%** (meta 80%, 585/585), infra **95,1%** (meta 70%, 2208/2321) — 3/3 `[OK]`
- `live_availability_write.py` isolado: **9 stmts, 0 miss, 100%**; `quarantine_terms.py`: **32 stmts, 1 miss (linha pré-existente de `T-02.2`), 95%**
- `bash backend/scripts/lint.sh` → `ruff check`+`ruff format --check`+`mypy --strict`: **sem achado**, 237 arquivos (2 achados `D401` de imperativo em docstring de teste, corrigidos no mesmo ciclo antes deste commit)
- `harness rules --mode sweep --changed-only` → **0 achados** no estado final (1 `[AVISO]` `core.module-docstring-single-line` em `live_availability_write.py`, corrigido no mesmo ciclo: conteúdo movido para comentário `#`, mesma forma de `quarantine_terms.py`)
- `bash backend/scripts/boundaries.sh` → **3 kept, 0 broken** (157 arquivos, 712 dependências)
- `bash backend/scripts/natureza.sh` → **72 arquivo(s) (53 domain + 19 use_cases), 0 leitura(s) de relógio**
- Ciclo de correção nomeado (dois achados, os dois fechados antes deste commit):
  1. 1ª rodada de `test.sh` reprovou `test_as_of_is_the_single_reader.py::test_no_production_module_imports_this_accessor_yet_and_that_is_recorded_not_claimed` — o docstring de `quarantine_terms.py` citava literalmente `as_of_accessor.py` num comentário (não um import), e o guard escaneia TEXTO de arquivo, não import real; reescrito para citar "`T-04.4`'s decision-read module" sem a substring. Rodadas seguintes não reproduzem.
  2. Uma rodada intermediária reprovou `test_ingest_record_crash_borders.py::test_concurrent_recorders_neither_lose_rows_nor_corrupt_the_file` — achado de AMBIENTE, não desta task: sob contenção de CPU (6+ worktrees da fase 06 rodando `pytest` completo em paralelo na mesma máquina, `T-06.2`/`T-06.3`/`T-06.4`/`T-06.9` observados via `ps aux`), um subprocesso do teste estourou o timeout de 60s. Arquivo que esta task não toca. Reproduzido isolado sem contenção: `.venv/bin/python -m pytest tests/sentimento/test_ingest_record_crash_borders.py -q` → **4 passed**. Não reaparece na rodada final.
  3. `master@fdb22aa` (`T-06.5`) mesclou `domain/open_interest_catalog.py` (linhas REAIS de produção para o OI da Coinalyze/Binance) enquanto esta task já tinha uma fixture própria com `label_shift=0` para o `D6.2`. O merge PROVOU o palpite errado — o valor real é `label_shift=300_000` (`+interval`, `SPEC-001` §2.1) — e `test_quarantine_terms.py` foi reescrito para ler a linha de produção diretamente (`open_interest_catalog_entries().entry_for(...)`) em vez de manter uma fixture divergente. Os testes `D6.1` foram reescritos junto para rodar sobre as 5 linhas reais em vez de um catálogo de 2 linhas construído à mão.
Cobertura: 97,63% total / domain 99,8% / use_cases 100,0% / infra 95,1% (alvo: domain 90%, use_cases 80%, infra 70% — `ADR-009/D1`, confirmado por `check-coverage-layers.sh`)
Doc delta:
- `backend/README.md`: atualizado — nova seção "📎 2026-09-03 por `T-06.6` — quarentena generalizada sobre o catálogo inteiro (`D6.1`/`D6.2`) + regra de escrita `D6.3`" (conflito de merge com as seções de `T-06.5`/`T-06.2` resolvido mantendo AMBAS)
- `docs/INDEX.md`: linha `/build` acrescentada (append-only; conflito de merge resolvido mantendo a linha de `T-06.5` e a desta task)
- ADR: não necessário — generalização de mecanismo já decidido (`SPEC-001` §5.2); nenhuma decisão arquitetural nova, `Natureza`/`ADR-016` preservada (tudo `domain` puro, sem I/O)
Bloqueado (se houver): nenhum item do DoD desta task. Escopo explicitamente NÃO fechado, nomeado no handoff e reafirmado no doc delta: a Coinalyze permanece em quarentena (o terceiro termo só abre quando `Q19` medir `lag_ms` para os endpoints dela especificamente, fora desta task); nenhum mecanismo de promoção construído; a fórmula MODELED do ramo endpoint-MEDIDO (grade nativa + `p99_lag` + margem) não implementada — `D6.3` pede só o ramo não-medido, o outro ramo é recusado por `MeasuredLagCannotUseUnmeasuredPathError`, nomeado e não aproximado; nenhuma linha NOVA de produção do catálogo é escrita por esta task (as 5 linhas de OI que `D6.2` consome já existiam, de `T-06.5` — esta task só lê pelo predicado, nunca edita o catálogo). `tasks.toml`, ledger e Jira INTOCADOS; nenhum `gate-record`, `approve` ou `advance` emitido por este builder.
