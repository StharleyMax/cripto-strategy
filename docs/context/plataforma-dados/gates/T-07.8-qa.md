[QA GATE — Fase 07: `universe_at(ts, filtro)` com `universe_source` inadmissível POR TIPO]
Feature: `plataforma-dados`   Componente: `sentimento`   Card: `CST-62`
PR: https://github.com/StharleyMax/cripto-strategy/pull/88 (`tasks/T-07.8-fase07` → `master`, MERGEABLE, OPEN)
Worktree: `/tmp/claude-1002/wt/T-07.8`
Handoff: `docs/context/plataforma-dados/handoff/T-07.8.md`
Builder: `docs/context/plataforma-dados/gates/T-07.8-builder.md` (na worktree, ainda não mesclado)
Arquivos sob revisão: `backend/src/modules/sentimento/domain/universe_at.py` (novo, 188 linhas),
`backend/tests/sentimento/test_universe_at.py` (novo, 9 testes)

## Testes que já existiam (rodados antes de criar/mutar qualquer coisa)

- `bash backend/scripts/test.sh` → **rc=0, 1162 passed, 3 warnings, 290,52s**. Cobertura total
  **97,47%**; piso por camada (`ADR-009/D1`): domain **99,8%** (2304/2308, meta 90%), use_cases
  **100,0%** (549/549, meta 80%), infra **95,0%** (2166/2279, meta 70%) — as três folgam.
- `bash backend/scripts/lint.sh` → "All checks passed!" / "216 files already formatted" /
  "Success: no issues found in 216 source files" (`ruff check` + `format --check` +
  `mypy --strict` sobre `src`+`tests`).
- `bash backend/scripts/boundaries.sh` → "Camadas por contexto ... KEPT", "Fronteira de
  contexto ... KEPT", "Natureza: domain e use_cases nao falam com socket nem ssl ... KEPT" —
  **3 kept, 0 broken**, 146 arquivos, 665 dependências.
- `bash backend/scripts/natureza.sh` → universo 65 arquivos, **0 leitura(s) de relogio** —
  confirma `universe_at.py` como função pura de `domain` (`ADR-016`).
- `harness rules --mode sweep --changed-only` → saída vazia, `rc=0` sobre os 5 arquivos do diff
  (`backend/README.md`, `universe_at.py`, `test_universe_at.py`, `docs/INDEX.md`, o gate do
  builder). Nenhuma das 7 regras `[BLOQUEIO]` se aplica: os dois arquivos de código são domínio
  puro sem import relativo, sem `except` silencioso, sem `print`, sem credencial.
- `git diff --name-only master...HEAD` → 5 arquivos, igual ao declarado pelo builder.

Isolado: `pytest tests/sentimento/test_universe_at.py -v` → **9 passed em 0,20s**.

## Mutações reais plantadas (planta → confirma que o teste morde → reverte → `git diff` limpo)

Todas revertidas com `git checkout -- backend/src/modules/sentimento/domain/universe_at.py`;
`git status --short` ao final mostra só `frontend/node_modules` (untracked, pré-existente, não
relacionado a esta task) — `git diff --stat` vazio.

**Mutação 1 — quebra a UNIÃO das duas testemunhas** (`symbols=decided_symbols | s3_witness_symbols`
→ `symbols=decided_symbols`, um merge que descarta a testemunha S3 em silêncio): **3 testes
morderam** — `test_d7_7_includes_icxusdt_and_excludes_dosusdt_with_no_snapshot_available`,
`test_retrospective_result_carries_the_s3_witness_as_the_whole_union`,
`test_maticusdt_absent_from_snapshot_but_present_in_s3_witness_is_a_marked_divergence`. Prova a
alegação "UNIÃO das duas testemunhas, nunca merge silencioso" com defeito real, não com leitura de
código.

**Mutação 2 — tenta alimentar a decisão com `s3_inferred` de verdade, no CALL SITE real** (não no
arquivo-escrutínio descartável do builder): inseri `witnesses[S3_INFERRED] = s3_witness_symbols`
dentro de `universe_at`, antes de `decide_universe_membership(witnesses)`.
- `mypy --strict src/modules/sentimento/domain/universe_at.py` → **recusa de verdade, no arquivo
  de produção**: `error: Invalid index type "Literal['snapshot', 's3_inferred',
  'premium_index_witness']" for "dict[Literal['snapshot', 'premium_index_witness'],
  frozenset[str]]"; expected type "Literal['snapshot', 'premium_index_witness']"  [index]`. Isto é
  mais forte que a evidência do builder (que testou num arquivo fora da árvore) — prova a recusa
  POR TIPO no site de uso real, não num scratch.
- `pytest tests/sentimento/test_universe_at.py -q` com a mesma mutação (para checar a segunda
  camada de defesa, já que Python não impede a mutação em runtime) → **3 testes morderam também**
  em runtime (`decided_symbols`/`divergence` contaminados por `s3_inferred`), confirmando que a
  suíte pega o vazamento mesmo se alguém ignorasse o erro do `mypy`.

**Mutação 3 — planta o literal `"s3_inferred"` DENTRO do corpo executável real de
`decide_universe_membership`** (não a manipulação de string que o próprio teste já faz
internamente sobre sua cópia da fonte — isto edita o arquivo de verdade):
```python
if "s3_inferred" in decided:
    decided = decided - {"s3_inferred"}
```
`pytest ...::test_structural_falsifier_decide_universe_membership_cannot_spell_s3_inferred` →
**morde**: `AssertionError: assert 's3_inferred' not in {'s3_inferred'}`. Prova que o scanner AST
detecta uma mutação real no arquivo, não só o mutante sintético que o próprio teste constrói.

Após reverter as três: `pytest tests/sentimento/test_universe_at.py -q` → 9 passed;
`mypy --strict src/.../universe_at.py tests/.../test_universe_at.py` → "Success: no issues found
in 2 source files".

## DoD, item a item

- [OK] `universe_at(ts, filtro)` como função pura de `domain` — `natureza.sh` mede 0 leituras de
  relógio no universo de 65 arquivos; sem socket/`ssl` (`boundaries.sh` KEPT).
- [OK] `s3_inferred` inadmissível POR TIPO no caminho de decisão, não por `if`/validação —
  confirmado por Mutação 2 acima (recusa real de `mypy --strict` no call site de produção,
  não paráfrase do relatório do builder) + `test_decisive_universe_source_excludes_s3_inferred_
  by_member_set` (`get_args`) + `test_structural_falsifier_...` (AST), este último confirmado
  contra mutação real de arquivo pela Mutação 3.
- [OK] `D7.7`: `universe_at('2025-08-01')` inclui `ICXUSDT`, exclui `DOSUSDT` — confirmado, e a
  Mutação 1 prova que o teste realmente depende do comportamento de união correto, não é
  tautológico quanto a ESSA parte da alegação.
- [OK] União das duas testemunhas com divergência marcada, nunca merge silencioso — Mutação 1
  morde 3 testes; `divergence` reusa `compare_symbol_sets` de `instrument_universe_snapshot.py`
  (grep confirma o import, nenhuma terceira representação inventada).
- [OK] `docs/INDEX.md` append-only — `git diff master...HEAD -- docs/INDEX.md` mostra só `+1`
  linha no fim, nenhuma linha existente tocada.

## Cobertura

`universe_at.py` fecha em **100%** (medido dentro da rodada de `test.sh` acima, 40 statements/6
branches, 0 missing) contra o piso declarado domain **90%** — folga de 10pp mesmo isolando o
arquivo; a camada inteira fecha 99,8%.

## Observação — não bloqueante, registrada para quem consumir esta função depois

`universe_at` **não consulta `ts` para computar nada** — `ts` é um campo de passagem
(`UniverseAtResult.ts`), e `snapshot_rows`/`s3_witness_symbols` chegam PRÉ-FILTRADOS pelo
chamador. Logo, o `D7.7` como escrito na suíte (`_s3_witness_symbols_2025_08_01()` é um
`frozenset` fabricado à mão, não derivado de um dump S3 real com datas de onboarding) prova a
MECÂNICA de união/rótulo usando os símbolos que o DoD cita — não prova, dentro deste repositório,
que existe um cálculo real "que símbolo existia em `ts`" a partir de dado bruto. O handoff já
nomeia isto como escopo explicitamente NÃO fechado por esta task ("não persiste `onboard_ts`/
`deliveryDate` por símbolo... extensão pertence a `SPEC-001` §3.4/`T-02.1`"), e o docstring do
teste (linhas 1-10) é honesto sobre isso ("used only to exercise the DECISIVE-PATH mechanism...
not to claim it is the true universe on any date this suite names"). Não é defeito desta task —
é lacuna que `T-07.14`/`T-07.15` (ou uma extensão de `T-02.1`) terá de fechar antes que
`universe_at` decida um `ts` real sem depender de um chamador já ter feito o trabalho de data.

## Veredito

## QA Gate — Fase 07 [sentimento]
- [OK] core.relative-import — `harness rules --mode sweep --changed-only`: saída vazia, rc=0
- [OK] core.silent-except — idem
- [OK] core.print-statement — idem
- [OK] core.hardcoded-secret — idem
- [OK] web-fullstack.browser-imports-server — N/A (sem código de `web`/navegador nesta task)
- [OK] web-fullstack.tenant-from-request — N/A (sem request/tenant nesta task)
- [OK] web-fullstack.server-test-directory-present — N/A (regra de presença de diretório, não de
  diff; `backend/tests/` já existe)
- [OK] Testes existem e passam — `bash backend/scripts/test.sh` (1162 testes, rc=0, 290,52s)
- [OK] Cobertura 97,47% total / `universe_at.py` 100% contra alvo domain 90% (`harness policy
  --key test_cmd` + `ADR-009/D1`)
- [OK] DoD da fase — 5/5 itens, cada um com mutação real ou grep citável acima
- [anomalia] nenhuma
Regras bloqueantes avaliadas: 7 de 7 listadas por `harness rules list --severity block`
Veredito: APPROVED
