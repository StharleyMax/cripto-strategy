[QA GATE — Fase 06: `series_catalog` como contrato lido pelos testes]
Feature: `plataforma-dados`     Componente: `sentimento`     Task raiz: `T-06.1` (destrava T-06.2..T-06.10)
PR: https://github.com/StharleyMax/cripto-strategy/pull/96 (branch `tasks/T-06.1-fase06`)
Worktree: `/tmp/claude-1002/wt/T-06.1`
Arquivos avaliados:
- `backend/src/modules/sentimento/domain/series_catalog.py` (novo, produção)
- `backend/tests/sentimento/test_series_catalog.py` (novo, 18 `def test_*`, 27 casos coletados com parametrize)
- `backend/README.md` (seção nova, append), `docs/INDEX.md` (linha nova, append)
- Handoff: `docs/context/plataforma-dados/handoff/T-06.1.md`
- Relatório do builder: `docs/context/plataforma-dados/gates/T-06.1-builder.md`

## Suite existente rodada antes de qualquer teste novo (procedimento §1)
- `bash backend/scripts/test.sh` (suite completa, com coverage): **1277 passed**, 0 failed,
  274,16s — `Coverage XML written`, `Required test coverage of 70.0% reached. Total coverage:
  97.61%`. Camadas (`check-coverage-layers.sh`, `ADR-009/D1`): domain **99,8%**
  (2499/2503, meta 90%), use_cases **100,0%** (585/585, meta 80%), infra **95,1%**
  (2208/2321, meta 70%) — 3/3 `[OK]`. Bate com o relatório do builder, número a número.
- `bash backend/scripts/test.sh tests/sentimento/test_series_catalog.py --no-cov -v`:
  **27 passed** (18 funções `def test_*`, expandidas por `@pytest.mark.parametrize` — o
  builder relatou "26 testes novos"; a contagem de itens coletados por `pytest` é 27, não 26.
  Não é defeito de teste, é imprecisão de rótulo no relatório do builder — registrado aqui,
  não bloqueia).
- `bash backend/scripts/lint.sh`: `ruff check` + `ruff format --check` + `mypy --strict` →
  sem achado, 233 arquivos.
- `bash backend/scripts/boundaries.sh`: 3 kept, 0 broken (155 arquivos, 703 dependências) —
  inclui `Natureza: domain e use_cases nao falam com socket nem ssl KEPT`.
- `bash backend/scripts/natureza.sh`: 70 arquivos (51 domain + 19 use_cases), 0 leituras de
  relógio.
- `harness rules --mode file --path backend/src/modules/sentimento/domain/series_catalog.py`
  → `rc=0`. Idem para `test_series_catalog.py` → `rc=0`. `harness rules --mode sweep
  --changed-only` → `rc=0`.

## Mutação real — falsificador central e cada campo obrigatório do DoD

Procedimento em todos os casos: `Edit` no arquivo de produção → roda o teste-alvo →
confirma `FAILED` → `git checkout -- <arquivo>` → confirma `git status --porcelain` limpo.
Todas as seis mutações abaixo foram plantadas, capturadas e revertidas nesta sessão
(evidência bruta não citada aqui por economia — reproduzível pelos comandos).

1. **`unit`/`denom` obrigatórios** — mutação: `SeriesKey.__post_init__` pula a checagem de
   branco para `unit`/`denom` (`if term in ("unit","denom"): continue`). Resultado:
   `test_catalog_row_refuses_a_blank_identity_term_via_the_key[unit]` e `[denom]` →
   `FAILED: DID NOT RAISE IncompleteSeriesKeyError` (o `[verified_by]` do mesmo parametrize
   continuou passando, como esperado — a mutação foi cirúrgica). **Pega.** Revertido,
   `git status --porcelain` limpo.
2. **`native_grid` obrigatório por linha** — mutação: remove o `if not
   self.native_grid.strip(): raise ...` de `SeriesCatalogEntry.__post_init__`. Resultado:
   `test_catalog_row_refuses_a_blank_native_grid` → `FAILED: DID NOT RAISE
   InvalidCatalogEntryError`. **Pega.** Revertido, limpo.
3. **`max_staleness_ms` obrigatório/positivo** — mutação: troca `<= 0` por `< -999999`.
   Resultado: `test_catalog_row_refuses_a_non_positive_max_staleness_ms[0]` e `[-1]` →
   `FAILED: DID NOT RAISE`. **Pega.** Revertido, limpo.
4. **Erro publicado `(mediana, p99, n)` — as duas direções simétricas** — mutação: substitui
   as duas checagens (`reconstructed_from without published_error` e o inverso) por `pass`.
   Resultado: `test_reconstruction_without_published_error_is_refused` **e**
   `test_published_error_without_a_declared_reconstruction_is_refused` → ambos `FAILED: DID
   NOT RAISE`. **Pega nas duas direções**, confirmando o teste simétrico pedido pelo DoD.
   Revertido, limpo.
5. **Unicidade de `SeriesKey`** — mutação: `if False and key_id in seen:` em
   `SeriesCatalog.__post_init__`. Resultado:
   `test_build_series_catalog_refuses_two_rows_for_the_same_series_key` → `FAILED: DID NOT
   RAISE DuplicateSeriesKeyError`. **Pega.** Revertido, limpo.
6. **Falsificador central** ("o teste lê o catálogo; catálogo e teste não podem divergir sem
   reprovar"): as mutações 1–5 acima SÃO essa prova — cada uma ataca a validação real de
   produção (em `series_key.py` ou `series_catalog.py`) e o teste correspondente falha, sem
   que nenhum teste tenha um valor esperado hardcoded que mascare a quebra. Inspeção adicional
   do arquivo de teste: todo `pytest.raises` referencia um dos tipos importados de
   `src.modules.sentimento.domain.series_catalog`/`series_key`
   (`IncompleteSeriesKeyError`, `InvalidCatalogEntryError`, `InvalidPriceUseError`,
   `DuplicateSeriesKeyError`) — nenhum valor esperado é uma string ou número copiado à mão
   que não venha do módulo de produção. `binance_oi_key`/`binance_oi_entry` são fixtures que
   CONSTROEM entradas via os tipos reais, não duplicam a validação.

`git status --porcelain -- src/ tests/` ao final de todas as mutações: **vazio** (confirmado
duas vezes, após a mutação 3 e após a 5/6).

## Itens do DoD, um a um

- [OK] `series_catalog`: uma linha por `SeriesKey`, 15 termos reusados de `series_key.py`
  (`T-04.2`), não redefinidos — `series_catalog.py:115` (`key: SeriesKey`), comentário
  explícito linhas 21-26 sobre não duplicar `unit`/`denom`/`label_shift`/`verified_by`.
- [OK] `unit`/`denom` obrigatórios, verbatim — via `SeriesKey.__post_init__`
  (`series_key.py:178-186`), mutação 1 confirma que a checagem é real e exercida pelo
  catálogo, não simulada no teste.
- [OK] `native_grid` é campo por linha, lido em runtime, não constante — `series_catalog.py:116`
  (campo de instância, não módulo), `test_native_grid_is_a_per_row_field_not_a_shared_constant`
  constrói duas linhas com `1min`/`5min` lado a lado na mesma `SeriesCatalog` sem conflito;
  mutação 2 confirma obrigatoriedade real.
- [OK] `label_shift` com `verified_by` apontando IDENTIFICAÇÃO de teste, não booleano —
  `verified_by: str` em `series_key.py:176`, valor no fixture é uma string de caminho de
  teste (`"test_series_catalog.py::test_a_complete_entry_builds"`,
  `test_series_catalog.py:58`), e `test_catalog_row_refuses_a_key_with_no_label_shift_witness`
  prova que `verified_by=""` é recusado.
- [OK] `max_staleness_ms` obrigatório — mutação 3.
- [OK] Erro publicado `(mediana, p99, n)` obrigatório quando reconstrução, com teste simétrico
  (com e sem os 3 campos) — `test_reconstruction_without_published_error_is_refused` (falta
  o erro) e `test_published_error_without_a_declared_reconstruction_is_refused` (erro sem
  reconstrução) — mutação 4 confirma ambas as direções.
- [OK] Duas linhas para a mesma `SeriesKey` rejeitadas —
  `test_build_series_catalog_refuses_two_rows_for_the_same_series_key`, mutação 5.
- [OK] Escopo NÃO invadido — `grep -niE "quarantine|available_at|lag_ms|MODELED"` sobre os
  dois arquivos novos só acha as MENÇÕES em comentário que apontam para `T-06.6`/`quarantine
  _terms.py` como fora de escopo (`series_catalog.py:29-30`); nenhuma implementação de
  predicado de quarentena. `git diff --name-only master...HEAD` não cria nenhum arquivo
  `infra/*_series_catalog_store.py` nem popula shifts/séries reais — só a fixture
  `binance_oi_key`/`binance_oi_entry` e uma entrada de reconstrução (`coinalyze_bv`-like)
  usadas exclusivamente pelo teste. Confirma o handoff (linha 28) e o doc delta do builder.
- [OK] Contrato `Natureza` (`ADR-016`) — `series_catalog.py` em `domain/`, imports
  (`collections.abc`, `dataclasses`, `decimal`, `series_key`) sem I/O; `boundaries.sh` →
  3 kept incluindo a checagem `domain e use_cases nao falam com socket nem ssl`; `natureza.sh`
  → 0 leituras de relógio no universo de 70 arquivos.

## Regras bloqueantes (severidade block)

- [OK] `core.relative-import` — `harness rules --mode file` nos dois arquivos → `rc=0`, e
  inspeção confirma import absoluto (`from src.modules.sentimento.domain.series_key import
  SeriesKey`, `series_catalog.py:40`).
- [OK] `core.silent-except` — nenhum `except` no arquivo de produção (todo controle é
  `raise` em `__post_init__`); `rc=0`.
- [OK] `core.print-statement` — nenhuma chamada `print(...)`; `rc=0`.
- [OK] `core.hardcoded-secret` — nenhuma credencial; `rc=0`.
- [OK] `web-fullstack.browser-imports-server` — não aplicável (sem código de navegador
  tocado); `rc=0` no sweep.
- [OK] `web-fullstack.tenant-from-request` — não aplicável; `rc=0`.
- [OK] `web-fullstack.server-test-directory-present` — não aplicável a esta task (não mexe
  em servidor web); presença geral do diretório de testes já é verdadeira no repo.
- Mensagens de exceção — todas em inglês, verificadas por leitura direta do arquivo
  (`series_catalog.py:92-153`, `series_key.py:183-190`): nenhuma string em português.

Regras bloqueantes avaliadas: 7 de 7 listadas por `harness rules list --severity block`.

## Achado de escopo (não bloqueante, para o review avaliar)
Nenhum. A task respeitou os limites declarados no handoff — não construiu a quarentena de
3 termos (`T-06.6`) nem povoou séries/shifts reais de produção (`T-06.2`/`6.3`/`6.4`/`6.9`).

## Nitpick não bloqueante
O relatório do builder rotula "26 testes novos"; a contagem real por `pytest` é **27 itens
coletados** (18 funções `def test_*`, com `@pytest.mark.parametrize` expandindo 3 delas em
mais casos). Não afeta o veredito — é imprecisão de rótulo, não de cobertura ou de teste
faltante.

Veredito: APPROVED

## Anomalia no registro do veredito — escalada, não resolvida aqui
`harness gate-record plataforma-dados 06 QA APPROVED "..."` retornou **exit 4**, recusado
pelo documento de tarefas (não pela avaliação desta task): 11 de 14 tarefas da **fase 08**
(`T-08.1, T-08.3, T-08.4, T-08.6, T-08.7, T-08.8, T-08.9, T-08.10, T-08.12, T-08.13,
T-08.14`) estão em estado não-terminal, contradizendo um veredito QA APPROVED anterior de
`2026-09-02T18:59:07Z`. Isso é anterior e não-relacionado a `T-06.1`/fase 06. Conforme
instrução de protocolo desta task, **não executei `harness pipeline override`** — esse é
o escape único e é ato do orquestrador/owner, não do QA. **O veredito de conteúdo desta
task permanece APPROVED; o registro no ledger está pendente até a anomalia da fase 08 ser
resolvida ou explicitamente sobrescrita por quem tem autoridade para isso.**
