# T-05.14 — build (builder), rodada 2 — resposta ao NEEDS_FIX do QA

Contra: `gates/T-05.14-qa.md` (veredito `NEEDS_FIX`, 5 achados `[FAIL]`), `ADR-019`,
`docs/plans/SPEC-001-plataforma-dados/05_fatia_visivel.md:74,87,225-226`. Nenhum arquivo de
produção (`ingest-health-query.ts`) mudou de comportamento nesta rodada — só os testes que
faltavam foram acrescentados, exatamente como o QA pediu. Worktree: `/tmp/claude-1002/wt/T-05.14`,
branch `tasks/T-05.14-cliente-rota-real`.

## Achado 1 — DoD `D5.14` MORDE(i) "servidor ausente" sem teste versionado

`frontend/src/features/s1-console/ingest-health-query-http.test.ts`: dois testes novos.

1. `"MORDE D5.14(i): porta sem listener nenhum ⇒ fetchIngestHealthProjectionViaHttp REJEITA"` —
   reserva uma porta OS-assigned via `net.createServer().listen(0, …)`, fecha o socket
   IMEDIATAMENTE (`probe.close()`), e chama `fetchIngestHealthProjectionViaHttp` contra ela.
   `ECONNREFUSED`, a `Promise` rejeita.
2. `"MORDE D5.14(i): servidor cai NO MEIO do fetch ⇒ a promise REJEITA, nunca resolve com corpo
   parcial/vazio"` — `http.createServer` que só manda o cabeçalho (`response.flushHeaders()`,
   nunca termina o corpo), `fetch` é disparado, 200ms depois `server.closeAllConnections()` +
   `server.close()` derrubam a conexão NO MEIO do request.

**Falsificador mostrado, não alegado**: mutei `fetchIngestHealthProjectionViaHttp` para engolir a
exceção do `doFetch` num `try/catch` e devolver uma projeção vazia em vez de rejeitar (patch
temporário, revertido byte a byte pelo `cp` do backup — `grep -n "MUTATION FOR T-05.14"
ingest-health-query.ts` → 0 linhas depois do revert). Rodei os dois testes novos com a mutação
viva:

```
node --test src/features/s1-console/ingest-health-query-http.test.ts
# -> "porta sem listener" REPROVOU (o teste pegou o engolimento)
# -> "servidor cai no meio" continuou passando (o ponto de falha dele é response.json(), fora
#    do try/catch mutado — o dois juntos cobrem os dois estágios: conexão recusada e conexão
#    derrubada em voo)
```

A mutação foi revertida antes de qualquer outro passo; suíte voltou a 48/48.

## Achado 2 — CALA "view-model montado" nunca exercitado sobre a rota real (G1, `ADR-019`)

`SERVER_SCRIPT` (embutido no arquivo de teste HTTP) ganhou um `MutationMode` novo,
`"view_model_multi_run"` — SEM mutação de servidor (`apply_mutation` trata como `"original"`),
só troca o dataset semeado por `build_runs_multi()`/`build_gaps_multi()`: 4 runs (2 deles
compartilhando `(source, endpoint)` em `started_at` diferentes, mesma forma do fixture que
`T-07.13` tinha antes da migração) + 1 gap.

Dois testes novos usam esse modo:

- `"CALA G1: fetchIngestHealthProjectionViaHttp -> S1ViewModel completo, sobre a rota real (4
  runs, 3 series, PARADO primeiro)"` — chama `collectorRowsFromIngestHealthProjection` E
  `buildS1ViewModelFromIngestHealthProjection` sobre `result.projection` (o retorno real de
  `fetchIngestHealthProjectionViaHttp`, não uma fixture TS pura): 4 runs → 3 séries (a chave
  compartilhada colapsa em 1, mantendo o run MAIS RECENTE — `r2`, `uptimePercent: 95`, não
  `r1`), `bybit` com `status: "PARADO"`/`uptimePercent: 0`, e `viewModel.rows[0].statusCell.status
  === "PARADO"` (D17: PARADO ordena primeiro).
- `"CALA D7.12: janela_de_perda esta presente e null na projecao real"` (ver achado 4).

## Achado 3 — `parseIngestHealthEnvelope` só tinha CALA testado

`frontend/src/features/s1-console/ingest-health-query.test.ts` ganhou 11 testes novos, puros
(sem CLI, sem servidor — `parseIngestHealthEnvelope` é chamada direto sobre um objeto JS), a
partir de um `validEnvelopeBody()` bem formado, clonado e mutado por teste:

corpo não-objeto (`null`/`undefined`/string/número/array) · `runs` ausente/não-array · `gaps`
ausente/não-array · coluna faltando em `runs[]` · coluna faltando em `gaps[]` · tipo errado numa
coluna `string` de `runs[]` · tipo errado numa coluna `nullable-number` de `runs[]` ·
`n_runs !== runs.length` · `n_gaps !== gaps.length` · `query` com nome diferente do esperado.
Cada `assert.throws` casa a mensagem exata que `assertEnvelopeFieldType`/
`assertIngestHealthRunRow`/`assertIngestHealthGapRow`/`parseIngestHealthEnvelope` emitem —
confirmando que a rejeição nomeia a coluna, não só "algo deu errado".

## Achado 4 — teste de `janela_de_perda: null` não migrado

`"CALA D7.12: janela_de_perda esta presente e null na projecao real — F0 nunca inventa a
formula"` (`ingest-health-query-http.test.ts`, mesmo `"view_model_multi_run"` fixture do achado
2) — itera as 4 `runs` do envelope REAL e confirma `janela_de_perda === null` em todas, mesma
asserção que o teste apagado tinha contra o CLI (`ingest_record.py:91`,
`LOSS_WINDOW_NOT_COMPUTED_IN_F0`), agora sobre `GET /ingest-health`.

## Achado 5 — doc delta ausente

Este arquivo + a linha nova em `docs/INDEX.md` (append, ver commit). `ADR-019` não muda de
status nesta rodada (segue `RASCUNHO`, aprovação é gate do owner) — nenhuma decisão nova, só a
cobertura que a decisão já prometia (D5, "Consequência").

## Comandos rodados, com universo (rodada 2)

```
npm --prefix frontend run test:s1
# -> tests 48 / pass 48 / fail 0   (era 33 pass antes desta rodada — 8 arquivo novo mantido +
#    13 novos: 2 MORDE(i) servidor ausente, 2 G1/D7.12 sobre a rota real, 11 MORDE de
#    parseIngestHealthEnvelope; universo: src/features/s1-console/*.test.ts, 4 arquivos)

node --test src/features/s1-console/ingest-health-query-http.test.ts   # (3x seguidas, checando flake)
# -> tests 8 / pass 8 / fail 0, todas as 3 rodadas

make lint-frontend
# -> npm run lint (eslint src) — limpo, saída vazia
# -> npm run typecheck (tsc -p tsconfig.json --noEmit --strict) — limpo, saída vazia

harness rules --mode sweep --changed-only
# -> rc=0, saída vazia
```

## O que NÃO mudou nesta rodada, registrado para não parecer omissão

- `ingest-health-query.ts` (produção): zero linha de comportamento alterada — só os dois testes
  que provaram e reverteram a mutação de falsificação (achado 1), nunca commitados vivos.
- `runIngestHealthCli`/`DoD-3` sobre CLI: intocado, fora do escopo de `T-05.14` (`ADR-019`,
  "Não fecha").
- Wiring em `S1Console.tsx`/`painel/page.tsx`: continua fora do DoD desta task, como `ADR-019`
  já registrava.

## Pendências que continuam do QA, fora do escopo desta rodada de correção

- `make verify` no worktree segue `INDETERMINADO` por mecanismo de `harness policy`/regras não
  resolvível fora da árvore principal — mesma classe de gap de ambiente que `T-05.11-builder.md`
  já registrou, não é defeito desta task. `harness rules --mode sweep --changed-only` (rodado
  acima) é o substituto direto, confirmado `rc=0`.
