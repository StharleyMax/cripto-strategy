# Gate — `/review` T-03.2 (`CST-18`, componente `sentimento`, fase 03, PR #56)

**Veredito: COMPLIANT**

## Denominador

- Regras bloqueantes em vigor: **7** (`harness rules list --severity block`, packs `core` +
  `web-fullstack`). Avaliadas: **7/7**, via `harness rules --mode sweep --changed-only` rodado no
  worktree `/tmp/claude-1002/wt/T-03.2` (branch `tasks/T-03.2-forceorder-liquidacao`, HEAD
  `bfc1753`) → **0 achados**.
- Contratos `import-linter`: **3/3** avaliados via `bash backend/scripts/boundaries.sh` → **3
  kept, 0 broken** (inclui o contrato "Natureza: domain e use_cases nao falam com socket nem
  ssl").
- Arquivos varridos: **6 novos** listados por `gh pr diff 56 --name-only` sob `backend/src` e
  `backend/tests` (mais `docs/INDEX.md` e `docs/medicao-conectividade-forceorder.md`, fora do
  universo de código):
  - `backend/src/modules/sentimento/domain/force_order_capture_outcome.py`
  - `backend/src/modules/sentimento/domain/force_order_envelope.py`
  - `backend/src/modules/sentimento/use_cases/capture_force_order_stream.py`
  - `backend/src/modules/sentimento/infra/force_order_raw_recorder.py`
  - `backend/src/modules/sentimento/infra/force_order_collector_cli.py`
  - `backend/tests/sentimento/test_force_order_collector.py`
- Cobertura por camada: `bash backend/scripts/check-coverage-layers.sh` → domain **100,0%**
  (piso 90), use_cases **100,0%** (piso 80), infra **98,5%** (piso 70) — as 3 camadas acima do
  piso declarado (`ADR-009/D1`).

## Achados

1. **`[INFO]` `estagio` — identificador local em português em código de produção** —
   `backend/src/modules/sentimento/infra/force_order_collector_cli.py:111` (e as duas f-strings
   que o citam, `:95` e `:113`). Viola a tabela de fronteira de idioma, linha 1
   (`identificador de produção → inglês`, `[PREMISSA-OWNER: 2026-08-29]`, `ADR-013/D3`) —
   `CLAUDE.md` §"Idioma de identificador". **Não é BLOCKER**: `CLAUDE.md` declara explicitamente
   que idioma de identificador é **convenção, não portão** (`ADR-011/D1.10` proíbe declarar
   `[[rules.own]]` de idioma), então não existe id de regra mecânica para citar — só a citação de
   documento. Correção concreta: renomear `estagio` → `stage` nas 3 ocorrências.

2. Extração de todos os identificadores (`ast`, `FunctionDef`/`ClassDef`/`Name`/`arg`/`Attribute`)
   dos 5 arquivos de produção novos não encontrou nenhuma outra ocorrência de português — `estagio`
   é a única.

## O que foi checado e passou (sem achado)

- **Contrato "Natureza"** (`import-linter`, `pyproject.toml:264-271`): nenhum dos módulos novos
  em `domain/` ou `use_cases/` importa `socket`/`ssl` — chamada de rede vive só em
  `infra/force_order_collector_cli.py` (via `connect_tls`, de `T-03.1`). `boundaries.sh`: KEPT.
- **Camadas** (`infra > use_cases > domain`): `force_order_collector_cli.py` (infra) importa
  domain + use_cases + infra; `capture_force_order_stream.py` (use_cases) importa só domain +
  use_cases; `force_order_envelope.py`/`force_order_capture_outcome.py` (domain) importam só
  domain. Nenhuma seta invertida.
- **`docs/INDEX.md` append-only**: `gh pr diff 56` mostra hunk `@@ -106,4 +106,5 @@` — a única
  mudança é uma linha nova ao final; nenhuma linha existente foi reescrita.
- **Pisos de cobertura**: domain 100/90, use_cases 100/80, infra 98,5/70 — todos acima.
- **Nomenclatura do vocabulário de componentes / segmentos de diretório**: falsificador de
  `CLAUDE.md` (`git ls-tree` + `awk` + `grep -vxE`) não acusa nenhum segmento novo em português
  sob `backend/src|tests`.

## Relatório do builder consultado

`docs/context/plataforma-dados/gates/T-03.2-builder.md` (via `gh pr diff 56`).
