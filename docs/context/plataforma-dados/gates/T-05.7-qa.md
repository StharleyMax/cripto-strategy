# QA Gate — T-05.7 (plataforma-dados, fase 05, charts)

## QA Gate — Fase 05 [charts] T-05.7 "Cor como token nomeado por papel; critical fora do canal de cor" (CST-41)

**Ambiente:** worktree isolada `/home/stharley/Documentos/projects/cripto-strategy/.claude/worktrees/wf_2a5f5015-5f1-9`, checkout **detached** em `5844bd4` (a branch `tasks/T-05.7-cor-token` já estava aberta em outra worktree; `git ls-remote origin refs/heads/tasks/T-05.7-cor-token` confirma `5844bd4eedabbb7c544190b94994d0e58afd994c` = ponta remota — mesmo conteúdo). `data/` recriado como symlink não rastreado (`git status --porcelain` → só `?? data`), necessário para os testes que leem fixture real.

Diff sob teste (`git diff --stat 6ecb9ae..5844bd4 -- frontend/src frontend/README.md`): `color-tokens.ts` (novo, 142 linhas), `color-tokens.test.ts` (novo, 145 linhas), `s2-headless-run.ts` (+27/−0, aditivo — campo `style?` default `{}`), `s2-axis-integration.test.ts` (+1 assert real contra `series.options()`), `frontend/README.md` §13.

- [OK] `core.*`/`web-fullstack.*` (7 regras bloqueantes listadas por `harness rules list --severity block`) — `harness rules --mode sweep --changed-only` → **saída vazia, rc=0** (0 achados sobre o diff desta task); `harness rules --mode sweep` completo (universo do repo) só acusa avisos (`[AVISO]`, severidade warn) em arquivos fora do diff de `T-05.7` (sentimento/`web-fullstack.hardcoded-url` pré-existentes) — nenhum bloqueante no escopo desta task.
- [OK] Testes existem e passam — `npm --prefix frontend run test:charts` (reproduzido do zero, não confiei no relato do builder) → **76 pass, 0 fail** (`ℹ tests 76 / ℹ pass 76 / ℹ fail 0`), batendo com o número do builder report. Isolamento do delta: não re-verifiquei o commit-pai (o builder já mediu 69→76, delta 7, e a contagem de `color-tokens.test.ts` isolado bate: 5 `test()` + 2 gerados pelo laço `for (mode of ["light","dark"])`).
- [OK] `npm --prefix frontend install` → `added 128 packages, and audited 129 packages, 0 vulnerabilities` (rc=0).
- [OK] `npm --prefix frontend run lint` → `eslint src`, **rc=0, saída vazia** (0 erro/aviso).
- [OK] DoD **D5.6** (`docs/plans/SPEC-001-plataforma-dados/05_fatia_visivel.md:34`) — comando `node scripts/validate_palette.js` → **`rc=0`**, `"medicoes executadas nesta rodada: 361"`, `VEREDITO: passa nos criterios ... sob as TRES dicromacias`. Par falsificador reproduzido na saída literal: `#f23645 #eb6834  14.0  5.3  7.0  15.0  5.3  FAIL` — confirma que `critical` (vermelho TradingView) não cabe no canal de cor contra o laranja, exatamente como o DoD exige.
- [OK] Item **5.9** do plano (`CA-F4-10`, "Cor como token nomeado por papel; `critical` fora do canal de cor") — verificado em duas camadas: **estática** (`ColorRole` é união fechada de 6 papéis, sem `critical`/`severidade` — `frontend/src/charts/color-tokens.ts:47-53`) e **runtime** (`assertNoForbiddenColorRoles` + `FORBIDDEN_COLOR_ROLE_SUBSTRINGS`, `color-tokens.ts:65-85`).
- [OK] **Mutação plantada e morta** — provei o defeito antes de aceitar o verde: substituí `assertNoForbiddenColorRoles` por um no-op (`return;` sem lançar), independente do que o builder já tinha testado. Resultado: `npm --prefix frontend run test:charts` → **1 fail** (`actual: 1, expected: 0`), teste morto: `"the negative control: a role named like operational severity is REJECTED..."` com `AssertionError [ERR_ASSERTION]: Missing expected exception ... expected: /critical/i`. Revertido (`cp` do original salvo antes; `git diff` vazio depois), suíte volta a **76 pass, 0 fail**. A guarda de `CA-F4-10` não é vácua — o teste efetivamente a exercita e falha quando ela quebra.
- [OK] Escopo — `candlestickSeriesColors` deriva as 6 chaves de `CandlestickStyleOptions` de EXATAMENTE 2 tokens (`directionUpFill`/`directionDownFill`), sem hue ad hoc; round-trip real contra `lightweight-charts` via `series.options()` lido de volta (não o objeto passado) em `s2-axis-integration.test.ts:162-167`. `acao-*`/`foco` (chrome DOM) ficam fora por desenho — dentro do boundary `charts`⇄`web` (`ADR-003` FR-1, `D5.12`), sem import cruzado (`D5.12 MORDE+CALA` — 4 testes existentes, todos passando).
- [OK] Cobertura — sem piso numérico declarado para `charts` (`harness policy --key test_cmd` só cobre `sentimento`: `{"sentimento": {...}}`); não há alvo contra o qual reprovar. 7 testes novos cobrem: 2 modos × cross-check hex-a-hex contra `scripts/validate_palette.js` (lido como texto, nunca executado), o par falsificador do D5.6, `candlestickSeriesColors` (round-trip de 2 tokens), guarda positiva e guarda negativa (4 substrings rejeitadas, incluindo case-insensitivity).
- [anomalia] Nenhuma que impediu veredito. Observação não-bloqueante: ainda não existe componente `.tsx` real consumindo `candlestickSeriesColors` (só o headless round-trip contra a biblioteca real) — consistente com o escopo declarado da task (`charts` = geometria/dados, não renderização DOM; `find frontend/src -name '*.tsx'` → só `Filter.tsx`/`S3Inspector.tsx`/`S1Console.tsx`, nenhum é o painel S2). A wiring visual é tarefa futura de `web`, fora de `CA-F4-10`.

Regras bloqueantes avaliadas: 7 de 7 listadas por `harness rules list --severity block`.

Veredito: **APPROVED**

