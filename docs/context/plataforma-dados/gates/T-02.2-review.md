# Review Gate — T-02.2 (CST-13), Fase 02, componente `sentimento` — PR #61

**Worktree:** `/tmp/claude-1002/wt/T-02.2` · **Branch:** `tasks/T-02.2-coinalyze-one-shot` (`58911ea`, `0df3128`)
**Veredito: COMPLIANT**

## Denominador
7/7 regras bloqueantes avaliadas (`harness rules list --severity block`), via `harness rules --mode sweep`
na worktree → `rc=0`, 0 achados (verificação independente, não apenas citação do relatório do builder).
16 arquivos alterados (8 src novos + 7 test novos + `test_as_of_is_the_single_reader.py` modificado +
`backend/README.md`). Contratos import-linter: 3/3 avaliados via `bash scripts/boundaries.sh` →
`Contracts: 3 kept, 0 broken` (80 arquivos, 296 dependências) — reproduzido por mim.

## Achados
Nenhuma violação bloqueante. Nenhuma chave/segredo em nenhum arquivo (`grep` por literal de 32+ hex e por
atribuição direta de API key: zero ocorrências fora de `os.environ`/`Final[str]` de constantes não
sensíveis). Idioma de identificador: 100% inglês em `def`/`class` dos 8 módulos novos (`grep` manual,
zero achado em português). `Natureza` (domain/use_cases não importam `socket`/`ssl`): confirmado por
leitura direta dos imports dos 3 arquivos `domain` e 1 `use_cases` novos — nenhum importa transporte.

## O guard de T-04.4 (ADR-006) — julgamento de mérito, não só citação do builder
`DECLARED_TOUCHERS` ganhou `sqlite_series_quarantine_store.py: {"record"}`. Avaliado como **exceção
legítima**, não bypass: `record()` só grava `entry.available_at` na tupla do `INSERT` — nunca compara
contra um instante de decisão `t`, mesma categoria de `provenance.reject_clock_skew` já declarada.
`read_promoted()` filtra `available_at IS NOT NULL` dentro de uma string SQL, nunca como atributo Python;
o `ast.Attribute` do scanner genuinamente não alcança string, e a query não computa "valor no instante
t" — é um filtro de presença para promoção futura, categoria diferente da que o guard protege (segundo
leitor de `as_of`). Reproduzi a suíte do guard intacta (`test_as_of_is_the_single_reader.py`, isolada,
11 testes) → verde; e confirmei via `entry.available_at` sempre `None` hoje (`COINALYZE_ONE_SHOT_TERMS.
available_at_present=False`), então nenhuma linha desta task jamais é promovida.

## Verificação mecânica independente (não confiei nos relatórios sem rodar)
- `bash scripts/boundaries.sh` → 3 kept, 0 broken.
- `bash scripts/natureza.sh` → universo de 32 arquivos, 0 leitura de relógio.
- `harness rules --mode sweep` → rc=0.
- `.venv/bin/python -m pytest` (TMPDIR isolado, para evitar a colisão de `/tmp/pytest-of-*`
  observada sob contenção de outros worktrees ativos no mesmo host — mesmo artefato que o builder já
  documentou para cobertura) — 74/74 testes novos + guard verdes, incluindo o falsificador `D2.6`
  (`test_d2_6_the_two_freshly_captured_series_read_zero_promoted_rows`).
- Cobertura por camada aceita como `[DOC]` do relatório do builder/QA (domain 99,9%/90, use_cases
  100%/80, infra 98,4%/70) — não remedida por mim por custo de tempo, mas os 8 arquivos novos aparecem
  a 93–100% na tabela `coverage.xml` que eu mesmo gerei numa rodada completa.

## Nota operacional, fora do veredito
Rodadas de suíte completa neste host produziram, transitoriamente, 2 falsos-negativos em
`test_sqlite_series_quarantine_store.py` quando outro worktree (`T-02.1`) rodava pytest concorrente —
mesmo `TMPDIR` compartilhado. Reproduzido e descartado como artefato de ambiente, não de código
(isolando `TMPDIR` o resultado volta a 100% verde). Não é achado de arquitetura; registrado para não
ser confundido com regressão por quem rodar de novo neste host.
