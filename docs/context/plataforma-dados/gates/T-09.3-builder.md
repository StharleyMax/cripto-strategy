# T-09.3 — builder report (self-attested, NOT final QA/review)

**Task:** `[docs] 09 · Registro consolidado de ADR-004 e do finalista do motor, com os números
que o spike de 08 produziu` (`CST-85`). **Componente:** `docs`. **Status:** implementado;
`tasks.toml` continua `todo` — não me atribuo `done`, `APPROVED` nem `COMPLIANT`. O orquestrador
despacha `/qa` e `/review` independentes.

## O que a task pedia

Plano `09` itens `9.4` (registro consolidado de `ADR-004` — decisão já é gate da fase `03`,
registro é aqui) e `9.5` (registro do finalista do motor com os números do spike `08`), com
`DoD D9.4`: "o finalista de motor está registrado com números medidos, não com preferência" —
comando `ler ADR-002 atualizado`, universo `free -m, df -h, região, e os 5 critérios do spike —
todos com valor`. Não escreve código de produção (fronteira explícita da fase `09`, "Não faz").

## O que foi construído — duas emendas, nenhum arquivo novo

| arquivo | o que a emenda faz |
|---|---|
| `docs/adr/ADR-004-reconexao-de-stream-sem-sequencia.md` (**modificado**, emenda append) | Confere as três classes de `ADR-004` contra o código real, arquivo por arquivo, não por afirmação |
| `docs/adr/ADR-002-motor-de-armazenamento.md` (**modificado**, emenda append) | Verifica `D9.4` linha a linha contra a decisão já registrada por `T-08.1` (emenda 2026-09-04, já mergeada) |

Nenhum header de ADR foi editado (`Status: proposto` continua em ambos) — mesmo padrão que
`ADR-002` já demonstrava antes desta task (`D4` decidido, header intocado): estado vive nas
emendas append-only, não no cabeçalho.

### ADR-004 — o que a auditoria achou, por classe

- **Classe B (completa):** B1-B4 têm arquivo e teste, citando `ADR-004` pelo nome. `grep -c
  "ADR-004" backend/tests/sentimento/test_force_order_reconnection.py` → **5** ocorrências sobre
  **25** funções de teste. `T-03.3`: **897 passed**, cobertura **98,06%** (`backend/README.md:2234-2238`).
- **Classe C (satisfeita, sem citação):** C1 (`oi_history_paginator.py`, `ClosedWindow`) e C2
  (`binance_oi_history_client.py`, sempre `startTime`+`endTime`) fazem exatamente o que `ADR-004`
  pede, mas `grep -c "ADR-004"` nos dois arquivos → **0** — chegaram à regra por `SPEC-001 §5.7`/
  `D7.3`/`D7.4` (`T-07.1`), rota independente. Registrado como observação, não bloqueio.
  **Achado e reparado durante a auditoria**: a primeira leitura via `grep -rn "ADR-004"
  backend/src/modules/sentimento/infra/binance_oi_history_client.py` fora do universo certo
  teria escondido essa lacuna textual — só apareceu ao contar as citações nos dois arquivos junto.
- **Classe A (parcial — o achado que mais importa):** A2/A4 (identidade/contiguidade por
  `agg_id`, nunca tempo/`trade_id`) construídos e testados: `domain/aggtrade_contiguity.py`,
  **27** testes (`18+9`, `test_aggtrade_contiguity.py`+`_fixtures.py`) contra dump real. **A1
  (sobreposição na reconexão) e A3 (buraco → `md.ingest_gap`) NÃO têm implementação**:
  `grep -rniE "reconnect.*aggtrade|aggtrade.*reconnect|overlap.*agg_id" backend/src backend/tests`
  → 1 ocorrência, e é um comentário de teste sobre unicidade ENTRE ARQUIVOS de dump, não
  reconexão ao vivo — **0 reais**. `grep -n "Classe A" docs/context/plataforma-dados/tasks.toml`
  → **0** fora deste próprio registro: **nenhuma task do plano constrói A1/A3.** Nomeado como
  bloqueado na própria emenda — não é defeito desta task consertar (componente `docs`), mas
  ficaria como dívida silenciosa se não fosse escrito.

### ADR-002 — verificação de `D9.4`, não nova decisão

`D4` já foi decidido por `T-08.1` (PR #123, mergeado antes desta task começar). A emenda desta
task **não redecide nada** — confere, linha a linha, contra o texto já existente: `free -m`
(153,8 MiB RSS + 5.219 MB livres na VPS), `df -h` (~900 KB delta + 75 GB livres), 5 critérios
(espaço 1,506×, backtest 0,05-0,09s, fixture bit-idêntica) — **todos com valor**. **Região da
VPS continua `[NÃO MEDIDO]`** — a própria ADR já classificava esse número como "coluna de F0,
janela fecha quando o primeiro coletor liga", não como bloqueio de `D4`; medi-la exige acesso à
VPS, que nem `T-08.1` teve. Rastreada por `[GAP G7]`, dono `T-03.9` (`tasks_review.md`), fora do
escopo de uma task `docs` de fase `09`. Nomeado explicitamente, não fabricado.

## Comandos rodados nesta sessão, literais

```bash
grep -c "ADR-004" backend/tests/sentimento/test_force_order_reconnection.py          # -> 5
grep -c "def test_" backend/tests/sentimento/test_force_order_reconnection.py        # -> 25
grep -c "def test_" backend/tests/sentimento/test_aggtrade_contiguity.py \
  backend/tests/sentimento/test_aggtrade_contiguity_fixtures.py                       # -> 18 / 9
grep -c "ADR-004" backend/src/modules/sentimento/domain/oi_history_paginator.py \
  backend/src/modules/sentimento/infra/binance_oi_history_client.py                   # -> 0 / 0
grep -rniE "reconnect.*aggtrade|aggtrade.*reconnect|overlap.*agg_id" \
  backend/src backend/tests                                                           # -> 1 (falso positivo, comentário)
grep -n "Classe A" docs/context/plataforma-dados/tasks.toml                           # -> 0
harness rules --mode sweep --changed-only
  -> rc=0, saída vazia (universo: 2 arquivos alterados — ambos docs/adr/*.md)
```

## Cobertura

Não há piso declarado para `docs` (`harness policy --key test_cmd` só cobre `sentimento`, mesmo
padrão que `T-09.2` já registrou). Universo desta task: 2 arquivos de ADR, ambos append-only,
zero código de produção. O falsificador aqui é o próprio grep de ausência (A1/A3, citação
`ADR-004` em Classe C) — mostrado, não alegado, com o comando que produz o `0`.

## Doc delta

`docs/adr/ADR-004-reconexao-de-stream-sem-sequencia.md` — **atualizado**, emenda append
("Registro consolidado — fase 09"). `docs/adr/ADR-002-motor-de-armazenamento.md` — **atualizado**,
emenda append ("Verificação de fechamento — fase 09"). `docs/plans/SPEC-001-plataforma-dados/09_consolidacao_de_fronteira.md`
— **sem mudança**: é o plano aprovado desta fase, fora do escopo de uma invocação de builder
alterar. `docs/INDEX.md` — **atualizado**, entrada nova (append-only). `tasks.toml` — **sem
mudança**: por instrução do despacho, não altero task nem ledger.

## Bloqueado (nomeado, não escondido)

- **A1/A3 de Classe A (`ADR-004`)** — sobreposição na reconexão e reparo de buraco em
  `md.ingest_gap` para `aggTrade` — **sem implementação e sem task no plano `03`-`08`**. Não é
  escopo desta task consertar; é nomeado para não virar dívida silenciosa.
- **Região da VPS (`[GAP G7]`)** — `[NÃO MEDIDO]`, por desenho declarado desde `ADR-002`
  original, não por esta task. Dono é `T-03.9`, fora deste escopo.
- Nenhum dos dois bloqueia `D9.4`: o critério lê "o finalista de motor está registrado com
  números medidos" e os cinco critérios do spike + `free -m`/`df -h` estão, todos, com valor.

Relatório completo desta implementação: este arquivo.
