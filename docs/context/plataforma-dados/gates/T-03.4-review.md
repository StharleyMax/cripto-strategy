# T-03.4 · `/review` — relatório do portão

**Card:** `CST-20` · **Componente:** `sentimento` · **Fase:** `03` · **PR:** #59
(`tasks/T-03.4-agregado-q-nq`, commit `133682b` sobre `master@634833b`)

## Veredito: COMPLIANT

Denominador: **7 regras bloqueantes** em vigor (`harness rules list --severity block`), todas
avaliadas via `harness rules --mode sweep --changed-only` rodado de novo, independentemente, no
worktree `/tmp/claude-1002/wt/T-03.4` → `rc=0`, 0 achados. Universo varrido: os 9 arquivos do
diff de PR #59 (`gh pr diff 59`, 1654 linhas) — 4 de produção, 4 de teste, 1 doc.

## O que foi reexecutado (não só relido do relatório do builder)

- `bash backend/scripts/boundaries.sh` → **3 kept, 0 broken**, incluindo o contrato
  `Natureza: domain e use_cases nao falam com socket nem ssl` — confirmado por leitura de
  código: `qnq_divergence.py` e `aggtrade_bucket_aggregate.py` importam só `collections.abc`,
  `dataclasses`, `decimal`, `typing`; `datetime` só aparece em
  `infra/aggtrade_rest_snapshot_reader.py` (camada onde o contrato permite).
- `bash backend/scripts/natureza.sh` → universo 29 arquivos domain+use_cases, 0 leituras de
  relógio.
- `pytest -q` escopado aos 4 arquivos de teste novos (37 testes) → `rc=0`, todos passam.
- `ruff check` nos 4 arquivos de produção novos/modificados → `All checks passed!`.
- Suite completa (`bash backend/scripts/test.sh`) terminou em background após a janela
  síncrona desta revisão (máquina com múltiplos workers pytest concorrentes de outras tasks —
  `T-02.1`, `T-02.2`, `T-03.5` — disputando CPU): `[MEDIDO 2026-09-01, worktree
  /tmp/claude-1002/wt/T-03.4]` **652 passed in 742.24s**, `rc=0`. Cobertura por camada
  (`ADR-009/D1`) reexecutada e confirmada, não só citada do builder: `domain 100,0%
  (1250/1250, meta 90%)` · `use_cases 100,0% (220/220, meta 80%)` · `infra 97,8% (872/892,
  meta 70%)` — total 99,09%. As três camadas acima do piso declarado.

## Reaproveitamento de `aggtrade_contiguity.py` (T-04.3) — confirmado por leitura, não por afirmação

`aggtrade_bucket_aggregate.py` importa `AggTradeTick`, `detect_agg_id_gaps`,
`require_unique_agg_ids` de `aggtrade_contiguity.py` e `require_contiguous()` só ADAPTA o
formato (`AggTradeBucketTrade` → `AggTradeTick`) antes de chamar as duas funções — nenhuma
lógica de detecção de buraco é reimplementada. `test_aggtrade_bucket_aggregate_fixtures.py`
roda sobre o MESMO arquivo fixture que `T-04.3` provou em escala
(`binance/aggtrades/BTCUSDT-aggTrades-2026-08-21.csv`, md5 `31f5b006714d6cbc41f8a0b4e10a7aae`),
só numa fatia de 20.000 linhas — decisão custeada e nomeada no comentário do arquivo, não
escondida. `D3.5` satisfeito por delegação, conforme handoff exigia.

## Idioma de identificador

Todo identificador (classe, função, variável, parâmetro) dos 4 arquivos de produção e 4 de
teste está em inglês. Mensagens de `raise` ficam divididas PT/EN dentro do próprio módulo
(`AggTradeGapError`/`PartialNqBucketError` em português; `InvalidBucketQuantityError`/
`InvalidQnqQuantityError`/`InvalidRestSnapshotError` em inglês) — **não é violação**: esta
superfície é a lacuna já registrada em `CLAUDE.md` § idioma como `⏸ NÃO DECIDIDO`, dono
`/architect`, não portão. Fica registrado para o gatilho de reabertura (hoje o corpus daquela
lacuna era 3 PT/2 EN; esta PR soma mais 2 mensagens em português — número a levar ao próximo
`/architect` que reabrir o assunto).

## D3.8 (fronteira `SEM_FONTE`)

`test_nq_bucket_capture_boundary.py` liga o agregado novo ao mecanismo `as_of`/
`first_capture_at`/`Absence.NO_SOURCE` de `T-04.4`, sem reimplementá-lo — confirmado por
leitura (importa de `as_of_accessor.py`, não duplica o `if`).

## Confirmação tardia (suite completa)

A suite completa (`bash backend/scripts/test.sh`, 652 testes) rodou em background e terminou
após a redação inicial deste relatório — `[MEDIDO 2026-09-01]`: `rc=0`, mesmos números de
cobertura que o builder relatou. Nenhuma divergência encontrada entre o relatado e o
reexecutado.

## Achados

Nenhum — 0 de 7 regras bloqueantes violadas.

## Gate

`harness gate-record plataforma-dados 03 REVIEW COMPLIANT "T-03.4/PR#59: 0/7 regras bloqueantes
violadas (rules sweep rc=0), boundaries 3kept/0broken reexecutado, Natureza confirmada por
leitura, D3.5 delegado a aggtrade_contiguity.py (T-04.3), D3.8 delegado a as_of_accessor
(T-04.4). Cobertura 100/100/97,8% citada do builder, não remedida por CPU contention na
máquina."`

## Gravação recusada — dívida da fase 02, não do veredito

`harness gate-record plataforma-dados 03 REVIEW COMPLIANT "..."` → **exit 4**, recusado pelo
documento de tarefas: `T-02.1` e `T-02.2` (fase 02) em estado não-terminal, contradizendo o
veredito QA APPROVED de `2026-08-29T13:08:52Z`. Não usei `harness pipeline override` — dívida
reportada, não contornada. O veredito arquitetural desta revisão continua **COMPLIANT**; o gate
não pôde ser gravado no ledger até a fase 02 fechar o desfecho de `T-02.1`/`T-02.2`.
