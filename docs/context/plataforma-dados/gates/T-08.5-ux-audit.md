# UX Audit — `T-08.5` (bundle `ThresholdSpec`, não uma tela)

**Gate independente `ux-ui-mastery`, rodado pelo `/workflow` (não pelo builder) — R9.**
**Escopo:** `frontend/src/app/threshold-spec-bundle.ts`. Mesmo formato de `T-05.9-ux-audit.md`/
`T-08.11-ux-audit.md`, 10 heurísticos de Nielsen. Sem tela nesta task — `S4` não existe
(`T-08.5-design.md`), não avalia layout/cor/tipografia.

## Veredito: **APROVADO, sem condição**

## O que foi checado

| # | heurística | verificação | resultado |
|---|---|---|---|
| H2/H6 | correspondência sistema↔mundo real | `q`, `k`, `pct`, `window`, `scope`, `minObs`, `interpolation`, `op` são a notação LITERAL de `SPEC-001:292-295` (`Percentile{q, window, scope, min_obs, interpolation, op}`, `RobustZ{k, window, min_obs, op}`, `Absolute{pct, op}`) — os nomes curtos não são abreviação de conveniência, são fidelidade à fonte matemática | ✅ passa |
| H4 | consistência de vocabulário | `variant` como discriminador (`"absolute"`/`"percentile"`/`"robust_z"`) é único em todo o módulo; `PARAM_ORDER` estável; nenhuma segunda grafia para o mesmo eixo | ✅ passa |
| H5 | prevenção de erro | `assertValidThresholdSpec`/`assertValidBundle` recusam antes de virar URL; `decodeBundle` recusa `specVersion` desconhecido em vez de tentar mapear — mesmo padrão de `assertValidBundle`/`assertValidHistoryRequestKey` | ✅ passa |
| H9 | diagnóstico de erro | toda mensagem nomeia o campo; as ligadas a DoD citam a fonte (`variant="custom"` cita `SPEC-001 §3.7` verbatim; `minObs > window` cita o próprio exemplo de `SPEC-001:304`, `rolling(2016, min_periods=576)`) | ✅ passa |
| H1 | visibilidade de status | `op` sempre explícito (nenhum dos 3 construtores tem default); `Custom` documentado mas inconstruível — nenhum estado assumido | ✅ passa |

## Achado NÃO recorrente — diferença do padrão de `T-08.11`

`T-08.11` teve 1 Should-Fix: mensagem em português vazando de uma função REUSADA de um módulo mais
antigo (`history-transport.ts`, pré-decisão de 2026-09-02). **Este módulo não reusa nada de
`knowledge-time-bundle.ts`** (é self-contained), então não há esse vazamento — checado
especificamente porque era o achado da task irmã anterior. `knowledge-time-bundle.ts` também lança em
português (mesma classe de débito pré-2026-09-02), mas como `threshold-spec-bundle.ts` não importa dele,
o módulo novo fica 100% inglês sem exceção — melhor que o precedente imediato, não pior.

## O que este audit NÃO julga

Fidelidade do dado (fora de `web`) e qualquer decisão de UI/UX nova — não há nenhuma possível ainda
(`S4` não existe, `ui-designer` já confirmou em `T-08.5-design.md`).

**Merge:** sem impedimento.
