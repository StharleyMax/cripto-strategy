[REVIEW GATE — Fase 06: `funding_settled` ≠ `funding_estimado`, séries distintas]
Feature: `plataforma-dados`   Componente: `sentimento`   Task: `T-06.4` (`CST-48`)
PR: https://github.com/StharleyMax/cripto-strategy/pull/105 (branch `tasks/T-06.4-fase06`)
Worktree: `/tmp/claude-1002/wt/T-06.4`

## Denominador

`harness rules list --severity block` → **7 regras bloqueantes** em vigor (`core.relative-import`,
`core.silent-except`, `core.print-statement`, `core.hardcoded-secret`,
`web-fullstack.browser-imports-server`, `web-fullstack.tenant-from-request`,
`web-fullstack.server-test-directory-present`). Avaliei as 7 sobre os 4 arquivos que `git diff
master...HEAD --stat` credita a esta task (`funding_settlement.py`, `test_funding_settlement.py`,
`test_funding_next_funding_time_grid.py`, `test_as_of_is_the_single_reader.py` modificado) via
`harness rules --mode file --path <arquivo>`, e sobre o repositório inteiro via `harness rules
--mode sweep` (cobre `backend/src/`, `backend/tests/`, `frontend/src/` por `code_paths`).
**Resultado: 0 BLOQUEIO** nos 4 arquivos e **0 BLOQUEIO** no sweep completo (17 AVISO no total,
nenhum bloqueante). `git diff master...origin/master` confirmado vazio (branch local já igual a
`origin/master`, `6ecb9ae`); `git log HEAD..origin/master` mostra **1 commit** (`6ecb9ae`, `T-06.3`)
que a branch ainda não incorporou — sem sobreposição de arquivo de produção com `T-06.4` (`T-06.3`
toca `series_key.py`, que esta task não toca); único risco de conflito textual é
`backend/README.md`/`docs/INDEX.md`, ambos append-only nos dois lados.

## Achados

**Nenhum BLOQUEIO.** Um WARNING real, citável, não previamente reportado como presente pelo
builder/QA:

- **[WARNING]** Docstring de módulo multi-linha viola a convenção já em vigor no próprio
  componente (`series_key.py:1`, `series_catalog.py:1` — resumo de uma linha, abre e fecha nela,
  com o "porquê" em bloco de comentário `#` abaixo) —
  `backend/src/modules/sentimento/domain/funding_settlement.py:1` — regra
  `core.module-docstring-single-line` (severidade `warn`, não `block`). Confirmado ao vivo com
  `harness rules --mode file --path backend/src/modules/sentimento/domain/funding_settlement.py`
  e reproduzido em `harness rules --mode sweep` (mesmo achado, entre os 17 AVISO do repositório
  inteiro). **Contradiz** a evidência do builder/QA ("`harness rules --mode sweep --changed-only`
  → vazio, `rc=0`, 0 achados", `T-06.4-qa.md:129,143`): `--changed-only` filtra pelo diff NÃO
  commitado da árvore de trabalho — com a árvore limpa (pós-commit), ele sempre devolve vazio,
  **independente do que o sweep completo acusa**. Não é fabricação: é uma lacuna metodológica de
  evidência (mesma classe de regra que `T-06.5` encontrou e corrigiu na mesma passada, ver
  `docs/INDEX.md` linha de `T-06.5`) — aqui ela ficou sem corrigir. Correção concreta: reduzir a
  linha 1 a um resumo de uma linha que abre e fecha nela mesma, movendo o "WHY TWO SERIES"/"WHY
  `interval_hours_declared`..." para comentário `#` (mesmo padrão de `series_catalog.py`).

## Pontos de arquitetura verificados (citação, não opinião)

- **`Natureza`/`ADR-016` (domain puro):** `funding_settlement.py` importa só
  `collections.abc.Sequence`, `dataclasses`, `decimal.Decimal`, `enum.Enum`, `typing.Final` — 0
  I/O, 0 relógio, confirmado por leitura direta do arquivo + `bash backend/scripts/natureza.sh`
  (`76 arquivo(s), 0 leitura(s) de relogio`) + `bash backend/scripts/boundaries.sh` (`3 kept, 0
  broken`, 162 arquivos/735 dependências) — `funding_settlement.py` não aparece como violador.
- **D6.11 — `settlement_slot` da PRÓPRIA LINHA:** confirmado lendo o código, não só o relato.
  `compute_settlement_slot(observed_at_ms, interval_hours_declared)` (linha 88) recebe o
  intervalo como PARÂMETRO por chamada; `grep -rn "settlement_slot\|settle_bucket"
  backend/src/modules/sentimento/` não devolve NENHUMA outra implementação — não existe grade
  fixa/global em lugar nenhum do domínio. `FundingRecord.__post_init__` (linha 156) recomputa e
  RECUSA (`InvalidFundingRecordError`) um `settle_bucket` que não bata com o intervalo da própria
  linha — o defeito antigo (grade fixa) reprova na fronteira do próprio registro, não só no teste.
- **D6.12 — PK inclui `source`:** `FundingRecord.primary_key()` (linha 170) retorna
  `(instrument_id, settle_bucket, source.value, observed_at)`. `test_funding_settled_and_
  funding_estimado_never_collide_at_the_same_window` prova que mesma janela + fonte diferente ⇒
  PKs distintas e ambas sobrevivem a `deduplicate_funding_records`. QA já mediu a mutação real
  (remover `source` da tupla ⇒ colisão + falha de teste), consistente com o código lido aqui.
- **D6.16 — divergência 569/570 nomeada, não escondida:** `test_funding_next_funding_time_grid.py`
  linhas 1-14 (docstring do módulo) e `test_d6_16_the_matched_triple_has_569_common_trading_
  perpetual_symbols` nomeiam explicitamente que o par `2026-08-25` do plano não tem `premiumIndex`
  companheiro (`data/MANIFEST.md:144`) e que a medição real é `569/569` em `2026-09-01`, com a
  data ao lado do número. `backend/README.md` (seção `T-06.4`) e `docs/INDEX.md` (linha
  `08:30Z`) repetem `569`, nunca `570`, e nomeiam a divergência de fixture. Nenhuma citação de
  "570/570" como se fosse medição encontrada em código ou doc desta task.
- **`funding_settled`/`funding_estimado` reusam `SeriesKey`/`SeriesCatalogEntry` de `T-06.1`:**
  confirmado por import direto (`test_funding_settlement.py:31-38`) e pelo teste
  `test_funding_settled_and_funding_estimado_are_two_catalog_entries`, que constrói duas
  `SeriesCatalogEntry` diferindo só em `metric` e prova que `build_series_catalog` — a mesma
  função que reprova `DuplicateSeriesKeyError` em colisão genuína — as aceita lado a lado.
  `series_catalog.py`/`series_key.py` não foram modificados por esta task (nenhuma redefinição).
- **Idioma inglês (produção e teste):** inspeção por AST de toda `Call` cujo nome termina em
  `Error`/`Exception` em `funding_settlement.py` — todas as mensagens literais/f-string em
  inglês. Identificadores, docstrings e nomes de teste também em inglês. Nenhum segmento de
  diretório em português introduzido (`git ls-tree` + filtro do falsificador da exceção de
  vocabulário — só os 6 componentes declarados aparecem, `painel`→`panel` já renomeado).
- **`docs/INDEX.md`/`backend/README.md` append-only:** `git diff master...HEAD` mostra as duas
  edições como blocos NOVOS ao final do arquivo — nenhuma linha pré-existente (das entradas de
  `T-06.2`/`T-06.5`/`T-05.2`, mescladas em paralelo) foi reescrita ou removida.

## Veredito

**COMPLIANT** — 0 de 7 regras bloqueantes violadas, nos 4 arquivos da task e no sweep completo do
repositório. 1 WARNING real e citável (`core.module-docstring-single-line`), correção descrita
acima; não bloqueia o veredito por ser severidade `warn`.
