[QA GATE — Fase 08: Primitivo `swing_point` em `<Anotacao>` — zero algoritmo, zero limiar, zero "nível"]
Feature: `plataforma-dados`     Componente: `charts`     Task: `T-08.10` (`CST-78`)
Arquivos avaliados (worktree `/tmp/claude-1002/wt/T-08.10`, branch `tasks/T-08.10-swing-point-anotacao`):
- `frontend/src/charts/s2-swing-point.ts` (new, produção — restaurado byte-a-byte após mutação, ver abaixo)
- `frontend/src/charts/s2-swing-point.test.ts` (new, 12 testes)
- `frontend/README.md` §17 (modified, append-only)
Builder gate: `docs/context/plataforma-dados/gates/T-08.10-builder.md` — **reconstruído pelo `/qa`**
(anomalia registrada abaixo; o texto reconstruído cita `README.md` §17, que É do builder).
Refs: `SPEC-001` §3.6 (linha 282, literal), plano `08` item 8.7, `tasks.toml:1090-1097`.

## Baseline — testes existentes rodados ANTES de qualquer mutação

```
cd /tmp/claude-1002/wt/T-08.10 && npm --prefix frontend run test:charts
-> node --test 'src/charts/*.test.ts'
-> ℹ tests 130 / ℹ pass 114 / ℹ fail 16
```

Isolado, só o módulo desta task:
```
cd frontend && node --test src/charts/s2-swing-point.test.ts
-> ℹ tests 12 / ℹ pass 12 / ℹ fail 0
```

## As 16 falhas do universo — confirmadas ENOENT de `data/` ausente, zero relação com esta task

```
ls /tmp/claude-1002/wt/T-08.10/data
-> "Arquivo ou diretório inexistente" (data/ é .gitignore:51, dado bruto não versionado)

awk '/^✖ failing tests:/{p=1} p' <log> | grep -c "ENOENT"   -> 16 de 16
```
Arquivos com falha (nenhum tocado por este diff — `git diff --name-only HEAD` só lista
`frontend/README.md`, `s2-swing-point.ts`, `s2-swing-point.test.ts`):
`canonical-grid-sha256-proof.test.ts`, `s2-absence-policy.test.ts`, `s2-axis-integration.test.ts`,
`s2-cvd.test.ts`, `s2-klines-loader.test.ts`, `s2-oi-loader.test.ts`, `s2-panels.test.ts` — todos
lêem `data/binance/**` (klines/metrics reais), ausente neste worktree.

⚠️ **Correção do número citado pelo builder:** `T-08.10-builder.md`/README §17 registram
"147 testes, 114 pass / 33 fail" para o mesmo comando. Medido de novo nesta sessão, na mesma
worktree: **130/114/16**, não 147/114/33. `pass=114` bate nos dois; `fail` diverge. Causa
identificada por comparação com o repositório principal (`/home/stharley/.../cripto-strategy`,
que TEM `data/binance` e `data/bybit` completos): rodando lá, **0 fail, 126 pass** — ou seja,
"33" também não reproduz no repo principal. `[INFERRED]`: dois arquivos desta suíte
(`canonical-grid-sha256-proof.test.ts`, `s2-axis-integration.test.ts`) fazem leitura de fixture
NO ESCOPO DO MÓDULO (fora de qualquer `test()`) — quando `data/` está **totalmente** ausente
(este worktree), o `ENOENT` derruba o arquivo inteiro e conta como **1** falha por arquivo;
quando `data/` está **parcialmente** presente (o ambiente em que o builder mediu "33", hoje
irreproduzível), o mesmo `ENOENT` acontece dentro de `test()`s individuais e cada um conta
separado — mais entradas, mesma causa raiz. **Isto não muda o veredito**: em nenhum dos três
ambientes medidos (`33` do builder, `16` aqui, `0` no repo principal) uma falha tem qualquer
relação com `s2-swing-point.ts`/`.test.ts` — o próprio módulo desta task passa 12/12 em todos.
`[MEDIDO nesta sessão, comando acima; "33" do builder não é reproduzível por mim e fica citado
como não confirmado, não como refutado — o ambiente dele já não existe]`.

## Falsificadores estruturais atacados — mutação provada, não só afirmada

Alvo: provar que `SwingPoint` **não** vaza algoritmo, limiar ou "nível" (`SPEC-001:282`).

```bash
cp frontend/src/charts/s2-swing-point.ts <scratchpad>/s2-swing-point.ts.orig
# mutação: return { ...priceBound, primitive:"swing_point", kind, eventTimeMs, price,
#                    fibLevel: 0.618 }   <- campo de "nível" contrabandeado
cd frontend && node --test src/charts/s2-swing-point.test.ts
-> ℹ tests 12 / ℹ pass 11 / ℹ fail 1
-> ✖ falsifier: SwingPoint carries ONLY the declared fields — no threshold, no level, no zone smuggled in
   AssertionError: unexpected field(s) on SwingPoint: fibLevel
```
**PEGO** — o falsificador estrutural (último teste do arquivo) morde exatamente o vazamento que
`SPEC-001:282` proíbe.

```bash
cp <scratchpad>/s2-swing-point.ts.orig frontend/src/charts/s2-swing-point.ts
md5sum frontend/src/charts/s2-swing-point.ts   # idêntico ao hash pré-mutação (f51e82e5...)
git diff -- frontend/src/charts/s2-swing-point.ts   -> vazio (rc=0)
git status --short   -> A (staged, não M) nos 3 arquivos — nenhuma edição de produção sobrevive
```

Leitura de código (sem mutação, porque não há caminho de teste que a exercite — a ausência do
comportamento É a prova): `createSwingPoint` não compara barras vizinhas, não roda janela de
lookback, não importa `ThresholdSpec` (`SPEC-001` §3.7), não referencia razão de Fibonacci nem
`structure_definition`. `PriceUse`/`resolvePriceSource` (dependência composta, `T-05.5`) é tabela
de lookup fixa (`price_use -> price_source`), não um algoritmo nem um limiar — pré-existente,
não tocado por este diff.

## Regras bloqueantes (`harness rules list --severity block`, n=7)

```bash
harness rules --mode file --path frontend/src/charts/s2-swing-point.ts        # rc=0, sem saída
harness rules --mode file --path frontend/src/charts/s2-swing-point.test.ts   # rc=0, sem saída
harness rules --mode sweep --changed-only                                      # rc=0, sem saída
```

- [OK] `core.relative-import` — imports são `"./s2-annotation-price-binding.ts"` etc., relativos
  ao próprio módulo (padrão TS/ESM local) — sweep não achou.
- [OK] `core.silent-except` — módulo não tem `try/catch`; toda recusa é `throw`.
- [OK] `core.print-statement` — sweep não achou.
- [OK] `core.hardcoded-secret` — escopo `production`; sweep não achou (arquivo não tem string de
  credencial).
- [OK] `web-fullstack.browser-imports-server` — os 3 imports de `s2-swing-point.ts` são todos de
  `./s2-*` dentro de `frontend/src/charts/`, nenhum de `backend/`.
- [OK] `web-fullstack.tenant-from-request` — não há noção de tenant/request neste módulo.
- [OK] `web-fullstack.server-test-directory-present` — não aplicável a este delta (não cria nem
  remove diretório de teste de servidor).
Regras bloqueantes avaliadas: 7 de 7.

## Testes existem e passam

`node --test src/charts/s2-swing-point.test.ts` (de dentro de `frontend/`) — **12 testes, 12 pass,
0 fail**. Cobrem: `SWING_KINDS` fechado, os 2 `kind`s round-trip, 6 recusas independentes
(`kind` inválido antes de tocar `eventTimeMs`/`price`; `eventTimeMs` NaN e negativo; `price`
`Infinity`, zero e negativo), propagação das 2 recusas de `T-05.5`/`T-05.2` através da composição,
e o falsificador estrutural de campo fechado (morde, ver mutação acima).

## Cobertura

Sem piso numérico declarado para `charts` (`harness policy --key test_cmd` só cobre `sentimento`
— `[DOC: harness.toml §"[test_cmd.web] NÃO foi declarado"`, mesma ausência vale para `charts`]).
Medição qualitativa: 12 testes cobrem 100% das 3 funções/constantes exportadas
(`SWING_KINDS`, `assertSwingKind`, `createSwingPoint`) e 100% dos ramos de recusa
(3 campos × 1 recusa cada + 2 recusas herdadas por composição). Sem alvo declarado, não há
"abaixo do alvo" a reprovar — a lacuna do alvo em si é `[DOC]`, não desta task.

## DoD do item 8.7 (plano `08`, texto do próprio item — não há `Dx` dedicado na tabela DoD)

- [OK] **Zero algoritmo** — nenhuma comparação de barras vizinhas, nenhuma janela de lookback;
  `createSwingPoint` registra, não detecta. Confirmado por leitura de código.
- [OK] **Zero limiar** — nenhum `ThresholdSpec`, nenhum tamanho mínimo de movimento. Confirmado
  por leitura de código e pelo falsificador estrutural (mutação acima).
- [OK] **Zero "nível"** — nenhuma razão de Fibonacci, nenhuma zona, nenhum `structure_definition`.
  Falsificador estrutural PEGA um `fibLevel` contrabandeado (mutação acima).
- [OK] **`Q20` não bloqueia** — task não referencia `Q20` além de citá-la como não-bloqueante
  (`tasks.toml:1096`); nenhuma decisão de vocabulário de zona foi tomada aqui.
- [OK] **Composição, não reimplementação** — `createPriceBoundAnnotation`/`createAnnotationIdentity`
  intocados (`git diff --name-only` não os lista); reuso verbatim confirmado por leitura.
- [OK] **Conflito com `ADR-017` (RASCUNHO) nomeado, não resolvido por adivinhação** — o módulo e
  o README §17 registram o conflito e a razão da recusa em construir os campos do rascunho;
  correto per `CLAUDE.md`/gate de owner: `ADR-017` não tem evento `approve` no ledger.

## Anomalias

1. **Builder não gravou `gates/T-08.10-builder.md`.** Reconstruído pelo `/qa` nesta sessão a
   partir de `frontend/README.md` §17 (evidência do próprio builder) e do diff — ver
   `T-08.10-builder.md`, com o aviso de reconstrução no topo. Isto não é um veredito `FAIL` de
   regra (nenhuma das 7 listadas exige o artefato), mas é desvio de processo: cada task deveria
   sair do build com o gate próprio gravado, não delegado ao QA para reconstruir.
2. **Número de falhas pré-existentes divergente do citado pelo builder** (33 vs. 16 medido aqui,
   0 no repo principal) — explicado acima; não muda o veredito porque a causa raiz (ENOENT sobre
   `data/`, dado bruto não versionado) é a mesma nos três ambientes e nenhuma falha toca esta
   task.

Regras bloqueantes avaliadas: 7 de 7
Veredito: APPROVED

Notas: zero algoritmo/limiar/nível confirmado por leitura + mutação estrutural (falsificador
morde); composição com `T-05.2`/`T-05.5` intocada; 12/12 testes próprios; 7/7 regras bloqueantes
OK; as 16 falhas do universo `test:charts` são 100% ENOENT de `data/` ausente, em arquivos que
este diff não toca. Duas anomalias de processo registradas (gate do builder ausente; número de
falha pré-existente não reproduzível), nenhuma delas reprovando o item.
