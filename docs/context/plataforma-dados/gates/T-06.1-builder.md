[QA GATE — Fase 06: `series_catalog` como contrato lido pelos testes]
Feature: `plataforma-dados`     Componente: `sentimento`
Arquivos alterados:
- `backend/src/modules/sentimento/domain/series_catalog.py` (new)
- `backend/tests/sentimento/test_series_catalog.py` (new)
- `backend/README.md` (modified — nova seção "📎 2026-09-02 por `T-06.1`")
Spec: docs/specs/SPEC-001-plataforma-dados.md (§3.3)
Plan: docs/plans/SPEC-001-plataforma-dados/06_semantica_declarada.md (itens 6.1 + 6.5 + 6.15)
DoD:
- [x] `series_catalog`: UMA linha por `SeriesKey` (os 15 termos reusados de `series_key.py`/`T-04.2`, não redefinidos) — `SeriesCatalog.__post_init__` recusa duas linhas com o mesmo `series_key_id()` (`DuplicateSeriesKeyError`) — `bash backend/scripts/test.sh -k test_build_series_catalog_refuses_two_rows_for_the_same_series_key`
- [x] `label_shift` com `verified_by` apontando para um teste (string, não booleano) — já é termo de `SeriesKey`, reusado; `test_catalog_row_refuses_a_key_with_no_label_shift_witness` prova que uma linha não se constrói com `verified_by` em branco
- [x] `unit`/`denom` obrigatórios, verbatim, ausência detectável — via `SeriesKey.__post_init__` (`IncompleteSeriesKeyError`), exercido pela catálogo em `test_catalog_row_refuses_a_blank_identity_term_via_the_key` (parametrizado em unit/denom/verified_by)
- [x] `price_use` quando aplicável — campo opcional, restrito ao conjunto fechado de `SPEC-001` §3.7 (`InvalidPriceUseError` fora dele) — `test_price_use_accepts_every_member_of_the_closed_set` + `test_price_use_outside_the_closed_set_is_refused`
- [x] `max_staleness_ms` obrigatório e positivo — `test_catalog_row_refuses_a_non_positive_max_staleness_ms`
- [x] erro publicado `(mediana, p99, n)` quando reconstrução de outra fonte, recusa de publicação sem ele (`SPEC-001` §3.3 literal) — `test_reconstruction_without_published_error_is_refused` + simétrico `test_published_error_without_a_declared_reconstruction_is_refused`
- [x] `native_grid` é propriedade da `source`, resolvida em runtime, por linha — `test_native_grid_is_a_per_row_field_not_a_shared_constant` (duas linhas, dois grids, sem constante de módulo)
- [x] Falsificador central: "o teste lê o catálogo; catálogo e teste não podem divergir sem reprovar" — todo `pytest.raises` em `test_series_catalog.py` chama tipo/exceção de produção diretamente, nenhum valor esperado duplicado à mão
Comandos rodados (literais) e resultado:
- `bash backend/scripts/test.sh` (suite completa) → **1277 passed**, 0 failed, 0 error (universo: toda `backend/tests/`, incluindo os 26 testes novos de `test_series_catalog.py`)
- cobertura total **97,61%**; por camada (`check-coverage-layers.sh`, `ADR-009/D1`): domain **99,8%** (meta 90%, `2499/2503` linhas), use_cases **100,0%** (meta 80%, `585/585`), infra **95,1%** (meta 70%, `2208/2321`) — 3/3 camadas `[OK]`
- `series_catalog.py` isolado: **58 stmts, 0 miss, 100%** (`bash backend/scripts/test.sh tests/sentimento/test_series_catalog.py -q`, 26 passed)
- `bash backend/scripts/lint.sh` → `ruff check` + `ruff format --check` + `mypy --strict`: **sem achado**, 234 arquivos
- `harness rules --mode file --path backend/src/modules/sentimento/domain/series_catalog.py` e idem para o teste → `rc=0` ambos (1 achado `[AVISO] core.module-docstring-single-line` encontrado durante o desenvolvimento e corrigido antes deste commit — docstring de módulo agora abre e fecha na 1ª linha, resto em comentário `#`, mesmo estilo de `quarantine_terms.py`)
- `harness rules --mode sweep --changed-only` → `rc=0`, **0 achados**
- `bash backend/scripts/boundaries.sh` → **3 kept, 0 broken** (155 arquivos, 703 dependências) — `Camadas por contexto`, `Fronteira de contexto` e `Natureza: domain/use_cases não falam com socket/ssl`, todos `KEPT`
- `bash backend/scripts/natureza.sh` → **70 arquivo(s) (51 domain + 19 use_cases), 0 leitura(s) de relógio**
Cobertura: 97,61% total / domain 99,8% / use_cases 100,0% / infra 95,1% (alvo: domain 90%, use_cases 80%, infra 70% — `ADR-009/D1`, confirmado por `check-coverage-layers.sh`)
Doc delta:
- `backend/README.md`: atualizado — nova seção "📎 2026-09-02 por `T-06.1` — `series_catalog`: o contrato lido pelos testes, raiz da fase 06", mesmo padrão das seções de `T-04.6`/`T-07.9`/etc (peça, falsificador, comandos literais, escopo não fechado nomeado)
- `docs/INDEX.md`: linha `/build` acrescentada (append-only), apontando para este gate
- ADR: não necessário — nenhuma decisão arquitetural nova; a forma (`SeriesCatalogEntry` envolvendo `SeriesKey`, sem duplicar seus termos) segue diretamente `SPEC-001` §3.3 e o precedente de composição já usado por `quarantine_terms.py`
Bloqueado (se houver): nenhum. Escopo explicitamente NÃO fechado por esta task, nomeado no handoff e reafirmado no doc delta: povoar séries/shifts reais (`T-06.2`/`T-06.3`/`T-06.4`/`T-06.9`), construir o mecanismo de quarentena (`T-06.6`), e qualquer persistência de `SeriesCatalog` (não há `infra/*_series_catalog_store.py` — decisão consciente, nada na task pede um ainda).
