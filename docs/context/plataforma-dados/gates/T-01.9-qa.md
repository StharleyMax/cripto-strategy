# QA Gate — T-01.9 (fase 01, componente `docs`)

Escopo do delta avaliado (não commitado, branch `tasks/T-01.8-juizes-e-deploy`):
`Makefile` (target `lint-agents`), `docs/gate-de-design.md` (+59 linhas),
`scripts/agents-by-component.golden.json`, `scripts/check-agents-by-component.sh`,
`scripts/check_agents_by_component.py`. **Não tocados**: `harness.toml`,
`docs/context/plataforma-dados/tasks.toml`, `corpus/`, `deploy/` — de outra task (`T-01.10`).

## Reprodução, comando a comando

**1. `make lint-agents` sobre a árvore real** — `cd /home/stharley/Documentos/projects/cripto-strategy && make lint-agents`
→ `agents.by_component: bate com o dourado — 6 componente(s), 10 papel(is).`, `rc=0`.
O dourado (`scripts/agents-by-component.golden.json`) é byte-a-byte o `json.dumps` de
`harness policy --key agents.by_component` de hoje (6 componentes, 10 papéis, `docs` ausente
por congelamento deliberado).

**2. As 5 mutações da tabela "MORDE — 5 de 5" de `docs/gate-de-design.md`, reproduzidas eu
mesma** — bancada em `/tmp/.../scratchpad/bench`, cópia de `git show HEAD:harness.toml`,
nunca o arquivo real do repositório:

| mutação | `harness validate --strict` | `check-agents-by-component.sh` |
|---|---|---|
| `design_gate` apagado das 2 entradas | `rc=0`, silêncio (10→10 avisos, sem erro) | `rc=1`, 2 pontos: `charts.design_gate`, `web.design_gate` — PAPEL AUSENTE |
| donos TROCADOS `charts`↔`web` (conteúdo inteiro) | `rc=0`, silêncio, 12 avisos | `rc=1`, 6 pontos, primeiro nomeado `charts.architect` esperado `quant-architect.md` obtido `frontend-architect.md` |
| `[agents.by_component.charts]` vazia | `rc=0`, silêncio | `rc=1`, 2 pontos: `architect`+`design_gate` PAPEL AUSENTE |
| `docs` ganha juiz (componente a mais) | `rc=0`, silêncio | `rc=1`, 1 ponto: `agents.by_component.docs` — COMPONENTE INESPERADO |
| seção inteira apagada | `rc=0`, silêncio, `policy` devolve `{}` | `rc=1`, 6 pontos: os 6 componentes — COMPONENTE AUSENTE |

Todo número bate exatamente com a tabela do documento (contagem de pontos, chave nomeada,
`+5`/`+1` etc.). **5 de 5 mutações silenciosas para o `harness` são pegas pelo script novo.**

**3. A mutação que o `harness` de fato pega** (referida na seção anterior do mesmo doc,
`T-01.3`) — `[agents.by_component.frontend]` (fora do enum): `harness validate --strict` →
`rc=1`, `[erro] V-16 agents.by_component.frontend: recebido 'frontend'; esperado um dos
componentes declarados [...] (componente fora do enum nunca vai ser consultado)`. Confirma
que o mecanismo do harness morde exatamente 1 classe de mutação, nunca as 5 acima — é o "UMA"
do texto.

**4. Prova de ponta a ponta do `make lint`** — bancada com `Makefile` + `scripts/` reais +
`harness.toml` mutado (donos trocados): `make lint-agents` → imprime as 6 divergências e
`make: *** [Makefile:131: lint-agents] Erro 1`, `rc=2`. Com o `harness.toml` sem mutação,
mesma bancada: `rc=0`. Bate com a alegação "`make lint` → `Erro 1` em `lint-agents`, `rc=2`"
do documento.

**5. O falsificador falsifica de verdade** — nenhuma das 5 mutações foi alegada: cada uma foi
escrita numa cópia (`/tmp/.../scratchpad/bench/harness.toml`, nunca o arquivo do repositório),
rodada, o `rc` e a saída capturados, e a cópia restaurada de `git show HEAD:harness.toml` antes
da mutação seguinte. `git status` no repositório real mostra **zero mudança** produzida por
esta sessão de QA.

## Regras bloqueantes

`harness rules list --severity block` → 8 regras (5 `core`, 2 `web-fullstack`, 1 `own`, esta
última — `own.compose-hardcoded-secret` — pertence a `T-01.10`, fora deste delta).

- [OK] Nenhuma das 8 regras se aplica: `harness rules --mode file --path Makefile` → 0 byte,
  `rc=0`; idem para `docs/gate-de-design.md`, `scripts/check-agents-by-component.sh`,
  `scripts/check_agents_by_component.py`.
- [OK] `harness code-paths classify scripts/check_agents_by_component.py` →
  `nao-producao: nenhum include_prefixes casa` — os scripts do delta estão fora de
  `code_paths` (`backend/src/`, `backend/tests/`, `frontend/src/`), então nenhuma regra de
  escopo `code`/`production` os alcança; consistente com o precedente de outros scripts em
  `scripts/` (`validate_order_fill_env.py`, `verify_screen.py`) já sem gate de lint próprio.
- [OK] `ruff check scripts/check_agents_by_component.py` → `All checks passed!` (rodado por
  curiosidade extra; não é gate declarado para este caminho — `mypy --strict` não pôde ser
  executado com o interpretador certo no ambiente de QA, mas não é gate declarado do plano
  para `scripts/`, só para `backend/`).
- [anomalia] `shellcheck` não está no `PATH` do ambiente de QA — não pude rodar lint de shell
  sobre `check-agents-by-component.sh`. Não é regra bloqueante declarada (`harness rules list`
  não lista nenhuma de shell), então não impede o veredito, mas fica registrado como o que
  não foi medido.

## Testes existem e passam

Não há suite de unidade para `docs`/`scripts/` (`harness policy --key test_cmd` só declara
`sentimento`). **O teste É o próprio mecanismo de mutação** (item 1.13 do plano, D1.12): a
prova de que ele funciona é rodá-lo contra mutação real, o que foi feito no passo 2/3 acima —
5 mutações codificadas, 5 pegas, com `rc` e saída completa capturados.

- [OK] `make lint-agents` sobre a árvore real — `rc=0`, `1 comando`.
- [OK] 5 mutações reproduzidas manualmente contra cópia de `harness.toml`, todas pegas com o
  ponto exato que a tabela do doc alega — `5 bancadas independentes`.
- [OK] Mutação "fora do vocabulário" reproduzida — confirma que o `harness` nativo morde só
  esta, nunca as 5 do script novo.
- [OK] Prova de ponta a ponta de `make lint` (não só do script) com bancada isolada — `rc=2`
  mutado, `rc=0` limpo.

## Cobertura

Não há política de cobertura numérica para `docs` (`harness policy --key coverage` →
`campo ausente na politica: coverage`, `rc=1`). **Alvo não aplicável a este componente/task**;
o critério de suficiência declarado no plano é comportamental (MORDE 5/5, CALA sobre a árvore
real), não percentual, e foi medido no passo 2.

## DoD da fase (D1.12, item 1.13, `ADR-012/D5b`)

- [OK] `make lint` ≠ 0 quando `agents.by_component` divergir do declarado — confirmado
  (passo 4, `rc=2`).
- [OK] Verde sobre a política de hoje — confirmado (`rc=0` limpo, passo 1 e 4).
- [OK] Reprova as 3 mutações nomeadas no DoD original (design_gate apagado, donos trocados,
  seção vazia) — as 3 estão entre as 5 pegas.
- [OK] Não reprova só por presença de chave (o defeito de `D1.2`, `{"charts": {}}`) — a
  mutação "charts vazia" é pega nomeando os PAPÉIS ausentes, não só a chave `charts`.
- [OK] Saída é JSON de chaves ordenadas, universo de 6 componentes (não mais 5) — confirmado
  em `harness policy --key agents.by_component`.
- [OK] `docs` ausente de `agents.by_component` fica congelado no dourado — confirmado (o
  dourado tem 6 chaves, sem `docs`; mutação "docs ganha juiz" é pega como COMPONENTE
  INESPERADO).
- [OK] Não é roteamento (`ADR-012/D5a`) — o script só compara, não despacha nada; confirmado
  por leitura de `check_agents_by_component.py` (só `diff()`, sem chamada a nenhum comando de
  roteamento).

Regras bloqueantes avaliadas: 8 de 8 listadas por `harness rules list --severity block`.

Veredito: **APPROVED**

Notas: nenhum arquivo de produção foi alterado por este QA (nem pela sessão, nem exigido pela
task); todas as mutações usadas para atacar o mecanismo foram feitas em cópias descartáveis em
`/tmp`, nunca no `harness.toml` do repositório, e restauradas/apagadas ao final. A única
anomalia (`shellcheck` ausente no ambiente) não é regra bloqueante declarada e não impede o
fechamento.
