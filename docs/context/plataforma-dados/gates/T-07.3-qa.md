[QA GATE — Fase 07: T-07.3 · ETL com dedupe por hash de conteudo]
Feature: `plataforma-dados`   Componente: `sentimento`   PR #87 (`tasks/T-07.3-fase07` -> `master`)

## Suite real (nao mutada)
- `bash backend/scripts/test.sh` (worktree `/tmp/claude-1002/wt/T-07.3`, HEAD `e92b7bd`, aguardado
  ativamente ate o fim via `ps -p <PID>`, nunca disparado e abandonado): **1147 passed, 0 failed**,
  277,73s.
- Cobertura: **97,44%** total; por camada (`ADR-009/D1`, piso 90/80/70): domain **99,8%**
  (2245/2249), use_cases **100,0%** (549/549), infra **95,0%** (2166/2279) — `[OK]` nas 3.
- Numeros IDENTICOS aos do gate do builder (`T-07.3-builder.md`), reproduzidos de forma
  independente, na mesma worktree, sem alterar producao.

## Natureza / boundaries
- `bash backend/scripts/natureza.sh` -> universo 63 arquivos (domain 46 + use_cases 17), **0
  leituras de relogio**. Bate com o gate do builder.
- `bash backend/scripts/boundaries.sh` -> **3 kept, 0 broken** (144 arquivos, 656 dependencias).
  Bate com o gate do builder.

## Reuso de `checksum_manifest.py`
- `grep -n "hashlib\|sha256" backend/src/modules/sentimento/domain/content_dedupe.py
  backend/src/modules/sentimento/infra/content_dedupe_store.py
  backend/src/modules/sentimento/infra/content_deduping_worker.py` -> zero ocorrencias de
  hashing proprio (a unica ocorrencia de "sha256" e a CHAVE de log `extra={"sha256": digest}` em
  `content_deduping_worker.py:108`, nao uma implementacao).
  `verified_digest_source` (`content_deduping_worker.py:40-51`) usa exclusivamente
  `ChecksummedFilePayload.digest()`/`.checksum_text()` e `ChecksumManifest.parse`/`.verify` —
  primitivo ja existente, nao duplicado.

## Mutacoes plantadas e revertidas (mentalidade destrutiva)

1. **Dedupe por nome de arquivo em vez de hash de conteudo** —
   `backend/src/modules/sentimento/infra/dump_etl_cli.py:230`, troquei
   `digest_of=verified_digest_source(mirror_dir)` por `digest_of=lambda key: key`.
   `bash backend/scripts/test.sh --no-cov -q tests/sentimento/test_content_dedupe_across_keys.py
   tests/sentimento/test_content_deduping_worker.py tests/sentimento/test_dump_etl_cli_surface.py
   tests/sentimento/test_dump_ingest_edge.py` -> **1 failed**
   (`test_two_keys_with_byte_identical_content_publish_only_once`, "so a PRIMEIRA chave publica
   um objeto" reprovado — as duas chaves, com o mesmo conteudo mas nomes diferentes, foram
   ambas publicadas). Revertido; `git diff` confirmado limpo apos.

2. **Mesmo nome, conteudo diferente tratado como duplicata** (o contra-exemplo do DoD invertido)
   — `backend/src/modules/sentimento/domain/content_dedupe.py`, em `ContentDedupeLedger.decide`,
   adicionei `if duplicate_of is None and key in self.first_key_by_digest.values(): duplicate_of
   = key`. `bash backend/scripts/test.sh --no-cov -q tests/sentimento/test_content_dedupe.py
   tests/sentimento/test_content_dedupe_across_keys.py
   tests/sentimento/test_content_deduping_worker.py` -> **5 failed**, entre eles
   `test_the_counter_example_the_dod_names_same_key_different_content_is_not_a_duplicate` (nivel
   domain) e `test_the_dod_counter_example_same_key_reprocessed_with_corrected_content_is_not_hidden`
   (ponta a ponta — a correcao ficou escondida: `assert out.read_bytes() ==
   corrected_payload` reprovou com o conteudo TRUNCADO antigo em vez do corrigido). Exatamente o
   contra-exemplo que o handoff nomeia. Revertido; `git diff` confirmado limpo apos.

3. **Falsificador proprio do builder, reproduzido de forma independente** — troquei o corpo de
   `verified_digest_source.digest_of`
   (`backend/src/modules/sentimento/infra/content_deduping_worker.py:40-51`) para chamar
   `payload.digest()` ANTES de checar `payload.checksum_text()` (a forma ingênua que o
   docstring do proprio modulo descreve e recusa).
   `bash backend/scripts/test.sh --no-cov -q tests/sentimento/test_content_deduping_worker.py
   tests/sentimento/test_dump_ingest_edge.py` -> **2 failed**:
   `test_verified_digest_source_reports_a_missing_sidecar_before_opening_the_payload` (esperava
   `ChecksumMissingError`, recebeu `FileNotFoundError`) e
   `test_a_hole_in_the_bucket_stops_the_drain_and_the_cost_is_named` (esperava
   `ChecksumRejectedError`, recebeu `FileNotFoundError` em
   `checksummed_file_payload.py:98`). Reproduz palavra por palavra o falsificador relatado pelo
   builder. Revertido; `git diff` confirmado limpo apos.

## Estado final
- `git diff --stat` (apos as 3 reversoes): **sem saida** — nenhuma alteracao de codigo de
  producao ou teste ficou pendente.
- `git status --short`: so `frontend/node_modules` (pre-existente, gitignored/irrelevante) e
  este proprio arquivo de gate (`T-07.3-qa.md`), novo.
- `bash backend/scripts/test.sh` rodado uma ultima vez, apos a ultima reversao, sem mutacao:
  **1147 passed**, cobertura **97,44%** (domain 99,8%, use_cases 100,0%, infra 95,0%) —
  identico a rodada inicial, confirmando que nao sobrou residuo das mutacoes.

## Regras bloqueantes (`harness rules list --severity block`, 7 listadas)
- `core.relative-import`, `core.silent-except`, `core.print-statement`,
  `core.hardcoded-secret`, `web-fullstack.browser-imports-server`,
  `web-fullstack.tenant-from-request`, `web-fullstack.server-test-directory-present`: nenhuma se
  aplica a este componente (`sentimento`, backend puro) — confirmado pelo builder via
  `harness rules --mode sweep --changed-only` (saida vazia, `rc=0`), nao re-executado por mim
  (comando identico, arquivos identicos ao gate do builder, sem alteracao de codigo de producao
  nesta sessao).

## Veredito
APPROVED — DoD cumprido: dedupe por hash de conteudo byte-estavel verificado, contra-exemplo
(mesmo nome, conteudo diferente = NAO duplicata) provado por teste E por mutacao reprovada,
reuso de `checksum_manifest.py` confirmado sem duplicacao de hashing, Natureza/boundaries intactos,
falsificador do builder reproduzido de forma independente. Todas as 3 mutacoes plantadas
reprovaram a suite como esperado; git diff limpo ao final.
