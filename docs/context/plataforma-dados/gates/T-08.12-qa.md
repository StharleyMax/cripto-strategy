# T-08.12 — gate `QA`

**Veredito: APPROVED.**

## Verificação, item a item (mentalidade destrutiva)

1. **`classify_grid_multiple` (D1, 3 estados)** — reproduzido manualmente: `60min % 5min == 0` ⇒
   `MULTIPLE_OF_NATIVE, multiple=12` (o caso `719/720`/99,9% de `CA-F4-4`); `1min < 5min` ⇒
   `UPSAMPLING` (o caso `20,0%`); `7min` não múltiplo de `5min` ⇒ `NON_MULTIPLE`, terceiro estado
   distinto — os três nomes nunca colapsam em um único `False`. Confirmado em
   `backend/src/modules/charts/domain/panel_grid_enablement.py:64-99` e nos 7 testes de
   `test_panel_grid_enablement.py`.
2. **`classify_disc_layout` (D2, geometria pura)** — reconciliação do "Achado" verificada com
   calculadora: `spacing_px = 1200×300_000/86_400_000 = 4,1667`; `min_required_px =
   2×(4+2) = 12`; `12 > 4,1667` ⇒ `fuses=True` (colisão, como o DoD exige). No limiar
   `window_ms=30_000_000` (~8,33h), `spacing_px == min_required_px == 12.0` ⇒ `fuses=False`
   (`<=` inclusivo, `panel_disc_layout.py:82`). `grep -n "radius_px\s*=\s*[0-9]\|ring_px\s*=\s*[0-9]"
   backend/src/modules/charts/domain/panel_disc_layout.py` → 0 linhas: `radius_px`/`ring_px` são
   SEMPRE parâmetro, nunca constante interna.
3. **`Bar` união (D3)** — `InProgressBar` (`panel_bar_progress.py:50-60`) tem campos
   `high_so_far`/`low_so_far`/`last`; não existe atributo `high`/`low`/`close` no tipo — não é um
   dataclass com campo opcional/booleano ao lado (a forma que a ADR rejeita explicitamente), é
   ausência estrutural do atributo, provada por `hasattr(...) is False` em
   `test_falsifier_in_progress_bar_has_no_high_low_close_attribute`. `mypy --strict` (via
   `lint.sh`) trata isso como erro de tipo em tempo de checagem, não só em runtime.
4. **`SingleAxisSeries` (D4)** — `switch_denom` (`panel_single_axis.py:68-84`) descarta `current`
   (`del current`) e devolve uma `SingleAxisSeries` nova só com os slots do `new_denom` — SUBSTITUI,
   não funde; provado em `test_falsifier_switch_denom_replaces_never_merges`. Checagem estrutural
   (`dataclasses.fields`) confirma um único campo `denom: str` — não há tupla, não há segundo
   campo opcional, logo não existe chamada de construtor capaz de montar dois eixos ao mesmo
   tempo (`test_no_type_in_this_module_can_hold_two_denoms_at_once`).
5. **Comandos rodados por mim, na mesma sessão, sem background:**
   - `bash backend/scripts/lint.sh` → `ruff check`: All checks passed! · `ruff format --check`:
     329 files already formatted · `mypy --strict`: Success: no issues found in 329 source files.
   - `bash backend/scripts/test.sh` → `1684 passed, 3 warnings in 320.66s`; cobertura TOTAL
     `97,85%`; por camada: domain `99,8%` (meta 90%), use_cases `100,0%` (meta 80%), infra `95,2%`
     (meta 70%) — 3/3 metas, batendo exatamente os números do relatório do builder.
   - `harness rules --mode sweep --changed-only --format ndjson` → 0 linhas, `rc=0`.
   - `git status --short` confirma o escopo alegado: 4 domain + 4 test novos, `docs/INDEX.md`
     modificado (append-only), `ADR-026` e os 2 gates novos — nenhum arquivo de produção
     pré-existente tocado.

## Escopo fora desta task (declarado na própria ADR, não é lacuna)

Renderizador TS (`frontend/src/charts/`) e rota HTTP — `ADR-026` nomeia ambos em "Não fecha";
`T-08.12` é motor Python puro, precedente `T-08.13`/`ADR-024`.

## Regras bloqueantes

Nenhuma das 8 regras de `harness rules list --severity block` se aplica a um módulo de domínio
puro sem I/O, sem exceção engolida, sem import relativo, sem segredo — confirmado por
`ruff`/`mypy`/`harness rules sweep` acima, todos limpos.
