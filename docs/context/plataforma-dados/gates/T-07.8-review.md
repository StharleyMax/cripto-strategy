[REVIEW GATE — Fase 07: `universe_at(ts, filtro)` com `universe_source` inadmissível POR TIPO]
Feature: `plataforma-dados`   Componente: `sentimento`   Card: `CST-62`
PR: https://github.com/StharleyMax/cripto-strategy/pull/88 (`tasks/T-07.8-fase07` → `master`)
Worktree: `/tmp/claude-1002/wt/T-07.8`   Base: `master@5cba0074`
Handoff: `docs/context/plataforma-dados/handoff/T-07.8.md`
QA: `docs/context/plataforma-dados/gates/T-07.8-qa.md` (APPROVED)
Arquivos sob revisão: `backend/src/modules/sentimento/domain/universe_at.py` (novo, 188 linhas),
`backend/tests/sentimento/test_universe_at.py` (novo, 9 testes), `backend/README.md`,
`docs/INDEX.md`.

## Camada 1 — o que o runner mede

`harness rules list --severity block` → **7 regras em vigor** (`core.relative-import`,
`core.silent-except`, `core.print-statement`, `core.hardcoded-secret`,
`web-fullstack.browser-imports-server`, `web-fullstack.tenant-from-request`,
`web-fullstack.server-test-directory-present`).

`harness rules --mode sweep --changed-only` (rodado por mim, independente do QA, na worktree) →
**saída vazia, rc=0**. As 3 regras `web-fullstack.*` são N/A (sem código de `web`/request/tenant
nesta task). Nenhuma das 7 regras bloqueantes acusa achado.

**Regras bloqueantes avaliadas: 7 de 7. Achados: 0.**

## Camada 2 — arquitetura declarada, achado a achado

### [OK] Contrato `Natureza` (`ADR-016`) — domain puro, sem I/O

`bash backend/scripts/natureza.sh` (rodado por mim) → `universo de 65 arquivo(s), 0 leitura(s) de
relogio`. `bash backend/scripts/boundaries.sh` (rodado por mim) → `Contracts: 3 kept, 0 broken`
(146 arquivos, 665 dependências), incluindo `Natureza: domain e use_cases nao falam com socket
nem ssl KEPT`. `universe_at.py` não importa `socket`/`ssl`/`datetime.now`/nenhum client de
storage — os dois "testemunhas" (`snapshot_rows: Sequence[InstrumentRow] | None`,
`s3_witness_symbols: frozenset[str]`) chegam como parâmetros já carregados pelo chamador; a
leitura de snapshot/S3 real fica fora deste arquivo, como o handoff exige.

### [OK] `s3_inferred` excluído POR TIPO, não por `if`/runtime

`DecisiveUniverseSource = Literal["snapshot", "premium_index_witness"]` (sem `s3_inferred`) é o
único tipo na assinatura de `decide_universe_membership(witnesses: Mapping[DecisiveUniverseSource,
frozenset[str]])`. Nenhum `if fonte == "s3_inferred": raise` existe no arquivo (`grep` confirma:
zero `raise`/`Error(`/`Exception(` em `universe_at.py`). QA já mediu com mutação real (Mutação 2
do gate QA) que `mypy --strict` recusa a atribuição no call site de produção — não reproduzi essa
mutação porque QA já a plantou e reverteu no arquivo real, com evidência citável; validei
independentemente que a assinatura declarada é exatamente essa via AST (`inspect`/`ast.parse`
sobre `decide_universe_membership`, mesma técnica do teste estrutural).

### [OK] Reuso real de T-02.1 (`instrument_universe_snapshot.py`) — sem terceira representação

`universe_at.py` importa `InstrumentRow`, `SymbolSetDivergence`, `compare_symbol_sets` de
`instrument_universe_snapshot.py` (grep confirma, linhas 62-66) — o mesmo `compare_symbol_sets`
que `T-02.1`/`T-07.2` já usam. `divergence` do resultado é literalmente essa função, não uma nova
comparação.

**Observação, não bloqueante (já registrada pelo QA, confirmo por leitura própria):**
`universe_at.py` **não importa nada de `dump_survivorship.py`** (`grep -n "dump_survivorship\|
SurvivorshipVerdict\|build_survivorship_gap" universe_at.py test_universe_at.py` → só cita o
módulo em comentário/docstring, nunca importa símbolo dele). O handoff pedia reuso de
`dump_survivorship.py`/`SurvivorshipVerdict`/`build_survivorship_gap` "para a testemunha do S3".
Na implementação entregue, a testemunha S3 entra como `frozenset[str]` puro — decisão de design
defensável sob `ADR-016` (a função de domínio não deveria conhecer o tipo `SurvivorshipVerdict`,
que é resultado de uma decisão de ingestão, só a lista de símbolos que sobrevive dela) e não cria
uma segunda REPRESENTAÇÃO do universo (não há novo dataclass paralelo a `InstrumentRow`). Não
elevo isto a violação: nenhum documento (`SPEC-001`, `ADR-016`, handoff) proíbe explicitamente
receber o witness S3 como frozenset já reduzido, e o próprio handoff nomeia esse ponto como
extensão de escopo futuro (`T-07.14`/`T-07.15`). Fica como leitura registrada, não como achado.

### [WARNING] Idioma de identificador — 3 desvios não cobertos por nenhuma das 12 exceções

`CLAUDE.md` §"Idioma de identificador" declara a superfície **convenção, não portão** — nenhuma
`[[rules.own]]` a mede, e o veredito de conformidade desta revisão não muda por ela (não é regra
bloqueante). Ainda assim são achados reais, citáveis, fora da tabela fechada de 12 exceções:

1. **`backend/src/modules/sentimento/domain/universe_at.py:154-159`** — o parâmetro de produção
   `filtro` em `def universe_at(ts: str, filtro: UniverseFilter | None = None, ...)` e a variável
   local `resolved_filtro` (linha 173) são identificadores em português. Confirmado por AST
   (`ast.parse` sobre a função lista `filtro` entre os argumentos). Não é nome de coluna de
   contrato (linha 11 da tabela) nem deriva do vocabulário de componentes (linha 9) — é
   identificador comum de produção, linha 1 da tabela (`[PREMISSA-OWNER: 2026-08-29]` — "todo
   código gerado é em inglês … var, tudo"). Nenhuma outra ocorrência de `filtro` como
   identificador existe hoje em `backend/src` ou `backend/tests` (`grep -rn '\bfiltro\b'` fora
   deste arquivo → vazio) — não é padrão herdado, é introdução nova desta task. Correção
   concreta: renomear para `universe_filter`/`resolved_filter` (a classe já se chama
   `UniverseFilter`, o parâmetro pode reusar o mesmo substantivo em inglês).
2. **`backend/tests/sentimento/test_universe_at.py:126`** — `def
   test_filtro_market_restricts_the_decided_symbols_to_one_market` carrega `filtro` dentro de um
   nome de teste majoritariamente em inglês — linha 2 da tabela (identificador de teste, "incluindo
   nome `test_*`"). Correção: `test_market_filter_restricts_the_decided_symbols_to_one_market`.
3. **`backend/src/modules/sentimento/domain/universe_at.py:138`** — docstring de
   `UniverseAtResult`: `"the union of both testemunhas, divergence marked"` — palavra portuguesa
   solta, SEM aspas, dentro de prosa em inglês (diferente das citações literais de `SPEC-001`/
   `PRD-001` no mesmo arquivo, que aparecem corretamente entre aspas como transcrição de fonte —
   linhas 10-11, 20-21, 82-83). Linha 5 da tabela (docstring/comentário em inglês). Correção:
   trocar "testemunhas" por "witnesses" (o resto do arquivo já usa esse termo consistentemente:
   `s3_witness_symbols`, "snapshot witness").

Nenhuma mensagem de `raise`/`Error(`/`Exception(` existe neste arquivo ou no teste (`grep`
confirma) — não há o que violar da decisão de 2026-09-02 sobre mensagens de exceção em inglês.

### [OK] `docs/INDEX.md` append-only

`git diff master...HEAD -- docs/INDEX.md` (rodado por mim) → **apenas `+1` linha no final**,
nenhuma linha existente tocada. `backend/README.md` idem: diff mostra só uma seção nova apensada
ao final (`## 📎 2026-09-02 por T-07.8`), nenhuma linha pré-existente alterada ou removida.

### [OK] Commit / autoria

`git log -1 --format='%an <%ae> / %cn <%ce>'` sobre `fabc34d` → autor e committer
`Stharley Maxwell <stharleymax@gmail.com>` nos dois campos; corpo do commit sem
`Co-Authored-By:` em nenhuma variação de caixa.

## Denominador

- Regras bloqueantes existentes: **7** (`harness rules list --severity block`).
- Regras bloqueantes avaliadas nesta revisão: **7 de 7**, via `harness rules --mode sweep
  --changed-only` rodado por mim na worktree — rc=0, saída vazia.
- Arquivos varridos: os **5** do diff `git diff master...HEAD --name-only` (2 de código + 2 docs
  + 1 gate de builder já existente), com leitura integral dos 2 arquivos de código
  (`universe_at.py`, `test_universe_at.py`) e leitura de diff dos outros 3.
- Comandos de arquitetura rodados por mim, independentes do QA: `natureza.sh`, `boundaries.sh`,
  `rules --mode sweep --changed-only`, `git diff` (INDEX/README), `git log` (autoria), AST sobre
  `universe_at.py` (assinaturas + identificadores), 3 `grep` de idioma.
- Achados: **0 BLOCKER, 3 WARNING (idioma, convenção não-portão), 1 observação não-bloqueante**
  (já nomeada pelo QA, confirmada por mim).

## Veredito

**COMPLIANT** — nenhuma das 7 regras bloqueantes em vigor foi violada, `Natureza`/`ADR-016` está
respeitado (domain puro, 0 leituras de relógio, boundaries KEPT), a exclusão de `s3_inferred` é
estrutural (tipo, não `if`), o reuso de `T-02.1` é real e citável, e `docs/INDEX.md` permanece
append-only. Os 3 achados de idioma são WARNING por definição do próprio `CLAUDE.md` (idioma de
identificador é convenção declarada, não portão) e não alteram o veredito — mas são reais,
citáveis linha a linha, e recomendo corrigi-los no mesmo ciclo (mesmo padrão que este repositório
já aplicou às 50 mensagens de exceção em português).
