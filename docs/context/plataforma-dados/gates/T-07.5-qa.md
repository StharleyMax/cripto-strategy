# QA Gate report — T-07.5 · Escritor único; backfill MODELADO nunca sobrescreve captura OBSERVADA

**Card:** `CST-59` · **Componente:** `sentimento` · **Fase:** 07, item 7.7 · **DoD:** `D7.16`
**Branch:** `tasks/T-07.5-fase07` · **Worktree:** `/tmp/claude-1002/wt/T-07.5` · **PR:** #89
**Ciclo:** 2 (ciclo 1 em `git log` desta seção, preservado abaixo por referência — não reescrito,
apenas fechado).

## Bootstrap (repetido no ciclo 2)

```
harness policy --key docs.bootstrap_docs   → ["docs/proposta-discovery.md"]
harness policy --key test_cmd              → lint: bash backend/scripts/lint.sh
                                              test: bash backend/scripts/test.sh
harness rules list --severity block        → 7 regras em vigor
```

## O que mudou entre ciclo 1 e ciclo 2

`git log --oneline`: `c99125b` (fix) seguido de `6d1dd48` (merge de `master`, que só trouxe
`T-07.8` — `universe_at.py`/`test_universe_at.py`, nada de `T-07.5`).

`git diff c41aec6..HEAD -- backend/src/modules/sentimento/use_cases/write_series_row.py
backend/src/modules/sentimento/use_cases/run_single_writer.py` confirma que **as únicas linhas
de produção alteradas desde a aprovação dos pontos D7.16/escritor único no ciclo 1 são**:
- `write_series_row.py:7` e `run_single_writer.py:5`: citação corrigida de
  `tests/sentimento/test_write_series_row_call_sites.py` (inexistente) para
  `tests/sentimento/test_single_writer_call_sites.py` (real).
- `write_series_row.py:1`: `"""The escritor único: ...` → `"""The single writer: ...` — o
  português dentro do docstring em inglês corrigido. A citação literal de `ADR-002/D5` em
  `run_single_writer.py:2-3` (entre aspas, "os coletores 24/7 produzem para fila durável; o
  escritor único é o único que toca a série.") foi mantida intocada — é transcrição de ADR, não
  prosa do docstring, e mexer nela destruiria a citação exata que a doutrina deste repositório
  exige.

Nenhuma outra linha de `write_series_row.py`, `run_single_writer.py`,
`redis_series_write_queue.py` ou `domain/provenance.py` mudou. **Conclusão: os dois achados
`[OK]` do ciclo 1 sobre D7.16 e sobre a guarda estrutural do escritor único continuam válidos
sem replantar mutação** — a lógica provada por mutação no ciclo 1 é byte-idêntica no ciclo 2.

## O falsificador do ciclo 1 — confirmado verde, e generalizado

`backend/tests/sentimento/test_single_writer_call_sites.py::test_the_docstrings_pointing_at_this_guard_cite_a_file_that_exists`
foi reescrito para computar o path citado via `Path(__file__).name` em vez do path hardcoded —
continua válido como falsificador (se o arquivo for renomeado de novo no futuro, o teste
acompanha o nome real e não fica ele mesmo desatualizado).

```
cd backend && ./.venv/bin/python -m pytest -q tests/sentimento/test_single_writer_call_sites.py -v
→ 5 passed in 0.14s
```

Os 5 testes do arquivo: `test_the_scanner_sees_a_direct_call`,
`test_the_scanner_does_not_fire_on_an_unrelated_name`,
`test_exactly_one_production_module_calls_write_series_row` (a guarda),
`test_the_source_tree_the_gate_scans_is_not_empty` (`ADR-012`, universo não-vazio) e o
falsificador do ciclo 1, todos verdes.

## Testes existem e passam — suíte completa

`bash backend/scripts/lint.sh` → `rc=0`. `ruff check` "All checks passed!", `ruff format
--check` 223 arquivos já formatados, `mypy --strict` "Success: no issues found in 223 source
files".

`bash backend/scripts/test.sh` (rodado do zero neste ciclo, aguardado até o PID terminar) →
**`rc=0`, `1184 passed, 3 warnings` em 267,66 s**. Um teste a mais que o ciclo 1 (1183→1184) é
exatamente o falsificador novo somado à suíte. Cobertura total **97,50%**.

```
=== Cobertura por camada (ADR-009/D1) ===
  [OK] domain      99.8% (meta 90%)  [2306/2310 linhas]
  [OK] use_cases  100.0% (meta 80%)  [585/585 linhas]
  [OK] infra       95.1% (meta 70%)  [2187/2300 linhas]
```

Números idênticos aos reportados pelo builder para este ciclo — confirmados por medição
independente, não por citação. `run_single_writer.py` e `write_series_row.py` seguem 100% cada.

## Regras bloqueantes — reavaliadas

- `core.relative-import` — `grep -rn "^from \.\.\?" backend/src/modules/sentimento/**/*.py` →
  nenhum import relativo nos arquivos tocados neste ciclo. **OK**
- `core.silent-except` — nenhum `except: pass`/`except: continue` introduzido pelo fix (fix é só
  docstring). **OK**
- `core.print-statement` — nenhum `print(` novo. **OK**
- `core.hardcoded-secret` — nenhuma credencial nos arquivos tocados. **OK**
- `web-fullstack.*` (3 regras) — fora de escopo, task 100% backend `sentimento`. **N/A**

Regras bloqueantes avaliadas: 7 de 7 listadas por `harness rules list --severity block`.

## Outros achados do ciclo 1 — releitura confirma que continuam válidos

- **D7.16 direção única** (`domain/provenance.py:235-265`,
  `test_only_modelado_is_ever_blocked_by_this_rule`) — arquivo byte-idêntico ao ciclo 1
  (confirmado pelo `git diff` acima, que não lista `provenance.py`). **[OK], sem reteste de
  mutação necessário.**
- **Ler-antes-de-escrever no escritor único** (`ADR-002/D5`, ordem de duas linhas em
  `write_series_row.py:58-72`) — lógica intocada, só o docstring de topo mudou. **[OK]**
- **Escritor único estrutural** — a guarda AST em `test_single_writer_call_sites.py` é a mesma
  do ciclo 1 mais o falsificador atualizado; `SANCTIONED_MODULE` continua apontando
  `run_single_writer.py`. **[OK]**
- **Contrato `Natureza`** (`ADR-016`) — nenhum import de I/O introduzido pelo fix. **[OK]**
- **Idioma de identificador** — o próprio defeito do ciclo 1 era um resíduo de português dentro
  de um docstring em inglês (`escritor único`); corrigido para `single writer`. Nova varredura
  do docstring de `write_series_row.py:1-8` e `run_single_writer.py:1-8`: zero termos em
  português fora da citação literal entre aspas de `ADR-002/D5` (linha 9 da tabela de fronteira
  do `CLAUDE.md` — citação literal de ADR não é identificador de código, não entra na fronteira
  do inglês). **[OK]**
- **Doc delta / mensagens de exceção** — nenhum `raise`/`Error`/`Exception` tocado neste ciclo.
  **[OK], sem mudança.**

## DoD — item a item

- [OK] D7.16 direção única — provado por teste e por mutação no ciclo 1, código inalterado.
- [OK] Ler-antes-de-escrever no escritor único — confirmado por `ADR-002/D5` + `Natureza`,
  código inalterado.
- [OK] Escritor único estrutural — provado por mutação no ciclo 1; guarda releu neste ciclo,
  falsificador verde.
- [OK] Contrato `Natureza` — I/O em `infra`, decisão pura em `domain`, inalterado.
- [OK] Citação exata de evidência — **defeito do ciclo 1 corrigido**: as duas citações apontam
  para `tests/sentimento/test_single_writer_call_sites.py`, arquivo real, confirmado por
  `test_the_docstrings_pointing_at_this_guard_cite_a_file_that_exists` verde.

Regras bloqueantes avaliadas: 7 de 7 listadas por `harness rules list --severity block`.

## Veredito

**APPROVED** — o único motivo de `NEEDS_FIX` do ciclo 1 (citação de arquivo de teste
inexistente) está corrigido e provado pelo falsificador plantado naquele ciclo, que agora passa.
Nenhum ponto novo encontrado neste ciclo.

`tasks.toml`/ledger fechados por `harness gate-record` e `harness tasks resolve`/`validate`
abaixo — nenhuma edição manual.
