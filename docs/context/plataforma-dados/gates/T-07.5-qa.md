# QA Gate report — T-07.5 · Escritor único; backfill MODELADO nunca sobrescreve captura OBSERVADA

**Card:** `CST-59` · **Componente:** `sentimento` · **Fase:** 07, item 7.7 · **DoD:** `D7.16`
**Branch:** `tasks/T-07.5-fase07` · **Worktree:** `/tmp/claude-1002/wt/T-07.5` · **PR:** #89
**Merge com `master`:** já feito antes desta sessão (`git log`: `23274d5` = merge de
`origin/master@c04d59a`); `git fetch origin` + `git merge-base HEAD origin/master` confirmam
`c04d59a` em ambos os lados — worktree já está à frente do `master` atual, nenhum novo merge
necessário.

## Bootstrap

```
harness policy --key docs.bootstrap_docs   → ["docs/proposta-discovery.md"]
harness policy --key test_cmd              → lint: bash backend/scripts/lint.sh
                                              test: bash backend/scripts/test.sh
harness rules list --severity block        → 7 regras em vigor (17 linhas incl. cabeçalho/total)
```

## Regras bloqueantes — avaliadas com evidência

- `core.relative-import` — `grep -rn "^from \.\.\?" backend/src/modules/sentimento/{domain,use_cases,infra}/provenance.py write_series_row.py run_single_writer.py redis_series_write_queue.py` → nenhum import relativo; todos `from src.modules...`. **OK**
- `core.silent-except` — nenhum `except.*pass`/`except.*continue` nos 4 arquivos de produção novos/alterados (leitura direta, ver corpo acima). **OK**
- `core.print-statement` — nenhum `print(` nos arquivos novos; `logger.info(...)` nomeado por módulo em `write_series_row.py:18`. **OK**
- `core.hardcoded-secret` — nenhuma credencial/connection string nos arquivos novos. **OK**
- `web-fullstack.*` (3 regras) — fora de escopo, task é 100% backend `sentimento`. **N/A, não disparam**
- `harness rules --mode sweep --changed-only` (mecanismo do plugin, sweep real) → `rc=0`, sem
  saída — 0 achados sobre os arquivos alterados.

Regras bloqueantes avaliadas: 7 de 7 listadas por `harness rules list --severity block`.

## Testes existem e passam

`bash backend/scripts/lint.sh` → `rc=0`. `ruff check` limpo, `ruff format --check` 223 arquivos
já formatados, `mypy --strict` — "Success: no issues found in 223 source files".

`bash backend/scripts/test.sh` (rodado do zero, aguardado ativamente até o PID terminar, não
disparado em background sem espera) → **`rc=0`, `1183 passed, 3 warnings` em 330,91 s**. Os 3
`ResourceWarning` são o achado pré-existente já nomeado na seção `T-03.11` do README, não desta
task. Cobertura total **97,50%**.

```
=== Cobertura por camada (ADR-009/D1) ===
  [OK] domain      99.8% (meta 90%)  [2306/2310 linhas]
  [OK] use_cases  100.0% (meta 80%)  [585/585 linhas]
  [OK] infra       95.1% (meta 70%)  [2187/2300 linhas]
```

Todos os três pisos acima do alvo declarado — os três arquivos novos (`write_series_row.py`,
`run_single_writer.py`, `redis_series_write_queue.py`) fecham em 100% cada.

`bash backend/scripts/boundaries.sh` → `3 kept, 0 broken` (149 arquivos, 680 dependências):
`infra > use_cases > domain` KEPT, fronteira de contexto `sentimento` KEPT, **`Natureza`: domain
e use_cases não falam com socket nem ssl — KEPT**. Confirma independentemente do relatório do
builder que a lógica `D7.16` não depende de I/O.

## Mutações plantadas e revertidas — a prova do defeito, não só o veredito verde

**Mutação 1 — remover a checagem `D7.16` em `write_series_row.py`** (`if False and
modeled_write_overwrites_observed(...)`, revertido em seguida):
`pytest -q tests/sentimento/test_write_series_row.py` →
**2 failed**:
`test_a_modeled_candidate_over_an_observed_bucket_never_reaches_the_sink` (o sink aceita o
candidato que `D7.16` proíbe) e `test_the_lookup_is_consulted_before_the_sink_is_ever_touched`
(o `and` de curto-circuito também prova que a leitura pararia de acontecer antes da escrita).
`git diff` confirmado limpo após reverter.

**Mutação 2 — segundo call site de produção** (`_qa_mutant_second_writer.py`, novo módulo em
`use_cases/` chamando `write_series_row(row, lookup=lookup, sink=sink)`, deletado em seguida):
`pytest -q tests/sentimento/test_single_writer_call_sites.py::test_exactly_one_production_module_calls_write_series_row`
→ **1 failed** — `len(calls) == 2`,
`['modules/sentimento/use_cases/_qa_mutant_second_writer.py:13:call',
'modules/sentimento/use_cases/run_single_writer.py:70:call']`. A guarda AST estrutural pega o
bypass. `git status --short` confirmado limpo (arquivo mutante deletado) antes de seguir.

Ambas as mutações confirmam o que o relatório do builder já havia demonstrado — reproduzidas de
forma independente por este gate, não aceitas por citação.

## D7.16 — direção única, confirmada por leitura de código e teste

`domain/provenance.py:235-265` — `modeled_write_overwrites_observed(provenance, *,
observed_already_present)` retorna `True` só quando `provenance is Provenance.MODELED and
observed_already_present`. `OBSERVED`, `DERIVED` e `HUMAN` nunca são bloqueados — confirmado por
`test_only_modelado_is_ever_blocked_by_this_rule` (parametrizado nos 3), e por leitura direta:
não há nenhum outro `if` no predicado. "Modelado preenche gap" (`observed_already_present=False`)
retorna `False`, aceito — `test_a_modeled_candidate_filling_a_gap_reaches_the_sink`.

## Ler-antes-de-escrever vive na aplicação, não no motor

`ADR-002-motor-de-armazenamento.md:69-75` (D5), lido inteiro: "todos os caminhos de escrita
convergem para um processo escritor... as duas invariantes vivem nele", e nomeia
explicitamente que a garantia "vale nos cinco candidatos... porque a decisão de recusar sob
divergência de `knowledge_time` é da aplicação em qualquer motor". `write_series_row.py:58-72`
chama `lookup.observed_already_present(row)` ANTES de `sink.accept(row)`, como ordem de duas
linhas, não comentário — confirmado por `test_the_lookup_is_consulted_before_the_sink_is_ever_touched`
(um lookup que sempre levanta impede `sink.accept` de ser alcançado). Nenhum sink concreto
existe ainda (`ADR-002/D4`, candidato 4×5, `T-08.1` bloqueado) — `SeriesSink`/`ObservedLookup`
são `Protocol`s puros, e `boundaries.sh`/`Natureza` confirma `domain`/`use_cases` sem
`socket`/`ssl`. Não há dependência de `ReplacingMergeTree` nem de nenhum engine equivalente.

## Escritor único — garantia de design, provada por teste (não convenção)

`test_single_writer_call_sites.py::test_exactly_one_production_module_calls_write_series_row`
varre `backend/src` por AST (149 módulos) e exige exatamente 1 chamada de produção a
`write_series_row`, dentro de `run_single_writer.py`. Falsificador da própria guarda
(`test_the_scanner_sees_a_direct_call`) e teste de universo não-vazio
(`test_the_source_tree_the_gate_scans_is_not_empty`, `ADR-012`) presentes. Mutação 2 acima prova
que a guarda realmente reprova um segundo call site.

Ponto cego nomeado no próprio arquivo (herdado de `test_verified_edge_call_sites.py`):
`getattr(modulo, "write_series_row")(...)` com nome calculado em runtime não é visto — mesmo
limite que `[tool.importlinter]` já documenta. Não é um defeito desta task; é um limite
declarado, coerente com o resto do repositório.

## Contrato `Natureza` (`ADR-016`)

`domain/provenance.py` — sem import de `socket`/`ssl`/I/O; `modeled_write_overwrites_observed`
recebe `observed_already_present: bool` já computado pelo chamador, nunca lê um store.
`use_cases/write_series_row.py` e `run_single_writer.py` só falam com `Protocol`s (`ObservedLookup`,
`SeriesSink`, `SeriesWriteQueue`) — zero I/O concreto. `infra/redis_series_write_queue.py` é o
único lugar que toca `RedisStreamConsumerGroup` (I/O real, reusando `T-07.4`, nenhuma segunda
conexão Redis aberta). `boundaries.sh` confirma as três camadas KEPT.

## Idioma de identificador — inglês, sem exceção não-declarada

Varredura por AST (não regex) de `FunctionDef`/`ClassDef`/`Name`/`arg` nos 9 arquivos novos/
alterados desta task contra o vocabulário PT que `T-07.8` pegou (`filtro`, `testemunha(s)`,
`janela`, `numero`, `ausencia`, etc.) → **0 ocorrências**. Varredura por AST de toda chamada
`*Error(...)`/`*Exception(...)` com argumento string/f-string nos arquivos de produção novos →
**7 mensagens, todas em inglês** (`provenance.py` × 6, `redis_series_write_queue.py` × 1).
Nenhuma mensagem em português sobrou.

## Doc delta — append-only, sem conflito com `T-07.8`

`git diff origin/master...HEAD -- docs/INDEX.md backend/README.md`: uma linha nova em
`docs/INDEX.md` (append, a linha de `T-07.8` já mesclada permanece intocada logo acima), uma
seção nova `## 📎 2026-09-02 por T-07.5` em `backend/README.md` (nenhuma seção existente
reescrita). Correção incidental correta: `test_as_of_is_the_single_reader.py` ganhou a 4ª
entrada declarada em `DECLARED_TOUCHERS` (`write_series_row`) e corrigiu um número de módulos já
desatualizado (36→116) que preexistia à task — nomeado no próprio diff, não escondido.

## Defeito real encontrado — mensagens de produção citam arquivo de teste inexistente

`write_series_row.py:7` e `run_single_writer.py:5-6` citam
`tests/sentimento/test_write_series_row_call_sites.py` como a prova estrutural do escritor
único. **Esse arquivo não existe** — o arquivo real é
`backend/tests/sentimento/test_single_writer_call_sites.py`
(`ls backend/tests/sentimento/ | grep call_sites` confirma). Isso é o mesmo defeito que a
doutrina deste repositório nomeia para número sem comando: uma citação que ninguém segue até o
fim. **Falsificador plantado e confirmado:**
`test_the_docstrings_pointing_at_this_guard_cite_a_file_that_exists`, novo, em
`backend/tests/sentimento/test_single_writer_call_sites.py`
(`pytest -q tests/sentimento/test_single_writer_call_sites.py::test_the_docstrings_pointing_at_this_guard_cite_a_file_that_exists`
→ **1 failed**, `AssertionError`: "write_series_row.py and run_single_writer.py cite
'tests/sentimento/test_write_series_row_call_sites.py', but no such file exists"). Lint limpo
sobre o teste novo (`ruff check`/`format --check`/`mypy --strict`). Suíte completa
`tests/sentimento/` rodada com o teste novo incluído: apenas essa 1 falha, nenhuma outra
regressão.

Este é um defeito de PRODUÇÃO (docstring errada em `write_series_row.py`/`run_single_writer.py`)
— fora do escopo deste QA (só edita teste), reportado como `NEEDS_FIX`.

## DoD — item a item

- [OK] D7.16 direção única (modelado nunca vence observado; observado sempre vence; modelado
  preenche gap) — provado por teste e por mutação.
- [OK] Ler-antes-de-escrever no escritor único da aplicação, sem depender de engine — confirmado
  por `ADR-002/D5` + `Natureza`/`boundaries.sh`.
- [OK] Escritor único estrutural, provado por teste AST — confirmado por mutação (segundo call
  site pego).
- [OK] Contrato `Natureza`: I/O em `infra`, decisão pura em `domain`.
- [FAIL] Citação exata de evidência (doutrina deste repo) — dois docstrings de produção citam um
  arquivo de teste que não existe.

Regras bloqueantes avaliadas: 7 de 7 listadas por `harness rules list --severity block`.

## Veredito

**NEEDS_FIX** — não pelo D7.16 nem pelo escritor único (ambos provados, inclusive por mutação),
mas por um defeito real de produção fora do meu escopo de edição.

### Ações
1. Em `backend/src/modules/sentimento/use_cases/write_series_row.py:7` e
   `run_single_writer.py:5-6`, corrigir a citação de
   `tests/sentimento/test_write_series_row_call_sites.py` para
   `tests/sentimento/test_single_writer_call_sites.py` (o nome real do arquivo).
2. Rodar `pytest -q backend/tests/sentimento/test_single_writer_call_sites.py` para confirmar
   que `test_the_docstrings_pointing_at_this_guard_cite_a_file_that_exists` (teste desta gate,
   deixado no branch) passa a verde.
3. Manter o restante do build intocado — nenhuma outra ação apontada por este gate.

**`tasks.toml`, ledger e Jira INTOCADOS; nenhum `gate-record`, `approve` ou `advance` disparado
por este QA além do `gate-record` de veredito abaixo.**
