# QA Gate Context Block — Fase 07: Paginador correto (T-07.1)

**Feature:** `plataforma-dados`
**Componente:** `sentimento`
**Card:** `CST-55`
**Spec:** `docs/specs/SPEC-001-plataforma-dados.md` §5.7
**Plan:** `docs/plans/SPEC-001-plataforma-dados/07_aquisicao_em_regime.md`, itens 7.1+7.2+7.4
**Refs:** `CA-F3-2`, `CA-F3-1`, `CA-F3-5`, `D7.1`, `D7.3`, `D7.4`, `D7.5`, `D7.8`

## Arquivos alterados

- `backend/src/modules/sentimento/domain/oi_history_paginator.py` (new)
- `backend/src/modules/sentimento/domain/s3_bucket_listing.py` (new)
- `backend/src/modules/sentimento/infra/binance_oi_history_client.py` (new)
- `backend/src/modules/sentimento/infra/binance_dump_bucket_listing_client.py` (new)
- `backend/tests/sentimento/test_oi_history_paginator.py` (new, 17 testes)
- `backend/tests/sentimento/test_s3_bucket_listing.py` (new, 6 testes)
- `backend/tests/sentimento/test_binance_oi_history_client.py` (new, 6 testes)
- `backend/tests/sentimento/test_binance_dump_bucket_listing_client.py` (new, 6 testes)
- `backend/README.md` (modified — doc delta, seção `### 📎 2026-09-02 por T-07.1`)

## DoD

- [x] **D7.1** — `api_code == -1130` classificado como `end_of_history` ⇒ `verdict='REJECTED'`,
      zero pontos escritos. `test_end_of_history_api_code_is_rejected_with_zero_rows_written`.
- [x] **D7.3** — replay do caso "resposta com cauda de hoje para janela antiga, HTTP 200, sem
      aviso" ⇒ `REJECTED`, zero pontos. `test_d7_3_the_binance_measured_exploit_is_rejected_not_accepted`
      planta exatamente esse cenário (`OiHistoryPageResponse(status=200, points=(_point(hoje),))`
      contra uma janela de 60 dias atrás) e prova que `classify_page` recusa, não aceita.
- [x] **D7.4** — invariante permanente: qualquer ponto fora de `[start_time_ms, end_time_ms]`
      reprova a página inteira. `test_d7_4_the_permanent_invariant_one_bad_point_rejects_the_whole_page`
      (um único ponto ruim entre três bons ainda reprova tudo).
- [x] **D7.5** — sem teto de linhas: 501 pontos dentro da janela são aceitos e escritos, contra o
      máximo documentado de 500. `test_d7_5_a_page_with_501_points_inside_the_window_is_accepted_uncapped`.
- [x] **D7.8** — falha se `IsTruncated=true` sem paginação subsequente.
      `test_d7_8_the_falsifier_merging_only_the_first_of_two_truncated_pages_is_refused` (unidade,
      contra `merge_pages`) e `test_d7_8_list_all_object_keys_follows_nextcontinuationtoken_across_pages`
      (ponta a ponta, XML real de 2 páginas, 500+480=980 chaves).

## Comandos rodados (literais) e resultado

- `bash backend/scripts/lint.sh` → `All checks passed!` / `188 files already formatted` /
  `Success: no issues found in 188 source files` (ruff check + ruff format --check + mypy
  --strict). Universo: 188 arquivos.
- `bash backend/scripts/test.sh` → **rc=0**, `1063 passed, 3 warnings in 504.14s` (era 1029 em
  `master@031e007`, `+34` desta task). Universo: 1063 testes coletados.
  Cobertura por camada (`check-coverage-layers.sh`):
  `domain 99,9%` (meta 90%) · `use_cases 100,0%` (meta 80%) · `infra 95,2%` (meta 70%) —
  os 3 arquivos novos de `domain`/`infra` fecham em 100% cada um; a queda de infra vs. o total
  do repositório (95,2% < cobertura global 97,84%) é pré-existente (`system_probe_clock.py`,
  `daily_instrument_universe_snapshot_cli.py`, não tocados por esta task).
- `bash backend/scripts/boundaries.sh` → `Camadas por contexto: infra > use_cases > domain KEPT`,
  `Fronteira de contexto: sentimento nao importa outro contexto KEPT`,
  `Natureza: domain e use_cases nao falam com socket nem ssl KEPT` — `3 kept, 0 broken`.
  Universo: 131 arquivos, 592 dependências.
- `bash backend/scripts/natureza.sh` → `natureza: universo de 57 arquivo(s), 0 leitura(s) de
  relogio`.
- `harness rules --mode sweep --changed-only` (rodado após `git add -A`, arquivos staged) →
  `rc=0`, 0 achados. Primeira rodada (antes do ajuste de docstring) achou 4 `[AVISO]`
  `core.module-docstring-single-line` — não bloqueante, corrigido no mesmo ciclo (docstring de
  módulo virou uma linha + comentário `#`, padrão já usado em `dump_window.py` e
  `binance_futures_snapshot_client.py`).
- `harness rules --mode sweep` (completo, sem `--changed-only`) → `rc=0`, os únicos achados são
  3 `[AVISO]` pré-existentes em `use_cases/run_availability_probe.py` e
  `infra/availability_probe_cli.py`/`availability_http_client.py` (fora do diff desta task) e 2
  `[AVISO]` de `frontend/` — nenhum novo, nenhum bloqueante.

## Cobertura

97,84% total (medido: `bash backend/scripts/test.sh`, `TOTAL 4584 stmts, 97 miss, 98%` na
tabela `coverage.xml`/`term-missing`). Piso por camada: domain 99,9% (meta 90%) · use_cases 100%
(meta 80%) · infra 95,2% (meta 70%) — todos acima do piso declarado em `harness policy --key
test_cmd`/`check-coverage-layers.sh`.

## Doc delta

- `backend/README.md`: seção nova `### 📎 2026-09-02 por T-07.1 — o paginador correto: janela
  [startTime, endTime] fechada, enumerada A PRIORI, e a listagem S3 por NextContinuationToken`,
  acrescentada ao final do arquivo (append, sem reescrever seção existente).
- `docs/INDEX.md`: linha nova apontando para este relatório (append-only).
- ADR: não necessário — nenhuma decisão arquitetural nova além do que `SPEC-001` §5.7 e o handoff
  já fixavam; a implementação segue o padrão de conexão já estabelecido por
  `binance_futures_snapshot_client.py`/`coinalyze_history_client.py`.

## Bloqueado (se houver)

Nenhum. Escopo desta task (plano `07` itens 7.1+7.2+7.4) fechado por completo. Fora de escopo,
nomeado: survivorship na borda de ingestão (`T-07.2`), dedupe por hash de conteúdo (`T-07.3`),
escritor único / `md.ingest_run` real (`T-03.8`/`T-07.5`) — nenhum destes é tocado aqui.

**`tasks.toml`, ledger e Jira INTOCADOS; nenhum `gate-record`, `approve` ou `advance`** — veredito
é do `/qa`.
