# `T-03.6` — relatório de gate do `/review`

**Feature:** `plataforma-dados` · **Componente:** `sentimento` · **Fase:** `03`
**PR:** https://github.com/StharleyMax/cripto-strategy/pull/73 · **Commit:** `85a3e6a`
**Branch:** `tasks/T-03.6-availability-probe-set` · **Worktree:** `/tmp/claude-1002/wt/T-03.6`

## Veredito: **COMPLIANT**

Denominador: **7 regras bloqueantes em vigor** (`harness rules list --severity block`), avaliadas
todas via `harness rules --mode sweep --changed-only --format ndjson` → `rc=0`, saída vazia, **0
achado bloqueante** sobre os **16 arquivos** do diff (`git diff --name-only master...HEAD`).

## O que a máquina mediu

1. **Natureza (ADR-016):** `bash backend/scripts/natureza.sh` → universo de 55 arquivo(s) em
   `domain`+`use_cases` do módulo `sentimento`, **0 leitura(s) de relógio**.
2. **Boundaries:** `bash backend/scripts/boundaries.sh` → `Contracts: 3 kept, 0 broken` (camadas,
   fronteira de contexto, natureza) sobre 125 arquivos / 572 dependências.
3. **Sweep de regras:** `harness rules --mode sweep --changed-only --format ndjson` → `rc=0`,
   saída vazia.

## O que só a arquitetura declarada diz (julgamento, com citação)

- **`observer_region`:** confirmado que nenhum arquivo novo implementa captura real — todo uso
  aponta para `UNKNOWN_OBSERVER_REGION` (`domain/provenance.py`), inclusive o default do CLI em
  `infra/availability_probe_cli.py:154-155`. Escopo de `T-03.9` respeitado.
- **Broker de cota:** `use_cases/run_availability_probe.py` reusa `domain/local_quota_broker.py`
  (`T-02.2`) sem modificação de contrato — `LocalQuotaBroker` documenta a si mesmo como "fixed-
  interval pace over a BLIND bucket — no header read, no acceleration, no burst"
  (`domain/local_quota_broker.py:38`). Igual ao padrão já aceito.
- **`docs/INDEX.md` / `backend/README.md`:** `git diff master...HEAD --stat -- docs/INDEX.md
  backend/README.md` → vazio, nenhum dos dois arquivos tocado nesta rodada — consistente com o
  gate do builder (`docs/context/plataforma-dados/gates/T-03.6-builder.md` §5).
- **Idioma de identificador (`CLAUDE.md`, tabela de 12 linhas):** nomes de função/classe/variável
  nos 8 arquivos novos de `src/` são 100% inglês — nenhum achado em varredura por radicais
  portugueses fora de string literal. **Sem violação de regra/linha da tabela.**

## WARNING (documento, não regra — não muda o veredito)

`[WARNING-1] Mensagens de exceção em português cresceram sem registro — CLAUDE.md §"A lacuna
conhecida desta tabela — mensagem de exceção"`. O PR adiciona **~11 novos `raise X("…")`** com
texto em português nascendo em `domain`/`use_cases` (`availability_probe_set.py:155,176,180,186`;
`availability_poll.py:50,84,100`; `availability_lag_stats.py:44,73`; `availability_lag.py:41`;
`run_availability_probe.py:113`), além de `help=` de `argparse` em português em
`availability_probe_cli.py:150,155` (esta última plausivelmente coberta pela linha 8 da tabela —
microcopy de operador de CLI local, já com precedente em `quota_ramp_cli.py`; não contestada
aqui). O `CLAUDE.md` documenta esta superfície como **`⏸ NÃO DECIDIDO`, dono `/architect`**, com
gatilho explícito: *"se o número de mensagens em português subir, a lacuna deixou de ser inércia
e virou rampa — e volta à mesa com um caso concreto"* (medição-base: 3 PT / 2 EN em
2026-08-29). Este PR é exatamente esse caso concreto — a contagem sobe de 3 para ~14. Não é
regra bloqueante (a tabela está congelada em 12 linhas por `CA-F1-1`, e a mensagem de `raise` é
prosa adjacente, não 13ª linha) — **não entra no veredito**, mas o dono declarado (`/architect`)
deveria ver este número antes que a rampa cresça mais.

## Escopo auditado

16 arquivos do diff `master...HEAD`: 8 novos em `backend/src/modules/sentimento/{domain,infra,
use_cases}/`, 7 novas suítes em `backend/tests/sentimento/`, 1 gate doc do builder. Nenhum outro
arquivo do repositório tocado.

## Comandos rodados (reprodutíveis)

```
harness rules list --severity block
bash backend/scripts/natureza.sh
bash backend/scripts/boundaries.sh
harness rules --mode sweep --changed-only --format ndjson
git diff --stat / --name-only master...HEAD
git diff master...HEAD --stat -- docs/INDEX.md backend/README.md
grep -rn observer_region backend/src/modules/sentimento/
```
