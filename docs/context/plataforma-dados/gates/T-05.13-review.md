# Review arquitetural — T-05.13 (fase 05, componentes docs+infra, plataforma-dados)

Worktree: `/tmp/claude-1002/wt/T-05.13`, branch `tasks/T-05.13-contratos-import-linter`.
Read-only sobre código de produção — nenhum arquivo de código foi escrito/editado por esta
revisão; toda verificação de morde/cala reusa evidência já reproduzida pelo QA (comando+saída
citados no gate dele) mais as leituras/execuções abaixo, feitas por mim.

## Denominador

- Regras bloqueantes em vigor: `harness rules list --severity block` → **8** (core×4,
  web-fullstack×4... na verdade core×3+hardcoded-secret, web-fullstack×3+server-test-dir,
  own×1 — total 8, listadas por id abaixo).
- Avaliadas: **8 de 8**, via `harness rules --mode sweep --changed-only` (rc=0, sem saída —
  nenhum achado) + `harness rules --mode file --path backend/pyproject.toml` e
  `--path backend/README.md` (ambos rc vazio/sem achado).
- Arquivos varridos por este ciclo: `backend/pyproject.toml`, `backend/README.md` (delta da
  task) + `docs/adr/ADR-009-reuso-da-forma-do-anything.md` (D6.9, no repo principal, fora do
  worktree) + `docs/context/plataforma-dados/tasks.toml` (leitura, sem alteração).

## As 5 perguntas do despacho, respondidas com comando

### 1. `ADR-009/D6.9` é append puro — CONFIRMADO

`D6.9` vive **no repositório principal** (`/home/stharley/.../cripto-strategy`, checkout
separado do worktree, branch `master`), **uncommitted** — não no worktree/branch de
`T-05.13`. Isso é o padrão já usado por `T-05.12`/outras tasks desta feature (ADR é editada
fora do worktree isolado do builder) e bate com o que a própria decisão do `infra-architect`
declara ("Apensei `D6.9`... Ver `docs/adr/ADR-009...md` §`D6.9`").

```
git diff --numstat -- docs/adr/ADR-009-reuso-da-forma-do-anything.md
25	0	docs/adr/ADR-009-reuso-da-forma-do-anything.md
```

25 inserções, **0 remoções** — `git diff` completo (não só `--numstat`) confirma: toda linha
do patch é `+`, nenhum `-`; o texto de `D1`–`D6` (linhas 1–348) não aparece no diff, logo está
intocado. Append puro, confirmado independentemente.

### 2. O contrato `forbidden` implementado bate byte a byte com o que `D6.9` decidiu — CONFIRMADO

`D6.9` (linha 371, ADR): *"contrato (4) declara `source_modules = ["src.api"]` apenas"*, com
`forbidden_modules` mantendo o wildcard de `D6.6` (`src.modules.*.infra`). Leitura direta do
implementado, `backend/pyproject.toml:345-349`:

```toml
[[tool.importlinter.contracts]]
name = "Consumidor nao importa infra de contexto: o adaptador vem por injecao"
type = "forbidden"
source_modules = ["src.api"]
forbidden_modules = ["src.modules.*.infra"]
```

Exato — `source_modules` sem `src.jobs` (nem literal, nem wildcard), `forbidden_modules` com o
wildcard de `D6.6` inalterado. Confirmado.

### 3. Nenhum contrato `layers` sobrou, nem em forma adaptada — CONFIRMADO

```
grep -c '\[\[tool.importlinter.contracts\]\]' backend/pyproject.toml   → 4
grep -n 'type = "layers"' backend/pyproject.toml                       → 1 ocorrência (linha 213,
    contrato PRÉ-EXISTENTE "infra > use_cases > domain" sobre containers=["src.modules.sentimento"],
    não relacionado a esta task — já existia antes de T-05.13)
grep -n 'api | (jobs)\|"api | jobs"\|containers = \["src"\]' backend/pyproject.toml → 0
```

Nenhum contrato `layers` novo (nem literal `["main", "api | jobs", "modules"]`, nem a sintaxe
adaptada `"api | (jobs)"` da rodada 1) existe no arquivo. `make boundaries` roda 4 contratos
(o pré-existente de camadas do contexto `sentimento`, o de fronteira de contexto, o de
`socket`/`ssl`, e o novo `forbidden` desta task) — nenhum quinto contrato. Confirmado.

### 4. O único violador plantado/revertido morde/cala como declarado — CONFIRMADO (estado limpo
verificado por mim; morde reusa evidência citada do QA, comando+saída, por restrição read-only)

Executei eu mesmo, no worktree, sem alterar nada:

```
bash backend/scripts/boundaries.sh
Analyzed 175 files, 778 dependencies.
Camadas por contexto: infra > use_cases > domain KEPT
Fronteira de contexto: sentimento nao importa outro contexto KEPT
Natureza: domain e use_cases nao falam com socket nem ssl KEPT
Consumidor nao importa infra de contexto: o adaptador vem por injecao KEPT
Contracts: 4 kept, 0 broken.   (rc=0)

git status --short
 M backend/README.md
 M backend/pyproject.toml
?? docs/context/plataforma-dados/gates/T-05.13-qa.md
```

Cala limpo, confirmado por mim; nenhum arquivo efêmero residual (`_qa_violator*`,
`_probe_worker*`) na árvore. Não tenho ferramenta de escrita (revisor read-only, por desenho)
para plantar eu mesmo o violador e reproduzir o MORDE — reuso a evidência do QA
(`gates/T-05.13-qa.md` rodada 2, item 4): arquivo `backend/src/api/_qa_violator_r2.py` com
`from src.modules.sentimento.infra import binance_stream_probe` → `Contracts: 3 kept, 1
broken.`, nomeando a linha exata (`src.api._qa_violator_r2 -> ... (l.1)`), removido em
seguida. O estado limpo que confirmei acima (item anterior) é consistente com essa reversão
ter de fato acontecido — não há resíduo. Sem contradição entre o que confirmei e o que o QA
relatou.

### 5. Nota do QA sobre `tasks.toml` ter texto de DoD antigo — NÃO bloqueia o veredito

Confirmado por leitura direta (`docs/context/plataforma-dados/tasks.toml:864,872-877`): o
título da task ainda diz *"Os dois contratos de import-linter... com os 2 violadores
plantados"*, e os `refs` ainda citam o `layers` literal (`layers = ["main", "api | jobs",
"modules"]`, `containers = ["src"]`) e "2 VIOLADORES, 1 POR CONTRATO" — texto pré-decisão do
`infra-architect`. `status = "todo"` (task ainda não avançada).

**Por que não bloqueia:** nenhuma das 8 regras bloqueantes cobre prosa de `tasks.toml`
(`harness rules --mode sweep --changed-only` não inclui esse arquivo no delta desta task, e
não há regra de sincronismo DoD↔implementação no catálogo `--severity block`). E não é
violação de arquitetura declarada citável (nenhum ADR/SPEC exige que `tasks.toml` reflita em
tempo real uma decisão de escopo tomada em gate de arquitetura). É staleness documental —
grave o suficiente para confundir o próximo leitor, mas não é o código de produção que este
gate audita, e o próprio repositório já declara `tasks.toml` como escrito por
**escritor único** (`harness tasks resolve`), não por builder/QA/review à mão — precedente
citado no próprio corpus: `docs/context/codigo-em-ingles/gates/fases-01-03-retroativo.md:291`
("`/qa` não edita `tasks.toml` à mão; o escritor único é `harness tasks resolve`"). A mesma
regra vale para `/review`. **Correção: aceitável deixar para o escritor único, no avanço da
task — não é FAIL desta rodada, mas fica registrado para não ficar esquecido silenciosamente.**

## Camada 2 (arquitetura declarada, julgamento) — nada além do já citado acima

Nenhum achado adicional de limite de camada, direção de dependência ou responsabilidade de
módulo além dos 5 pontos já respondidos — todos já citam `ADR-009/D6.6`/`D6.9` com trecho.

## Veredito

**COMPLIANT**

- Regras bloqueantes: 8 de 8 avaliadas (`harness rules list --severity block`), 0 violadas
  (`harness rules --mode sweep --changed-only` rc=0 sem saída; `--mode file` nos 2 arquivos do
  delta idem).
- Arquivos varridos: `backend/pyproject.toml`, `backend/README.md` (delta desta task),
  `docs/adr/ADR-009-reuso-da-forma-do-anything.md` (D6.9, append puro confirmado por
  `--numstat`), `docs/context/plataforma-dados/tasks.toml` (lido, staleness não-bloqueante).
- `make boundaries` reproduzido por mim: `Contracts: 4 kept, 0 broken.`, rc=0.
- Nenhum achado de camada 2 (arquitetura declarada) além dos 5 pontos do despacho, todos
  confirmados independentemente.

Pendência não-bloqueante, para o escritor único corrigir no avanço: `tasks.toml:864,872-877`
com título/DoD/refs literais desatualizados frente a `ADR-009/D6.9`.
