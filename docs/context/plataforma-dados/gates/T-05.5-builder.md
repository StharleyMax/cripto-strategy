# T-05.5 — builder report (self-attested, NOT final QA/review)

[QA GATE — Fase 05: S2-mínima, item 5.7/D5.5 — price_source/price_use no painel de Preço]
Feature: plataforma-dados     Componente: charts
Arquivos alterados:
- frontend/src/charts/s2-price-source.ts (new)
- frontend/src/charts/s2-price-source.test.ts (new)
- frontend/src/charts/s2-annotation-price-binding.ts (new)
- frontend/src/charts/s2-annotation-price-binding.test.ts (new)
- frontend/src/charts/s2-panels.ts (modified — `buildPricePanel` passa a exigir `priceUse`, `PricePanel` agora carrega `priceSource`+`priceUse`)
- frontend/src/charts/s2-panels.test.ts (modified)
- frontend/src/charts/s2-axis-integration.test.ts (modified — segue a nova assinatura de `buildPricePanel`)
- frontend/README.md (modified — §13 nova, doc delta)
- frontend/package-lock.json (modified — incidental, `npm install` em worktree novo, node_modules gitignored)

Spec: docs/adr/ADR-007-price-source-por-uso.md (PS-1, PS-3)
Plan: docs/plans/SPEC-001-plataforma-dados/05_fatia_visivel.md (item 5.7, DoD D5.5)

DoD:
- [x] item 5.7 — "o painel de Preço declara `price_source` E `price_use` na linha do painel", verificado por `npm --prefix frontend run test:charts` → "buildPricePanel: 4 gapless days at 1-minute resolution, BTCUSDT" + "T-05.5/5.7: the price panel declares price_source AND price_use on the panel row" (ambos PASS)
- [x] D5.5 — "a marcação fica amarrada à série de preço": marcar sob `price_source = klines_last` e reabrir sob `mark_price` ⇒ NÃO reexibida como a mesma (ou rotulada `marcada sobre outra série de preço`), verificado por `npm --prefix frontend run test:charts` → "D5.5 falsifier: mark under klines_last, reopen under mark_price — NOT reexhibited as the same mark" (PASS) + controle positivo "positive control: reopening under the SAME price_source carries no mismatch label" (PASS, prova que o comparador não é um `false` fixo)
- [x] PS-1 (mecanismo, não convenção) — `price_use` omitido (`null`) é erro, nunca default silencioso, verificado por "PS-1 falsifier: omitting price_use (null) throws MissingPriceUseError, never a default" (PASS) + "PS-1 falsifier: a price_use outside the closed set throws InvalidPriceUseError, never a default" (PASS)

Comandos rodados (literais) e resultado:
- `npm --prefix frontend install` → "up to date, audited 129 packages" (node_modules ausente por padrão em worktree novo, instalado antes do gate)
- `npm --prefix frontend run lint` → `eslint src`, saída vazia = 0 erro, 0 aviso (universo: todo `frontend/src`)
- `npm --prefix frontend run test:charts` → `node --test 'src/charts/*.test.ts'` → **78 pass, 0 fail** (universo: 9 arquivos `*.test.ts` em `frontend/src/charts/`, incluindo os 2 novos deste task — 8 testes novos: 3 em `s2-price-source.test.ts`, 5 em `s2-annotation-price-binding.test.ts`)
- `harness rules --mode sweep --changed-only` → rc=0, 0 achado (cobre a regra `path-presence` repo-wide; `--changed-only` usa `git diff --name-only HEAD`, que devolveu vazio porque o commit já estava feito quando este gate rodou — universo real coberto pela chamada seguinte)
- `harness rules --mode file --path <arquivo>` rodado individualmente para os 8 arquivos de código/doc alterados no commit `6131ce2` (excluído `package-lock.json`, não é código) → rc=0, 0 achado em todos os 8 (universo: 8 arquivos)

Cobertura: sem piso declarado para `charts` (`harness policy --key test_cmd` só declara `sentimento`). Medição qualitativa: as 2 funções novas (`resolvePriceSource`, `describeAnnotationOnReopen`) e o construtor novo (`createPriceBoundAnnotation`) têm teste para cada ramo de decisão — as 5 entradas de `PRICE_USES`, os 2 casos de recusa de PS-1 (`null` e valor inválido), o falsificador D5.5 e seu controle positivo, e a propagação da recusa de D5.10 (`principalId` vazio) através do novo construtor. [MEDIDO 2026-09-03 via `npm run test:charts`, n=78 testes, 0 fail]

Doc delta:
- frontend/README.md: atualizado — §13 nova (append-only), motivo: registrar o que `T-05.5` fecha, os arquivos novos, o teste negativo D5.5 literal e os comandos
- docs/adr/ADR-007-price-source-por-uso.md: sem mudança — PS-1/PS-3 já fixam a decisão; esta task consome o lado `charts`, não emenda a ADR
- frontend/src/charts/s2-annotation-identity.ts: sem mudança — D5.10's construtor e recusa (`createAnnotationIdentity`) continuam intocados, reusados por composição em `s2-annotation-price-binding.ts`
- ADR: não necessário — decisão já registrada em ADR-007, esta fase apenas a torna executável no lado `charts`

Bloqueado: `git push -u origin tasks/T-05.5-price-source-use` reporta "failed to push some refs" no pre-push hook local (`[push] RECUSADO — nenhuma ref no protocolo do pre-push (entrada padrao vazia)`), mas verificado NO-OP: `git rev-parse HEAD` = `6131ce268fef07604e33775bd03de345ab286baa`, `git ls-remote origin refs/heads/tasks/T-05.5-price-source-use` = mesmo sha — o branch remoto já tem exatamente este commit (empurrado por invocação anterior desta mesma task). O hook recebe stdin vazio porque não há ref-update real a fazer (local==remote), e o `require-push` do harness trata isso como recusa em vez de no-op. Sem efeito no estado remoto. Não contornado com `--no-verify` (proibido).
