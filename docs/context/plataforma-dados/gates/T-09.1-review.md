[REVIEW GATE — Fase 09] Feature: plataforma-dados · Task: T-09.1 (CST-83) · Componente: docs

## Denominador
- Regras bloqueantes em vigor: 8 (`harness rules list --severity block`). Nenhuma tem escopo
  `docs`; confirmado empiricamente (não por suposição) por `harness rules --mode sweep
  --changed-only` → saída vazia, rc=0, reproduzido neste review.
- Arquivos no escopo (git status --porcelain): 6 — docs/INDEX.md (M), tasks.toml (M),
  docs/adr/README.md (novo), gates/T-09.1-builder.md, T-09.1-qa.md, T-09.1-qa-ciclo2.md (novos).
- Camada 2 (arquitetura declarada): avaliada contra CLAUDE.md §"Nenhum número sem o comando
  que o produziu" e §"Registro de artefatos é append-only".

## Reprodução independente
- `ls docs/adr/ADR-*.md | wc -l` → **26**. `ls docs/adr/*.md | wc -l` → **27** (self-match
  confirmado, README.md conta a si mesmo). README.md e builder.md usam `ADR-*.md` em toda
  citação — fix do ciclo 1 confirmado, consistente.
- `grep -c "^#\{2,3\} .*[Ff]alsificador" docs/adr/ADR-*.md | awk -F: '$2==0'` → vazio (26/26 OK).
- `comm` de D9.1 partes a/b → ambos vazios, reproduzidos.
- `git diff --stat -- docs/plans` → vazio. docs/plans/ intocado, decisão de não editar o "9"
  do plano aprovado confirmada e correta (ADR-010 §5).
- `git diff docs/context/plataforma-dados/tasks.toml`: 9 hunks, todos append — 3 inserções de
  linha nova dentro de array multilinha (T-05.11, T-05.14, T-08.6) e 6 substituições de
  `refs=[...]` de uma linha pela mesma lista + 1 item no fim (T-08.4, T-08.7, T-08.8, T-08.12,
  T-08.13, T-08.14). Nenhum item pré-existente removido em nenhum hunk — confirmado por leitura
  linha a linha do diff completo.
- `git diff --numstat docs/INDEX.md` → 1/0. Append-only respeitado.

## Achado — WARNING, não coberto por regra do harness
[WARNING] `docs/INDEX.md:192` cita `` `ls docs/adr/*.md \| wc -l` `` como o comando que produz
"26 ADRs" — mas rodar esse comando exato hoje devolve **27**, não 26, pelo mesmo self-match
(README.md dentro de docs/adr/) que o ciclo 1 de QA achou e mandou corrigir. A correção foi
aplicada em `docs/adr/README.md` e em `gates/T-09.1-builder.md` (ambos usam `ADR-*.md`
consistentemente), mas **não** na linha do `docs/INDEX.md`, escrita depois da correção e fora
do escopo que QA revisou (QA listou explicitamente só README.md/tasks.toml/INDEX.md-mudança-de-
linha-anterior/builder.md como escopo do ciclo 1, mas o `grep`/reprodução do ciclo 1 mirou
README.md e builder.md, não esta linha nova). Como `docs/INDEX.md` é append-only
(CLAUDE.md §"Registro de artefatos é append-only"), a linha não pode ser reescrita — a correção
correta é uma **nova linha apensada** que emende a citação (ex.: `ls docs/adr/ADR-*.md | wc -l`).
Citação: CLAUDE.md §"Nenhum número sem o comando que o produziu" (número tem de ser reproduzível
pelo comando citado) — sem id de regra do harness, porque `docs/INDEX.md` está fora de
`code_paths` e fora do escopo de qualquer regra bloqueante listada.

## Veredito
**COMPLIANT** — 0 de 8 regras bloqueantes violadas (sweep vazio, reproduzido). O achado acima é
WARNING de convenção documental (CLAUDE.md), não regra bloqueante, e não muda o veredito por
definição do protocolo (`BLOCKER` > `WARNING` > `INFO`; só `BLOCKER` reprova).

Correção concreta sugerida: apensar linha em docs/INDEX.md corrigindo a citação do comando de
D9.1/D9.2 para `ADR-*.md`, no mesmo padrão já usado em README.md e builder.md.
