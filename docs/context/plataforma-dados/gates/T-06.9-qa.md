# QA Gate — T-06.9 · as quatro séries de preço, `implied_avg_price` PROIBIDO, `fee_schedule` datada, `cvd_source` com erro medido

**Fase:** 06 · **Componente:** `sentimento` · **PR:** https://github.com/StharleyMax/cripto-strategy/pull/100
**Branch:** `tasks/T-06.9-fase06` · **Worktree:** `/tmp/claude-1002/wt/T-06.9` · **Base:** `master@a495e98`

## Escopo lido antes de tudo

- `docs/context/plataforma-dados/handoff/T-06.9.md` (inteiro)
- `docs/adr/ADR-007-price-source-por-uso.md` (inteiro)
- `docs/context/plataforma-dados/gates/T-06.9-builder.md` (inteiro, na worktree)
- Linha `T-06.9` de `docs/context/plataforma-dados/tasks.toml:805-812` e de `tasks_review.md:324`
- `backend/src/modules/sentimento/domain/{price_source_catalog,fee_schedule,cvd_source_catalog}.py` (inteiros)
- `backend/tests/sentimento/{test_price_source_catalog,test_fee_schedule,test_cvd_source_catalog}.py` (inteiros)
- `backend/src/modules/sentimento/domain/series_catalog.py` e `series_key.py` (mecanismos reutilizados)

## 1. Testes existentes rodados antes de qualquer teste novo

`bash backend/scripts/test.sh` (suíte inteira, worktree limpa, sem mutação) →
**1319 passed, 3 warnings, 443,28s** `[MEDIDO 2026-09-03, universo: 1319 testes coletados]`.
Bate exatamente com o número do builder (que mediu 776,65s em máquina sob contenção
diferente — mesma contagem de testes e cobertura).

Cobertura por camada (`bash backend/scripts/check-coverage-layers.sh`, acoplado ao `test.sh`):

| camada | medido | alvo | veredito |
|---|---|---|---|
| domain | 99,8% (2613/2618) | 90% | OK |
| use_cases | 100,0% (585/585) | 80% | OK |
| infra | 95,1% (2208/2321) | 70% | OK |

Cobertura isolada dos 3 módulos novos (do mesmo relatório `coverage.xml`):
`fee_schedule.py` 100% (44 stmts), `cvd_source_catalog.py` 100% (45 stmts),
`price_source_catalog.py` 94% (26 stmts, 1 não coberta — linha do `RuntimeError` de guarda do
invariante módulo-level `PRICE_SOURCE_BY_USE`/`PRICE_USES`, inalcançável sem corromper o módulo
em runtime; documentado no docstring como substituto de `assert`, que `ruff S101` recusa).

`bash backend/scripts/lint.sh` → `All checks passed!` / `239 files already formatted` /
`Success: no issues found in 239 source files`.

`bash backend/scripts/boundaries.sh` → `Contracts: 3 kept, 0 broken` (159 arquivos, 721
dependências) `[MEDIDO]`.

`bash backend/scripts/natureza.sh` → `73 arquivo(s), 0 leitura(s) de relogio` `[MEDIDO]`.

`harness rules --mode file --path <arquivo>` rodado individualmente nos 6 arquivos
(3 domain + 3 teste) → `rc=0`, saída vazia (0 achados) em todos.

## 2. Regras bloqueantes (`harness rules list --severity block`, 7 em vigor)

| id | veredito | evidência |
|---|---|---|
| `core.relative-import` | OK | `grep -nE '^from \.\|^import \.'` nos 6 arquivos → 0 ocorrências |
| `core.silent-except` | OK | `grep -nE 'except.*:\s*$\|except.*pass'` nos 3 módulos domain → 0 ocorrências (nenhum `try/except` nos módulos novos) |
| `core.print-statement` | OK | `grep -nE '^\s*print\('` → 0 ocorrências |
| `core.hardcoded-secret` | OK | `grep -inE 'api[_-]?key\|secret\|password\|token'` → 0 ocorrências |
| `web-fullstack.browser-imports-server` | N/A | componente `sentimento`, não `web` |
| `web-fullstack.tenant-from-request` | N/A | idem |
| `web-fullstack.server-test-directory-present` | N/A | idem |

Regras bloqueantes avaliadas: 4 de 7 listadas (as 3 restantes são do pack `web-fullstack`,
fora do universo desta task de `sentimento`).

## 3. Mutação real por item do DoD — planta, confirma vermelho, reverte, confirma `git diff` limpo

Procedimento para cada item: `Edit` no arquivo de produção → roda o teste específico (falha
vermelha, colada abaixo) → `git checkout -- <arquivo>` → `git status --short` vazio.

### 3.1 — `implied_avg_price` PROIBIDO como nome

- **Grep estrutural em produção:** `grep -rn "implied_avg_price" backend/src` → 4 ocorrências,
  todas em `series_key.py` (declaração do denylist, `FORBIDDEN_METRIC_NAMES`) e em
  comentários/docstring de `price_source_catalog.py` explicando a proibição — **nenhuma**
  instancia um `SeriesKey`/`metric=` real com esse nome.
- **Mutação:** `FORBIDDEN_METRIC_NAMES: Final[frozenset[str]] = frozenset({"implied_avg_price"})`
  → `frozenset()` em `series_key.py`.
- **Vermelho:** `test_price_source_catalog.py::test_implied_avg_price_is_still_forbidden_here`
  → `Failed: DID NOT RAISE IncompleteSeriesKeyError`.
- **Revertido:** `git checkout -- src/modules/sentimento/domain/series_key.py`; `git status --short` vazio.

### 3.2 — `price_use` usa `PRICE_USES` de `series_catalog.py` (reuso, não segunda validação)

- Confirmado por leitura: `price_source_catalog.py` importa `PRICE_USES` e
  `InvalidPriceUseError` de `series_catalog.py` — não redeclara nenhum dos dois.
- **Mutação:** em `resolve_price_source`, troquei o `if price_use not in PRICE_USES: raise
  InvalidPriceUseError(...); return PRICE_SOURCE_BY_USE[price_use]` por
  `return PRICE_SOURCE_BY_USE.get(price_use, "klines_last")` — simulando um default silencioso
  para valor fora do conjunto.
- **Vermelho:** `test_price_source_catalog.py::test_resolve_price_source_outside_the_closed_set_is_refused`
  → `Failed: DID NOT RAISE InvalidPriceUseError` (o `"unknown_use"` teria caído
  silenciosamente em `klines_last`, exatamente a classe de defeito que `ADR-007`/`PS-1` proíbe).
- **Revertido:** `git checkout -- src/modules/sentimento/domain/price_source_catalog.py`; `git status --short` vazio.

### 3.3 — `fee_schedule` datada, D6.13: as-of sem entrada vigente RECUSA, nunca default

- **Mutação:** em `FeeScheduleCatalog.resolve`, quando `candidates` vazio, inseri um fallback
  que devolve a entrada mais antiga do mesmo `(venue, market, tier)` em vez de levantar
  `NoFeeScheduleAsOfError`.
- **Vermelho:** `test_fee_schedule.py::test_resolve_refuses_when_no_entry_covers_the_date` →
  `Failed: DID NOT RAISE NoFeeScheduleAsOfError`.
- **Revertido:** `git checkout -- src/modules/sentimento/domain/fee_schedule.py`; `git status --short` vazio.

### 3.4 — `cvd_source` com `PublishedError(median_bp, p99_bp, n)`, D6.9: reuso do `__post_init__` de `series_catalog.py`

- **Mutação:** em `series_catalog.py::SeriesCatalogEntry.__post_init__`, neutralizei o guard
  `if self.reconstructed_from is not None and self.published_error is None: raise
  InvalidCatalogEntryError(...)` prefixando com `if False and ...`.
- **Vermelho — os DOIS arquivos caem juntos, provando mecanismo único, não duplicado:**
  - `test_series_catalog.py::test_reconstruction_without_published_error_is_refused` (suíte de
    `T-06.1`) → `Failed: DID NOT RAISE InvalidCatalogEntryError`.
  - `test_cvd_source_catalog.py::test_registering_a_cvd_source_reconstruction_without_published_error_is_refused`
    (suíte nova desta task) → `Failed: DID NOT RAISE InvalidCatalogEntryError`.
- **Revertido:** `git checkout -- src/modules/sentimento/domain/series_catalog.py`; `git status --short` vazio.

### 3.5 — Contrato `Natureza` (`ADR-016`): domain sem I/O

- `grep -nE "^import|^from" src/modules/sentimento/domain/{price_source_catalog,fee_schedule,cvd_source_catalog}.py`
  → só stdlib (`types`, `typing`, `dataclasses`, `datetime`, `decimal`, `enum`) + módulos
  irmãos de `domain`. Nenhum `socket`, `ssl`, `open`, cliente HTTP.
- `grep -nE "\.today\(\)|\.now\(\)"` nos 3 módulos → 0 ocorrências reais (só menções em
  comentário explicando a ausência).
- `bash backend/scripts/natureza.sh` → `73 arquivo(s), 0 leitura(s) de relogio` `[MEDIDO]`.
- `bash backend/scripts/boundaries.sh` → `3 kept, 0 broken`, incluindo o contrato "domain e
  use_cases nao falam com socket nem ssl" `[MEDIDO]`.

### 3.6 — `coinalyze_bv` com os valores do handoff e a nota de causa não diagnosticada

- `COINALYZE_BV_MEASUREMENT` em `cvd_source_catalog.py:167-178`:
  `published_error=PublishedError(median_bp=Decimal("0.0000"), p99_bp=Decimal("29.34"), n=699)`,
  `max_bp=Decimal("1955.80")`, `measured_on=date(2026, 8, 24)`,
  `tail_cause=TailCause.NOT_DIAGNOSED`, `refuted_hypotheses=(RefutedTailHypothesis(
  description="maker-side trades explain the tail of coinalyze_bv vs aggtrade_q",
  refuted_at_bp=Decimal("2584.87")),)`. Os 5 valores (median, p99, máx, n, hipótese refutada a
  2584,87 bp) batem literalmente com o handoff.
- `TailCause` é um `Enum` fechado com **um único membro, `NOT_DIAGNOSED`** — a hipótese
  "maker" não tem membro correspondente, então não há como o módulo afirmar essa causa por
  engano (é estruturalmente impossível, não só convenção).
- Teste `test_coinalyze_bv_measurement_records_the_refuted_maker_hypothesis_without_asserting_it`
  pina isso: `tail_cause is TailCause.NOT_DIAGNOSED` **e** a hipótese refutada registrada à parte.
  Não rodei mutação adicional aqui — a prova estrutural (enum de 1 membro) e o teste que lê os
  valores exatos já cobrem o ponto sem precisar simular a regressão.

## 4. Pontos fora de escopo, nomeados pelo builder e conferidos como corretos

- `index_price`/`premium_index` (membros de `PRICE_SOURCES`) não recebem linha de catálogo:
  `ADR-007`'s tabela de decisão não atribui `price_use` a nenhum dos dois hoje — confirmado por
  leitura de `ADR-007` linha 31-37 (tabela só cobre os 5 usos ↔ `klines_last`/`mark_price`).
  A DoD (`tasks_review.md:324`) pede "`price_mark_close` declarado" e "`implied_avg_price`
  proibido", não as 4 linhas de catálogo — não é lacuna do DoD.
- `aggtrade_q`/`aggtrade_nq` sem `published_error` fabricado, `QF-5` `[NÃO MEDIDO]` — correto:
  `ADR-001` só mede o par pontual DOGEUSDT (déficit 80,56 bp / gap 6,01%), não uma tripla
  `(mediana, p99, n)` agregada; sintetizar uma violaria a disciplina de número sem comando.
- `fee_schedule` com zero entradas reais — `PRD-001` confirma "hoje não existe nenhum"; o
  mecanismo (schema + `resolve` as-of + recusa) está completo e testado, curadoria é dívida
  nomeada para task futura.

## 5. DoD, item a item

- **D6.9** (`cvd_source` sem `(mediana, p99, n)` reprova) — OK, provado por mutação em 3.4.
- **D6.13** (`fee_schedule` as-of sem cobertura recusa) — OK, provado por mutação em 3.3.
- **`price_mark_close` declarada, `implied_avg_price` proibido** — OK, provado por mutação em 3.1.
- **`price_source` por `price_use`, reuso do conjunto fechado** — OK, provado por mutação em 3.2.
- **`cvd_source` erro medido por fonte (`aggtrade_q`, `aggtrade_nq`, `coinalyze_bv`)** — OK,
  as 3 fontes registradas, valores de `coinalyze_bv` conferidos em 3.6.
- **Contrato `Natureza`, domain sem I/O** — OK, confirmado em 3.5.

## Veredito

```
## QA Gate — Fase 06 [sentimento]
- [OK] core.relative-import — grep nos 6 arquivos novos, 0 ocorrências
- [OK] core.silent-except — grep nos 3 módulos domain, 0 ocorrências
- [OK] core.print-statement — grep nos 6 arquivos, 0 ocorrências
- [OK] core.hardcoded-secret — grep nos 6 arquivos, 0 ocorrências
- [OK] Testes existem e passam — bash backend/scripts/test.sh (1319 testes, 443,28s)
- [OK] Cobertura domain 99,8%/use_cases 100,0%/infra 95,1% contra alvo 90/80/70
- [OK] DoD D6.9 — mutação em series_catalog.py's __post_init__ prova reuso (2 suítes caem juntas)
- [OK] DoD D6.13 — mutação em fee_schedule.resolve prova recusa, não default
- [OK] implied_avg_price PROIBIDO — mutação em FORBIDDEN_METRIC_NAMES prova enforcement
- [OK] price_use reusa PRICE_USES — mutação em resolve_price_source prova, não 2ª validação
- [OK] coinalyze_bv com os 5 valores do handoff e causa NÃO DIAGNOSTICADA (não hipótese refutada)
- [OK] Natureza (ADR-016): domain sem I/O — boundaries.sh 3 kept/0 broken, natureza.sh 0 leituras
Regras bloqueantes avaliadas: 4 de 7 listadas (3 web-fullstack fora do universo de sentimento)
Veredito: APPROVED
```

Relatório completo: `docs/context/plataforma-dados/gates/T-06.9-qa.md` (este arquivo).
