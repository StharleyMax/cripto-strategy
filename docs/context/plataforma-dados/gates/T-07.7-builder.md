# QA Gate Context Block — T-07.7

```
[QA GATE — Fase 07: Broker de cota com jitter e circuit breaker, contagem local]
Feature: plataforma-dados     Componente: sentimento
Arquivos alterados:
- backend/src/modules/sentimento/domain/circuit_breaker.py (new)
- backend/src/modules/sentimento/domain/jitter.py (new)
- backend/src/modules/sentimento/domain/quota_circuit_broker.py (new)
- backend/tests/sentimento/test_circuit_breaker.py (new)
- backend/tests/sentimento/test_jitter.py (new)
- backend/tests/sentimento/test_quota_circuit_broker.py (new)
- backend/README.md (modified — doc delta, secao T-07.7)
Spec: docs/specs/PRD-001-plataforma-dados.md (CA-F3-9)
Plan: docs/plans/SPEC-001-plataforma-dados/07_aquisicao_em_regime.md (item 7.9)
DoD:
- [x] Broker com jitter e circuit breaker, contagem LOCAL, compondo domain/local_quota_broker.py
      sem duplicar logica (verificado: `git status --short` mostra 0 arquivos modificados sob
      local_quota_broker.py/quota_bucket.py)
- [x] Ciclo completo CLOSED->OPEN->HALF_OPEN->CLOSED testado
      (test_circuit_breaker.py::test_the_full_cycle_closed_to_open_to_half_open_to_closed_again)
- [x] 429/5xx abrem o circuito na 1a ocorrencia, falha generica respeita o limiar
      (test_circuit_breaker.py::test_a_rate_limit_or_server_error_trips_on_the_first_occurrence,
      test_a_transport_error_alone_does_not_trip_before_the_threshold)
- [x] Jitter varia de fato entre chamadas, nao e mock fixo
      (test_jitter.py::test_the_real_random_source_is_not_hardcoded_to_the_same_value)

Comandos rodados (literais) e resultado:
- `bash backend/scripts/lint.sh` -> "All checks passed! / 186 files already formatted /
  Success: no issues found in 186 source files" (ruff check + format --check + mypy --strict;
  universo: 186 arquivos Python de backend/)
- `bash backend/scripts/test.sh` -> "1064 passed, 3 warnings in 417.65s" (rc=0; 3 warnings sao
  ResourceWarning pre-existente ja nomeado em backend/README.md, nao desta task). Cobertura por
  camada: domain 99,8% (meta 90%, 2111/2115 linhas) · use_cases 100,0% (meta 80%, 537/537) ·
  infra 95,1% (meta 70%, 1786/1878). Arquivos desta task: jitter.py e quota_circuit_broker.py em
  100%; circuit_breaker.py em 97% (2 linhas: guarda defensiva inalcancavel do tipo).
- `bash backend/scripts/boundaries.sh` -> "Contracts: 3 kept, 0 broken" (import-linter; universo:
  129 arquivos, 583 dependencias)
- `harness rules --mode sweep --changed-only` -> rc=0, sem saida (nenhum achado bloqueante sobre
  os 6 arquivos desta task)
- Mutacao 1 (revertida): `sample_uniform()` -> `return 0.42` fixo; `pytest -q
  tests/sentimento/test_jitter.py -k real_random_source_is_not_hardcoded` -> 1 failed
  (`assert 1 > 1`, exatamente a falha de variancia que o DoD pede para detectar)
- Mutacao 2 (revertida): `record_failure` -> removido `kind.trips_immediately or` (so limiar
  generico); `pytest -q tests/sentimento/test_circuit_breaker.py -k first_occurrence` -> 2 failed
  (RATE_LIMITED e SERVER_ERROR deixam de abrir o circuito na 1a falha)
- Ambas as mutacoes revertidas manualmente (sed inverso); `git status --short` confirmado sem
  residuo (arquivos novos, sem diff contra o conteudo original) apos cada reversao

Cobertura: 97,84% total (alvo global: 70%; alvo por camada 90/80/70 — todos atingidos, ver acima)

Doc delta:
- backend/README.md: atualizado — nova secao "2026-09-02 por T-07.7 — jitter e circuit breaker
  sobre o broker cego, por composicao" (tabela de arquivos, motivo de random ser parametro,
  falsificador com os 2 mutantes, comandos rodados, escopo nao fechado)
- ADR: nao necessario — motivo: extensao por composicao de vocabulario ja fixado em
  domain/quota_bucket.py/local_quota_broker.py (T-02.2/T-03.7), nenhuma decisao arquitetural nova
  fora do que ADR-016/Natureza ja rege (domain/use_cases sem socket/ssl; time/datetime guardado
  por natureza.py) — random nao e coberto por nenhuma das duas ADRs e nao criou excecao nova

Bloqueado (se houver): nenhum. PR aberta: https://github.com/StharleyMax/cripto-strategy/pull/82
(branch tasks/T-07.7-fase07, contra master, base do merge local: master@34141bb apos fetch+merge
de origin/master; frontend/package-lock.json revertido apos `npm --prefix frontend install` local
necessario apenas para o gate lint-frontend do pre-push, fora do escopo desta task)
```
