# QA Gate — T-05.6 (plataforma-dados, fase 05, charts)

## QA Gate — Fase 05 [charts] T-05.6 (pointer_mode)

**Worktree usado:** `/home/stharley/Documentos/projects/cripto-strategy/.claude/worktrees/wf_2a5f5015-5f1-8`, `git checkout e743cd6` (a branch `tasks/T-05.6-pointer-mode` já estava aberta em outro worktree, `wf_2a5f5015-5f1-4`; checkout do SHA correspondente evitou conflito). `git status` limpo ao final; nenhuma alteração persistida (mutações plantadas e revertidas — ver §3).

Diff real da task, isolado do resto de `master` (`git diff 6ecb9ae..e743cd6 --stat`, base = merge-base correto — o primeiro diff que rodei usou um commit errado, `6d3f284`, e trazia `T-06.3` junto; corrigido):
```
frontend/README.md                          |  79 +++++++++
frontend/src/charts/s2-pointer-mode.test.ts |  97 ++++++++
frontend/src/charts/s2-pointer-mode.ts      | 151 ++++++++++
3 files changed, 327 insertions(+)
```
Escopo correto: só `charts` + doc delta, `backend/` intocado, nenhum arquivo fora de `frontend/src/charts/`.

### 1. Regras bloqueantes (`harness rules list --severity block` → 7 regras)
- [OK] `core.relative-import` — `grep -n "^export" frontend/src/charts/s2-pointer-mode.ts`: módulo não tem NENHUM `import` (self-contido); n/a por vacuidade, não por omissão de checagem.
- [OK] `core.silent-except` — nenhum `try/catch` no módulo; nenhuma exceção é engolida (as duas funções de assert só `throw`).
- [OK] `core.print-statement` — `grep -n "console\.\|print(" frontend/src/charts/s2-pointer-mode.ts` → nenhuma ocorrência.
- [OK] `core.hardcoded-secret` — nenhuma string de credencial no arquivo.
- [OK] `web-fullstack.browser-imports-server` — `ADR-003`/FR-1 ("charts não faz I/O") citado no cabeçalho do módulo E verificado: `grep -n "fetch\|readFileSync\|node:fs\|addEventListener\|document\.\|window\." frontend/src/charts/s2-pointer-mode.ts` → só a MENÇÃO em comentário JSDoc (linha 24), zero I/O real em código executável.
- [OK] `web-fullstack.tenant-from-request` — n/a, módulo não lê request.
- [OK] `web-fullstack.server-test-directory-present` — n/a, não é `backend/`.
- Comando agregado: `harness rules --mode file --path frontend/src/charts/s2-pointer-mode.ts` → `rc=0`; `harness rules --mode file --path frontend/src/charts/s2-pointer-mode.test.ts` → `rc=0`; `harness rules --mode sweep --changed-only` → `rc=0`, saída vazia.
Regras bloqueantes avaliadas: 7 de 7.

### 2. Testes existem e passam (reproduzidos, não relatados pelo builder)
- `npm --prefix frontend install` → `added 128 packages … found 0 vulnerabilities`.
- `npm --prefix frontend run lint` → `eslint src`, saída vazia (0 erro, 0 aviso).
- `npm --prefix frontend run test:charts` (universo total de `charts`) → `ℹ tests 75 / pass 64 / fail 11`. **Idêntico ao número que o builder corrigiu em `e743cd6`** (o commit anterior do doc dizia 23 falhas — errado; a correção para 11 é a que reproduzi).
- Isolado: `node --test src/charts/s2-pointer-mode.test.ts` → `ℹ tests 14 / pass 14 / fail 0`.
- As 11 falhas do universo total são **pré-existentes e não relacionadas**: `git log --oneline -- frontend/src/charts/s2-cvd.test.ts frontend/src/charts/s2-panels.test.ts frontend/src/charts/s2-oi-loader.test.ts frontend/src/charts/s2-klines-loader.test.ts frontend/src/charts/canonical-grid-sha256-proof.test.ts frontend/src/charts/s2-axis-integration.test.ts` → último commit em todos é `0670c52`/`a155db5` (`T-05.2`/`T-05.1`), nenhum toca T-05.6 (`e36d688`/`e743cd6`). Causa confirmada por inspeção da saída: `ENOENT` em `data/binance/**` (dado bruto não versionado, `.gitignore`) + 3 asserções de dias cobertos que dependem do mesmo fixture ausente.

### 3. Mutação — prova de que a suíte morde (2 mutações independentes plantadas e revertidas)
**Mutação 1** — inverteu a sentença central de `SPEC-001:284` ("clique/Espaço só travam crosshair em `read`"): fiz `resolvePointerAction` retornar `lock_crosshair` também em `annotate`.
`node --test src/charts/s2-pointer-mode.test.ts` → `pass 13 / fail 1`, nomeando exatamente `MORDE: in annotate mode, click/Space NEVER lock the crosshair` com `actual: {kind:'lock_crosshair'} === expected: {kind:'lock_crosshair'}` (a asserção era `notDeepStrictEqual`). **Morreu.**

**Mutação 2** — independente da primeira, no outro metade do DoD (overlay entre plot e crosshair): reordenei `LAYER_ORDER` para `["plot", "crosshair", "overlay"]` (overlay acima do crosshair, violando `SPEC-001:284`).
`node --test src/charts/s2-pointer-mode.test.ts` → `pass 12 / fail 2`, nomeando `the overlay layer sits strictly between plot and crosshair` e `assertOverlayIsSandwiched CALA: the real LAYER_ORDER passes`, com `RangeError` explícito citando os índices errados. **Morreu.**

Ambas revertidas com `Edit`; `git diff --stat` final → vazio, `git status` → `nothing to commit, working tree clean`. Reconfirmado após reversão: `node --test src/charts/s2-pointer-mode.test.ts` → `14/14 pass`.

### 4. Cobertura
`grep -n "^export " frontend/src/charts/s2-pointer-mode.ts` → 13 exports; os 9 runtime (2 constantes de enum, 2 classes de erro, 3 funções de assert/resolve, `LAYER_ORDER`) têm ao menos 1 caso positivo e ≥1 `MORDE` cada nos 14 testes; os 4 restantes são `type` (compile-time, não testável em runtime). Sem piso numérico declarado para `charts` (`harness policy --key test_cmd` só cobre `sentimento`) — medição qualitativa, mesma metodologia do doc delta do builder, confirmada por inspeção direta em vez de aceita por relato.

### 5. DoD da task, item a item (plano `05_fatia_visivel.md:19` + `SPEC-001:284`)
- [OK] `pointer_mode ∈ {read, annotate}` declarado — tipo fechado + `POINTER_MODES` runtime-checável + `assertPointerMode` recusa terceiro valor (`InvalidPointerModeError`, testado).
- [OK] overlay reservado acima do plot e abaixo do crosshair — `LAYER_ORDER` + `assertOverlayIsSandwiched`, checado por POSIÇÃO (não por confiar no array literal), com 3 casos MORDE (abaixo do plot, acima do crosshair, landmark faltando).
- [OK] `clique`/`Espaço` só significam "travar crosshair" em `read` — `resolvePointerAction`, testado nos dois modos e provado por mutação (§3, mutação 1) que o teste reprova se a regra colapsar.
- [OK] `ADR-003`/FR-1 ("`charts` não faz I/O") — zero I/O real no módulo, verificado por grep.
- [OK] Escopo correto (`charts` isolado; `T-08.7`/`swing_point` e `Q11` explicitamente fora, conforme o comentário do módulo e `docs/context/plataforma-dados/tasks.toml:666`).
- [Observação, não bloqueante] Nenhum `docs/context/plataforma-dados/gates/T-05.6-{builder,qa,review}.md` foi criado — divergindo do padrão usado em `T-05.1`/`T-05.2` (que têm os três). O builder colocou o delta inteiro em `frontend/README.md §13` em vez de um gate dedicado. Não é regra bloqueante nem item de DoD do plano — é um gap de convenção documental, citado para o `/review` avaliar se cabe correção.
- [Observação, não bloqueante] `docs/INDEX.md` não recebeu linha para `T-05.6` — mas o mesmo vale para `T-05.3`/`T-05.4`/`T-05.5` na mesma fase (`grep -n "T-05.3\|T-05.4\|T-05.5" docs/INDEX.md` → 0 ocorrência), então não é uma regressão desta task especificamente.
- [anomalia] Nenhuma — todas as checagens produziram veredito.

Regras bloqueantes avaliadas: 7 de 7.

**Veredito: APPROVED**

Os dois pontos observacionais (ausência de gate files dedicados e de linha em `INDEX.md`) não bloqueiam — não são regra `severidade block`, não são item da tabela de DoD do plano, e o segundo já é o padrão vigente entre os irmãos da mesma fase. Comportamento central da task (enum fechado, sanduíche de camadas por posição, semântica click/Space restrita a `read`) está implementado, testado e sobrevive a duas mutações independentes plantadas por mim.

