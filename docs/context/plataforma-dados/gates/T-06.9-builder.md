# Builder report — T-06.9

**Card:** `CST-53` · **Componente:** `sentimento` · **Base:** `master@a495e98` · **Branch:** `tasks/T-06.9-fase06`
**PR:** https://github.com/StharleyMax/cripto-strategy/pull/100

## Escopo

Plano `06` itens **6.6 + 6.7 + 6.8 + 6.9** (`ADR-007`/PS-1+PS-2, `CA-F2-14`, `CA-F2-16`,
`ADR-001`/5). DoD verificados: **D6.9**, **D6.13** (os demais D-números da fase 06 pertencem a
outras tasks paralelas, per o handoff).

Não avança fase, não cria/edita `tasks.toml`, não roda `gate-record`/`approve`/`advance`.

## Arquivos alterados

- `backend/src/modules/sentimento/domain/price_source_catalog.py` (new) — itens 6.6+6.7
- `backend/src/modules/sentimento/domain/fee_schedule.py` (new) — item 6.8
- `backend/src/modules/sentimento/domain/cvd_source_catalog.py` (new) — item 6.9
- `backend/tests/sentimento/test_price_source_catalog.py` (new, 13 testes)
- `backend/tests/sentimento/test_fee_schedule.py` (new, 13 testes)
- `backend/tests/sentimento/test_cvd_source_catalog.py` (new, 16 testes)
- `docs/INDEX.md` (modified, append-only — linha `2026-09-03T08:15Z`)
- `backend/README.md` (modified — seção `## 📎 2026-09-03 por T-06.9`)

## Design decisions (documentadas nos docstrings dos módulos)

1. **`price_use` continua escalar em `SeriesCatalogEntry` (T-06.1, intocado).** `mark_price`
   serve 3 usos (`ADR-007`); uma linha por uso duplicaria a `SeriesKey`. `PRICE_SOURCE_BY_USE`
   (tabela transcrita de `ADR-007`) é a fonte da atribuição; as linhas do catálogo só provam
   que `klines_last`/`price_mark_close` existem.
2. **`aggtrade_q`/`aggtrade_nq` não carregam `published_error` fabricado.** `QF-5` pede
   `(mediana, p99, máx, n, data)` para as duas; só existe o ponto DOGEUSDT de `ADR-001`
   (déficit 80,56 bp, gap 6,01%) — não uma tripla agregada. Sintetizar uma violaria "nenhum
   número sem o comando que o produziu". Marcado `[NÃO MEDIDO]` no docstring, dono nomeado.
3. **`fee_schedule` fica com zero linhas reais** — `PRD-001` confirma "hoje não existe
   nenhum"; mecanismo (schema + `resolve` as-of + recusa) completo e testado.

## Comandos rodados (literais) e resultado

- `bash backend/scripts/test.sh` → **1319 passed**, 3 warnings, 776,65s (universo: 1319
  testes coletados). Cobertura total **97,63%**; por camada — domain **99,8%**
  (2613/2618, meta 90%), use_cases **100,0%** (585/585, meta 80%), infra **95,1%**
  (2208/2321, meta 70%) — 3/3 `[OK]`.
- `bash backend/scripts/lint.sh` → `ruff check` + `ruff format --check` + `mypy --strict`
  sem achado (universo: 242 arquivos).
- `bash backend/scripts/boundaries.sh` → **3 kept, 0 broken** (universo: 159 arquivos, 721
  dependências).
- `bash backend/scripts/natureza.sh` → **73 arquivo(s), 0 leitura(s) de relógio**.
- `harness rules --mode sweep --changed-only` (após `git add -A`) → **0 achados** (3
  `[AVISO]` `core.module-docstring-single-line` achados durante o desenvolvimento nos 3
  módulos novos, corrigidos no mesmo ciclo antes do commit — conteúdo movido para comentário
  `#`, forma de `series_catalog.py`).

## Cobertura por módulo novo

- `fee_schedule.py`: 100% (44 stmts).
- `cvd_source_catalog.py`: 100% (45 stmts).
- `price_source_catalog.py`: 94% (26 stmts, 1 não coberta) — a linha é o `RuntimeError` de
  guarda do invariante `PRICE_SOURCE_BY_USE`/`PRICE_USES`, inalcançável sem corromper o
  módulo em runtime; substitui um `assert` de nível de módulo que `ruff` (`S101`) recusa em
  código de produção.

## Falsificador mostrado (D6.9)

`test_cvd_source_catalog.py::test_registering_a_cvd_source_reconstruction_without_published_error_is_refused`
constrói a MESMA `SeriesKey` que `build_coinalyze_bv_entry` usa, omite `published_error`, e
prova `InvalidCatalogEntryError` — reprodução do mecanismo de `T-06.1`, não reimplementação.

`test_fee_schedule.py::test_resolve_refuses_when_no_entry_covers_the_date` e
`test_resolve_refuses_on_an_empty_catalog` provam D6.13: sem cobertura, `resolve` RECUSA
(`NoFeeScheduleAsOfError`), nunca cai para um default.

## Doc delta

- `docs/INDEX.md`: linha nova `2026-09-03T08:15Z` (append-only, nenhuma linha reescrita).
- `backend/README.md`: seção `## 📎 2026-09-03 por T-06.9` acrescentada ao final.
- ADR: não necessário — nenhuma decisão nova de arquitetura; `ADR-007`/`ADR-001` já
  decidiram o que este código implementa.

## Git

- `git fetch origin master` (duas vezes: antes do commit e antes do push) → `master` parado
  em `a495e98` nas duas checagens, mesma base da worktree — sem conflito, sem rebase
  necessário.
- Commit único `d3e72a3`, autor/committer `Stharley Maxwell <stharleymax@gmail.com>`, sem
  trailer `Co-Authored-By`.
- Push via `HARNESS_MECHANISM=.../bin/harness`, URL explícita, sem `--force`/`--force-with-lease`.
  Pré-push exigiu `frontend/node_modules` (ausente na worktree nova) — resolvido com
  `npm ci` (cache local, sem rede real) antes do push bem-sucedido.
- PR aberta: https://github.com/StharleyMax/cripto-strategy/pull/100

## Bloqueado

Nenhum item do escopo (6.6+6.7+6.8+6.9, D6.9, D6.13) ficou bloqueado.

Fora de escopo, nomeado (não é bloqueio desta task): `index_price`/`premium_index` sem linha
de catálogo (ADR-007 não atribui `price_use` a nenhum hoje); `QF-5` de `aggtrade_q`/
`aggtrade_nq` `[NÃO MEDIDO]`; `fee_schedule` sem entrada real curada; `PS-3` (`<Anotacao>`) é
`charts`/`backtest`/`web`, fora da fronteira de `sentimento`.
