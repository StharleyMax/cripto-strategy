[QA GATE — Fase 06: As QUATRO séries de L/S com colunas próprias; `ls_ratio` genérico PROIBIDO]
Feature: `plataforma-dados`     Componente: `sentimento`
Arquivos alterados:
- `backend/src/modules/sentimento/domain/series_key.py` (modified)
- `backend/src/modules/sentimento/domain/long_short_ratio_series.py` (new)
- `backend/tests/sentimento/test_series_identity.py` (modified)
- `backend/tests/sentimento/test_long_short_ratio_series.py` (new)
- `backend/README.md` (modified — nova seção "📎 2026-09-03 por `T-06.3`")
- `docs/INDEX.md` (modified — linha `/build` acrescentada, append-only)
Spec: docs/specs/SPEC-001-plataforma-dados.md (§3.1, §5.11)
Plan: docs/plans/SPEC-001-plataforma-dados/06_semantica_declarada.md (itens 6.3 + 6.10)

DoD:
- [x] `ls_ratio` genérico PROIBIDO como nome de série — `SeriesKey.FORBIDDEN_METRIC_NAMES` ganha `"ls_ratio"`, recusado por `IncompleteSeriesKeyError` — `bash backend/scripts/test.sh -k test_ls_ratio_is_refused_as_a_generic_metric_name`
- [x] As quatro séries nomeadas (`count_long_short_ratio`, `count_toptrader_long_short_ratio`, `sum_toptrader_long_short_ratio`, `sum_taker_long_short_vol_ratio`) constroem sem serem confundidas com o nome banido — `test_the_four_named_l_s_metrics_are_not_forbidden` (parametrizado) + `test_the_four_metrics_are_named_and_ls_ratio_is_not_one_of_them`
- [x] **D6.4**: autocorrelação lag-1 medida sobre dado real (BTC/COTI/DOGE/SLX, 2026-08-17..23) — três séries de posicionamento ≥0,99; taker `|r|<0,10` nos 4 símbolos; ortogonalidade `|r|<0,10` em 12/12 pares — `test_d6_4_the_three_positioning_series_have_high_lag1_autocorrelation`, `test_d6_4_the_taker_series_has_near_zero_lag1_autocorrelation`, `test_d6_4_taker_orthogonality_is_below_0_10_in_all_12_pairs`
- [x] **D6.5**: `delta()` rejeita o taker POR TIPO, não por `if` em runtime — `PositionRatioMetric` (`Literal` de 3 membros) exclui o taker estruturalmente; `TakerRatioComponents` não tem campo `.value`; `mypy --strict` reprova a chamada errada (medido à mão, comando no cabeçalho do módulo) — `test_position_ratio_metric_excludes_the_taker_metric_by_member_set`, `test_delta_signature_is_keyed_by_position_ratio_observation_only`, `test_structural_falsifier_delta_body_cannot_spell_the_taker_metric` (escaneamento `ast`, mesma técnica de `test_universe_at.py`), `test_taker_ratio_components_has_no_value_field_delta_could_reach_for`
- [x] **D6.6**: pedir TF mais largo sobre a série taker nua RECUSA, nunca devolve `3,1809` — `resample_bare_taker_ratio_refuses` levanta `NonAggregableFlowRatioError` ANTES de qualquer aritmética, mesmo sobre valores reais — `test_d6_6_naive_resample_of_the_real_taker_series_refuses` (3 buckets reais de `data/binance/metrics/btcusdt/2026-08-23.csv`), `test_resample_bare_taker_ratio_refuses_never_touches_its_argument`
- [x] Item 6.10: `buyVol`/`sellVol` do REST `takerlongshortRatio` não são descartados — `TakerRatioComponents` os carrega e `resample_taker_ratio_to_timeframe` recomputa `Σbuy/Σsell` (a única forma legítima, `SPEC-001` §5.11) — `test_taker_ratio_components_carries_both_legs_and_derives_the_ratio`, `test_resample_taker_ratio_recomputes_from_sigma_buy_over_sigma_sell`, mais recusa de perna negativa/denominador zero (`test_taker_ratio_components_refuses_a_negative_leg`, `test_taker_ratio_components_refuses_a_zero_denominator`)

Comandos rodados (literais) e resultado:
- `bash backend/scripts/test.sh` (suite completa) → **1307 passed**, 0 failed, 0 error, 3 warnings, 382s (universo: toda `backend/tests/`, incluindo os 25 testes novos de `test_long_short_ratio_series.py` + 2 novos em `test_series_identity.py`; era 1277 antes desta task)
- cobertura total **97,63%**; por camada (`check-coverage-layers.sh`, `ADR-009/D1`): domain **99,8%** (meta 90%, 2547/2551 linhas), use_cases **100,0%** (meta 80%, 585/585), infra **95,1%** (meta 70%, 2208/2321) — 3/3 camadas `[OK]`
- `long_short_ratio_series.py` isolado: **47 stmts, 0 miss, 100%** (medido no relatório `term-missing` de `bash backend/scripts/test.sh -k test_long_short_ratio_series`, 25 passed); `series_key.py` **80 stmts, 0 miss, 100%**
- `bash backend/scripts/lint.sh` → `ruff check` + `ruff format --check` + `mypy --strict`: **sem achado**, 235 arquivos
- `harness rules --mode sweep --changed-only` → `rc=0`, **0 achados** (1 `[AVISO] core.module-docstring-single-line` encontrado durante o desenvolvimento — docstring de módulo multi-linha em `long_short_ratio_series.py` — e corrigido antes deste commit: docstring abre/fecha na 1ª linha, resto em comentário `#`, mesmo estilo de `series_catalog.py`/`quarantine_terms.py`)
- `bash backend/scripts/boundaries.sh` → **3 kept, 0 broken** (156 arquivos, 708 dependências)
- `bash backend/scripts/natureza.sh` → **71 arquivo(s) (52 domain + 19 use_cases), 0 leitura(s) de relógio**
- `mypy --strict` sobre um scratch file chamando `delta(TakerRatioComponents(...), PositionRatioObservation(...))` → `error: Argument 1 to "delta" has incompatible type "TakerRatioComponents"; expected "PositionRatioObservation"  [arg-type]`, `Found 1 error in 1 file` `[MEDIDO 2026-09-03, à mão — não faz parte da suite, documentado no cabeçalho de teste]`

Falsificador mostrado, não só afirmado:
- **D6.5 morde**: o `mypy --strict` acima é a mutação-tipo que a proteção rejeita — trocar o primeiro argumento de `delta()` de `PositionRatioObservation` para `TakerRatioComponents` reprova antes de rodar.
- **D6.6 morde**: `test_resample_bare_taker_ratio_refuses_never_touches_its_argument` passa `[Decimal("0"), Decimal("0"), Decimal("0")]` — se a função tentasse computar qualquer coisa (soma, média, razão) antes de recusar, uma divisão por zero apareceria em vez de `NonAggregableFlowRatioError`; a recusa fica provada como o PRIMEIRO efeito, não um efeito colateral de um cálculo que falhou.
- **D6.4 morde**: os 12 pares de ortogonalidade são contados (`assert checked == 12`) — se a lista de símbolos ou de métricas de posicionamento encolher silenciosamente, o teste reprova por contagem, não só por limiar.

Cobertura: 97,63% total / domain 99,8% / use_cases 100,0% / infra 95,1% (alvo: domain 90%, use_cases 80%, infra 70% — `ADR-009/D1`, confirmado por `check-coverage-layers.sh`)

Doc delta:
- `backend/README.md`: atualizado — nova seção "📎 2026-09-03 por `T-06.3` — as QUATRO séries de L/S; `ls_ratio` PROIBIDO; `delta()`/TF recusam o taker POR TIPO", mesmo padrão das seções de `T-06.1`/`T-07.6`/etc (peça, falsificador, comandos literais, escopo não fechado nomeado)
- `docs/INDEX.md`: linha `/build` acrescentada (append-only), apontando para este gate
- ADR: não necessário — nenhuma decisão arquitetural nova. A pergunta de arquitetura que este trabalho toca ("`SeriesKey.nature` precisa de um sexto membro, ou `SPEC-001` §5.11 precisa de um segundo termo?") já estava registrada como aberta, com dono `/architect`, em `as_of_accessor.py` (`T-04.4`) — esta task resolve D6.5/D6.6 UMA CAMADA ACIMA de `SeriesKey` (dois tipos de valor distintos, sem base comum), sem amender a identidade de 15 termos, e sem fechar aquela pergunta em nome de outra ADR

Bloqueado (se houver): nenhum. Escopo explicitamente NÃO fechado por esta task, nomeado no doc delta:
- `label_shift`/`unit`/`denom`/`reduction` de produção das quatro séries não são populados aqui — território de `T-06.2` (tabela de shift) e de task futura que grave linhas reais de `SeriesCatalogEntry`.
- Nenhuma ingestão REST de `takerlongshortRatio` existe ainda — `TakerRatioComponents` é a forma que uma REST client futura preenche; até lá, `resample_bare_taker_ratio_refuses` é o comportamento correto sobre o dump (que nunca tem `buy_vol`/`sell_vol`).
- `SeriesKey.nature` continua com UM único membro `RATIO` — decisão de `/architect`, não desta task.
