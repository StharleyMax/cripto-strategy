# T-08.12 — gate `review` (arquitetural, read-only)

**Veredito: COMPLIANT.**

## Denominador

- Regras bloqueantes em vigor: **8** (`harness rules list --severity block`, packs `core`+`web-fullstack` + `own`).
- Avaliadas contra o escopo: **8/8** (nenhuma se aplica a domínio puro sem I/O; nenhuma dispara).
- Arquivos varridos: `harness rules --mode sweep --changed-only` → `rc=0`, saída vazia (universo:
  8 arquivos alterados no worktree — 4 domain + 4 test, conferido também via `git status --short`).
- `make boundaries` (`bash backend/scripts/boundaries.sh`) → **Contracts: 7 kept, 0 broken** (inclui
  "charts só importa sentimento, nunca convergencia/backtest" e "domain/use_cases não falam com
  socket/ssl") — fronteira `charts` motor Python sem I/O preservada; nenhum arquivo em
  `frontend/src/` tocado (confirmado por `git status`/`git diff --name-only`).

## Os 4 módulos batem com a ADR-026 (D1–D4)

- `panel_grid_enablement.py` → `classify_grid_multiple`/`GridMultipleVerdict`, 3 estados nomeados
  (`MULTIPLE_OF_NATIVE`/`UPSAMPLING`/`NON_MULTIPLE`) — bate com D1.
- `panel_disc_layout.py` → `classify_disc_layout`/`DiscLayout`, `radius_px`/`ring_px` sempre
  parâmetro (grep por constante interna: 0 linhas), fórmula `2×(radius_px+ring_px) <= spacing_px`
  — bate com D2 e com a reconciliação do "Achado".
- `panel_bar_progress.py` → `Bar = FinalBar | InProgressBar` (tipo-soma), `InProgressBar` sem
  atributo `.high`/`.low`/`.close` — bate com D3.
- `panel_single_axis.py` → `SingleAxisSeries{denom: str}` campo único, `switch_denom` substitui
  (`del current`, nunca funde) — bate com D4.

Nenhuma forma recusada pela ADR ("alternativas recusadas") aparece no código.

## O achado de D8.12 está declarado, não sobrescreve o PRD

`ADR-026` §"Achado" rotula a divergência de fórmula como `[ACHADO: quant-architect, 2026-09-04]`
e a escolha de leitura como `[OPINIÃO: quant-architect, 2026-09-04]`, com a frase explícita:
"Isto não corrige `PRD-001`/o plano `08` — a redação ambígua continua lá ... essa correção de
redação é do dono daqueles documentos, não desta ADR." O docstring de `panel_disc_layout.py`
repete a mesma ressalva. Nenhum arquivo em `docs/plans/SPEC-001-plataforma-dados/` ou
`docs/specs/PRD-001-plataforma-dados.md` foi tocado (confirmado por `git status --short` —
ausentes da lista de modificados/untracked). Achado citável, não decisão silenciosa.

## Idioma de identificador

Mensagens de `raise X(...)` nos 4 módulos e docstrings — todas em inglês (superfícies 1/2/3/5 da
tabela de fronteira do `CLAUDE.md`); nenhuma nova mensagem em português encontrada.

## Fora do escopo desta revisão

QA já aprovado (`gates/T-08.12-qa.md`, reproduziu os 3 estados/fórmula/tipos/switch_denom).
Renderizador TS e rota HTTP permanecem fora de escopo, declarado na própria ADR ("Não fecha") —
não avaliados aqui por não existirem ainda.
