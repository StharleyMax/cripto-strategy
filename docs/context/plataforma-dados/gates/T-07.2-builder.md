# Gate — `T-07.2` (builder) · Survivorship na borda de ingestão

`CA-F3-14`, `SPEC-001` §5.6, plano `07` item 7.3, DoD `D7.2`+`D7.6`. Card `CST-56`, componente
`sentimento`. Handoff: [`docs/context/plataforma-dados/handoff/T-07.2.md`](../handoff/T-07.2.md).

## O que o DoD pedia

Símbolo ausente do `exchangeInfo` CORRENTE mas presente no dump histórico do S3 tem que ser
`ACCEPTED_WITH_WARNING` + linha em `md.ingest_gap` — **NUNCA `REJECTED`**. Teste: ingerir o dump
de `MATICUSDT` (existe no S3, não existe no `exchangeInfo` de hoje) e conferir que gravou, com
aviso. **109 símbolos históricos são invisíveis hoje** (21,6% do universo, 727 → 570) é o número
que justifica a regra — `[DOC: SPEC-001 §5.6]`, não remedido por esta task.

## Arquivos alterados

- `backend/src/modules/sentimento/domain/dump_survivorship.py` (new)
- `backend/tests/sentimento/test_dump_survivorship.py` (new, 6 testes)
- `backend/README.md` (doc delta: seção `## 📎 2026-09-02 por T-07.2`)
- `docs/INDEX.md` (append)

Spec: `docs/specs/SPEC-001-plataforma-dados.md` §5.6
Plan: `docs/plans/SPEC-001-plataforma-dados/07_aquisicao_em_regime.md` (item 7.3, `D7.2`/`D7.6`)

## DoD

- [x] `SurvivorshipVerdict = Literal["ACCEPTED", "ACCEPTED_WITH_WARNING"]` — sem `REJECTED` no
      tipo, verificado por `test_structural_falsifier_classify_symbol_survivorship_cannot_spell_rejected`.
- [x] "Universo corrente" reusa `instrument_universe_snapshot.exchange_info_symbols` (`T-02.1`),
      nenhuma segunda representação — verificado por `test_d7_2_maticusdt_absent_from_the_real_exchange_info_capture`
      carregando a MESMA fixture que `test_instrument_universe_snapshot.py` já usa.
- [x] `D7.2`: `MATICUSDT` (real, ausente de `ei.json`) classifica `ACCEPTED_WITH_WARNING` e
      persiste com `n_written=288` (nunca zero) através do `SqliteIngestRecordStore` real —
      `test_d7_2_maticusdt_dump_is_persisted_with_warning_through_the_real_store`.
- [x] `md.ingest_gap` ganha a linha, com `gap_class="SURVIVORSHIP_WARNING"` (novo membro da
      enumeração aberta que `SOURCE_GAP_CLASS` já registrava) e `n_missing=0` (não é buraco de
      dado, é aviso de universo).
- [x] Vocabulário de `verdict` não diverge de `domain/ingest_record.py` —
      `test_the_verdict_vocabulary_matches_ingest_record_s_and_is_not_a_second_one`.
- [x] `Natureza` (`ADR-016`): `domain/dump_survivorship.py` não importa `datetime`/socket/file —
      todo timestamp chega já formatado (`window_from_ts`/`window_to_ts`/`detected_at`).

## Comandos rodados (literais) e resultado

- `bash backend/scripts/lint.sh` → `All checks passed!` / `198 files already formatted` /
  `Success: no issues found in 198 source files` (universo: `backend/src` + `backend/tests`).
- `bash backend/scripts/test.sh` (pós-merge de `master@21a5f1e`, que trouxe `T-07.4`/`T-07.7`) →
  **1128 passed**, 0 failed, 3 warnings pré-existentes (`ResourceWarning`, achado nomeado em
  `T-03.11`, não desta task), `286,23s`. Cobertura por camada (`ADR-009/D1`): **domain 99,8%**
  (meta 90%, 2239/2243 linhas) · **use_cases 100,0%** (meta 80%, 549/549) · **infra 95,0%**
  (meta 70%, 2073/2183). `dump_survivorship.py` fecha em **100%** (19 linhas, 2 branches).
  Universo: suíte completa do componente `sentimento`.
- `.venv/bin/python -m pytest tests/sentimento/test_dump_survivorship.py -q` → **6 passed**
  (isolado, antes da rodada completa).
- `bash backend/scripts/boundaries.sh` → `Contracts: 3 kept, 0 broken` (import-linter, 142
  arquivos, 641 dependências).
- `harness rules --mode sweep --changed-only` → `rc=0`, saída vazia (0 achados bloqueantes,
  universo: os 2 arquivos desta task após `git add -A`).

### Achado de ambiente, não desta task

`bash backend/scripts/test.sh` reprovava na COLETA (`ModuleNotFoundError: No module named
'fakeredis'`) antes do merge de `master`. `T-07.4` já declarara `fakeredis = "2.37.1"` em
`pyproject.toml`/`poetry.lock` (dev-only), mas o `.venv` compartilhado por todas as worktrees
(symlink para o `.venv` do checkout principal, já que `git worktree` não duplica ambientes) ainda
não tinha o pacote instalado. Corrigido com `pip install fakeredis==2.37.1` (pin exato do lock)
diretamente no `.venv` compartilhado — nenhuma mudança em `pyproject.toml`/`poetry.lock`, é
instalação alcançando o que o lock já declarava, não uma dependência nova.

## Cobertura

99,8% domain / 100,0% use_cases / 95,0% infra sobre a suíte completa (universo: 1128 testes,
200 arquivos fonte) — acima dos alvos 90/80/70 declarados em `test_cmd`/`ADR-009/D1`.
`dump_survivorship.py` isolado: 100% (19 statements, 2 branches).

## Doc delta

- `backend/README.md`: seção nova `## 📎 2026-09-02 por T-07.2 — survivorship na borda de
  ingestão: ACCEPTED_WITH_WARNING, nunca REJECTED`, com a peça de domínio, o `D7.2` medido sobre
  dado real, o falsificador estrutural e o escopo que a task não fecha.
- `docs/INDEX.md`: linha append-only para `T-07.2`.
- ADR: não necessário — aplica `SPEC-001` §5.6 já aprovada; nenhuma decisão arquitetural nova
  (a técnica de tipo sem `REJECTED` segue o precedente já em produção de `ClosedWindow`).

## Bloqueado

Nenhum item do DoD desta task ficou bloqueado. **Fora do escopo, nomeado explicitamente**: o
pipeline real de ingestão (ETL do S3 — `T-07.3`; escritor único — `T-07.5`; fila — `T-07.4`) não
foi escrito aqui — esta task entrega a DECISÃO pura de `domain`, exercida ponta a ponta contra o
`SqliteIngestRecordStore` real, não contra um caminho de produção que ainda não existe (é dono de
tasks posteriores da mesma fase, como o `T-07.1` já registrou para o próprio escopo dele).
`T-07.8` (`universe_at`, união de duas testemunhas) depende desta task mas não é decidido aqui.

**`tasks.toml`, ledger e Jira INTOCADOS; nenhum `gate-record`, `approve` ou `advance`** — veredito
é do `/qa`.
