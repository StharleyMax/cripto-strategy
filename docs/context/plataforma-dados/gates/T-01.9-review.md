# Review Gate — T-01.9 (fase 01, componente `docs`)

Escopo do delta auditado (não commitado, branch `tasks/T-01.8-juizes-e-deploy`), exatamente o
mesmo do QA: `Makefile` (target `lint-agents`), `docs/gate-de-design.md` (+59/-0 linhas, `git
diff --stat`), `scripts/agents-by-component.golden.json`, `scripts/check-agents-by-component.sh`,
`scripts/check_agents_by_component.py`. **Não tocados por mim**: `harness.toml`,
`docs/context/plataforma-dados/tasks.toml`, `corpus/`, `deploy/` — fora de escopo (`T-01.10`
e outras).

## Camada 1 — o que o runner mede

`harness rules list --severity block` → **8 regras em vigor** (5 `core`, 2 `web-fullstack`,
1 `own`). Avaliadas as 8, sobre os 5 arquivos do delta:

```
harness rules --mode file --path Makefile                                  → vazio, rc=0
harness rules --mode file --path docs/gate-de-design.md                    → vazio, rc=0
harness rules --mode file --path scripts/agents-by-component.golden.json   → vazio, rc=0
harness rules --mode file --path scripts/check-agents-by-component.sh      → vazio, rc=0
harness rules --mode file --path scripts/check_agents_by_component.py      → vazio, rc=0
```

`harness code-paths classify <arquivo>` → `nao-producao` para os 5 (`nenhum include_prefixes
casa ['backend/src/', 'backend/tests/', 'frontend/src/', 'deploy/']`) `[MEDIDO 2026-09-03]`.
Nenhuma das 8 regras bloqueantes alcança o delta — nem por engano de path, nem por escopo
`code`/`production`. **0 de 8 mordem; 8 de 8 avaliadas.**

## Camada 2 — arquitetura declarada, as 3 perguntas do despacho

**(1) `make lint-agents` cabeado em `make lint`, ordem correta.** `Makefile:116`:
`lint: lint-agents lint-backend lint-frontend`. `lint-agents` é o primeiro dos três, e o
comentário em `Makefile:112-115` explicita o motivo medido: é o único que não depende de
`backend/.venv` nem `frontend/node_modules` — posto depois, nunca rodaria num clone sem
`make setup`. Confere com `ADR-012/D4` (o alcance de `harness rules` é o arquivo-fonte sob
`code_paths`; o resto mora no `make`) e com o plano `01` item `1.13`/`D1.12`. `make lint-agents`
sobre a árvore real → `agents.by_component: bate com o dourado — 6 componente(s), 10 papel(is).`,
`rc=0` `[MEDIDO 2026-09-03, reproduzido por mim]`.

**(2) O script compara contra o arquivo dourado, não contra cópia solta de `harness.toml`.**
Confirmado por leitura de `scripts/check-agents-by-component.sh:50-52,86,97`: `DOURADO` é
resolvido por `dirname "$0"` (sempre `scripts/agents-by-component.golden.json` do repositório
onde o script vive), e a política comparada (`POLITICA`) vem de `harness policy --key
agents.by_component` executado no **diretório corrente** — nunca lê `harness.toml` diretamente,
nem o do repositório nem cópia. Verifiquei que o dourado é byte-idêntico à política de hoje:
`python3 -c "json.load(golden) == json.load(harness policy output)"` → `True`
`[MEDIDO 2026-09-03]`. Reproduzi 1 das 5 mutações do gate de QA de forma independente, em cópia
isolada de `harness.toml` em `/tmp` (nunca o arquivo do repositório — confirmado por `md5sum
harness.toml` antes/depois da bancada, inalterado): `design_gate` apagado das 2 entradas →
`rc=1`, 2 pontos nomeados (`agents.by_component.charts.design_gate`,
`…web.design_gate` — PAPEL AUSENTE), batendo exatamente com a tabela do gate de QA e de
`docs/gate-de-design.md`.

**(3) Nenhuma regra bloqueante alcança estes arquivos por engano.** Coberto na Camada 1 acima —
`classify` confirma `nao-producao` para os 5, e `harness rules --mode file` devolve vazio para
os 5. Consistente com o precedente citado no gate de QA (outros scripts em `scripts/` sem lint
próprio).

## Achados adicionais (julgamento, sobre arquitetura declarada)

Nenhum achado bloqueante. Um ponto **já declarado pelo próprio delta**, não uma violação nova:
`docs/gate-de-design.md` (linhas finais do trecho novo) registra que `scripts/verify.sh` **não**
chama `lint-agents` — os "seis portões" que `CLAUDE.md` §"Higiene de contexto" nomeia continuam
seis, e fechar essa lacuna é ato do owner (edita `CLAUDE.md`). Isto é limitação **declarada em
prosa no próprio artefato**, não acobertada — não constitui violação de regra nem de arquitetura
por conta disso, e por isso não vira item do veredito.

Idioma de identificador: `scripts/check_agents_by_component.py` levanta `ValueError` com 2
mensagens em português (`load_golden`, `load_actual`). A decisão de `CLAUDE.md`
§"Mensagem de exceção" que move `raise`/`Error`/`Exception` para inglês mede e se ampara
especificamente em `backend/src` (linha 1 da tabela, "identificador de produção — `backend/src`,
`frontend/src`"); `scripts/` não está enumerado nas 12 linhas da tabela nem no universo medido
daquela decisão. Sem uma linha da tabela ou universo medido que declare `scripts/` dentro da
fronteira do inglês, não cito isto como violação — seria opinião, não citação. Registro como
observação, fora do veredito.

## Denominador

- Regras bloqueantes existentes: **8** (`harness rules list --severity block`).
- Regras avaliadas contra o delta: **8 de 8**, individualmente, arquivo por arquivo (5 arquivos
  × `harness rules --mode file`, mais `harness code-paths classify` para confirmar escopo).
- Arquivos varridos: **5** (o delta inteiro declarado pelo QA e pelo despacho), com
  `git status --short` e `git diff --stat` conferidos para excluir qualquer arquivo fora do
  escopo declarado (`harness.toml`, `tasks.toml`, `corpus/`, `deploy/` — não tocados por esta
  review, presentes na árvore por outras tasks do mesmo branch).
- Mutação independente reproduzida por mim: **1 de 5** (a mesma que o QA já reproduziu 5/5;
  suficiente para corroborar o mecanismo sem repetir o trabalho integral do QA).

## Veredito

**COMPLIANT**

Nenhuma das 8 regras bloqueantes em vigor alcança o delta de `T-01.9`; o cabeamento em
`make lint` está na ordem correta e com o motivo medido; o script compara contra o arquivo
dourado versionado, nunca contra `harness.toml` (real ou cópia); e a asserção nova reproduz o
que o gate de QA alegou. Gate de QA (`docs/context/plataforma-dados/gates/T-01.9-qa.md`)
permanece **APPROVED**, consistente com esta review.

```
harness gate-record plataforma-dados 01 REVIEW COMPLIANT "T-01.9: 8/8 regras bloqueantes avaliadas, 0 mordem; lint-agents cabeado 1o em make lint; comparador usa golden versionado via dirname, nunca harness.toml direto; 1/5 mutacoes do QA reproduzida de forma independente (design_gate apagado -> rc=1, 2 pontos)."
```
