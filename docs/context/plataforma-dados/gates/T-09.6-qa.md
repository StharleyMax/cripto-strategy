# QA — T-09.6 auditoria append-only de `docs/INDEX.md`

Componente: `docs`. Reprodução independente do falsificador do builder
(`docs/context/plataforma-dados/gates/T-09.6-builder.md`) + verificação byte-a-byte dos 4
achados reais + verificação dos 3 merges por multiset + verificação de ancestralidade do
commit corrompido.

## 1. Reprodução do falsificador

`git log --oneline -- docs/INDEX.md | wc -l` → **149** (bate). Falsificador Python reescrito
do zero (subsequência ordenada, linha a linha, contra cada pai) rodado sobre os mesmos 149
commits → **8 candidatos, mesmos hashes**: `dad63fe08e`, `add2c100ad`, `7c068c07a3`,
`f750928841`, `8de383239f`, `b235be5c86`, `e92b7bd77c`, `5e07654347`.

## 2. Os 4 "violação real" — confirmados byte a byte

- `dad63fe`: diff de caractere (`difflib.SequenceMatcher`) → **0 chars removidos por `delete`**,
  só `replace` que envolve o texto antigo em `~~...~~` e insere a nota de tarja. Texto antigo
  sobrevive integralmente. Confirma "tarja, sem perda".
- `7c068c0`: mesmo método → **0 chars removidos**, só inserção de `\` antes de `|`. Confirma
  "escape cosmético".
- `e92b7bd` (grave): linha `T-07.3` reescrita in-place — `1108 passed`→`1147 passed`,
  `97,83%`→`97,44%`, `99,9%`→`99,8%`, `95,4%`→`95,0%`, `138`→`144`, `636`→`656`, `60`→`63`
  arquivos. Números antigos **não sobrevivem** na linha nova (só via `git show e92b7bd^:docs/INDEX.md`).
  Confirmado: reescrita real de conteúdo já publicado, sem erratum.
- `5e07654` (grave): linha `T-05.15` — antes termina em `` Gate: `...T-05.15-builder.md` | ``;
  depois passa a `` QA `APPROVED` (fuzz 643 casos `sha256.ts` vs `node:crypto`, 0 falha),
  review `COMPLIANT`. Gates: `...T-05.15-{builder,qa,review}.md` | ``. Confirmado: veredito
  de QA/review inserido dentro da linha do `/builder` já commitada, e `Gate:`→`Gates:` mudado,
  em vez de linha nova. O mesmo commit também acrescenta a linha `T-08.1` nova (correta, não é
  o problema).

## 3. Os 3 "reordenação de merge sem perda" — confirmados por multiset

`Counter(pai) − Counter(filho)` para cada pai:
- `add2c100ad`: **0 linhas ausentes** dos dois pais (`14ef986`, `9775c7e`).
- `f7509288`: **0 linhas ausentes** dos dois pais (`5523167`, `1ad1434`) — conflito real
  declarado no corpo do commit, resolvido sem perda.
- `b235be5c86`: pai `34141bb0` → 0 ausente; pai `8de3832` → **2 linhas ausentes**, mas são
  exatamente as 2 linhas corrompidas daquele pai (a truncada de `T-03.6` e a de `T-07.4`) —
  o merge adota a versão íntegra do outro lado. `grep -F 'aplicada aqui a T-03.6/PR #73'` no
  resultado do merge → **1570 chars** (íntegra, não 296). Nenhuma informação válida perdida.

## 4. O "morreu on-branch" (`8de3832`) — confirmado, com checagem mais forte que a do builder

`git merge-base --is-ancestor 8de3832 master` → **true** (é ancestral no grafo, esperado: PR
não foi squash). Mas isso não prova conteúdo — teste mais forte: `git rev-list --first-parent
origin/master | grep -x 8de3832` → **vazio, não está na história first-parent** (nunca foi
`HEAD` de master). E o merge real para master (`21a5f1e`, PR #83) tem `docs/INDEX.md` com a
linha em **1570 chars** (íntegra) — a corrupção nunca foi o conteúdo de master em nenhum ponto
do tempo, só existiu transitoriamente na branch `tasks/T-07.4-fase07` entre `8de3832` e o fix
`90c3b9b`, ambos pré-merge.

## 5. Julgamento da classificação do builder

**Classificação correta, nenhuma reclassificação.** 3 merge-ok + 1 morreu-on-branch + 4
violação-real = 8, todos re-verificados com evidência independente (diff de caractere,
multiset, ancestralidade first-parent) — não apenas re-lida a narrativa do builder.

## Achados reais finais (append-only violado, sem erratum documentado)

1. `dad63fe08e` — tarja em linha `ADR-010`, sem perda de texto.
2. `7c068c07a3` — escape `\|` cosmético em linha `T-03.7`.
3. `e92b7bd77c` — **reescrita de números** (`1108→1147 passed`, `97,83%→97,44%`, etc.) na
   linha `T-07.3`, irrecuperável na própria linha.
4. `5e07654347` — **inserção de veredito QA/review** dentro da linha `T-05.15` já commitada,
   em vez de linha nova.

Sem ação corretiva tomada por esta task de QA (mesma restrição do builder — decisão de escopo
para o owner: dívida histórica aceita ou chore de correção).

## Comandos citáveis

- `git log --oneline -- docs/INDEX.md | wc -l` → 149
- falsificador Python (subsequência ordenada) → 8 candidatos, mesmos hashes do builder
- `difflib.SequenceMatcher` sobre `dad63fe`/`7c068c0` → 0 chars removidos por `delete`
- `Counter(pai) - Counter(filho)` sobre `add2c100ad`/`f7509288`/`b235be5c86` → 0/0/2(corrupção)
- `git rev-list --first-parent origin/master | grep -x 8de3832` → vazio
- `git show 21a5f1e:docs/INDEX.md | grep -F '...T-03.6.../PR #73' | awk '{print length}'` → 1570

## Veredito

Ver `docs/context/plataforma-dados/gates/T-09.6-qa.md` (este arquivo) — **APPROVED**. A
auditoria é factualmente correta, reproduzível de forma independente, e a classificação dos 8
candidatos está certa. Achado real (4 violações não documentadas) fica nomeado para decisão do
owner, não é defeito da task de auditoria em si.
