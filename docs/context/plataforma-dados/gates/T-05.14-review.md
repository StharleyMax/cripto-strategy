# Review Gate — `T-05.14` / `CST-105` — "O cliente liga na rota real"

**Papel:** revisor arquitetural (read-only) · **Data:** 2026-09-04 · **Branch:**
`tasks/T-05.14-cliente-rota-real` · **Worktree:** `/tmp/claude-1002/wt/T-05.14` (zero escrita em
código de produção nesta sessão). **Contra:** `ADR-019-cliente-http-de-ingest-health-e-paridade-de-fingerprint.md`,
`ADR-005-transporte-de-leitura.md` §D6, `gates/T-05.14-{architect,builder,qa}.md`, `CLAUDE.md`.

## Camada 1 — o que a máquina mede

```
harness rules list --severity block                                          # 8 regras em vigor
harness rules --mode file --path frontend/src/features/s1-console/ingest-health-query.ts        # rc=0, vazio
harness rules --mode file --path frontend/src/features/s1-console/ingest-health-query-http.test.ts # rc=0, vazio
harness rules --mode file --path frontend/src/features/s1-console/ingest-health-query.test.ts   # rc=0, vazio
harness rules --mode sweep --changed-only                                    # rc=0, vazio
```
Denominador: **8 regras bloqueantes em vigor** (`core.relative-import`, `core.silent-except`,
`core.print-statement`, `core.hardcoded-secret`, `web-fullstack.browser-imports-server`,
`web-fullstack.tenant-from-request`, `web-fullstack.server-test-directory-present`,
`own.compose-hardcoded-secret`), **8 avaliadas** (sweep cobre todas), **3 arquivos varridos
isoladamente** (`ingest-health-query.ts`, `ingest-health-query.test.ts`,
`ingest-health-query-http.test.ts`) + sweep sobre o diff inteiro. **Zero achado mecânico.**

Testes e lint rodados por mim, de novo (não só citados):
- `npm --prefix frontend run test:s1` → **48 pass / 0 fail** (universo: 4 arquivos
  `src/features/s1-console/*.test.ts`) `[MEDIDO]`.
- `npm run lint` (`eslint src`) → saída vazia. `npx tsc -p tsconfig.json --noEmit --strict` →
  saída vazia `[MEDIDO]`.

## Camada 2 — arquitetura declarada, item a item do que foi pedido

**(1) `fingerprint()` continua síncrona no TS.** Confirmado —
`frontend/src/features/s1-console/ingest-health-query.ts:278-280`, `export function
fingerprint(projection): string`, corpo síncrono (`createHash(...).update(...).digest("hex")`),
zero `await`/`Promise` na cadeia `parseIngestHealthEnvelope → fingerprint` (só a borda de I/O em
`fetchIngestHealthProjectionViaHttp`, linhas 537-556, é `async`) — bate com `ADR-005/D6.4` e
`ADR-019/D3`. `node:crypto` (linha 88, `import { createHash } from "node:crypto"`) é import de
VALOR, mas é Node, não browser — `D6.4` não proíbe isso, proíbe o browser de importar por VALOR
este módulo. Medi essa segunda parte: `grep -rn 'from ".*ingest-health-query\.ts"'
frontend/src --include='*.ts' --include='*.tsx' | grep -v '\.test\.'` → **3 importadores, os 3
`import type`** (`s3-inspector/{fixtures,domain,view-model}.ts`) — zero aresta de runtime de
browser para dentro do módulo, mesma medição que `ADR-005/D6.4` já registra em `8c002e4`, ainda
válida após `T-05.14`. Nenhum `"use client"` referencia o módulo.

**(2) `runIngestHealthCli` e o resto do transporte CLI intocados.** Confirmado por diff —
`git diff HEAD -- frontend/src/features/s1-console/ingest-health-query.ts` mostra que o corpo de
`runIngestHealthCli` (linhas 467-494), `IngestHealthCliOptions`/`IngestHealthCliResult`,
`HOSTED_SCRIPT`, `DEFAULT_BACKEND_ROOT`/`THIS_FILE_DIR` e os 4 imports `node:` não têm nenhuma
linha `-`/`+` própria — só a docstring do módulo ganhou uma seção nova narrando a mudança.
`ingest-health-query.test.ts` mantém o teste de `DoD-3` sobre o CLI real
(`runIngestHealthCli`, linha 87) e os dois testes de `fingerprint` sobre fixture pura (linhas
123, 153) — exatamente o que `ADR-019/D1`/"Consequência" prometia manter. `git diff HEAD --stat
-- backend/` → vazio: zero arquivo de `backend/` tocado.

**(3) URL só via `process.env.INGEST_HEALTH_API_BASE_URL`.** Confirmado —
`resolveIngestHealthBaseUrl` (linha 515-526) só lê `options.baseUrl ?? process.env.
INGEST_HEALTH_API_BASE_URL`, lança se nenhum dos dois existir; a mensagem de erro nomeia
explicitamente a proibição de `NEXT_PUBLIC_*` (`ADR-019/D4`). `grep -n 'https\?://'` no módulo de
produção → zero ocorrência; as únicas strings `http://127.0.0.1:...` literais estão em
`ingest-health-query-http.test.ts` (8 ocorrências, todas em `baseUrl` apontando para o servidor
que o próprio teste sobe) — dentro da allowlist que `web-fullstack.hardcoded-url` já concede a
`127.0.0.1`, e confirmado pelo `harness rules --mode file` limpo acima.

**(4) Os 2 warnings do QA rodada 2 — avaliação.**
- **Achado 1 do QA** (elemento não-objeto de `runs[]`/`gaps[]` sem teste MORDE versionado):
  confirmei por `grep` que `ingest-health-query.test.ts` só cobre corpo-raiz não-objeto (linha
  210, `/response body is not a plain JSON object/`) — nenhum teste aciona
  `assertIngestHealthRunRow`/`assertIngestHealthGapRow` com `runs: [null]` ou `gaps: [["x"]]`.
  Gap real, comportamento correto (o `if (!isPlainRecord(value))` de cada função está lá,
  linhas 351/366), só falta a prova versionada. **Fica WARNING** — não é regra bloqueante (as 8
  regras em vigor não cobrem cobertura de teste), não é violação de arquitetura declarada (nenhum
  documento exige 100% de branch coverage), e o comportamento em produção está certo. Não
  cumpre o padrão desta revisão para virar achado formal com id de regra.
- **Achado 2 do QA** (README sem frase de justificativa por escrito): confirmei —
  `git diff HEAD -- frontend/README.md` vazio, e correto que fique vazio: o corpo de
  `runIngestHealthCli`/`spawnSync` que a seção existente do README descreve não mudou (item 2
  acima). **Fica WARNING** — processo/hygiene, não regra bloqueante nem violação de arquitetura.

## Um alarme falso que EU levantei e descartei — registrado para não virar ruído depois

`git diff origin/master -- docs/INDEX.md` mostra a linha de `T-05.13` sendo substituída pela de
`T-05.14` (aparência de reescrita, o que violaria "`docs/INDEX.md` é append-only",
`CLAUDE.md`). **Investiguei antes de reportar**: `origin/master` já avançou (merge de `T-05.13`,
commit `b19f92f`) além do ponto onde este branch nasceu (`merge-base` = `56b866c`). Contra o
`merge-base` e contra `HEAD` do próprio branch, `git diff` mostra **só `+1` linha** — append
puro, nenhuma linha existente reescrita. A aparência de reescrita é artefato de comparar contra
um `origin/master` que andou por cima do ponto de partida deste branch, não uma ação deste
branch. **Não é achado** — é o tipo de comparação contra baseline errado que produziria falso
positivo, e falso positivo reportado treina o leitor a ignorar o relatório.

## ADR-019 em `RASCUNHO`

Aprovação de ADR é gate do owner (`CLAUDE.md` §"O ledger é a identidade do estado"). `RASCUNHO`
durante a implementação segue o precedente já registrado no ledger para `ADR-018` (`WARNING`
não-bloqueante, `docs/INDEX.md` linha `T-05.11`). Mesmo tratamento aqui: **WARNING, não
bloqueia**.

## Veredito

**COMPLIANT**

- Denominador: **8/8 regras bloqueantes avaliadas**, **3 arquivos varridos individualmente +
  sweep completo do diff**, **0 violação mecânica**.
- **0 violação de arquitetura declarada** (`ADR-019`, `ADR-005/D6.4`) — os 4 itens pedidos pelo
  despacho (fingerprint síncrono, CLI intocado, URL só por env, escopo dos 2 warnings)
  confirmados por leitura de código + diff, não por citação do gate.
- **2 WARNINGS** (não bloqueantes, ambos já registrados pelo QA rodada 2 e aqui confirmados como
  reais porém não-arquiteturais): teste MORDE ausente para elemento não-objeto em
  `runs[]`/`gaps[]`; frase de justificativa do README ausente por escrito.
- **1 WARNING de processo herdado**: `ADR-019` segue `RASCUNHO` (aprovação é gate do owner),
  mesmo precedente de `ADR-018`.

Achados:
1. [WARNING] `parseIngestHealthEnvelope`: `runs[i]`/`gaps[i]` não-objeto sem teste MORDE
   versionado — comportamento correto, cobertura ausente. `ingest-health-query.test.ts`.
   Correção sugerida: 1-2 `assert.throws` com `runs: [null]` / `gaps: [["x"]]`.
2. [WARNING] `gates/T-05.14-builder.md` não registra por escrito por que `frontend/README.md`
   não ganhou seção — verificado que a omissão está correta (seção existente continua válida),
   falta só a frase.
3. [WARNING] `ADR-019` em `RASCUNHO` — aprovação pendente do owner, precedente `ADR-018`.

Nenhum achado com id de regra bloqueante ou citação de documento arquitetural chegou a
`NON_COMPLIANT`.
