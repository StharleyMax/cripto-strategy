[REVIEW GATE — Fase 05: grade canônica de `charts` (D5.9) + fronteira `charts`⇄`web` executável (D5.12)]

**Feature:** `plataforma-dados`   **Componente:** `charts`   **Card:** `CST-35`
**Revisor:** `/review` (read-only, arquitetural)
**Worktree:** `.claude/worktrees/T-05.1`, branch `tasks/T-05.1-grade-canonica-charts`
**Builder:** `docs/context/plataforma-dados/gates/T-05.1-builder.md`
**QA:** `docs/context/plataforma-dados/gates/T-05.1-qa.md` (3 rodadas, veredito final `APPROVED`)
**ADR:** `docs/adr/ADR-003-fronteira-charts-web.md` (2 addenda desta task: `D5.12` executable-boundary
rationale, e o limite de escopo permanente para forma dinâmica ofuscada)

## Denominador

- Regras bloqueantes em vigor: 7 (`harness rules list --severity block`).
- Avaliadas: 7 de 7, sobre os 7 arquivos escopo desta revisão
  (`canonical-grid.ts`, `canonical-grid-chart-consumer.ts`, `canonical-grid-accessor-consumer.ts`,
  os 3 arquivos `*.test.ts` correspondentes, `frontend/eslint.config.mjs`).
- Comandos rodados por mim, independentes do relatório do builder/QA:
  - `harness rules --mode file --path <arquivo>` × 7 → 0 achados em cada um.
  - `git add -A -- frontend/src/charts docs/adr/ADR-003-fronteira-charts-web.md frontend/eslint.config.mjs && harness rules --mode sweep --changed-only && git reset` → `rc=0`, 0 achados `[MEDIDO 2026-09-03, reprodução minha, terceira vez independente após builder e QA]`.
  - `npm --prefix frontend run test:charts` → **36 pass, 0 fail** `[MEDIDO 2026-09-03]` — confere com a Rodada 3 de QA.
  - `npm --prefix frontend run lint` → `rc=0` `[MEDIDO 2026-09-03]`.

## Achados

Nenhuma violação de regra bloqueante (`core.*`, `web-fullstack.*`) encontrada. Nenhuma violação
de arquitetura declarada (ADR-003 FR-1/FR-2/FR-3) encontrada nos arquivos de código.

- **[OK] Fronteira de idioma (`CLAUDE.md`, tabela de 12 linhas + seção de mensagem de exceção).**
  Todo identificador, nome de arquivo, comentário, docstring e mensagem de `throw new
  RangeError(...)`/`throw new Error(...)` nos 7 arquivos está em inglês — inclusive as
  mensagens novas em `canonical-grid.ts` (`"timeframeMs must be positive..."`,
  `"candle openTimeMs ... does not land on a grid slot..."`, `"duplicate candle for grid slot
  ..."`) e em `canonical-grid-sha256-proof.test.ts` (`"fixture CSV is missing an expected
  column..."`). Nada em português nestes 7 arquivos, exceto citações literais de nomes de ADR
  em português dentro de comentário (`"grade canônica"`, `"nao reimplementa"`) — que é citação
  de documento (linha 7 da tabela), não identificador de código, e portanto fora da fronteira.
- **[OK] `ADR-016` (natureza do relógio).** Não aplicável a este escopo: `make natureza`
  (`backend/scripts/natureza.sh`) varre só `backend/src/modules/*/domain`/`use_cases`
  `[DOC: backend/scripts/natureza.sh:1-9]`; não há instrumento equivalente para `frontend/`, e
  o próprio módulo declara "zero I/O... every timestamp arrives as a parameter" — a única
  chamada de relógio de parede no escopo é `Date.UTC(2026, 7, 20, ...)` em
  `canonical-grid-sha256-proof.test.ts:47-48`, uma CONSTANTE literal de teste, não uma leitura
  de `Date.now()`/relógio do sistema.
- **[OK] Fronteira executável `charts`⇄`web` (`ADR-003` D5.12), citação exata.**
  `frontend/eslint.config.mjs:103-179` (bloco `src/charts/**`) e `:180-232` (bloco
  `src/app/**`+`src/features/**`) implementam exatamente o que os 2 addenda de
  `ADR-003:365-407` e `:411-473` descrevem: `no-restricted-imports` para import/export ES
  estático, `no-restricted-syntax` com 3 seletores `esquery` espelhados por bloco
  (`ImportExpression` com `source` `Literal`, `ImportExpression` com `TemplateLiteral` sem
  interpolação, `CallExpression` de `require` com `Literal`). Reproduzi as 4 formas fechadas e
  as 2 fora de escopo com sondas próprias — ver abaixo.
- **[OK] Honestidade do limite de escopo declarado (Classe 2 do addendum, `ADR-003:436-473`).**
  Reli a alegação técnica ("concatenação e template interpolado são indecidíveis
  estaticamente") e a confirmei eu mesmo, de forma independente do argumento de QA rodada 3
  (que já havia verificado por `espree`): `BinaryExpression` não carrega `.value`/`.cooked`, e
  um `TemplateLiteral` com `expressions.length > 0` não tem campo único que junte `quasis` e
  `expressions` antes da execução — os 2 seletores `esquery` do arquivo
  (`frontend/eslint.config.mjs:153,206`) de fato só casam `source.value` (nó `Literal`), então
  a alegação da ADR não SUPERESTIMA cobertura: ela nomeia exatamente o que os seletores
  alcançam e o que não alcançam, e o código bate com o texto. Não achei nenhuma reivindicação
  no addendum que prometa mais do que `eslint-boundary.test.ts` prova (o arquivo de teste
  também não tem sonda para as 2 formas fora de escopo — "morde+cala não afirma cobertura que
  o contrato não tem", `eslint-boundary.test.ts:60-62`, e isso é verdade por inspeção: os 4
  testes do arquivo cobrem exatamente as 4 formas fechadas, nenhuma sonda de concatenação ou
  template interpolado existe).
- **[OK] `D5.9` — UMA implementação, prova por `sha256` sobre dado real.** `canonical-grid.ts`
  é a única fonte da aritmética de grade; os 2 consumidores (`chart-consumer`,
  `accessor-consumer`) só compõem `buildCanonicalGrid`/`alignCandlesToGrid`, nunca
  reimplementam — condição textual de `ADR-003` FR-3 ("nunca reimplementa"), verificada por
  leitura direta dos 2 arquivos de consumidor (nenhum dos dois declara lógica de bucket
  própria).
- **[ANOMALIA, não bloqueante, herdada de QA] Sem alvo de cobertura declarado para
  `charts`/`frontend`** em `harness policy --key test_cmd` (só lista `sentimento`) — mesma
  observação das 3 rodadas de QA, `[NÃO MEDIDO]`, não decide o veredito.
- **[INFO] `docs/INDEX.md`, entrada desta task (linha adicionada em 2026-09-03T01:15Z).** Cita
  "**33 pass**" e descreve só a mutação da Rodada 1 (reordenar slots + neutralizar 1 direção do
  lint) — a entrada não reflete o estado final (**36 pass**, 3 rodadas de QA, o achado e
  fechamento do bypass de `import()` dinâmico/template/`require`). Não é uma regra bloqueante
  (`docs/INDEX.md` é append-only por convenção, não por `[[rules.own]]`, e o texto já linka os 3
  gates que TÊM o histórico completo) — mas é um número que ficou defasado no mesmo documento
  que a doutrina "nenhum número sem o comando" rege. Sugestão concreta: se uma próxima entrada
  tocar esta linha, corrigir para "36 pass" e citar as 3 rodadas; não é ato deste revisor
  reescrever uma linha existente (`docs/INDEX.md` é append-only).

## Sondas próprias (não reusadas do builder nem do QA)

```
$ npm --prefix frontend run test:charts   # reprodução direta, sem qualquer probe manual
36 pass, 0 fail                                                    [MEDIDO 2026-09-03]
$ npm --prefix frontend run lint
rc=0                                                                [MEDIDO 2026-09-03]
$ git add -A -- frontend/src/charts docs/adr/ADR-003-fronteira-charts-web.md frontend/eslint.config.mjs \
  && harness rules --mode sweep --changed-only && git reset
rc=0, 0 achados                                                     [MEDIDO 2026-09-03]
```

Não plantei sondas efêmeras adicionais de bypass de import — as 3 rodadas de QA já cobriram
morde+cala nas 2 direções para as 4 formas fechadas e verificaram, por AST próprio (`espree`),
as 2 formas fora de escopo; reexecutar o mesmo experimento sobre código inalterado mediria a
mesma coisa, não uma nova (mesmo princípio que a Rodada 3 de QA já aplicou a `D5.9`).

## Veredito

**COMPLIANT**

Regras bloqueantes avaliadas: 7 de 7. Arquivos varridos: 7 (via `harness rules --mode file` ×7 e
`--mode sweep --changed-only` sobre a árvore inteira desta task). Nenhum achado com id de regra
ou citação de documento chega a nível `[BLOQUEIO]`/`BLOCKER`. Um item `[INFO]` (número defasado
em `docs/INDEX.md`) e uma `[ANOMALIA]` herdada (cobertura não instrumentada) não mudam o
veredito.
