# T-05.7 — builder report (self-attested, NOT final QA/review)

**Task:** `[charts] 05 - Cor como token nomeado por papel; critical fora do canal de cor`
(`CST-41`). **Dono de julgamento:** quant-architect. **Status:** implementado e testado;
`tasks.toml` continua `todo` — não me atribuo `done`, `APPROVED` nem `COMPLIANT`. O
orquestrador despacha `/qa` e `/review` independentes.

**Nota de continuidade:** o código (commit `a10aa71`) já existia no branch e em
`origin/tasks/T-05.7-cor-token` ao início desta invocação — implementado, testado e commitado
por uma sessão anterior, sem gate report nem entrada em `docs/INDEX.md`. Esta invocação: (1)
verificou o trabalho existente do zero (build limpo, `npm install`, lint, testes, dado real via
symlink `data/` — ausente em worktree novo, não rastreado por git), (2) achou e corrigiu 1
defeito (abaixo), (3) escreve o gate report e o `INDEX.md` que faltavam. Não reimplementei nada
de `T-05.2` — `color-tokens.ts` compõe sobre `s2-headless-run.ts`/`s2-axis-integration.test.ts`
existentes (campo `style` opcional, default `{}`, nenhum call site anterior quebra).

## O que foi construído (commit `a10aa71`, sessão anterior)

| arquivo | papel |
|---|---|
| `frontend/src/charts/color-tokens.ts` | 6 papéis (`directionUpFill`, `directionDownFill`, `directionOn`, `dataBrokenInk`, `provenanceStrong`, `provenanceWeak`) × 2 modos, união fechada SEM `critical`/`severidade` (`ADR-010/D-5`, `CA-F4-10`) — guarda dupla: estática (union sem o membro) e runtime (`assertNoForbiddenColorRoles` + `FORBIDDEN_COLOR_ROLE_SUBSTRINGS`). `candlestickSeriesColors(mode)` deriva as 6 chaves de `CandlestickStyleOptions` de EXATAMENTE 2 tokens |
| `frontend/src/charts/color-tokens.test.ts` | cross-check hex a hex contra `scripts/validate_palette.js` (lido como TEXTO, nunca executado/importado — evita herdar `process.exitCode`), falsificador D5.6 (`#f23645`↔`#eb6834`), e o caso negativo da guarda mostrado REJEITANDO (`criticalFill`/`severityFill`/`severidadeFill`/`CriticalFill`) |
| `frontend/src/charts/s2-headless-run.ts` | +campo opcional `style` em `HeadlessSeriesSpec` (default `{}`) e `appliedCandlestickColors` no resultado, lido de `series.options()` da biblioteca REAL — aditivo, zero call site anterior quebrado |
| `frontend/src/charts/s2-axis-integration.test.ts` | +1 assert: a série de preço de `T-05.2` passa a ser estilizada com `candlestickSeriesColors("dark")` e o round-trip real contra `lightweight-charts` é verificado |
| `frontend/README.md` | `§13` nova (append-only) — ver Doc delta |

Detalhe completo, com o argumento de escopo (`acao-*`/`foco` ficam fora — chrome de `web`, não
`charts` — `ADR-003` FR-1 + boundary `D5.12`): `frontend/README.md` §13.

## O achado desta rodada, e a correção aplicada

`README.md` §13 dizia **"76 pass (10 novos)"** sem comando nem universo. Isolei o commit:
worktree no pai de `a10aa71` (`6ecb9ae`) + `npm --prefix frontend run test:charts` → **69
pass**; neste branch, mesmo comando → **76 pass**. `76 − 69 = 7`, não `10`
`[MEDIDO 2026-09-03: git worktree add --detach /tmp/t057-parent 6ecb9ae && npm --prefix
frontend run test:charts; worktree removido depois]`. Os 7 batem exato com
`color-tokens.test.ts` isolado (`node --test src/charts/color-tokens.test.ts` → **7 pass**: 5
`test(...)` estáticos + 2 gerados pelo laço `for (const mode of ["light","dark"])`). Corrigido
em `frontend/README.md` §13 (commit `57da0a3`, nesta sessão) — o número errado não tinha
etiqueta nem comando, era estimativa escrita como medição.

## Comandos rodados nesta sessão, literais

```bash
npm --prefix frontend install
  -> up to date, audited 129 packages, 0 vulnerabilities
npm --prefix frontend run lint
  -> eslint src, rc=0, 0 erro/aviso
npm --prefix frontend run test:charts
  -> node --test 'src/charts/*.test.ts' -> 76 pass, 0 fail (universo: todo frontend/src/charts/*.test.ts)
node scripts/validate_palette.js
  -> exit 0, "medicoes executadas nesta rodada: 361"
harness rules --mode sweep --changed-only
  -> 0 achado, rc=0 (rodado 2x: antes e depois da correção do README)
```

**Pré-condição de ambiente, worktree novo:** `data/` é gitignored e não existe em worktree novo
(`ls data/` → `ENOENT`) — 2 dos 20 arquivos de teste de `charts` leem dado real de disco
(`s2-panels.test.ts`, `s2-oi-loader.test.ts`, e indiretamente `s2-axis-integration.test.ts`).
Symlink criado apontando para o `data/` do repo principal (`ln -s
/home/stharley/Documentos/projects/cripto-strategy/data data`) — confirmado NÃO rastreado
(`git check-ignore -v data` → rc=1, mas o padrão `data/` do `.gitignore` cobre diretórios, não
symlinks; nada foi `git add`ado além de `frontend/README.md`, staged e commitado por nome
explícito, nunca `git add -A`).

## Cobertura

Sem piso declarado para `charts` (`harness policy --key test_cmd` só cobre `sentimento`). 7
testes novos (medido acima) cobrem as 3 funções/constantes exportadas × 2 modos, o cross-check
contra `validate_palette.js`, e o caso negativo da guarda; +1 assert (não teste novo) em
`s2-axis-integration.test.ts`.

## Doc delta

`frontend/README.md` — **atualizado**, `§13` nova (commit `a10aa71`) + correção da contagem de
testes (commit `57da0a3`, esta sessão). `docs/specs/SPEC-001-plataforma-dados.md` §6.2 e
`docs/plans/SPEC-001-plataforma-dados/05_fatia_visivel.md` `D5.6` — **sem mudança**: já carregam
a correção de citação de `ADR-010` (commit `452e1f8`, anterior a esta task); esta task consome
os números publicados ali. `scripts/validate_palette.js` — **sem mudança**: `docs`-owned; esta
task só lê o texto dele. `docs/INDEX.md` — **atualizado**, entrada nova (append-only), mesmo
padrão self-attested que `T-05.2` usou.

Relatório completo desta implementação: `frontend/README.md` §13. `tasks.toml` continua `todo`;
ledger e Jira intocados; nenhum `gate-record`/`approve`/`advance` executado.
