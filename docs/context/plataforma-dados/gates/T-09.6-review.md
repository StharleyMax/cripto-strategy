# Review — T-09.6 · Auditoria append-only de `docs/INDEX.md`

**Veredito: COMPLIANT**

## Denominador

8 regras bloqueantes em vigor (`harness rules list --severity block`). `git diff --name-only
origin/master...HEAD` vazio — a branch está nivelada com `origin/master` exceto pelos dois
arquivos novos de gate (`gates/T-09.6-{builder,qa}.md`). `harness rules --mode sweep
--changed-only` rodou sobre 0 arquivos de código, `rc=0`, 0 achados — nenhuma das 8 regras é
avaliável porque nada de código mudou. Nenhum código de produção alterado.

## Escopo respeitado

`git log origin/master..HEAD --name-only -- docs/INDEX.md` vazio — esta task não tocou
`docs/INDEX.md`. Correto: "corrigir" a disciplina append-only reescrevendo o próprio arquivo
repetiria exatamente o defeito que a task audita.

## Decisão de não corrigir unilateralmente — correta

Os 4 commits com violação real (`dad63fe`, `7c068c0`, `e92b7bd`, `5e07654`) já estão em
`master`. Corrigi-los exigiria reescrever histórico já publicado (`rebase`/force-push),
operação destrutiva não autorizada e não pedida pelo owner. `CLAUDE.md` marca gates de decisão
de arquitetura/histórico como ato do owner, não do agente. Nomear a correção como pendência
para `/qa`/owner — em vez de agir sozinho — é a escolha certa dado que os commits são imutáveis
e já publicados.

## Spot-check independente

- `git diff --numstat 5e07654~1 5e07654 -- docs/INDEX.md` → `2 1` (append + 1 linha
  modificada in-place — não é append puro).
- `git diff --numstat e92b7bd~1 e92b7bd -- docs/INDEX.md` → `1 1` (mesma classe).

Ambos consistentes com o que o builder e o QA relataram (reescrita in-place de uma linha já
commitada, não append puro).

## Referências lidas

`docs/context/plataforma-dados/gates/T-09.6-builder.md`,
`docs/context/plataforma-dados/gates/T-09.6-qa.md`, `CLAUDE.md` §"Registro de artefatos é
append-only".
