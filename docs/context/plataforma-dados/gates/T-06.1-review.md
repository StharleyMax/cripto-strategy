[REVIEW GATE — Fase 06: `series_catalog` como contrato lido pelos testes]
Feature: `plataforma-dados`   Componente: `sentimento`   Task raiz: `T-06.1` (destrava T-06.2..T-06.10)
PR: https://github.com/StharleyMax/cripto-strategy/pull/96 (branch `tasks/T-06.1-fase06`)
Worktree auditada: `/tmp/claude-1002/wt/T-06.1` (HEAD `29b759e`)
Arquivos no escopo do diff `master...HEAD`:
- `backend/src/modules/sentimento/domain/series_catalog.py` (novo, produção)
- `backend/tests/sentimento/test_series_catalog.py` (novo, teste)
- `backend/README.md` (append), `docs/INDEX.md` (append), `docs/context/plataforma-dados/gates/T-06.1-builder.md` (novo)

Handoff lido inteiro: `docs/context/plataforma-dados/handoff/T-06.1.md`.
Builder e QA lidos inteiros (na worktree, não estão espelhados no checkout principal):
`docs/context/plataforma-dados/gates/T-06.1-builder.md`, `.../T-06.1-qa.md`. QA = APPROVED,
6 mutações reais confirmadas (pega/reverte/limpo), com registro no ledger pendente por anomalia
NÃO relacionada da fase 08 (QA corretamente não rodou `pipeline override`).

## Camada 1 — o que o runner mede (rodado nesta auditoria, não só citado do QA)

- `harness rules --mode file --path backend/src/modules/sentimento/domain/series_catalog.py` → `rc=0`
- `harness rules --mode file --path backend/tests/sentimento/test_series_catalog.py` → `rc=0`
- `harness rules --mode sweep --changed-only` → `rc=0`, 0 achados
- `bash backend/scripts/boundaries.sh` → 3 kept, 0 broken (155 arquivos, 703 dependências),
  inclui `Camadas por contexto: infra > use_cases > domain KEPT`,
  `Fronteira de contexto: sentimento nao importa outro contexto KEPT`,
  `Natureza: domain e use_cases nao falam com socket nem ssl KEPT`
- `bash backend/scripts/natureza.sh` → universo de 70 arquivos (51 domain + 19 use_cases),
  0 leituras de relógio
- `bash backend/scripts/test.sh tests/sentimento/test_series_catalog.py --no-cov -q` (rodado
  isolado nesta auditoria, dentro da worktree, venv `.venv/bin/python`) → **27 passed**, 0 failed
  (confirma independentemente o número que o QA já havia coletado — 18 `def test_*` expandidos
  por `@pytest.mark.parametrize` em 27 itens; discrepância do builder, "26 testes", é rótulo, não
  cobertura, já anotada pelo QA como não-bloqueante)
- Denominador de regras bloqueantes: `harness rules list --severity block` = **7 regras** em
  vigor (`core.relative-import`, `core.silent-except`, `core.print-statement`,
  `core.hardcoded-secret`, `web-fullstack.browser-imports-server`,
  `web-fullstack.tenant-from-request`, `web-fullstack.server-test-directory-present`). **7 de 7
  avaliadas** — nenhuma viola: import absoluto (`series_catalog.py:40`), zero `except` no
  arquivo de produção, zero `print`, zero credencial, e as três regras `web-fullstack` não se
  aplicam a este diff (nenhum código de navegador/servidor web tocado).

Nenhum achado de camada 1.

## Camada 2 — arquitetura declarada (julgamento, citação obrigatória)

1. **Contrato `Natureza` (`ADR-016`)** — `series_catalog.py` inteiro em
   `backend/src/modules/sentimento/domain/`, imports apenas de `collections.abc`,
   `dataclasses`, `decimal` e `series_key` (mesmo pacote `domain`) — sem I/O de disco/rede.
   Confirmado por `boundaries.sh` (regra `Natureza: domain e use_cases nao falam com socket nem
   ssl` KEPT) e `natureza.sh` (0 leituras de relógio no universo de 70 arquivos que inclui este
   módulo). **Sem violação.**

2. **Reuso real de `SeriesKey`** — `SeriesCatalogEntry.key: SeriesKey` (`series_catalog.py:115`)
   é o único lugar que carrega os 15 termos; `unit`/`denom`/`label_shift`/`verified_by` NÃO são
   redeclarados no catálogo (comentário explícito `series_catalog.py:21-26` nomeando o risco de
   segunda representação e por que foi evitado). A obrigatoriedade de `unit`/`denom`/
   `verified_by` não-brancos é delegada a `SeriesKey.__post_init__` (`series_key.py:178-186`),
   exercida pelo teste `test_catalog_row_refuses_a_blank_identity_term_via_the_key` — que
   importa `IncompleteSeriesKeyError` de `series_key.py`, não duplica a checagem. **Nenhuma
   segunda representação paralela dos 15 termos.**

3. **Escopo não invadido** (verificado de forma independente do QA):
   `grep -niE "quarantine|available_at|lag_ms|\bMODELED\b"` sobre os dois arquivos novos só
   acha 2 linhas de COMENTÁRIO em `series_catalog.py:29-30` apontando `T-06.6`/
   `quarantine_terms.py` como fora de escopo — nenhuma implementação do predicado de 3 termos.
   `find backend/src backend/tests -iname "*catalog*"` só retorna os 2 arquivos do próprio diff
   (produção + teste) — nenhum `infra/*_series_catalog_store.py`, nenhum arquivo de dados de
   produção para `T-06.2`/`T-06.3`/`T-06.4`/`T-06.9`. As fixtures do teste (`binance_oi_key`,
   entrada `cvd_source`-like) são valores de exemplo locais ao arquivo de teste, não uma tabela
   de séries reais publicada pelo módulo de produção. **Escopo respeitado — bate com o handoff
   linhas 27-28 e com o achado do QA.**

4. **`native_grid` resolvido em runtime, não amarrado a constante (`CA-F2-11`)** —
   `native_grid: str` é campo de instância de `SeriesCatalogEntry` (`series_catalog.py:116`),
   não `Literal[...]` nem constante de módulo; `PRICE_USES` (a única `frozenset` do módulo) não
   toca `native_grid`. O teste
   `test_native_grid_is_a_per_row_field_not_a_shared_constant` (`test_series_catalog.py:127-142`)
   constrói duas linhas — `"1min"` (Coinalyze) e `"5min"` (Binance) — na mesma execução sem
   nenhum mecanismo forçar concordância entre elas, que é o falsificador correto para "não é
   constante". **Sem violação de `CA-F2-11`.**

5. **`label_shift.verified_by` é referência/identificador de teste, não booleano** —
   `verified_by: str` (`series_key.py:176`), fixture usa uma string literal de caminho de teste
   (`"test_series_catalog.py::test_a_complete_entry_builds"`, `test_series_catalog.py:58`), e
   `test_catalog_row_refuses_a_key_with_no_label_shift_witness` prova que uma string vazia é
   recusada — não existe em lugar nenhum do diff um campo `bool` para esta finalidade.
   **Conforme o DoD e `SPEC-001` §3.3.**

6. **Idioma inglês** — leitura direta de `series_catalog.py` (docstrings, comentários, todas as
   mensagens de `raise ...Error(...)`) e de `test_series_catalog.py` (nomes de teste, docstrings,
   comentários): 100% inglês, nenhuma string em português encontrada. Aplica-se também a
   `CLAUDE.md` §"Mensagem de exceção" (linha 1 da tabela de fronteira, estendida por decisão de
   2026-09-02) — todas as 6 mensagens de exceção novas (`InvalidPriceUseError`,
   `InvalidCatalogEntryError` ×4 variações, `DuplicateSeriesKeyError`) nascem em inglês.
   **Sem violação.**

7. **`docs/INDEX.md` / `backend/README.md` append-only** — `git diff master...HEAD` mostra, em
   ambos os arquivos, exclusivamente linhas adicionadas ao final da seção/tabela existente
   (`+` sem `-` correspondente no hunk); nenhuma linha anterior foi reescrita ou removida.
   **Conforme.**

8. **Alinhamento com `SPEC-001` §3.3 (transcrição literal lida em
   `docs/specs/SPEC-001-plataforma-dados.md:225-231`)**: "uma linha por `SeriesKey`", os campos
   obrigatórios listados, e a recusa de publicação de reconstrução sem `(mediana, p99, n)`
   batem, campo a campo, com `SeriesCatalogEntry`/`PublishedError` e os testes que os exercitam
   (`test_reconstruction_without_published_error_is_refused` e seu simétrico). **Sem desvio.**

## Denominador

- Regras bloqueantes existentes: **7** (`harness rules list --severity block`). Avaliadas: **7**.
  Violadas: **0**.
- Arquivos no diff auditados: **5** (`git diff --name-status master...HEAD`) — 2 de código
  (produção+teste, ambos lidos por inteiro), 3 de documentação (README/INDEX/gate do builder,
  diffs inspecionados por inteiro).
- Comandos que produziram os achados: listados linha a linha na Camada 1 acima; todos rodados
  nesta sessão dentro da worktree `/tmp/claude-1002/wt/T-06.1`, não apenas citados do relatório
  do QA.

## Veredito

**COMPLIANT** — nenhuma regra bloqueante violada, nenhuma violação de arquitetura declarada
encontrada nos 7 pontos verificados (Natureza/domain, reuso de `SeriesKey`, escopo não
invadido, `native_grid` em runtime, `verified_by` como referência, idioma inglês,
append-only). A task-raiz da fase 06 está pronta para destravar `T-06.2`..`T-06.10` sem dívida
arquitetural herdada.
