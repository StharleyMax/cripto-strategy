[QA GATE — Fase 09: Consolidação de fronteira]
Feature: plataforma-dados     Componente: docs
Arquivos alterados:
- docs/adr/README.md (new)
- docs/context/plataforma-dados/tasks.toml (modified)
- docs/INDEX.md (modified)

Spec: docs/specs/SPEC-001-plataforma-dados.md
Plan: docs/plans/SPEC-001-plataforma-dados/09_consolidacao_de_fronteira.md

DoD:
- [x] D9.1 (parte a — zero referência órfã): `comm -23 <(grep -rohE 'ADR-[0-9]{3}' docs/plans docs/context/*/tasks.toml docs/specs 2>/dev/null | sort -u) <(ls docs/adr/ADR-*.md | sed -E 's#.*/(ADR-[0-9]{3}).*#\1#' | sort -u)` → vazio.
- [x] D9.1 (parte b — toda ADR de `plataforma-dados` referenciada pela fase/task que a usa): `comm -13 <(grep -rohE 'ADR-[0-9]{3}' docs/plans/SPEC-001-plataforma-dados/*.md docs/context/plataforma-dados/tasks.toml docs/specs/*.md 2>/dev/null | sort -u) <(ls docs/adr/ADR-*.md | sed -E 's#.*/(ADR-[0-9]{3}).*#\1#' | sort -u) | grep -vxE 'ADR-013|ADR-015'` → vazio (após a correção; antes devolvia 9 linhas: ADR-018,019,020,021,022,023,024,025,026).
- [x] D9.2 (todo ADR com falsificador `≥1`): `grep -c "^#\{2,3\} .*[Ff]alsificador" docs/adr/ADR-*.md | awk -F: '$2==0'` → vazio (26/26 com ≥1).
- [x] Contagem real registrada, não o "9" obsoleto do plano nem o "10" de 2026-08-25: `ls docs/adr/ADR-*.md | wc -l` → 26.

Comandos rodados (literais) e resultado:
- `ls docs/adr/ADR-*.md | wc -l` → `26` (universo: docs/adr/; padrão `ADR-*.md`, não `*.md`, para não casar com o próprio `docs/adr/README.md`, que também vive em `docs/adr/`)
- `grep -c "^## Falsificador" docs/adr/*.md` → 26 linhas, contagens 1 ou 2 cada (comando literal do plano, SUBCONTA por variantes de heading — não usado como veredito final)
- `grep -c "^#\{2,3\} .*[Ff]alsificador" docs/adr/ADR-*.md | awk -F: '$2==0 {print}'` → saída vazia (universo: 26 arquivos; comando corrigido, veredito de D9.2)
- `comm -23 <(grep -rohE 'ADR-[0-9]{3}' docs/plans docs/context/*/tasks.toml docs/specs 2>/dev/null | sort -u) <(ls docs/adr/ADR-*.md | sed -E 's#.*/(ADR-[0-9]{3}).*#\1#' | sort -u)` → vazio (zero referência órfã, universo: 2 tasks.toml + todo docs/plans + todo docs/specs)
- `comm -13 <(grep -rohE 'ADR-[0-9]{3}' docs/plans/SPEC-001-plataforma-dados/*.md docs/context/plataforma-dados/tasks.toml docs/specs/*.md 2>/dev/null | sort -u) <(ls docs/adr/ADR-*.md | sed -E 's#.*/(ADR-[0-9]{3}).*#\1#' | sort -u) | grep -vxE 'ADR-013|ADR-015'` → vazio DEPOIS da correção (universo: 9 arquivos de plano da fase 01-09 + 1 tasks.toml + specs); ANTES da correção devolvia `ADR-018 ADR-019 ADR-020 ADR-021 ADR-022 ADR-023 ADR-024 ADR-025 ADR-026` (9 linhas) — o caso que a correção rejeita
- `python3 -c "import tomllib; tomllib.load(open('docs/context/plataforma-dados/tasks.toml','rb'))"` → sem exceção (TOML válido após as 9 edições)
- `harness rules --mode sweep --changed-only` → saída vazia, rc=0 (universo: docs/INDEX.md, docs/context/plataforma-dados/tasks.toml, docs/adr/README.md — 3 arquivos, todos docs)

Cobertura: não aplicável — componente `docs`, sem `test_cmd` de código para esta trilha (`harness policy --key test_cmd` só declara lint/test para `sentimento`). Alvo do plano é `D9.1`+`D9.2`, ambos medidos acima.

Doc delta:
- docs/adr/README.md: criado — não existia índice de ADR (`README`/`index` sob `docs/adr/`) antes desta task; verificado por busca antes de criar. Contém a tabela completa das 26 ADRs (número, título, status, feature, falsificador, referência) e os comandos de reconciliação.
- docs/context/plataforma-dados/tasks.toml: atualizado — 9 tasks (`T-05.11`, `T-05.14`, `T-08.4`, `T-08.6`, `T-08.7`, `T-08.8`, `T-08.12`, `T-08.13`, `T-08.14`) ganharam uma linha em `refs` citando a ADR que produziram, fechando a lacuna de D9.1(b). Nenhuma linha removida.
- docs/INDEX.md: atualizado (append) — 1 linha nova para esta task, no fim do arquivo.
- docs/plans/SPEC-001-plataforma-dados/09_consolidacao_de_fronteira.md: SEM MUDANÇA — motivo explícito: o "9 ADRs" de `D9.1` é DoD de plano aprovado; corrigi-lo aqui sem gate do owner é o defeito que `ADR-010 §5` evita. A contagem real vive em `docs/adr/README.md`, criado por esta task.
- ADR: nao necessario — esta task é reconciliação documental, não decisão de arquitetura nova.

Bloqueado (se houver): nenhum. `ADR-013` e `ADR-015` são numeradas, têm falsificador e são referenciadas — mas na trilha `codigo-em-ingles` (`docs/context/codigo-em-ingles/tasks.toml`, `SPEC-002`), declarado no próprio cabeçalho de cada uma (`ADR-013`: "SPEC: — a decisão é do repositório, não de SPEC-001"; `ADR-015`: "Feature: codigo-em-ingles"). Fora do escopo de `T-09.1`/`CST-83` (feature `plataforma-dados`) — não tocadas, dona é `SPEC-002`.
