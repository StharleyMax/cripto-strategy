# T-08.6 — QA: motor `distribution`/`scan`/`firing_rate` (`ADR-020`)

Escopo do gate: `backend/src/modules/charts/{domain,use_cases}/*.py` (9 arquivos + 3 `__init__.py`),
`backend/tests/charts/*.py` (9 arquivos), `backend/tests/helpers/charts_fixtures.py`,
`backend/pyproject.toml` (boundaries), `docs/INDEX.md` (linha nova), reportado em
`docs/context/plataforma-dados/gates/T-08.6-builder.md`. Uncommitted no worktree, não tocado —
só lido e exercitado.

## O que foi verificado, com o comando que produziu o número

1. **`D8.6` (bordas por quantil, não corte absoluto):** lido `histogram.py`/`histogram_recipe.py`
   linha a linha — bordas vêm de `percentile(remainder, q, interpolation)` sobre `recipe.quantiles`,
   zero literal de corte. `test_d8_6_falsifier_overflow_is_proportional_not_47_percent` roda contra
   `synthetic_taker_like_population` (n=2013, extreme=2055,3 plantado) →
   `overflowRight.count=21 (1,04%)`, `extreme=2055.3` — reproduzido por mim
   (`pytest tests/charts/test_histogram.py -k d8_6 -q` → 1 passed). Fiz também um teste MEU de
   cauda pesada bimodal (2 valores extremos ± um corpo estreito, `point_mass_min_share=1.0` para
   forçar o caminho de quantil): overflow escalou com a receita em todos os casos, nunca fixo.
2. **Overflow nas duas pontas, soma bate:** `HistogramResult.__post_init__` reprova por construção
   se `bins+overflow_left+overflow_right+point_masses != n_total` — exercitado diretamente em
   `test_histogram_result_refuses_when_parts_do_not_sum_to_n_total`. Rodei EU MESMO 3 casos de
   borda adicionais (bimodal com toda a massa nos extremos, população de 2 valores únicos,
   população de 3 pontos com quantis assimétricos) via `python3 -c` importando `compute_histogram`
   diretamente — todos batem `sum == n_total`, nas duas pontas.
3. **Receita versionada, não bordas congeladas:** `HistogramRecipe` (`spec_version`, `quantiles`,
   `interpolation`, `point_mass_min_share`) é o único estado persistente do bundle — confirmado
   lendo `histogram_recipe.py:54-99`; nenhuma borda numérica é armazenada em lugar nenhum do
   domínio, `derive_edges`/`_quantile_edges` recalcula a cada chamada.
4. **`make boundaries` + plantio de import proibido — refeito por mim, não só relatado:**
   `import socket` plantado em `field_identity.py` → `make boundaries` → contrato "Natureza"
   **BROKEN**, nomeando arquivo+linha e a cadeia transitiva via `compute_distribution.py`.
   Revertido (`diff` contra backup, zero bytes de diferença) → `make boundaries` →
   **5 kept, 0 broken, 824 dependências** — bate exatamente com o relatório do builder.
5. **`bash backend/scripts/lint.sh`** (rodado por mim): `ruff check` all checks passed,
   `ruff format --check` 288 arquivos, `mypy --strict` sucesso em 288 arquivos.
6. **`bash backend/scripts/test.sh`** (rodado por mim, suíte inteira): **1509 passed**, 3 warnings,
   263s. `check-coverage-layers.sh`: domain 99,8%, use_cases 100,0%, infra 95,1% — 3/3 metas.
7. **`pytest tests/charts --cov=src/modules/charts --cov-report=term-missing`** (comando literal
   do relatório do builder, rodado por mim): **100% linha+branch, 338 stmts/98 branches, 0 miss**
   — bate com o relatório.
8. **`harness rules --mode sweep --changed-only`**: minha primeira tentativa devolveu vazio
   porque arquivos novos são `??` (untracked) e `git diff HEAD` não os enxerga sem
   `git add -N`. Com `git add -N` (index resetado depois, `git reset`, nada do worktree tocado)
   → **9 `[AVISO]` `core.module-docstring-single-line`, rc=0, zero `[BLOQUEIO]`** — bate
   exatamente com o relatório do builder. Sem regra bloqueante violada.

## Achado — NEEDS_FIX: contagem de testes relatada não reproduz

`docs/context/plataforma-dados/gates/T-08.6-builder.md` e a linha nova de `docs/INDEX.md` afirmam,
duas vezes, **"65 passed"** para `pytest tests/charts --cov=src/modules/charts --cov-report=term-missing`.
Rodei o MESMO comando literal:

```
cd backend && .venv/bin/python -m pytest tests/charts --cov=src/modules/charts --cov-report=term-missing
-> 72 passed in 0.24s
```

Confirmado por `--collect-only -q` por arquivo (soma = 72) e por contagem de `def test_` no fonte
(62 funções, 72 casos após expansão de `@pytest.mark.parametrize` em `test_field_identity.py`,
`test_histogram_recipe.py`, `test_firing_rate.py`, `test_scan.py`). **65 não bate com nenhuma das
duas convenções de contagem** (62 funções-fonte OU 72 casos coletados) — não é um erro de
"contei função em vez de caso", é um número que não corresponde a nada mensurável no estado atual
do código. Cobertura (100% linha+branch), `make boundaries`, lint e a suíte inteira (1509) **todos
reproduziram exatamente** o que o relatório alega — só a contagem de `tests/charts` diverge.

Isto é exatamente o defeito que este repositório já mediu duas vezes (`CLAUDE.md`, "Nenhum número
sem o comando que o produziu"): um número quantitativo, com comando citado, que não reproduz. Não é
defeito funcional — todos os 72 testes passam, a cobertura bate, os falsificadores de `D8.1`/`D8.6`
provam o que alegam provar — mas é uma alegação falsa em um relatório de gate e numa linha já
escrita (ainda não commitada) no ledger `append-only`.

## QA Gate — Fase 08 [charts]
- [OK] core.relative-import — `grep` vazio em `backend/src/modules/charts`, `backend/tests/charts`
- [OK] core.silent-except — `grep` vazio (nenhum `except:` nu, nenhum `except ... :` sem uso)
- [OK] core.print-statement — `grep` vazio
- [OK] core.hardcoded-secret — nenhuma credencial nos arquivos novos (revisão manual, módulos são
  puro domínio/use_cases sem I/O)
- [N/A] web-fullstack.* — escopo é backend Python, regras de `web-fullstack` não se aplicam aos
  arquivos tocados
- [OK] Testes existem e passam — `bash backend/scripts/test.sh` (1509 testes, suíte inteira);
  `pytest tests/charts --cov=src/modules/charts --cov-report=term-missing` (72 testes — ver achado
  acima sobre o número relatado, não sobre o resultado)
- [OK] Cobertura 100,00% linha+branch (`charts`) contra alvo declarado (`ADR-009/D1`: domain 90%/
  use_cases 80%/infra 70%) — `charts` não tem `infra/` ainda (fora de escopo, `ADR-020/D7`)
- [OK] DoD `D8.1` — `scan{Absolute 5.0}` acha 0, `distribution.max=2,4017`, dois caminhos
  concordam (`test_d8_1_falsifier_...`, reproduzido)
- [OK] DoD `D8.6` — overflow proporcional a 1% (21/2013), não 47,2%, `extreme=2055,3` batendo
  (`test_d8_6_falsifier_...`, reproduzido + 3 casos de borda meus)
- [OK] DoD `D8.7` — massa pontual extraída antes dos quantis, `0,76` de share em 1140/1500,
  nunca contada duas vezes (`test_point_mass_is_extracted_before_quantiles_...`, reproduzido)
- [FAIL] Número relatado — "65 passed" não reproduz; medido "72 passed" pelo mesmo comando citado
- [anomalia] nenhuma — todas as checagens produziram veredito

Regras bloqueantes avaliadas: 8 de 8 listadas por `harness rules list --severity block`
Veredito: NEEDS_FIX
Ações:
1. Corrigir "65 passed"/"65 testes charts novos" para o número real (72 casos coletados, 62
   funções-fonte — escolher e declarar UMA convenção) em
   `docs/context/plataforma-dados/gates/T-08.6-builder.md` e na linha ainda não commitada de
   `docs/INDEX.md`, com o comando que o reproduz.
2. Re-rodar `pytest tests/charts --cov=src/modules/charts --cov-report=term-missing` depois da
   correção e colar o número real no relatório atualizado.
