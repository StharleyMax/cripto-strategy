# Review Gate — T-01.8 (fase 01, componente docs) — CST-100

Checkout: `/home/stharley/Documentos/projects/cripto-strategy`, branch
`tasks/T-01.8-juizes-e-deploy`, HEAD no momento da revisão em `008be7b` (2 commits à frente de
`T-01.8`: `T-01.9`/`T-01.10`, fora de escopo desta task). Revisão feita sobre o delta do commit
`53801a9` (`git show --stat 53801a9`: 7 arquivos — `.claude/agents/{infra-architect,
frontend-architect,frontend-builder,frontend-qa}.md` novos, `harness.toml`,
`docs/gate-de-design.md`, `docs/INDEX.md`), reconfirmado no estado atual da árvore
(`harness policy --key agents.by_component`, `harness validate --strict`).

## Camada 1 — o que o runner mede

`harness rules list --severity block` → **8 regras bloqueantes em vigor** (`core.relative-import`,
`core.silent-except`, `core.print-statement`, `core.hardcoded-secret`,
`web-fullstack.browser-imports-server`, `web-fullstack.tenant-from-request`,
`web-fullstack.server-test-directory-present`, `own.compose-hardcoded-secret`). Avaliadas as **8
de 8** contra os 7 arquivos do delta via `harness rules --mode file --path <arquivo>`, um por um:
saída vazia nos 7 (`.claude/agents/*.md` ×4, `harness.toml`, `docs/gate-de-design.md`,
`docs/INDEX.md`). Nenhuma tem escopo sobre este delta — as 8 regras são `escopo code`/
`production`/`compose` (`backend/src`, `frontend/src`, `docker-compose`), e a task é `[docs]`,
confirmado por `harness code-paths classify .claude/agents/infra-architect.md` → `nao-producao`.
**0 de 8 disparadas — nada a citar por id de regra.**

## Camada 2 — o que só a arquitetura declarada diz (as 3 confirmações pedidas)

**(1) O `[DECISÃO-OWNER: 2026-09-03]` está citado corretamente.**
`harness.toml:687-691` e `frontend-architect.md:16` apontam para
`docs/decisoes-do-owner.md §"A6 — web ganha arquiteto de front próprio; o ui-designer volta a
julgar só design"` — o cabeçalho existe **literalmente** em `docs/decisoes-do-owner.md:677` e o
conteúdo bate: "Escolhido: criar o arquiteto de front e portar do anything a dupla builder/QA de
front… O ui-designer mantém o design_gate e deixa de ser architect de web." O juiz de `infra` cita
`docs/decisoes-do-owner.md §"como o vocabulário vira 7, e quem julga infra"` (`harness.toml:719`,
`infra-architect.md:9-10` → `ADR-009 §D6.5/§D6.8`, `T-09.4`/`CST-86`), cabeçalho confirmado em
`docs/decisoes-do-owner.md:866`. Ambas as citações resolvem para o texto que alegam citar — **sem
divergência**.

**(2) `infra-architect.md` e `frontend-architect.md` não reivindicam julgamento sobre `charts` nem
sobre interação/UX de `web`.**
- `frontend-architect.md` frontmatter (linha 3): "NÃO julga interação (isso é o `design_gate`) nem
  fidelidade do dado." Seção "O que você NÃO julga" (linhas 77-85) nomeia explicitamente
  "Fidelidade do dado em `charts`… é do `quant-architect`" e "Interação, acessibilidade, carga
  cognitiva, motion, microcopy — é o `design_gate` (`ux-ui-mastery`), e ele é bloqueante."
- `infra-architect.md` seção "O que você NÃO julga" (linhas 81-89): "Domínio quantitativo… é do
  `quant-architect`" e "Superfície e transporte do lado do browser — é do `frontend-architect`;
  interação é do `design_gate`."
- `harness.toml` confirma que a seção `[agents.by_component.charts]` (linha 120, fora do diff de
  `53801a9`) permanece `architect = ".claude/agents/quant-architect.md"`, intocada.
- `docs/gate-de-design.md:30` (diff do commit) mantém a tabela com `charts → quant-architect /
  ux-ui-mastery` sem alteração, e reescreve só a linha `web` para
  `frontend-architect / ux-ui-mastery`, preservando o `design_gate`.
Nenhuma reivindicação cruzada encontrada.

**(3) `harness.toml [agents.by_component]` tem exatamente os papéis que `A6` pede, sem papel
extra.**
`harness policy --key agents.by_component` (estado atual):
`web = {architect: frontend-architect.md, builder: frontend-builder.md, qa: frontend-qa.md,
design_gate: gate-de-design.md}`; `infra = {architect: infra-architect.md}`. Isto bate,
papel-a-papel, com o texto de `A6` ("criar o arquiteto de front e portar… a dupla builder/QA…
`ui-designer` mantém o `design_gate`") e com o texto de "quem julga `infra`" ("juiz de `infra` é
um agent NOVO: `infra-architect`" — só `architect`, nenhum `builder`/`qa` de infra proposto ou
decidido). `charts`, `sentimento`, `convergencia`, `backtest` permanecem só com `architect =
quant-architect.md`, fora do diff. **Nenhum papel extra não autorizado** (não há
`infra.builder`, `infra.qa`, nem papel algum em `docs`). Reprodução independente do comparador
dourado: `bash scripts/check-agents-by-component.sh` → `agents.by_component: bate com o dourado —
6 componente(s), 10 papel(is)`, `rc=0`.

## Verificação adicional (suporte às 3 confirmações, não achado novo)

- `harness validate --strict` → `politica valida: cripto-strategy (schema_version=1)`, rc=0.
- `ADR-009 §D6.5` (`grep -n F-D6-6 docs/adr/ADR-009-reuso-da-forma-do-anything.md:220`): o
  falsificador exigia "`infra` presente COM `architect`" — confirmado presente; `F-D6-6` fecha.
- `docs/INDEX.md`: linha nova acrescentada (`git show 53801a9 -- docs/INDEX.md`), nenhuma linha
  existente reescrita — respeita "registro de artefatos é append-only".
- `frontend-builder.md`/`frontend-qa.md` (porte) delegam decisão de UI/UX ao `ui-designer` +
  `design_gate` explicitamente (`frontend-builder.md:44-58`, item 4: "Decisão de UI/UX não é sua.
  Quem decide é o `ui-designer`… Em dúvida sobre aparência ou interação, pergunte, não decida.") —
  consistente com a mesma fronteira.

## Veredito

**COMPLIANT**

Denominador: **8 regras bloqueantes em vigor**, **8 de 8 avaliadas** por
`harness rules --mode file --path <arquivo>` sobre os **7 arquivos** do delta de `53801a9`
(4 agents novos + `harness.toml` + `docs/gate-de-design.md` + `docs/INDEX.md`) — 0 disparadas. As
3 confirmações da camada arquitetural pedidas pelo despacho foram verificadas independentemente
por leitura direta dos arquivos citados (não por repetição do texto do commit ou do gate de QA) e
todas resolvem sem divergência. Nenhum achado com id de regra ou citação de documento sobrou fora
do veredito — nada a reportar como BLOCKER/WARNING/INFO.

Gate do QA reconfirmado, não substituído: `docs/context/plataforma-dados/gates/T-01.8-qa.md`
(APPROVED).
