# T-08.14 — Review arquitetural (`/review`)

**Veredito: COMPLIANT**

**Denominador:** 8 regras bloqueantes em vigor (`harness rules list --severity block`), avaliadas
todas contra os arquivos alterados neste worktree (9 modificados + 3 novos, `git status`).
Comandos rodados: `harness rules --mode sweep --changed-only` (rc=0, 0 bloqueio, 3 `[AVISO]`
`core.module-docstring-single-line` — não-bloqueante, pré-existente aos 3 arquivos de produção
tocados) e `make boundaries` (7 contratos, 0 broken, incluindo os dois que tocam `backtest`).

## Checklist do despacho

1. **`grid_version` estende `run_registry` corretamente** — sim, nas 4 camadas: `domain`
   (`run_registry_entry.py:68` campo `int` + validação `>=0` em `__post_init__`), DDL
   (`postgres_run_registry_store.py:40` `INTEGER NOT NULL CHECK (grid_version >= 0)`), os dois
   `SELECT` (`_SELECT_BY_TRIPLE_SQL`/`_SELECT_BY_RUN_ID_SQL`) e o `INSERT` (coluna+placeholder+
   valor, contagem de `%s` bate com a lista de colunas). Prova a nível de banco:
   `test_the_grid_version_check_constraint_bites_at_the_database` insere `-1` via SQL cru e
   espera `psycopg.errors.CheckViolation`.
2. **`GridVersionDivergenceError` (G6) é distinta de G1** — sim, classe própria em
   `record_run.py:46`, nunca reusa `RunRegistryDivergenceError`.
3. **Precedência G1 > G6 quando ambos divergem** — código (`record_run.py:107-124`) checa
   `partitions_content_hash` antes de `grid_version`; provado por
   `test_g1_takes_precedence_when_both_hash_and_grid_version_diverge`. Documentada em
   `ADR-025` H4 e no docstring do módulo (linhas 17-23). QA (`gates/T-08.14-qa.md:18,56`)
   reproduziu de forma independente (probe3).
4. **Emenda a `ADR-021` é append puro** — `git diff --numstat` confirma `12  0` (12 inserções, 0
   deleções); a emenda vive numa seção nova ao final do arquivo, nenhuma linha D1-D5/G1-G5
   anterior tocada.

## O que não entrou no veredito

Nenhum achado de camada "só arquitetura declarada" a reportar — não há violação de limite de
camada, direção de dependência ou responsabilidade de módulo que eu consiga citar contra
`docs/adr/ADR-021`/`ADR-025` ou `docs/context/plataforma-dados/tasks.toml:1355-1363`. O
`frontend/src/charts/canonical-grid.ts` (`CANONICAL_GRID_VERSION` explícito, "Consequência para
quem implementar" da ADR) segue não implementado — declarado como fora de escopo pelo builder,
não escondido, e QA já confirmou a obrigação como prospectiva (D5), sem alegação falsa de que
existe.

Testes: `.venv/bin/python -m pytest tests/backtest -q` → 29 passed (rodado nesta review, subset
`test_record_run.py`+`test_run_registry_entry.py`+`test_postgres_row_mapping.py`+
`test_postgres_run_registry_store.py`).
