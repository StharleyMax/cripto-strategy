# Builder — T-06.10 · S3 — inspetor de série

**Card:** `CST-54` · **Componente:** `web` · **Plano:** `06`, item 6.16, `D6.15` · **Data:** 2026-09-03
**Design:** [`gates/T-06.10-design.md`](T-06.10-design.md) — gate PENDENTE (§6 daquele arquivo), tokens reaproveitados de `S1Console.tsx` (já gated).

## 0. Sequência seguida

1. Li o handoff inteiro (`docs/context/plataforma-dados/handoff/T-06.10.md`) e confirmei que
   `docs/context/plataforma-dados/gates/T-06.10-design.md` **não existia** antes desta sessão
   (`ls` do diretório de gates).
2. Tentei `mcp__stitch__generate_screen_from_text` **duas vezes** (§1 do design gate) — ambas
   `The operation timed out.`, confirmado sem efeito por `list_screens` (7 telas antes, 7 telas
   depois, nenhuma `S3`). Documentado, não escondido, com falsificador para retentativa futura.
3. Escrevi a decisão de design ANTES do código (`T-06.10-design.md`), com o `[NÃO SEI]` explícito
   de que o gate `ux-ui-mastery` não rodou nesta sessão (sem acesso a invocar aquele subagente
   neste ambiente de execução) — R9 exige o gate, não apenas a decisão.
4. Implementei, testei, fiz lint. `backend/` **intocado** (nenhum arquivo sob `backend/` no diff).

## 1. O que foi construído

`frontend/src/features/s3-inspector/` (novo), mesma fronteira que `s1-console` (`T-07.12`)
estabeleceu: **sem app Next.js real ainda** (`src/app/routes.ts` — scaffold é escopo de outra
task), então este é um módulo de domínio/fórmula + um componente presentacional lint-only, prova
por teste unitário sobre a lógica, não por app rodando.

| arquivo | conteúdo |
|---|---|
| `series-catalog.ts` | Porte fiel de `series_key.py::SeriesKey`/`series_catalog.py::SeriesCatalogEntry` — os 15 termos, `PRICE_USES`, `PublishedError`, `assertValidSeriesKey`/`assertValidCatalogEntry`, `buildSeriesLabel` (rótulo completo, §9 item 10). `SERIES_KEY_TERMS` é uma transcrição independente comparada por teste contra a ordem de `series_key.py:38-54`. |
| `quarantine.ts` | Porte fiel de `quarantine_terms.py::QuarantineTerms` — `isQuarantined`, `openTerms` (mesma ordem do predicado), `COINALYZE_ONE_SHOT_TERMS` (reusa o exemplo REAL de `SPEC-001` §5.2, não um caso inventado). |
| `domain.ts` | `CatalogRow`, `Completeness` (grid/tick, §9 item 10), `RawDataRow`+`GapMarkerRow` (union discriminado — `InspectorRow`), `mergeRawAndGapRows` (D6.15: intercala cronologicamente, nunca uma tabela separada), `DivergenceRow`/`DivergenceReading` (o falsificador estrutural de "não reconcilia" — o tipo não tem campo para "o valor escolhido"), `filterCatalogRows`, `buildQuarantineDrawer` (`isEmpty` explícito). |
| `view-model.ts` | Formatação determinística (`formatEventTimeIso`, sem `Intl`/locale — `Q14`), `completenessText` (grid vs tick, singular/plural), `quarantineBadgeText`, `buildS3ViewModel` — o objeto único que o componente consome. |
| `fixtures.ts` | 2 linhas de catálogo (Binance OI 5m resolvida; Coinalyze OI 1m CLOSE em quarentena — `COINALYZE_ONE_SHOT_TERMS`, o caso real de `SPEC-001` §5.2), 3 linhas cruas + 1 lacuna reproduzindo o exemplo PUBLICADO de `D4.2` (`285 linhas · 1 linha em ingest_gap com n_missing=3 · 1 vão de 20 min`), 1 divergência (valores sintéticos, citando `D6.8` só como justificativa do formato, não como fonte dos números — declarado no comentário). |
| `S3Inspector.tsx` | Presentacional, mesmo tier que `S1Console.tsx` (lint-only, sem `tsconfig.json`/renderer no pacote). Duas camadas + gaveta de quarentena (losango + palavra + `#e0aaff`, nunca preenchimento, nunca vermelho); linha de lacuna em tinta neutra; ação "abrir" é texto sublinhado, nunca botão colorido. |
| `series-catalog.test.ts`, `quarantine.test.ts`, `domain.test.ts`, `view-model.test.ts` | 37 testes. |

`frontend/package.json`: `+"test:s3": "node --test 'src/features/s3-inspector/*.test.ts'"`.

## 2. Por que `IngestHealthGapRow` foi REUSADO, não reinventado

`domain.ts`/`fixtures.ts` importam `IngestHealthGapRow` de
`../s1-console/ingest-health-query.ts` em vez de definir um segundo tipo de 8 colunas para
`md.ingest_gap`. É a mesma tabela (`source, symbol, series_key_id, from_ts, to_ts, n_missing,
class, detected_at`, `SPEC-001` §3.5) — duas cópias do mesmo contrato divergiriam exatamente como
`ADR-008/D3` adverte para `ingest_health_query`. Handoff, literal: "reuse... porte, não invente
uma forma diferente".

## 3. Testes e lint

```
npm --prefix frontend run test:s3   ->  37 pass, 0 fail
npm --prefix frontend run lint      ->  rc=0 (após `npm install --offline`; node_modules não
                                          existia nesta worktree)
node --test 'src/**/*.test.ts'      ->  202 tests, 192 pass, 10 fail — os 10 são PRÉ-EXISTENTES,
                                          em src/app/universe-at.test.ts,
                                          src/charts/canonical-grid-sha256-proof.test.ts e
                                          src/features/s1-console/ingest-health-query.test.ts,
                                          todos dependentes de `backend/.venv` (não bootstrapado
                                          nesta worktree — `ls backend/.venv/bin/python3` falha)
                                          ou de dado (`data/`) não versionado. Nenhuma falha
                                          toca `s3-inspector/`.
```

`backend/` intocado: `git diff --stat -- backend/` vazio.

## 4. O que este relatório NÃO reivindica

- **Não reivindico** que a composição visual de S3 passou por `/design-critique` ou por uma
  geração Stitch bem-sucedida — `T-06.10-design.md` §6 registra isso como gate PENDENTE.
- **Não reivindico** paridade byte-a-byte com um dado real de `series_catalog`/`ingest_gap` — não
  há store/CLI real para esta task consumir ainda (T-06.1 é só o contrato; T-06.2-06.9, em
  paralelo, populam linhas reais). `fixtures.ts` é sintético, com proveniência de cada valor
  citada por comentário, mesma convenção de `s1-console/fixtures.ts`.
- **Não escrevi** em `docs/product/STITCH_CONTEXT.md` §4 (não pedido nesta task, e R6 exige
  aprovação antes daquela escrita).
