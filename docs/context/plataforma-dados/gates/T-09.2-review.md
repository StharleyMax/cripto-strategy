# T-09.2 — Review Gate (arquitetural, read-only)

**Task:** `[docs] 09 · env em {mainnet, testnet, demo, replay} em toda linha de ordem/fill, com
chip no chrome` (`CST-84`). **Componente:** `docs`. **Fase:** `09` — item 9.3, `D9.3`.
**Escopo dado:** delta = `scripts/validate_order_fill_env.py` (novo) + `docs/INDEX.md` (append).

## Bootstrap

```
harness rules list --severity block   -> 7 regras em vigor (core.relative-import,
  core.silent-except, core.print-statement, core.hardcoded-secret,
  web-fullstack.browser-imports-server, web-fullstack.tenant-from-request,
  web-fullstack.server-test-directory-present)
harness policy --key docs.bootstrap_docs -> ["docs/proposta-discovery.md"]
harness policy --key glossary_doc        -> vazio (dívida conhecida, ADR-013/D4; não bloqueia esta task)
harness policy --key code_paths          -> include_prefixes: backend/src/, backend/tests/, frontend/src/
```

## Camada 1 — o que o runner mede

- `harness code-paths classify scripts/validate_order_fill_env.py` -> `nao-producao: nenhum
  include_prefixes casa ['backend/src/', 'backend/tests/', 'frontend/src/']`, rc=1. Confirmado
  contra positivo de controle: `harness code-paths classify backend/src/__init__.py` ->
  `producao: ... nada exclui`, rc=0. O script está corretamente fora de `backend/tests/` — ele
  não é "código de teste do backend", é falsificador de contrato hospedado em `scripts/`, mesmo
  padrão já aceito de `scripts/validate_palette.js` (`ADR-010`).
- `harness rules --mode file --path scripts/validate_order_fill_env.py --format ndjson` -> 0
  linhas, rc=0. `harness rules --mode file --path docs/INDEX.md --format ndjson` -> rc=0.
  `harness rules --mode sweep --changed-only --format ndjson` (universo: `docs/INDEX.md` +
  `scripts/validate_order_fill_env.py`, os 2 arquivos alterados no worktree) -> 0 achado, rc=0.
- **Denominador:** 7 regras bloqueantes em vigor; 7 avaliadas (via `list` + `sweep
  --changed-only` sobre o universo real do diff); 0 violação. 5 delas têm `scope: code`, que
  `code_paths` define — `scripts/` e `docs/` não casam `include_prefixes`, então ficam fora por
  construção do próprio mecanismo do harness, não por escape do revisor. `core.hardcoded-secret`
  é `scope: production`, mesmo argumento, e leitura integral do arquivo (127 linhas) não achou
  string de credencial/connection-string. `web-fullstack.server-test-directory-present` é
  `path-presence` sobre `backend/tests/**`, propriedade do repositório não afetada por este diff.
  O arquivo usa `print()` (linhas 108-117) — se estivesse sob `include_prefixes`,
  `core.print-statement` reprovaria; como não está, a regra corretamente não avalia.

## Camada 2 — arquitetura declarada (julgamento, citado)

- **Escopo da task batido contra o plano aprovado**: `docs/plans/SPEC-001-plataforma-dados/09_consolidacao_de_fronteira.md`
  linha 14 (item 9.3) e linha 27 (`D9.3`: "teste que rejeita linha de ordem/fill sem `env`" —
  `≥ 1 caso negativo`) — o script entrega 5 casos negativos (ausência da chave, `None`, string
  vazia, valor fora do conjunto, erro de digitação na chave) + 4 positivos. Acima do piso.
- **"Não faz" da fase 09** (mesmo arquivo, linha 32): "Não escreve código de produção... não
  decide política de coleta, não aprova a SPEC." Verificado por `git status --short` +
  `git diff --stat` no worktree: só `docs/INDEX.md` (1 linha, append) e 2 arquivos novos
  (`scripts/validate_order_fill_env.py`, gates). Nenhum arquivo sob `backend/src`,
  `backend/tests` ou `frontend/src` tocado. `tasks.toml` e `docs/adr/ADR-005-*.md` intocados
  (confirmado por `git status`, sem entrada para nenhum dos dois).
- **A decisão em si não foi alterada por esta task** — conferido por leitura direta (não por
  citação do builder/QA) dos quatro documentos que a fixam: `docs/recorte-plataforma.md`
  (tabela, linha "Entrada de ordem / execução", "fora... Consequência de plataforma que fica:
  `env ∈ {mainnet, testnet, demo, replay}`..."), `docs/plataforma-superficies-e-faseamento.md:68`
  ("Chrome global mostra sempre: ambiente (mainnet/testnet/demo/replay)..."),
  `docs/specs/PRD-001-plataforma-dados.md` §12 (Non-goals, linha 1012), e
  `docs/product/STITCH_CONTEXT.md:138` ("Chrome global, sempre visível: chip de `env ∈
  {mainnet, testnet, demo, replay}`..."). Nenhum desses 4 arquivos aparece em `git status`, logo
  a citação do builder está correta: registro pré-existente, não criado por esta task.
- **"Chip no chrome"** é parte do texto do item 9.3 mas não é o critério mensurável de `D9.3` —
  a tabela DoD da própria fase lista só o teste negativo como comando/universo verificável para
  `D9.3`. Não há tela nesta SPEC para hospedar o chip (fase é `docs`, não `web`/`charts`), e o
  requisito já está registrado em `STITCH_CONTEXT.md` para quando a tela existir. Isto é
  julgamento de camada 2, e a citação acima o sustenta — não entra como violação porque o
  próprio DoD aprovado não exige implementação do chip aqui.
- **Falsificador é falsificação real, não decoração**: QA registra 2 mutações próprias
  (guarda de ausência removida -> `KeyError` não capturado; corpo trocado por
  `linha.get("env")` -> `REPROVADO 5/9`, todos os 5 negativos capturando a regressão) — reproduzi
  o comando publicado (`python3 scripts/validate_order_fill_env.py` -> `APROVADO`, 9/9, exit 0)
  e não encontrei motivo para desconfiar da reprodução relatada; o padrão (guarda desligada
  quebra visivelmente) é consistente com o código lido linha a linha.

## Achados

Nenhum. 0 violação de regra bloqueante, 0 violação de arquitetura declarada citável.

**Não-bloqueante, já registrado pelo QA e aqui apenas confirmado, não repetido como achado
novo:** identificadores/docstring do script em português. `CLAUDE.md` §"Idioma de identificador"
é doutrinariamente **convenção, não portão** — sem `[[rules.own]]` de idioma — e o mesmo padrão
já é aceito em `scripts/validate_palette.js` (também `nao-producao`, também comentários em
português, citado por múltiplos documentos sem ressalva). Isto não é a superfície de
`raise X("...")` que a exceção de 2026-09-02 fechou (o arquivo não levanta mensagem em
português em `raise`/`Error`/`Exception` — as mensagens de `LinhaOrdemFillRejeitada` são
f-strings em português, o que é a mesma classe que a decisão de 2026-09-02 endereçou; note-se
para follow-up, mas fora do escopo desta review porque o arquivo é `nao-producao` e a fronteira
de idioma da tabela de 12 linhas não alcança `scripts/`).

## Denominador

- Regras bloqueantes existentes: **7** (`harness rules list --severity block`).
- Regras avaliadas sobre o universo real do diff: **7** (via `harness rules --mode sweep
  --changed-only`, universo 2 arquivos: `docs/INDEX.md`, `scripts/validate_order_fill_env.py`).
- Arquivos varridos: **3** (`git status --short`: `docs/INDEX.md` modificado + 2 novos —
  `scripts/validate_order_fill_env.py`, e os gates builder/qa não são código, são doc de
  processo).
- Documentos de arquitetura citados nesta review: plano `09` (item 9.3, DoD `D9.3`, seção "Não
  faz"), `docs/recorte-plataforma.md`, `docs/plataforma-superficies-e-faseamento.md:68`,
  `docs/specs/PRD-001-plataforma-dados.md` §12, `docs/product/STITCH_CONTEXT.md:138,1827`.

## Veredito

**COMPLIANT** — 0 de 7 regras bloqueantes violadas; nenhuma violação de arquitetura declarada
citável. `harness code-paths classify` confirma classificação não-produção; falsificador cobre
ausência/`None`/vazio/fora-do-conjunto/erro-de-digitação (5 negativos, piso do DoD é 1); nenhum
arquivo de produção tocado.
