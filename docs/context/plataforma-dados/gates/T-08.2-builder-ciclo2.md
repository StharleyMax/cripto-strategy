# T-08.2 (`CST-70`) — Builder, ciclo 2 · as três correções do `NEEDS_FIX`

**Feature:** `plataforma-dados` · **Fase:** `08` · **Componente:** `charts`
**PR** #41 · **base** `22541a9` · **branch** `tasks/T-08.2-spike-eixo-charts`
**Entrada:** [`T-08.2-qa.md`](T-08.2-qa.md) (`NEEDS_FIX`, `F-1`/`F-2`/`F-3`)

> **Nenhuma das três toca a conclusão da task.** `D8.19` continua PASSANDO e o achado do eixo
> ordinal continua de pé. O que mudou foi **o tamanho em que ele pode ser afirmado** e **quem
> cobra a guarda que o torna medível**. O relatório do ciclo 1 (`T-08.2-builder.md`) foi
> corrigido no lugar, com a versão anterior citada em cada correção — não reescrito por cima.

---

## `F-1` — a aritmética: `314/1.440` é `21,81%`, não `20,0%`

**Onde:** `T-08.2-builder.md` §1, célula de cobertura da linha **B**, e o novo parágrafo logo
abaixo da tabela.

| | antes | depois |
|---|---|---|
| célula | `20,0% (314/1.440)` | **`20,0% alvo` ⇒ `21,81% realizada` (314/1.440)** |

```bash
python3 -c "print(f'{314/1440*100:.2f}%')"    # 21.81%
```

`[MEDIDO 2026-08-29, n=1 divisão]`. **São duas grandezas, e a célula publicava uma com o
numerador da outra:** `20,0%` é o **parâmetro alvo** passado a `buildSparseGridWorkload` (o
número de `D8.11`); `21,81%` é a **cobertura realizada** pelo LCG com `seed = 20260829`. O erro
era de **1,81 pp para cima** — na direção que aumenta o achado, e é por isso que ele é defeito
e não arredondamento.

---

## `F-2` — o *"por quanto"* cai; a faixa `n=5` entra no lugar

**Onde:** §1 (célula do pior caso), §2 (a linha da média), §3 (a frase da ressalva) e a **§3.1
nova**, que é onde a faixa passa a morar.

A frase do §3 terminava em *"Ele afirma que **naquela cobertura** o eixo ordinal deixa de
concordar com `event_time`, **e por quanto**"* — extrapolação escrita na frase **seguinte** à
ressalva que existia para impedi-la. Agora lê: *"naquela cobertura alvo, **e em toda semente
testada** … **Ele NÃO afirma por quanto** — a magnitude é propriedade da semente, não da
cobertura"*.

**A medição, reproduzida byte a byte neste ciclo** (`n=5`, mesma cobertura alvo `0.2`, mesmo
caminho de código do runner — `buildSparseGridWorkload` -> `collectAxisCoordinates` ->
`measureAxisFidelity`; o corpo da sonda vai **inteiro** em §3.1 do relatório do ciclo 1, e não
como caminho em `/tmp`, porque número cujo comando mora fora do repositório não reproduz):

```
seed=20260829 candles=314 cobertura=21.81% pior=36.390px (72.8x) media=19.216px
seed=1        candles=294 cobertura=20.42% pior=27.333px (54.7x) media=7.664px
seed=42       candles=258 cobertura=17.92% pior=13.664px (27.3x) media=6.122px
seed=987654   candles=276 cobertura=19.17% pior=27.753px (55.5x) media=11.300px
seed=20260830 candles=286 cobertura=19.86% pior=27.254px (54.5x) media=12.088px
```

`[MEDIDO 2026-08-29, n=5 sementes × 602±36 itens, rc=0]` — idêntico ao que o `/qa` mediu.

**ROBUSTO:** **5 de 5 sementes ficam FORA da tolerância**, a menos ruim ainda a **27,3×**
(`13,664 px`). Nenhuma semente testada produziu eixo que concordasse com `event_time`.
**NÃO ROBUSTO:** a **magnitude** — faixa **`13,664 – 36,390 px`, razão `2,66×`**, e **a semente
publicada é o MÁXIMO das cinco** (e a de maior cobertura realizada). Logo `36,390 px` e o `73×`
que dele deriva são **o valor daquela semente**, não *"o"* número daquela cobertura.

---

## `F-3` — a guarda passa a REGREDIR: 0% → 4 casos, e a mutação mata

`assertViewportFitted` — a única coisa que separa este spike do falso verde que ele mesmo
produziu — **não era carregada por teste nenhum**: `headless-chart.ts` (272 l.) estava com **0%
de cobertura**. A prova de que ela é *load-bearing* era a mutação **manual** do `/qa`, e mutação
manual não regride.

**Exportada** (`headless-chart.ts`) e coberta por **4 casos** em `headless-chart.test.ts` (novo):

| caso | o que pinta |
|---|---|
| **ACEITA** | `barSpacing = 1200/1440` sobre 1.440 slots — o eixo que de fato coube |
| **RECUSA** | `barSpacing = 6 px`, 1.440 slots, pane de 1.200 px — **a geometria literal do falso verde** |
| banda de 2% | segura **dos dois lados** (`×1,019` aceita · `×1,021` recusa): guarda que aceita tudo deixa passar o falso verde; guarda que não aceita nada transforma toda corrida real em `rc=3` |
| pane zero | `drift` vira `NaN`, e **`NaN > 0.02` é FALSO** — sem esse ramo um modelo não-dimensionado passaria como *"coube"* |

`package.json`: `test:charts` passou de um arquivo fixo para `node --test 'src/charts/*.test.ts'`
— senão o arquivo novo não seria executado por ninguém.

**A mutação MATA — e é este número, não o verde, que prova a proteção:**

```bash
cd frontend && sed -i 's/^  if (drift > 0.02) {$/  if (false) {  \/\/ MUTANT/' src/charts/headless-chart.ts
node --test 'src/charts/*.test.ts'   # tests 13 · pass 11 · fail 2
#   ✖ assertViewportFitted REFUSES the default 6 px spacing that produced the false green
#   ✖ assertViewportFitted holds the 2% drift band on each side of it
git checkout src/charts/headless-chart.ts && node --test 'src/charts/*.test.ts'   # rc=0 · 13/13
```

`[MEDIDO 2026-08-29, n=13 testes; mutação mata 2]`. Árvore restaurada, `git diff --stat` de
volta a `9 insertions(+), 1 deletion(-)`.

---

## Portões — `make verify`, uma chamada (R7)

```bash
export HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness
make verify
```

```
[OK       ] lint-backend    rc=0  52 source files
[OK       ] lint-frontend   rc=0  ESLint do projeto sobre frontend/src
[OK       ] test            rc=0  325 passed · Total coverage: 99.16%
[OK       ] boundaries      rc=0  3 kept, 0 broken
[OK       ] regras          rc=0  0 bloqueio(s), 0 aviso(s)
[OK       ] política        rc=0
veredito: VERDE — 6 portões mediram e passaram
```

`[MEDIDO 2026-08-29T21:25:03Z, log em /tmp/verify-T-08.2-20260829T212503Z.log]`.

```bash
npm --prefix frontend run test:charts    # tests 13 · pass 13 · fail 0 · rc=0
```

**Não-vacuidade do motor de regras sobre o arquivo NOVO** — porque `rc=0` vazio é ambíguo
(`ADR-012`), e o `/qa` provou nos 5 arquivos do ciclo 1, não neste:

```bash
harness rules --mode file --path frontend/src/charts/headless-chart.test.ts        # rc=0
printf '\nimport { thing } from "backend/src/whatever";\n' >> frontend/src/charts/headless-chart.test.ts
harness rules --mode file --path frontend/src/charts/headless-chart.test.ts        # rc=2
# {"decision": "block", "reason": "[BLOQUEIO] [web-fullstack.browser-imports-server]
#  frontend/src/charts/headless-chart.test.ts:64: ... 1 ocorrencia(s)"}
```

`[MEDIDO 2026-08-29, n=1 arquivo]` — o motor **lê** e **bloqueia** nele. Arquivo restaurado
(`diff` contra a cópia ⇒ idêntico), `harness rules --mode sweep --changed-only` ⇒ `rc=0`.

---

## `docs/INDEX.md` — a correção é uma LINHA NOVA, nunca a reescrita da antiga

**`F-1` e `F-2` não estavam só no relatório: os dois números errados também foram publicados no
`INDEX`.** A linha `18:37Z` (`docs/INDEX.md:94`) diz *"**36,390 px, 73× a tolerancia**, na
cobertura de **20,0%** que `D8.11` mediu"* e *"os **9** testes"*. Corrigir ali seria reescrever
linha existente, que o `CLAUDE.md` proíbe (*"Acrescente linha; **não reescreva linha
existente**"*) — então a correção é **append**, com o mesmo precedente da linha `16:55Z`, que
corrigiu `8` para `10` sem tocar na linha de origem.

```bash
git diff --numstat docs/INDEX.md      # 1  0  docs/INDEX.md
tail -1 docs/INDEX.md | grep -oP '(?<!\\)\|' | wc -l    # 5  — igual à linha 94
```

`[MEDIDO 2026-08-29]` — **1 inserção, 0 remoções** (append-only respeitado) e **5 separadores de
coluna não escapados**, idêntico à linha de referência: a tabela do `INDEX` não foi alargada,
que é o defeito que a própria linha `16:55Z` registra ter consertado.

---

## O que NÃO foi tocado, e é deliberado

- **O grafo.** `tasks.toml` intocado; `D-2` continua `NÃO APLICADA`.
- **A decisão de arquitetura** (grade completa com *whitespace* × X não legível como tempo) —
  é do `/architect`, e continua com ele.
- **`A-1`/`A-2` do `/qa`** — não há `test_cmd.charts` nem `coverage_target` na política, então
  os 13 testes desta task **não são cobrados por portão nenhum**. Declarar a chave é **ato do
  owner**; não é meu. **Enquanto não existir, `F-3` está corrigida no código mas não é exigida
  por máquina** — quem apagar `headless-chart.test.ts` amanhã passa no `make verify`.
- **`A-4`** — o `gate-record` do veredito continua **recusado** (`rc=4`) por 3 tasks abertas da
  **fase 02** (`T-02.1`, `T-02.2` em `blocked`; `T-02.4b` em `todo`). Dívida herdada, sem
  relação com `T-08.2`, e fechá-la não é ato de builder.

## Cobertura

**Não há alvo declarado para `charts`** (`harness policy --key coverage_target` ⇒ *campo ausente
na politica*), então não há piso contra o qual reprovar — `A-1` do `/qa`. O que mudou é medível
mesmo assim: `headless-chart.ts` saiu de **0 teste** para **4 casos** sobre a única função que
o `/qa` provou ser *load-bearing*. `axis-spike.ts` (172 l.) **continua sem teste** — ele é o
runner, e o que ele executa está coberto pelos 13 testes dos módulos que ele chama.
