[QA GATE — Fase 06: Quarentena pelo predicado de TRÊS termos, D6.2 falsificador da fase]
Feature: `plataforma-dados`   Componente: `sentimento`   PR: #104 (`tasks/T-06.6-fase06` → `master`, OPEN)
Arquivos avaliados: `backend/src/modules/sentimento/domain/quarantine_terms.py`,
`backend/src/modules/sentimento/domain/live_availability_write.py`,
`backend/tests/sentimento/test_quarantine_terms.py`, `backend/tests/sentimento/test_live_availability_write.py`.
Nenhum arquivo de produção foi alterado por este gate — só leitura e mutação temporária, revertida.

## Testes existentes rodados antes de qualquer coisa nova
- `bash backend/scripts/test.sh` (suite inteira, sem filtro) → **1358 passed, 0 failed**, 310s
  (contenção de CPU confirmada: 6 worktrees da fase 06 rodando pytest em paralelo, como o builder
  já havia registrado).
- Cobertura total **97,66%**; por camada: domain **99,8%** (2675/2680, meta 90%), use_cases
  **100,0%** (585/585, meta 80%), infra **95,1%** (2208/2321, meta 70%) — 3/3 `[OK]` via
  `check-coverage-layers.sh` embutido no `test.sh`.
- `bash backend/scripts/lint.sh` → `ruff check` + `ruff format --check` + `mypy --strict`: sem achado, 245 arquivos.
- `harness rules --mode sweep --changed-only` → rc=0, nenhum achado impresso (0 regras bloqueantes violadas nos arquivos alterados).
- `bash backend/scripts/natureza.sh` → 76 arquivos (57 domain + 19 use_cases), 0 leituras de relógio.
- `bash backend/scripts/boundaries.sh` → 3 contracts kept, 0 broken (162 arquivos, 734 dependências) — inclui "domain e use_cases não falam com socket nem ssl" (`ADR-016`).

## Mutação real — planta, confirma vermelho, reverte, confirma `git diff` limpo

**Mutação 1 — D6.2, o falsificador central.** Em `quarantine_terms.py:54`, troquei
`not (label_shift_present and unit_present and available_at_present)` por
`not (label_shift_present and unit_present)` — predicado de DOIS termos, ignorando `available_at`.
Rodei `pytest tests/sentimento/test_quarantine_terms.py -q --no-cov`:
**6 testes falharam**, incluindo exatamente
`test_d6_2_the_third_term_alone_isolates_a_series_with_the_other_two_resolved` (o falsificador
nomeado no DoD) e `test_d6_1_gaveta_count_matches_the_predicate_applied_by_hand_over_the_whole_catalog`
(`assert 0 == 4`) e `test_d6_1_readable_by_backtest_never_intersects_the_gaveta`. Revertido; `git diff`
sobre o arquivo ficou vazio. **O teste pega o defeito nomeado no DoD.**

**Mutação 2 — D6.3, a aproximação proibida.** Em `live_availability_write.py:66`, troquei
`return UNMEASURED_AVAILABLE_AT, AvailabilitySource.MODELED` por
`return 1_700_000_000_000, AvailabilitySource.MODELED` (simulando o `event_time` "361× otimista"
que a spec proíbe). Rodei `pytest tests/sentimento/test_live_availability_write.py -q --no-cov`:
**5 testes falharam**, incluindo os 3 casos parametrizados de
`test_the_output_is_never_event_time_nor_event_time_plus_interval`. Revertido; `git diff` limpo.

Após as duas reversões: `git diff --stat` (worktree inteira) devolveu vazio — nenhum resíduo de produção.

## D6.1 — provado sobre o catálogo real (5 linhas de `T-06.5`, não hardcoded)
`quarantine_gaveta`/`readable_by_backtest` operam sobre `SeriesCatalog` inteiro. Confirmado por
leitura de código (não filtram por fonte) e pelos testes `test_d6_1_*`, que rodam sobre as 5 linhas
reais de `open_interest_catalog_entries()` (4 Coinalyze + 1 Binance). `readable ∩ gaveta == frozenset()`
provado por construção (`readable_by_backtest` é o complemento de conjunto de `quarantine_gaveta`
sobre o MESMO catálogo/mapeamento) — a prova por construção é aceitável aqui porque a mutação 1
já demonstrou que o `is_quarantined` que alimenta essa construção É testado e quebra quando errado.

## D6.2 sobre dado real da Coinalyze — quarentena não abriu prematuramente
`_coinalyze_close_entry()` lê `open_interest_catalog_entries().entry_for(coinalyze_open_interest_key(Reduction.CLOSE))`
— linha de PRODUÇÃO real, não fixture. Confirmei em código-fonte:
`backend/src/modules/sentimento/domain/open_interest_catalog.py:53` define `_INTERVAL_MS = 300_000` e
`:61` `OPEN_INTEREST_LABEL_SHIFT_MS = _INTERVAL_MS` — **bate exatamente com o `label_shift=300_000`
citado pelo builder**, sem nenhum valor hardcoded divergente sobrando no teste (o `label_shift=0`
mencionado como erro do ciclo anterior não aparece em nenhum lugar do arquivo atual —
`grep -n "label_shift" test_quarantine_terms.py` só mostra leitura via `entry.key`, nunca literal).
Com `available_at_present=False` e os outros dois termos presentes, `is_quarantined is True` —
a Coinalyze permanece isolada, como o handoff exige ("não force uma abertura prematura").

## D6.3 — endpoint sem `lag_ms` nunca aproxima
`resolve_unmeasured_endpoint_availability` tem assinatura de retorno `tuple[None, AvailabilitySource]`
— não há branch `int`, então `event_time_ms`/`event_time_ms+interval` são estruturalmente
inatingíveis (confirmado pela mutação 2, que teve de FORÇAR um `int` fora do tipo declarado para
sequer tentar, e mesmo assim os testes pegaram). O lado espelhado — endpoint MEDIDO (`lag_n>0`) —
é recusado com `MeasuredLagCannotUseUnmeasuredPathError`, testado em
`test_a_measured_endpoint_refuses_the_unmeasured_path_instead_of_silently_taking_it`.

## Contrato `Natureza` (ADR-016)
Nenhum dos dois arquivos de domínio importa `socket`/`ssl`/I/O — `quarantine_terms.py` importa só
`series_catalog` (domain); `live_availability_write.py` importa só `availability_lag_stats` e
`provenance` (domain). `resolve_unmeasured_endpoint_availability` retorna um par, não persiste nada —
"decide, não persiste", como o handoff exige. `boundaries.sh` confirma "domain e use_cases não falam
com socket nem ssl" KEPT.

## Idioma
Identificadores, docstrings e mensagem de exceção (`MeasuredLagCannotUseUnmeasuredPathError`) em
inglês nos dois arquivos de produção e nos dois de teste — sem achado de `harness rules --mode sweep`.

## DoD, item a item
- [OK] D6.1 — `count(gaveta) == count(catálogo WHERE <predicado>)` e interseção vazia, sobre o catálogo
  real de 5 linhas — `test_d6_1_gaveta_count_matches_the_predicate_applied_by_hand_over_the_whole_catalog`,
  `test_d6_1_readable_by_backtest_never_intersects_the_gaveta`.
- [OK] D6.2 (falsificador da fase) — mutação plantada em `is_quarantined` (3→2 termos) derruba
  6 testes, incluindo o nomeado; revertida, `git diff` limpo.
- [OK] D6.3 — mutação plantada no valor de retorno (`None`→`event_time`-like) derruba 5 testes;
  revertida, `git diff` limpo.
- [OK] Coinalyze não abre prematuramente — provado sobre `label_shift=300_000` real, batendo com
  `open_interest_catalog.py:53,61`, sem hardcode divergente remanescente.
- [OK] `Natureza`/`ADR-016` — domain puro, sem I/O; `boundaries.sh` KEPT.
- [OK] Idioma inglês em produção e teste — 0 achados de regra.
- [OK] Testes existem e passam — `bash backend/scripts/test.sh` (1358 testes, 0 falhas).
- [OK] Cobertura 97,66% total / domain 99,8% / use_cases 100,0% / infra 95,1% contra alvo
  domain 90% / use_cases 80% / infra 70% (`ADR-009/D1`).
Regras bloqueantes avaliadas: 7 de 7 listadas por `harness rules list --severity block`.
Anomalia: nenhuma.
Veredito: APPROVED
