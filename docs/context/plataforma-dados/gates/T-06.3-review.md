[REVIEW GATE — Fase 06: As QUATRO séries de L/S com colunas próprias; `ls_ratio` genérico PROIBIDO]
Feature: `plataforma-dados`   Componente: `sentimento`   Task: `T-06.3` (`CST-47`)
PR: https://github.com/StharleyMax/cripto-strategy/pull/103, branch `tasks/T-06.3-fase06`
Worktree: `/tmp/claude-1002/wt/T-06.3`. Diff avaliado: `git diff master...HEAD` (three-dot), commit `206d211`.

## Escopo (4 arquivos, igual ao QA)
- `backend/src/modules/sentimento/domain/series_key.py` (editado)
- `backend/src/modules/sentimento/domain/long_short_ratio_series.py` (novo)
- `backend/tests/sentimento/test_long_short_ratio_series.py` (novo)
- `backend/tests/sentimento/test_series_identity.py` (editado)

## Camada 1 — o que o runner mede
`harness rules --mode file --path <arquivo>` nos 4 arquivos → 0 achados cada (saída vazia).
`harness rules --mode sweep --changed-only` na raiz → rc=0, 0 achados `[MEDIDO, reproduzido
independentemente do QA]`. Denominador: **7 regras bloqueantes declaradas
(`harness rules list --severity block`), 7 avaliadas** — 4 aplicáveis (`core.relative-import`,
`core.silent-except`, `core.print-statement`, `core.hardcoded-secret`), todas OK por leitura
direta; 3 N/A por escopo (as três `web-fullstack.*`, task é 100% `backend`/`sentimento`, sem
rota HTTP e sem diretório de teste de servidor `web` envolvido).

## Camada 2 — arquitetura declarada, cada achado citado

1. **Contrato `Natureza`/`ADR-016` (domain puro)** — `long_short_ratio_series.py` importa só
   `__future__`, `collections.abc`, `dataclasses`, `decimal`, `typing`: zero I/O, zero relógio.
   `bash scripts/natureza.sh` → 73 arquivos, 0 leituras de relógio `[MEDIDO, reproduzido]`.
   `bash scripts/boundaries.sh` → 3 kept, 0 broken (158 arquivos, 716 dependências) `[MEDIDO,
   reproduzido — números diferem dos do QA/builder porque o merge com `origin/master` avançou
   entre as sessões; a forma — 3 kept, 0 broken — é idêntica]`. Sem violação.

2. **D6.5 — `delta()` sobre a série taker é erro de TIPO** — reproduzido À MÃO nesta sessão,
   não só lido no relatório do builder: scratch file chamando
   `delta(TakerRatioComponents(buy_vol=Decimal("1"), sell_vol=Decimal("1")),
   PositionRatioObservation(metric="count_long_short_ratio", value=Decimal("1")))` sob
   `.venv/bin/python -m mypy --strict` → `error: Argument 1 to "delta" has incompatible type
   "TakerRatioComponents"; expected "PositionRatioObservation"  [arg-type]`. Confirma a
   exclusão estrutural: `PositionRatioMetric` é `Literal` de 3 membros (exclui
   `sum_taker_long_short_vol_ratio`), `TakerRatioComponents` não tem campo `.value`, e não há
   `isinstance`/`nature ==` em `delta()` — a refusal é por assinatura, como o handoff exige.
   Sem violação.

3. **Reuso de `series_key.py`/`series_catalog.py`** — `Reduction`/`QuantityField`/`Nature`
   (`series_key.py`) NÃO são duplicados; `long_short_ratio_series.py` não cria enum paralelo
   para STOCK/FLOW, resolvendo a distinção uma camada acima via dois dataclasses sem base
   comum (decisão documentada no próprio cabeçalho do módulo, linhas 15-25, citando o
   `[NÃO SEI]` de `T-04.4` sobre `SeriesKey.nature` ter um sexto membro). `FORBIDDEN_METRIC_
   NAMES` (banimento de `ls_ratio`) vive em `series_key.py`, reusando o mecanismo já existente
   para `implied_avg_price`, com um motivo por nome (`_FORBIDDEN_METRIC_REASONS`) em vez de uma
   mensagem hardcoded que já estava errada para um segundo nome. **Nenhuma `SeriesCatalogEntry`
   real das 4 séries é criada nesta task** — verificado por `grep -rln "count_long_short_ratio"
   backend/src`: só aparece nos 2 arquivos desta task e em `metrics_shift.py`/
   `availability_probe_set.py`/`as_of_accessor.py`/`metrics_csv_reader.py`, que já existiam
   antes (dados brutos do CSV, não catálogo). Isso é FIEL ao escopo declarado: o próprio
   `backend/README.md` desta task nomeia "nenhuma linha real de `series_catalog` é escrita
   (T-06.2/T-06.3/T-06.5/T-06.9)" na seção "Escopo que esta task NÃO fecha" — não é uma reuse
   quebrada, é uma reuse do vocabulário/identidade (`SeriesKey`, `FORBIDDEN_METRIC_NAMES`), não
   do catálogo de produção, que é território de outras tasks. Sem violação.

4. **`ls_ratio` PROIBIDO** — `grep -rn "ls_ratio" backend/src backend/tests --include='*.py'`
   → 11 ocorrências (4 em `backend/src`, 7 em `backend/tests`, todas contando comentário +
   código + docstring de teste), e as de produção batem exatamente as 4 que o QA relatou:
   `series_key.py:57` (comentário), `:67` (`FORBIDDEN_METRIC_NAMES`), `:76`
   (`_FORBIDDEN_METRIC_REASONS`), `long_short_ratio_series.py:66` (comentário). Nenhuma como
   nome ativo de série/coluna/variável — todas dentro da lista de proibição ou do texto que a
   explica. `LONG_SHORT_METRICS` lista as 4 nomeadas, e
   `test_the_four_metrics_are_named_and_ls_ratio_is_not_one_of_them` fixa a ausência. Sem
   violação.

5. **Item 6.10 (buyVol/sellVol)** — confirmado que só a FORMA foi entregue e está NOMEADA, não
   escondida: `backend/README.md` §"Escopo que esta task NÃO fecha" diz literalmente "Nenhuma
   ingestão REST de `takerlongshortRatio` existe. `TakerRatioComponents` é a forma que uma REST
   client futura preenche" — mesma frase no cabeçalho do módulo (linhas 27-33, 37-44) e no
   docstring de `resample_bare_taker_ratio_refuses`. `TakerRatioComponents` carrega
   `buy_vol`/`sell_vol` como campos reais (não descarta), com `ratio` como property computada.
   Sem violação.

6. **Idioma inglês (produção e teste)** — `grep -nP '[áàâãéêíóôõúçÁÀÂÃÉÊÍÓÔÕÚÇ]'` nos 4
   arquivos → 1 única ocorrência, `test_long_short_ratio_series.py:278`, citação literal entre
   aspas de `SPEC-001` §5.11 ("pedir TF 15m na série taker recusa; nunca devolve 3,1809") dentro
   de docstring em inglês — mesmo padrão já aceito em `series_key.py` (cita "RATIO de estoque").
   Não é identificador novo em português. Sem violação da convenção da linha 1/2/5 da tabela de
   `CLAUDE.md` (identificador de produção/teste, docstring — inglês).
   Mensagens de `raise`/`Error(...)` nos 2 arquivos de produção: todas em inglês (conferido por
   leitura completa de `InvalidTakerVolumeError`, `MismatchedRatioMetricError`,
   `NonAggregableFlowRatioError`, `IncompleteSeriesKeyError` — nenhuma string de mensagem em
   português), consistente com a decisão de 2026-09-02 (`raise X("…")` entra na fronteira do
   inglês).

7. **`docs/INDEX.md`/`backend/README.md` append-only** — `git diff master...HEAD` nos dois
   arquivos mostra só `+` linhas (uma linha nova na tabela de `INDEX.md`, uma seção nova
   `## 📎 2026-09-03 por T-06.3` ao final de `README.md`); nenhuma linha existente
   removida/reescrita. Sem violação.

## Testes reproduzidos nesta sessão (não só lidos do relatório do QA)
- `harness rules --mode file --path <cada um dos 4 arquivos>` → 0 achados cada.
- `harness rules --mode sweep --changed-only` → rc=0.
- `.venv/bin/python -m pytest -k "test_long_short_ratio_series or test_series_identity" -q` →
  todos passam (sem contagem de `-q`, mas 0 falhas, 0 erros).
- `mypy --strict` sobre chamada `delta(TakerRatioComponents(...), PositionRatioObservation(...))`
  → erro `[arg-type]`, confirma D6.5.
- `bash scripts/boundaries.sh` → 3 kept, 0 broken.
- `bash scripts/natureza.sh` → 73 arquivos, 0 leituras de relógio.

## Nota — rebase pendente (não bloqueante para este veredito)
Confirmado que `master` avançou (`T-06.9` mesclada, entre outras) e a branch não foi rebaseada;
QA já mediu (`merge-tree`) que o único conflito é em `docs/INDEX.md` (append-only, ambas as
seções cabem). Nenhum arquivo do escopo desta review foi tocado pelas branches concorrentes.
Isso é ação de merge/orquestração, não achado de arquitetura.

## Veredito
Regras bloqueantes: **7 declaradas, 7 avaliadas** (4 aplicáveis OK, 3 N/A por escopo).
Arquivos varridos: **4** (2 produção, 2 teste), via `harness rules --mode file` individual +
`harness rules --mode sweep --changed-only`.

**COMPLIANT** — nenhuma violação, mecânica ou arquitetural, encontrada nos 4 arquivos do escopo
de `T-06.3`. A distinção STOCK-like/FLOW-like é resolvida uma camada acima de `SeriesKey`, com
o gap (`nature` de 1 membro `RATIO`) nomeado e atribuído a `/architect`, não escondido.
