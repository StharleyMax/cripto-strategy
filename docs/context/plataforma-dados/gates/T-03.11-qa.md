# QA Gate — Fase 03 [sentimento] T-03.11 (CST-27) — PR #67

**Worktree:** `/tmp/claude-1002/wt/T-03.11` · **Base:** `master@07193e6` · **Commit:** `1d0711a`
**DoD:** `docs/context/plataforma-dados/tasks_review.md:275` · bloco `id = "T-03.11"` em `tasks.toml:478`
· plano `docs/plans/SPEC-001-plataforma-dados/03_captura_continua.md` item 3.12 (nota: o `D3.12` da
tabela de DoD daquele arquivo é de OUTRO item — 3.7/3.10, "dois baldes cegos" — a linha normativa
para 3.12 é a de `tasks_review.md:275`, conferida contra o código).

## 1. Requisito central — a ressalva na saída de máquina

Confirmado literalmente, não por inferência:

- `RECONCILIATION_CAVEAT` (`liquidation_reconciliation.py:56-63`) é campo **fixo** em toda
  `DailyLiquidationReconciliation` (`caveat: str = RECONCILIATION_CAVEAT`, linha 221) —
  independe do valor da razão. Testado por
  `test_liquidation_reconciliation.py::test_caveat_travels_regardless_of_hypothesis` (dia com
  captura=0/coinalyze=0, ainda assim `caveat == RECONCILIATION_CAVEAT`).
- As DUAS interpretações do handoff são dois dos quatro membros de `CoinalizeStreamHypothesis`
  (`SAME_STREAM_INCONCLUSIVE` = razão~1 não prova nada; `INDEPENDENT_STREAM_MEASURES_LOSS` = mede
  perda real), cada uma com texto pronto em `HYPOTHESIS_SCREEN_LABEL` (linhas 93-110).
- Na saída de máquina real (`liquidation_reconciliation_cli.py:_report_line`, linhas 83-95), cada
  linha JSON carrega `hypothesis`, `screen_label` **e** `caveat` junto do `ratio` — nunca o número
  sozinho. Verificado ponta a ponta (SQLite + JSONL reais, `tmp_path`, sem fake) por
  `test_liquidation_reconciliation_cli.py::test_dispatch_reads_quarantine_and_evidence_and_emits_one_line_per_day_plus_summary`
  (linha 111: `assert "Nao se sabe se a Coinalyze" in payloads[0]["caveat"]`).

## 2. Leitura direta da quarentena, sem promoção

`liquidation_reconciliation_cli.dispatch` chama `store.read_latest(...)` (linha 121), NUNCA
`read_promoted`. `read_latest` (`sqlite_series_quarantine_store.py:164-174`) não filtra
`available_at IS NOT NULL` — todo ponto que este one-shot grava tem `available_at = NULL` por
desenho (`D2.6`). Testado por
`test_sqlite_series_quarantine_store.py::test_read_latest_returns_the_quarantined_row_even_though_available_at_is_null`,
que falharia se a implementação reusasse por engano o filtro de `read_promoted`.

## 3. Mutação — reproduzida de forma independente (não apenas relida do relatório do builder)

`sha256sum` do arquivo antes/depois de cada mutante: `00e12024f8a83f44374ca42cb98efaa05eaab503b1b502e5ca8f152568485377` (idêntico nos 3 casos).

| # | mutante | comando | resultado observado |
|---|---|---|---|
| 1 | `ratio > near_one_upper_bound` → `>=` | `.venv/bin/python -m pytest -q tests/sentimento/test_liquidation_reconciliation.py -k classify` | **FAILED** `test_classify_ratio_at_upper_boundary_is_inclusive` |
| 2 | `ratio < near_one_lower_bound` → `<=` | idem | **FAILED** `test_classify_ratio_at_lower_boundary_is_inclusive` |
| 3 | `if day in seen_days:` → `if False:` | `... -k duplicate` | **FAILED** `test_reconcile_duplicate_coinalyze_point_for_the_same_day_raises` (`DID NOT RAISE`) |

## 4. Testes e cobertura

`bash backend/scripts/test.sh -q` → `rc=0`. Cobertura total **98,09%**; por camada
(`check-coverage-layers.sh`): `domain 99,9%` (alvo 90%) · `use_cases 100%` (alvo 80%) ·
`infra 95,9%` (alvo 70%) — todos acima do piso declarado em `ADR-009/D1`.

`liquidation_reconciliation_cli.py`: **82/82 linhas · 14/14 branches, 100%** (medido na mesma
rodada, tabela `--cov-report=term-missing`).

## 5. Regras bloqueantes

`harness rules --mode sweep` (árvore completa do worktree) → `rc=0`, 0 bloqueios. 7 de 7 regras
`--severity block` avaliadas (nenhuma tem escopo que exclua este diff). Complementar:
`bash backend/scripts/boundaries.sh` → `Contracts: 3 kept, 0 broken` (111 arquivos, 485
dependências); `bash backend/scripts/natureza.sh` → `universo de 46 arquivo(s), 0 leitura(s) de
relogio`.

## 6. PR

`gh pr view 67` → `state OPEN`, `baseRefName master`, `headRefName
tasks/T-03.11-reconciliacao-coinalyze`, `+1625/-0`.

## Veredito

**APPROVED.** Nenhum achado bloqueante. O achado do builder (item 7 do relatório: 3
`ResourceWarning` pré-existentes em `test_sqlite_series_quarantine_store.py`, fora deste diff, sem
efeito em `rc`) é nomeado e não bloqueante — confirmado fora do escopo do diff de `T-03.11`.

## Nota de escopo — desfecho da fase 03

Esta validação cobre **apenas `T-03.11`**. `tasks.toml`/`harness tasks list` mostram, hoje, TODAS
as demais tasks de fase `03` ainda como `status = "todo"` (exceto `T-03.6`/`T-03.9` = `blocked`),
inclusive `T-03.2`/`T-03.10`, cujos handoffs afirmam PR já mesclada. Fechar o desfecho de toda a
fase (`harness tasks resolve plataforma-dados 03 ...`) exigiria evidência de QA para CADA task da
fase, que este gate não produziu — resolver aqui seria declarar outcome sem prova para tasks fora
do meu escopo. Deixado para quem tiver o gate de cada uma das demais tasks de `03` em mãos.
