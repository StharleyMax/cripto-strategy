# Review Gate — T-08.9 (fase 08, componente `charts`) — `S2` completa: moldura de as-of + marcação com teclado obrigatório

Worktree: `/tmp/claude-1002/wt/T-08.9` (branch `tasks/T-08.9-s2-completa-marcacao`).
Delta: `frontend/src/charts/s2-asof-frame.ts` (+test), `frontend/src/charts/s2-review-mode.ts`
(+test), `frontend/README.md` §18 (com subseção "Rodada 2"). Confirmado por
`git status --porcelain=v1` nesta sessão: só esses 4 arquivos de produção/teste + README + os 2
gates de QA — nenhum arquivo fora do delta declarado.

Ciclo completo lido: `docs/context/plataforma-dados/gates/T-08.9-qa.md` (ciclo 1, NEEDS_FIX) e
`docs/context/plataforma-dados/gates/T-08.9-qa-ciclo2.md` (ciclo 2, APPROVED).

## Camada 1 — o que o runner mede

```bash
harness rules list --severity block            # 7 regras bloqueantes em vigor
harness rules --mode sweep --changed-only       # rc=0, 0 linha de saída
npm --prefix frontend run lint                  # rc=0, 0 erro/aviso
node --test frontend/src/charts/s2-asof-frame.test.ts frontend/src/charts/s2-review-mode.test.ts
  # 27/27 pass (rodado nesta sessão, isolado dos 16 fails pré-existentes de data/binance/**)
node --test frontend/src/charts/eslint-boundary.test.ts
  # 4/4 pass — fronteira charts<->web (D5.12) íntegra com os 2 módulos novos
```

Nenhuma das 7 regras bloqueantes (`core.relative-import`, `core.silent-except`,
`core.print-statement`, `core.hardcoded-secret`, `web-fullstack.browser-imports-server`,
`web-fullstack.tenant-from-request`, `web-fullstack.server-test-directory-present`) se aplica ao
delta — confirmado por `harness rules --mode sweep --changed-only` (rc=0) e por leitura direta:
sem import relativo (`s2-*.ts` importa só `./s2-badge.ts`/`./s2-swing-point.ts`), sem `except`
silencioso (TS, não se aplica), sem `console.log`, sem segredo, sem import de `web`/`app` em
`charts`, sem request/tenant. **7 de 7 regras bloqueantes avaliadas.**

## Camada 2 — arquitetura declarada, os 3 pontos pedidos

**(1) A correção do achado do QA é estrutural, não band-aid.** Diff isolado
(`git diff -- frontend/src/charts/s2-asof-frame.ts`): a guarda em `assertFrameIsNoticeable`
(linha 137-140) passou de uma única comparação `<` para
`!Number.isFinite(frame.widthPx) || frame.widthPx < ...`. `Number.isFinite` é a variante NÃO
coercitiva (`typeof x === "number" && !isNaN(x) && isFinite(x)`) — fecha a CLASSE inteira
(`NaN`, `Infinity`, `-Infinity`, string numérica, `null`, `undefined`, objeto com `valueOf`),
não um caso pontual. Confirmado por execução própria nesta sessão (não só relato do QA-ciclo2):
`node --test` isolado → 11/11 pass, incluindo o teste plantado pelo QA
(`s2-asof-frame.test.ts:87-90`, nome preservado byte a byte, "nao corrigido" mantido de
propósito — o achado fica documentado mesmo depois de corrigido, e o teste MORDE continua
válido porque `assertFrameIsNoticeable` é a única porta que `widthPx` atravessa). Não há
band-aid (ex.: checagem só no chamador, ou clamp silencioso) — o guard é de runtime, na função
que é o falsificador declarado do próprio título da task ("impossível de não notar"), e vale
para todo `widthPx` que a passar, presente ou futuro.

**(2) "Painéis restantes" corretamente fora de escopo.** `tasks.toml:1080-1086` (`T-08.9`) cita
só `SPEC-001 §6`, `plano 08 item 8.6`, e os `refs` de desbloqueio de `Q11`/`Q20`/`ADR-017`
`D2`/`D3` — nenhum nomeia painel nem `DoD` de renderização. Verificação independente no plano
`08` (`docs/plans/SPEC-001-plataforma-dados/08_superficie_e_reprodutibilidade.md`): a tabela de
`DoD` não tem uma linha dedicada ao item `8.6` — `D8.5` (dispersão de z, item 8.5), `D8.6`
(bin de overflow, item de distribuição) e `D8.7` (histograma de funding) são sobre tópicos
diferentes por coincidência numérica de rótulo, não pelo item 8.6; as regras de renderização de
painel (`D8.11`-`D8.20`: habilitação por múltiplo de grade, colisão de disco, nunca dois eixos
Y, idade só na borda direita) estão explicitamente nos `refs` de `T-08.12`
(`tasks.toml:1110-1117`) e `T-08.13` (`tasks.toml:1119-1127`), status `todo`, não `T-08.9`.
README §18 "Bloqueado" registra a mesma leitura. Nenhuma regra viola — escopo corretamente
recusado, não esquecido.

**(3) Nenhum campo de `ADR-017` (RASCUNHO) vazou para os tipos novos.** `harness pipeline state
ADR-017` → `INIT` (sem evento `approve`), confirmado nesta sessão. `grep` sobre
`s2-review-mode.ts`/`s2-asof-frame.ts`/`s2-swing-point.ts`: `detector_key`,
`structure_definition`, `break_by` e `provenance` aparecem SÓ em comentário/docstring (citação
do que está fora de escopo), nunca como campo de `interface`/`type`. `ReviewCandidate` é
`{candidateId, kind}`; `ReviewAction` carrega `verdict`/`candidateId`/`swingKind` — nenhum campo
de `ADR-017` além do vocabulário literal de `D2`/`Q20` já citado nos `refs` da task
(`review_verdict ∈ {accept, reject, add}`, candidatos `swing`/`zone`). `zone`'s shape (OB) não
foi construída — só o `kind` opaco, mesma disciplina que `T-08.10` já aplicou a `swing_point`.

## Achado registrado, não bloqueante

**[INFO]** O nome do teste em `s2-asof-frame.test.ts:87` continua
`"MORDE (achado QA, nao corrigido): ..."` mesmo depois de o defeito ter sido corrigido — decisão
deliberada, confirmada pelo QA-ciclo2 ("confirmado intacto, não removido nem enfraquecido") e
pelo README §18 ("não foi alterado"). Não é regra violada nem contradiz nenhum documento — é
trilha de evidência preservada de propósito (o comentário acima do teste já explica: "não é
correção, é evidência para o QA gate"). Registrado aqui só para não ficar tácito.

## Veredito

- [OK] Regras bloqueantes — `harness rules --mode sweep --changed-only`: rc=0, 0 achado. 7 de 7 avaliadas.
- [OK] Correção do achado QA — estrutural (`Number.isFinite`, fecha a classe, não o caso pontual) — `frontend/src/charts/s2-asof-frame.ts:137-140`
- [OK] "Painéis restantes" fora de escopo — `docs/context/plataforma-dados/tasks.toml:1080-1086` (sem ref de painel) + plano `08` (sem `D8.x` dedicado ao item 8.6) + `D8.11`-`D8.20` explicitamente em `T-08.12`/`T-08.13`
- [OK] Fronteira `ADR-017` (RASCUNHO, `harness pipeline state ADR-017` = `INIT`) — nenhum campo além de `D2`/`Q20` literal vazou para `ReviewCandidate`/`ReviewAction`/`ReviewInput`
- [OK] Fronteira `charts`↔`web` (`D5.12`) — `node --test eslint-boundary.test.ts`: 4/4 pass
- [OK] `npm --prefix frontend run lint` — rc=0

Denominador: 7 regras bloqueantes em vigor (`harness rules list --severity block`), 7 avaliadas
sobre 4 arquivos de produção/teste do delta + `frontend/README.md` §18, via
`harness rules --mode sweep --changed-only` + leitura direta dos 4 arquivos + `git status
--porcelain=v1` (confirma delta fechado) + `harness pipeline state ADR-017`.

**Veredito: COMPLIANT**

## Nota — `harness gate-record` não persistiu no ledger

`harness gate-record plataforma-dados T-08.9 REVIEW COMPLIANT "..."` foi recusado (exit 4), não
pela auditoria de código, mas pelo documento de tarefas: `tasks.toml` ainda lista `T-08.9` (e mais
9 tasks da fase 08) com `status = "todo"`, o que o harness trata como contradição ao veredito
`QA=APPROVED` já registrado em `2026-09-03T18:21:32Z`. Isto é read-only por desenho deste agente —
não há edição de `tasks.toml` nem `harness pipeline override` aqui, porque nenhum dos dois é gate
de revisor. O veredito de arquitetura acima (`COMPLIANT`) fica de pé; o registro no ledger depende
de quem fecha `tasks.toml` (builder/PM) atualizar `status` antes de regravar.
