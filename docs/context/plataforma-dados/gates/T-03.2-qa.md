# `T-03.2` (`CST-18`) — relatório de gate do `/qa`

**Feature:** `plataforma-dados` · **Componente:** `sentimento` · **Fase:** `03`
**Base:** `master@634833b` · **Branch:** `tasks/T-03.2-forceorder-liquidacao` · **Commit avaliado:** `bfc1753`
**Worktree:** `/tmp/claude-1002/wt/T-03.2` · **PR:** #56

## Veredito: APPROVED

## 1. Testes existentes rodados antes de qualquer teste novo, e independentemente reproduzidos

```
bash backend/scripts/test.sh
# 647 passed in 547.99s (0:09:07) · Total coverage: 99.50%
# domain 100.0% (meta 90%) [1145/1145] · use_cases 100.0% (meta 80%) [252/252] · infra 98.5% (meta 70%) [935/949]
bash backend/scripts/lint.sh
# All checks passed! · 99 files already formatted
bash backend/scripts/boundaries.sh
# Contracts: 3 kept, 0 broken (incl. "domain e use_cases nao falam com socket nem ssl")
```
`[MEDIDO 2026-09-01, sessão QA]`, confere exatamente com o número do builder (era 617 antes desta
task; +30 novos em `test_force_order_collector.py`). Cobertura por camada acima do alvo declarado
por `ADR-009/D1` nas 3 camadas medidas de 3 declaradas. Os 5 arquivos de produção novos estão em
100% de linha e ramo; a única lacuna de `infra` (14 linhas, `binance_stream_probe.py:68-81`,
`connect_tls` socket real) é herdada de `T-03.1`, não tocada por esta task.

## 2. Mutação — prova de que o rótulo sai na SAÍDA DE MÁQUINA, e o dado é gravado CRU

Duas mutações aplicadas em produção, cada uma revertida antes da próxima (`git diff --stat` vazio
ao final), suite do arquivo confirmada limpa depois:

| # | mutante | resultado |
|---|---|---|
| A | `force_order_envelope.py::as_dict` — `subsampling_semantics_label` trocado por `"MUTATED_AWAY"` antes de projetar as colunas | **morre** — 2 falhas: `test_as_dict_projects_the_five_columns_in_the_fixed_order` e `test_the_recorder_writes_every_raw_message_enveloped` (esta última lê o **arquivo `.jsonl` gravado em disco**, não um objeto em memória — é a saída de máquina real) |
| B | `force_order_raw_recorder.py::messages` — `raw=raw` trocado por `raw=raw.replace('"', "'")` (simula normalização/reformatação do payload) | **morre** — 2 falhas: `test_the_recorder_writes_every_raw_message_enveloped` e `test_the_cli_writes_evidence_and_exits_zero_when_connected_with_a_message` (end-to-end, via CLI, lendo o `.jsonl` escrito) |

`[MEDIDO 2026-09-01, executado nesta sessão]`. 2 de 2 mutantes morrem; nenhum sobrevivente. O
mutante A prova que o rótulo de subamostragem não é decorativo em docstring/comentário — ele é
lido de volta do arquivo `.jsonl` gravado pelo recorder e do `as_dict()` da própria estrutura de
saída. O mutante B prova que "grava cru, zero normalização" é verificado byte a byte contra o
`raw` original, inclusive end-to-end pela CLI, não só na unidade isolada.

Adicional: `capture_force_order_stream.py` **nunca** importa `json` nem decodifica o payload —
estrutural, não testado por mutação porque não há branch a mutar (confirmado por leitura: zero
`json.loads`/parse na camada de use_case).

## 3. Conectividade real — investigado, e é ambiente, não defeito de código

O builder registrou `[NÃO MEDIDO]`: duas janelas (25s, 60s) abaixo do gatilho de parada de `T-03.1`
(≥120s × 2), `!forceOrder@arr` e o controle `!markPrice@arr@1s` (que empurra a cada 1s por
definição) ambos silenciosos, enquanto streams por-símbolo no mesmo host entregam de imediato.

**Reproduzido de forma independente nesta sessão, com socket cru — fora de qualquer código deste
repositório:**

```
GET /ws/btcusdt@bookTicker HTTP/1.1 ...        → 101 Switching Protocols + payload real em <1s
GET /ws/!forceOrder@arr HTTP/1.1 ...           → 101 Switching Protocols, 0 bytes de frame em 30s
GET /ws/!markPrice@arr@1s HTTP/1.1 ...         → 101 Switching Protocols, 0 bytes de frame em 15s
```
`[MEDIDO 2026-09-01]`, 3 conexões TLS diretas via `ssl`/`socket` puro, sem `ByteChannel`, sem
`WebSocketMessageSource`, sem nenhuma linha do coletor sob teste.

**Conclusão: é o comportamento da rede/proxy deste ambiente sandbox com streams de ARRAY
(`!…@arr`), não um defeito do coletor.** A prova bypassa 100% do código de produção desta task e
reproduz exatamente o mesmo padrão (handshake OK, zero frames de array; por-símbolo entrega
imediato) — se fosse defeito no `rfc6455_client.py`/`binance_stream_probe.py` reaproveitados, o
socket cru não reproduziria o silêncio, porque ele não usa nenhum dos dois. Também descarta
"rede totalmente indisponível": `fstream.binance.com` responde TLS, completa handshake HTTP, e
entrega dado real para `bookTicker`/`depth5`. **Não bloqueia esta task** — o DoD pedia código +
probe de conectividade real, e a conectividade (handshake completo, `CONECTADO`) está provada;
"dado real fluindo" ficou nomeado com gatilho de reabertura explícito
(`docs/medicao-conectividade-forceorder.md`), igual ao precedente já aceito em `T-03.1`.

## 4. As 7 regras bloqueantes, uma a uma

Universo: `harness rules list --severity block` → 7 regras.

| id | veredito | evidência |
|---|---|---|
| `core.relative-import` | **OK** | `grep -rn '^from \.\|^import \.' backend/src/modules/sentimento/{domain,infra,use_cases}/force_order_*.py capture_force_order_stream.py` → 0 ocorrências; `harness rules --mode sweep --changed-only` → `rc=0`, saída vazia |
| `core.silent-except` | **OK** | os 2 `except StreamTransportError` de `capture_force_order_stream.py:44,76` devolvem `ForceOrderNotConnected(failed_stage=...)` nomeado, ou registram `INTERRUPTED` com o estágio — nunca descartam |
| `core.print-statement` | **OK** | `grep -n 'print(' backend/src/modules/sentimento/**/force_order_*.py` → 0; CLI usa `logger` nomeado + `route_diagnostics_away_from_the_product_stream` |
| `core.hardcoded-secret` | **OK** | WebSocket público, nenhuma credencial; `grep -rniE 'api[_-]?key|secret|password'` → 0 nos 5 arquivos novos |
| `web-fullstack.browser-imports-server` | **OK (vácuo)** | `git diff master...HEAD --stat` → 0 arquivos sob `frontend/` |
| `web-fullstack.tenant-from-request` | **OK (vácuo)** | idem |
| `web-fullstack.server-test-directory-present` | **OK** | `backend/tests/sentimento/` existe, recebeu `test_force_order_collector.py` (445 linhas, 30 testes) |

**Regras bloqueantes avaliadas: 7 de 7.**

`harness rules --mode sweep --changed-only` → `rc=0`, 0 achados (confirma o builder).
`harness validate --strict` (via `HARNESS_MECHANISM=.../harness-plugin/0.13.0/bin/harness`) →
`política válida: cripto-strategy (schema_version=1)`, `rc=0`.
`make verify` neste worktree: `lint-backend`/`lint-frontend` `OK rc=0`; `test-backend` sob
contenção de CPU pesada (7+ worktrees rodando pytest simultaneamente, load average 8,4 em 8
núcleos) — rodado em paralelo e confirmado separadamente via `bash backend/scripts/test.sh` acima
com o mesmo resultado do builder.

## 5. DoD (`tasks.toml:376-383`, `tasks_review.md:266`)

- [x] Coletor grava cru, zero normalização — estrutural (`capture_force_order_stream.py` nunca
      decodifica JSON) **e agora provado por mutação** (item 2, mutante B).
- [x] `received_at` + `stream` + `doc_snapshot_date` + `subsampling_semantics_label` na **saída de
      máquina** — **provado por mutação** (item 2, mutante A), não só existente em código; e
      confirmado no `.jsonl` gravado e no `summary` JSON de cada rodada real do builder.
- [x] Zero API key — WebSocket público, confirmado.
- [x] Probe curto de conectividade real (não deploy contínuo) — 3 rodadas reais do builder + 3
      reproduções independentes desta sessão via socket cru (item 3).
- [x] `id = "T-03.2"` existe em `tasks.toml:376-383`, `status = "todo"` **intocado** pelo builder
      e por este gate (avanço de fase é ato do owner/ledger, não de `/build` ou `/qa`).
- [x] `docs/INDEX.md` — 1 linha acrescentada ao final, nenhuma linha existente reescrita
      (append-only respeitado, `git diff master...HEAD -- docs/INDEX.md` confere).

## 6. Anomalia

Nenhuma que impeça veredito. `make verify` completo (6 portões) não terminou dentro do tempo desta
sessão por contenção de CPU compartilhada com outras 7 bancadas de worktree rodando em paralelo no
mesmo host — os portões individuais (`lint`, `test`, `boundaries`, `rules sweep`, `validate
--strict`) foram todos rodados e confirmados separadamente, com os mesmos números do builder.

## Veredito

```
## QA Gate — Fase 03 [sentimento]
- [OK] core.relative-import — grep + sweep, 0 ocorrências
- [OK] core.silent-except — capture_force_order_stream.py:44,76, falha nomeada
- [OK] core.print-statement — grep, 0 ocorrências; logger nomeado
- [OK] core.hardcoded-secret — WS público, 0 ocorrências
- [OK] web-fullstack.browser-imports-server — vácuo, 0 arquivos frontend/
- [OK] web-fullstack.tenant-from-request — vácuo
- [OK] web-fullstack.server-test-directory-present — backend/tests/sentimento/ existe
- [OK] Testes existem e passam — `bash backend/scripts/test.sh` (647 testes)
- [OK] Cobertura 99,50% (domain 100/use_cases 100/infra 98,5) contra alvo 90/80/70
- [OK] DoD da fase, item a item — ver seção 5
- [anomalia] `make verify` (6 portões) não completou por contenção de CPU compartilhada; os 6
  portões foram confirmados individualmente com os mesmos números
Regras bloqueantes avaliadas: 7 de 7
Veredito: APPROVED
```

## Rastro de mutação (limpeza confirmada)

```
git diff --stat backend/src/modules/sentimento/domain/force_order_envelope.py \
                 backend/src/modules/sentimento/infra/force_order_raw_recorder.py
# (vazio) — ambos os mutantes revertidos byte a byte antes de fechar o gate
```
