# QA Gate Context Block — Fase 03: Reconciliação diária liquidação × agregado Coinalyze (T-03.11)

**Feature:** plataforma-dados · **Componente:** `sentimento` · **Card:** `CST-27`
**Spec:** `docs/specs/SPEC-001-plataforma-dados.md` §5.2/§8.5 (via `CA-F0-14`)
**Plan:** `docs/plans/SPEC-001-plataforma-dados/03_captura_continua.md` (item 3.12)
**Base:** `master@07193e6` · **Branch:** `tasks/T-03.11-reconciliacao-coinalyze`
**Worktree:** `/tmp/claude-1002/wt/T-03.11` · **Commit:** `1d0711a`
**PR:** https://github.com/StharleyMax/cripto-strategy/pull/67

## 1. Arquivos alterados

- `backend/src/modules/sentimento/domain/liquidation_reconciliation.py` (new) — `parse_force_
  order_message` (primeiro leitor do campo `o` de `!forceOrder@arr` nesta base; lê `l`, nunca
  `q`/`z`); `coinalyze_daily_liquidation_quantity` (`l+s`); `CoinalizeStreamHypothesis` (4
  membros); `classify_daily_reconciliation`; `reconcile_daily_liquidation`; `RECONCILIATION_
  CAVEAT`; `HYPOTHESIS_SCREEN_LABEL`.
- `backend/src/modules/sentimento/use_cases/reconcile_daily_liquidation.py` (new) —
  `run_daily_liquidation_reconciliation`, tolera linha malformada CONTANDO-A.
- `backend/src/modules/sentimento/infra/liquidation_reconciliation_cli.py` (new) — bancada
  offline (zero rede): lê a quarentena SQLite + `.jsonl` de evidência, emite JSON por dia.
- `backend/src/modules/sentimento/domain/coinalyze_daily_series.py` (modified) —
  `daily_points_from_stored_json` (novo) + refatoração de `parse_daily_points` para reusar
  `_daily_points_from_history_items`. Nenhum comportamento existente mudou (13 testes antigos
  continuam verdes, mais 4 novos).
- `backend/src/modules/sentimento/infra/sqlite_series_quarantine_store.py` (modified) —
  `read_latest()` (novo), sem o filtro `available_at IS NOT NULL` de `read_promoted()`.
- `backend/tests/sentimento/test_liquidation_reconciliation.py` (new, 36 testes)
- `backend/tests/sentimento/test_reconcile_daily_liquidation.py` (new, 3 testes)
- `backend/tests/sentimento/test_liquidation_reconciliation_cli.py` (new, 12 testes)
- `backend/tests/sentimento/test_coinalyze_daily_series.py` (modified, +4 testes)
- `backend/tests/sentimento/test_sqlite_series_quarantine_store.py` (modified, +4 testes)
- `backend/README.md` (modified) — seção `📎 2026-09-02 por T-03.11`.

## 2. Por que dois arquivos de outra task foram tocados

`coinalyze_daily_series.py` e `sqlite_series_quarantine_store.py` são de `T-02.2`. O handoff
exige ler a quarentena Coinalyze DIRETAMENTE ("não é o mesmo caminho que `backtest` usaria… não
tente promover a série aqui"), e nenhum dos dois módulos tinha essa leitura: `read_promoted()`
filtra `available_at IS NOT NULL` (sempre `NULL` para esta task, por desenho de `D2.6`), e
`points_json()` não tinha função inversa. As duas adições são estritamente aditivas — nenhuma
assinatura existente mudou, e `parse_daily_points` foi refatorado para DELEGAR à mesma validação
por item que a nova função usa, não duplicada.

## 3. DoD

- [x] **Reconciliação diária** `Σ(liquidação capturada)` vs agregado `daily` Coinalyze —
      `reconcile_daily_liquidation`, testado em
      `test_liquidation_reconciliation.py::test_reconcile_sums_multiple_orders_same_symbol_same_day`.
- [x] **A ressalva é o requisito central** — `RECONCILIATION_CAVEAT` fixo em toda linha
      (`test_caveat_travels_regardless_of_hypothesis`); `classify_daily_reconciliation` sempre
      devolve hipótese nomeada, nunca `Decimal` solto (12 testes cobrindo as 4 hipóteses e as
      fronteiras `<=`/`>=`); `HYPOTHESIS_SCREEN_LABEL` carrega o texto pronto para a tela.
- [x] **Ler a quarentena diretamente, sem promoção** — `read_latest()` testado por
      `test_read_latest_returns_the_quarantined_row_even_though_available_at_is_null`, que
      falharia se a implementação reusasse por engano o filtro de `read_promoted`.

## 4. Comandos rodados (literais) e resultado

| comando | resultado | universo |
|---|---|---|
| `bash backend/scripts/bootstrap.sh` | `ambiente pronto: Python 3.13.13 … CONFERIDO` | 1 venv novo |
| `bash backend/scripts/lint.sh` | `All checks passed!` / `159 files already formatted` / `Success: no issues found in 159 source files` | 159 arquivos |
| `bash backend/scripts/test.sh -q` | `rc=0`; cobertura total **98,09%**; `check-coverage-layers.sh` → `[OK] domain 99,9%` (1693/1694) `[OK] use_cases 100%` (447/447) `[OK] infra 95,9%` (1598/1667), pisos 90/80/70 | `932 testes coletados` (`pytest --collect-only -q`, contagem por arquivo somada) — **nota**: esta versão fixada (`pytest 9.1.1`) não imprime a linha `"N passed"` com `-q` (reproduzido também com `--no-cov -p no:warnings`, mesmo comportamento em `master@07193e6` antes desta task); `rc=0` do script inteiro é a evidência, pois `set -euo pipefail` aborta antes do piso de camada se o pytest falhar |
| `bash backend/scripts/boundaries.sh` | `Contracts: 3 kept, 0 broken` | 111 arquivos, 485 dependências |
| `bash backend/scripts/natureza.sh` | `universo de 46 arquivo(s), 0 leitura(s) de relogio` | 46 arquivos (domain+use_cases) |
| `harness rules --mode sweep --changed-only` (após `git add`) | `rc=0` | 11 arquivos alterados |
| `harness rules --mode sweep` (completo) | `rc=0` | árvore completa |
| `git push` (com `HARNESS_MECHANISM` explícito) | 3 portões do `pre-push` verdes (`boundaries`/`natureza`/`lint`, backend+frontend); `frontend/node_modules` precisou de `npm --prefix frontend install` (gitignored, ausente em worktree nova) | — |

Arquivos novos/estendidos individualmente, todos **100% de cobertura**
(`liquidation_reconciliation.py` 104/104 linhas·28/28 branches; `reconcile_daily_liquidation.py`
21/21·2/2; `liquidation_reconciliation_cli.py` 82/82·14/14; `coinalyze_daily_series.py`
96/96·28/28; `sqlite_series_quarantine_store.py` 46/45·4/3 — o único ponto não coberto, linha
188, é pré-existente de `T-02.2`, fora do meu diff).

## 5. Mutação — a proteção que não morde não prova nada

Três mutantes, cada um aplicado por `python3` sobre o arquivo real, revertido em seguida e
reconferido por `sha256sum` (idêntico ao original nos 3 casos: `00e12024…`):

| # | mutante | comando de verificação | resultado |
|---|---|---|---|
| 1 | `classify_daily_reconciliation`: `ratio > near_one_upper_bound` → `ratio >= near_one_upper_bound` | `pytest -q tests/sentimento/test_liquidation_reconciliation.py -k classify` | **1 failed** — `test_classify_ratio_at_upper_boundary_is_inclusive` |
| 2 | `classify_daily_reconciliation`: `ratio < near_one_lower_bound` → `ratio <= near_one_lower_bound` | idem | **1 failed** — `test_classify_ratio_at_lower_boundary_is_inclusive` |
| 3 | `reconcile_daily_liquidation`: `if day in seen_days:` → `if False:` (checagem de dia duplicado da Coinalyze desativada) | `pytest -q tests/sentimento/test_liquidation_reconciliation.py -k duplicate` | **1 failed** — `test_reconcile_duplicate_coinalyze_point_for_the_same_day_raises` (`Failed: DID NOT RAISE`) |

## 6. Cobertura

`domain 99,9%` (alvo 90%) · `use_cases 100%` (alvo 80%) · `infra 95,9%` (alvo 70%) — medido por
`bash backend/scripts/test.sh -q` (comando acima), piso por camada verificado por
`check-coverage-layers.sh` sobre `coverage.xml` mais novo que todo `.py` de `src/`.

## 7. Achado, não bloqueante, não meu: `ResourceWarning` pré-existente

`test_sqlite_series_quarantine_store.py` já produz **3 `ResourceWarning: unclosed database`**
sob `-W error::ResourceWarning`, IDÊNTICO em `master@07193e6` (antes desta task) e nesta árvore
`[MEDIDO nas duas árvores]` — três usos pré-existentes de `with sqlite3.connect(...) as
connection:` (fora do meu diff) que não fecham a conexão (o `with` do `sqlite3.Connection` só
comita/reverte, nunca fecha; o padrão correto, que o próprio módulo de produção usa, é `with
closing(sqlite3.connect(...))`). O único ponto novo que abre uma conexão crua nesta task
(`test_read_latest_prefers_the_most_recently_received_row`) já nasceu com `closing()`. Não
corrigido nos três pontos pré-existentes — não é desta fase, e não afeta `rc` (aviso, não falha).

## 8. Doc delta

- `backend/README.md`: **atualizado** — seção `📎 2026-09-02 por T-03.11`, com a tabela de
  arquivos, a explicação campo-a-campo de como o código satisfaz a ressalva, a bancada de
  mutação, e o escopo que a task não fecha.
- `docs/context/plataforma-dados/tasks.toml` / `tasks_review.md:275` /
  `docs/plans/SPEC-001-plataforma-dados/03_captura_continua.md`: **sem mudança** — são os
  ponteiros de DoD que esta implementação satisfaz, não documentos que este papel edita
  (mesma decisão registrada por `T-02.2`).
- `docs/INDEX.md`: **sem mudança** — mesma decisão de `T-02.2` (a mais próxima em forma: só
  código + `README.md`, nenhum doc novo de nível superior).
- ADR: **não necessário** — nenhuma decisão de arquitetura nova; reaproveita o desenho de
  quarentena de `ADR-014`/`T-02.2` e a disciplina de "incremento, nunca total corrente" que
  `cvd.py` já estabelece.

## 9. Entrega

Commit `1d0711a`. Push para
`https://github.com/StharleyMax/cripto-strategy.git tasks/T-03.11-reconciliacao-coinalyze` com
`HARNESS_MECHANISM` explícito — passou nos três portões do `pre-push` (`boundaries`/`natureza`/
`lint`, backend e frontend; `frontend/node_modules` precisou de `npm --prefix frontend install`,
ausente por ser gitignored em worktree nova — o `package-lock.json` regenerado por esse install
foi revertido antes do PR, por não pertencer a este escopo). PR aberta:
https://github.com/StharleyMax/cripto-strategy/pull/67.

## Bloqueado

Nenhum item do DoD desta fase. **Nomeado, não bloqueante:** nenhuma reconciliação real foi
rodada — `docs/medicao-conectividade-forceorder.md` (`T-03.2`) mediu **zero eventos** de
`!forceOrder@arr` chegando a este observador em 85 s combinados, abaixo do gatilho de parada de
`T-03.1` (≥ 120 s × 2). O CLI está testado ponta a ponta contra arquivos reais em `tmp_path`
(sem fake), mas a prova desta rodada é a suíte — não uma saída ao vivo. O limiar "perto de 1"
não tem default: exige a distribuição real que ainda não existe.
