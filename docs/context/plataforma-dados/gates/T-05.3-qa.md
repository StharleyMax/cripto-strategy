# QA Gate — T-05.3 (plataforma-dados, fase 05, charts)

## QA Gate — Fase 05 [charts] — T-05.3 (CST-37) "selo de quatro campos sem hover; içamento em três níveis"

**Branch avaliada:** `origin/tasks/T-05.3-selo-icamento` (checkout local `qa-T-05.3` em `/home/stharley/Documentos/projects/cripto-strategy/.claude/worktrees/wf_2a5f5015-5f1-7`, já que a branch original estava ocupada em outro worktree). Commit avaliado: `393b061897d75bc1acad2b1bdba3fb6c90a3849c`. Diff vs `master` restrito a: `frontend/src/charts/s2-badge.ts` (novo), `frontend/src/charts/s2-badge.test.ts` (novo), `docs/INDEX.md` (+1 linha), `docs/context/plataforma-dados/gates/T-05.3-builder.md` (novo), `frontend/package-lock.json` — `git diff --name-only master...HEAD` `[MEDIDO]`.

- **[OK] core.relative-import / core.silent-except / core.print-statement / web-fullstack.browser-imports-server / web-fullstack.tenant-from-request** — `harness rules --mode sweep --changed-only` → `rc=0`, sem saída `[MEDIDO]`. (`core.*` são `**/*.py`, não alcançam TS — documentado em `harness.toml:119-124`; TS é coberto por `frontend/eslint.config.mjs`, ver item de lint abaixo.)
- **[OK] web-fullstack.server-test-directory-present** — não aplicável a este diff (nenhum arquivo de servidor tocado).
- **[OK] core.hardcoded-secret** — nenhuma credencial no diff (inspeção visual dos dois arquivos novos, ambos puros/sem I/O).
- **[OK] Testes existem e passam** — `node --test src/charts/s2-badge.test.ts` → **17 pass, 0 fail** `[MEDIDO]`. `npm --prefix frontend run test:charts` → **67 pass, 11 fail** `[MEDIDO, log completo em /tmp/.../t053_test_charts.log]`; as 11 falhas são de **outros** módulos (`canonical-grid-sha256-proof`, `s2-axis-integration`, `s2-cvd`, `s2-klines-loader`, `s2-oi-loader`, `s2-panels`), todas `ENOENT` sob `data/` (gitignored, ausente neste worktree) — confirmado pré-existentes porque `git diff --name-only master HEAD -- frontend/src/charts/` só retorna os dois arquivos `s2-badge.*`, então nenhuma outra suíte foi tocada por esta task. Bate exatamente com o relato do builder.
- **[OK] npm --prefix frontend run lint** → `rc=0`, sem output/achado `[MEDIDO]`. Config não é vacua: `eslint.config.mjs` usa `typescript-eslint recommended` + regras `no-explicit-any`/`no-console` pinadas, cobrindo `.ts`/`.tsx`/`.js`/`.jsx`/`.mts`/`.cts`/`.mjs`/`.cjs`.
- **[OK] Mutação independente prova que o teste MORRE** — 2 mutações plantadas em produção (`frontend/src/charts/s2-badge.ts`, revertidas com `git checkout --` logo após, árvore confirmada limpa em ambos os casos):
  1. `countGapRuns` trocado de contagem de RUNS contíguos para contagem naive de `null` (o bug que o próprio builder documentou ter encontrado e corrigido) → `node --test src/charts/s2-badge.test.ts`: **16 pass, 1 fail** — `countGapRuns: 3 ADJACENT missing slots are ONE lacuna, not three` morre com `3 !== 1` `[MEDIDO]`.
  2. Guarda de forma do içamento em `buildHoistedScreen` (`if (panelCells.length !== SCREEN_WIDTH_CELLS) throw ...`) removida, aceitando qualquer tamanho → **16 pass, 1 fail** — `buildHoistedScreen: FALSIFIER — refuses a screen that is not 570×6...` morre com `Missing expected exception (RangeError)` `[MEDIDO]`.
  Ambas as mutações atingem os dois comportamentos centrais da task (completude por lacuna e içamento contratual/`CA-F4-14`) e ambas morreram — nenhum achado bloqueante aqui.
- **[OK] Cobertura** — não há alvo declarado para `frontend`/`charts`: `harness policy --key coverage_target` → `campo ausente na politica` `[MEDIDO]`. Sem alvo numérico, não há piso a reprovar; universo do módulo é 17/17 testes verdes, cobrindo `buildSerieLabel` (1), `buildIdade` (5, incl. 2 falsificadores), `countGapRuns`/`buildCompletudeLabel` (6, incl. 1 falsificador), `buildBadge` (1), içamento/custo (4, incl. 2 falsificadores + 1 medição própria).
- **[OK] DoD da fase (plano `05_fatia_visivel.md` itens 5.3+5.4)**:
  - 5.3 "selo de quatro campos" — os 4 campos (`serie`/`idade`/`procedencia`/`completude`) existem em `Badge`/`buildBadge`, com os formatos literais de `SPEC-001` §6.1 (`"OI · grade 5m · BTC · bn-dump"`, `"285/288 · 1 lacuna"`, `"idade ?"`, `"~5m"`) verificados nos testes 63-147 de `s2-badge.test.ts`.
  - 5.4 "içamento em três níveis" — `SessionEnvelope`/`PanelEnvelope`/`CellEnvelope` reproduzem literalmente a tabela de `ADR-005`/D3 linha a linha (`docs/adr/ADR-005-transporte-de-leitura.md:42-44`: sessão = fuso/agora/modo/versão/env/principal_id; painel = SeriesKey/source/unit/denom/provenance/label_shift/universo/n-lido-n-esperado; célula = valor|ausência/event_time/available_at + referência à coluna). Falsificador de custo (`CA-F4-14`, tela 570×6=3.420) prova estruturalmente 6 vs 3.420 afirmações de identidade — `[MEDIDO]` pelos testes `buildHoistedScreen`/`denormalizeCell`.
  - **Ressalva declarada, não um achado novo**: o item de `tasks_review.md`/`SPEC-001`§6.1 "o selo é lido **sem interação**" / "visível sem hover" é, em sentido estrito, uma afirmação de **renderização/DOM**, e este módulo é puramente `.ts` — `frontend/src/charts/` não tem nenhum ponto de montagem ainda (confirmado: `find frontend/src -iname '*.tsx'` só acha arquivos sob `frontend/src/features/**`, componente `web`, não `charts`). Isto é consistente com o precedente arquitetural já estabelecido e QA-aprovado em `T-05.1`/`T-05.2` (ambas também `.ts` puro, sem `.tsx`, ambas `APPROVED`) e com a separação de papéis em `harness policy --key agents.by_component` (`charts` = `quant-architect`, computação; `web` = `ui-designer`, DOM). O builder nomeou esse limite explicitamente na seção "Fora de escopo". Não é achado bloqueante desta task — é dívida de rendering que pertence à(s) task(s) `web` que primeiro montarem um gráfico, mas fica registrado aqui para quem for QA dessa task futura auditar a promessa "visível sem hover" contra DOM real.
  - Honestidade de número: builder mediu **1,72×** (hoisted 115B / denormalized 198B) sobre schema PRÓPRIO deste módulo, e declarou explicitamente que **não** é o **9,6×**/519B/54B histórico de `ADR-005`/D3 (schema original não está no repo) — conforme `CLAUDE.md` "Nenhum número sem o comando que o produziu". Correto e verificado: `[MEDIDO]` reproduzido no teste `jsonByteLength`.
  - `docs/INDEX.md` — 1 linha acrescentada, append-only confirmado via `git diff master...HEAD -- docs/INDEX.md` (só `+1` linha no fim, nenhuma linha anterior alterada).
- **[anomalia]** nenhuma — todos os comandos exigidos produziram veredito.

Regras bloqueantes avaliadas: 7 de 7 listadas por `harness rules list --severity block`.

Veredito: **APPROVED**

### Comandos-chave rodados (reprodutibilidade)
```
npm --prefix frontend install                     → up to date, 129 packages, 0 vulnerabilities
npm --prefix frontend run lint                     → rc=0
node --test src/charts/s2-badge.test.ts            → 17 pass, 0 fail (rc=0)
npm --prefix frontend run test:charts              → 67 pass, 11 fail (11 pré-existentes, ENOENT sob data/)
harness rules --mode sweep --changed-only          → rc=0, sem achado
git diff --name-only master...HEAD                 → docs/INDEX.md, gates/T-05.3-builder.md,
                                                       frontend/{package-lock.json,src/charts/s2-badge.ts,
                                                       src/charts/s2-badge.test.ts}
```

### Artefatos relevantes (caminhos absolutos)
- `/home/stharley/Documentos/projects/cripto-strategy/.claude/worktrees/wf_2a5f5015-5f1-7/frontend/src/charts/s2-badge.ts`
- `/home/stharley/Documentos/projects/cripto-strategy/.claude/worktrees/wf_2a5f5015-5f1-7/frontend/src/charts/s2-badge.test.ts`
- `/home/stharley/Documentos/projects/cripto-strategy/.claude/worktrees/wf_2a5f5015-5f1-7/docs/context/plataforma-dados/gates/T-05.3-builder.md`
- `/home/stharley/Documentos/projects/cripto-strategy/.claude/worktrees/wf_2a5f5015-5f1-7/docs/plans/SPEC-001-plataforma-dados/05_fatia_visivel.md` (DoD itens 5.3/5.4)
- `/home/stharley/Documentos/projects/cripto-strategy/.claude/worktrees/wf_2a5f5015-5f1-7/docs/specs/SPEC-001-plataforma-dados.md` (§6.1)
- `/home/stharley/Documentos/projects/cripto-strategy/.claude/worktrees/wf_2a5f5015-5f1-7/docs/adr/ADR-005-transporte-de-leitura.md` (D3)
- Log completo de `test:charts`: `/tmp/claude-1002/-home-stharley-Documentos-projects-cripto-strategy/66006931-717a-4206-a5f1-911a52e30b35/scratchpad/t053_test_charts.log`

**Nota operacional:** não executei `harness gate-record` nem `harness tasks resolve` — este agente QA não tem permissão de avançar estado/aprovar gate; o veredito acima é para o orquestrador registrar.

