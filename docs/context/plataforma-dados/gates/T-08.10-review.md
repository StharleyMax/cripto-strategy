[REVIEW GATE — Fase 08: Primitivo `swing_point` em `<Anotacao>` — zero algoritmo, zero limiar, zero "nível"]
Feature: `plataforma-dados`     Componente: `charts`     Task: `T-08.10` (`CST-78`)
Worktree: `/tmp/claude-1002/wt/T-08.10`, branch `tasks/T-08.10-swing-point-anotacao`
Escopo dado pelo despacho: `frontend/src/charts/s2-swing-point.ts`,
`frontend/src/charts/s2-swing-point.test.ts`, `frontend/README.md` §17.
Builder gate: `T-08.10-builder.md` (reconstruído pelo QA, anomalia já registrada — não reaberta aqui).
QA gate: `T-08.10-qa.md` — `APPROVED`.

## Camada 1 — o que o runner mede

```
harness rules list --severity block                                            # n=7, universo total
harness rules --mode file --path frontend/src/charts/s2-swing-point.ts         # rc=0, sem saída
harness rules --mode file --path frontend/src/charts/s2-swing-point.test.ts    # rc=0, sem saída
harness rules --mode sweep --changed-only                                       # rc=0, sem saída (cobre os 3 arquivos do diff, inclusive README.md)
```
7 de 7 regras bloqueantes avaliadas sobre os 2 arquivos de código do delta (README.md não é `code_paths`
— `harness policy --key code_paths` restringe a `backend/src/`, `backend/tests/`, `frontend/src/`).
0 achado nas duas camadas mecânicas. Reexecutei os três comandos nesta sessão — mesmos resultados do QA.

## Camada 2 — arquitetura declarada

**Verificação independente do achado do builder (o ponto de maior risco desta task):**
`docs/adr/ADR-017-deteccao-autonoma-com-auditoria-por-excecao.md:3` declara `**Status:** RASCUNHO
(aprovar é gate do owner)`. Busquei evento `approve` para `ADR-017` no ledger
(`harness pipeline show plataforma-dados | grep -i 017` → vazio) e não achei nenhum. `CLAUDE.md`
("Gates marcados owner… não podem ser feitos por agente") e o protocolo de despacho concordam: um
builder não tem autoridade para ratificar rascunho de ADR construindo os campos que ele propõe. A
decisão de **não** implementar `provenance`, `detector_key`, `review_verdict`,
`structure_definition{break_by,ref_policy,impulse}` está correta — e nomeada, não escondida:
`s2-swing-point.ts:38-50` e `frontend/README.md` §17 ("Conflito registrado com ADR-017") citam o
rascunho por arquivo, citam a frase de status, e argumentam a partir de `refs` (`tasks.toml:1096`,
que cita só `SPEC-001` §3.6 + plano `08` item `8.7`, nenhuma menção a `ADR-017`).

**Confirmação de que `SwingPoint` não vaza nenhum dos campos do rascunho, nem algoritmo/limiar/nível**
(`SPEC-001:282`, citação conferida verbatim em `docs/specs/SPEC-001-plataforma-dados.md:282`, e plano
`08_superficie_e_reprodutibilidade.md:18` + `:48-50`, seção "Não faz": "não desenha zona SMC, não
detecta estrutura"):
- Li `s2-swing-point.ts` linha a linha: `SwingPoint` (interface, L92-97) tem exatamente `primitive`,
  `kind`, `eventTimeMs`, `price` + o que herda de `PriceBoundAnnotation` (`priceSource`, `priceUse`) e
  de `AnnotationIdentity` (`principalId`, `createdAtMs`, lido em `s2-annotation-price-binding.ts` e
  `s2-annotation-identity.ts`, nenhum dos dois tocado pelo diff — `git diff --cached --name-only`
  lista só os 3 arquivos do delta). Nenhum campo de `ADR-017`, nenhum `ThresholdSpec`, nenhuma razão
  de Fibonacci, nenhuma `zone`.
- Rodei os 12 testes eu mesmo (`node --test src/charts/s2-swing-point.test.ts` dentro de `frontend/`):
  **12 pass, 0 fail** — bate com o número citado por QA e builder.
- O falsificador estrutural (`s2-swing-point.test.ts:107-122`) fecha a lista de campos permitidos por
  `Set` e falha se um campo extra aparecer — QA já provou que morde (mutação `fibLevel`, reproduzida
  e revertida no gate de QA, hash conferido). Não repeti a mutação (destrutiva mesmo revertida, e já
  provada); aceitei a evidência do QA porque ela é falsificável e citável, não porque é afirmação.

**Escopo/refs:** `tasks.toml:1090-1097` (`refs = ["SPEC-001 §3.6", "plano 08 item 8.7", …]`) bate com
o que o módulo de fato implementa. `docs/INDEX.md` não foi tocado neste worktree (`git status --short
docs/INDEX.md` vazio) — consistente com a convenção "append-only, uma linha por task, escrita depois
do ciclo completo" que o README §17 cita.

## Anomalias do QA — não reabertas, só confirmadas como não-bloqueantes

1. Gate do builder ausente no momento em que o builder terminou (reconstruído pelo QA) — desvio de
   processo, nenhuma das 7 regras exige o artefato, não afeta este veredito.
2. Contagem de falhas pré-existentes divergente (33 do builder vs. 16 do QA vs. 0 no repo principal)
   — raiz comum (ENOENT sobre `data/`, não versionado), zero relação com os arquivos deste delta
   (nenhum dos arquivos que falham está no diff). Não reproduzi essa contagem de novo; não muda
   veredito porque nenhuma regra listada depende dela.

## Veredito

**COMPLIANT.**

Denominador: **7 regras bloqueantes em vigor** (`harness rules list --severity block`), **7 de 7
avaliadas**, **0 violação**. **2 arquivos de código varridos** (`s2-swing-point.ts`,
`s2-swing-point.test.ts`, via `harness rules --mode file` + `--mode sweep --changed-only` sobre os 3
arquivos do diff). Camada 2 (arquitetura declarada): **1 achado citável verificado** — o não-uso dos
campos de `ADR-017` — e ele é **correto**, não uma violação: `ADR-017` está em `RASCUNHO`, sem evento
`approve` no ledger, e a task nomeia o conflito em vez de resolvê-lo por adivinhação.

```
harness gate-record plataforma-dados 08 REVIEW COMPLIANT "T-08.10: 7/7 regras bloqueantes OK (rc=0 em file×2 + sweep --changed-only); SwingPoint fecha zero-algoritmo/limiar/nivel por leitura + falsificador estrutural (mutacao ja provada pelo QA); campos de ADR-017 (RASCUNHO, sem approve no ledger) corretamente NAO implementados, conflito nomeado em codigo e README §17; 12/12 testes proprios reexecutados nesta sessao"
```
