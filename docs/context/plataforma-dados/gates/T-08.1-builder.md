# Gate report — T-08.1 (builder), CST-69

**Fase 08, componente `docs`. Papel: builder, self-attested — NÃO é QA/review.**

## Escopo

`T-08.1`: "[docs] 08 · Spike do motor de armazenamento, com os 5 critérios declarados
ANTES de rodar", `ADR-002`/D4, `CA-F4-24`, `plano 08 item 8.1`, `DoD D8.21`. Os cinco
critérios já estavam pré-registrados em `ADR-002` (tabela de `D4`) e em `D8.21`; o trabalho
desta task era rodar o experimento e produzir o artefato de decisão.

## O que foi feito

1. **Dado real, não sintético.** `data/` está ausente deste checkout (gitignored, dívida
   de disco por máquina). `docs/spike/T-08.1-motor-armazenamento/fetch_data.py` puxa Open
   Interest real da API pública da Binance Futures (sem key) — 4 símbolos × 30 dias × 5 min,
   34.496 linhas `[MEDIDO 2026-09-04]`.
2. **Dataset bitemporal + 3 classes de veneno** (`SPEC-001` §5.1) injetadas por
   `build_dataset.py`, com a referência esperada calculada em **Python puro**, sem
   compartilhar código com nenhum dos dois motores — "dois caminhos independentes",
   disciplina de `D8.1`.
3. **Candidato 4**: `timescale/timescaledb:2.17.2-pg15` (proxy documentado da instância
   `postgres:15` real — sem acesso a ela neste ambiente), hypertable comprimida, `schema.sql`.
4. **Candidato 5**: Parquet particionado (`symbol`, `fonte`), zstd nível 19, via
   `write_parquet.py`; consultado por DuckDB local (proxy do R2 — ver lacuna abaixo).
5. **Bateria de verificação** (`verify_timescale.py`, `verify_duckdb.py`) roda o scan de
   backtest cronometrado e as 3 classes de fixture contra os DOIS motores, comparando
   bit-a-bit com a referência Python.
6. **ADR-002 emendada** (append, nada reescrito) com a decisão, a tabela de resultado por
   critério, e as três lacunas nomeadas sem disfarce.

## DoD

- [x] Os 5 critérios de `D8.21` declarados **antes** de rodar — já estavam em `ADR-002`/D4 e
      em `D8.21`, confirmado por leitura antes de qualquer comando de experimento.
- [x] `espaço ≤ 2× o zipado da fonte` — candidato 4 **1,506×**, candidato 5 **1,989×**
      (comando: `du -sb` sobre `hypertable_size()`/diretório Parquet, ver README).
- [x] `varredura de 30d × 4 símbolos com as_of ≤ 60s` — candidato 4 **0,05–0,09s** (5
      execuções), candidato 5 **0,01–0,02s** LOCAL (sem a rede do R2 — ver bloqueio).
- [x] `fixture envenenada (3 classes) passa` — bit-idêntica à referência Python nos dois
      motores, n=30 (classe a) + 30 (classe b, 2 modos) + 863 (classe c).
- [x] `free -m e df -h MEDIDOS` — já entregue por `T-08.1`/refs (VPS: 5.219 MB RAM
      disponível, 75 GB disco livre); este spike mediu o footprint de CADA candidato contra
      esse teto (container 153,8 MiB RAM / ~900 KB disco; DuckDB ~105 MiB RAM pico).
- [ ] `latência de rede medida (candidato 5)` — **`[NÃO MEDIDO]`**, nomeado como bloqueio.

## Comandos rodados (literais) e resultado

Todos em `docs/spike/T-08.1-motor-armazenamento/README.md`, seção "Passo a passo
(reprodutível)" — não repetido aqui por inteiro (protocolo de despacho R3: sem `cat`/colagem
de corpo grande). Resumo com universo:

- `python3 fetch_data.py --symbols BTCUSDT,ETHUSDT,SOLUSDT,BNBUSDT --days 30 --out-dir ./raw`
  → 34.496 linhas reais (universo: 4 símbolos × ~8.624 pontos/símbolo)
- `python3 build_dataset.py …` → 35.419 linhas (34.496 + 923 injetadas)
- `psql … -c "SELECT hypertable_size('market_series');"` → 909.312 bytes
- `du -sb built/parquet_z19` → 1.200.986 bytes
- `python3 verify_timescale.py` → `OVERALL: PASS` (5/5 blocos, universo: 1 scan + 3 classes)
- `python3 verify_duckdb.py` → `OVERALL: PASS` (mesmo universo, mesma referência)
- `harness rules --mode sweep --changed-only` → `rc=0`, saída vazia (universo: 9 arquivos
  alterados/novos, todos fora de `code_paths.include_prefixes`)

## Cobertura

Não aplicável no sentido de cobertura de teste de código — `docs` não tem `test_cmd`
declarado (`harness policy --key test_cmd` só cobre `sentimento`). A "cobertura" aqui é dos
5 critérios do DoD: **4 de 5 medidos e passando**, 1 nomeado como não-medível neste ambiente.

## Doc delta

- `docs/adr/ADR-002-motor-de-armazenamento.md`: **atualizado** — linha de status +
  emenda `## ✅ Emenda 2026-09-04` (append, nada reescrito), motivo: resolve `D4`.
- `docs/spike/T-08.1-motor-armazenamento/`: **novo** — scripts + README, o comando que
  produz cada número desta emenda.
- `docs/INDEX.md`: **atualizado** — linha nova (append-only).
- ADR novo: **não necessário** — `ADR-002` já é o foro desta decisão (`D4` estava
  explicitamente "pendente de spike" nela); emendar é o padrão já usado por esta mesma ADR
  em 2026-08-29.

## Bloqueado

**`latência de rede real ao R2 (candidato 5)` — `[NÃO MEDIDO]`.** Duas lacunas de acesso:
(1) sem `.env`/credencial R2 neste checkout; (2) sem SSH à VPS (São Paulo) — este ambiente
mediu de Curitiba/PR (`curl -s ipinfo.io`), residencial, não a VPS. Um proxy informativo
(TLS handshake + TTFB ao domínio `r2.cloudflarestorage.com`, 64–107ms, 5 amostras) foi
registrado mas rotulado como não-substituto — não fecha o falsificador `FA-2` desta ADR.

**Decisão tomada apesar do bloqueio, não escondendo-o**: candidato 4 passa nos 5 critérios
medidos; candidato 5 passa em 4 de 5, com o 5º nomeado como não-medível aqui, não como
reprovado. A emenda de `ADR-002` registra o critério de reabertura: medição de rede feita
DA VPS contra o bucket real.

**Achado lateral (não bloqueio, registrado para quem operar candidato 4 em produção)**: a
config inicial do candidato 4 (`chunk_time_interval = 1 day`) comprimia para ~4,8× — teria
reprovado o critério de espaço. Corrigida para 45 dias + `segmentby = symbol,fonte`, caiu
para 1,506×. Documentado em `schema.sql` e no `README.md` do spike.
