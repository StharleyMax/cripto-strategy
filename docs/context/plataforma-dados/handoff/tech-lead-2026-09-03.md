# Handoff ao `/tech-lead` — as tasks da superfície servida, 2026-09-03

**Ato pedido:** narrativa de review PRIMEIRO, aprovação do owner, e só então materializar tasks no
`tasks.toml` e no Jira (`CST`). **Nada de `gate-record`, `approve` ou `advance`.**

`harness pipeline state plataforma-dados` → **`BUILD_AUTHORIZED`** `[MEDIDO 2026-09-03]` ⇒ acrescentar
task **não** exige mudança de estado. Nenhuma task `done` é reaberta.

## As fontes, e elas são normativas — leia antes de decompor

| o que | onde |
|---|---|
| itens novos da fase `05` | `docs/plans/SPEC-001-plataforma-dados/05_fatia_visivel.md` — **`5.13`–`5.17`** + DoD `D5.13`, `D5.13b`, `D5.13c`, `D5.13d`, `D5.14`, `D5.15`, `D5.16`, `D5.16b`, `D5.17` |
| as 7 decisões de owner | `docs/decisoes-do-owner.md`, **todas as seções de 2026-09-03** |
| transporte | `docs/adr/ADR-005-transporte-de-leitura.md` §`D5`/`D6` (emenda de hoje) |
| forma do backend | `docs/adr/ADR-009-...md` §`D6` (`D6.0`–`D6.5`, `F-D6-1..6`) |
| o parecer que originou tudo | `docs/context/plataforma-dados/gates/consulta-fronteira-web-2026-09-03.md` |

## O escopo, e ele tem TRÊS origens distintas — não as achate numa fase só

**(a) Fase `05`, itens `5.13`–`5.17`** — componentes `infra` (`5.13`) e `web` (`5.14`–`5.17`).

**(b) Fase `01`, item `1.13`** — `A6`: `web` ganha `architect` próprio; a dupla
`frontend_builder`/`frontend_qa` é **portada** de `anything_monorepo/.github/agents/`; o
`ui-designer` **mantém o `design_gate`**. `[DECISÃO-OWNER: 2026-09-03]`.
**O `/architect` mediu que os itens `1.12`/`1.13` NUNCA tiveram task** — `T-01.1`..`T-01.7` são
**7 de 7 `done`** `[MEDIDO 2026-09-03]`. Logo é task nova sobre item que já existe, **não item novo**.

**(c) Fase `09`, task `T-09.4`/`CST-86`, ANTECIPADA** — `status = "todo"` hoje. É ela que materializa
`harness policy --key components` de **6 para 7** (`infra`) e registra `ADR-009/D5` como **adotada**.
**Nenhum item de plano novo** — o item `9.6` já existe e é exatamente isto.

## ⛔ A ordem entre (b), (c) e o resto é OBRIGATÓRIA, e o motivo é medido

1. **(c) primeiro:** `V-16` **reprova componente fora do enum** (`lib/policy.py:539-543`). O item
   `5.13` **declara `infra`**, então uma task de `5.13` criada antes de `infra` existir no enum
   **reprova a política**.
2. **o juiz depois do enum:** criar `.claude/agents/infra-architect.md` e a chave
   `[agents.by_component.infra]`. `[DECISÃO-OWNER: 2026-09-03]` — é **criação, não porte**; nenhum dos
   12 agents do `anything` é de infra `[MEDIDO]`.
3. **(b) antes das tasks de `web`:** sem `A6`, as tasks novas de `web` nascem julgadas pelo
   `ui-designer`, que é exatamente o defeito que `A6` corrige.

## O que NÃO fazer, e cada linha aqui é um defeito que já aconteceu neste ciclo

- **NÃO reabra nada em `done`.** `git diff --numstat` do plano `05` → **`171 0`**, e de `ADR-009` →
  **`119 0`** — **zero deleção**, `5.1`–`5.12`/`D5.1`–`D5.12` byte-idênticos. Mantenha essa propriedade.
- **NÃO crie item de plano.** Isso é ato do `/architect`, e os itens já existem.
- **NÃO decida `A1`–`A3`** (paridade dos 9 ports Python→TS) — dono `quant-architect`,
  gatilho de `ADR-003/D2` já disparado.
- **NÃO decida a partição de `sentimento`.** O `quant-architect` está escrevendo
  `gates/estudo-particao-sentimento-2026-09-03.md`; a escolha é do owner e **não bloqueia** estas tasks.
- **NÃO invente a rota de `deploy/`.** Lacuna declarada e sem item: `code_paths.include_prefixes` tem
  **3** entradas e `ls -d deploy` → **inexistente** `[MEDIDO]`. Rótulo não é cobertura.

## Restrições de conteúdo das tasks

- Cada task carrega **o DoD do item** como critério, com **o comando** que o prova. `D5.13` exige
  prova **pela rede** (processo ASGI em loopback + `GET` real por `http.client`), **nunca** por
  subprocesso.
- **`D5.13c` é restrição dura:** o handler chama `ingest_health_query(source)`
  (`use_cases/ingest_health.py:32`), **zero SQL próprio** — senão é *"segunda definição"* de
  `ADR-008/DoD-1`. Dona da pergunta: `ADR-008/D4`.
- **`5.15`** tira `frontend/src/features/s1-console/ingest-health-query.ts` do universo de produção:
  **209 de 500 linhas (41,8%)** saem, e o módulo vira teste de contrato ou morre — o item diz qual.
- **Auth:** `[DECISÃO-OWNER: 2026-09-03]` a rota **não é exposta à rede pública** (loopback; o proxy é
  a borda, como em `anything/deploy/compose.prod.yml:162-163` → `127.0.0.1:8000:8000`).
  **`5.11` continua REBAIXADO** — não o reabra.
- **O `fingerprint` NÃO é exibido na tela** (o operador não vê o `sha256`) ⇒ `D5.17` fica **síncrono**.
  Isto foi resolvido pela emenda `D6.3`/`D6.4`, não por decisão sua.

## Jira

Tracker `CST`, `parent_kind = "Epic"`, `child_kind = "Tarefa"` (`harness.toml:59-84`). Epics:
`CST-3` (F1 = fase `05`), `CST-1` (F5a = fase `01`), `CST-7` (F5b = fase `09`).
**Já espelhado por comentário hoje:** `CST-3` (reabertura + `A4`) e `CST-1` (`A6`).
⚠️ O Jira converte markdown para ADF e **degrada** — cabeçalho e negrito viram texto plano, e par de
`_` no mesmo identificador vira itálico. **Escreva descrição em texto plano**, não em markdown rico.

## Devolução

**Narrativa de review para o owner PRIMEIRO** — nenhuma task criada antes do aval dele. Depois,
no máximo **15 linhas**: as tasks propostas por id, a ordem, e o caminho da narrativa.
