# Handoff — T-03.6 · `availability_probe` contínuo, `availability_probe_set` DECLARADO

**Card:** `CST-22` · **Componente:** `sentimento` · **Base:** `master@2fea41e` · **Worktree:** `/tmp/claude-1002/wt/T-03.6`

## Por que agora

`Q19` foi respondida pelo owner em 2026-09-02 (`docs/decisoes-do-owner.md` §Q19) — destrava esta task.
Escopo: **mecanismo** do probe contínuo + prova local curta (poucos minutos, não deploy 24/7). O owner
foi explícito: VPS fica de fora por enquanto, execução local primeiro.

## O `availability_probe_set` — DECLARADO, não invente outro

- **Símbolos (4, não o universo):** `BTCUSDT`, `ETHUSDT`, `LINKUSDT`, `SOLUSDT`.
- **Endpoints Binance:** os 5 de `/futures/data/*` (mesmo balde de 200 req/min).
- **Endpoints Coinalyze:** incluídos nesta rodada — orçamento PRÓPRIO e CEGO (40 req/min, resposta
  `200` não traz header de cota — a contagem tem de ser local/conservadora, igual ao broker de
  `T-02.2`, reusa o padrão se fizer sentido).
- **Resolução:** 10s para os endpoints Binance — `5 endpoints × 4 símbolos × 6/min = 120 chamadas/min`
  (60% da cota de 200/min). Não invente uma resolução para a Coinalyze: declare uma cadência
  conservadora dentro do balde de 40/min e documente o cálculo, não copie o número da Binance sem
  refazer a conta.

## O DoD

Bloco `id = "T-03.6"` em `docs/context/plataforma-dados/tasks.toml`. Refs: `CA-F0-9`, `CA-F0-3`, plano
`03` itens **3.4+3.9**. Linha completa em `docs/context/plataforma-dados/tasks_review.md:270`.

- **D3.3:** `5 × S × (60/período) ≤ 200 req/min` — **`período ≥ 60s` REPROVA o falsificador desta DoD**
  (60s é mais grosso que a dispersão de latência já medida, 99,6–200,8s — não informa). Com `S=4` e
  período de 10s você está bem dentro da cota (120/200), e bem abaixo do teto de 60s.
- **D3.2:** tabela de defasagem com `p99` por `(endpoint, observer_region)` **e o `n` ao lado** — mais
  `lag_stat`, `lag_n`, `lag_resolution_s`, `lag_window` **como colunas** do resultado (não só prosa).
  `≥ 5 endpoints` — hoje o probe-medição anterior tinha `n=2`, 1 símbolo, janela de 10 min, dispersão
  de 55% `[MEDIDO]`; esta task tem que cobrir os 5 endpoints Binance + os da Coinalyze, para os 4
  símbolos declarados.
- **D3.4:** a razão `OBSERVED/total` é **exibida, não estimada** — todas as linhas da janela de
  medição, não uma amostra.
- `observer_region` **NÃO** é escopo desta task (é `T-03.9`, que segue `blocked` por depender da VPS,
  fora por decisão do owner). Se o schema de saída tiver uma coluna `observer_region`, ela pode ficar
  `NULL`/`local` — não trave esta task nisso.

## Contexto que já custou defeito aqui

- **Capture-or-lose, mas não confunda com "precisa rodar para sempre agora":** o mecanismo tem que
  existir e provar que mede `lag_ms` corretamente; a task PROVA com uma rodada curta local (minutos),
  não com dias de captura contínua — isso é trabalho para quando o serviço for de fato implantado.
- **Coinalyze é balde CEGO** (`T-02.2`/`T-03.11` já mediram isso): nenhum header de cota no `200`,
  contagem tem que ser local/conservadora. Não assuma que dá para "ler a resposta" para saber quanto
  falta de cota.
- **`T-03.9` depende desta task** para o probe completo, mas continua bloqueada (VPS). Não implemente
  `observer_region` de verdade aqui — é escopo de outra task, fora de alcance.
- Contrato `Natureza` (`ADR-016`): chamada de rede mora em `infra`, nunca em `domain`/`use_cases`.

## Restrições do repositório (as mesmas de toda task desta sessão)

- Import-linter (`Natureza` incl.), cobertura domain 90/use_cases 80/infra 70%, idioma em inglês,
  `docs/INDEX.md` append-only, sem `Co-Authored-By:`.
- Push em worktree: `HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness`
  (nunca `.harness/mechanism`), URL explícita
  (`https://github.com/StharleyMax/cripto-strategy.git`, não `origin`/SSH), **nunca
  `--force-with-lease`** — use `--force` simples se precisar.
- **Ao rodar `bash backend/scripts/test.sh`, aguarde ATIVAMENTE o comando terminar antes de encerrar
  seu turno** — não dispare em background e diga "vou esperar a notificação": isso não funciona entre
  turnos de subagente nesta sessão (medido repetidamente). Continue chamando ferramentas até confirmar
  via novo comando que o processo já terminou.
- **A fase 03 já está fechada no ledger (`QA=APPROVED`/`REVIEW=COMPLIANT`) com `T-03.6` reaberta como
  `todo`** — isso é uma inconsistência TEMPORÁRIA e intencional (documentada, não um bug seu): assim
  que esta task fechar QA+Review, o orquestrador vai rodar `harness tasks resolve` para fase 03 de
  novo, reincluindo `T-03.6=done`. Não tente "consertar" isso você mesmo.

## Protocolo de custo — R6/R7

- Se projetar passar de ~150 turnos, PARE e escreva o estado neste arquivo.
- Devolva **no máximo 15 linhas**, caminho do relatório em
  `docs/context/plataforma-dados/gates/T-03.6-builder.md`. **Nunca cole o corpo.**

## Entrega

Commit no branch `tasks/T-03.6-availability-probe-set`, push, PR aberta contra `master`.
