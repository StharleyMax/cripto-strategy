# Handoff — T-06.10 · S3 — inspetor de série: catálogo filtrável + `src_label_raw` ao lado de `event_time`

**Card:** `CST-54` · **Componente:** `web` · **Base:** `master` (faça `git fetch` antes de criar a worktree — várias tasks da fase 06 rodam em paralelo) · **Worktree:** `/tmp/claude-1002/wt/T-06.10`

## Por que agora

`T-06.1` (`series_catalog`) e `T-05.8` (ambas dependências) já `done`/mescladas. Owner autorizou puxar a fase 06 inteira em paralelo (2026-09-03). Esta é a ÚNICA task de frontend da fase 06.

## ⚠️ Esta é uma TELA REAL, diferente de S5 (`T-07.14`)

`docs/product/STITCH_CONTEXT.md:485` cataloga **S3 — inspetor de série · `[NÃO EXISTE]` · fase 06** como uma das 5 telas do produto (linha 132: `S3 inspetor de série — o que este número é, e quais linhas exatas o produziram`). Isso é diferente de `T-07.14`/S5, que era explicitamente "embutido, não é tela". `CLAUDE.md` §"Design — autonomia delegada, com gate de validação": o `ui-designer` decide UI/UX **sem pedir permissão**, mas **nenhuma decisão de design vale antes de `ux-ui-mastery` concordar** — não é revisão opcional, é a condição da autonomia.

**Antes de implementar**, verifique se já existe `docs/context/plataforma-dados/gates/T-06.10-design.md`. Se não existir, o passo de design (via `ui-designer`, operador do Stitch) precisa acontecer primeiro — não pule direto para código de UI com aparência significativa.

## O DoD

Bloco `id = "T-06.10"` em `docs/context/plataforma-dados/tasks.toml`. Refs: `SPEC-001 §6`, plano `06` item **6.16**, DoD **D6.15**. Linha completa em `docs/context/plataforma-dados/tasks_review.md:325`.

- **Catálogo FILTRÁVEL**: a tela lista o `series_catalog` (de `T-06.1`, mais os valores reais que `T-06.2`–`T-06.9` populam — pode não estar tudo pronto ainda; a tela funciona sobre o que o catálogo tiver no momento) e permite filtrar por série.
- **D6.15**: abrir as linhas CRUAS de uma série ⇒ `src_label_raw` aparece **NA MESMA LINHA** que `event_time` (não em uma tela/tabela separada que exige correlacionar manualmente), mais as lacunas de `md.ingest_gap` com `n_missing`. A tela responde *"o que este número é, e quais linhas exatas o produziram"*.
- **Inclui a gaveta de quarentena** (`docs/product/STITCH_CONTEXT.md:1645`) — séries em quarentena (de `T-06.6`, que está sendo construída em paralelo — pode não estar pronta ainda; trate a ausência como "quarentena vazia por enquanto", não como erro).
- **NÃO reconcilia automaticamente**: uma divergência entre fontes é EXIBIDA como divergência — nunca corrigida silenciosamente antes de mostrar. Uma fonte que "corrige antes de gravar" destrói a evidência de que havia o que corrigir.

## Contexto que já custou defeito aqui

- Reuse `series_catalog.py`/`SeriesCatalog`/`SeriesCatalogEntry` (Python, `T-06.1`) como referência de schema — se o frontend precisar de uma cópia TS do contrato (mesmo padrão de `T-07.14`, que portou `universe_at.py` para `frontend/src/app/universe-at.ts`), leia o Python e porte, não invente uma forma diferente.
- `backend/` é intocável a partir desta task de frontend (mesma regra de `T-07.14`).
- Se não houver app Next.js real rodando ainda para esta tela (confira o que `T-05.8`/`T-05.9` já deixaram), siga a mesma fronteira que `T-05.8`/`T-08.5`/`T-08.11` usaram: prova por teste unitário sobre a lógica/componente, documentando explicitamente a lacuna de app real no relatório — não é dívida silenciosa se for nomeada.

## Restrições do repositório (as mesmas de toda task desta sessão)

- `docs/INDEX.md` append-only, sem `Co-Authored-By:`, idioma em inglês em identificadores de produção/teste.
- Push em worktree: `HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness`, URL explícita, **nunca `--force-with-lease`**.
- **⚠️ Múltiplas sessões e múltiplas tasks desta MESMA fase rodam em paralelo agora** — sempre `git fetch` + resolva contra `master` mais recente antes do push; conflito típico em `docs/INDEX.md`/`backend/README.md` — mantenha AMBAS as seções.

## Protocolo de custo — R6/R7

- Se projetar passar de ~150 turnos, PARE e escreva o estado neste arquivo.
- Devolva **no máximo 15 linhas**, caminho do relatório em `docs/context/plataforma-dados/gates/T-06.10-builder.md` (e `T-06.10-design.md` se você mesmo fizer o passo de design). **Nunca cole o corpo.**

## Entrega

Commit no branch `tasks/T-06.10-fase06`, push, PR aberta contra `master`.
