# Handoff — T-03.5 · Coletor `premiumIndex` (funding estimado)

**Card:** `CST-21` · **Componente:** `sentimento` · **Base:** `master@634833b` · **Worktree:** `/tmp/claude-1002/wt/T-03.5`

## Por que agora

`Q1` respondida (2026-09-01). `T-01.1` (dependência) já `done`. Escopo: código + probe curto, não
deploy contínuo (sem frontend ainda, decisão do owner).

## O DoD

Bloco `id = "T-03.5"` em `docs/context/plataforma-dados/tasks.toml`. Refs: `CA-F0-1b`, plano `03`
item **3.3**. Linha completa em `docs/context/plataforma-dados/tasks_review.md:269`.

- Coletor `premiumIndex` — funding **ESTIMADO**, que **não tem endpoint de histórico em fonte
  nenhuma**. A série só existe a partir do dia em que o coletor liga, nunca antes — não tente
  backfillar.
- **Batch confirmado por medição:** `premiumIndex` **sem `symbol`** devolve os **875 símbolos por
  peso 10** contra `REQUEST_WEIGHT 2400/min` — chame o endpoint SEM filtro de símbolo, é o jeito
  barato de pegar o universo inteiro numa chamada.
- Zero API key (endpoint público da Binance Futures).

## Contexto que já custou defeito aqui

- `T-02.1` (rodando em paralelo, mesma rodada) também lê `premiumIndex` **como segunda testemunha**
  do snapshot diário de `exchangeInfo`/`fundingInfo` — **não é a mesma coisa que esta task**: aqui é
  o coletor CONTÍNUO da série efêmera (funding estimado ao longo do tempo); lá é uma leitura pontual
  dentro do snapshot diário. Não funda os dois, mas também não duplique o cliente HTTP do endpoint —
  se ambas as branches criarem um client Binance separado, isso é esperado (ficam em módulos
  diferentes); um /review posterior pode apontar consolidação, não é bloqueante agora.
- `premiumIndex` estimado é **fundamentalmente diferente** de `fundingRate` realizado — não
  confunda os dois campos no schema.

## Restrições do repositório (as mesmas de toda task desta sessão)

- Import-linter (`Natureza` incl.), cobertura domain 90/use_cases 80/infra 70%, idioma em inglês,
  `docs/INDEX.md` append-only, sem `Co-Authored-By:`.
- Push em worktree: `HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness`
  (nunca `.harness/mechanism`), URL explícita, **nunca `--force-with-lease`**.

## Protocolo de custo — R6/R7

- Se projetar passar de ~150 turnos, PARE e escreva o estado neste arquivo.
- Devolva **no máximo 15 linhas**, caminho do relatório em
  `docs/context/plataforma-dados/gates/T-03.5-builder.md`. **Nunca cole o corpo.**

## Entrega

Commit no branch `tasks/T-03.5-premiumindex`, push, PR aberta contra `master`.
