# Handoff — T-04.1 · Shift canônico de `event_time` + ordenação obrigatória

**Card:** `CST-28` · **Componente:** `sentimento` · **Base:** `master@840c500` · **Worktree:** `/tmp/claude-1002/wt/T-04.1`

## Por que esta task é a próxima liberada

Das 16 tasks do escopo `build` autorizado (`docs/context/plataforma-dados/decisoes-de-execucao-2026-08-28.md`
§1.1), 11 já estão mergeadas em `master` — incluindo `T-03.1`, `T-03.7`, `T-03.10` e `T-04.4`, cujo
`status` em `tasks.toml` ainda diz `"todo"` por bookkeeping atrasado (mesma classe de anomalia que o
dashboard já flagra na fase 02 — **não reabra essas quatro, cheque o gate/PR antes de assumir pendência**).
As 5 genuinamente não iniciadas são todas da fase `04`: `T-04.1`, `T-04.3`, `T-04.5`, `T-04.6`, `T-04.7`.
`T-04.1` é a única sem dependência pendente além de `T-01.1` (`done`) e é pré-requisito de `T-04.3` e
`T-04.6` — por isso vai primeiro.

## O DoD

Bloco `id = "T-04.1"` em `docs/context/plataforma-dados/tasks.toml`. Refs declaradas: `SPEC-001 §2.2` ·
`CA-F1-1` · `CA-F1-14` · `CA-F1-2` · plano `04` itens **4.1 + 4.2 + 4.4** · DoD **D4.1, D4.2, D4.3, D4.10**
(`docs/plans/SPEC-001-plataforma-dados/04_contrato_temporal.md`).

- **4.1** `event_time = create_time + 300000` aplicado **uma vez** às oito colunas, com `src_label_raw`
  gravado ao lado.
- **4.2** ordenação obrigatória do arquivo inteiro antes de emitir evento — bypassar o sort **reprova**.
- **4.4** `md.ingest_gap` persistido, **lacuna nunca preenchida por interpolação**.

Fixtures em `data/` (offline, zero rede, zero API key): `BTCUSDT-metrics-2026-08-18.csv`
(md5 `b8ef79c353f2adce853c68084cc3b631`, D4.1: 13/30 dias fora de ordem) e o dia `2026-08-12`
(md5 `bf1ddd8ba4248f975e92daae23ee3dc3`, D4.2: 285 linhas, 1 gap com `n_missing=3`).

## Contexto que já custou defeito aqui

- `T-04.3` (unicidade por `agg_id`) e `T-04.6` (R-1/R-2) `depends_on = ["T-04.1"]` — modelar o shift e
  a ordenação de um jeito que essas duas não consigam consumir depois quebra a task seguinte em
  silêncio, do mesmo jeito que `T-04.2`→`T-04.4` já mordeu.
- **D4.3** é sobre o carimbo do FECHO do bucket (`00:05:00Z`, nunca `00:00:00Z`) — não é só somar
  300000 ms, é confirmar que o resultado aponta para o fim do intervalo, não o início.

## Restrições do repositório que mordem aqui

- **Contratos `import-linter`** (`backend/pyproject.toml [tool.importlinter]`): `domain`/`use_cases`
  não importam `socket`, `ssl`, `time`, `datetime` (contrato `Natureza`, `ADR-016`). Tempo entra como
  valor injetado.
- **Pisos de cobertura:** domain 90%, use_cases 80%, infra 70%.
- **Idioma:** identificador de produção e de teste em **inglês**, inclusive parâmetro/variável
  local/constante de módulo (`CLAUDE.md` linhas 1-2). Exceção viva: nome de coluna de contrato em
  português (linha 11). Mensagem de `raise`: lacuna `⏸ NÃO DECIDIDO` — não é licença para português novo.
- **Evento de log e chaves de `extra={}`: inglês, prospectivamente** (linha 10) — hoje 4/9 em
  português, o número **não pode subir**.
- **`docs/INDEX.md` é append-only.**
- **Nenhum número sem o comando que o produziu**, com `n` e rótulo de força.
- **Sem `Co-Authored-By:`.** Autor e committer são o owner.

## Protocolo de custo — vinculante, R6/R7 (`docs/protocolo-de-despacho.md`)

- **`make verify`** roda os seis portões numa chamada, ~10 linhas impressas, saída bruta em arquivo.
- **Se projetar passar de ~150 turnos, PARE**, escreva o estado neste arquivo e devolva.
- Agrupe medições independentes numa chamada só; não releia arquivo já lido.
- Devolva **no máximo 15 linhas**: veredito, números com o comando, e o caminho do relatório completo
  em `docs/context/plataforma-dados/gates/T-04.1-builder.md`. **Nunca cole o corpo.**

## Entrega

Commit no branch `tasks/T-04.1-shift-canonico-event-time`, PR aberta contra `master`.
