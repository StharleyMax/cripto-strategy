# Handoff — T-04.7 · `principal_id` como dimensão obrigatória

**Card:** `CST-34` · **Componente:** `sentimento` · **Base:** `master@840c500` · **Worktree:** `/tmp/claude-1002/wt/T-04.7`

## Por que esta task agora, em paralelo com QA/Review de T-04.5

`T-04.7` depende só de `T-04.2` (`done`, já em `master`) — **não** de `T-04.1`. Roda em paralelo sem
base incerta com `T-04.5` (PR #46, em QA/Review agora) nem com `T-04.1` (PR #45, aprovado mas ainda
não mesclado — merge é ato do owner). `T-04.3` e `T-04.6` ficam de fora deste lote porque
`depends_on = ["T-04.1"]`, só `done` depois do merge do PR #45.

## O DoD — e uma particularidade real, não erro de leitura

Bloco `id = "T-04.7"` em `docs/context/plataforma-dados/tasks.toml`. Refs: `SPEC-001 §4.4` ·
`Q2 (RESPONDIDA)` · plano `04` item **4.11** · `ADR-009/D2` recusa `organization_id`.

**Não há `D4.x` citado nas refs desta task** — ao contrário de todas as outras da fase. A própria
lista de refs diz **"verificado em 05 por D5.10"**: o falsificador formal de `principal_id` mora no
DoD da fase `05`, não da `04`. Isso não é uma DoD ausente por descuido — é a task construindo a
**dimensão** (o campo existe, é obrigatório, nunca `NULL`, nunca constante implícita) enquanto a
fase que a **usa** end-to-end (autenticação/ator humano) é a `05`. Não invente um `D4.x` para esta
task nem tente satisfazer `D5.10` agora — ele pertence à fase seguinte e provavelmente depende de
código que `05` ainda vai escrever.

**O que testar aqui, então:** que toda linha de ato humano no domínio `sentimento` carrega
`principal_id` como dimensão de primeira classe (não injetado por infra silenciosamente, não
default implícito), e que a ausência dele é erro, não `NULL` silencioso — leia `SPEC-001 §4.4` para
o contrato exato antes de modelar.

## Contexto que já custou defeito aqui

- `ADR-009/D2` **recusa `organization_id`** como substituto ou complemento — não reintroduza esse
  conceito por analogia com outros sistemas multi-tenant.
- `Q2` já foi **respondida** (não é gate aberto) — não trate como bloqueio pendente.

## Restrições do repositório que mordem aqui

- **Contratos `import-linter`** (`backend/pyproject.toml [tool.importlinter]`): `domain`/`use_cases`
  não importam `socket`, `ssl`, `time`, `datetime` (contrato `Natureza`, `ADR-016`).
- **Pisos de cobertura:** domain 90%, use_cases 80%, infra 70%.
- **Idioma:** identificador de produção e de teste em **inglês**, inclusive parâmetro/variável
  local/constante de módulo (`CLAUDE.md` linhas 1-2). Exceção viva: nome de coluna de contrato em
  português (linha 11). Mensagem de `raise`: lacuna `⏸ NÃO DECIDIDO` — não é licença para português novo.
- **`docs/INDEX.md` é append-only.**
- **Nenhum número sem o comando que o produziu**, com `n` e rótulo de força.
- **Sem `Co-Authored-By:`.** Autor e committer são o owner.

## Push — atenção ao mecanismo em worktree

O worktree não está registrado no plugin do Claude Code. **Não aponte `$HARNESS_MECHANISM` para
`.harness/mechanism` (o próprio shim) — causa recursão infinita.** Valor correto:

```
HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness git push ...
```

Se `frontend/node_modules` não existir no worktree, `npm --prefix frontend install` antes do push.

## Protocolo de custo — vinculante, R6/R7 (`docs/protocolo-de-despacho.md`)

- **`make verify`** roda os seis portões numa chamada. Se `make verify` recusar por anomalia de
  plugin em worktree (medido nas duas tasks anteriores), rode `harness rules --mode sweep` e
  `harness validate --strict` direto — ambos têm de dar `rc=0`.
- **Se projetar passar de ~150 turnos, PARE**, escreva o estado neste arquivo e devolva.
- Devolva **no máximo 15 linhas**: veredito, números com o comando, e o caminho do relatório completo
  em `docs/context/plataforma-dados/gates/T-04.7-builder.md`. **Nunca cole o corpo.**

## Entrega

Commit no branch `tasks/T-04.7-principal-id-dimensao`, push (ver seção acima), PR aberta contra `master`.
