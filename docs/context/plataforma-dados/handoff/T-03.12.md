# Handoff — T-03.12 · Portão de natureza por USO (scanner de AST) + estreitamento atômico do contrato 3

**Card:** nenhum, de propósito — owner disse "não crie card no Jira — eu cardo" (`tasks.toml`, `[PREMISSA-OWNER]`). Não é `local_only`. **Componente:** `sentimento` · **Base:** `master@a3c63c5` (via `https://github.com/StharleyMax/cripto-strategy.git`, branch `master` — fetch com URL explícita, não `origin`/SSH, que não tem credencial neste ambiente) · **Worktree:** `/tmp/claude-1002/wt/T-03.12`

## Por que esta task, agora, fora do lote original

Não estava no escopo dos 16 tasks autorizados em 2026-08-28. Foi promovida porque **está
bloqueando `git push` de QUALQUER branch neste repositório, agora** — descoberto ao tentar
empurrar `T-04.5`/`T-04.7` depois do merge de `T-04.1`/`T-03.10`. `make boundaries` no `master`
atual já reprova:

```
Natureza: domain e use_cases nao falam com socket, ssl nem relogio  BROKEN
  src.modules.sentimento.domain.dump_window -> datetime (l.6)
  src.modules.sentimento.domain.retention_probe -> datetime (l.6)
```

Isso já era **conhecido e documentado** quando `T-03.10` (PR #28) foi mesclado — a própria
`tasks.toml` desta task diz: *"DESTRAVA: PR #28 / T-03.10 — `git push --dry-run` rc=1 hoje, DoD-8
exige rc=0"*. Ninguém pegou porque merge pelo GitHub não roda hook local.

## Leia estas três fontes ANTES de escrever qualquer linha — nesta ordem

1. `docs/adr/ADR-016-relogio-e-capacidade-tipo-de-data-e-valor.md` — a decisão: **relógio é
   capacidade** (`datetime.now()`, `time.time()`, `time.sleep()` — leitura/efeito do mundo real,
   proibido em `domain`/`use_cases`), **tipo de data é valor** (`date`, `timedelta` — dado puro,
   permitido). O import-linter atual (`forbidden_modules = ["socket", "ssl", "time", "datetime"]`
   em `backend/pyproject.toml [[tool.importlinter.contracts]]`, contrato "Natureza") é **cego a
   essa distinção** — ele bloqueia o módulo inteiro, pegando `date`/`timedelta` como falso
   positivo. É exatamente esse contrato que precisa estreitar.
2. `docs/adr/bancadas/ADR-016-natureza.py` — a bancada de mutação já escrita para a ADR. **Não
   mova este arquivo — copie e endureça** (DoD-11: a bancada permanece em `docs/adr/bancadas/`,
   reprodutível pela ADR).
3. `docs/context/plataforma-dados/tasks_review-T-03.12.md` — a narrativa completa desta task.

## O bloco de DoD é o próprio `tasks.toml` — não o resuma, leia-o de lá

`grep -n -A25 '^id = "T-03.12"$' docs/context/plataforma-dados/tasks.toml`. Ele tem 11 itens de
DoD com número, linha e comando de cada medição — inclusive **dois achados que a própria ADR não
declarou** (o `from time import monotonic` chamado nu escapa do scanner, DoD-3 Mutante 5; e
`getattr(date, 'today')()` escapa, DoD-9). **Reproduza os dois antes de aceitar a bancada como
suficiente** — copiá-la sem reproduzir esses dois furos é exatamente o modo de falha que a
própria task nomeia.

## O invariante atômico — D5, não é sugestão

**O scanner de AST por uso e o estreitamento do contrato 3 entram no MESMO commit.** Dois commits
reprovam mesmo com a árvore final correta: estreitar o import-linter primeiro abre uma janela sem
portão nenhum (contrato solto + scanner ainda não existe = `datetime.now()` novo em `domain/`
passaria batido). Não faça em dois passos.

## Falsificador da própria ADR — DoD-10, não pule

Uma linha estável e grepável contando falso-positivo vs. verdadeiro-positivo desde o início da
fase 03. Placar de referência (2026-08-29): **0 FP × 3 VP**. Se `FP >= VP`, a ADR cai e o par
volta a `['socket', 'ssl']` — isto não é hipotético, é o critério de reversão já escrito.

## Restrições do repositório que mordem aqui

- **`ADR-012/D4`**: o motor do harness não é o lugar — o scanner nasce no `make`, script próprio,
  não regra `[[rules.own]]`.
- **`ADR-011/D3a`/`D3b`**: estrutura (AST), não regex; costurado em `boundaries.sh`, mesma
  fronteira dos outros contratos.
- **Idioma:** identificador de produção e teste em inglês (`CLAUDE.md` linhas 1-2).
- **`docs/INDEX.md` é append-only.**
- **Sem `Co-Authored-By:`.** Autor e committer são o owner.

## Push — atenção ao mecanismo em worktree

O worktree não está registrado no plugin do Claude Code. **Não aponte `$HARNESS_MECHANISM` para
`.harness/mechanism` (o próprio shim) — causa recursão infinita**, medido nesta sessão. Valor
correto:

```
HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness git push ...
```

Empurre para `https://github.com/StharleyMax/cripto-strategy.git <branch>` (não `origin` — SSH
sem credencial neste ambiente). Se `frontend/node_modules` faltar, `npm --prefix frontend install`
antes do push.

**Esta é a task que, se sair certa, faz `git push --dry-run` sair `rc=0` de novo para o
repositório inteiro — inclusive as branches de `T-04.5`/`T-04.7`, hoje reescritas mas ainda
represadas por causa deste bloqueio. Verifique isso explicitamente como parte do DoD-8.**

## Protocolo de custo — vinculante, R6/R7 (`docs/protocolo-de-despacho.md`)

- Se projetar passar de ~150 turnos, PARE, escreva o estado neste arquivo e devolva.
- Devolva **no máximo 15 linhas**: veredito, os 11 itens de DoD com PASS/FAIL e o comando de
  cada, caminho do relatório completo em `docs/context/plataforma-dados/gates/T-03.12-builder.md`.
  **Nunca cole o corpo.**

## Entrega

Commit único (D5 é atômico) no branch `tasks/T-03.12-natureza-por-uso`, push, PR aberta contra
`master`.
