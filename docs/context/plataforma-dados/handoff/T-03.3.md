# Handoff — T-03.3 · Política de reconexão POR CLASSE de stream

**Card:** `CST-19` · **Componente:** `sentimento` · **Base:** `master@07193e6` · **Worktree:** `/tmp/claude-1002/wt/T-03.3`

## Por que agora

Depende de `T-03.2` (coletor `!forceOrder@arr`), que **já está mesclado** (PR #56). Escopo: código +
prova offline/probe curto, não deploy contínuo (sem frontend ainda, decisão do owner já registrada
em `docs/decisoes-do-owner.md` §Q1).

## O DoD

Bloco `id = "T-03.3"` em `docs/context/plataforma-dados/tasks.toml`. Refs: `ADR-004` (gate de F0),
plano `03` item **3.2**, DoD **D3.6**. Linha completa em
`docs/context/plataforma-dados/tasks_review.md:267`.

- **Política de reconexão POR CLASSE de stream** (`ADR-004`, gate de F0 — leia o ADR inteiro antes
  de desenhar): Classe B (a classe de `!forceOrder@arr`) exige **sobreposição obrigatória** na
  reconexão, **chave natural declarada** para dedupe, **taxa de colisão publicada**, e **direção do
  viés escrita** (colisão não resolvida ⇒ subcontagem, nunca supercontagem — documente por quê).
- **D3.6**: contagem de colisão por símbolo e por dia, ≥ 30 dias × ≥ 20 símbolos. **Não é medível em
  regime real hoje** (precisa de dias rodando) — construa a MECÂNICA de contagem/publicação de forma
  que essa leitura seja trivial depois; prove a lógica sobre uma simulação de reconexão com overlap
  conhecido.
- **Alternativas já fechadas no ADR-004 — não as reabra:** reconexão SEM sobreposição produz buraco
  IRREVERSÍVEL a cada 24h (a Binance derruba WS após 24h por doc); dedupe por HASH do payload
  subcontaria eventos reais SEM registrar que subcontou (pior que colisão declarada).

## Contexto que já custou defeito aqui

- Esta política é **por classe de stream**, não específica de `!forceOrder@arr` — desenhe a
  abstração pensando que `T-03.2`'s sucessor (o WS de `aggTrade`, já coberto por `T-03.1`/`T-03.4`)
  também vai precisar dela eventualmente, mas **não construa a integração com `aggTrade` aqui** —
  fora de escopo desta task.
- `T-03.2` grava CRU, sem reconexão — esta task é a camada que fecha essa lacuna. Não modifique o
  gravador cru de `T-03.2` além do necessário para plugar a política de reconexão.

## Restrições do repositório (as mesmas de toda task desta sessão)

- Import-linter (`Natureza` incl.), cobertura domain 90/use_cases 80/infra 70%, idioma em inglês,
  `docs/INDEX.md` append-only, sem `Co-Authored-By:`.
- Push em worktree: `HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness`
  (nunca `.harness/mechanism`), URL explícita
  (`https://github.com/StharleyMax/cripto-strategy.git`, não `origin`/SSH), **nunca
  `--force-with-lease`** (medido causando "entrada padrão vazia" nesta sessão) — use `--force`
  simples se precisar reescrever histórico.
- Provável conflito de merge em `backend/README.md`/`docs/INDEX.md` com a branch irmã `T-03.11` —
  resolva mantendo AMBAS as seções/linhas, nunca reescrevendo a existente.
- **Ao rodar `bash backend/scripts/test.sh`, aguarde ATIVAMENTE o comando terminar antes de encerrar
  seu turno** — não dispare em background e diga "vou esperar a notificação": isso não funciona
  entre turnos de subagente nesta sessão (medido repetidamente). Se precisar de background, continue
  chamando ferramentas até confirmar via novo comando que o processo já terminou.

## Protocolo de custo — R6/R7

- Se projetar passar de ~150 turnos, PARE e escreva o estado neste arquivo.
- Devolva **no máximo 15 linhas**, caminho do relatório em
  `docs/context/plataforma-dados/gates/T-03.3-builder.md`. **Nunca cole o corpo.**

## Entrega

Commit no branch `tasks/T-03.3-reconexao-por-classe`, push, PR aberta contra `master`.
