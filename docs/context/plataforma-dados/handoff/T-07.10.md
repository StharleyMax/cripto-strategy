# Handoff — T-07.10 · `clock_skew_tolerance_ms` CALIBRADO sobre a distribuição de `03`

**Card:** `CST-64` · **Componente:** `sentimento` · **Base:** `master@031e007` · **Worktree:** `/tmp/claude-1002/wt/T-07.10`

## ⚠️ Leia isto antes de tudo — o DoD pede 7 DIAS de dado real que NÃO EXISTEM ainda

`T-03.8` (medição de `clock_skew_ms`) está **`done`**, mas isso significa que o MECANISMO foi construído
e provado com **5 corridas de probe curto** (`docs/context/plataforma-dados/medicoes/T-03.8-ntp-skew/`),
não que existem **≥ 7 dias de `ingest_run` acumulados** — o repositório está rodando local/probe-curto
por decisão do owner (VPS fora por enquanto, `docs/decisoes-do-owner.md` §Q19), não como serviço
contínuo 24/7. **Isto NÃO é motivo para bloquear a task** — é o mesmo padrão já usado em `T-03.3`/`D3.6`
e `T-03.6` nesta sessão: **construa a MECÂNICA de calibração agora, prove a lógica com uma distribuição
SIMULADA (ou os poucos dados reais que já existem), e documente explicitamente que a calibração REAL
depende de 7 dias de captura contínua que ainda não aconteceu.**

**Não invente 7 dias de dado.** Se o código precisar de uma distribuição para se exercitar, gere uma
simulada e rotule como tal no teste; nunca maquiada de medição real.

## O DoD

Bloco `id = "T-07.10"` em `docs/context/plataforma-dados/tasks.toml`. Refs: `CA-F3-13`, plano `07`
item **7.12**, DoD **D7.18**. Linha completa em `docs/context/plataforma-dados/tasks_review.md:343`.

- **D7.18**: ler a distribuição de `clock_skew_ms` por `ingest_run` — **≥ 7 dias de runs** é o padrão
  de calibração real (não construa isso aqui, é dado futuro). **Calibrado, não adivinhado** — o
  mecanismo tem que CALCULAR a tolerância a partir de uma distribuição de entrada (percentil, ex: p99
  dos `clock_skew_ms` observados), nunca um número fixo hardcoded.
- Reuse `domain/clock_skew.py` e `use_cases/persist_ntp_skew_run.py` (de `T-03.8`) como fonte do
  vocabulário de entrada — não duplique o tipo `ClockSkewSample`.
- A saída (`clock_skew_tolerance_ms`) tem que **recusar** calcular se o `n` de amostras for
  insuficiente (por exemplo, menos que alguns dias de dado) — documente o limiar escolhido e por quê,
  e é aceitável que hoje, com 5 corridas, o mecanismo RECUSE e diga explicitamente "dado insuficiente
  para calibração real, ainda não são 7 dias" em vez de calcular um número sem base.

## Contexto que já custou defeito aqui

- Mesmo padrão de `T-03.3`/D3.6 (colisão de reconexão) e `T-03.6`/D3.2-D3.4 (probe de disponibilidade):
  quando o DoD pede um volume de tempo real que ainda não se acumulou, a task certa é construir a
  MECÂNICA + provar com simulação, não fingir que o volume existe nem recusar construir o mecanismo.
- Contrato `Natureza` (`ADR-016`): a leitura de `ingest_run` (SQLite/storage) mora em `infra`; o cálculo
  de percentil/tolerância é lógica pura de `domain`.

## Restrições do repositório (as mesmas de toda task desta sessão)

- Import-linter (`Natureza` incl.), cobertura domain 90/use_cases 80/infra 70%, idioma em inglês
  (produção e teste, inclusive mensagem de exceção — `CLAUDE.md` §"Mensagem de exceção"),
  `docs/INDEX.md` append-only, sem `Co-Authored-By:`.
- Push em worktree: `HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness`
  (nunca `.harness/mechanism`), URL explícita, **nunca `--force-with-lease`** — use `--force` simples.
- **⚠️ Múltiplas sessões de Claude Code rodam em paralelo no diretório principal** — sua worktree é
  isolada, mas **sempre `git fetch` + resolva contra `master` mais recente antes do push**; conflito
  típico é em `docs/INDEX.md`/`backend/README.md` — mantenha AMBAS as seções.
- **Aguarde ATIVAMENTE `bash backend/scripts/test.sh` terminar** — não dispare em background e pare o
  turno esperando notificação; não funciona para subagente nesta sessão.

## Protocolo de custo — R6/R7

- Se projetar passar de ~150 turnos, PARE e escreva o estado neste arquivo.
- Devolva **no máximo 15 linhas**, caminho do relatório em
  `docs/context/plataforma-dados/gates/T-07.10-builder.md`. **Nunca cole o corpo.**

## Entrega

Commit no branch `tasks/T-07.10-fase07`, push, PR aberta contra `master`.
