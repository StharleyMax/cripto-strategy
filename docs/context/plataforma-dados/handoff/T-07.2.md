# Handoff — T-07.2 · Survivorship na borda de ingestão: ACCEPTED_WITH_WARNING, nunca REJECTED

**Card:** `CST-56` · **Componente:** `sentimento` · **Base:** `master@a330e77` · **Worktree:** `/tmp/claude-1002/wt/T-07.2`

## Por que agora

`T-07.1` (paginador de janela fechada) já mesclada em `master`. Owner autorizou puxar fase 07
backend (2026-09-02) e confirmou explicitamente puxar a cadeia que `T-07.1` destrava.

## O DoD

Bloco `id = "T-07.2"` em `docs/context/plataforma-dados/tasks.toml`. Refs: `CA-F3-14`,
`SPEC-001 §5.6`, plano `07` item **7.3**, DoD **D7.2**, **D7.6**. Linha completa em
`docs/context/plataforma-dados/tasks_review.md:335`.

- **Survivorship na borda de ingestão**: um símbolo AUSENTE do `exchangeInfo` CORRENTE (o
  snapshot de hoje, de `T-02.1`) mas presente no dump histórico do S3 tem que ser **ACEITO com
  aviso** (`ACCEPTED_WITH_WARNING` + registro em `md.ingest_gap`) — **NUNCA `REJECTED`**.
- **D7.2**: ingerir o dump de `MATICUSDT` (existe no S3, **não** existe no `exchangeInfo` de
  hoje — foi renomeado para `POLUSDT`, ver `Q12`) ⇒ tem que **GRAVAR, com aviso**, não descartar.
- **Por que isto importa, com número**: **109 símbolos históricos são invisíveis hoje**
  `[MEDIDO]` — **21,6%** do universo cripto-perp com histórico não existe mais no snapshot
  corrente (727 → 570 símbolos). Rejeitar esses símbolos na borda de ingestão apagaria 21,6% do
  histórico já capturado, silenciosamente.
- **D7.6**: backfill de um dia a 5 minutos ⇒ **288 pontos distintos** esperados (24h × 12/h);
  `2026-08-12` é um dia com buraco conhecido ⇒ **285 pontos + gap registrado** (não 288
  silenciosos, não erro — o gap tem que aparecer como dado, em `md.ingest_gap`).

## Contexto que já custou defeito aqui

- Reuse o vocabulário de `T-02.1` (`instrument_universe_snapshot.py`) para o que é
  "`exchangeInfo` corrente" — não invente uma segunda representação do snapshot.
- `T-07.1` (paginador) já fixa a leitura da JANELA — esta task decide o que fazer com um símbolo
  que a janela devolve mas que o universo corrente não reconhece; não misture as duas
  responsabilidades (não reabra `oi_history_paginator.py` além do necessário para consumi-lo).
- Contrato `Natureza` (`ADR-016`): chamada de rede/storage mora em `infra`, nunca em
  `domain`/`use_cases`. A decisão ACCEPTED_WITH_WARNING vs REJECTED é lógica pura de `domain`.

## Restrições do repositório (as mesmas de toda task desta sessão)

- Import-linter (`Natureza` incl.), cobertura domain 90/use_cases 80/infra 70%, idioma em inglês
  (produção e teste, inclusive mensagem de `raise`/`Error`/`Exception` — `CLAUDE.md` §"Mensagem
  de exceção"), `docs/INDEX.md` append-only, sem `Co-Authored-By:`.
- Push em worktree: `HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness`
  (nunca `.harness/mechanism`), URL explícita (`https://github.com/StharleyMax/cripto-strategy.git`,
  não `origin`/SSH), **nunca `--force-with-lease`** — use `--force` simples se precisar.
- **⚠️ Múltiplas sessões de Claude Code rodam em paralelo no diretório principal** — sua worktree
  é isolada, mas **sempre `git fetch` + resolva contra `master` mais recente antes do push**;
  conflito típico é em `docs/INDEX.md`/`backend/README.md` (append duplo) — mantenha AMBAS as
  seções, nunca descarte uma.
- **Aguarde ATIVAMENTE `bash backend/scripts/test.sh` terminar** — não dispare em background e
  pare o turno esperando notificação: isso não acorda subagente nesta sessão (medido
  repetidamente). Se precisar de background, sua PRÓXIMA ação no mesmo turno é checar
  `ps -p <PID>` até confirmar que já terminou.

## Protocolo de custo — R6/R7

- Se projetar passar de ~150 turnos, PARE e escreva o estado neste arquivo.
- Devolva **no máximo 15 linhas**, caminho do relatório em
  `docs/context/plataforma-dados/gates/T-07.2-builder.md`. **Nunca cole o corpo.**

## Entrega

Commit no branch `tasks/T-07.2-fase07`, push, PR aberta contra `master`.
