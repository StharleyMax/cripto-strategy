# Handoff — T-08.2 · Spike do eixo do Lightweight Charts com CARGA CHEIA (D8.19)

**Card:** `CST-70` · **Componente:** `charts` · **Base:** `master@aac3442` · **Worktree:** `/tmp/claude-1002/wt/T-08.2`

## Por que esta task e por que AGORA

Ela é a **única** das 25 tasks de front com `depends_on` transitivo vazio
`[MEDIDO 2026-08-29 em aac3442, fecho transitivo sobre docs/context/plataforma-dados/tasks.toml, n=25]`.

O plano a rotula literalmente **"o maior risco técnico desta especificação"**, com força
`[NÃO MEDIDO]` — ver `docs/plans/SPEC-001-plataforma-dados/08_superficie_e_reprodutibilidade.md:44`.

O `/tech-lead` já argumentou que ela deve rodar **antes** de `T-05.2`
(`docs/context/plataforma-dados/tasks_review.md:360` e §7/`D-2`): **16 tasks de `charts` são
construídas sobre a premissa que ela testa**, e o custo é *"uma página estática com 1.728 pontos
sintéticos, zero rede, zero API key, zero dependência de outra fase"*.

## O que NÃO está autorizado

`D-2` está `NÃO APLICADA` no `tasks.toml` e a decisão é do **owner**, porque aplicá-la migraria a task
de Epic (`CST-6` → `CST-1`). **Não acrescente a aresta `T-08.2` ao `depends_on` de `T-05.2`** e não
mexa em atribuição de Epic. Rodar a task não exige essa decisão; alterar o grafo exige.

## O DoD, literal

`D8.19` — `docs/plans/SPEC-001-plataforma-dados/08_superficie_e_reprodutibilidade.md:44`:
**288 pontos + 1.440 candles no mesmo eixo, coordenadas X contra `event_time`, tolerância 0,5 px.**

Refs declaradas em `tasks.toml` (bloco `id = "T-08.2"`): `SPEC-001 §9.2`, plano 08 DoD `D8.19`.

## O que o spike tem de responder, e o veredito é um NÚMERO

O ponto não é "renderizou". É **o eixo dizer a verdade sobre `event_time` sob carga cheia**. Meça o
erro em px entre a coordenada X que o Lightweight Charts atribui e a que `event_time` implica, sobre
os 1.728 pontos, e **publique o pior caso** — não a média. Se passar de 0,5 px, o veredito é que
`D8.19` **falha**, e isso é um resultado tão entregável quanto o contrário: a SPEC declarou este risco
como não medido justamente para poder ouvir "não".

## Restrições do repositório que mordem aqui

- **Idioma:** identificador de produção e de teste em **inglês** (`CLAUDE.md`, tabela de fronteira,
  linhas 1–4). String visível de UI em **pt-BR** (linha 8). Diretório sob `frontend/src/` tem de casar
  com `harness policy --key components` — `charts` casa.
- **`docs/INDEX.md` é append-only.** Acrescente linha; nunca reescreva uma existente.
- **Nenhum número sem o comando que o produziu**, com `n` e rótulo de força.
- **Sem `Co-Authored-By:`.** Autor e committer são o owner (hook `scripts/hooks/commit-msg`).

## Protocolo de custo — vinculante, R6/R7

`docs/protocolo-de-despacho.md` mudou hoje (PR #37). Leia **R6** e **R7** antes de começar:

- **`make verify`** roda os seis portões numa chamada, ~10 linhas impressas, saída bruta em arquivo.
  Use-o no lugar de `make lint` + `make test` + `harness rules` + `git diff` soltos.
- **Se projetar passar de ~150 turnos, PARE**, escreva o estado neste arquivo e devolva.
- Agrupe medições independentes numa chamada só.
- Devolva **no máximo 15 linhas**: veredito, números com o comando, e o caminho do relatório completo
  em `docs/context/plataforma-dados/gates/T-08.2-builder.md`. **Nunca cole o corpo.**

## Entrega

Commit no branch `tasks/T-08.2-spike-eixo-charts`, PR aberta contra `master`.
