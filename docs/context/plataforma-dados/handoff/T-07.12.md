# Handoff — T-07.12 (`plataforma-dados`, fase 07, componente `web`)

**Escrito pelo `/workflow` em 2026-09-02.** Próxima raiz `web` sem pendência de backend:
`depends_on = ["T-02.3", "T-05.8"]` — ambas `done`. `T-05.10` foi marcada `blocked` por
decisão do owner (auth só é necessária ao subir pra VPS; rodando local por enquanto — ver
`blocked_reason` no `tasks.toml`), e `T-06.10` segue bloqueada por `T-06.1` (`sentimento`,
`todo`) — por isso `T-07.12` é a próxima, fora de ordem de fase mas dentro da árvore de
dependências real.

## ⚠️ Isto é diferente de `T-05.8`/`T-05.9` — é a primeira TELA nova desta sessão

`docs/product/STITCH_CONTEXT.md` §4.2: **`S1 — console de coleta e retenção · [NÃO EXISTE] ·
fase 07`**. `T-05.8`/`T-05.9` foram módulos TS puros porque o contrato deles já estava coberto
por `S2` canônica + decisões `D1/D2/D7`. `S1` não existe — este é o primeiro trabalho real de
`ui-designer`/Stitch nesta sessão. Espere R7 do `ui-designer` NÃO se aplicar aqui (R7 é sobre
`S2`, núcleo operacional) — mas trate como decisão nova de verdade, não como "provavelmente já
coberto" (esse foi o padrão em `T-05.8`/`T-05.9`, não repita a suposição sem checar).

## O que a task pede, literal

`title`: *"S1 console: janela_de_perda como FORMULA por serie, fila de ETL, orcamento
aritmetico, GB/dia"*. `refs`: `SPEC-001 §6`, plano 07 item 7.13, `DoD D7.12`-`D7.15`, "S1 NAO
e o canal de alarme" (isso já está registrado em `STITCH_CONTEXT.md:411`).

`docs/plans/SPEC-001-plataforma-dados/07_aquisicao_em_regime.md:41-44`:

| DoD | o quê | onde | exemplo/medida |
|---|---|---|---|
| D7.12 | `janela_de_perda` é FÓRMULA, não constante | por coletor e por série | Coinalyze OI 1min ≈1,5d · OI 5min ≈7,0d · liquidação 1min ≈8d · `daily` sem apagamento · `/futures/data/*` 30d · liquidação intraday imediata · dump S3 "retenção NÃO MEDIDA" |
| D7.13 | escolha do trilho de resiliência vai escrita no painel | painel | trilhar 5min em vez de 1min multiplica o orçamento do SLO P1 por ~4,7 |
| D7.14 | retenção anticorrelacionada com a necessidade | painel de liquidação | "janela válida no regime atual, não garantida em cascata" — nunca um número seco |
| D7.15 | reconexão é normal, não erro | S1 | desconexão de 24h aparece como rotina |

## ⛔ Gatilho de reabertura que este task DISPARA — não é sua decisão resolver

`CLAUDE.md` (tabela de fronteira, linha 11, `janela_de_perda`): **"a reabertura acontece quando
T-07.12/T-07.13 ... escreverem o consumidor da projeção canônica. Quem decide é ADR-008/D3, não
esta feature."** Se o `/build` desta task de fato escrever esse consumidor, o gatilho dispara —
**registre isso no relatório, não tente resolver a pergunta de nomenclatura você mesmo.** A
pergunta (renomear `janela_de_perda` para inglês ou manter a exceção) é do `ADR-008/D3`/owner.

## ⛔ Fronteira de backend — mesma regra de `T-05.9`

`ingest_health_query` (a consulta nomeada que `T-02.3` já criou, `status = done`) é o que
`T-07.13` vai consumir de verdade — **não esta task**, pelo `depends_on` (`T-07.13` depende de
`T-07.12`, não o contrário). Se `T-07.12` precisar de dado real do backend além do que já
existe, **não crie nada em `backend/`** — use dado sintético/fixture local para a tela, como
`T-05.2` já faz (`met/2026-08-23.csv`), e registre a lacuna. As 15+ worktrees da trilha
`sentimento` seguem ativas em paralelo.

## ⚠️ Ambiente de trabalho — descoberta desta sessão

O diretório principal do repo é **compartilhado com outras sessões ativas**, que rodam
`git checkout`/`commit` diretamente nele. **Todo trabalho a partir de agora — inclusive deste
handoff — usa worktree isolado**, nunca a raiz principal. Este handoff foi escrito em
`/tmp/claude-1002/wt/workflow-t07-12-prep` (branch `workflow/t07-12-prep`), não na raiz.
