# Handoff — T-02.1 · Coletor de snapshot diário datado de `exchangeInfo`+`fundingInfo`

**Card:** `CST-12` · **Componente:** `sentimento` · **Base:** `master@634833b` · **Worktree:** `/tmp/claude-1002/wt/T-02.1`

## Por que agora

`Q1` foi respondida pelo owner em 2026-09-01 (`docs/decisoes-do-owner.md` §Q1) — **"pode ligar o q1 e
ligar os coletores"**, literal. `T-02.1` estava travada só por isso; `T-01.1` (dependência) já está
`done`. **Escopo desta rodada: só o CÓDIGO do coletor, com prova offline/probe curto — NÃO um deploy
contínuo.** Não há frontend ainda para exibir o dado (`frontend/src/` tem só spikes de chart e um
config de bench de lint, nenhuma página real) — rodar 24/7 agora não tem consumidor, decisão do
owner na mesma conversa que autorizou isso.

## O DoD real — e uma parte dele é estrutural, não temporal

Bloco `id = "T-02.1"` em `docs/context/plataforma-dados/tasks.toml`. Refs: `CA-F0-1`, `CA-F0-1b`,
`CA-F0-7`, `CA-F0-11`, `SPEC-001 §3.4`, plano `02` itens **2.1+2.2**, DoD **D2.1 a D2.5, D2.7**.
Linha completa em `docs/context/plataforma-dados/tasks_review.md:251`.

- **Coletor de snapshot diário DATADO** de `exchangeInfo` + `fundingInfo`, com `market`,
  `underlyingSubType`, `interestRate` por símbolo e o conjunto de `premiumIndex` como **segunda
  testemunha**. Hash sobre a **projeção canônica**, confirmado em duas leituras.
- **D2.1** (`data(último snapshot) == hoje` por 7 dias consecutivos) **não é medível hoje** — precisa
  de 7 dias de execução real, que só faz sentido depois do deploy. **Construa o coletor de forma que
  essa medição seja TRIVIAL de rodar depois** (idempotente, seguro para rodar 1x/dia, grava a data
  no próprio nome/metadado do snapshot) e documente no relatório que D2.1 fica pendente de operação.
- **D2.2, D2.3, D2.4, D2.5, D2.7 SÃO medíveis agora**, sobre uma ou duas capturas reais (não fixture
  sintética — os dados de `exchangeInfo`/`premiumIndex`/`fundingInfo` de verdade mudam entre
  chamadas, é isso que D2.3/D2.4 provam): distribuição de `fundingIntervalHours` difere entre 2
  capturas separadas por dias; `exchangeInfo` × `premiumIndex` tem contagens diferentes (extras
  esperados, é DADO não erro); `fundingInfo` × `exchangeInfo` também diverge; duas chamadas separadas
  por milissegundos produzem hash IGUAL mesmo com payloads brutos diferentes (a projeção canônica
  ignora ruído de ordem/whitespace); `ls -la` do artefato grava o tamanho real.

## Contexto que já custou defeito aqui (mesma família de bug em `T-02.3`/`T-04.x`)

- **Contrato `Natureza` (`ADR-016`, `T-03.12` já mesclada):** `domain`/`use_cases` não importam
  `socket`, `ssl`, `time`, `datetime` **como capacidade** (leitura do relógio real). `date`/
  `timedelta` como **valor puro** são permitidos — o scanner de USO (`make natureza`) distingue os
  dois. A chamada de rede em si (requests/httpx) mora em `infra`, nunca em `domain`.
- **`SeriesKey`** (`T-04.2`, já mesclada) é o termo de identidade que consumidores downstream vão
  usar para chavear esta série — não invente um segundo esquema de chave paralelo.
- Nenhuma chave de API é necessária para `exchangeInfo`/`fundingInfo`/`premiumIndex` (endpoints
  públicos da Binance Futures) — não toque em `.env`.

## Restrições do repositório (as mesmas de toda task desta sessão)

- Import-linter (`backend/pyproject.toml [tool.importlinter]`): 3 contratos, incl. `Natureza`.
- Cobertura por camada: domain 90%, use_cases 80%, infra 70%.
- Idioma: identificador de produção e teste em **inglês**, inclusive parâmetro/variável
  local/constante de módulo (`CLAUDE.md` linhas 1-2).
- `docs/INDEX.md` é **append-only**. Nenhum número sem o comando que o produziu.
- Sem `Co-Authored-By:`. Autor e committer são o owner.
- **Push em worktree:** NÃO aponte `$HARNESS_MECHANISM` para `.harness/mechanism` (recursão
  infinita, já medida). Valor correto:
  `HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness`.
  Empurre para `https://github.com/StharleyMax/cripto-strategy.git <branch>` (não `origin`/SSH — sem
  credencial neste ambiente). **Nunca `--force-with-lease`** (causa "entrada padrão vazia" medido
  nesta sessão) — use `--force` simples se precisar reescrever histórico.
- Se `frontend/node_modules` faltar, `npm --prefix frontend install` antes do push.

## Protocolo de custo — R6/R7 (`docs/protocolo-de-despacho.md`)

- Se projetar passar de ~150 turnos, PARE e escreva o estado neste arquivo.
- Devolva **no máximo 15 linhas**: veredito, números com o comando, caminho do relatório em
  `docs/context/plataforma-dados/gates/T-02.1-builder.md`. **Nunca cole o corpo.**

## Entrega

Commit no branch `tasks/T-02.1-snapshot-diario`, push, PR aberta contra `master`.
