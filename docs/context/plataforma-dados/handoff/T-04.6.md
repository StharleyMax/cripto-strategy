# Handoff — T-04.6 · Serialização de numeral invariante de locale

**Card:** `CST-33` · **Componente:** `sentimento` · **Base:** `master@a3c63c5` · **Worktree:** `/tmp/claude-1002/wt/T-04.6`

## Por que esta task agora

`T-04.6` depende de `T-04.1`, que **já está mesclado** (PR #45). É a segunda das duas últimas
tasks da fase 04 — a outra é `T-04.3`, rodando em paralelo, sem sobreposição de arquivo esperada.

**A mesma pegadinha de infraestrutura de `T-04.3` vale aqui:** o `master` atual tem o contrato de
arquitetura "Natureza" quebrado por dois arquivos de OUTRA task (`T-03.10`), sendo corrigido por
`T-03.12` (PR #50, em review). `bash backend/scripts/boundaries.sh` vai reportar `2 kept, 1 broken`
até lá mesclar — não é problema seu, a menos que o "broken" cite outro arquivo.

## O DoD — e uma particularidade real, não erro de leitura

Bloco `id = "T-04.6"` em `docs/context/plataforma-dados/tasks.toml`. Refs: `SPEC-001 §3.8` · plano
`04` item **4.12** · DoD **D4.12**.

**O número da task (`T-04.6`) NÃO casa com o número do item de plano (`4.12`) nem com um `D4.6`.**
Isso já aconteceu antes nesta fase (`T-04.7` mapeia para o item `4.11`) — não é erro de digitação,
é o `tasks.toml` numerando tasks na ordem de criação, não na ordem do plano. **Ignore qualquer
"4.6"/"D4.6" que você encontrar no plano** (aquele é sobre `R-1`/`R-2` como conjunção — task
diferente, não construída ainda) — o item e o DoD corretos desta task são **4.12** e **D4.12**.

- **Item 4.12:** serialização de numeral **invariante de locale** em todo caminho de dado.
- **D4.12:** exportar a mesma fixture com `LANG=pt_BR.UTF-8` e `LANG=C` e comparar `sha256sum` —
  **têm de ser iguais, ou reprova**. `pt_BR` usa vírgula decimal e ponto de milhar; `C` usa ponto
  decimal sem separador de milhar — qualquer caminho de serialização que dependa do locale do
  processo (em vez de fixar o formato) quebra esse DoD.

## Contexto que já custou defeito aqui

- `T-04.5` (`cvd_delta`/`cvd_cum`, em review agora) já usa `Decimal` para a aritmética canônica —
  o problema desta task é **serialização** (texto de saída), não o tipo aritmético interno. Não
  confunda os dois: `Decimal` correto internamente ainda pode serializar errado sob locale.
- A refs cita explicitamente: esta task **"tira de `Q14` o poder de invalidar fixture"** — ou
  seja, existe um `Q14` (já respondido/neutralizado em outro contexto) sobre formatação de
  fixture que esta task fecha de vez. Não reabra `Q14` como se estivesse pendente.

## Restrições do repositório que mordem aqui

- **Contratos `import-linter`**: `domain`/`use_cases` não importam `socket`, `ssl`, `time`,
  `datetime` (contrato `Natureza`, `ADR-016`).
- **Pisos de cobertura:** domain 90%, use_cases 80%, infra 70%.
- **Idioma:** identificador de produção e de teste em **inglês** (`CLAUDE.md` linhas 1-2).
- **`docs/INDEX.md` é append-only** — provável conflito de merge com `T-04.3`. Resolva mantendo
  AMBAS as linhas, nunca reescrevendo a existente.
- **Sem `Co-Authored-By:`.** Autor e committer são o owner.

## Push — atenção ao mecanismo em worktree

O worktree não está registrado no plugin do Claude Code. **Não aponte `$HARNESS_MECHANISM` para
`.harness/mechanism` (o próprio shim) — causa recursão infinita.** Valor correto:

```
HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness git push ...
```

Empurre para `https://github.com/StharleyMax/cripto-strategy.git <branch>` (não `origin`/SSH). Se
`frontend/node_modules` faltar, `npm --prefix frontend install` antes do push.

## Protocolo de custo — vinculante, R6/R7 (`docs/protocolo-de-despacho.md`)

- Se `make verify`/`make boundaries` acusar SÓ `dump_window.py`/`retention_probe.py`, é o estado
  conhecido do `master` — prossiga. Qualquer outro achado é seu.
- Se projetar passar de ~150 turnos, PARE, escreva o estado neste arquivo e devolva.
- Devolva **no máximo 15 linhas**: veredito, números com o comando, caminho do relatório completo
  em `docs/context/plataforma-dados/gates/T-04.6-builder.md`. **Nunca cole o corpo.**

## Entrega

Commit no branch `tasks/T-04.6-numeral-invariante-de-locale`, push (ver seção acima), PR aberta
contra `master`.
