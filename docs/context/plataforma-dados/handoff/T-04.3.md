# Handoff — T-04.3 · Unicidade por `agg_id` com verificação de contiguidade

**Card:** `CST-30` · **Componente:** `sentimento` · **Base:** `master@a3c63c5` · **Worktree:** `/tmp/claude-1002/wt/T-04.3`

## Por que esta task agora

`T-04.3` depende de `T-04.1`, que **já está mesclado** (PR #45, `master@a3c63c5` o contém). É a
primeira das duas últimas tasks da fase 04 — a outra é `T-04.6`, rodando em paralelo, sem
sobreposição de arquivo esperada.

**Atenção a uma pegadinha de infraestrutura, não de código:** o `master` atual ainda tem o
contrato de arquitetura "Natureza" quebrado por dois arquivos de OUTRA task (`T-03.10`,
`dump_window.py`/`retention_probe.py` importando `datetime`) — isso está sendo corrigido por
`T-03.12` (PR #50, em review agora) e **não é problema seu**: `bash backend/scripts/boundaries.sh`
vai reportar `2 kept, 1 broken` até `T-03.12` mesclar. Rode e leia mesmo assim — se o "broken"
citar QUALQUER arquivo que não seja `dump_window.py`/`retention_probe.py`, aí sim é seu.

## O DoD

Bloco `id = "T-04.3"` em `docs/context/plataforma-dados/tasks.toml`. Refs: `CA-F1-5` · `CA-F1-6` ·
plano `04` item **4.3** · DoD **D4.4, D4.5**
(`docs/plans/SPEC-001-plataforma-dados/04_contrato_temporal.md`).

- **4.3** Unicidade por `agg_id` com verificação de **contiguidade** — nunca por tempo, nunca
  `first`/`last trade_id`.
- **D4.4** contiguidade de tick sobre `aggTrades`: **0 saltos, 0 ts decrescente, 8.873.078
  linhas**; o buraco de 08-22 aparece como **descontinuidade de FLOW, não costurada** (1.620.908
  ausentes entre `agg_id` 3420055157 e 3421676065) — não preencha esse buraco, apenas detecte-o.
- **D4.5** unicidade sob colisão de milissegundo: até **184 aggTrades no mesmo ms**, **25,6% dos
  ms com colisão** — é exatamente por isso que a chave é `agg_id`, nunca timestamp.

## Contexto que já custou defeito aqui

- `T-04.1` (já mesclada) construiu `label_and_sort_metrics_rows` para o CSV de `metrics` — **não
  é o mesmo dado** de `aggTrades`. Não tente reaproveitar aquele módulo para esta task; a
  contiguidade aqui é sobre `agg_id`, não sobre `event_time`.
- O falsificador global da fase (`F-2`, `docs/plans/.../04_contrato_temporal.md`): duas séries com
  a mesma `SeriesKey` cujos `cvd_cum` divirjam. A detecção de contiguidade que esta task constrói
  é o que faz `T-04.4`/`T-03.2` conseguirem confiar no dado — não invente uma segunda noção de
  "buraco" incompatível com a de `T-03.10`/`T-03.3` (reconexão por classe de stream).

## Restrições do repositório que mordem aqui

- **Contratos `import-linter`** (`backend/pyproject.toml [tool.importlinter]`): `domain`/`use_cases`
  não importam `socket`, `ssl`, `time`, `datetime` (contrato `Natureza`, `ADR-016` — ver aviso
  acima sobre o estado atual do `master`).
- **Pisos de cobertura:** domain 90%, use_cases 80%, infra 70%.
- **Idioma:** identificador de produção e de teste em **inglês** (`CLAUDE.md` linhas 1-2).
- **`docs/INDEX.md` é append-only** — e vai ter conflito de merge se `T-04.6` ou outra branch
  chegar primeiro. Resolva mantendo AMBAS as linhas, nunca reescrevendo a existente.
- **Sem `Co-Authored-By:`.** Autor e committer são o owner.

## Push — atenção ao mecanismo em worktree

O worktree não está registrado no plugin do Claude Code. **Não aponte `$HARNESS_MECHANISM` para
`.harness/mechanism` (o próprio shim) — causa recursão infinita.** Valor correto:

```
HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness git push ...
```

Empurre para `https://github.com/StharleyMax/cripto-strategy.git <branch>` (não `origin`/SSH — sem
credencial neste ambiente). Se `frontend/node_modules` faltar, `npm --prefix frontend install`
antes do push.

## Protocolo de custo — vinculante, R6/R7 (`docs/protocolo-de-despacho.md`)

- Se `make verify`/`make boundaries` acusar SÓ `dump_window.py`/`retention_probe.py`, é o estado
  conhecido do `master` — prossiga. Qualquer outro achado é seu.
- Se projetar passar de ~150 turnos, PARE, escreva o estado neste arquivo e devolva.
- Devolva **no máximo 15 linhas**: veredito, números com o comando, caminho do relatório completo
  em `docs/context/plataforma-dados/gates/T-04.3-builder.md`. **Nunca cole o corpo.**

## Entrega

Commit no branch `tasks/T-04.3-unicidade-agg-id`, push (ver seção acima), PR aberta contra `master`.
