# Handoff — T-05.1 (fase 05, componente `charts`, CST-35)

**Worktree isolada:** `.claude/worktrees/T-05.1` · branch `tasks/T-05.1-grade-canonica-charts`.
Dono de julgamento de `charts`: **quant-architect** (`Q16`, `[PREMISSA-OWNER: 2026-08-28]`
literal: "charts -> quant-architect · web -> ui-designer").

## O que a task pede (2 DoDs, não 1)

1. **`D5.9`** (`docs/plans/SPEC-001-plataforma-dados/05_fatia_visivel.md:37`): a grade
   canônica é **UMA** implementação — `charts` a possui, o motor de backtest a importa,
   nunca reimplementa. Prova: `sha256` da saída da grade usada pelo gráfico == usada pelo
   acessor, sobre **4 dias × 1 símbolo × 3 TFs**.
2. **`D5.12`** (mesmo doc, linha ~40; herdou de `D1.6` da fase 01 — ver
   `01_governanca_gateante.md:38-68` para a história de por que migrou): a fronteira
   `charts` ⇄ `web` é **executável** — contrato reprova import nas duas direções, prova de
   dois lados na MESMA passada: **morde** (2 violadores efêmeros, 1 por direção, `rc≠0`
   nomeando o contrato) + **cala** (módulos REAIS de `charts` e `web` continuam verdes).
   `ADR-003:46` já recusou amarrar o contrato a CAMINHO de diretório (`charts -> path`
   quebra na primeira reorganização); o único instrumento disponível em TS que casa
   especificador de MÓDULO é `no-restricted-imports` do ESLint (`frontend/eslint.config.mjs`
   — flat config, comentários no topo explicam por que é AST e não `[[rules.own]]` regex).

## Estado real do universo hoje — NÃO é o que a task original media (2026-08-28: 4 arquivos)

`find frontend/src -type f` cresceu desde então. Hoje existem, entre outros:
`frontend/src/charts/{axis-fidelity,axis-spike,headless-chart,synthetic-series}.ts` (de
`T-08.2`, spike do eixo) e `frontend/src/app/{history-transport,live-transport,
threshold-spec-bundle,universe-at}.ts` + `frontend/src/features/**` (de `T-05.8/9`,
`T-07.x`). O "cala" desta task tem que ler esse universo REAL, não o de 2026-08-28 — meça
com `find frontend/src -type f` antes de escrever o teste, não confie neste handoff.

## Falsificador de vacuidade — o ADR-012 que a própria task cita

Um contrato `forbidden` sobre um universo vazio passa com `rc=0` por não ter o que reprovar
— conformidade falsa. Prove que o contrato MORDE plantando os 2 violadores efêmeros
(1 import de `charts` para dentro de algo em `frontend/src/app` ou `features`, e 1 do
sentido inverso), confirmando `rc≠0`, e SÓ DEPOIS remova-os e confirme `rc=0` limpo.

## Fora de escopo desta task

`T-05.2..T-05.7` (S2-mínima, selo, política de ausência, etc.) — não implemente o gráfico
em si, só a grade canônica + a fronteira executável. Não toque `backend/` (read-only se
precisar citar `ADR-003`/`ADR-016`).

## Pipeline esperado

Mesmo rigor das tasks `web` desta sessão: implemente, depois dispatch `/qa` (destrutivo,
mutação) e `/review` (compliance), registre gates em
`docs/context/plataforma-dados/gates/T-05.1-{builder,qa,review}.md`. Sem UI, então sem
`ui-designer`/ux-audit aqui — é lógica pura + configuração de lint.
