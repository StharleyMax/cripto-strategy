# Handoff — T-06.2 · Tabela de shift POR ENDPOINT: dump = REST −5 min

**Card:** `CST-46` · **Componente:** `sentimento` · **Base:** `master` (faça `git fetch` e confirme o HEAD antes de criar a worktree — várias tasks da fase 06 estão sendo construídas em paralelo agora) · **Worktree:** `/tmp/claude-1002/wt/T-06.2`

## Por que agora

`T-06.1` (`series_catalog`, o contrato) já mesclada em `master`. Owner autorizou puxar a fase 06 inteira, uma PR por task, em paralelo onde possível (2026-09-03). Esta task, `T-06.3`, `T-06.4`, `T-06.5`, `T-06.6`, `T-06.9` e `T-06.10` estão TODAS sendo construídas ao mesmo tempo, cada uma na sua worktree.

## O DoD

Bloco `id = "T-06.2"` em `docs/context/plataforma-dados/tasks.toml`. Refs: `CA-F2-1`, plano `06` item **6.2**. Linha completa em `docs/context/plataforma-dados/tasks_review.md:317`.

- **Tabela de shift POR ENDPOINT** (não global): `openInterestHist`, `topLongShortPositionRatio`, `topLongShortAccountRatio`, `globalLongShortAccountRatio` → o dump (arquivo mensal do S3) tem timestamp **REST − 5 min** em relação ao que o mesmo instante representa via API REST ao vivo. `takerlongshortRatio` é a **EXCEÇÃO — SEM shift**, e a exceção é POR ENDPOINT, nunca aplicada globalmente por engano.
- **Falsificador medido**: comparar os conjuntos de timestamp do dump vs REST para `openInterestHist` dá **288 vs 288** timestamps idênticos e **MAE = 0,000000** depois de aplicar o shift de −5 min — se a tabela estiver errada (shift no endpoint errado, ou faltando a exceção do `takerlongshortRatio`), esse teste tem que reprovar.
- **Onde isso entra no catálogo (`T-06.1`, `series_catalog.py`)**: o campo `label_shift` já existe em `SeriesKey` (`series_key.py`, um dos 15 termos, em milissegundos) — esta task POPULA o valor real de `label_shift` para cada `SeriesKey` desses 4 endpoints (−300000 ms para os 3 primeiros, `0` para `takerlongshortRatio`), com `verified_by` apontando para o teste que mediu o shift (o teste dos 288 vs 288 / MAE 0,000000 acima).
- Não reabra `series_catalog.py`/`series_key.py` além do necessário para consumi-los — a validação de forma já existe lá; esta task só produz linhas de catálogo REAIS com os valores corretos.

## Contexto que já custou defeito aqui

- Reuse `SeriesKey`/`series_catalog.py` de `T-06.1` (leia o módulo inteiro antes de codar — ele documenta explicitamente que `T-06.2`–`T-06.9` são quem povoa os valores reais).
- Não confunda esta tabela de shift com o shift canônico de `event_time = create_time + 300000` de `T-04.1` (fase 04, oito colunas do stream ao vivo) — são shifts DIFERENTES, em superfícies diferentes (esta é dump-vs-REST de endpoints de fase 06, aquela é create_time-vs-event_time de stream).
- Contrato `Natureza` (`ADR-016`): a tabela de shift e a comparação de timestamps são lógica pura de `domain`; qualquer leitura de arquivo real de dump/REST para o teste mora em fixture, não em I/O de produção.

## Restrições do repositório (as mesmas de toda task desta sessão)

- Import-linter (`Natureza` incl.), cobertura domain 90/use_cases 80/infra 70%, idioma em inglês (produção e teste, inclusive mensagem de exceção — `CLAUDE.md` §"Mensagem de exceção"), `docs/INDEX.md` append-only, sem `Co-Authored-By:`.
- Push em worktree: `HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness` (nunca `.harness/mechanism`), URL explícita (`https://github.com/StharleyMax/cripto-strategy.git`), **nunca `--force-with-lease`**.
- **⚠️ Múltiplas sessões e múltiplas tasks desta MESMA fase rodam em paralelo agora** — sempre `git fetch` + resolva contra `master` mais recente antes do push; conflito típico em `docs/INDEX.md`/`backend/README.md` — mantenha AMBAS as seções.
- **Aguarde ATIVAMENTE `bash backend/scripts/test.sh` terminar** — não dispare em background esperando notificação; ela não acorda subagente nesta sessão.

## Protocolo de custo — R6/R7

- Se projetar passar de ~150 turnos, PARE e escreva o estado neste arquivo.
- Devolva **no máximo 15 linhas**, caminho do relatório em `docs/context/plataforma-dados/gates/T-06.2-builder.md`. **Nunca cole o corpo.**

## Entrega

Commit no branch `tasks/T-06.2-fase06`, push, PR aberta contra `master`.
