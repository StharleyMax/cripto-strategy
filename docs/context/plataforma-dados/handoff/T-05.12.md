# Handoff T-05.12 para o builder

Decisão completa: `docs/context/plataforma-dados/gates/T-05.12-infra-architect.md` — leia inteiro
antes de codar, tem o comando de cada número e o porquê de cada escolha.

Resumo executável:

1. `backend/pyproject.toml`: `poetry add fastapi uvicorn`, depois corrigir para pin exato
   `fastapi (==0.141.1)` e `uvicorn (==0.52.4)` (§3 do gate) — sem `[standard]`.
2. `backend/src/modules/sentimento/domain/ingest_record.py`: extrair projeção para `dict` +
   `IngestHealthReport.to_envelope()` público (§5) — não muda nenhuma coluna nem o `fingerprint()`.
3. Novos diretórios `backend/src/api/` e `backend/src/main/` (árvore exata em §4 do gate).
   `src/api/dependencies.py` NUNCA importa `src.modules.sentimento.infra` — quem constrói
   `SqliteIngestRecordStore` é `src/main/__init__.py` via `app.dependency_overrides`.
4. Handler (`src/api/routes/ingest_health.py`): só `Depends(...)` + `ingest_health_query(source)`
   + `report.to_envelope()`. Zero SQL, zero import de `infra`.
5. Bind `127.0.0.1` (loopback), rota `GET /ingest-health` (placeholder, §6/§7 do gate).
6. Teste novo: `backend/tests/api/test_ingest_health_route_over_the_network.py` — técnica em §8
   do gate (uvicorn.Server em thread, `http.client` real, par morde/cala).

Ordem de execução: §9 do gate, passos 1–6. Rode `make lint && make boundaries` antes de devolver.
