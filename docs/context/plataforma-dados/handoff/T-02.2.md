# Handoff — T-02.2 · One-shot Coinalyze daily (OI + liquidação), nascendo em quarentena

**Card:** `CST-13` · **Componente:** `sentimento` · **Base:** `master@634833b` · **Worktree:** `/tmp/claude-1002/wt/T-02.2`

## Por que agora, e um aviso sobre chave de API

`Q1` respondida (2026-09-01, `docs/decisoes-do-owner.md` §Q1) — destrava esta task. **Esta é a ÚNICA
das 6 desta rodada que precisa de chave real** (`$COINALYZE_API_KEY`, em `.env`, gitignored,
permissão 600). **Verifique você mesmo se ela existe** (`test -f .env && grep -q COINALYZE_API_KEY
.env`) — se não existir, **PARE, não invente uma chave nem peça para o usuário digitá-la em texto**,
escreva o código com a chamada real implementada e testada contra fixture/mock, documente no
relatório que a chamada ao vivo (o "one-shot" real) fica pendente da chave, e devolva assim mesmo.
**Nenhuma chave em documento, nunca** (`CLAUDE.md`).

Escopo desta rodada: código do coletor + prova. Se a chave existir, o "one-shot" É a operação real
(não é um coletor contínuo — chama uma vez, guarda em quarentena, acaba) — rodar é barato e seguro
mesmo sem frontend, já que não é um processo de longa duração.

## O DoD

Bloco `id = "T-02.2"` em `docs/context/plataforma-dados/tasks.toml`. Refs: `CA-F0-13`, `CA-F3-9`,
`avaliacao:A3`, `SPEC-001 §5.2`, plano `02` itens **2.3+2.4**, DoD **D2.6**. Linha completa em
`docs/context/plataforma-dados/tasks_review.md:252`.

- One-shot Coinalyze `daily` — **OI** (≥ 2.400 pontos, 1ª data ≤ 2020-01-21) e **liquidação**
  (≥ 700 pontos, 1ª data ≤ 2024-08-26) — **nascendo em QUARENTENA** (não visível a consumidor até
  validação explícita).
- **Broker de cota CEGO:** a resposta `200` da Coinalyze **não traz nenhum header de cota**
  `[MEDIDO]` — a contagem tem de ser **local e conservadora**, nunca por leitura de header.
- **D2.6:** leitura de `backtest` sobre as duas séries recém-capturadas devolve **ZERO linhas**
  (elas estão em quarentena, ainda não promovidas) — prove isso com um teste que tenta ler e
  confirma vazio.
- Custo declarado do one-shot real: **1.140 chamadas ≈ 28,5 min, uma vez** (se a chave existir e
  você rodar de verdade — não repita isso múltiplas vezes por engano).

## Contexto que já custou defeito aqui

- **Resíduo `Q4`** (Coinalyze free/pago/descartar): parcialmente resolvido, "o mecanismo anda" —
  não é um segundo bloqueio duro, é uma decisão de produto pendente que não impede o código.
- **Quarentena não é um campo booleano ingênuo** — a task-irmã `T-03.11` (reconciliação diária) vai
  ler esta série depois; desenhe o esquema de promoção pensando nesse consumidor futuro, mas não o
  construa aqui (fora de escopo).
- Contrato `Natureza` (`ADR-016`/`T-03.12`): chamada de rede mora em `infra`, nunca em `domain`.

## Restrições do repositório (as mesmas de toda task desta sessão)

- Import-linter, 3 contratos, incl. `Natureza`. Cobertura: domain 90/use_cases 80/infra 70%.
- Idioma de identificador em inglês (produção e teste). `docs/INDEX.md` append-only.
- Sem `Co-Authored-By:`. Autor e committer são o owner.
- Push em worktree: `HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness`,
  nunca apontado para `.harness/mechanism`. URL explícita (`https://github.com/StharleyMax/cripto-strategy.git`),
  nunca `origin`/SSH. **Nunca `--force-with-lease`** — use `--force` simples se precisar.

## Protocolo de custo — R6/R7

- Se projetar passar de ~150 turnos, PARE e escreva o estado neste arquivo.
- Devolva **no máximo 15 linhas**: veredito, números com o comando (incluindo se a chamada real
  rodou ou ficou pendente de chave), caminho do relatório em
  `docs/context/plataforma-dados/gates/T-02.2-builder.md`. **Nunca cole o corpo.**

## Entrega

Commit no branch `tasks/T-02.2-coinalyze-one-shot`, push, PR aberta contra `master`.
