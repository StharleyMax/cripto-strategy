# Handoff — T-07.14 (`plataforma-dados`, fase 07, componente `web`)

**Worktree:** `.claude/worktrees/T-07.14` · branch `tasks/T-07.14-s5-embutido` (a partir de
`origin/master` em `a5be162`, já com `T-07.8` fechada) — isolado, diretriz permanente pós-incidente.

## O que a task pede

`title`: *"S5 embutido: seletor por `universe_at`, badge de delisting de `deliveryDate`,
`universe_source` carimbado"*. `depends_on = ["T-07.8"]` — **done** (`universe_at.py`, PR #88, QA
APPROVED, review COMPLIANT).

## S5 NÃO é tela — isso muda a natureza da task

`docs/specs/SPEC-001-plataforma-dados.md:521` (tabela §6): **"S5 universo point-in-time — não é
tela: `universe_at(ts, filtro)` atrás de todo seletor"**. `docs/product/STITCH_CONTEXT.md:136`
confirma o mesmo. Isto não é uma tela nova — é lógica embutida atrás de QUALQUER seletor de símbolo
que outras telas (S1, S2...) venham a ter. `tasks_review.md:347`: *"o seletor de símbolo NÃO EXISTE
fora de `universe_at`"* — ou seja, nenhum componente de seleção de símbolo pode existir sem passar
por esta função.

## O contrato normativo — `universe_at.py` (backend, `T-07.8`, PURO — sem I/O)

`backend/src/modules/sentimento/domain/universe_at.py` (leia, não edite — `backend/` intocável).
É lógica de domínio pura (`ADR-016`/Natureza: "no socket, no clock, no file") — mesma classe de
`ThresholdSpec` em `T-08.5`: **reimplementar o algoritmo em TS, citando a fonte, é o caminho certo**,
não invocar um CLI Python que **não existe** (o próprio builder de `T-07.8` registrou: "não escreve
o use_case/CLI que lê snapshot real e chama `universe_at`" — não há subprocess para chamar).

Contrato a portar (nomes TS sugeridos entre parênteses):
- `UniverseSource = "snapshot" | "s3_inferred" | "premium_index_witness"` (`UniverseSource`)
- `DecisiveUniverseSource = "snapshot" | "premium_index_witness"` — **sem `s3_inferred`, por TIPO**
  (o TS não tem `mypy --strict`, mas o discriminador de união faz o mesmo papel: nenhuma função de
  decisão aceita um objeto com `source: "s3_inferred"` no tipo do parâmetro)
- `decideUniverseMembership(witnesses)` — união dos símbolos de fontes ADMISSÍVEIS só
- `UniverseFilter { market?, underlyingSubType? }` — `null`/`undefined` = "não filtra este eixo",
  não "casa linhas com o campo ausente" (mesma distinção `None`-vs-vazio do Python)
- `UniverseAtResult { ts, symbols, decidedSymbols, s3WitnessSymbols, divergence, label }` —
  `symbols` é SEMPRE a união (decidido ∪ testemunha S3), nunca só um lado
  `RETROSPECTIVE_LABEL = "retrospective_before_first_snapshot"` quando `snapshotRows` é `null`
  (ausência de testemunha admissível), nunca confundido com "decidido e vazio"

## O que esta task ACRESCENTA que `T-07.8` não fez — o `deliveryDate`

**`InstrumentRow` (`T-02.1`) NÃO tem campo `deliveryDate`** — confirmado em
`backend/src/modules/sentimento/domain/instrument_universe_snapshot.py` (grep não encontra o campo)
e no próprio `T-07.8-builder.md:98`: *"não persiste `onboard_ts`/`deliveryDate` por símbolo"*. Então
o badge de delisting **não pode vir de `universe_at`** — tem de ler o `exchangeInfo` CRU
separadamente, direto do dado real (não é `backend/`, é `data/`, liberado):

```
data/snapshots/2026-08-25_exchangeInfo.json  — 877 símbolos, captura MANUAL real de 2026-08-25
```

Verificado nesta sessão, direto no arquivo:
```python
{'symbol': 'BTCUSDT', 'deliveryDate': 4133404800000, ...}   # sentinela — SEM delisting real
{'symbol': 'ICXUSDT', 'deliveryDate': 1787734800000, ...}   # = 2026-08-26T09:00:00Z, TRADING
{'symbol': 'STORJUSDT', 'deliveryDate': 1787734800000, ...} # idem
{'symbol': 'SCRTUSDT', 'deliveryDate': 1787734800000, ...}  # idem
```
**Sentinela `4133404800000` em 568/570 símbolos** (`docs/recorte-plataforma.md:90`) — qualquer
`deliveryDate` DIFERENTE da sentinela é delisting real agendado ⇒ badge. Não adivinhe o valor da
sentinela por outro caminho — é este, medido no arquivo real.

## `universe_source` carimbado em TODA saída

Literal da task: `universe_source` tem de estar em toda linha que a "S5 embutida" produz para quem a
consome (não é campo opcional/debug) — cada símbolo devolvido carrega de qual fonte ele veio
(`snapshot`, `s3_inferred` ou ambos, via `divergence`).

## Casos de teste obrigatórios (ancorados em dado real, não sintético)

1. **Caso-âncora da task**: `universeAt('2026-08-25', ...)` com o snapshot real acima →
   `ICXUSDT`/`STORJUSDT`/`SCRTUSDT` aparecem com badge de delisting (`deliveryDate` ≠ sentinela),
   `BTCUSDT` sem badge.
2. **Caso retrospectivo de `T-07.8`** (mesma fixture, não reinventar): `universeAt('2025-08-01', ...)`
   sem snapshot admissível → `decidedSymbols = {}`, `label = RETROSPECTIVE_LABEL`, `symbols` vem só
   da testemunha S3 se fornecida.
3. **Divergência**: `MATICUSDT` aparece na união mas não na decisão quando ausente do snapshot
   corrente (mesmo padrão de `T-07.8`).

## Escopo — fronteira (`CLAUDE.md`)

`backend/` intocável (leitura via `git show`/`Read` apenas, nunca edição). `data/snapshots/` é dado
real, liberado para leitura (não é `backend/`, não é segredo). Sem app Next.js — prova por teste
unitário, mesma fronteira de `T-05.8`/`T-08.5`/`T-08.11`.

## Passos

1. `ui-designer`: gate de design — S5 não é tela (confirmado acima); verificar se S1/S2 canônicas já
   preveem algum slot de seletor de símbolo com espaço para badge, ou se não há nada a verificar
   ainda (nenhuma tela tem seletor de símbolo construído hoje). Registrar, não presumir.
2. `harness-plugin:builder`: `frontend/src/app/universe-at.ts` (nome sugerido) — porta o contrato de
   `universe_at.py` + a extensão de `deliveryDate`/badge que só existe aqui, usando o snapshot real
   de `data/snapshots/2026-08-25_exchangeInfo.json` como fixture de teste (não sintético).
3. `harness-plugin:qa`: mutação — reintroduzir os defeitos que `T-07.8` já provou no lado Python
   (fusão silenciosa das duas testemunhas, `s3_inferred` entrando na decisão, `RETROSPECTIVE_LABEL`
   confundido com decidido-vazio) MAIS os novos desta task (sentinela de `deliveryDate` não
   reconhecida, `universe_source` ausente de alguma saída).
4. UX audit independente (eu mesmo, R9).
5. Push por relay, PR, merge se o gate de design concordar.
