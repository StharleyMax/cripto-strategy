# Handoff — `T-04.4` · Acessor único `as_of()` = `argmin(observed_at)` com LOCF

**Card:** `CST-31` · **Componente:** `sentimento` · **Base:** `master@20144b8` ·
**Worktree:** `/tmp/claude-1002/wt/T-04.4` · **Branch:** `tasks/T-04.4-acessor-as-of`

## Por que esta é a task de maior alavancagem do backlog inteiro

`T-04.4` gateia **23 das 25 tasks de front**
`[MEDIDO 2026-08-29, fecho transitivo de depends_on sobre docs/context/plataforma-dados/tasks.toml, n=25]`.
`T-04.2` acabou de entrar em `master` (PR #39, `20144b8`), e ela era a última aresta. **Enquanto esta
não entra, a fase 05 inteira — todo o front — não pode começar.**

## O DoD

Bloco `id = "T-04.4"` em `docs/context/plataforma-dados/tasks.toml`. Refs declaradas lá. O título é o
contrato: **acessor ÚNICO** `as_of()` = `argmin(observed_at)`, com **LOCF**, **sem interpolate**,
`R-1` e `R-2`, e `knowledge_time` na leitura.

**"Acessor ÚNICO" é a metade que se perde primeiro.** Se existirem dois caminhos de leitura da série,
um deles vai divergir em silêncio. O falsificador natural: procure por qualquer outro lugar que leia a
série sem passar por aqui, e prove que não existe — não que você não achou.

**`sem interpolate` é regra anti-lookahead.** Este repositório já teve **uma regra anti-lookahead
invertida e propagada por dois documentos**, achada pela disciplina de citar o comando. Interpolar
entre dois pontos usa o ponto **futuro** para produzir o valor presente. LOCF (*last observation
carried forward*) só olha para trás. **Se você se pegar escrevendo algo que precisa do próximo ponto,
pare** — é a falha que a task existe para impedir.

## O que `T-04.2` deixou pronto para você, e o que ela deixou aberto

**Pronto:** a `SeriesKey` de 15 termos com as sete colunas de procedência, e — importante para você —
`observed_at` utilizável por `argmin`. O `sha256` da identidade tem **vetor dourado** fixado
(`2045d032…` e `c64fa8cf…`, recomputados **fora** de `_canonical_json`, de literal escrito à mão).

**Aberto, com dono, e NÃO é seu:**
- **`verified_by` na identidade** — `SPEC-001 §3.3` o lista **duas vezes**, tratando-o como metadado de
  catálogo, enquanto **§2.1** o trata como termo da chave. As duas leituras não valem juntas. Dono:
  `/architect`; portão `spec` é do owner. **Implemente pela §2.1, como a `T-04.2` fez, e não emende.**
- **`_canonical_json` duplicado** — dono `T-04.6`, e agora está **coberto pelo vetor dourado**.

## Uma lição do ciclo anterior que vale para a sua bancada

A bancada do build da `T-04.2` publicou **10 mortos / 0 sobreviventes**. O `/qa` rodou a própria
(`n=15`) e achou **11 mortos / 4 SOBREVIVENTES** — todos numa superfície que a primeira não cobria.

E o conserto teve o próprio defeito: **fixar o vetor dourado NÃO matou `Q3` (`ensure_ascii`)**, porque
sobre chave 100% ASCII as duas grafias dão **bytes idênticos**. Precisou de um segundo vetor com
`unit="µBTC"`. **Publique a atribuição de matadores por mutante**, não só o placar — foi ela que
expôs o `Q3 → 1 só`.

## Restrições do repositório que mordem aqui

- **Contrato `Natureza`** (`backend/pyproject.toml` `[tool.importlinter]`): `domain` e `use_cases`
  **não importam** `socket`, `ssl`, `time` nem `datetime`. **E esta task lida com TEMPO** — logo
  `knowledge_time` e `observed_at` entram como **valor injetado**, nunca lidos do relógio dentro das
  camadas puras. Se isso parecer impossível, é sinal de que a fronteira está no lugar errado; escreva o
  argumento em vez de afrouxar o contrato.
- **Pisos de cobertura por camada:** domain 90, use_cases 80, infra 70. Hoje 100 / 100 / 97,7.
- **Idioma:** identificador de produção **e de teste** em **inglês** (`CLAUDE.md`, linhas 1 e 2),
  incluindo parâmetro, variável local e constante de módulo. Exceção viva: nome de **coluna de
  contrato** (linha 11). Mensagem de `raise` é lacuna `⏸ NÃO DECIDIDO` — não a use como licença.
- **Evento de log e chaves de `extra={}`: inglês, prospectivamente** (linha 10). Hoje 4 PT de 9, e
  **o número não pode subir**. ⚠️ O critério que mede isso (`CA-F4-5` da `codigo-em-ingles`) é **cego a
  chamadas quebradas em duas linhas** — então a disciplina aqui é sua, não do portão.
- **`docs/INDEX.md` é append-only.** Verifique **por conjunto**, nunca por `git diff | grep '^-'`.
- **Nenhum número sem o comando que o produziu**, com `n` e rótulo de força. **Três defeitos desta
  família saíram hoje, os três em relatório de gate** — número publicado ao lado de comando que não o
  produz. Confira cada número do seu relatório rodando o comando que você escreveu ao lado dele.
- **Sem `Co-Authored-By:`.** Autor e committer são o owner.

## Protocolo de custo — vinculante, R6/R7

`docs/protocolo-de-despacho.md`. **Leia R6 e R7 antes de começar.**

- **`make verify`** roda os seis portões numa chamada. Ele distingue `VERDE` de `INDETERMINADO`:
  **`rc=3` é *"não mediu"*, e não é *"passou"*.**
- **Se projetar passar de ~150 turnos, PARE**, escreva o estado neste arquivo e devolva.
- Agrupe medições independentes numa chamada só; não releia arquivo já lido.

**Ambiente:** este worktree não tem `.venv` nem `frontend/node_modules`, e sem eles os portões dão
`rc=3`. Linke-os de `/home/stharley/Documentos/projects/cripto-strategy/` e **apague os symlinks antes
de qualquer `git add -A`** — o `.gitignore` usa barra final e symlink não casa padrão de diretório.
Exporte `HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness`
para o `pre-push` resolver.

## Já feito neste branch, não refaça

O status de `T-04.2` foi virado para `done` no `tasks.toml` (commit de coordenação, separado) — era
dívida deixada pela PR #39.

## Entrega

Commit no branch `tasks/T-04.4-acessor-as-of`, PR contra `master`. Relatório completo em
`docs/context/plataforma-dados/gates/T-04.4-builder.md`.

**Devolva no máximo 15 linhas:** veredito, números com o comando que os produziu, o `n`, e o caminho do
relatório. **NUNCA cole o corpo.**

---

# ESTADO — ciclo 1 interrompido por limite de gasto, 2026-08-29T19:2xZ

**O agente anterior morreu com trabalho NÃO COMMITADO na árvore.** Nada foi perdido: os três
arquivos estão em disco, `git status` os mostra como `??`.

```
backend/src/modules/sentimento/domain/as_of_accessor.py           458 linhas
backend/tests/sentimento/test_as_of_accessor.py                   699
backend/tests/sentimento/test_as_of_is_the_single_reader.py       149
                                                                 ----
                                                                1.306
```

**Base:** `dfff81f` (o commit de coordenação que virou `T-04.2` para `done`). **Nada commitado além
disso.**

## O achado que ele estava consertando quando parou — e ele é REAL, não confusão

Última linha que ele devolveu, literal:

> *"Publication lag exposed a real modelling gap: the `FLOW` no-carry rule needs the bucket
> interval, not `age > 0`. Fixing both module and fixture."*

**O argumento, já escrito por ele em `as_of_accessor.py:170-174` e reproduzido aqui porque é o que
decide a task:**

> *"`LOCF` sobre `FLOW` é erro de tipo" means a flow value stops being the answer once a WHOLE
> BUCKET has gone by, and "a whole bucket" is not derivable from `bucket_end` and `t` alone.
> **Publication lag is why `age_ms > 0` is not a substitute: a bucket becomes readable one lag AFTER
> it closes, so under `age_ms > 0` a flow series would be unreadable for ever.**"*

Isto é: a guarda ingênua (*"flow não carrega se tem qualquer idade"*) **torna toda série de fluxo
permanentemente ilegível**, porque o dado só fica legível depois do lag de publicação — sempre com
idade > 0. A guarda correta usa **o intervalo do bucket**, e é por isso que
`bucket_interval_ms` entra como campo **injetado** (`:162`), **nunca derivado de
`SeriesKey.interval`**, que carrega a string `"5m"`.

O código já reflete a correção em `:330`:

```python
if age_ms >= policy.bucket_interval_ms and not CARRY_FORWARD_BY_NATURE[series.nature]:
```

**⚠️ Ele disse *"fixing both module and fixture"* — o módulo parece feito; CONFIRA A FIXTURE.** Um
conserto meio-feito com a suíte verde é o pior desfecho possível aqui.

## O que fazer, na ordem

1. **Rode a suíte antes de tocar em qualquer coisa** e publique o estado encontrado. Se estiver
   vermelha, a fixture é o suspeito nomeado acima.
2. **Termine o que falta**, não recomece. As 1.306 linhas têm docstring densa citando `SPEC-001`
   §3.2/§5.11 e `D4.11` — leia antes de reescrever.
3. **A bancada de mutação ainda não existe.** Publique **atribuição de matadores por mutante**, não
   só o placar. Precedente medido nesta feature: o build da `T-04.2` publicou `10/0` e o `/qa` achou
   `11/4`; e o conserto teve o próprio defeito, porque o vetor dourado **não matava** `Q3`.
4. **Mutante obrigatório, e ele é o desta task:** troque
   `age_ms >= policy.bucket_interval_ms` por `age_ms > 0` e prove que a suíte **reprova**. Se ela
   passar, o achado acima não está fixado por teste e a task não está pronta — é exatamente a
   diferença entre *"consertei"* e *"o conserto não pode voltar"*.
5. **Segundo mutante obrigatório:** faça `bucket_interval_ms` ser derivado de `SeriesKey.interval`
   em vez de injetado, e prove que reprova — `:165` declara que a injeção é a regra.

## O resto do handoff acima continua valendo

Em especial: acessor **único** (prove que não há outro caminho de leitura), **anti-lookahead** (LOCF
olha para trás; interpolate usa o ponto futuro), contrato **`Natureza`** (`time`/`datetime` proibidos
em `domain`/`use_cases` — tempo é valor injetado), e pisos de cobertura 90/80/70.
