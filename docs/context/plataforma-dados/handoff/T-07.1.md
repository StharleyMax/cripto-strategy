# Handoff — T-07.1 · Paginador correto: janela `[startTime, endTime]` fechada, A PRIORI

**Card:** `CST-55` · **Componente:** `sentimento` · **Base:** `master@031e007` · **Worktree:** `/tmp/claude-1002/wt/T-07.1`

## Por que agora

`T-01.1` já mesclada. Owner autorizou puxar fase 07 backend (2026-09-02). **Prioridade alta:**
esta task é **pré-requisito de qualquer backfill grande** (`T-03.10` e além) — não é só mais uma task
da fase, é a que destrava as outras.

## O DoD

Bloco `id = "T-07.1"` em `docs/context/plataforma-dados/tasks.toml`. Refs: `CA-F3-2`, `CA-F3-1`,
`CA-F3-5`, `SPEC-001 §5.7`, plano `07` itens **7.1+7.2+7.4**. Linha completa em
`docs/context/plataforma-dados/tasks_review.md:334`.

- **Janela `[startTime, endTime]` fechada e enumerada A PRIORI** — nunca por cursor. `-1130` (código de
  erro da Binance) é **fim de histórico**, não erro transitório. Listagem S3 por `NextContinuationToken`.
- **D7.1**: `startTime` de 60 dias atrás ⇒ `verdict='REJECTED'`, `api_code=-1130`, **zero linhas
  gravadas**.
- **D7.3** (o ponto mais perigoso desta task): replay do caso `startTime` **sozinho** tem que
  **reprovar, não gravar** — `[MEDIDO]`: a API devolve **a cauda de HOJE, HTTP 200, sem aviso**,
  comportamento **NÃO DOCUMENTADO**. Um paginador ingênuo com `cursor += janela` **nunca avança** e
  grava dado de hoje rotulado com timestamp de semanas atrás — corrupção silenciosa de `available_at`.
  Escreva um teste que planta esse comportamento (resposta de hoje disfarçada de página antiga) e prove
  que o paginador RECUSA, não aceita.
- **D7.4**: invariante permanente — nenhum timestamp gravado fora da janela requisitada.
- **D7.5**: `limit=501` já foi medido devolvendo **501 linhas** contra doc de máximo 500 — use o
  **observado**, não o documentado, no teste de limite.
- **D7.8**: falha se `IsTruncated=true` sem paginação subsequente. `980 prefixos` contra `MaxKeys=1000`
  — folga de 20, e o universo cresce (`+28 símbolos em 30d`, `+136 em 90d` `[MEDIDO]`) — não hardcode
  o limite sem checar `IsTruncated`.

## Contexto que já custou defeito aqui

- O bug de D7.3 é o tipo de coisa que passa despercebida se você só testar o caminho feliz — a Binance
  devolvendo dado de HOJE para uma consulta de janela ANTIGA, com `HTTP 200`, é o comportamento real
  medido, não uma hipótese. Teste isso explicitamente, não assuma que "erro" sempre vem com status de
  erro.
- Contrato `Natureza` (`ADR-016`): chamada de rede/S3 mora em `infra`, nunca em `domain`/`use_cases`.

## Restrições do repositório (as mesmas de toda task desta sessão)

- Import-linter (`Natureza` incl.), cobertura domain 90/use_cases 80/infra 70%, idioma em inglês
  (produção e teste — inclusive mensagem de `raise`/`Error`/`Exception`, decisão do owner registrada em
  `CLAUDE.md` §"Mensagem de exceção"), `docs/INDEX.md` append-only, sem `Co-Authored-By:`.
- Push em worktree: `HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness`
  (nunca `.harness/mechanism`), URL explícita (`https://github.com/StharleyMax/cripto-strategy.git`,
  não `origin`/SSH), **nunca `--force-with-lease`** — use `--force` simples se precisar.
- **⚠️ Este repositório tem MÚLTIPLAS sessões de Claude Code rodando em paralelo no mesmo diretório
  principal** (`/home/stharley/Documentos/projects/cripto-strategy`), mas você está numa worktree
  ISOLADA (`/tmp/claude-1002/wt/T-07.1`) — isso te protege de colisão de branch, mas **antes de
  fazer push, sempre `git fetch` + rebase/merge contra `master` mais recente**, porque outra sessão
  pode ter mesclado algo em `docs/INDEX.md`/`backend/README.md` (mesmo padrão de conflito já visto
  nesta sessão — resolva mantendo AMBAS as seções, nunca descartando).
- **Ao rodar `bash backend/scripts/test.sh`, aguarde ATIVAMENTE terminar antes de encerrar seu turno**
  — não dispare em background e diga "vou esperar a notificação": isso não funciona entre turnos de
  subagente nesta sessão (medido repetidamente). Continue chamando ferramentas até confirmar via novo
  comando que o processo já terminou.
- **Nenhum teste depende do texto de mensagem de exceção sem checar antes** — se você mudar uma
  mensagem já existente, rode a suíte cedo; vários testes deste repo usam `pytest.raises(match=...)`.

## Protocolo de custo — R6/R7

- Se projetar passar de ~150 turnos, PARE e escreva o estado neste arquivo.
- Devolva **no máximo 15 linhas**, caminho do relatório em
  `docs/context/plataforma-dados/gates/T-07.1-builder.md`. **Nunca cole o corpo.**

## Entrega

Commit no branch `tasks/T-07.1-fase07`, push, PR aberta contra `master`.
