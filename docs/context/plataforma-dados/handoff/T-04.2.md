# Handoff — T-04.2 · SeriesKey completa + as sete colunas de procedência

**Card:** `CST-29` · **Componente:** `sentimento` · **Base:** `master@aac3442` · **Worktree:** `/tmp/claude-1002/wt/T-04.2`

## Por que esta task é a de maior alavancagem do backlog

`T-04.2` e `T-04.4` gateiam **23 das 25 tasks de front**
`[MEDIDO 2026-08-29 em aac3442, fecho transitivo sobre docs/context/plataforma-dados/tasks.toml, n=25]`.
`T-04.2` é a única das duas desbloqueada hoje (`depends_on = ["T-01.1"]`, `done`), e `T-04.4` depende
diretamente dela. **Enquanto ela não entra, a fase 05 inteira — todo o front — não pode começar.**

## O DoD

Bloco `id = "T-04.2"` em `docs/context/plataforma-dados/tasks.toml`. Refs declaradas:
`ADR-001` · `CA-F2-17` · `SPEC-001 §2.1` · `SPEC-001 §3.1` · plano 04 itens **4.7 + 4.9** ·
DoD **`D4.9`** · **falsificador global `F-2`**.

**15 termos** na `SeriesKey`, incluindo `quantity_field` e `reduction`, mais as **sete colunas de
procedência**. Os números vêm da SPEC — leia-os de lá e **não os reproduza de memória**; se o que a
SPEC diz divergir do que o `tasks.toml` diz, isso é um achado a reportar, não uma ambiguidade a
resolver sozinho.

## Contexto que já custou defeito aqui

- `reduction` na `SeriesKey` é o que `T-06.5` usa para dizer que **OI da Coinalyze = 4 linhas**. A
  modelagem tem de comportar isso sem uma coluna nova depois.
- `T-04.4` (o acessor `as_of()`) lê o que esta task escrever. Modelar procedência sem `observed_at`
  utilizável por `argmin` quebraria a task seguinte silenciosamente.

## Restrições do repositório que mordem aqui

- **Contratos `import-linter`** em `backend/pyproject.toml` `[tool.importlinter]`: camadas
  (`infra > use_cases > domain`), proibições por contexto, e o contrato **`Natureza`** —
  `domain` e `use_cases` **não importam** `socket`, `ssl`, `time` nem `datetime`. Se precisar de
  tempo no domínio, ele entra como **valor injetado**, não como import.
- **Pisos de cobertura por camada:** domain 90%, use_cases 80%, infra 70% (hoje 100/100/100).
- **Idioma:** identificador de produção **e de teste** em **inglês** (`CLAUDE.md`, linhas 1 e 2) —
  incluindo parâmetro, variável local e constante de módulo. Exceção viva: **nome de coluna de
  contrato** pode ficar em português (linha 11, `ingest_record.py:87-89`). Mensagem de `raise` é
  **lacuna declarada, `⏸ NÃO DECIDIDO`** — não a use como licença para português novo.
- **Evento de log e chaves de `extra={}`: inglês, prospectivamente** (linha 10). Hoje são 4 em
  português de 9; **o número não pode subir**.
- **`docs/INDEX.md` é append-only.**
- **Nenhum número sem o comando que o produziu**, com `n` e rótulo de força.
- **Sem `Co-Authored-By:`.** Autor e committer são o owner.

## Protocolo de custo — vinculante, R6/R7

`docs/protocolo-de-despacho.md` mudou hoje (PR #37). Leia **R6** e **R7** antes de começar:

- **`make verify`** roda os seis portões numa chamada, ~10 linhas impressas, saída bruta em arquivo.
- **Se projetar passar de ~150 turnos, PARE**, escreva o estado neste arquivo e devolva.
- Agrupe medições independentes numa chamada só; não releia arquivo já lido.
- Devolva **no máximo 15 linhas**: veredito, números com o comando, e o caminho do relatório completo
  em `docs/context/plataforma-dados/gates/T-04.2-builder.md`. **Nunca cole o corpo.**

## Entrega

Commit no branch `tasks/T-04.2-serieskey-completa`, PR aberta contra `master`.
