# Handoff — T-06.6 · Quarentena pelo predicado de TRÊS termos

**Card:** `CST-50` · **Componente:** `sentimento` · **Base:** `master` (faça `git fetch` antes de criar a worktree — várias tasks da fase 06 rodam em paralelo) · **Worktree:** `/tmp/claude-1002/wt/T-06.6`

## Por que agora

`T-06.1` (`series_catalog`) já mesclada. Owner autorizou puxar a fase 06 inteira em paralelo (2026-09-03). **`T-06.7` e `T-06.8` dependem desta task** — elas só começam depois que esta mergear. `T-06.2`, `T-06.3`, `T-06.4`, `T-06.5`, `T-06.9`, `T-06.10` estão sendo construídas ao mesmo tempo que esta.

## O DoD — este é o falsificador central da fase inteira

Bloco `id = "T-06.6"` em `docs/context/plataforma-dados/tasks.toml`. Refs: `CA-F2-2`, `CA-F2-13`, `SPEC-001 §5.2`, plano `06` item **6.12**, DoD **D6.1/D6.2 (falsificador da fase)/D6.3**. Linha completa em `docs/context/plataforma-dados/tasks_review.md:321`, `docs/specs/PRD-001-plataforma-dados.md:587,297`.

- **Quarentena pelo predicado de TRÊS termos**: uma série entra na "gaveta" (quarentena) se `label_shift IS NULL OR unit IS NULL OR available_at IS NULL` — os TRÊS termos, não um só. `label_shift` e `unit` já são campos de `SeriesKey`/`series_catalog` (`T-06.1`); `available_at` é um conceito de `md.ingest_run`/`T-03.x` (a série ao vivo carrega `available_at` quando a defasagem foi medida — ver `Q19`/`T-03.6`).
- **Regra de ESCRITA, explícita**: um endpoint **SEM `lag_ms` medido** grava `available_at = NULL` + `availability_source = MODELED`, e a série **NASCE ISOLADA** (em quarentena) — nunca grava `event_time` nem `event_time + interval` como aproximação de `available_at` (isso seria **361× otimista**, `[MEDIDO]`).
- **D6.1**: `count(gaveta) == count(catálogo WHERE <predicado de 3 termos>)` **E** `count(painéis sincronizados ∩ quarentena) == 0` — sobre **todo o catálogo**. Um painel sincronizado nunca pode conter uma série em quarentena.
- **D6.2 — O FALSIFICADOR DA FASE, literal**: uma série com `label_shift` **E** `unit` PREENCHIDOS mas `available_at` **NULL** ⇒ uma leitura de `backtest` sobre ela devolve **ZERO linhas**, e a gaveta CONTA essa série. **Se D6.2 devolver QUALQUER linha, um mecanismo de três termos que se abre quando só dois passam NÃO É um mecanismo de três termos** — este é o teste mais importante desta task, prove com pelo menos uma série real da Coinalyze (que hoje tem `label_shift`/`unit` resolvidos mas `available_at` ainda não, porque a defasagem dela depende de `Q19`/`T-03.6`).
- **D6.3**: ingerir uma linha AO VIVO de um endpoint sem `lag_ms` grava `NULL`+`MODELED` (nunca aproxima com `event_time`).
- **Nota importante**: a medição da Coinalyze já resolveu `unit` e `label_shift`, mas NÃO resolveu `available_at` — então a quarentena dela **não se abre** com esta task; abre quando `Q19` (`T-03.6`, já `RESPONDIDA`/`done`) tiver a defasagem medida para a Coinalyze especificamente. Não force uma abertura prematura.

## Contexto que já custou defeito aqui

- Reuse `series_catalog.py`/`SeriesKey` de `T-06.1` inteiro antes de codar — ele documenta explicitamente que a quarentena É desta task (`T-06.6`), não de `T-06.1`.
- Não confunda esta quarentena com a leitura de `backtest`/`as_of` já existente (`T-04.x`) — esta task decide QUAIS séries a leitura de backtest pode ver; não reimplemente o acessor `as_of` em si.
- Contrato `Natureza` (`ADR-016`): o predicado de quarentena é lógica pura de `domain`; a persistência de `available_at`/`availability_source` em `md.ingest_run` é `infra`.

## Restrições do repositório (as mesmas de toda task desta sessão)

- Import-linter (`Natureza` incl.), cobertura domain 90/use_cases 80/infra 70%, idioma em inglês (produção e teste, inclusive mensagem de exceção), `docs/INDEX.md` append-only, sem `Co-Authored-By:`.
- Push em worktree: `HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness`, URL explícita, **nunca `--force-with-lease`**.
- **⚠️ Múltiplas sessões e múltiplas tasks desta MESMA fase rodam em paralelo agora** — sempre `git fetch` + resolva contra `master` mais recente antes do push; conflito típico em `docs/INDEX.md`/`backend/README.md` — mantenha AMBAS as seções.
- **Aguarde ATIVAMENTE `bash backend/scripts/test.sh` terminar** — não dispare em background esperando notificação.

## Protocolo de custo — R6/R7

- Se projetar passar de ~150 turnos, PARE e escreva o estado neste arquivo.
- Devolva **no máximo 15 linhas**, caminho do relatório em `docs/context/plataforma-dados/gates/T-06.6-builder.md`. **Nunca cole o corpo.**

## Entrega

Commit no branch `tasks/T-06.6-fase06`, push, PR aberta contra `master`.
