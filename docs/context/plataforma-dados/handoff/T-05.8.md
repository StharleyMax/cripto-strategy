# Handoff — T-05.8 (`plataforma-dados`, fase 05, componente `web`)

**Escrito pelo `/workflow` em 2026-09-02.** Task escolhida porque é a primeira do vocabulário
`web` sem dependência de backend pendente.

## Por que esta task, e não outra

`depends_on = ["T-04.4"]` — `T-04.4` está `done`. É a única aresta. `[MEDIDO: grep -n -A6
'id = "T-05.8"' docs/context/plataforma-dados/tasks.toml]`

Toda outra task `web` do backlog encadeia nesta ou em algo do componente `sentimento` ainda
`todo`: `T-05.9` (também só depende de `T-04.4` — candidata igualmente livre, mas `T-05.8` vem
primeiro no arquivo e no plano) → `T-05.10` depende de `T-05.8` → `T-06.10`/`T-07.12` dependem
de `T-05.8` **e** de tasks `sentimento` (`T-06.1`, `T-02.3`) → `T-08.5` depende de `T-05.8`.
`T-05.8` é a raiz da árvore `web`.

## Zero conflito com as sessões em paralelo — verificado, não assumido

`git worktree list` mostra 15 worktrees ativas, todas em `tasks/T-0{2,3,4}.*` — **100%
componente `sentimento`**, zero `web`, zero `charts`. Confirma o que
`decisoes-de-execucao-2026-08-28.md` §1.2 já previa: as 16 tasks daquele escopo (12
`sentimento`, 4 `docs`) e a trilha de layout **não tocam os mesmos arquivos por construção**.

`ui-designer` (`R4` do próprio agente) **nunca edita `frontend/`** — só `docs/product/` e o
Stitch. A colisão possível seria só em `docs/product/STITCH_CONTEXT.md` / `DESIGN_SYSTEM.md`,
e nenhuma worktree ativa toca esses arquivos (`grep -rl "STITCH_CONTEXT\|DESIGN_SYSTEM"` sobre
os `git status` das worktrees não foi necessário — nenhuma delas é `web`).

## O que já está decidido — leia antes de gerar qualquer coisa nova

`docs/product/STITCH_CONTEXT.md` §7 já fixa, como **imutável sem pedido explícito**:

- **D1** — `AO VIVO` × `COMO EM T` são modos de primeira classe no chrome, não em menu.
- **D2** — voltar de `COMO EM T` para `AGORA` **tem sintoma visível**; sem sintoma, reprova
  (é o teste negativo obrigatório do `DoD D5.4`).
- **D7** — o **bundle é a URL**; gerenciador de presets é produto prematuro.

A `S2` canônica (`8174234965cd4ffbacfb7b2a0a61a427`, aprovada, 0 reprovações — §4.1.0) **já
modela** `AO VIVO`/`COMO EM T` no chrome (Prompt de Continuidade, §9, linhas ~1548/1704/1748).

## O que a task pede, literal

`title`: *"knowledge_time na URL; o bundle É a URL, não um CRUD; COMO EM T sobrevive à
navegação"*. `refs`: `SPEC-001 §7`, `plano 05 item 5.6`, `DoD D5.4`, "teste negativo
obrigatório". Ver `docs/plans/SPEC-001-plataforma-dados/05_fatia_visivel.md:17,32`.

## O que isto parece ser, e a decisão fica com quem executa

Pelas fontes acima, o **design visual já está resolvido** (D1/D2/D7 + `S2` canônica). O que
falta parece ser **contrato de URL/estado**, não uma tela nova — mas quem confirma isso é o
`ui-designer`, seguindo `R5` (nunca redesenhar sem instrução explícita) e `R9` (autonomia com
gate `ux-ui-mastery`). Se não houver decisão de UI/UX nova, registre isso como veredito — não
é obrigatório inventar uma tela onde a `S2` já cobre.

## Próximo passo depois do design

`ui-designer` **nunca edita `frontend/`** (R4 do próprio agente) — a implementação em código é
de `/build` (`harness-plugin:builder`), depois do veredito do gate `ux-ui-mastery`.
