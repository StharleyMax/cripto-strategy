# Handoff — T-08.5 (`plataforma-dados`, fase 08, componente `web`)

**Worktree:** `.claude/worktrees/T-08.5` · branch `tasks/T-08.5-thresholdspec-bundle` (a partir de
`origin/master` em `34141bb`, já com T-08.11 mergeada) — isolado, mesma diretriz permanente pós-incidente
de colisão entre sessões.

## O que a task pede

`title`: *"Bundle de parâmetros versionado e hasheável — que É a URL, não um CRUD; nenhum eixo tem
default"*. `depends_on = ["T-05.8"]` (done — o precedente direto: `knowledge_time` como bundle/URL).

## ⚠️ Alerta de citação — não repetir sem verificar

`docs/context/plataforma-dados/tasks_review.md:363` (documento de rascunho/revisão, **não** normativo)
descreve o DoD como *"`field`, `H`, `mode`, `direction`, `op` são explícitos"*. **Esse vocabulário NÃO
aparece em `SPEC-001` nem no plano 08** — busquei `direction`/`mode` em ambos, zero ocorrência ligada a
`ThresholdSpec`. A fonte normativa (abaixo) usa um tipo-soma diferente. Trate a lista de
`tasks_review.md` como pista não confirmada, não como o contrato — mesma classe de defeito de citação
que `T-05.9-design.md` achou uma vez (regra citada errada por um documento intermediário).

## Fontes de verdade, em ordem (as normativas)

1. `docs/specs/SPEC-001-plataforma-dados.md:292-295` (§3.7, tipo-soma) — o contrato REAL:
   ```
   ThresholdSpec = Absolute{pct, op}
                 | Percentile{q, window, scope, min_obs, interpolation, op}
                 | RobustZ{k, window, min_obs, op}
                 + spec_version + Custom{expr} DESABILITADO por padrão
   ```
   linha 303: **"`ThresholdSpec` sem default em nenhum eixo. O operador vale 20×"** — `|r| > 0,0001` →
   9/1500 contra `|r| >= 0,0001` → 184/1500, `[MEDIDO]`. `op` (`>`/`>=` etc.) é o eixo mais perigoso de
   default silencioso.
2. `docs/specs/SPEC-001-plataforma-dados.md:560-568` (§7, Reprodutibilidade) — `reproduzir(run) =
   (bundle_hash, window, knowledge_time)`; **"o bundle de parâmetros é versionado e hasheável, e ele É a
   URL — não um CRUD. Gerenciador de presets é produto prematuro e non-goal."**
3. `docs/plans/SPEC-001-plataforma-dados/08_superficie_e_reprodutibilidade.md:15,28` — item 8.4, **D8.3**:
   *"carregar a tela sem `ThresholdSpec` na URL → ZERO números derivados"* — este é o teste negativo
   obrigatório (mesmo padrão do `D5.4` de `T-05.8`).
4. `frontend/src/app/knowledge-time-bundle.ts` + `.test.ts` — **precedente direto** (T-05.8, mesma
   família de contrato: bundle versionado/hasheável, zero default, teste negativo obrigatório). Modelo
   de forma a seguir, não copiar campo a campo (o domínio é diferente: `ThresholdSpec`, não
   `knowledge_time`).
5. **`min_obs` (Percentile/RobustZ) não é opcional silenciosamente ausente** — linha 304 do SPEC:
   *"`min_obs` não atendido ⇒ AUSÊNCIA (`—`), nunca `expanding` em silêncio."* Isto é comportamento de
   RUNTIME de outra task (`T-08.7`), não desta — mas o bundle desta task é quem CARREGA `min_obs`
   explícito o suficiente para aquela regra funcionar depois. Não implemente a regra de ausência aqui,
   só garanta que o tipo não permite `min_obs` ausente/default quando `mode = Percentile | RobustZ`.

## O que NÃO existe ainda, e por quê isso muda o gate de design

`S4` (a tela que consumiria este bundle) está `[NÃO EXISTE]` em `docs/product/STITCH_CONTEXT.md §4.4`,
fase `08`, task separada (`T-08.6`, `depends_on = ["T-08.2", "T-08.5"]`). Ao contrário de T-05.9/T-08.11
(que verificaram contra a S2 JÁ canônica), aqui **não há tela para verificar convergência** — o
`ui-designer` deve registrar isso explicitamente (não simular uma verificação que não pode fazer), e o
veredito provável é "sem decisão de UI nova possível ainda — este módulo entrega o contrato de dado, S4 é
quem desenha por cima dele, tarefa própria." Se o `ui-designer` julgar que há uma decisão de UI real e
inevitável mesmo sem tela (pouco provável), ele tem autonomia para propor — mas não pode inventar S4.

## Escopo — lembrete de fronteira (`CLAUDE.md`)

`backend/` intocável. Módulo de contrato puro (encode/decode), sem app Next.js rodando — prova por teste
unitário, mesma fronteira de T-05.8/T-05.9/T-08.11.

## Passos

1. `ui-designer`: gate de design — ler §3.7/§7 diretamente (não `tasks_review.md`), confirmar que S4 não
   existe e por isso não há convergência de tela a checar; registrar veredito.
2. `harness-plugin:builder`: implementar `frontend/src/app/threshold-spec-bundle.ts` (nome sugerido,
   decisão do builder) espelhando a forma de `knowledge-time-bundle.ts` — tipo-soma `ThresholdSpec`
   fiel ao §3.7 (três variantes + `spec_version`, `Custom` desabilitado), `encode`/`decode`, `assertValid`
   que recusa QUALQUER eixo ausente (inclusive `op`, inclusive `min_obs` quando a variante o exige), e o
   teste negativo de D8.3 (URL sem `ThresholdSpec` → decode recusa, não assume).
3. `harness-plugin:qa`: mutação — reintroduzir cada default silencioso que o DoD proíbe (`op` default,
   `min_obs` ausente aceito, variante decodificada sem discriminador) e confirmar que o teste pega.
4. UX audit independente (rodo eu mesmo, R9) — mesmo formato de `T-05.9-ux-audit.md`/`T-08.11-ux-audit.md`.
5. Push via relay, PR, merge se o gate de design concordar (autorização já dada pelo owner nesta sessão).
