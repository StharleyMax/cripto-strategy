# Handoff — T-06.3 · As QUATRO séries de L/S com colunas próprias; `ls_ratio` genérico PROIBIDO

**Card:** `CST-47` · **Componente:** `sentimento` · **Base:** `master` (faça `git fetch` antes de criar a worktree — várias tasks da fase 06 rodam em paralelo) · **Worktree:** `/tmp/claude-1002/wt/T-06.3`

## Por que agora

`T-06.1` (`series_catalog`) já mesclada. Owner autorizou puxar a fase 06 inteira em paralelo (2026-09-03). `T-06.2`, `T-06.4`, `T-06.5`, `T-06.6`, `T-06.9`, `T-06.10` estão sendo construídas ao mesmo tempo.

## O DoD

Bloco `id = "T-06.3"` em `docs/context/plataforma-dados/tasks.toml`. Refs: `CA-F2-3`, `SPEC-001 §5.11`, plano `06` itens **6.3+6.10**, DoD **D6.4/D6.5/D6.6**. Linha completa em `docs/context/plataforma-dados/tasks_review.md:318` e `docs/specs/SPEC-001-plataforma-dados.md:225`.

- **As QUATRO séries de Long/Short têm colunas PRÓPRIAS** — `count_long_short_ratio`, `count_toptrader_long_short_ratio`, `sum_toptrader_long_short_ratio`, `sum_taker_long_short_vol_ratio`. **`ls_ratio` genérico é PROIBIDO** como nome de série no catálogo — não crie uma coluna/série guarda-chuva que misture as quatro.
- **A documentação do repositório dizia TRÊS séries e está errada** — esta SPEC adota QUATRO; o corpus de fixtures fixa a contagem. Não reduza para três por economia.
- **D6.4** (falsificador por autocorrelação, `[MEDIDO]`): autocorrelação lag-1 das quatro é **0,9999 / 0,9999 / 0,9996** nas três de POSICIONAMENTO e **0,0955** na de TAKER, nos 4 símbolos do conjunto declarado. **Ortogonalidade do taker**: `|r| < 0,10` em **12 de 12 pares**. Prove isso com um teste que calcula a autocorrelação sobre dado real/fixture e confirma os números.
- **D6.5**: pedir `delta()` sobre a série TAKER é **erro de TIPO**, não uma convenção documentada — a série taker é `FLOW` (fluxo), e `delta()` só faz sentido sobre séries de nível/estoque. Modele isso de forma que o tipo IMPEÇA a chamada (mesma técnica de exclusão-por-tipo já usada em `universe_at.py`/`dump_survivorship.py` desta fase 07), não uma checagem em runtime.
- **D6.6**: pedir TF de 15 min sobre a série taker tem que **RECUSAR** — a série é uma razão (ratio), e razões **não somam** entre timeframes. Nunca retorne `3,1809` — a razão verdadeira de 15 min medida é **~0,9707**, então um resampling ingênuo que soma/agrega a razão está errado por construção, e o mecanismo tem que recusar antes de produzir esse número errado.
- **Item 6.10 (dentro desta mesma task)**: `buyVol`/`sellVol` persistidos do REST `takerlongshortRatio` — não descarte esses dois campos brutos ao processar a série.

## Contexto que já custou defeito aqui

- Reuse `series_catalog.py`/`SeriesKey` de `T-06.1` — cada uma das 4 séries é uma `SeriesCatalogEntry` distinta com seu próprio `SeriesKey` (o termo `metric` diferencia as quatro).
- `Reduction`/`QuantityField`/`Nature` já existem em `series_key.py` — reuse-os; não crie um enum paralelo para o mesmo conceito.
- Contrato `Natureza` (`ADR-016`): a decisão "delta() é erro de tipo sobre FLOW" e o cálculo de autocorrelação de teste são lógica pura de `domain`.

## Restrições do repositório (as mesmas de toda task desta sessão)

- Import-linter (`Natureza` incl.), cobertura domain 90/use_cases 80/infra 70%, idioma em inglês (produção e teste, inclusive mensagem de exceção), `docs/INDEX.md` append-only, sem `Co-Authored-By:`.
- Push em worktree: `HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness`, URL explícita, **nunca `--force-with-lease`**.
- **⚠️ Múltiplas sessões e múltiplas tasks desta MESMA fase rodam em paralelo agora** — sempre `git fetch` + resolva contra `master` mais recente antes do push; conflito típico em `docs/INDEX.md`/`backend/README.md` — mantenha AMBAS as seções.
- **Aguarde ATIVAMENTE `bash backend/scripts/test.sh` terminar** — não dispare em background esperando notificação.

## Protocolo de custo — R6/R7

- Se projetar passar de ~150 turnos, PARE e escreva o estado neste arquivo.
- Devolva **no máximo 15 linhas**, caminho do relatório em `docs/context/plataforma-dados/gates/T-06.3-builder.md`. **Nunca cole o corpo.**

## Entrega

Commit no branch `tasks/T-06.3-fase06`, push, PR aberta contra `master`.
