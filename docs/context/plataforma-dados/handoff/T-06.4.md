# Handoff — T-06.4 · `funding_settled` ≠ `funding_estimado`, séries distintas

**Card:** `CST-48` · **Componente:** `sentimento` · **Base:** `master` (faça `git fetch` antes de criar a worktree — várias tasks da fase 06 rodam em paralelo) · **Worktree:** `/tmp/claude-1002/wt/T-06.4`

## Por que agora

`T-06.1` (`series_catalog`) já mesclada. Owner autorizou puxar a fase 06 inteira em paralelo (2026-09-03). `T-06.2`, `T-06.3`, `T-06.5`, `T-06.6`, `T-06.9`, `T-06.10` estão sendo construídas ao mesmo tempo.

## O DoD

Bloco `id = "T-06.4"` em `docs/context/plataforma-dados/tasks.toml`. Refs: `CA-F2-7`, `SPEC-001 §3.4`, plano `06` item **6.4**, DoD **D6.11/D6.12/D6.16**. Linha completa em `docs/context/plataforma-dados/tasks_review.md:319`.

- **`funding_settled` (o que realmente foi cobrado/pago) e `funding_estimado` (a estimativa antes do settlement) são SÉRIES DISTINTAS** — nunca a mesma `SeriesKey`, nunca um campo que sobrescreve o outro. Cada linha carrega `interval_hours_declared` **por linha** (o intervalo de funding pode mudar ao longo do tempo para o mesmo símbolo — não é constante do símbolo).
- **PK inclui `source`** — a chave primária do registro de funding distingue de qual fonte veio (settled vs estimado), então a mesma janela de tempo pode ter uma linha de cada.
- **D6.11** (falsificador medido): `settlement_slot` correto = `funding_interval_hours × 3600000`, usando o intervalo **DA PRÓPRIA LINHA** (não um valor global assumido). Medido: **0 slots fora da grade em 16.979 liquidações**, resíduo em `[0, 20] ms`, nunca negativo. **A fórmula antiga (usando um intervalo fixo assumido) erra 11.658 de 16.919 = 68,9%** — prove isso com um teste que mostra a fórmula antiga errando e a nova acertando.
- **D6.12** (dedupe): fixture `1000XECUSDT-fundingRate-2026-07.csv` tem **321 linhas**; ingerir duas vezes tem que dar `count(*) = 321` (não 642) — a dupla ingestão não duplica. Este símbolo especificamente atravessa uma transição de intervalo **8h→1h→4h** com um delta de **3,0 h** entre settlements — o teste de dedupe tem que sobreviver a essa transição sem confundir linhas.
- **D6.16**: `nextFundingTime % (interval_hours × 3600000) == 0` tem que valer para **570/570** símbolos, **com a data do snapshot registrada ao lado do número** (não um número solto sem proveniência) — no snapshot de 2026-08-25 a distribuição real é `{4h: 433, 8h: 136, 1h: 1}`. **4h é a regra (433 de 570), NÃO 8h** — não assuma 8h como default em lugar nenhum do código.

## Contexto que já custou defeito aqui

- Reuse `series_catalog.py`/`SeriesKey` de `T-06.1` — `funding_settled` e `funding_estimado` são duas `SeriesCatalogEntry` com `metric` diferente.
- O fixture `1000XECUSDT-fundingRate-2026-07.csv` provavelmente já existe em `data/` — confira `data/MANIFEST.md` antes de pedir/gerar um novo.
- Contrato `Natureza` (`ADR-016`): o cálculo de `settlement_slot` e a lógica de dedupe são `domain` puro; leitura do CSV de fixture é `infra`/teste, não produção.

## Restrições do repositório (as mesmas de toda task desta sessão)

- Import-linter (`Natureza` incl.), cobertura domain 90/use_cases 80/infra 70%, idioma em inglês (produção e teste, inclusive mensagem de exceção), `docs/INDEX.md` append-only, sem `Co-Authored-By:`.
- Push em worktree: `HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness`, URL explícita, **nunca `--force-with-lease`**.
- **⚠️ Múltiplas sessões e múltiplas tasks desta MESMA fase rodam em paralelo agora** — sempre `git fetch` + resolva contra `master` mais recente antes do push; conflito típico em `docs/INDEX.md`/`backend/README.md` — mantenha AMBAS as seções.
- **Aguarde ATIVAMENTE `bash backend/scripts/test.sh` terminar** — não dispare em background esperando notificação.

## Protocolo de custo — R6/R7

- Se projetar passar de ~150 turnos, PARE e escreva o estado neste arquivo.
- Devolva **no máximo 15 linhas**, caminho do relatório em `docs/context/plataforma-dados/gates/T-06.4-builder.md`. **Nunca cole o corpo.**

## Entrega

Commit no branch `tasks/T-06.4-fase06`, push, PR aberta contra `master`.
