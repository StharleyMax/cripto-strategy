# Handoff — T-03.2 · Coletor `!forceOrder@arr` (liquidação de mercado inteiro)

**Card:** `CST-18` · **Componente:** `sentimento` · **Base:** `master@634833b` · **Worktree:** `/tmp/claude-1002/wt/T-03.2`

## Por que agora

`Q1` respondida (2026-09-01). `T-01.1` (dependência) já `done`. **Escopo: código + probe curto de
conectividade real (1 símbolo/mensagem, como `T-03.1` já fez), NÃO um processo contínuo 24/7** — sem
frontend para exibir ainda, rodar de verdade fica para o deploy.

`T-03.3` (política de reconexão) **depende desta task** e virá numa rodada seguinte — não implemente
a lógica de reconexão aqui, só o coletor base gravando cru.

## O DoD

Bloco `id = "T-03.2"` em `docs/context/plataforma-dados/tasks.toml`. Refs: `CA-F0-2`,
`SPEC-001 §5.10`, `SPEC-001 §8.5/5`, plano `03` item **3.1**. Linha completa em
`docs/context/plataforma-dados/tasks_review.md:266`.

- Coletor de `!forceOrder@arr` (**stream de mercado inteiro**, 1000ms de subamostragem por doc da
  Binance — *"only the latest one liquidation order within 1000ms will be pushed"*) gravando
  **cru, zero normalização**.
- Cada evento carrega: `received_at`, nome do stream, **data do snapshot da doc consultada**, e o
  rótulo **`semântica de subamostragem NÃO RESOLVIDA (latest|largest)`** — **na SAÍDA de máquina**,
  não só em comentário/payload. A página USDT-M da Binance diz `latest`; a página COIN-M diz
  `largest`; a divergência é doc, `[DOC]`, e **não foi resolvida** — o rótulo existe para que o
  consumidor que calcula percentil saiba que não sabe qual dos dois está lendo.
- Zero API key (WebSocket público). `T-03.1` (já mesclada) já confirmou por medição real que o WS
  `<symbol>@aggTrade` carrega `nq` — reaproveite o padrão de probe curto e controlado que ela usou,
  se aplicável (mesma classe de stream, `wss://fstream.binance.com`).

## Contexto que já custou defeito aqui

- **Não existe `liquidation*` em `data.binance.vision`** — não tente montar um backfill histórico
  a partir de dump, essa série só existe a partir de quando o coletor liga.
- A tarefa **grava cru** — resistir à tentação de normalizar/agregar aqui é o ponto (T-03.4 é a
  camada de agregado, separada de propósito).

## Restrições do repositório (as mesmas de toda task desta sessão)

- Import-linter (`Natureza` incl.), cobertura domain 90/use_cases 80/infra 70%, idioma em inglês,
  `docs/INDEX.md` append-only, sem `Co-Authored-By:`.
- Push em worktree: `HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness`
  (nunca `.harness/mechanism`), URL explícita (não `origin`/SSH), **nunca `--force-with-lease`**.

## Protocolo de custo — R6/R7

- Se projetar passar de ~150 turnos, PARE e escreva o estado neste arquivo.
- Devolva **no máximo 15 linhas**, caminho do relatório em
  `docs/context/plataforma-dados/gates/T-03.2-builder.md`. **Nunca cole o corpo.**

## Entrega

Commit no branch `tasks/T-03.2-forceorder-liquidacao`, push, PR aberta contra `master`.
