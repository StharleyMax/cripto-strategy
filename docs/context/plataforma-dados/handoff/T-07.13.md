# Handoff — T-07.13 (`plataforma-dados`, fase 07, componente `web`)

**Escrito pelo `/workflow` em 2026-09-02**, logo após `T-07.12` mergear (PR #74, `c90442e`).
`depends_on = ["T-02.3", "T-07.12"]` — ambas `done`. Escolhida em vez de pular pra fase `08`
porque é a sequência natural: depende diretamente do que acabou de fechar.

## O que a task pede, literal

`title`: *"S1 le pela consulta nomeada ingest_health_query — a MESMA de 02"*. `refs`:
`ADR-008/D3`, `ADR-008/DoD-3`, plano 07 item 7.14, **`DoD D7.17` (falsificador da fase)**,
"unico teste que expoe duplicacao silenciosa".

`docs/plans/SPEC-001-plataforma-dados/07_aquisicao_em_regime.md:23,46,56`:

> **D7.17** — a consulta é UMA: `sha256` da projeção canônica da saída do CLI **igual** à que
> alimenta `S1`; e um `verdict` INÉDITO ⇒ os dois mudam juntos ou os dois reprovam
> (`ADR-008/DoD-3`, ≥1 run de cada verdict + 1 valor inédito).
> *"se um consumidor passar e o outro não, existem duas implementações da mesma verdade — e o
> teste diz qual."*

`docs/adr/ADR-008-registro-cru-de-f0.md` §D3 ("UMA consulta nomeada, DOIS consumidores") —
**leia a ADR inteira**, ela já decide a forma. O ponto central: `ingest_health_query` (função
em `backend/src/modules/sentimento/use_cases/ingest_health.py:32`) **já existe** e já tem
**consumidor #1**: `backend/src/modules/sentimento/infra/ingest_health_cli.py` (CLI que roda
`report(store_path)` e imprime pro stdout). `S1`/`web` é o **consumidor #2** que esta task cria
— e é isso que faz o gatilho abaixo disparar de verdade, ao contrário de `T-07.12`.

## ⛔ O gatilho de reabertura de `ADR-008/D3` DISPARA nesta task — não é mais hipotético

`CLAUDE.md` (tabela de fronteira, linha 11, `janela_de_perda`): *"a reabertura acontece quando
T-07.12/T-07.13 ... escreverem o consumidor da projeção canônica."* `T-07.12` NÃO escreveu esse
consumidor (usou fixture sintética, registrado em `T-07.12-builder.md`). **Esta task, se
implementada de verdade (D7.17 exige leitura da MESMA consulta), é o consumidor real.**

**Não decida a pergunta de nomenclatura.** Registre no relatório que o gatilho disparou, cite
`backend/src/modules/sentimento/domain/ingest_record.py:87-89` (onde a exceção de
`janela_de_perda` está documentada) e pare aí — quem decide é `ADR-008/D3`/owner, não esta
task nem este workflow.

## ⛔ Fronteira de backend — mais estreita que nas duas tasks anteriores, leia com cuidado

Backend **não tem framework HTTP** (`T-05.9` já mediu: 0 `fastapi`/`flask`/`uvicorn`). Não é
possível o browser chamar `ingest_health_query` ao vivo sem um endpoint que não existe — e
**criar esse endpoint está fora desta trilha agora** (mesma regra de `T-05.9`/`T-07.12`).

**O que É viável sem tocar `backend/`**: `ingest_health_cli.py` já é invocável como
**subprocesso, read-only**, contra um fixture SQLite local — isso NÃO é editar `backend/`, é
consumir o que já existe, exatamente como `T-05.9` consumiu `as_of_accessor.py`. Use isso pra
gerar a saída de referência do consumidor #1 e provar `D7.17` (`sha256`/verdict) em teste,
sem servidor. **Se isso não bastar para fechar `D7.17` de verdade, pare e reporte — não
invente endpoint novo.**

## Precedente — repita o que funcionou em `T-05.8`/`T-05.9`/`T-07.12`

- `ui-designer` confirma (não assume) se há decisão de UI/UX nova — `S1` já existe e é
  canônica (`STITCH_CONTEXT.md` §4.2), então provavelmente NÃO há tela nova aqui, só fonte de
  dado — mas confirme.
- Gate `ux-ui-mastery` só roda se houver decisão nova pra validar.
- `/build` roda `npm --prefix frontend run test:app`/`test:s1` + `lint` + sweep.
- `/qa` valida por MUTAÇÃO — inclusive o `verdict` inédito que `DoD-3` exige, não só os
  vereditos conhecidos.

## Zero conflito, verificado

`git worktree list` — nenhuma das 15+ worktrees ativas toca `web`. Trabalhe em worktree
isolada (mesmo padrão de `T-07.12`: `.claude/worktrees/agent-<id>`, nunca `/tmp`).
