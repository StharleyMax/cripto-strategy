# Handoff — T-03.4 · Agregado de bucket `q`/`nq` do stream `aggTrade` (CL-5)

**Card:** `CST-20` · **Componente:** `sentimento` · **Base:** `master@634833b` · **Worktree:** `/tmp/claude-1002/wt/T-03.4`

## Por que esta é a task de maior prioridade de relógio do projeto inteiro

`nq` (quantidade não-agressora, o `nq`/`q` do `aggTrade`) existe numa **janela deslizante de 48h**
(`GET /fapi/v1/aggTrades` em T-48h → `200` com `nq`; em T-49h → `400 -4166` `[MEDIDO]`) **e em
nenhum histórico** — o dump tem 7 colunas e `nq` não é uma delas. **Cada dia sem esta task rodando é
um dia de `nq` perdido para sempre.** Mesmo assim: escopo desta rodada é **código + prova sobre
fixture/probe curto**, não deploy contínuo — sem frontend, não há consumidor pro dado ao vivo ainda
(decisão do owner). O que importa AGORA é o código estar pronto pra ligar no minuto em que fizer
sentido.

`T-01.1` e `T-03.1` (dependências) já `done` — `T-03.1` confirmou por medição real que `nq` está
presente no payload do WS.

## O DoD

Bloco `id = "T-03.4"` em `docs/context/plataforma-dados/tasks.toml`. Refs: `ADR-001/6`,
`SPEC-001 §1.4 (CL-5)`, plano `03` item **3.5**, DoD **D3.5, D3.7, D3.8**. Linha completa em
`docs/context/plataforma-dados/tasks_review.md:268`.

- **Agregado de bucket** (não é captura de tick): `Σq_buy · Σq_sell · Σnq_buy · Σnq_sell · tx · btx
  · agg_id_min · agg_id_max`.
- **D3.5** — detector de buraco é **CONTIGUIDADE, nunca taxa**: invariante `a[i+1] == a[i]+1` sobre
  `agg_id`. Fixture real já citada por `T-04.3` (mesclada): **8.873.078 linhas, 0 saltos de
  `agg_id`** contra **11.327 descontinuidades de `f/l` (0,862%)** — reaproveite o padrão de
  `aggtrade_contiguity.py` (`T-04.3`, já em `master`) se fizer sentido, mas este agregado é uma
  camada NOVA (soma por bucket), não duplique a lógica de unicidade que já existe lá.
- **D3.7** — `count(q≠nq)/n` e déficit em bp, por símbolo e por dia, ≥ 7 dias × conjunto declarado.
  Referência: base DOGEUSDT 16/1000 (80,56 bp), BTC/ETH/SOL/XRP 0/1000. **Não é medível hoje em
  regime real** (precisa de 7 dias rodando) — prove a MECÂNICA do cálculo sobre os dados históricos
  já disponíveis em `data/` (que têm profundidade suficiente para simular vários dias).
- **D3.8** — leitura sob `quantity_field = nq` de uma janela **anterior à 1ª captura** ⇒
  `SEM_FONTE`, **nunca um valor** (nunca 0, nunca interpolado). Teste: 1 janela que atravesse a
  borda de quando o coletor começou a existir.

## Contexto que já custou defeito aqui

- **Não é captura de tick** — se você se pegar persistindo linha por linha em vez de bucket
  agregado, releia o título da task.
- `SeriesKey` (`T-04.2`, mesclada) inclui `quantity_field` — este agregado provavelmente PRODUZ uma
  série chaveada por essa dimensão (`q` vs `nq`); não invente um segundo esquema.
- `LOCF` sobre `FLOW` é erro de tipo (`D4.11`, task vizinha em `04`) — se este agregado alimentar
  algo tipo `FLOW`, ausência é ausência, nunca o valor anterior.

## Restrições do repositório (as mesmas de toda task desta sessão)

- Import-linter (`Natureza` incl.), cobertura domain 90/use_cases 80/infra 70%, idioma em inglês,
  `docs/INDEX.md` append-only, sem `Co-Authored-By:`.
- Push em worktree: `HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness`
  (nunca `.harness/mechanism`), URL explícita, **nunca `--force-with-lease`**.

## Protocolo de custo — R6/R7

- Se projetar passar de ~150 turnos, PARE e escreva o estado neste arquivo.
- Devolva **no máximo 15 linhas**, caminho do relatório em
  `docs/context/plataforma-dados/gates/T-03.4-builder.md`. **Nunca cole o corpo.**

## Entrega

Commit no branch `tasks/T-03.4-agregado-q-nq`, push, PR aberta contra `master`.
