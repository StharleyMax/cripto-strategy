# Handoff — T-03.8 · NTP como dependência de runtime; skew persistido por `ingest_run`

**Card:** `CST-24` · **Componente:** `sentimento` · **Base:** `master@634833b` · **Worktree:** `/tmp/claude-1002/wt/T-03.8`

## Por que agora

`Q1` respondida (2026-09-01). `T-01.1` (dependência) já `done`. Escopo: código + probe curto, não
deploy contínuo.

## O DoD — a tolerância NÃO é decisão sua

Bloco `id = "T-03.8"` em `docs/context/plataforma-dados/tasks.toml`. Refs: `CA-F0-8`, `[GAP G6]`,
plano `03` item **3.7**, DoD **D3.10**. Linha completa em
`docs/context/plataforma-dados/tasks_review.md:272`.

- **NTP como dependência de runtime**: monitore o skew do relógio local contra `/fapi/v1/time` (o
  timestamp de servidor da Binance Futures), e **persista o skew medido em cada `ingest_run`**.
- **D3.10** — ler a distribuição acumulada de `clock_skew_ms`, ≥ 7 dias de runs. **Não é medível
  hoje** (precisa de dias reais rodando) — construa o mecanismo de persistência de forma que essa
  leitura seja trivial depois (uma consulta sobre `ingest_run`, não um script novo).
- **A tolerância de skew NÃO se calibra aqui.** A refs é explícita: *"a tolerância se calibra em
  `07`, não aqui"* — `T-07.10` (fase futura) lê a distribuição que esta task acumula e DECIDE o
  limiar. Se você se pegar hardcodando um `MAX_SKEW_MS` com valor de julgamento próprio, pare —
  esta task só MEDE e PERSISTE, nunca decide "aceitável"/"inaceitável".
- Zero API key (`/fapi/v1/time` é público).

## Contexto que já custou defeito aqui

- `ingest_run` já é um conceito existente no schema (`T-02.3`, mesclada — `md.ingest_run` /
  `md.ingest_gap`). Persista o skew como campo desse registro existente, não crie uma tabela nova
  paralela sem necessidade.
- Contrato `Natureza` (`ADR-016`): a comparação de tempo em si (ler o relógio local) é **capacidade**
  e mora em `infra`; `domain` recebe o valor de skew já calculado, como `int`/`Decimal` injetado.

## Restrições do repositório (as mesmas de toda task desta sessão)

- Import-linter (`Natureza` incl.), cobertura domain 90/use_cases 80/infra 70%, idioma em inglês,
  `docs/INDEX.md` append-only, sem `Co-Authored-By:`.
- Push em worktree: `HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness`
  (nunca `.harness/mechanism`), URL explícita, **nunca `--force-with-lease`**.

## Protocolo de custo — R6/R7

- Se projetar passar de ~150 turnos, PARE e escreva o estado neste arquivo.
- Devolva **no máximo 15 linhas**, caminho do relatório em
  `docs/context/plataforma-dados/gates/T-03.8-builder.md`. **Nunca cole o corpo.**

## Entrega

Commit no branch `tasks/T-03.8-ntp-skew`, push, PR aberta contra `master`.
