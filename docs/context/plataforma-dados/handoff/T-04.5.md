# Handoff — T-04.5 · `cvd_delta` fato + `cvd_cum(anchor)` view + Decimal canônico

**Card:** `CST-32` · **Componente:** `sentimento` · **Base:** `master@840c500` · **Worktree:** `/tmp/claude-1002/wt/T-04.5`

## Por que esta task agora, em paralelo com QA/Review de T-04.1

`T-04.5` depende só de `T-04.2` (`done`, já em `master`) — **não** de `T-04.1` (PR #45, ainda em
review). Rodar em paralelo é seguro porque não compartilha base incerta com o que está sendo
verificado agora. `T-04.3` e `T-04.6` **ficam de fora deste lote** de propósito: ambos
`depends_on = ["T-04.1"]`, que só é `done` depois do merge do PR #45.

## O DoD

Bloco `id = "T-04.5"` em `docs/context/plataforma-dados/tasks.toml`. Refs: `CA-F1-8` · `SPEC-001 §2.6`
· plano `04` item **4.8** · DoD **D4.7, D4.8**
(`docs/plans/SPEC-001-plataforma-dados/04_contrato_temporal.md`).

- **4.8** `cvd_delta` como **fato** por bucket; `cvd_cum(anchor)` como **view com âncora
  obrigatória** — chamar sem âncora é erro, não default.
- **D4.7** três âncoras diferentes (00:00/12:00/20:00) sobre o mesmo `cvd_delta` produzem totais
  diferentes **e podem mudar o sinal** — isso é esperado, não bug.
- **D4.8** aritmética é `Decimal` sobre a string crua de `q` (não `float`), soma **ordenada por
  `agg_id`**, bucket por `transact_time // 60000`. O falsificador citado no plano: um `awk` com
  `OFMT=%.6g` publicado no PRD **reprova uma implementação correta** por erro de formatação
  (+4 mBTC) — não reproduza esse padrão de arredondamento.

## Contexto que já custou defeito aqui

- **F-2 (falsificador global da fase 04):** duas séries com a mesma `SeriesKey` cujos `cvd_cum`
  divirjam. Se `cvd_cum` não isolar por `SeriesKey` completa (a de `T-04.2`, com `quantity_field`
  e `reduction`), esse falsificador dispara.
- `cvd_delta` é fato — nunca recalculado a partir de `cvd_cum`. Modelar do jeito errado (view sobre
  view) inverte a direção de dependência que `T-04.8`/consumidores de front esperam.
- `LOCF` sobre `FLOW` é erro de tipo (`D4.11`, tarefa vizinha) — não vaza esse padrão para
  `cvd_delta`: bucket ausente é ausência, nunca o valor anterior.

## Restrições do repositório que mordem aqui

- **Contratos `import-linter`** (`backend/pyproject.toml [tool.importlinter]`): `domain`/`use_cases`
  não importam `socket`, `ssl`, `time`, `datetime` (contrato `Natureza`, `ADR-016`).
- **Pisos de cobertura:** domain 90%, use_cases 80%, infra 70%.
- **Idioma:** identificador de produção e de teste em **inglês**, inclusive parâmetro/variável
  local/constante de módulo (`CLAUDE.md` linhas 1-2). Exceção viva: nome de coluna de contrato em
  português (linha 11). Mensagem de `raise`: lacuna `⏸ NÃO DECIDIDO` — não é licença para português novo.
- **Evento de log e chaves de `extra={}`: inglês, prospectivamente** (linha 10) — hoje 4/9 em
  português, o número **não pode subir**.
- **`docs/INDEX.md` é append-only.**
- **Nenhum número sem o comando que o produziu**, com `n` e rótulo de força.
- **Sem `Co-Authored-By:`.** Autor e committer são o owner.

## Push — atenção ao mecanismo em worktree

O worktree não está registrado no plugin do Claude Code, então o `pre-push` não resolve o mecanismo
sozinho. **Não aponte `$HARNESS_MECHANISM` para `.harness/mechanism` (o próprio shim) — isso causa
recursão infinita**, já medido nesta sessão. O valor correto:

```
HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness git push ...
```

Se `frontend/node_modules` não existir no worktree, `npm --prefix frontend install` antes do push
(o lint-frontend do hook recusa com `rc=3` sem isso).

## Protocolo de custo — vinculante, R6/R7 (`docs/protocolo-de-despacho.md`)

- **`make verify`** roda os seis portões numa chamada, ~10 linhas impressas, saída bruta em arquivo.
- **Se projetar passar de ~150 turnos, PARE**, escreva o estado neste arquivo e devolva.
- Agrupe medições independentes numa chamada só; não releia arquivo já lido.
- Devolva **no máximo 15 linhas**: veredito, números com o comando, e o caminho do relatório completo
  em `docs/context/plataforma-dados/gates/T-04.5-builder.md`. **Nunca cole o corpo.**

## Entrega

Commit no branch `tasks/T-04.5-cvd-delta-e-cvd-cum`, push (ver seção acima), PR aberta contra `master`.
