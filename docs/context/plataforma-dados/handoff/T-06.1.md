# Handoff — T-06.1 · `series_catalog` como contrato lido pelos testes

**Card:** `CST-45` · **Componente:** `sentimento` · **Base:** `master@6fbaf4c` (confirme `git fetch` antes de criar a worktree — outra sessão pode ter avançado `master`) · **Worktree:** `/tmp/claude-1002/wt/T-06.1`

## Por que agora

Raiz de dependência da fase 06 inteira: `T-06.2`–`T-06.9` (todas `sentimento`) e `T-06.10` (`web`, S3 · inspetor de série) dependem desta task. `T-04.2` (única dependência de `T-06.1`) já está `done`. O time de front sinalizou que a fase 06 está travada por esta task ainda não construída — owner autorizou puxar (2026-09-03).

## O DoD

Bloco `id = "T-06.1"` em `docs/context/plataforma-dados/tasks.toml`. Refs: `SPEC-001 §3.3`, `CA-F2-2`, `CA-F2-11`, plano `06` itens **6.1 + 6.5 + 6.15**. Linha completa em
`docs/context/plataforma-dados/tasks_review.md:316` e `docs/specs/SPEC-001-plataforma-dados.md:225`.

- **`series_catalog`: UMA LINHA POR `SeriesKey`** (os 15 termos que `series_key.py`/`T-04.2` já fixa — reuse esse tipo, não invente um segundo). Campos obrigatórios por linha:
  - os 15 termos da `SeriesKey`;
  - `label_shift` com `verified_by` apontando **para um teste que mediu o shift** (string/identificador do teste — não é booleano "foi verificado", é a REFERÊNCIA ao teste; T-06.2 vai popular o shift real de cada endpoint, mas o CAMPO e a obrigatoriedade nascem aqui);
  - `unit` e `denom` **obrigatórios, verbatim da fonte** — nunca inferidos, nunca traduzidos; se a fonte não declara, o campo fica ausente e a linha é candidata a quarentena (a quarentena em si — o predicado de 3 termos completo com `available_at` — é escopo de `T-06.6`, não desta task; aqui você só garante que `unit`/`denom` existem como campos e que a ausência é DETECTÁVEL);
  - `price_use` quando aplicável;
  - `max_staleness_ms`;
  - erro publicado (`mediana, p99, n` — mesmo formato de `cvd_source`) quando a série é reconstrução de outra fonte.
- **`native_grid` é propriedade da `source`, RESOLVIDA EM RUNTIME** (`CA-F2-11`) — não amarre a um valor fixo tipo "5 min" no tipo/schema: a Coinalyze expõe `1min` e outras fontes têm grades diferentes. `native_grid` é um campo que o catálogo carrega por linha, lido da fonte, não uma constante do módulo.
- **O falsificador central desta task, literal**: *"o teste lê o catálogo; catálogo e teste não podem divergir sem reprovar."* Isso significa: escreva pelo menos um teste que **importa/lê `series_catalog` como fonte de verdade** (não um valor hardcoded duplicado no teste) e que reprova se uma linha do catálogo violar um dos campos obrigatórios acima (ausência de `unit`/`denom`/`label_shift.verified_by`/`max_staleness_ms`). Se o teste tiver seus próprios valores esperados copiados à mão em vez de ler o catálogo, o falsificador não vale.

## Contexto que já custou defeito aqui

- **Reuse `SeriesKey`** (`backend/src/modules/sentimento/domain/series_key.py`, de `T-04.2`) para os 15 termos — o arquivo já cita `series_catalog` em comentário (linhas 36 e 158) como o contrato que esta task cria; não duplique a definição dos termos.
- **Não construa a quarentena aqui.** O predicado de 3 termos (`label_shift IS NULL OR unit IS NULL OR available_at IS NULL`, `CA-F2-2`) e a regra de escrita (`available_at = NULL` + `MODELED` para endpoint sem `lag_ms`) são de `T-06.6` (`plano 06 item 6.12`). Esta task só garante que os CAMPOS existem e são obrigatórios/detectáveis — misturar as duas responsabilidades vai deixar `T-06.6` sem o que construir.
- **Não povoe o catálogo com os shifts/séries reais ainda** — isso é `T-06.2` (tabela de shift por endpoint), `T-06.3` (as 4 séries de L/S), `T-06.4` (funding), `T-06.9` (séries de preço/`cvd_source`/`fee_schedule`). `T-06.1` fixa o CONTRATO (schema + validação), não o conteúdo de produção de cada série — um catálogo com 0 ou poucas linhas de exemplo/teste é esperado aqui.
- Contrato `Natureza` (`ADR-016`): definição do catálogo e validação de campos obrigatórios são lógica pura de `domain`; se houver persistência (arquivo/DB) do catálogo, ela mora em `infra`.

## Restrições do repositório (as mesmas de toda task desta sessão)

- Import-linter (`Natureza` incl.), cobertura domain 90/use_cases 80/infra 70%, idioma em inglês
  (produção e teste, inclusive mensagem de `raise`/`Error`/`Exception` — `CLAUDE.md` §"Mensagem
  de exceção"), `docs/INDEX.md` append-only, sem `Co-Authored-By:`.
- Push em worktree: `HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness`
  (nunca `.harness/mechanism`), URL explícita (`https://github.com/StharleyMax/cripto-strategy.git`,
  não `origin`/SSH), **nunca `--force-with-lease`** — use `--force` simples se precisar.
- **⚠️ Múltiplas sessões de Claude Code rodam em paralelo no diretório principal** (inclusive uma
  sessão de front trabalhando na fase 06/07) — sua worktree é isolada, mas **sempre `git fetch` +
  resolva contra `master` mais recente antes do push**; conflito típico é em
  `docs/INDEX.md`/`backend/README.md` (append duplo) — mantenha AMBAS as seções, nunca descarte
  uma. Pode haver também mudanças NÃO commitadas de outra sessão no checkout principal
  (`docs/INDEX.md`, `.gitignore`) — isso é WIP alheio, não mexa nele fora da sua worktree.
- **Aguarde ATIVAMENTE `bash backend/scripts/test.sh` terminar** — não dispare em background e
  pare o turno esperando notificação: isso não acorda subagente nesta sessão (medido
  repetidamente). Se precisar de background, sua PRÓXIMA ação no mesmo turno é checar
  `ps -p <PID>` até confirmar que já terminou.

## Protocolo de custo — R6/R7

- Se projetar passar de ~150 turnos, PARE e escreva o estado neste arquivo.
- Devolva **no máximo 15 linhas**, caminho do relatório em
  `docs/context/plataforma-dados/gates/T-06.1-builder.md`. **Nunca cole o corpo.**

## Entrega

Commit no branch `tasks/T-06.1-fase06`, push, PR aberta contra `master`.
