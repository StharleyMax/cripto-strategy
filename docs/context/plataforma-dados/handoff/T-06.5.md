# Handoff — T-06.5 · `reduction` na `SeriesKey`: OI Coinalyze = 4 linhas, Binance = 1

**Card:** `CST-49` · **Componente:** `sentimento` · **Base:** `master` (faça `git fetch` antes de criar a worktree — várias tasks da fase 06 rodam em paralelo) · **Worktree:** `/tmp/claude-1002/wt/T-06.5`

## Por que agora

`T-06.1` (`series_catalog`) já mesclada. Owner autorizou puxar a fase 06 inteira em paralelo (2026-09-03). `T-06.2`, `T-06.3`, `T-06.4`, `T-06.6`, `T-06.9`, `T-06.10` estão sendo construídas ao mesmo tempo.

## O DoD

Bloco `id = "T-06.5"` em `docs/context/plataforma-dados/tasks.toml`. Refs: `CA-F2-17`, plano `06` item **6.11**, DoD **D6.7/D6.8**. Linha completa em `docs/context/plataforma-dados/tasks_review.md:320`.

- **`reduction` já é um dos 15 termos de `SeriesKey`, e o enum `Reduction` (`series_key.py`) JÁ TEM os membros certos**: `POINT`, `OPEN`, `HIGH`, `LOW`, `CLOSE`, `SUM` — o docstring do enum já cita literalmente a medição D6.8 abaixo. Esta task não cria o campo nem o enum, ela POPULA os valores corretos de `reduction` para Open Interest das duas fontes:
  - **Coinalyze**: OI vem como **4 linhas por bucket** — uma `SeriesCatalogEntry`/`SeriesKey` por membro `OPEN`/`HIGH`/`LOW`/`CLOSE`.
  - **Binance**: OI vem como **1 linha por bucket** — `reduction = POINT`.
- **D6.7** (falsificador): pedir *"o OI da Coinalyze"* **SEM** especificar `reduction` tem que dar **ERRO**, nunca um default silencioso escolhendo uma das 4 linhas arbitrariamente.
- **D6.8** (medido): o `c` (close) da Coinalyze casa com o `sumOpenInterest` da Binance no MESMO `create_time` a **1,86 bp de mediana / 9,46 bp de p99 (n=1.706)**. Isso prova que o `t` da Coinalyze é o **INÍCIO** do bucket (não o fim) — e `o(t) ≠ c(t−300)` (só 6 de 2.141 iguais), confirmando que são **4 linhas de catálogo genuinamente distintas, não 3** (a hipótese de que `open` de um bucket é igual ao `close` do bucket anterior é FALSA e medida como tal).
- **Por que isso importa**: sem o termo `reduction` na chave, essas 5 séries (4 da Coinalyze + 1 da Binance) colapsariam em identidades ambíguas — "o OI" deixaria de ter significado único.

## Contexto que já custou defeito aqui

- Reuse `Reduction` (enum já existente em `series_key.py`, membros `POINT`/`OPEN`/`HIGH`/`LOW`/`CLOSE`/`SUM` — NÃO crie um enum novo nem novos membros) e `series_catalog.py` de `T-06.1`.
- As 4 linhas da Coinalyze são 4 `SeriesCatalogEntry`/`SeriesKey` distintas (mesma `metric`="open_interest", `reduction` diferente cada) — não um campo de sub-tipo dentro de uma linha só.
- Contrato `Natureza` (`ADR-016`): a decisão de qual `reduction` cada fonte usa e a comparação `c(t)` vs `sumOpenInterest` são lógica de `domain`; leitura de fixture real (se usar dado de `data/`) é teste/infra.

## Restrições do repositório (as mesmas de toda task desta sessão)

- Import-linter (`Natureza` incl.), cobertura domain 90/use_cases 80/infra 70%, idioma em inglês (produção e teste, inclusive mensagem de exceção), `docs/INDEX.md` append-only, sem `Co-Authored-By:`.
- Push em worktree: `HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness`, URL explícita, **nunca `--force-with-lease`**.
- **⚠️ Múltiplas sessões e múltiplas tasks desta MESMA fase rodam em paralelo agora** — sempre `git fetch` + resolva contra `master` mais recente antes do push; conflito típico em `docs/INDEX.md`/`backend/README.md` — mantenha AMBAS as seções.
- **Aguarde ATIVAMENTE `bash backend/scripts/test.sh` terminar** — não dispare em background esperando notificação.

## Protocolo de custo — R6/R7

- Se projetar passar de ~150 turnos, PARE e escreva o estado neste arquivo.
- Devolva **no máximo 15 linhas**, caminho do relatório em `docs/context/plataforma-dados/gates/T-06.5-builder.md`. **Nunca cole o corpo.**

## Entrega

Commit no branch `tasks/T-06.5-fase06`, push, PR aberta contra `master`.
