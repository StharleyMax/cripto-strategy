# Handoff — T-07.8 · `universe_at(ts, filtro)` com `universe_source` inadmissível POR TIPO

**Card:** `CST-62` · **Componente:** `sentimento` · **Base:** `master@5cba0074` · **Worktree:** `/tmp/claude-1002/wt/T-07.8`

## Por que agora

`T-07.2` (survivorship na borda de ingestão) já mesclada em `master` — ela fixa o vocabulário de
"símbolo ausente do exchangeInfo corrente" que esta task consome. `T-02.1` (snapshot diário) já
`done`. Owner autorizou puxar esta task junto com `T-07.5` em paralelo (2026-09-02).

## O DoD

Bloco `id = "T-07.8"` em `docs/context/plataforma-dados/tasks.toml`. Refs: `CA-F3-4`,
`SPEC-001 §3.7`, plano `07` item **7.10**. Linha completa em
`docs/context/plataforma-dados/tasks_review.md:341`.

- **`universe_at(ts, filtro)`**: dado um timestamp histórico e um filtro, devolve o universo de
  símbolos que estava vigente NAQUELE momento — não o universo de hoje.
- **`s3_inferred` é INADMISSÍVEL POR TIPO no caminho de decisão** — não por convenção, não por
  validação em runtime. Isto significa: o tipo que representa uma fonte aceitável para decidir
  pertencimento ao universo histórico **não pode nem compilar/tipar** com o valor `s3_inferred`
  como membro — use um tipo (`Literal`/enum) que EXCLUA essa variante do conjunto admissível na
  assinatura da função de decisão, não um `if fonte == "s3_inferred": raise`. Sem essa exclusão
  por tipo, todo resultado transversal do passado vira retrospectivo por construção (o dado
  inferido do S3 "vaza" para decisão, mascarando que a fonte é fraca).
- **D7.7** (falsificador, `[MEDIDO]`): `universe_at('2025-08-01')` ⇒ tem que **incluir `ICXUSDT`**
  e **excluir `DOSUSDT`** (`DOSUSDT` fez onboard em 2026-08-11 — não existia em 2025-08-01, então
  não pode aparecer no universo daquela data).
- **Devolve a UNIÃO das duas testemunhas com a divergência marcada** — as duas testemunhas são o
  snapshot de `exchangeInfo` (T-02.1) e o survivorship do dump S3 (T-07.2, `md.ingest_gap`); a
  saída de `universe_at` é a união dos símbolos que qualquer uma das duas atesta como vigente
  naquele `ts`, com um campo explícito marcando ONDE as duas divergem (não um merge silencioso).

## Contexto que já custou defeito aqui

- Reuse `instrument_universe_snapshot.py` (T-02.1) para o snapshot de `exchangeInfo` e
  `dump_survivorship.py` (T-07.2, `SurvivorshipVerdict`/`build_survivorship_gap`) para a
  testemunha do S3 — não invente uma terceira representação do universo.
- Contrato `Natureza` (`ADR-016`): leitura de snapshot/storage mora em `infra`; a função
  `universe_at` e a decisão do tipo admissível de fonte são lógica pura de `domain`.
- Não confundir "inadmissível por tipo" com validação: o teste que prova isto é estrutural (AST,
  como em `T-07.2`'s `SurvivorshipVerdict` sem membro `"REJECTED"`), não um teste que chama a
  função com `s3_inferred` e espera exceção.

## Restrições do repositório (as mesmas de toda task desta sessão)

- Import-linter (`Natureza` incl.), cobertura domain 90/use_cases 80/infra 70%, idioma em inglês
  (produção e teste, inclusive mensagem de `raise`/`Error`/`Exception` — `CLAUDE.md` §"Mensagem
  de exceção"), `docs/INDEX.md` append-only, sem `Co-Authored-By:`.
- Push em worktree: `HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness`
  (nunca `.harness/mechanism`), URL explícita (`https://github.com/StharleyMax/cripto-strategy.git`,
  não `origin`/SSH), **nunca `--force-with-lease`** — use `--force` simples se precisar.
- **⚠️ Múltiplas sessões de Claude Code rodam em paralelo no diretório principal, e outra task
  (`T-07.5`) está sendo construída em paralelo AGORA mesmo, na mesma janela de tempo** — sua
  worktree é isolada, mas **sempre `git fetch` + resolva contra `master` mais recente antes do
  push**; conflito típico é em `docs/INDEX.md`/`backend/README.md` (append duplo) — mantenha
  AMBAS as seções, nunca descarte uma.
- **Aguarde ATIVAMENTE `bash backend/scripts/test.sh` terminar** — não dispare em background e
  pare o turno esperando notificação: isso não acorda subagente nesta sessão (medido
  repetidamente). Se precisar de background, sua PRÓXIMA ação no mesmo turno é checar
  `ps -p <PID>` até confirmar que já terminou.

## Protocolo de custo — R6/R7

- Se projetar passar de ~150 turnos, PARE e escreva o estado neste arquivo.
- Devolva **no máximo 15 linhas**, caminho do relatório em
  `docs/context/plataforma-dados/gates/T-07.8-builder.md`. **Nunca cole o corpo.**

## Entrega

Commit no branch `tasks/T-07.8-fase07`, push, PR aberta contra `master`.
