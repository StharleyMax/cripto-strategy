# Handoff — T-06.8 · Campo aditivo desconhecido → quarentena + alarme; ausente/renomeado → reprova

**Card:** `CST-52` · **Componente:** `sentimento` · **Base:** `master` (faça `git fetch` antes de criar a worktree) · **Worktree:** `/tmp/claude-1002/wt/T-06.8`

## Por que agora

`T-06.6` (quarentena de 3 termos) já mesclada. Owner autorizou puxar o restante da fase 06 assim que desbloqueado (2026-09-03).

## O DoD

Bloco `id = "T-06.8"` em `docs/context/plataforma-dados/tasks.toml`. Refs: `CA-F2-12`, `SPEC-001 §5.5`, plano `06` item **6.14**, DoD **D6.14**. Linha completa em `docs/specs/PRD-001-plataforma-dados.md:598` e `docs/plans/SPEC-001-plataforma-dados/06_semantica_declarada.md:25,46`.

- **Duas classes de mudança de schema no payload da fonte, duas reações OPOSTAS**:
  1. **Campo ADITIVO desconhecido** (a fonte passou a mandar um campo novo que o ingestor não esperava) → **QUARENTENA + ALARME**, **NUNCA parada da ingestão**. O dado continua sendo ingerido; o campo novo fica marcado/isolado até alguém decidir o que fazer com ele.
  2. **Campo AUSENTE ou RENOMEADO** (um campo que o contrato espera não veio, ou veio com outro nome) → **REPROVA** — a ingestão daquele payload é recusada, não silenciosamente aceita com o campo faltando.
- **D6.14** (o caso real que prova por que a regra antiga estava errada): a Binance **ADICIONOU** o campo `nq` ao payload de `aggTrade` — o REST tem `{a,p,q,nq,f,l,T,m}` (8 campos) mas o dump histórico do S3 só tem 7 colunas (sem `nq`). **Sob uma regra fail-closed ingênua (qualquer campo desconhecido reprova), NAQUELE DIA a ingestão inteira teria PARADO** só porque a fonte ficou mais generosa, não mais pobre. A regra correta desta task existe exatamente para que isso não aconteça de novo: campo novo/aditivo é oportunidade (quarentena + alarme para investigar), não motivo de parada.
- **A distinção operacional**: "aditivo" = o conjunto de campos ESPERADOS ainda está todo presente, mais um extra. "Ausente/renomeado" = pelo menos um campo ESPERADO não está onde deveria (falta, ou apareceu com nome diferente). O mecanismo tem que DIFERENCIAR essas duas situações a partir do payload bruto — não é a mesma checagem com resultado invertido, são dois testes distintos sobre o mesmo conjunto de campos.

## Contexto que já custou defeito aqui

- **Reuse `Absence`/`Absence.QUARANTINE`** (`backend/src/modules/sentimento/domain/provenance.py`, valor `"QUARENTENA"`) para o resultado de campo aditivo — não invente um segundo enum de destino de quarentena.
- Se houver um mecanismo de "alarme" já existente nesta fase (`T-05.10`/`T-07.11`/canal externo), NÃO construa um canal de alarme novo aqui — esta task decide QUANDO alarmar (o predicado: campo aditivo desconhecido), não POR ONDE o alarme sai; se não houver canal ainda, registre a decisão de alarme como um evento/flag que um consumidor futuro lê, e nomeie isso explicitamente como escopo não fechado.
- Contrato `Natureza` (`ADR-016`): a comparação schema-esperado vs schema-recebido é lógica pura de `domain`; a leitura do payload bruto real é `infra`/fixture de teste.
- O caso real do `nq` já está bem documentado no repositório (`ADR-001`, `series_key.py` comentários) — leia essas referências antes de construir o fixture de teste, para não reinventar uma versão sintética quando o caso real já está catalogado.

## Restrições do repositório (as mesmas de toda task desta sessão)

- Import-linter (`Natureza` incl.), cobertura domain 90/use_cases 80/infra 70%, idioma em inglês (produção e teste, inclusive mensagem de exceção), `docs/INDEX.md` append-only, sem `Co-Authored-By:`.
- Push em worktree: `HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness`, URL explícita, **nunca `--force-with-lease`**.
- **⚠️ Múltiplas sessões e outra task (`T-06.7`) rodam em paralelo agora** — sempre `git fetch` + resolva contra `master` mais recente antes do push; conflito típico em `docs/INDEX.md`/`backend/README.md` — mantenha AMBAS as seções.
- **Aguarde ATIVAMENTE `bash backend/scripts/test.sh` terminar** — não dispare em background esperando notificação.

## Protocolo de custo — R6/R7

- Se projetar passar de ~150 turnos, PARE e escreva o estado neste arquivo.
- Devolva **no máximo 15 linhas**, caminho do relatório em `docs/context/plataforma-dados/gates/T-06.8-builder.md`. **Nunca cole o corpo.**

## Entrega

Commit no branch `tasks/T-06.8-fase06`, push, PR aberta contra `master`.
