# Handoff — T-06.9 · As quatro séries de preço, `implied_avg_price` PROIBIDO, `fee_schedule` datada, `cvd_source` com erro medido

**Card:** `CST-53` · **Componente:** `sentimento` · **Base:** `master` (faça `git fetch` antes de criar a worktree — várias tasks da fase 06 rodam em paralelo) · **Worktree:** `/tmp/claude-1002/wt/T-06.9`

## Por que agora

`T-06.1` (`series_catalog`) já mesclada. Owner autorizou puxar a fase 06 inteira em paralelo (2026-09-03). `T-06.2`, `T-06.3`, `T-06.4`, `T-06.5`, `T-06.6`, `T-06.10` estão sendo construídas ao mesmo tempo.

## O DoD

Bloco `id = "T-06.9"` em `docs/context/plataforma-dados/tasks.toml`. Refs: `ADR-007/PS-1`, `ADR-007/PS-2`, `CA-F2-14`, `CA-F2-16`, `ADR-001/5`, plano `06` itens **6.6+6.7+6.8+6.9**, DoD **D6.9/D6.13**. Linha completa em `docs/context/plataforma-dados/tasks_review.md:324`. Leia `ADR-007` inteiro antes de codar — ele já decidiu as quatro séries de preço e o motivo de `implied_avg_price` ser proibido.

- **As QUATRO séries de preço**, com `price_mark_close` declarada como uma delas — **`implied_avg_price` é PROIBIDO COMO NOME** no catálogo (a razão: *"preço médio implícito"* ensina errado, e o catálogo é o veículo de propagação do erro se o nome entrar).
- **`price_source` POR `price_use`**: o campo `price_use` já existe em `SeriesCatalogEntry` (`T-06.1`, `series_catalog.py`) com o conjunto fechado `PRICE_USES = {structure_detection, liquidation_trigger, funding, execution, cost}` — esta task usa esse campo para declarar, para cada série de preço, PARA QUAL uso ela serve (uma série de preço pode não servir a todos os 5 usos).
- **`fee_schedule` DATADA**: schema `fee_schedule(venue, market, tier, maker_bps, taker_bps, effective_from, evidence_url)` (`SPEC-001 §3.4`). Resolver a taxa aplicável é sempre AS-OF uma data — nunca um valor único fixo. **`exchangeInfo` NÃO tem campo de taxa** — a única taxa que ele expõe é `liquidationFee`, que é outra coisa; não confunda as duas.
- **D6.13**: resolver `fee_schedule` as-of uma janela tem que dar **nenhum resultado de backtest sem `(maker_bps, taker_bps, effective_from, evidence_url)`** — se a tabela não tiver uma entrada vigente para a data pedida, o mecanismo recusa, não usa um default.
- **`cvd_source` com ERRO MEDIDO publicado por fonte** — reuse `PublishedError(median_bp, p99_bp, n)` de `series_catalog.py` (`T-06.1`). Fontes a registrar: `aggtrade_q`, `aggtrade_nq`, `coinalyze_bv`.
- **D6.9**: tentar registrar `cvd_source` **SEM** `(mediana, p99, n)` tem que **REPROVAR** (o `__post_init__` de `SeriesCatalogEntry` já reprova se `reconstructed_from` estiver setado sem `published_error` — use esse mecanismo, não reimplemente a checagem). Valores reais medidos para `coinalyze_bv`: `median_bp=0,0000`, `p99_bp=29,34`, `máx=1.955,80`, `n=699`, medido em `2026-08-24`, com `causa_da_cauda = NÃO DIAGNOSTICADA` (registre essa nota — a hipótese de que o "maker" explica a cauda foi REFUTADA a 2.584,87 bp; não afirme uma causa que foi refutada).

## Contexto que já custou defeito aqui

- Leia `series_catalog.py` (`T-06.1`) inteiro — `PRICE_USES`, `PublishedError`, e as regras de `reconstructed_from`/`published_error` já existem lá; esta task só popula linhas reais usando esses tipos.
- Reuse `cvd_delta`/`cvd_cum` de `T-04.5` se houver vocabulário de CVD já fixado — não crie um segundo conceito de CVD para as fontes de erro.
- Contrato `Natureza` (`ADR-016`): a decisão de proibir `implied_avg_price` como nome e a validação de `fee_schedule` datada são `domain` puro.

## Restrições do repositório (as mesmas de toda task desta sessão)

- Import-linter (`Natureza` incl.), cobertura domain 90/use_cases 80/infra 70%, idioma em inglês (produção e teste, inclusive mensagem de exceção), `docs/INDEX.md` append-only, sem `Co-Authored-By:`.
- Push em worktree: `HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness`, URL explícita, **nunca `--force-with-lease`**.
- **⚠️ Múltiplas sessões e múltiplas tasks desta MESMA fase rodam em paralelo agora** — sempre `git fetch` + resolva contra `master` mais recente antes do push; conflito típico em `docs/INDEX.md`/`backend/README.md` — mantenha AMBAS as seções.
- **Aguarde ATIVAMENTE `bash backend/scripts/test.sh` terminar** — não dispare em background esperando notificação.

## Protocolo de custo — R6/R7

- Se projetar passar de ~150 turnos, PARE e escreva o estado neste arquivo.
- Devolva **no máximo 15 linhas**, caminho do relatório em `docs/context/plataforma-dados/gates/T-06.9-builder.md`. **Nunca cole o corpo.**

## Entrega

Commit no branch `tasks/T-06.9-fase06`, push, PR aberta contra `master`.
