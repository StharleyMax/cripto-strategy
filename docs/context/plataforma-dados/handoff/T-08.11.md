# Handoff — T-08.11 (`plataforma-dados`, fase 08, componente `web`)

**Worktree:** `.claude/worktrees/T-08.11` · branch `tasks/T-08.11-sse-envelope-bucket` (a partir de
`origin/master` em `b9870bc`) — isolado por diretriz permanente do owner após o incidente de colisão
entre sessões (2026-09-02): nenhuma edição roda no diretório principal.

## O que a task pede

`title`: *"Transporte ao vivo por SSE com envelope de bucket"*. `depends_on = ["T-05.9"]` (done —
transporte HTTP para o histórico, mesma ADR, lado `COMO EM T`). Esta task é o lado `AO VIVO`.

## Fontes de verdade, em ordem

1. `docs/adr/ADR-005-transporte-de-leitura.md` — **D1** (linha 19-26): rota `AO VIVO` é SSE, um fluxo
   por sessão, unidirecional, reconecta sozinho; nunca WS bidirecional (sem consumidor no escopo). **D2**
   (linha 28-36): envelope de bucket parcial —
   `(bucket_open_ts, cvd_delta_parcial, last_price, n_trades, seq) @ max(1 Hz, 1/TF)`; a resolução
   EXIBIDA da idade nunca é mais fina que `1/f`; `seq` monotônico por fluxo, cliente detecta lacuna sem
   inferir do relógio. **D4** (linha 48-50): `bar_policy` é declarado pelo CONSUMIDOR na requisição —
   nunca default `intrabar`, servidor não infere de "é a borda direita".
2. **Falsificador** (linha 64): taxa de mensagens ao browser não pode exceder `max(1 Hz, 1/TF)` por
   série, e nenhum payload pode conter campo de nível de tick (`agg_id`, `price`/`quantity` por trade).
3. `docs/plans/SPEC-001-plataforma-dados/08_superficie_e_reprodutibilidade.md` — item 8.8, **D8.13**
   (bucket em formação não é escondido: `is_final = false` visível; `h`/`l`/`c` do bucket corrente NUNCA
   lidos como finais).
4. `frontend/src/app/history-transport.ts` + `.test.ts` — **precedente direto** (T-05.9, mesma ADR,
   lado histórico): `HistoryRequestKey`, `encodeHistoryRequest`/`decodeHistoryRequest`,
   `assertNoTickLevelFields`, `assertBucketSpacingWithinInterval`. O padrão de nomes/estrutura deste
   arquivo é o modelo a seguir para o lado ao vivo — não reescrever do zero.
5. `docs/context/plataforma-dados/gates/T-05.9-design.md` — veredito do gate anterior: **"SEM DECISÃO DE
   UI/UX NOVA. Contrato já coberto, nenhuma ação no Stitch necessária"**, verificado contra a S2 canônica
   (chip `AO VIVO` já existe no chrome, `s2.html:187`). Esta task deve repetir a mesma verificação pelo
   lado `AO VIVO` do mesmo chip — não é screen nova, é o mesmo par de modos já materializado.

## Escopo — lembrete de fronteira (`CLAUDE.md`)

`backend/` é **intocável** nesta sessão, mesmo read-only-consultivo se não for estritamente necessário.
Isto é módulo de contrato (encode/decode do envelope + parsing do stream SSE no cliente), não app Next.js
— não existe app rodando para testar E2E; a prova é por teste unitário + spec de tipo, como T-05.8/T-05.9.

## Passos

1. `ui-designer`: gate de design — repetir o padrão de `T-05.9-design.md` (verificar S2 canônica, D1/D2
   já materializados no chrome `AO VIVO`; provável veredito "sem ação no Stitch", mas a verificação é
   obrigatória, não presumida).
2. `harness-plugin:builder`: implementar `frontend/src/app/live-transport.ts` (nome sugerido, decisão do
   builder) espelhando a forma de `history-transport.ts` — envelope SSE, `seq` monotônico/detecção de
   lacuna, assert de taxa `max(1Hz, 1/TF)`, assert de zero campo tick-level, `is_final=false` explícito no
   tipo.
3. `harness-plugin:qa`: mutação — reintroduzir cada defeito que os DoD/falsificadores proíbem (campo
   tick-level vazando, taxa excedendo `1/TF`, `is_final` implícito/ausente, `intrabar` como default) e
   confirmar que o teste pega.
4. Push via técnica de relay (branch no repo principal apontando pro commit do worktree, checkout, push,
   checkout de volta pro master) — mesma técnica usada em T-07.12/T-07.13/PR#44.
