# Handoff — T-07.7 · Broker de cota com jitter e circuit breaker, contagem local

**Card:** `CST-61` · **Componente:** `sentimento` · **Base:** `master@031e007` · **Worktree:** `/tmp/claude-1002/wt/T-07.7`

## Por que agora

`T-03.7` já mesclada (rampa até o primeiro 429). Owner autorizou puxar fase 07 backend (2026-09-02).
Independente — pode rodar em paralelo com `T-07.1`/`T-07.4`/`T-07.10`.

## O DoD

Bloco `id = "T-07.7"` em `docs/context/plataforma-dados/tasks.toml`. Refs: `CA-F3-9`, plano `07`
item **7.9**. Linha completa em `docs/context/plataforma-dados/tasks_review.md:340`.

- **Broker de cota com JITTER e CIRCUIT BREAKER**, contagem **LOCAL** — dois dos três baldes conhecidos
  são cegos (Binance `/futures/data/*` e Coinalyze, ambos `200` sem header de cota — já medido e
  reutilizável de `T-02.2`/`T-07.1`... veja `domain/local_quota_broker.py` e `domain/quota_bucket.py`,
  que já existem e fixam o vocabulário `OBSERVED`/`BLIND`).
- **A contagem local conservadora NÃO é adaptação a um fornecedor pior — é o CASO GERAL.**
  `/futures/data/openInterestHist` devolve `200` com **zero headers `x-mbx-*`**, exatamente como a
  Coinalyze `[MEDIDO]`, e `avaliacao:A3` já foi **reproduzido**. Não trate isso como exceção rara —
  desenhe o broker assumindo que a maioria dos baldes é cega.
- **Jitter**: o circuit breaker/backoff não pode ser determinístico puro — precisa de aleatoriedade
  para evitar sincronização de retry entre múltiplos processos batendo no mesmo balde (thundering herd).
  Teste que o jitter realmente varia entre chamadas (não é hardcoded/mockado para sempre o mesmo valor).
- **Circuit breaker**: depois de N falhas consecutivas (ou um sinal de 429/५xx), o broker ABRE o
  circuito e recusa novas chamadas por um período, sem tentar de qualquer jeito — teste o ciclo
  completo: fechado → aberto (após falhas) → half-open (após cooldown) → fechado de novo (após sucesso).

## Contexto que já custou defeito aqui

- `domain/quota_bucket.py` e `domain/local_quota_broker.py` **já existem** (de `T-02.2`/`T-03.7`) —
  reuse esse vocabulário (`BucketVisibility.OBSERVED`/`BLIND`, `blindness_reason` obrigatório para
  balde cego) em vez de inventar um novo. Se o broker desta task precisar estender esses tipos, prefira
  composição a duplicar a lógica de contagem local.
- Contrato `Natureza` (`ADR-016`): chamada de rede mora em `infra`, nunca em `domain`/`use_cases`. O
  circuit breaker e o jitter são lógica pura (podem ficar em `domain`), mas a chamada HTTP real não.

## Restrições do repositório (as mesmas de toda task desta sessão)

- Import-linter (`Natureza` incl.), cobertura domain 90/use_cases 80/infra 70%, idioma em inglês
  (produção e teste, inclusive mensagem de exceção — `CLAUDE.md` §"Mensagem de exceção"),
  `docs/INDEX.md` append-only, sem `Co-Authored-By:`.
- Push em worktree: `HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness`
  (nunca `.harness/mechanism`), URL explícita, **nunca `--force-with-lease`** — use `--force` simples.
- **⚠️ Múltiplas sessões de Claude Code rodam em paralelo no diretório principal** — sua worktree é
  isolada, mas **sempre `git fetch` + resolva contra `master` mais recente antes do push**; conflito
  típico é em `docs/INDEX.md`/`backend/README.md` — mantenha AMBAS as seções.
- **Aguarde ATIVAMENTE `bash backend/scripts/test.sh` terminar** — não dispare em background e pare o
  turno esperando notificação; não funciona para subagente nesta sessão.

## Protocolo de custo — R6/R7

- Se projetar passar de ~150 turnos, PARE e escreva o estado neste arquivo.
- Devolva **no máximo 15 linhas**, caminho do relatório em
  `docs/context/plataforma-dados/gates/T-07.7-builder.md`. **Nunca cole o corpo.**

## Entrega

Commit no branch `tasks/T-07.7-fase07`, push, PR aberta contra `master`.
