# Handoff — T-07.9 · `instrument_alias` como YAML versionado com `evidence_url` obrigatório

**Card:** `CST-63` · **Componente:** `sentimento` · **Base:** `master@bf15df6` · **Worktree:** `/tmp/claude-1002/wt/T-07.9`

## Por que agora

`T-07.8` (`universe_at`) já mesclada e fechada no `tasks.toml`. Owner autorizou puxar esta task
junto com `T-07.6` em paralelo (2026-09-02).

## O DoD — BLOQUEIO PARCIAL, e a task é sobre a parte NÃO bloqueada

Bloco `id = "T-07.9"` em `docs/context/plataforma-dados/tasks.toml`. Refs: plano `07` item
**7.11**. Linha completa em `docs/plans/SPEC-001-plataforma-dados/07_aquisicao_em_regime.md:20` e
`docs/specs/SPEC-001-plataforma-dados.md:242,648`.

- **`Q12` está `ABERTA`** (`SPEC-001` linha 648) — mas a pergunta em aberto é só o **CONTEÚDO**
  (quais pares antigo→novo entram, ~5 linhas/ano), não se o **MECANISMO** deve existir. Igual ao
  padrão já usado em `T-07.10` (calibração de clock skew: mecanismo construído, recusa por falta
  de dado real em vez de fabricar) — aqui: **construa o mecanismo completo, populado com ZERO
  entradas reais** (ou, no máximo, um exemplo claramente rotulado como `EXAMPLE`/fixture de
  teste, nunca apresentado como dado real).
- **Contrato de schema** (`SPEC-001` §linha 242): `instrument_alias(de, para, effective_from,
  evidence_url)` — **os nomes de coluna na SPEC estão em português por serem prosa de documento**
  (`CLAUDE.md` linha 7 da tabela de fronteira). Ao implementar em código, use os nomes em
  **inglês** (`CLAUDE.md` linha 1: identificador de produção é inglês) — por exemplo
  `from_symbol`/`to_symbol`/`effective_from`/`evidence_url` — **esta NÃO é a mesma exceção de
  `janela_de_perda`** (`CLAUDE.md` linha 11): aquela é uma coluna de contrato JÁ EM PRODUÇÃO
  citada literalmente de `ADR-008/D3`; este é um contrato NOVO sendo criado agora, sem essa
  herança, então a regra geral (inglês) se aplica.
- **`evidence_url` é OBRIGATÓRIO, por tipo/validação, não por convenção** — uma entrada de alias
  sem `evidence_url` não pode ser carregada; o carregador rejeita o YAML inteiro (ou a entrada
  individual, decida e documente qual) se o campo estiver ausente ou vazio.
- **Versionado**: o YAML é um arquivo versionado no repositório (não gerado em runtime), cada
  entrada com `effective_from` — o carregador tem que conseguir responder "qual era o alias válido
  em uma data X" (o mesmo padrão temporal de `universe_at`/`T-07.8`, que também decide "o que era
  verdade num instante").
- **Por que isto importa, com os fatos já `[MEDIDO]` que motivam o mecanismo** (não recite como se
  fossem o conteúdo de `Q12` — são o CONTEXTO que prova que o mecanismo é necessário):
  `MATICUSDT→POLUSDT` e `RNDRUSDT→RENDERUSDT` foram **renomeados**, não deslistados, e a API da
  Binance **não expõe a continuidade** — sem um alias explícito, o survivorship (`T-07.2`)
  trataria os dois símbolos como duas séries desconectadas. Terceira testemunha, também
  `[MEDIDO]`: `MATICUSDT` não está na Coinalyze, mas `ICXUSDT` está — **nenhuma fonte deste
  projeto é rota de fuga para resolver isso sozinha**, por isso o alias precisa ser curado
  manualmente com `evidence_url` (prova externa), nunca inferido.
- **"Sem tela"**: não construa nenhuma interface — o mecanismo é backend puro (carregador +
  validação), consumido por outro código, não por um usuário via UI.

## Contexto que já custou defeito aqui

- Reuse o padrão de arquivo versionado + validação já usado nesta fase, se houver um (ex.: como
  `content_dedupe_store.py`/`checksum_manifest.py` tratam ausência/forma errada de arquivo —
  "cauda truncada tolerada, forma errada recusada por nome" é o vocabulário deste repositório).
- Não confunda `T-07.9` com `T-07.8` — `universe_at` decide PERTENCIMENTO ao universo num
  instante; `instrument_alias` decide CONTINUIDADE de identidade entre dois símbolos diferentes.
  São perguntas diferentes; não tente resolver survivorship aqui.
- Contrato `Natureza` (`ADR-016`): leitura do arquivo YAML do disco mora em `infra`; a validação
  de forma (evidence_url obrigatório, resolução por data) é lógica pura de `domain`.

## Restrições do repositório (as mesmas de toda task desta sessão)

- Import-linter (`Natureza` incl.), cobertura domain 90/use_cases 80/infra 70%, idioma em inglês
  (produção e teste, inclusive mensagem de `raise`/`Error`/`Exception` — `CLAUDE.md` §"Mensagem
  de exceção"), `docs/INDEX.md` append-only, sem `Co-Authored-By:`.
- Push em worktree: `HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness`
  (nunca `.harness/mechanism`), URL explícita (`https://github.com/StharleyMax/cripto-strategy.git`,
  não `origin`/SSH), **nunca `--force-with-lease`** — use `--force` simples se precisar.
- **⚠️ Múltiplas sessões de Claude Code rodam em paralelo no diretório principal, e outra task
  (`T-07.6`) está sendo construída em paralelo AGORA mesmo** — sua worktree é isolada, mas
  **sempre `git fetch` + resolva contra `master` mais recente antes do push**; conflito típico é
  em `docs/INDEX.md`/`backend/README.md` (append duplo) — mantenha AMBAS as seções.
- **Aguarde ATIVAMENTE `bash backend/scripts/test.sh` terminar** — não dispare em background e
  pare o turno esperando notificação: isso não acorda subagente nesta sessão (medido
  repetidamente). Se precisar de background, sua PRÓXIMA ação no mesmo turno é checar
  `ps -p <PID>` até confirmar que já terminou.

## Protocolo de custo — R6/R7

- Se projetar passar de ~150 turnos, PARE e escreva o estado neste arquivo.
- Devolva **no máximo 15 linhas**, caminho do relatório em
  `docs/context/plataforma-dados/gates/T-07.9-builder.md`. **Nunca cole o corpo.**

## Entrega

Commit no branch `tasks/T-07.9-fase07`, push, PR aberta contra `master`.
