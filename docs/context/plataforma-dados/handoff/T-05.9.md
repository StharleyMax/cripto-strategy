# Handoff — T-05.9 (`plataforma-dados`, fase 05, componente `web`)

**Escrito pelo `/workflow` em 2026-09-02**, logo após `T-05.8` mergear (PR #66,
`c36d584`). Segunda raiz da árvore `web`: `depends_on = ["T-04.4"]` — `done`, mesma aresta de
`T-05.8`, e são independentes uma da outra.

## ⛔ Fronteira que não pode ser cruzada nesta task

**O owner foi explícito: nada de `backend/` nesta trilha agora** — a trilha `sentimento`
(15 worktrees ativas, ver `git worktree list`) é quem toca lá, em paralelo, agora. Se a
implementação real de `D5.8` exigir um endpoint/servidor novo em `backend/` (não só consumo
client-side de algo que já existe), **pare e devolva o achado — não implemente backend.**
Isso não é dedução minha: é redução literal do que foi pedido nesta sessão.

## O que a task pede, literal

`title`: *"Transporte HTTP enderecavel por conteudo para o historico. Nenhum tick chega ao
browser"*. `refs`: `ADR-005/D1`, plano 05 item 5.12, `DoD D5.8`, "3.468 msg/s de pico medido".

`docs/plans/SPEC-001-plataforma-dados/05_fatia_visivel.md:23,36`:

> **D5.8** — nenhum tick chega ao browser. Ação: contar mensagens e inspecionar payload.
> Saída esperada: taxa **≤ `max(1 Hz, 1/TF)`** por série, e **zero campo de nível de tick**
> (`agg_id`, preço por trade, quantidade por trade).

`docs/adr/ADR-005-transporte-de-leitura.md` §D1 — duas rotas por classe de tempo, nenhuma é a
do fornecedor direto. Leia a ADR inteira antes de desenhar a interface — ela já decide a forma
do transporte; a task é fechar o **lado `web`** desse contrato, que `T-05.8` deixou como
precedente: um módulo TS puro (sem tela, sem edição de `frontend/src` fora de `src/app/`),
testado por mutação, sem chamada de rede real em teste.

## Precedente de `T-05.8` — repita o que funcionou

- `ui-designer` concluiu que não havia decisão de UI/UX nova (contrato de dado, não pixel) —
  relatório em `docs/context/plataforma-dados/gates/T-05.8-design.md`. Espere o mesmo aqui, mas
  **confirme, não assuma** — é `web`, dono de julgamento é `ui-designer`
  (`harness policy --key agents.by_component`), gate é `ux-ui-mastery`
  (`docs/gate-de-design.md`).
- O `ux-ui-mastery:ux-audit` achou 1 problema não-bloqueante em `T-05.8` (nome de query param
  pouco reconhecível) e a correção foi barata por ser feita ANTES do merge — repita esse audit
  aqui, focado em nomenclatura de payload/endpoint e mensagens de erro, se houver superfície
  que um humano vai ler.
- `/build` roda `npm --prefix frontend run test:app` + `lint` + `harness rules --mode sweep`.
  QA valida por MUTAÇÃO (reintroduzir o defeito e provar que reprova), não só observação.

## Zero conflito, verificado

`git worktree list` — nenhuma das 15 worktrees ativas toca `web`/`charts`. `ui-designer` nunca
edita `frontend/` (R4). Trabalhe em worktree isolada (`isolation: worktree` no `Agent`).
