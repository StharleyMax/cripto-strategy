# Handoff — T-03.11 · Reconciliação diária liquidação capturada × agregado Coinalyze

**Card:** `CST-27` · **Componente:** `sentimento` · **Base:** `master@07193e6` · **Worktree:** `/tmp/claude-1002/wt/T-03.11`

## Por que agora

Depende de `T-02.2` (Coinalyze one-shot) e `T-03.2` (`!forceOrder@arr`), **ambas já mescladas** (PRs
#61 e #56). Escopo: código + prova offline/probe curto, não deploy contínuo.

## O DoD — e o ponto central é a RESSALVA, não o número

Bloco `id = "T-03.11"` em `docs/context/plataforma-dados/tasks.toml`. Refs: `CA-F0-14`, plano `03`
item **3.12**. Linha completa em `docs/context/plataforma-dados/tasks_review.md:275`.

- **Reconciliação DIÁRIA**: `Σ(liquidação capturada no dia via T-03.2)` contra o agregado `daily` da
  Coinalyze (capturado por `T-02.2`, ainda em **quarentena** — leia como quarentena funciona antes
  de desenhar o consumo). **1 chamada/dia/símbolo** de custo declarado.
- **A ressalva obrigatória é o requisito central, não um detalhe:** **não se sabe** se a Coinalyze
  constrói o agregado dela a partir do MESMO stream subamostrado que `T-03.2` grava. Se sim, a razão
  tende a 1 e **não prova nada**; se não, a razão mede a perda real de subamostragem. **As DUAS
  saídas têm de informar em qual caso estamos** — a reconciliação tem de expor isso explicitamente
  no resultado (não é um número solto, é um número + a interpretação de qual dos dois casos ele
  sugere), **"com a ressalva na tela"** é literal no título da task: mesmo sem tela ainda, o dado de
  saída tem de carregar o texto da ressalva pronto para quando a tela existir.

## Contexto que já custou defeito aqui

- **`T-02.2`'s quarentena existe por design** — a leitura de `backtest` sobre a série da Coinalyze
  devolve ZERO linhas até promoção explícita (D2.6, já provado por `T-02.2`). Esta task **lê da
  quarentena diretamente** para fins de reconciliação (não é o mesmo caminho que `backtest` usaria)
  — não tente "promover" a série aqui, isso é decisão de outra task/fase.
- `T-03.2` grava CRU com um rótulo de subamostragem **NÃO RESOLVIDA** (`latest|largest`) — essa
  ambiguidade documentada é exatamente o tipo de coisa que a ressalva desta task precisa poder
  citar, não ignorar.

## Restrições do repositório (as mesmas de toda task desta sessão)

- Import-linter (`Natureza` incl.), cobertura domain 90/use_cases 80/infra 70%, idioma em inglês,
  `docs/INDEX.md` append-only, sem `Co-Authored-By:`.
- Push em worktree: `HARNESS_MECHANISM=/home/stharley/.claude/plugins/cache/harness/harness-plugin/0.13.0/bin/harness`
  (nunca `.harness/mechanism`), URL explícita, **nunca `--force-with-lease`** — use `--force`
  simples.
- Provável conflito de merge em `backend/README.md`/`docs/INDEX.md` com a branch irmã `T-03.3` —
  resolva mantendo AMBAS as seções/linhas.
- **Aguarde ATIVAMENTE `bash backend/scripts/test.sh` terminar** — não dispare em background e pare
  o turno dizendo que vai esperar a notificação; isso não funciona entre turnos de subagente nesta
  sessão.

## Protocolo de custo — R6/R7

- Se projetar passar de ~150 turnos, PARE e escreva o estado neste arquivo.
- Devolva **no máximo 15 linhas**, caminho do relatório em
  `docs/context/plataforma-dados/gates/T-03.11-builder.md`. **Nunca cole o corpo.**

## Entrega

Commit no branch `tasks/T-03.11-reconciliacao-coinalyze`, push, PR aberta contra `master`.
