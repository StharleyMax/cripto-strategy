# `T-03.2` — ciclo de correção 1 (`NEEDS_FIX` do `quant-architect`, `[Q-STAMP-1]`)

```
[QA GATE — Fase 03 (03a): carimbo do OI na grade de 1 min, janela [T − 20 s, T] com teto, fora dela AUSENTE]
Feature: paineis-de-fluxo     Componente: sentimento
Task: T-03.2 (CST-277) — correção 1 de 3
```

**Entrada:** `handoff/Q-STAMP-1-quant-architect.md` (commit `884bcc9`), §4 itens 1–6.
**Base:** `67d8c0e` (build original) + `884bcc9` (laudo), branch `worktree-wf_7417a641-472-12`.

## O que mudou, item por item do laudo §4

| # | laudo | feito | onde |
|---|---|---|---|
| 1 | teto, não piso: `T` = instante de grade **no ou depois** de `time`; admite se `T − time ≤ 20 000` | `grid_instant = event_time_ms + (-event_time_ms) % 60_000`; largura fica 20 s, fechada nas duas pontas | `open_interest_grid_stamp.py:admitted_grid_instant` |
| 2 | desempate: a leitura de **maior `time`** vence; empate de `time` ⇒ a primeira fica | duas passadas: 1ª escolhe o vencedor por slot (`>` estrito, então o empate fica com o primeiro), 2ª distribui os três destinos em ordem de entrada | `stamp_open_interest_readings` |
| 3 | `__post_init__` herda o teto | sem mudança de lógica; mensagem passa a citar `[T − 20000 ms, T]`. Agora também recusa o carimbo do piso (leitura em `T0 + 5 s` em `T0`) — teste novo | `StampedOpenInterest` |
| 4 | comentários/docstrings reescritos; sai o parágrafo *"look-ahead in reverse"* | reescritos; o bloco de cabeçalho explica por que a janela fica **antes** de `T` (`POINT_AT_BUCKET_END`) e cita o caso `L < 0` medido | módulo inteiro |
| 5 | testes | reescritos, **22 testes** (eram 17) — ver abaixo | `test_open_interest_grid_stamp.py` |
| 6 | mutações `M5'` e `M6'` têm de morrer | **9/9 mortos**, ver abaixo | — |

Comentários de `T-03.1` que citavam a janela antiga, corrigidos (só texto, nenhuma lógica):
`open_interest_snapshot.py:14-17`, `binance_open_interest_client.py:15`, `test_open_interest_snapshot.py:47-48`.

## Testes novos ou reescritos (os que o laudo pediu)

- bordas: `T` admitido com defasagem 0; `T − 20 000` admitido; `T − 20 001` ausente;
- `T + 1 ms` **não é puxado de volta a `T`** (`test_one_millisecond_after_the_grid_instant_is_never_pulled_back_to_it`);
- envio travado, `time = T + 2 240 ms` (o caso `L < 0` do laudo §2) ⇒ ausente;
- captura real de `T-03.1` **admitida** em `1_790_287_800_000`, defasagem **6 297 ms** (BTC) e **8 965 ms** (ETH),
  via `admitted_grid_instant` e via `stamp_open_interest_readings`;
- varredura de 60 000 offsets: admitidos exatamente `{0} ∪ [40 000, 59 999]` = **20 001**;
- **propriedade anti-lookahead**: 3 min de leituras a cada 7 ms (`n = 25 715` leituras, um símbolo por leitura), toda
  linha admitida tem `0 ≤ grid_instant_ms − event_time_ms ≤ 20 000`;
- desempate nas duas ordens de entrada (a mais fresca vence nas duas) e empate de `time` (a primeira fica).

## Mutações — o que a proteção REJEITA

Script: `mutate_t032.py` no scratchpad da sessão (aplica, purga `__pycache__` de `sentimento`, roda
`.venv/bin/python -m pytest -q -p no:cacheprovider --no-cov tests/sentimento/test_open_interest_grid_stamp.py`,
restaura). Universo: 22 testes.

| mutante | o que muda | testes que reprovam |
|---|---|---|
| M1 carry-forward (contornando a trava com `object.__new__`) | minuto fora da janela recebe o valor anterior | 3 — inclui `…never_carries_the_previous` e a propriedade anti-lookahead |
| M2 trava removida | `__post_init__` aceita `event_time` fora da janela | 2 — `…carried_value_cannot_even_be_represented…`, `…ahead_of_its_reading_by_a_look_ahead…` |
| M3 janela semiaberta (`<`) | `T − 20 s` sai | 3 |
| M4 janela de 21 s | `T − 20 001` entra | 3 |
| **M5' piso no lugar do teto** (a regra de `67d8c0e`) | volta a carimbar em `T` leituras de depois de `T` | **16** — inclui a propriedade anti-lookahead, `T + 1 ms`, o envio travado e a captura real |
| M5'' mais próximo no lugar do teto | `T + 1 ms` vira ponto de `T` | 6 — inclui a propriedade anti-lookahead |
| **M6' primeira vence** (a regra de `67d8c0e`) | a ordem de entrada escolhe o vencedor | 2 — `…freshest…handed_in_last`, `…accounted_for_exactly_once` |
| M6'' última vence no empate (`>=`) | empate de `time` troca o vencedor | 1 — `test_on_a_tie_of_time_the_first_reading_stays` |
| M7 slot sem símbolo | ETH do mesmo minuto vira `superseded` do BTC | 3 |

**9/9 mortos.** Original restaurado: 0 falhas.

## Comandos rodados (literais) e resultado

| comando | resultado | universo |
|---|---|---|
| `make test-fast K=open_interest` | 122 passed, 2682 deselected | 122 testes |
| `.venv/bin/ruff check` + `ruff format --check` + `.venv/bin/mypy` nos 5 `.py` alterados | OK / `5 files already formatted` / `no issues found in 5 source files` | 5 arquivos |
| `python mutate_t032.py <worktree>` | 9/9 KILLED, restaurado 0 falhas | 22 testes × 9 mutantes |
| `harness rules --mode sweep --changed-only` (com os arquivos em stage) | `rc=0`, saída vazia | arquivos alterados |
| `harness rules --mode file --path <sonda com print("x")> --format ndjson` | 1 achado, linha 2 (*"saida por impressao direta"*) — sonda apagada em seguida | 1 arquivo-sonda |
| `E2E_API_PORT=8822 E2E_NEXT_PORT=4322 make verify` | VERMELHO só no `e2e` (51 passed, 5 failed); os outros 7 portões OK — detalhe abaixo | 8 portões |

## Cobertura

`bash backend/scripts/test.sh` (dentro de `make verify`): `open_interest_grid_stamp.py` **100%** (51 stmts, 18
branches, 0 miss; linha 1932 do log). Total **96.38%**. Alvo: piso por camada da política, sem mudança.

### `make verify` (log bruto: `/tmp/verify-wf_7417a641-472-12-20260925T114017Z.log`, 352K)

- `lint-backend` OK (479) · `lint-frontend` OK · `test-frontend` OK (930 pass) · `test` OK — **2799 passed, cobertura
  96.38%** · `boundaries` OK (7 kept, 0 broken) · `regras` OK (0 bloqueio, 76 avisos) · `política` OK ·
  `e2e` **51 passed, 5 failed**.
- **4 dos 5 são a baseline** de `REGRAS-DE-DESPACHO` §2: `e2e/16…:204` (log `:2570`), `e2e/18…:107` (`:2604`),
  `e2e/18…:153` (`:2605`), `e2e/20…:383` (`:2661`).
- **1 fora da baseline: `e2e/11-canvas-fundo.spec.ts:62`** (log `:2422`, erro `:2674-2691`: *"nenhum canvas com pixel
  pintado foi encontrado em /symbol"*, `Received: 0`). Rodado de novo, isolado, nas mesmas portas:
  `playwright test … e2e/11-canvas-fundo.spec.ts --repeat-each=3` ⇒ **3 passed** (`rc=0`). Durante o `verify` a
  máquina estava com `load average 6,18` (`uptime`), com outras worktrees rodando. Leitura `[INFERRED: passa 3/3
  isolado, a falha é "nenhum pixel pintado ainda", e esta task não toca `frontend/`]`: **flaky de renderização sob
  carga, não regressão desta task**. `git diff --stat 884bcc9 -- frontend/` vazio.

## Doc delta

- `backend/README.md`: **atualizado** — seção `📎 2026-09-24 por T-03.2` reescrita para `[T − 20 s, T]`, teto, a mais
  fresca vence, a propriedade anti-lookahead; com bloco **CORREÇÃO 2026-09-25** que mantém escrito o que a versão
  de `67d8c0e` dizia e por que caiu.
- `docs/INDEX.md`: sem mudança — o registro é do orquestrador na integração da wave (mesmo motivo de `T-03.1`/`T-03.2`).
- ADR: não necessário — a decisão é o default de `[Q-STAMP-1]`, pergunta inferível com dono de validação
  `quant-architect` (`SPEC-009` §11); o laudo é o registro.

## Bloqueado / pendente, nomeado

- **Emenda de `SPEC-009` §6.1** (a frase *"a primeira chamada feita em `T` ou depois dele cujo `time` fique em
  `[T, T + 20 s]`"* → *"a leitura de maior `time` em `[T − 20 s, T]`"*): **não feita aqui** — vai por exceção ao
  owner, escrita por orquestrador ou `/architect` (laudo §5). Até lá, código e SPEC divergem **de propósito**.
- **Comentário stale em `T-03.3`, fora desta branch:** `open_interest_catalog.py:193` (em `wave/paineis-f03a`,
  commit `647230d`) diz *"no reading lands in `[T, T + 20 s]`"*. Só texto — o `OPEN_INTEREST_POLL_MAX_STALENESS_MS`
  (2 × 60 000) continua válido com o teto. Esta branch não tem `T-03.3` (não é ancestral de `wave/paineis-f03a`
  desde `49a0cc0`); quem integra corrige na mesma leva, ou uma linha no próximo toque de `T-03.3`.
- **`T-03.4`**: offset `T − 5 s`, agendado pelo relógio de parede, carimbado pelo `time` da resposta, `lag_ms`/
  `staleness_ms` por chamada — é contrato do laudo §3 para aquela task, nada disso é código desta.
- Nenhum `status` em `tasks.toml` alterado; nenhum `gate-record`/`approve`/`advance`/`resolve` rodado.
