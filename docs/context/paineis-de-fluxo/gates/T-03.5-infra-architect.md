# `T-03.5` — veredito do `infra-architect` (`CST-280`)

**Veredito: APPROVED** — a task fecha como está.

Feature `paineis-de-fluxo` · fase `03` (trilha `03a`, item 3a.4) · componente `infra`
Julgado: branch `worktree-wf_820bac26-f99-5`, `b6b044f` (código) + `f067a16` (relatório do builder)
Entrada: `gates/T-03.5-build.md`; DoD de `harness tasks json paineis-de-fluxo` (`T-03.5`); `SPEC-009` §6.1, §6.7;
plano `03_oi_candle.md` item 3a.4.

## 1. Contra o DoD da task

DoD literal: *"docker compose config valido; veredito do infra-architect em gates/."*

| critério | resultado | comando / força |
|---|---|---|
| `config` válido na HEAD | `rc=0` | `docker compose -f deploy/compose.yml --env-file .env config --quiet` numa árvore descartável (`deploy/` copiado, `.env` só com placeholders para os 3 `:?` e 3 `${}` interpolados) `[MEDIDO 2026-09-25]` |
| a mudança é só comentário | diff do YAML **renderizado** `master` → HEAD = **0 linhas** | `diff <(config master) <(config HEAD) \| wc -l` `[MEDIDO 2026-09-25]` |
| a variável chega ao `collector` pelo `.env` | `.env` com `=120` → `collector: '120'`; sem a linha → ausente (o default `60.0` do código vale) | `config --format json` `[MEDIDO 2026-09-25]` |
| a regra de boot que o comentário cita existe | `30`, `90`, `60.5`, `0`, `-60`, `nan`, `inf`, `sixty` recusados | `test_collectors_cli_open_interest_poll_collector.py:397`; `make test-fast K=open_interest_poll` → `36 passed` `[MEDIDO; test-fast NÃO é verde de portão — o builder reporta make verify 8/9, o 9º é o baseline de e2e]` |

**"No idioma de `OPEN_INTEREST_CYCLE_INTERVAL_S` (`:187`)"** foi lido certo. Aquele idioma **é** documentação no bloco de
comentário + valor em `../.env` via `env_file:`, nunca `environment:`. O próprio `compose.yml:159-165` escreve por quê,
e os precedentes `e10f1d2`/`ae24e23` fizeram igual. Pôr `60` num `environment:` teria violado o idioma que o DoD cita.

O conteúdo do comentário confere com o código: default `60.0` (`collectors_cli.py:398`), lead de `5 s` como contrato
(`:410`, `[Q-STAMP-1]` §3), janela `[T − 20 s, T]` (a corrigida por `Q-STAMP-1`, **não** a `[T, T + 20 s]` que ainda
aparece em `SPEC-009` §6.1:174), recusa de não-múltiplo de 60 (`_grid_multiple_cadence`, `:662`), gatilho de 2×
(1,14 MB/dia) de `03_oi_candle.md:36`, e os 99,06 B/linha de `SPEC-008`:362.

## 2. Contra `SPEC-009` §6.7 / plano 3a.4: "job agendado e pegada"

| dimensão | julgamento | força |
|---|---|---|
| **processo novo?** | **Não.** É o 7º thread do processo `collector`, que já existe (`ADR-027/D1`: um processo, N threads). Zero container, zero daemon, zero imagem nova | `[MEDIDO: grep name="collector-" collectors_cli.py → 7, pelo builder]` |
| **cota** | 4 de 2.400 peso/min = **0,17%**, e o teto é duro (sem retry ⇒ ≤ 4/min) | `[DOC + MEDIDO 2026-09-23, SPEC-009 §6.1]` |
| **fila (Redis)** | 4 entradas por ciclo, **zero pico de boot** (sem backfill). Contra `REDIS_STREAM_MAXLEN` 100.000: desprezível | `[INFERRED: aritmética; sem backfill por construção do endpoint]` |
| **disco** | **~570 KB/dia, ~208 MB/ano, no Postgres, disco da VPS compartilhada.** Nada no R2 | `[INFERRED: SPEC-009 §6.1]` — a medição é 3a.5, depois de 24 h ligado |
| **RAM/CPU** | um thread a mais dormindo 55 s por minuto | `[NÃO MEDIDO]` — não bloqueia: a mesma classe de 6 threads já está em produção |
| **pegada desta task** | **0 MB.** Mudança só de comentário | `[MEDIDO: diff renderizado = 0]` |

⚠️ **O que a pegada NÃO resolve, e fica dito:** os 208 MB/ano **acumulam sem teto**. Não há retenção nem export para
o R2 desta série, e o gatilho de 2× de 3a.5 mede **taxa**, não **estoque**. Numa VPS com disco sob pressão
`[PREMISSA-OWNER]`, o número a vigiar em dois anos é ~416 MB, não 570 KB/dia. **Não bloqueia `T-03.5`**: a cadência e o
custo anual foram postos para veto do owner em `[Q-CAD-1]` `[DECISÃO-OWNER: 2026-09-23, 2ª rodada]`. Mas retenção é
decisão estrutural ainda não tomada, e cabe numa ADR quando 3a.5 trouxer o número medido.

## 3. Achado do builder nº 1: nenhum portão reprova fixar a cadência num `environment:`

**Confirmado, com o caso rejeitado que deveria existir e não existe:**

```
mutação (cópia descartável): collector.environment.OPEN_INTEREST_POLL_CYCLE_INTERVAL_S: "60", com .env = 120
  docker compose config --quiet                          → rc=0, e o collector recebe '60' (o .env perde em silêncio)
  harness rules --mode file --path deploy/compose.yml    → 0 linhas, exit=0   (arquivo restaurado; git status limpo)
```
`[MEDIDO 2026-09-25]`. Arquivo limpo: também 0 linhas, `exit=0`. **Os dois estados dão a mesma saída**, então o
instrumento não distingue um do outro. É o `rc=0` ambíguo.

**Julgamento: real e não bloqueante para esta task.**
- O DoD não pede portão, e o risco **não nasceu aqui**: vale igual para as 7 variáveis de cadência desde `T-03.3`.
  Esta task não piorou nada.
- A consequência é **configuração do operador ignorada**, não perda de dado nem vazamento. Para esta variável, fixar
  `60` é fixar o default. O dano aparece só quando o operador tentar mudar a cadência e a mudança não pegar.
- **O fechamento certo é uma `[[rules.own]]` que alcance YAML, nascida com corpus** (item `1.14`, parte (c)). Esboço:
  padrão de linha `^\s*-?\s*[A-Z_]+_(CYCLE_INTERVAL_S|CYCLE_OFFSET_S|BACKFILL[A-Z_]*)\s*[:=]` sob `deploy/`. As linhas de
  comentário começam por `#`, e o padrão não as alcança. Corpus MORDE: o pin acima, e a forma de lista
  `- KLINES_CYCLE_INTERVAL_S=30`. Corpus CALA: o `compose.yml` atual inteiro (7 nomes citados em comentário).
  **Fora do escopo de `T-03.5`** (toca `harness.toml` e governança). Vai para task própria de `infra`, a abrir por
  quem abre task.

## 4. Achado do builder nº 2: `collector` × `collectors`

**Confirmado. A verdade é `collector` (o `compose.yml`), e quem erra são os documentos.**

| lado | ocorrências | comando |
|---|---|---|
| comandos operacionais `docker compose … collector` no repositório | **15** | `git grep -nE 'compose[^\|]*(logs\|restart\|up\|run\|ps\|stop)[^\|]*\bcollector\b' \| wc -l` `[MEDIDO 2026-09-25]` |
| idem com `collectors` | **0** | mesmo comando com `collectors` |
| texto que chama o serviço de `collectors` | 4: `SPEC-009:258`, `03_oi_candle.md:25`, `collectors_cli.py:285` (comentário), título de `T-03.5` no `tasks.toml` | `git grep -n '`collectors`'` |

Renomear o serviço para `collectors` quebraria 15 comandos e o nome do container em produção, para acertar 4 frases.
**Recusado.** O certo é corrigir o texto, e a correção **não bloqueia**: o builder mirou o serviço real, e nada
executável depende da grafia errada.
- `collectors_cli.py:285`: comentário de código, correção de uma palavra. Vai na próxima task que tocar esse arquivo;
  não se reabre `T-03.5` para isso (tocaria `backend/`, fora do componente).
- `SPEC-009:258` e o plano: errata do dono do documento (`/architect`), junto com o `:150-231` / `:187` de lá, que já
  envelheceram. A variável de referência hoje está em `compose.yml:189`.

## 5. Fora do escopo, registrado para não sumir

1. **`LIQUIDATION_CYCLE_INTERVAL_S`** (default 300, `collectors_cli.py:289,421`) não está na lista de variáveis de
   cadência do `compose.yml`, e agora o cabeçalho que esta task reescreveu **nomeia** o 6º thread. A lacuna ficou mais
   visível, não maior. Dívida de `T-05.5`. Uma linha de comentário resolve.
2. **O `env_file: ../.env` é o mesmo em `api`, `writer`, `collector`, `web` e `caddy`** `[MEDIDO: .env=120 aparece nos
   5]`. Isso entrega todo segredo do `.env` (Postgres, e a key da Coinalyze, que `CLAUDE.md` põe em `.env`) também ao
   `caddy` e ao `web`, que não precisam deles `[INFERRED: não li o .env nem o .env.example]`. É raio de exposição
   anterior a esta feature, e merece task de `infra` própria.
3. **Sinal de `F-D6-5b`:** `deploy/` tem **3** arquivos versionados (`git ls-files deploy | wc -l` `[MEDIDO 2026-09-25]`) e com trabalho de infra de verdade (cadência,
   cota, fila, disco). O rótulo largo `infra` segue pagando. Não estagnou.

## 6. O que este veredito NÃO é

- Não mede `DoD-1`/`DoD-3` de `03a` (≥ 0,95 × 1.440 pontos/símbolo/dia). Isso exige deploy + 24 h, e é 3a.5.
- Não julga o carimbo nem o lookahead (`[Q-STAMP-1]`). Isso é do `quant-architect`; aqui só se conferiu que o
  comentário cita o contrato certo.
- Não grava `gate-record`, `approve` nem `advance`. Isso é ato do owner/orquestrador.
