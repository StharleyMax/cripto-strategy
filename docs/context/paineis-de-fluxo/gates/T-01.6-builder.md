# T-01.6 — camada de DOM por pane (builder)

```
[QA GATE — Fase 01: esqueleto — T-01.6 (plano 01 item 1.5)]
Feature: paineis-de-fluxo     Componente: web (+ charts: geometria vertical pura)
Spec: docs/specs/SPEC-009-paineis-de-fluxo.md  (RF-4, CA-12, [Q-DG-1])
Plan: docs/plans/SPEC-009-paineis-de-fluxo/01_esqueleto.md  (item 1.5)
```

## 1. Arquivos

| arquivo | | o quê |
|---|---|---|
| `frontend/src/charts/pane-stack-layout.ts` | new | `stackedPaneLayout` (altura que mantém o pane mais leve no piso de 72px) e `paneScaleMargins` (reserva da legenda por **compressão** da banda, folga de 4px do separador, `unmeasured`/`overflow` explícitos). Geometria mora em `charts` (`ADR-003`) |
| `frontend/src/charts/pane-stack-layout.test.ts` | new | 14 testes, 4 MORDE |
| `frontend/src/charts/index.ts` · `chart-theme.ts` | modified | seção 9 do barrel; `ChartSurfaceTheme.paneSeparatorColor = provenanceWeak` |
| `frontend/src/app/symbol/chart-options.ts` | modified | `layout.panes = { separatorColor, separatorHoverColor: token, enableResize: false }` — sem hex literal (`DR-1`) |
| `frontend/src/app/symbol/pane-chrome-options.test.ts` | new | 3 testes (`C-6`), 1 MORDE |
| `frontend/src/app/symbol/pane-registry.ts` · `.test.ts` | modified | `F1_PANE_STRETCH` (R-2, §3), `paneLayerTestId(pane_id)`; 2 testes novos |
| `frontend/src/app/symbol/SymbolClient.tsx` | modified | camada por pane via `createPortal` no wrapper do pane (`getHTMLElement().children[1].firstElementChild`), `PANE_LAYER_CLASS` (`pointer-events-none … [&_a]/[&_button]:pointer-events-auto`), `PaneLegend`/`PaneLegendLine`/`PaneDetails` (sr-only), `setStretchFactor`, efeito de layout que aplica `paneScaleMargins` e publica `data-pane-height-px`/`data-legend-bottom-px`/`data-reserved-scale-top-px`/`data-legend-reserve` na raiz da camada (valor **lido de volta** da lib), `ResizeObserver` nas legendas, `ChromeModeStamp` (`C-4`), canvases `aria-hidden` + tabelas `role=presentation` |
| 6 testes de contrato de fonte (`beyond-coverage-badge-…`, `cvd-pane-…`, `data-fact-ascii-…` 44→45, `long-short-pane-dom-…`, `long-short-pane-design-…` A-3, `oi-pane-…` V-1 9→10) | modified | re-ancorados, cada um com comentário do motivo; nenhum negativo removido |
| `frontend/e2e/23-pane-layer.spec.ts` | new | o render (§4) |
| `frontend/e2e/13`, `15`, `17`, `21`, `22` | modified | re-âncora mínima (§6). **`e2e/20` NÃO tocado** (T-01.F2) |

## 2. Regra de posicionamento (FORMA do builder, submetida ao gate de T-01.11)

**Visível na legenda:** identidade (h2/h3), leitura atual, idade/`COMO EM`, procedência de terceiro, badges de integridade/ausência/beyond, declaração log10 do volume (`BLOCKER-1`), frescor do OI (`RNF-2`).
**sr-only (`PaneDetails`):** frases explicativas — horizonte legível, slots, legenda de marcas, rodapés, equilíbrio.
⛔ Não é decisão de design; é a forma mínima para a camada existir. `ui-designer` + `ux-ui-mastery` decidem em `T-01.11`. **Nenhum gate aninhado foi possível** (restrição 5: subagente não aninha).

## 3. R-2 — pesos renormalizados (proposta, pendente do design_gate)

`F1_PANE_STRETCH = price 34 · liq long 11 · liq short 11 · OI 15 · L/S 9 · CVD 9` (Σ 89).
- A 5,5 por perna (a metade de 11) o piso de 72px exige gráfico **> 1.024px**. `[MEDIDO: pane-stack-layout.test.ts "R-2's arithmetic", sobre stackedPaneLayout]`
- A 11 por perna cabe no alvo de 910px sem crescer (`grewForFloor=false`). `[MEDIDO: idem, teste "F1"]`
- O alvo de 910px é premissa do gate `[INFERRED]`; os 28px do eixo de tempo são folga `[NÃO MEDIDO]`.
- **No render:** as alturas foram 335 · 108 · 108 · 148 · 89 · 89 px, todas ≥ 72. `[MEDIDO: e2e/23, fact "layers", n=6]`

## 4. Condições do gate r2 — o que prova cada uma

| | afirmação | prova | MORDE |
|---|---|---|---|
| **C-6 separador** | `separatorColor = hover = #8b949e`, `enableResize=false`, contraste ≥ 3:1 | unit `pane-chrome-options.test.ts` (3/3). e2e/23: 5 `td[colspan=3]`, `rgb(139, 148, 158)`, 0 alças | **tirar o bloco `panes`** ⇒ unit `✖ C-6 …` (1 falha em 3); e2e/23 `Expected "rgb(139, 148, 158)" · Received "rgb(224, 227, 235)"`, 2 alças por separador `[MEDIDO: wf14-e2e23-m1.log]` |
| **C-6 volume** | margem inferior ≥ 4px (`clearSeparator`) | unit `clearSeparator (C-6)` + MORDE (bottom 0 ⇒ 0px) | unit |
| **reserva da legenda** | topo da escala lido da lib ≥ fundo medido da legenda, nos 6 panes | e2e/23: 113,4≥54 · 77,6≥72 · 45,3≥38 · 64,4≥56 · 50,05≥44 · 61,45≥56 px; preço: **0** px de tinta de vela sob a legenda, 2.417 px abaixo, pixel mais alto = **113** contra 113,4 lido de volta | **não aplicar `belowLegend`** ⇒ e2e/23 `liquidation-cohort-long: o topo da escala (5.4px) está acima do fundo da legenda (72px)` `[MEDIDO: wf14-e2e23-m2.log]` |
| **C-5** | filho interativo continua clicável e alcançável por Tab | e2e/23: `<button>` sonda injetado na camada: `pointer-events: auto`, é o `elementFromPoint` do próprio centro, o clique chega (`data-clicks=1`), alcançado por Tab. **Ablação:** um `<span>` no mesmo lugar: `pointer-events: none`, `elementFromPoint` ≠ span | ablação no próprio spec |
| **C-4** | modo explícito no chrome | `data-fact="chrome_mode:as_of"` visível: *"COMO EM T · T = … UTC · as idades de cada painel contam contra T, não contra o relógio"* | — |

**Declarado, não escondido:**
- **O pixel não morde sozinho no pane de preço.** No universo do stub, a margem própria da lib (0,2 × 335 = 67px) já fica abaixo de uma legenda de 54px, e a mutação mediu 0 px sob a legenda também (`wf14-e2e23-m2b.log`). Quem morde é a leitura de volta. O pixel mais alto (113) = topo lido (113,4) é o que liga pixel → leitura → legenda.
- **O stub não serve dados de liquidação, OI, L/S nem CVD.** Nesses panes a prova é a leitura de volta da lib, não o pixel.
- **Não existe filho interativo nas camadas hoje:** `grep -nE '<(a|button|Link|details|summary|input|select)[ >]' SymbolClient.tsx` → só a TF bar, que fica fora das camadas. Por isso a C-5 é provada por uma sonda injetada, não por um elemento real.

## 5. Achado — a premissa do gate r2 sobre o default da lib está errada

`DESIGN-LAYOUT-ux-critique-r2.md:14,55,117` e `tasks_review.md:44` dizem que o separador padrão da lib é `#2B2B43` (1,30:1).
- No `lightweight-charts` 5.2.1 instalado, o padrão é **`#E0E3EB`**; `#2B2B43` é o `borderColor` padrão da price-scale. `[MEDIDO: node_modules/lightweight-charts/dist/lightweight-charts.development.mjs:12391 e :12405; render do mutante = rgb(224, 227, 235)]`
- A **condição C-6 continua valendo**: sem o override, aparece uma barra clara que o design não escolheu (a lib também reativa as alças).
- Só o **número 1,30:1 cai**.
- O teste lê o padrão do próprio bundle, então não pode divergir de novo.
- **Proposta ao `ui-designer`:** corrigir a linha do r2. Não editei o relatório do gate de outro agente.

## 6. Re-âncoras de e2e, e o que continua vermelho

A camada é **irmã** dos canvases (portal no wrapper do pane), não mais ancestral.
- **`15:244`/`:613`, `17:309`, `21:142`:** `[data-testid="price-pane"] canvas` virou `…price-pane` → `xpath=../canvas`.
- **`13`:** `liquidation-pane` agora é o cabeçalho do painel, dentro da camada da coorte long. Leituras por coorte passaram para `liquidation-cohort-${cohort}`; o seletor de degradação virou `[data-testid^="liquidation-cohort-"]`.
- **Marcador `liquidation_slots`:** saiu do `sr-only` e virou filho direto da raiz da camada, porque `e2e/13:700` lê o `clientWidth` dele (1px dentro de `sr-only`).
- **`22`:** só o comentário mudou (o preço agora tem ~335px; y=110 continua dentro dele).
- **⚠️ `e2e/20:288`** (`price-pane [data-visible-logical-from]`) **vai reprovar por esta task** e **não foi editado** (a T-01.F2 edita o arquivo em paralelo). Correção de 1 linha para a T-01.F2/T-01.8: trocar para `[data-testid="symbol-chart-host"]`, como os e2e/16 e 22 já fazem.

## 7. Fora do escopo, declarado

- **`mark-band-geometry` continua sobre `CHART_HEIGHT_PX = 220`**, que virou referência nominal: `productionNumber("CHART_HEIGHT_PX")` é lido pelos testes de geometria. Religar a banda de marcas à altura real do pane é trabalho de outra task.
- **Validação ao vivo via `mcp__playwright__*`:** o servidor não existe nesta instalação, então o passo não rodou. O e2e/23 versionado é o portão.

## 8. Comandos (literais) e resultado

Dentro de `frontend/`:
- `npm run test:app` → **511/511** (eram 506; +3 do chrome, +2 do registry).
- `npm run test:charts` → **300/300**, incluindo os 14 de `pane-stack-layout.test.ts`.
- `npm run test:s1` → **105/105**; `npm run test:s3` → **111/111**.
- `make lint-frontend` (eslint + `tsc --noEmit --strict`) → **rc=0**.
- `node_modules/.bin/next build && node_modules/.bin/playwright test 23-pane-layer` → **1 passed**, 3 builds (verde, M1, M2) + a volta.

Na raiz:
- `harness rules --mode sweep --changed-only` → **rc=0**, sem saída ⚠️ (não sei se ele enxerga arquivos não commitados).
- `E2E_API_PORT=8821 E2E_NEXT_PORT=4321 make verify` → ver §9.

**Cobertura:** o front não tem medidor de cobertura `[NÃO MEDIDO]`.

## 9. make verify

`E2E_API_PORT=8821 E2E_NEXT_PORT=4321 make verify` (depois de purgar `backend/**/__pycache__`) → **VERMELHO**.
Log: `/tmp/verify-wf_7417a641-472-14-20260924T231516Z.log`. `[MEDIDO 2026-09-24T23:15Z]`

| portão | resultado |
|---|---|
| lint-backend | OK, 473 arquivos |
| lint-frontend | OK |
| test-frontend | OK, 1027 pass / 0 fail em 4 suítes |
| test | OK, 2728 passed, cobertura 96,31% |
| boundaries | OK, 7 kept |
| regras | OK, 0 bloqueios, 76 avisos |
| política | OK |
| **e2e** | **57 passed, 1 failed** (58 testes, incluindo o novo `e2e/23`) |

**O único vermelho é `e2e/20:383`.** Esse número de linha **está** no baseline conhecido (`16:204`, `18:107`, `18:153`, `20:383`), mas **o mecanismo mudou, e é desta task**:
- Hoje ele morre por timeout em `readPriceRange` (`:294`), esperando `locator('[data-testid="price-pane"] [data-visible-logical-from]')`.
- É exatamente o seletor da linha `:288`, que o §6 previu quebrar. O vermelho do baseline era outro, de latência.
- **Não mascarar:** o `20:383` "esperado" agora esconde uma quebra de seletor.
- Conserto (1 linha, de T-01.F2/T-01.8): em `:288`, trocar para `[data-testid="symbol-chart-host"]`.

Os outros três do baseline passaram nesta rodada:
- `18:107` e `18:153` passaram.
- O e2e/16 passou; as linhas dele hoje são `:147` e `:179`.

Os e2e re-ancorados `13`, `17`, `21` e `22` também passaram.

⚠️ **As re-âncoras do `e2e/15` (`:244`/`:613`) NÃO foram exercitadas** `[NÃO MEDIDO]`.
- `CA-2` (`:803`) e `CA-4` (`:957`), que usam esses seletores, são pulados com `test.skip` no universo fraco (0 velas).
- O mesmo padrão (`layer.parentElement` → canvases irmãos) é exercitado e passa no `e2e/23` (`measureInkAroundPriceLegend`).
- Contra o deployment do owner (universo forte), o `e2e/15` precisa de uma rodada.

## 10. Doc delta

- `docs/INDEX.md`: acrescentada uma linha (append-only).
- `STITCH_CONTEXT.md` / `DESIGN_SYSTEM.md`: **sem mudança**. O token do separador (`provenanceWeak`) já existia e foi decidido no r2; a regra de posicionamento do §2 é proposta para `T-01.11`, não uma decisão.
- O gotcha do default da lib ficou registrado em `chart-theme.ts` (docstring) e no cabeçalho de `pane-chrome-options.test.ts`. A correção do relatório r2 fica proposta ao `ui-designer` (§5).
- ADR: **não necessária**. `ADR-044` (um gráfico com panes nativos v5) e o gate r2 (`[Q-DG-1]`) já decidem a camada; esta task só a implementa.
