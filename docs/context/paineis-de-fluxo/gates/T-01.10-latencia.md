[QA GATE — Fase 01: T-01.10, medição de latência na janela exclusiva (F-A, F-B, F-C, F-7, `make verify`)]
Feature: paineis-de-fluxo     Componente: web

# T-01.10 — medição do teto sobre o conserto do desenho §3

**Data:** 2026-09-25, das 17:08 às 17:56 (−03:00) · **SHA medido:** `f16673d` (árvore limpa, `git status --short` vazio)
· **Autor:** `frontend-builder` · **Janela:** exclusiva de latência (o workflow garante que nenhum outro agente dele roda)
· **Portas:** API 8821, Next 4321 · **Máquina:** 8 núcleos (`nproc`).
**Teto:** `axis_intra_gesture_interval_max_ms ≤ 160 ms` `[DECISÃO-OWNER: 2026-09-22, escolha entre alternativas apresentadas]`. Não subiu.

## 0. Veredito

**F-A verde em 5/5, os dois tetos verdes em 5/5, e F-B, F-C e o controle negativo do F-7 validam o instrumento.**
A escada do §6 **não foi usada**: o degrau 1 (o próprio desenho) bastou. O `make verify` completo ficou **VERDE,
8 portões, e2e 64 passed**, e isso inclui os 4 vermelhos que eram a baseline conhecida (`e2e/16:204`, `e2e/18:107`,
`e2e/18:153`, `e2e/20:383`), que agora passam.

## 1. Como foi medido

- **Stack:** `bash scripts/e2e-env.sh up 1 8821 4321`, UM `next build` de `f16673d` para todas as rodadas do lote
  (em 10 s, com cache), derrubada no fim com `e2e-env.sh down`. O desenho pedia "lote solo". Isso foi cumprido: a
  janela é exclusiva e a carga ficou entre **0,40 e 1,37** (load1 em 8 núcleos) em todas as medições.
- **Lote principal:** [`T-01.10-latencia-run-lote.sh.txt`](T-01.10-latencia-run-lote.sh.txt). São 5 rodadas. Em
  cada uma rodam, nesta ordem, `e2e/20` esparso (produção), `e2e/20` com `?e2eDenseSeries=1` (F-B), `e2e/20` com
  `?e2ePageApplyBusyMs=80` (F-C) e `e2e/17`. O `uptime` é gravado antes de cada medição
  ([`T-01.10-latencia-uptime.log`](T-01.10-latencia-uptime.log)).
- **F-7 (rAF):** o instrumento companheiro da `T-01.1` §4, com duas mudanças
  ([`T-01.10-latencia-raf-instrument.spec.ts.txt`](T-01.10-latencia-raf-instrument.spec.ts.txt), sha256 `9dcbf30fbb89…`,
  runner [`T-01.10-latencia-run-raf.sh.txt`](T-01.10-latencia-run-raf.sh.txt)):
  **(a)** o alvo do arrasto passou a ser `price-pane` → `xpath=../canvas`, igual ao `e2e/17:311`. O seletor antigo
  (`[data-testid="price-pane"] canvas`) não acha mais canvas, porque desde a `T-01.6` a camada é irmã dos canvases.
  A primeira rodada de *smoke* expirou por isso, e ela **não entra** nas tabelas.
  **(b)** `BASELINE_PAN_BUSY_MS=20` registra um listener `pointermove` em captura com *busy-wait* de 20 ms durante
  o arrasto. É o controle negativo que a emenda F-7 exige, e que o instrumento da `T-01.1` não tinha.
  O braço B lê dado real por proxy só-leitura (GET/HEAD) para `127.0.0.1:8000`, com 31 GETs por rodada e nenhuma
  escrita. O arquivo foi copiado para `frontend/e2e/91-baseline-t011.spec.ts` só durante a execução e apagado
  depois. Ele não foi commitado em `frontend/e2e/`.
- **Saída bruta:** [`T-01.10-latencia-facts.jsonl`](T-01.10-latencia-facts.jsonl), com **1.260 linhas**, todas as
  `E2E-FACT` dos dois lotes e as chaves `batch`/`run`. As tabelas saem de
  [`T-01.10-latencia-parse.mjs.txt`](T-01.10-latencia-parse.mjs.txt) (`node parse.mjs <lote>`).
- ⚠️ **Defeito do meu runner, e a correção:** o `rc.log` do lote principal gravou `rc=0` em tudo, porque o `$?`
  foi avaliado depois do `$(date)`. O resultado real de cada rodada foi lido do log do Playwright
  (`grep -E '^\s+[0-9]+ (passed|failed)'`, 20 logs). O runner do F-7 já captura o `rc` do jeito certo.

## 2. F-A: o teto do owner, e os critérios que a T-01.10 já tinha `[MEDIDO, n = 5 rodadas]`

| rodada | load1 | `e2e/20` intra-gesto **max** (n intervalos) | intra p95 | `e2e/20` borda→desenho **p95** (n pares) | `e2e/20` resultado | `e2e/17` **p95** (n amostras) | `e2e/17` resultado |
|---|---|---|---|---|---|---|---|
| 1 | 0,40 | **82,9** (318) | 35,1 | 72,1 (15) | passed | **32,8** (87) | passed |
| 2 | 0,83 | **67,1** (320) | 41,5 | 70,3 (15) | passed | **32,8** (87) | passed |
| 3 | 0,72 | **66,8** (317) | 37,1 | 69,0 (15) | passed | **28,3** (87) | passed |
| 4 | 1,17 | **69,0** (319) | 37,4 | 250,8 (15) | passed | **32,8** (87) | passed |
| 5 | 1,23 | **82,8** (318) | 37,6 | 72,8 (15) | passed | **27,6** (87) | passed |

- **Teto do owner (`≤ 160`): 5/5**, com o máximo em **82,9 ms**.
- `e2e/20` p95 borda→desenho `≤ 400` com `n ≥ 10`: **5/5**. A rodada 4 deu 250,8, um *outlier* dentro do teto.
- `e2e/17` p95 `≤ 160` com `n ≥ 61`: **5/5**. A banda da F-7 (a mediana das 5 `≤ 49,4`) dá mediana **32,8**.
- **No `make verify`** (load1 0,96 no início e 2,64 no fim): intra max **81,7** (n=315), borda→desenho p95
  **100,8** (n=15), `e2e/17` p95 **21** (n=87).

## 3. F-B: atribuição, esparso contra denso na mesma rodada `[MEDIDO, n = 5 pares]`

| rodada | `history_page_apply_p50_ms` esparso | denso (`?e2eDenseSeries=1`) | razão | intra max, denso |
|---|---|---|---|---|
| 1 | 23,2 | 49,2 | **2,12×** | 100,0 |
| 2 | 22,0 | 49,1 | **2,23×** | 117,2 |
| 3 | 23,8 | 48,7 | **2,05×** | 116,6 |
| 4 | 22,3 | 48,3 | **2,17×** | 133,5 |
| 5 | 22,8 | 49,7 | **2,18×** | 114,8 |

**Passa em 5/5** (o critério é `≥ 1,4×`, e o bench dava 1,67×). `data-series-feed` confirmou o braço em cada
rodada (`"sparse"`/`"dense"`). É o conserto que move o custo da página.

⚠️ **O que F-B NÃO prova, dito para não ser lido a mais:** nesta carga (≤ 1,4), **o braço denso também fica abaixo
de 160** (100,0–133,5). A esta carga, o F-A verde sozinho não distingue o conserto do código antigo. O conserto
baixa o pior intervalo de 100–133,5 para 66,8–82,9 ms, o que dá **~50 ms de folga a mais**, e essa é a margem que
o `e2e` sob carga 5 consome (o desenho, §8). **A carga 5 não foi medida nesta janela** `[NÃO MEDIDO]`.

## 4. F-C: o poder do instrumento, com *busy-wait* de 80 ms no efeito PAGE `[MEDIDO, n = 5 pares]`

| rodada | intra max com busy80 | sem busy (esparso, mesma rodada) | **subida** | resultado do `e2e/20` com busy80 |
|---|---|---|---|---|
| 1 | 150,4 | 82,9 | **+67,5** | passed |
| 2 | 150,7 | 67,1 | **+83,6** | passed |
| 3 | 164,9 | 66,8 | **+98,1** | **failed** (teto intra) |
| 4 | 151,9 | 69,0 | **+82,9** | passed |
| 5 | 587,7 | 82,8 | **+504,9** | **failed** (teto intra) |

**Passa em 5/5** (o critério é `≥ +60 ms`). A métrica intra-gesto enxerga a tarefa da página, e por isso F-A mede
alguma coisa. Nas rodadas 3 e 5, o próprio spec reprova pelo teto: a mutação é pega pelo portão, e não só pelo
fato. ⚠️ A rodada 1 subiu **+67,5**, a 7,5 ms do limiar. O controle passa, mas com pouca sobra.

## 5. F-7: rAF durante o arrasto, dado real (braço B), com o controle negativo `[MEDIDO, n = 5 rodadas]`

| rodada | load1 | **p95_rAF** sem busy (n quadros) | "+1 quadro" (`≥ 25,0`)? | p95_rAF com busy20 (n) | o controle morde (`≥ 33`)? |
|---|---|---|---|---|---|
| 1 | 0,74 | **16,8** (102) | não | 33,3 (199) | sim |
| 2 | 1,15 | **16,8** (104) | não | 33,3 (199) | sim |
| 3 | 0,99 | **16,8** (110) | não | 33,3 (197) | sim |
| 4 | 1,36 | **16,8** (106) | não | 33,3 (199) | sim |
| 5 | 1,18 | **16,8** (103) | não | 33,3 (196) | sim |

- **"+1 quadro" em 0/5.** Regressão exigiria `≥ 2/5`. A régua da baseline era 16,7–16,8 em 10/10, e o número
  bate. **Sem regressão de quadro.**
- **O controle morde em 5/5**, com 33,3 contra o limiar de 33, ou seja **0,3 ms de sobra**. O p95 de rAF é
  quantizado em quadros (16,7 · 33,3), e 33,3 é exatamente o degrau de "+1 quadro". A sobra é pequena, mas o
  degrau é discreto: um valor de 33,3 não está perto de 33 por acaso. `[INFERRED: quantização em múltiplos de
  16,7 medida nos dois instrumentos, emenda F-7]`
- O braço A (stub) deu p95_rAF **16,7–16,8** sem busy (n=98–104; a rodada 4 teve **98 < 100** quadros) e também
  **16,7–16,8 com busy20**. O *busy* no `pointermove` **não morde no stub** `[NÃO SEI por quê; a emenda F-7
  define o julgamento e o controle só no braço B, dado real]`. O braço A fica registrado e não entra no veredito.

## 6. `make verify` completo

`E2E_API_PORT=8821 E2E_NEXT_PORT=4321 make verify` resultou em **rc=0, VERDE, 8 portões**: `lint-backend` (473
arquivos), `lint-frontend` (ESLint + `tsc --noEmit --strict`), `test-frontend` **1056 pass / 0 fail** (4
suítes), `test` **2728 passed**, cobertura **96,31%**, `boundaries` 7 kept / 0 broken, `regras` **0 bloqueios**
(76 avisos), `política` ok, `e2e` **64 passed** (`grep -c '✘'` no log dá **0**). Log bruto:
`/tmp/verify-wf_820bac26-f99-1-20260925T203609Z.log` (344K).

**Vermelhos conhecidos (`REGRAS` §2):** nenhum vermelho, nem dentro nem fora da lista. Os 4 da baseline passaram
nesta árvore.

**O que não podia regredir (desenho §4) e agora foi medido dentro do verify:** `e2e/16`, `e2e/21`, `e2e/22`
(`data-chart-mount-count == 1`), `e2e/24` (legenda == API) e `e2e/25` (identidade de pixel) passaram, junto com o
resto dos 64.

## 7. O que ficou pendente (nomeado)

- **F-A sob carga ~5**: `[NÃO MEDIDO]`. A janela era de carga baixa por construção. O único dado com carga
  mais alta é o `make verify` (load1 2,64 no fim), que deu intra max 81,7.
- Validação ao vivo por `mcp__playwright__*`: não há servidor Playwright MCP nesta instalação, e ela não rodou.
  O que protege é o `e2e/20` versionado.
- `status` no `tasks.toml`, `gate-record` e `resolve` **não foram tocados**. São atos do orquestrador.

## 8. Doc delta

- `gates/T-01.10-builder.md` §6: **atualizado**, com um ponteiro para este laudo (os itens "não medido" estão agora
  medidos).
- `docs/INDEX.md`: **linha acrescentada**.
- `ADR-044`: **sem mudança**, porque o teto e a emenda F-7 foram aplicados como estavam escritos. O achado da
  seção 5 (o controle não morde no braço stub) é um dado para o dono da F-7 e não reabre nada aqui.
- `STITCH_CONTEXT.md` / `DESIGN_SYSTEM.md`: **sem mudança**, porque nenhum código mudou nesta etapa.
