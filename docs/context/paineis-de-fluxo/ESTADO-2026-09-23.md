# Estado de `paineis-de-fluxo` em 2026-09-23, ao parar

**Ledger:** `BUILD_AUTHORIZED` (`harness pipeline state paineis-de-fluxo`). **Nenhuma fase tem gate-record.**

## Onde está o trabalho

- **`master`** em `ba21f07`: PRD-009, SPEC-009, ADR-044/045, plano e `tasks.toml` (39 tasks), tudo commitado.
- **`wave/paineis-f01`** (W1, fase 01), **local, sem push, sem PR**. Worktree em `.claude/worktrees/wave-paineis-f01`.

| task | estado | commit na wave |
|---|---|---|
| T-01.0 spike | PASS: `S-1` mantido. Só o relatório entrou na wave; o código está na branch `spike/T-01.0-panes-nativos` (`e867e16`) | `70b17d3` |
| T-01.1 baseline de latência | feita | merge `f5b4c72` + achados `599a620` |
| T-01.3 geometria das bandas | feita | merge `64e5edb` |
| T-01.2 pane registry | feita | merge `7f524ad` |
| emenda `/architect` | ADR-044 D3′ (invariante iii por kind) + F-7 (critério de +1 quadro) | `1f5e448` |

`tasks.toml` continua com `status = "todo"` em todas as tasks. O `harness resolve` é atômico por fase, então elas
são resolvidas juntas no fecho da fase 01.

## ⛔ O que bloqueia o próximo lote — DIAGNOSTICADO (`gates/DIAG-e2e-master.md`, merge `777ad57`)

As 4 falhas de e2e **reprovam no master puro**. Nenhuma é timing, relógio ou dado que mudou. **Três são regressões
reais de produção da fase 05 de `candle-real-e-eixo-unico`**, que foi fechada `DONE` hoje com QA APPROVED:

| falha | veredito | origem | defeito visível |
|---|---|---|---|
| `18:107`, `18:153` | **regressão** (3/3 isolado, 2/2 com dado real) | `718cb1a` (T-05.2); o pai `0c9cb43` é verde | trocar para **4h** muda a URL, mas a tela continua com as velas de **1m**. Causa provável: `use-history-pager.ts:199-200` lê o seed uma vez só e `<SymbolClient>` não tem `key` |
| `16:204` | **regressão determinística** | `48d47e5` (T-05-FIX, o guard virou contador) | o painel de Preço é reescrito no meio do arrasto e pula 52 barras para trás |
| `20:383` | **o teste infla o número** (conta a pausa entre gestos) **e esconde um defeito real** | o spec nunca foi verde | quando a página chega, o remonte descarta o resto do arrasto, em 24/24 gestos |

**O laudo da fase 05, que dava 16/18 como herdados das fases 02/03, não se sustenta** `[MEDIDO: 16 passa em ef608ec; 16 e 18 passam em 08e5e75]`.

**Consequência para esta feature:** o `make verify` não fica verde enquanto esses defeitos não forem
corrigidos, e a W1 não vira PR. A correção é trabalho **de produção, fora do escopo desta feature**. Ela precisa de
**decisão do owner** sobre onde entra: feature de correção nova, ou fase de fix. Enquanto isso, o `T-01.10` não
pode ler o e2e/20 como latência.

## Próximo passo, na ordem

1. **Owner decide onde entra a correção das 3 regressões da fase 05** (tabela acima). Donos técnicos, pelo DIAG: `frontend-builder`, com `frontend-architect` no desenho (18: key × reset do pager; 16: descartar eco atrasado; 20: preservar arrasto no remonte e corrigir o instrumento).
2. **Lote 4:** `T-01.4` + `T-01.5`. A `T-01.5` edita `SymbolClient.tsx` e precisa aplicar a ADR-044 D3′ (três ajustes
   no teste de `pane-registry`, ver o `1f5e448`) e o achado 1 de `handoff/T-01.0-achados-para-a-F1.md`
   (`getHTMLElement` é null até o 1º frame).
3. Seguir `PLANO-DE-PARALELISMO.md` §3 lote a lote. A `03a` (coletor de OI) começa no lote 5.

## Como despachar builder (lições deste dia)

- Na worktree nova, **`git merge --ff-only wave/paineis-f01`**. O `git reset --hard` é negado pela permissão.
- e2e em paralelo precisa de portas próprias (`E2E_API_PORT`/`E2E_NEXT_PORT`), senão a 8811 colide.
- Em worktree nova, `backend/.venv` e `data/` entram por `cp -al`, senão o `make verify` dá INDETERMINADO.
- As worktrees dos agentes já integrados (`agent-aa468…`, `agent-acadc…`, `agent-ac860…`, `agent-af40b…`) estão
  limpas e com os commits preservados. A remoção automática foi negada pela permissão; remover na mão é seguro.
