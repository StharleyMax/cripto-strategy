# Handoff `infra` — `T-02.7`: CVD em produção, e por que o builder NÃO o fez

## 1. Por que isto não foi executado desta worktree — e a recusa é a lição de `T-01.10`

`gates/T-01.10-infra.md` registra o defeito: o projeto compose `deploy` ficou com
`com.docker.compose.project.working_dir` apontando para **a worktree de um agente**
`[MEDIDO 2026-09-11T14:12Z: `docker inspect deploy-api-1` → o diretório não existe]`. A partir
dali `docker compose … ps/logs/up` a partir do checkout canônico **não encontrava o projeto**.

Buildar as imagens `api`/`writer`/`collector` a partir desta worktree reproduziria exatamente
isso, porque o contexto de build (`../backend`) é resolvido contra o `--project-directory`.
⇒ **O deploy acontece do checkout canônico, DEPOIS do merge deste branch.** Não é cautela
genérica: é o modo de falha já medido neste projeto.

## 2. Os comandos, literais, do checkout canônico (`$R` = raiz canônica)

```bash
R=/home/stharley/Documentos/projects/cripto-strategy
docker compose --env-file "$R/.env" --project-directory "$R/deploy" \
  -f "$R/deploy/compose.yml" -f "$R/deploy/compose.local.yml" build api writer collector
docker compose --env-file "$R/.env" --project-directory "$R/deploy" \
  -f "$R/deploy/compose.yml" -f "$R/deploy/compose.local.yml" up -d --no-deps --force-recreate \
  api writer collector
```

⛔ **NENHUM serviço novo.** O dado de CVD vem do coletor de klines que a fase `01` já subiu; se
esta task precisar de um serviço a mais, o `DoD 7` (zero rede nova) foi violado em algum lugar.

## 3. Os `series_key_id` a medir — derivados, nunca copiados à mão

```bash
cd "$R/backend" && .venv/bin/python -c "
from src.modules.sentimento.domain.cvd_source_catalog import build_kline_takerbuy_entry
from src.modules.sentimento.domain.instrument import base_asset
for s in ('BTCUSDT','ETHUSDT','LINKUSDT','SOLUSDT'):
    print(s, build_kline_takerbuy_entry(s, unit=base_asset(s)).key.series_key_id())"
```

## 4. `DoD-1` — `count(*)` em `md.series`. **Hoje: 0**

```bash
docker exec deploy-postgres-1 psql -U cripto_strategy -d cripto_strategy -At -F'|' -c \
  "select series_key_id, symbol, count(*) from md.series
   where series_key_id in ('<os 4 ids acima>') group by 1,2 order by 2;"
```

Baseline antes do deploy, para o gate poder mostrar a diferença
`[MEDIDO 2026-09-12: `select source, symbol, count(*) from md.series group by 1,2` →
`/fapi/v1/klines` 31.329 linhas por símbolo, `/fapi/v1/premiumIndex` 8.726; **nenhuma de
`cvd_source`**]`.

## 5. `DoD-4` — `n_written > 0` de um run FECHADO

```bash
docker exec deploy-postgres-1 psql -U cripto_strategy -d cripto_strategy -At -F'|' -c \
  "select run_id, endpoint, n_returned, n_written, verdict from md.ingest_run
   where endpoint='/fapi/v1/klines' order by ended_at desc limit 5;"
```

⚠️ **`n_written` conta LINHAS, e a partir desta fase um bar vale DUAS.** `n_published` (log do
coletor) continua contando **bars**, de propósito: a subtração `n_returned - n_published` é o
tamanho do corte anti-lookahead (`RS-3.4`) e deixaria de significar isso se contasse linhas.
`_publish_klines_page` devolve `len({row.bucket_end …})` por essa razão, e o teste que fixa
ambos é `test_the_anti_lookahead_cut_stays_visible_in_the_gap_between_returned_and_published`.

## 6. `DoD-2` — `n_points > 0` na rota servida

```bash
curl -s "http://127.0.0.1:8000/api/v1/series-history?series_key_id=<id_cvd_BTCUSDT>\
&symbol=BTCUSDT&interval=1m&window_start_ms=<agora-2h>&window_end_ms=<agora>" \
  | python3 -c "import json,sys; b=json.load(sys.stdin); \
print(sum(1 for r in b['rows'] if r['value'] is not None), 'de', len(b['rows']))"
```

⛔ **Espere ~30 min de coleta AO VIVO antes de exigir `N ≥ 30`.** O backfill de 7 dias é
invisível ao `as_of` (`handoff/ACHADO-BACKFILL-INVISIVEL-AO-AS-OF.md`), defeito **pré-existente**
da série de volume e fora do escopo desta fase por `D15`/`D17`. O mecanismo já foi medido em
regime ao vivo: `n_points = 298` de 300 bars, 4 símbolos `[MEDIDO 2026-09-12:
`scripts/cvd-klines-falsifier/measure_cvd_vertical.py`]`.

## 7. O que NÃO fazer

⛔ Não semear `md.series` com dado sintético para adiantar `DoD-1`/`DoD-3`. Já vazou para a tela
do owner uma vez.
