# Handoff da fase `06` — `T-06.1` e `T-06.2`

**Leia antes:** [`tasks_review_06.md`](../tasks_review_06.md) (narrativa e alternativas recusadas) ·
[`plano 06`](../../../plans/SPEC-007-cinco-metricas-do-core/06_correcao_de_instrumentacao.md) ·
`ADR-035` §D1 e §D3, **emendas de 2026-09-11** · `DECISOES-OWNER.md` §D9 e §D12.
O `handoff_to_builder.md` da feature continua valendo integralmente (commit, hooks, `make verify`,
worktree) — este documento só acrescenta o que é próprio desta fase.

## Lote (teto de 2, `D8`)

**Lote único: `T-06.1` + `T-06.2`.** Ambas com `depends_on = []`, backend, **zero colisão de
arquivo** entre si:

| | `T-06.1` | `T-06.2` |
|---|---|---|
| produção | `infra/ingest_health_cli.py`, `infra/single_writer_cli.py`, `infra/collectors_cli.py` | `use_cases/collector_status.py`, `domain/collector_status.py`, `infra/postgres_ingest_record_store.py` (leitura), possivelmente `domain/ingest_record.py` |
| teste | `tests/sentimento/test_ingest_health_extra_rendering.py` (+ o que pina o formato) | `tests/sentimento/test_collector_status_query.py`, `tests/api/test_collector_status_route.py` |

⚠️ **`T-06.1` toca `single_writer_cli.py` e `collectors_cli.py`.** A proibição do lote `1B` (que
valia para `T-01.5`) **caiu**: o arquivo está livre desde `696707c` e `T-01.3`/`T-01.4` já
mergearam.

## As três coisas que reprovam, e nenhuma é estilo

1. **`build_service_stdout_handler` fora de `SHARED_HANDLER_BUILDERS`** (`T-06.1`). O universo da
   varredura AST é descoberto por nome de builder importado
   (`test_ingest_health_extra_rendering.py:266`); um módulo que importe só o nome novo ficaria fora
   da pergunta, com o teste de universo (`==`) **verde sobre universo encolhido**.
2. **Tocar `INGEST_HEALTH_RUN_COLUMNS` ou fazer `writer_accounted_at` entrar em
   `canonical_projection()`** (`T-06.2`). Muda o `sha256` de `ADR-008/DoD-2`. **Aborta a task** —
   não é uma discussão de desenho, é `RS-2`/`NG-6`/`CLAUDE.md` linha 11.
3. **Uma consulta a `store.writer_accounted_at(run_id)` por run** (`T-06.2`): `n = 2.006` runs na
   janela `[MEDIDO 2026-09-11T11:26Z]` ⇒ 2.006 idas ao banco por request. Recusada de saída.

## O comando que mede as duas, antes e depois

```bash
docker exec deploy-postgres-1 psql -U cripto_strategy -d cripto_strategy -At -F'|' -c \
"select endpoint, count(*), count(*) filter (where writer_accounted_at is not null),
        count(*) filter (where writer_accounted_at is not null and n_written > 0),
        round(100.0*sum(n_written)/nullif(sum(n_expected),0),2)
   from md.ingest_run where ended_at::timestamptz > now() - interval '24 hours' group by 1;"
```

Baseline `[MEDIDO 2026-09-11T11:26Z, n = 2.006 runs]`: klines `572 | 571 | 571` (fórmula de hoje
**90,45**), premiumIndex `1.431 | 582 | 582` (**0,36**), forceOrder `3 | 0 | 0` (**—**).

⚠️ **O `90,45` é UM run** — o backfill (`run_id 932c37fc…`, `n_expected 40.320`) **sai da janela em
`2026-09-12T01:40:39Z`** e o número cai para `34,21`. Se você medir depois desse instante, o
"antes" muda sozinho: **registre o timestamp junto do número**, sempre.

## O que NÃO fazer nesta fase

Não tocar `n_expected`/`collector_run_mapping.py`. Não versionar nem acrescentar campo a
`/api/v1/collector-status` (`D7`). Não renomear evento de log em português (`NG-5`). Não consertar
`frontend/src/features/s1-console/ingest-health-query.ts:709` — achado escalado, `PENDENCIAS.md`.
Não rodar `harness pipeline approve`/`advance`: `advance DONE` é gate do **owner**.

## Depois do merge

`T-01.10` depende das duas (aresta acrescentada em 2026-09-11, `tasks_review_06.md` §4). Ela é a
única task que paga produção na fatia `01` — **só a despache quando as duas estiverem mergeadas e
os containers reconstruídos**, senão a medição de `RS-1.a` terá de ser refeita.
