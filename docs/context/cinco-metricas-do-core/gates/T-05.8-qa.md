# QA GATE — `T-05.8` (fase `05`, componente `sentimento`) · PR #224 · `3e95ba9` (código em `071f7be`)

**Veredito: `APPROVED`.** Base medida: `e4b91cb` (merge-base com `master`). Worktree
`agent-a64a1171ff04f69ee`. ⛔ Nada de produção foi editado por este gate; ⛔ nada semeado no
Postgres (só `select`); ⛔ nenhum deploy; `PYTHONDONTWRITEBYTECODE=1` e `__pycache__` purgado
antes de cada medição.

## 1. As duas coortes entram separadas e nenhuma linha se perde — RECONTADO por mim

Não aceitei o número do builder: carreguei o módulo de `e4b91cb` (`git show`) LADO A LADO com o
da PR, contra os mesmos `domain/`, e comparei CONJUNTOS de `series_key_id`.

```
.venv/bin/python <scratchpad>/recount.py
BTCUSDT before=13 after=15   ·  PILOT before=52 after=60
LOST btc = []   ·   LOST pilot = []
PREFIX PRESERVED btc: True
ADDED idx=13 sum_liquidation long  USD quote FLOW 1m native_grid_ms=60000 id=23e4332307dc…
ADDED idx=14 sum_liquidation short USD quote FLOW 1m native_grid_ms=60000 id=bc0b8a78e16b…
ADDED pilot = 8, distinct = 8, DUPLICATE ids in new pilot = 0
```
`[MEDIDO 2026-09-13, n=15 linhas BTCUSDT / n=60 linhas do piloto]`

⚠️ **`PREFIX PRESERVED pilot: False`, e isso NÃO é regressão** — é consequência estrutural de o
piloto ser a concatenação de 4 catálogos por instrumento (`13→15` cada). A propriedade que vale
ali é outra, e ela É verdadeira: **`OLD IS SUBSEQUENCE OF NEW: True`** — nenhuma linha antiga
mudou de ordem RELATIVA, e as 8 novas são `sum_liquidation` no fim de cada bloco de instrumento
(`[('BTCUSDT','sum_liquidation','long'), ('BTCUSDT',…,'short'), ('ETHUSDT',…), …]`). Toda fase
anterior que acrescentou linha (`T-01.6`, `T-02.4`, `T-04.4`) moveu o índice PLANO do piloto da
mesma maneira; `RS-1` é preservado no eixo em que ele é afirmado.

**A premissa da task, confirmada contra produção (leitura, API implantada = `master` SEM a PR):**
```
curl .../api/v1/series-catalog        -> n_entries=52, sum_liquidation rows=0
curl .../api/v1/series-history?series_key_id=23e4332307dc…&…&bar_policy=final_only
  -> status=422 {"detail":"series_key_id '23e433…' has no row in the catalog"}
```
`[MEDIDO 2026-09-13 contra deploy-api-1]` — é exatamente o `422 UnknownSeriesKeyIdError` que a
PR remove, e a prova de que o registro estava faltando de fato, não só na leitura do plano.

## 2. O contrato esparso — a projeção NÃO preenche, e o controle negativo REALMENTE distingue

Suíte do recorte, antes de eu escrever qualquer coisa:
```
PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest \
  tests/sentimento/test_series_history_sparse_liquidation.py \
  tests/sentimento/test_series_catalog_use_case.py \
  tests/api/test_series_catalog_route.py \
  tests/sentimento/test_native_grid_ms_pairs.py -p no:cacheprovider
-> 53 passed in 1.49s     (6 no arquivo novo)
```

**Verde não prova nada até uma mutação reprovar. 5 mutações, 5 reprovas — e nenhuma tocou um byte
versionado:** cada uma é um plugin `-p` carregado ANTES da coleta (`PYTHONPATH=<scratchpad>`), que
remenda o objeto em memória; `git status` do worktree continua limpo.

| # | mutação | resultado |
|---|---------|-----------|
| `M1` | registro de `T-05.8` removido do catálogo servido | **18 failed, 35 passed** (inclui `…no_longer_refuses_either_liquidation_id[long]`/`[short]`, `…the_ones_the_collector_writes_under`, o teste HTTP da rota) |
| `M2` | `CARRY_FORWARD_BY_NATURE[FLOW] = True` (a projeção PREENCHE) | **4 failed, 2 passed** — as 2 que passam são exatamente o controle negativo |
| `M3` | `CARRY_FORWARD_BY_NATURE[STOCK] = False` (o controle deixa de preencher) | **2 failed** — `…holes_are_filled_for_a_carry_forward_nature[long]`/`[short]`, com `assert 4 > 4`; os testes de `FLOW` continuam verdes |
| `M4` | `unit` das linhas de PREÇO vira `USD` | `test_quote_denominated_rows_are_untouched_by_the_derivation` **FAILED** |
| `M5` | `unit` das linhas de LIQUIDAÇÃO vira `USDT` | mesmo teste **FAILED** |

⚠️ **`M3` é a mutação que o despacho pediu e que o relatório do builder não tinha:** sem ela,
"o controle negativo preenche" seria afirmação, não medida. Com ela está provado que o controle
**pode falhar pelo motivo certo** e que `FLOW` e `STOCK` são distinguidos pelo caminho de leitura
real — `M2` e `M3` reprovam LADOS OPOSTOS do mesmo par, que é a definição de discriminação.

O arquivo novo lê a entrada **SERVIDA** (`list_series_catalog(...).entry_for_id(...)`, com
`assert entry is not None`), então `M1` o derruba junto: ele não pode virar teste de fixture local.
A asserção de zero é sobre VALOR (`Decimal(row.value) != 0`), não sobre a grafia `"0"`.

## 3. Somar as coortes apagaria o sinal — elas são distintas ponta a ponta

```
reader ids {'long': '23e4332307dc', 'short': 'bc0b8a78e16b'}  distinct: True   both served: True
writer usa coinalyze_liquidation_key: True   |   writer soma coortes: False
grep -rn 'sum_liquidation' backend/src --include='*.py' | grep -v liquidation_catalog.py
  -> 2 linhas, AMBAS docstring de series_catalog.py (nenhum código)
grep -rnE 'cohort' use_cases/series_history.py domain/series_history_report.py -> rc=1, 0 linhas
```
`[MEDIDO 2026-09-13]` — o caminho de leitura endereça por `series_key_id` e não conhece a palavra
`cohort`: não existe ponto onde agregar. ESCRITOR (`collector_series_mapping:897`) e LEITOR passam
pelo MESMO `coinalyze_liquidation_key`.

## 4. A armadilha do `unit` — a asserção foi PARTIDA, não relaxada

`test_quote_denominated_rows_are_untouched_by_the_derivation` trocou
`assert {unit for quote_rows} == {"USDT"}` por **quatro** asserções: `len(quote_rows) == 4`,
`len(price_rows) == 2`, `{unit for price_rows} == {"USDT"}`, `{metric for liquidation_rows} ==
{LIQUIDATION_METRIC}` e `{unit for liquidation_rows} == {"USD"}`. **`M4` e `M5` provam que ela
continua mordendo nos DOIS lados da partição** — relaxar teria deixado `M4` passar.

## 5. `make verify` — 8 portões, VERDE

```
PYTHONDONTWRITEBYTECODE=1 make verify   (worktree, node_modules REAL — nunca symlink)
[OK] lint-backend  446 source files       [OK] test        2375 passed · 96.60%
[OK] lint-frontend ESLint + tsc --strict  [OK] boundaries  7 kept, 0 broken
[OK] test-frontend 592 pass, 0 fail       [OK] regras      0 bloqueio(s), 72 aviso(s)
[OK] política                             [OK] e2e         27 passed (34.4s)
veredito: VERDE — 8 portões mediram e passaram
log: /tmp/verify-agent-a64a1171ff04f69ee-20260913T001632Z.log
```
Piso por camada: `domain 99.8% (meta 90%)` · `use_cases 99.7% (meta 80%)` · `infra 92.9% (meta
70%)`. O arquivo de produção alterado: `use_cases/series_catalog.py` **100%** (39 linhas, 0 miss).

8 regras bloqueantes em vigor (`harness rules list --severity block`) avaliadas pelo portão
`regras`: **0 bloqueio**. Por arquivo alterado (`harness rules --mode file --path …`): único
apontamento é `[AVISO] core.module-docstring-single-line` em `series_catalog.py:1` — **warn, e
pré-existente**: o docstring do módulo já era multilinha em `e4b91cb`.

⚠️ **A máquina NÃO estava ociosa, e isso está medido, não suposto:** outra worktree
(`agent-a17f60de3e2f1fe14`, PID 3091891) rodava a própria suíte com cobertura durante toda a minha
rodada (`ps -eo pid,etime,cmd | grep pytest`). Isso afeta o RELÓGIO; `rc` e contagens são os
mesmos, e o portão mediu os 8.

## 6. `DoD-2` da FASE — o limite é real, e o motivo MUDOU desde o relatório do builder

O builder declarou `md.series` com **ZERO linha** para os dois ids. **Não é mais verdade** — o
coletor rodou entre o build e este gate:

```
docker exec deploy-postgres-1 psql … -At -c "select left(series_key_id,16), count(*) … group by 1"
  23e4332307dc408a | 135        bc0b8a78e16b36aa | 136
select source, count(*) from md.series group by 1 -> /v1/liquidation-history | 815
```
`[MEDIDO 2026-09-13, n=271 linhas nos dois ids; n=5 fontes]`

⚠️ **E mesmo assim `n_points` seria 0 — pelo motivo que o TERCEIRO teste do arquivo novo já pina:**
`min(available_at - bucket_end) = 735.863 ms`, mediana `3.855.859 ms`, e
**`count(*) filter (where available_at - bucket_end < 60000) = 0` em 271 de 271 linhas**. Para
`FLOW`, `as_of` recusa o bucket com `age_ms >= 60.000`. ⇒ **o bloqueio do `DoD-2` não é catálogo
faltando** (catálogo agora existe, e a rota deixa de responder `422`) — é **cauda de publicação**,
que é dono da fase `05`/coletor, exatamente a classe de
`ACHADO-SERIES-HISTORY-SEM-PONTO.md`. Não reprova esta task, e agora está medido em produção em vez
de previsto.

## 7. Observação de merge (não é defeito de teste)

`gh pr view 224` → `mergeable: CONFLICTING`. `git merge-tree` sobre a merge-base mostra **1 único
conflito, e ele é `docs/INDEX.md`** (as duas pontas acrescentaram a última linha do registro
append-only); **os 4 arquivos de código/teste fundem limpos**. Resolução é mecânica: manter as duas
linhas. Quem faz o rebase é o merger da wave, não este gate.

## Veredito

`APPROVED`. DoD de `T-05.8` (plano `05` item 5.4, metade backend) satisfeito item a item:
registro das DUAS coortes no catálogo servido (13→15, 52→60, `LOST=[]`), contrato esparso provado
no `build_series_history_report` REAL com controle negativo falsificável, e `make verify` verde nos
8 portões. Nenhuma anomalia: as 8 checagens produziram veredito.
