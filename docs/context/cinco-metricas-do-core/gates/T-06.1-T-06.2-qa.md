# QA Gate — fase `06` (`T-06.1` + `T-06.2`), feature `cinco-metricas-do-core`

**Branch:** `task/cinco-metricas-do-core-f06-instrumentacao` · **commit auditado:** `fd54ce7` ·
**worktree:** `agent-a9544fad66dc36b98` · **data:** `2026-09-11`

> ⚠️ Os normativos desta fase (`tasks_review_06.md`, `handoff/T-06.md`, as **emendas de
> `2026-09-11` do `ADR-035`**, `plans/SPEC-007-cinco-metricas-do-core/06_*.md` e os blocos
> `T-06.1`/`T-06.2` do `tasks.toml`) continuam **não versionados** — foram lidos do checkout
> principal. Não é achado deste gate (o builder já o declarou), mas **é pendência de quem
> mergear**: o plano que este veredito cita não existe no histórico.

---

## 1 · A ordem: rodei o que existia ANTES de escrever qualquer teste

```
backend/.venv/bin/python -m pytest \
  backend/tests/sentimento/test_ingest_health_extra_rendering.py \
  backend/tests/sentimento/test_collector_status_query.py \
  backend/tests/sentimento/test_ingest_health_contract_guards.py \
  backend/tests/api/test_collector_status_route.py -q
# -> 58 passed (20 + 21 + 13 + 4, `--collect-only -q`) [MEDIDO 2026-09-11]
backend/.venv/bin/python -m pytest \
  backend/tests/sentimento/test_postgres_ingest_record_store_credits_the_run.py -q
# -> 10 passed, contra um Postgres REAL que o proprio teste sobe [MEDIDO 2026-09-11]
```

## 2 · `T-06.1` — replantei M2 e M4, e os dois MORREM

Harness próprio (`planta → roda → restaura`, com `count(old) == 1` como pré-condição):
`scratchpad/mutate.py`. **Não reusei o harness do builder** — um mutante conferido pelo mesmo
script que o plantou mede o script, não a guarda.

| mutante | o que planta | rc | killers |
|---|---|---:|---|
| **M2** | só `build_service_stdout_handler` FORA de `SHARED_HANDLER_BUILDERS` (a letra do §2.2) | `1` | **4** |
| **M2b** | `SERVICE_HANDLER_BUILDERS` inteiro fora de `SHARED_HANDLER_BUILDERS` (o M2 do builder) | `1` | **4** |
| **M4a** | `collectors_cli` mantém o import e **chama** `build_stdout_handler` | `1` | 1 |
| **M4b** | idem em `single_writer_cli` | `1` | 1 |
| **M4c** | o nome referenciado sem ser chamado (`_unused = build_service_stdout_handler`) | `1` | 1 |
| **M4d** | `build_service_stdout_handler()` instalado num logger **descartável**, e o logger do módulo recebe o de projeção | **`0`** | **0 — SOBREVIVEU** |

**A pergunta do despacho, respondida com número:** o teste de universo **NÃO** fica verde sobre
universo encolhido. Com `build_service_stdout_handler` fora de `SHARED_HANDLER_BUILDERS`, os dois
processos de serviço — que importam **só** o builder novo — caem fora da varredura e
`test_the_universe_of_importers_is_the_one_this_guard_believes_it_is` reprova junto com outros 3.
A defesa é estrutural, não incidental: `SHARED_HANDLER_BUILDERS` é definido como união com
`SERVICE_HANDLER_BUILDERS`, então um builder de serviço novo entra no universo **por construção**.

**M4 (o que sobreviveu uma vez no gate do builder) morre nas 3 formas que plantei** — o conserto
`import E chamada` resolve; mas ele **não** cobre *onde o handler foi parar* (M4d). Fechei essa
meia-guarda **com teste**, não com prosa: `test_every_handler_a_service_process_installs_is_built_
by_a_service_builder` exige que todo `<logger>.addHandler(<builder>())` de um processo de serviço
venha de `SERVICE_HANDLER_BUILDERS`, e que a lista não seja vazia (senão passa vacuamente).
**Replantado depois:** M4d → `rc=1`, 1 killer; M4a → `rc=1`, 2 killers.

**`CA-F6-1`** `grep -rl 'build_service_stdout_handler' backend/src --include='*.py' | wc -l` → **3**.
**`CA-F6-3`**, medido por fora dos testes do diff (script próprio): com `extra={"n_accepted":3,
"n_rejected":0}`, o handler de **projeção** devolve `'{"run_id":"a",…}\n'` — **byte-idêntico** ao
`logging.Formatter` puro — e o de serviço devolve a mesma linha `+ ' n_accepted=3 n_rejected=0'`.
`type(build_stdout_handler().formatter).__name__` → `Formatter`; o de serviço →
`ExtraRenderingFormatter`.
**Confirmado e não assumido** (a nota da task): `clock_skew_tolerance_cli:40-42` define o próprio
`build_stdout_handler` sobre `build_stream_handler` ⇒ **herda o formatador puro**, e fica fora do
conjunto de serviço porque o nome que ele chama não está em `SERVICE_HANDLER_BUILDERS`.

## 3 · `T-06.2` — a fórmula, o contrato e o denominador zero

**`n_expected` não é lido:** `grep -n 'n_expected' use_cases/collector_status.py` → **4
ocorrências, todas em comentário** (`:120,121,137,138`), nenhuma em expressão. O mutante **N3**
(volta da fórmula antiga) morre com **6** killers.

**`ADR-008/DoD-2` intacto, medido nos DOIS commits** (worktree separada em `9a05a60`, mesmo run
fixo, mesmo script):

```
9a05a60 -> columns: 15  fingerprint: c1fc924acb8342f48094d71fc60ecabcdbb629e50f395669b1ce09d59e1c4068
fd54ce7 -> columns: 15  fingerprint: c1fc924acb8342f48094d71fc60ecabcdbb629e50f395669b1ce09d59e1c4068
```

`len(INGEST_HEALTH_RUN_COLUMNS) == 15`, `'writer_accounted_at' in INGEST_HEALTH_RUN_COLUMNS` →
`False`, `len(fields(IngestRun)) == 17`. O mutante **N4** (o campo entra na tupla) morre.

| mutante | o que planta | rc | killers |
|---|---|---:|---|
| **N1** | run **aberto** contado como fechado | `1` | 3 (inclui o teste de rota sobre HTTP real) |
| **N2** | `status_detail` volta ao `null` mudo | `1` | 3 |
| **N3** | volta `Σ n_written / Σ n_expected` | `1` | 6 |
| **N4** | `writer_accounted_at` entra em `INGEST_HEALTH_RUN_COLUMNS` | `1` | 1 |
| **N6** (meu) | `n_written > 0` afrouxado para `>= 0` | `1` | 3 |
| **N5** (meu) | `_SELECT_RUNS` devolve `NULL AS writer_accounted_at` | **`0`** | **0 — SOBREVIVEU** |
| **N7** (meu) | a 17ª coluna lida **fora de posição** | `1` | 3 *(depois do teste novo; ver abaixo)* |

### ⛔ O achado deste gate — `N5`, e ele decide se a fase funciona em produção

O **único** caminho de produção que leva *"fechado"* até `uptime_percent` é
`PostgresIngestRecordStore.runs()` → `IngestRun.writer_accounted_at`. Até este gate **nada o
exercitava**: `store.writer_accounted_at(run_id)` é outra instrução, o SQLite **não tem a coluna**,
e todo teste de `collector_status` usa fonte falsa construída à mão. Com
`NULL AS writer_accounted_at` no `_SELECT_RUNS`, **os 4 arquivos de teste do escopo e o arquivo de
Postgres real continuaram verdes** (`rc=0`, 0 killers) — e em produção esse mutante lê **todo run
como aberto**, serve `uptimePercent: null` para **todo** coletor e entrega exatamente o oposto da
correção, com o portão verde.

**Fechado com teste, sem tocar produção** (`test_postgres_ingest_record_store_credits_the_run.py`,
mesmo container real, mesmas fixtures):
`test_the_writer_stamp_reaches_the_domain_object_the_uptime_formula_reads` e
`test_uptime_over_a_real_postgres_counts_the_closed_run_and_ignores_the_open_one`.
Replantados depois: **N5 → `rc=1`, 2 killers**; **N7 (coluna fora de posição) → `rc=1`, 3 killers**.

## 4 · `CA-F6-4` e `CA-F6-5` contra a stack viva — **só leitura, nada semeado**

```bash
docker exec deploy-postgres-1 psql -U cripto_strategy -d cripto_strategy -At -F'|' -c \
"select endpoint, count(*), count(*) filter (where writer_accounted_at is not null),
        count(*) filter (where writer_accounted_at is not null and n_written > 0),
        round(100.0*sum(n_written)/nullif(sum(n_expected),0),2),
        round(100.0*count(*) filter (where writer_accounted_at is not null and n_written > 0)
              /nullif(count(*) filter (where writer_accounted_at is not null),0),2)
   from md.ingest_run where ended_at::timestamptz > now() - interval '24 hours' group by 1;"
```

`[MEDIDO 2026-09-11T12:55:11Z, n = 2.093 runs na janela de 24 h; SELECT apenas]`:

| endpoint | runs | fechados | fechados com linha | fórmula ANTIGA | fórmula NOVA |
|---|---:|---:|---:|---:|---:|
| `/fapi/v1/klines` | 659 | 658 | 658 | **89,23** | **100,00** |
| `/fapi/v1/premiumIndex` | 1.431 | 670 | 670 | **0,42** | **100,00** |
| `…forceOrder` | 3 | 0 | 0 | — | **null** |

⇒ **os números do builder (89,67/626 e 0,40/637, `12:22Z`) CONFIRMADOS no veredito e divergentes
no dígito**, exatamente como ele declarou: a janela desliza. O que importa é invariante nas duas
medições — `100,00` contra `89,23`/`0,42`.

**A cláusula de "morde" do `CA-F6-4`, testada:** excluindo o run de backfill (`n_expected < 1000`),
a fórmula **antiga** de `klines` desaba de `89,23` para **34,17**, e a **nova** fica em **100,00**
`[MEDIDO 2026-09-11T12:58Z, n=674 fechados]`. O número novo **não depende** do run que sai da
janela em `2026-09-12T01:40:39Z`.

**O "DEPOIS" servido pelo código novo, sem reconstruir container:** exportei as linhas vivas
(`json_agg`, leitura) e rodei `collector_status_query` **do commit auditado** sobre elas:

```
/fapi/v1/klines        uptimePercent=100.0  n_runs_in_window=660   statusDetail=None
/fapi/v1/premiumIndex  uptimePercent=100.0  n_runs_in_window=1431  statusDetail=None
/stream?…forceOrder    uptimePercent=None   n_runs_in_window=3
                       statusDetail='3 run(s) na janela, nenhum fechado pelo escritor: uptime não medível.'
```

⚠️ **A rota de produção ainda serve o código ANTIGO** — `curl localhost:8000/api/v1/collector-status`
devolve `89.23 / 0.42` e `statusDetail: null` **inclusive no `forceOrder`** (o `null` mudo). Isso
**não** é defeito desta fase: é o rebuild de container que a emenda declarou como custo e que é
trabalho de `T-01.10`. ⇒ `CA-F6-5` está provado sobre **HTTP real de loopback** dentro da suíte
(`test_the_two_null_uptimes_are_told_apart_on_the_wire`, `uvicorn` em porta real) e sobre os dados
vivos pela via acima; **`[NÃO MEDIDO]` o valor servido pela rota em produção**, por construção.

## 5 · Portões

```
HARNESS_MECHANISM=… make verify   # saida bruta em scratchpad/verify2.log
```

`[MEDIDO 2026-09-11T13:11:49Z, log bruto `/tmp/verify-agent-a9544fad66dc36b98-20260911T131149Z.log`]`:

| portão | resultado |
|---|---|
| `lint-backend` | `rc=0` — **413 arquivos** (`ruff` + `format --check` + `mypy --strict`) |
| `lint-frontend` | `rc=0` — ESLint + `tsc --noEmit --strict` |
| `test` | `rc=0` — **2.115 passed**, cobertura total **96,96%** (era 2.112/96,96% no gate do builder; +3 testes meus) |
| `boundaries` | `rc=0` — 7 kept, 0 broken |
| `regras` | `rc=0` — **0 bloqueio**, 66 avisos |
| `política` | `rc=0` |

**veredito do `make verify`: VERDE — 6 portões mediram e passaram.**

⚠️ A 1ª execução reprovou `lint-backend` (`rc=1`) por **ordem de import e formatação nos MEUS
testes novos** — `ruff format` + reordenação de `from datetime import` resolveram; nenhuma linha de
produção envolvida.

**Regras bloqueantes (`harness rules list --severity block` → 8 em vigor):** nenhuma se aplica ao
diff por inspeção (`core.relative-import` — todo import é absoluto; `core.silent-except` — nenhum
`except` novo; `core.print-statement` — nenhum `print`, o diff *reforça* o uso de logger nomeado;
`core.hardcoded-secret`/`own.compose-hardcoded-secret` — nenhum literal de credencial, a medição
viva usa `docker exec` sem senha em documento; `web-fullstack.*` — zero linha de `frontend/`;
`server-test-directory-present` — `backend/tests/` existe e cresceu). O portão `regras` do
`make verify` é a evidência de máquina.

## 6 · Escopo do que EU mudei — só teste, nenhuma linha de produção

```
git diff --stat   # sobre fd54ce7
 backend/tests/sentimento/test_ingest_health_extra_rendering.py               | +41
 backend/tests/sentimento/test_postgres_ingest_record_store_credits_the_run.py | +62
```

Três testes novos, todos fechando mutante que sobreviveu: o destino do handler (M4d) e o caminho
de leitura do Postgres (N5/N7). Zero alteração em `backend/src`, `frontend/` ou documento
normativo.

## 7 · Veredito

```
## QA Gate — Fase 06 [infra + sentimento]
- [OK] 8 regras bloqueantes em vigor — `make verify` portão `regras` rc=0, 0 bloqueio, 66 avisos
- [OK] Testes existem e passam — `make verify` → 2.115 passed (58 + 12 nos arquivos do escopo)
- [OK] Cobertura 96,96% (o piso por camada do `backend/scripts/test.sh` passou; a política do
       repositório não declara chave `coverage` — `harness policy --key coverage` → campo ausente)
- [OK] CA-F6-1 · 3 arquivos     [OK] CA-F6-2 · M2 morre com 4 killers
- [OK] CA-F6-3 · byte-idêntico  [OK] CA-F6-4 · 89,23→100,00 e 0,42→100,00 na stack viva
- [OK] CA-F6-5 · null + statusDetail + n_runs_in_window, sobre HTTP de loopback real
- [OK] CA-F6-6 · make verify VERDE
Mutantes: 13 plantados por mim, 13 mortos APÓS os 3 testes que escrevi (2 sobreviveram antes)
Veredito: APPROVED
```

**As duas ressalvas que viajam com o APPROVED, e nenhuma é defeito de código:**

1. **`ADR-030/F-5` morreu com a emenda** — o falsificador daquela ADR diz literalmente que
   `uptimePercent ≠ 100·Σ n_written/Σ n_expected ⇒ a decisão cai`, e a divergência agora é
   **por desenho** (`0,42` contra `100,00`, medido). O builder escalou e **não** consertou;
   concordo com a omissão — cruzar `ADR-030` com `ADR-035` é ato de `/architect`. **Mas enquanto
   não for escrito, há duas verdades sobre a mesma métrica**, que é o defeito que esta fase
   existe para matar.
2. **O merge precisa levar os normativos não versionados** e os **2 arquivos de teste deste
   gate**; sem o segundo, os mutantes `N5`/`N7`/`M4d` voltam a sobreviver.

## 8 · [ANOMALIA] o veredito NÃO pôde ser gravado desta worktree — e a recusa está certa

```
harness gate-record cinco-metricas-do-core 06 QA APPROVED "…"
# -> RECUSADO PELO DOCUMENTO DE TAREFAS, NAO PELA APROVACAO:
#    fase 06: veredito QA APPROVED sem NENHUMA tarefa correspondente no documento —
#    vacuidade e recusa (R-11), nunca aprovacao
harness tasks list cinco-metricas-do-core | grep -c 'T-06'   # 0
grep -c 'T-06' docs/context/cinco-metricas-do-core/tasks.toml  # 0  (worktree, 76.242 B)
# no checkout principal o mesmo arquivo tem 89.657 B e CONTÉM T-06.1/T-06.2 — nao versionado
```

**Causa, e é a mesma pendência do topo:** os blocos `T-06.1`/`T-06.2` só existem na árvore de
trabalho do checkout principal. **Não contornei** — `harness pipeline override` é escape de owner,
e editar `tasks.toml` à mão é proibido (escritor único, `ADR-005`).

⇒ **Ato do orquestrador, do checkout principal, depois de commitar os normativos:**

```
harness gate-record cinco-metricas-do-core 06 QA APPROVED "<notas deste relatorio>"
harness tasks resolve cinco-metricas-do-core 06 T-06.1=done T-06.2=done
harness tasks validate cinco-metricas-do-core
```

**Trabalho deste gate, para não se perder:** commit **`e40051e`** na branch
`worktree-agent-a9544fad66dc36b98` (filho direto de `fd54ce7`, fast-forward sobre
`origin/task/cinco-metricas-do-core-f06-instrumentacao`).
