# [QA GATE — Fase 05 · `T-05.8`: as DUAS coortes de `sum_liquidation` no catálogo SERVIDO]

**Feature:** `cinco-metricas-do-core` · **Componente:** `sentimento`
**Spec:** `docs/specs/SPEC-007-cinco-metricas-do-core.md` §4.5 · `RF-2` · `RN-1` · `RS-1`
**Plan:** `docs/plans/SPEC-007-cinco-metricas-do-core/05_liquidacoes.md` — item `5.4` (metade backend)
**Task:** `T-05.8`, `docs/context/cinco-metricas-do-core/tasks.toml:833-849`
**Base:** `master` `e4b91cb` (worktree `agent-a64a1171ff04f69ee`)

## Arquivos alterados — quatro, e nenhum fora do escopo

Produção (H2, e só ele):
- `backend/src/modules/sentimento/use_cases/series_catalog.py` (**modified**)

Teste:
- `backend/tests/sentimento/test_series_history_sparse_liquidation.py` (**new**) — o contrato esparso
- `backend/tests/sentimento/test_series_catalog_use_case.py` (**modified**)
- `backend/tests/api/test_series_catalog_route.py` (**modified**)

```
git status --short
 M backend/src/modules/sentimento/use_cases/series_catalog.py
 M backend/tests/api/test_series_catalog_route.py
 M backend/tests/sentimento/test_series_catalog_use_case.py
?? backend/tests/sentimento/test_series_history_sparse_liquidation.py
```

⛔ `frontend/` **intocado**. ⛔ Nenhum deploy. ⛔ **ZERO chamada à Coinalyze** — nenhuma linha desta
entrega abre socket; o `sitecustomize` de `backend/scripts/nonet/` continua amputando terceiro na
suíte. ⛔ Nada semeado no Postgres compartilhado: as duas consultas feitas ao banco foram de
**leitura** (`select … group by`), e estão transcritas abaixo.

## A mudança de produção, em duas linhas

1. `from src.modules.sentimento.domain.liquidation_catalog import liquidation_catalog_entries`
2. `*liquidation_catalog_entries(instrument_id).entries` **anexado ao fim** da lista `entries` de
   `list_series_catalog()`.

A identidade vem de `T-05.3` (`domain/liquidation_catalog.py`), **importada, nunca recriada** — este
arquivo não constrói `SeriesKey` nenhum. O builder **não recebe `verified_by`**: aquele módulo o
fixa, então o ESCRITOR (`collector_series_mapping.build_liquidation_history_to_row`, que chama
`coinalyze_liquidation_key`) e este LEITOR passam pelo mesmo construtor de chave e não podem cair em
dois `series_key_id` que apenas se parecem. O resto do diff é docstring.

## DUAS entradas, e por que somá-las seria o defeito

`cohort="long"` no índice **13** e `cohort="short"` no índice **14**, na ordem que `COHORTS`
declara. Liquidação de long é venda forçada e de short é compra forçada: a soma se move igual quer o
mercado tenha lavado longs, shorts ou os dois — exatamente a discriminação que a métrica existe para
dar (`RF-2`). O `DoD-2` da fase pede `n_points > 0` para as duas **separadamente**, e uma linha
líquida não responderia essa pergunta em direção nenhuma.

## Números — cada um com o comando e o universo

### O catálogo servido: 13 → 15 por instrumento, 52 → 60 no piloto, e `LOST=[]`

```
cd backend && PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -c "…list_series_catalog('BTCUSDT')…"
n_entries BTCUSDT = 15      (13 antes)   ·  pilot = 60   (52 antes)
13 sum_liquidation long  1m 1min 60000 23e4332307dc…
14 sum_liquidation short 1m 1min 60000 bc0b8a78e16b…
```

⚠️ **`LOST` não é opinião, é diferença de conjuntos medida contra o `HEAD`.** O módulo de `e4b91cb`
foi extraído (`git show HEAD:…series_catalog.py`) e carregado LADO A LADO com o novo, contra os
MESMOS módulos de `domain/`:

```
.venv/bin/python <scratchpad>/lost_check.py
BTCUSDT  before=13 after=15 LOST=[]
PILOT    before=52 after=60 LOST=[]
PREFIX PRESERVED (btc): True          # `RS-1`: ordem é FORMA, e o prefixo não se moveu
ADDED (btc) = 2 -> ['23e4332307dc…', 'bc0b8a78e16b…']
```

Três fases já acrescentaram linha neste arquivo e **auto-merge limpo não prova união** — por isso a
prova é a diferença de conjuntos, não o `git merge` ter passado.

### `native_grid_ms` é DECLARADO, nunca parseado

`liquidation_catalog.NATIVE_GRID_MS = 60_000` ao lado de `NATIVE_GRID = "1min"` (`ADR-037/D3`,
`ADR-003`/FR-3). No instante em que as duas linhas passam a ser servidas elas entram no universo de
`tests/sentimento/test_native_grid_ms_pairs.py`, que enumera **toda** entrada servida e recusa par
divergente — sem nada para lembrar de atualizar lá. Como `interval='1m'`, o bug de grade que o
`ADR-037` consertou não alcança esta série: `native_grid_ms == _GRID_STEP_MS`.

### O contrato ESPARSO: ausência é ausência, nunca zero

`backend/tests/sentimento/test_series_history_sparse_liquidation.py` (novo, 3 testes × 2 coortes = 6
provas) roda o **`build_series_history_report` real** sobre a entrada **SERVIDA** (lê
`list_series_catalog()`; se o registro for revertido, o arquivo inteiro falha):

- 10 instantes de grade, 4 com bucket (offsets `0,1,4,9`), 6 buracos. Resultado: os 4 trazem o valor
  RAW da fonte, os 6 trazem `value is None`, `absence == "SEM_PONTO"`, `available_at is None`, e
  **nenhum valor servido é zero em grafia alguma** (`Decimal(row.value) != 0`, não `!= "0"`).
- **CONTROLE NEGATIVO — é o ponto do arquivo:** as MESMAS observações, a MESMA função, só
  `Nature.FLOW → Nature.STOCK`, e os buracos **são preenchidos**. Sem ele, "tudo `SEM_PONTO`" seria
  satisfeito também por um caminho de leitura que devolve `SEM_PONTO` sempre — teste que não pode
  falhar pelo motivo certo não mede nada (`ADR-012`, o `rc=0` ambíguo, um andar acima).
- **A FRONTEIRA que o `DoD-2` depende, escrita em vez de suposta:** com `available_at - bucket_end ==
  NATIVE_GRID_MS`, **todos** os 10 instantes voltam `SEM_PONTO`. É a classe de
  `ACHADO-SERIES-HISTORY-SEM-PONTO.md` (180 linhas, 0 valores no `klines_volume`): `FLOW` recusa o
  bucket assim que `age_ms >= bucket_interval_ms`. Não é defeito do registro e o teste não afirma que
  seja — é a **pré-condição do coletor** para `n_points > 0` ser alcançável, fixada aqui para que um
  futuro `n_points = 0` seja diagnosticado como atraso de publicação e não redepurado como linha de
  catálogo faltando.

A esparsidade não é hipótese: **20,2%** dos buckets de 1 min têm liquidação `[MEDIDO 2026-09-12,
n=14.344 buckets possíveis, 2.900 preenchidos]`.

### A suíte

```
cd backend && PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -m pytest \
  tests/sentimento/test_series_history_sparse_liquidation.py \
  tests/sentimento/test_series_catalog_use_case.py \
  tests/api/test_series_catalog_route.py \
  tests/sentimento/test_native_grid_ms_pairs.py -p no:cacheprovider
# -> 53 passed in 2,88 s   (6 deles no arquivo novo; 5 casos novos no use-case test)
```

### Verde não prova nada até uma mutação reprovar — 2 aplicadas, 2 reprovas

Fora do git, em sandbox, com restauração conferida por `diff -q` (**`IDENTICAL both`**) e a suíte
verde de novo depois (**101 passed**, incluindo `test_as_of_accessor.py`):

| # | mutação | resultado |
|---|---------|-----------|
| `M1` | o registro REMOVIDO (a linha `*liquidation_catalog_entries(...)` apagada) | **18 failed, 30 passed** — entre elas `…no_longer_refuses_either_liquidation_id[long]` **e** `[short]`, `…the_ones_the_collector_writes_under`, e o teste HTTP da rota |
| `M2` | `CARRY_FORWARD_BY_NATURE[Nature.FLOW] = True` — a projeção passa a PREENCHER o buraco | **4 failed, 2 passed** no arquivo esparso, e as 2 que passam são **exatamente o controle negativo** (`…holes_are_filled_for_a_carry_forward_nature`), que é o comportamento que ele afirma |

`M2` é a que importa para `RN-1`: ela prova que o "ausência nunca vira zero" é medido pelo teste e
não herdado de um caminho de leitura que devolveria `SEM_PONTO` de qualquer jeito.

## `make verify` — 8 portões

Rodada final (`PYTHONDONTWRITEBYTECODE=1 make verify`, `__pycache__` purgado antes):

```
[OK] lint-backend   446 source files      [OK] test        2375 passed · 96,60% (piso 70%)
[OK] lint-frontend  ESLint + tsc --strict [OK] boundaries  7 kept, 0 broken
[OK] test-frontend  592 pass, 0 fail      [OK] regras      0 bloqueio(s), 72 aviso(s)
[OK] política                             [OK] e2e         27 passed (36,0 s)
```

`harness rules --mode sweep --changed-only` sobre o que eu escrevi: **0 bloqueante, 1 aviso**
(`core.module-docstring-single-line` no H2 — pré-existente, o docstring do módulo já era multilinha).

⚠️ **As duas primeiras rodadas saíram `INDETERMINADO`, e o motivo está escrito em vez de escondido —
inclusive o que EU causei.** A worktree nasce sem `backend/.venv`, sem `frontend/node_modules` e sem
`data/`; os três foram resolvidos **medindo**, nunca contornando o portão:

1. `backend/.venv` e `data/` → symlink do checkout principal (mesmo padrão que `T-05.5` registrou).
2. `frontend/node_modules` **por symlink QUEBROU o `e2e`**, e essa é a armadilha que vale registrar:
   `lint-frontend` e `test-frontend` ficaram verdes, e o `next build` do `e2e` morreu com
   `TurbopackInternalError: Symlink [project]/node_modules is invalid, it points out of the
   filesystem root` ⇒ `rc=3`, **`NÃO MEDIU`, não `FALHA`**. Um symlink que satisfaz 2 portões e
   reprova o 3º é exatamente o tipo de "verde" que não vale nada. Trocado por **cópia real**
   (`cp -a`, 1,4 GB), o `next build` passa e o `e2e` mede: **27 passed**, 12 canvases, cor modal
   `#131722` em 12 de 12.

⚠️ **A máquina NÃO estava ociosa:** outra worktree (`agent-a17f60de3e2f1fe14`) rodava a própria suíte
ao mesmo tempo — `ps -eo pid,etime,cmd | grep pytest` mostrou os dois processos —, e a suíte de
backend levou **~13 min** onde sozinha leva ~40 s. Isso afeta o RELÓGIO, não o veredito: `rc` e
contagens são os mesmos.

## Dois testes existentes mudaram de número, e um deles mudou de AFIRMAÇÃO

- Contagens: `13 → 15`, `52 → 60`, `12 → 14` não-reconstruídas, offsets `% 13 → % 15`, e o
  `served_metrics[-1]` que era `count_long_short_ratio` passa a ser `sum_liquidation` com o
  `count_long_short_ratio` fixado no índice **12** — a posição continua afirmada, não afrouxada.
- ⚠️ **`test_quote_denominated_rows_are_untouched_by_the_derivation` afirmava
  `{unit} == {"USDT"}` sobre TODAS as linhas `denom="quote"`, e isso teria reprovado um catálogo
  CORRETO:** as duas linhas de liquidação são `denom="quote"` com **`unit="USD"`** — Coinalyze com
  `convert_to_usd=true` publica USD, e este repositório não reescreve a unidade de terceiro. A
  asserção foi **partida por métrica** (2 linhas de preço em `USDT`, 2 de liquidação em `USD`), então
  ela continua mordendo o que foi escrita para morder, em vez de ser relaxada.

## Doc delta

- `docs/INDEX.md`: **linha acrescentada** (append-only), apontando para este relatório.
- `docs/plans/SPEC-007-cinco-metricas-do-core/05_liquidacoes.md`: **sem mudança — motivo:** a lacuna
  ("o plano não numerou o registro na fase 05") já está registrada em `tasks.toml` como a própria
  `T-05.8` e está nomeada no docstring do módulo de produção; reescrever plano aprovado é ato de
  `/architect`, não de builder.
- `docs/specs/SPEC-007-…`: **sem mudança — motivo:** §4.5 já exige o registro; nada aqui contradiz ou
  estende a spec.
- **ADR: não necessário — motivo:** nenhuma decisão nova. A linha entra pelo mesmo `RS-1` (ordem é
  forma ⇒ append) de `T-01.6`/`T-02.4`/`T-04.4`, e `native_grid_ms` segue `ADR-037/D3` como já
  declarado por `T-05.3`.

## ⛔ BLOQUEADO — o que NÃO fechou, nomeado

1. **`DoD-2` da fase (`n_points > 0` para as duas coortes) NÃO é demonstrável nesta entrega, e o
   motivo não é o catálogo — é que não há linha no banco.** Medido, em leitura:

   ```
   docker exec deploy-postgres-1 psql -U cripto_strategy -d cripto_strategy -At \
     -c "select series_key_id, count(*) … where series_key_id in ('23e4332…','bc0b8a7…') group by 1;"
   -> ZERO linhas
   docker exec … -c "select source, count(*) from md.series group by 1 order by 2 desc;"
   -> /fapi/v1/klines 167.176 · /fapi/v1/premiumIndex 37.968 · openInterestHist 8.064 ·
      globalLongShortAccountRatio 4.000   (n=4 fontes; `/v1/liquidation-history` NÃO aparece)
   ```

   O coletor de `T-05.5` existe e não está implantado — **deploy está proibido neste despacho**. O
   que esta task tira do caminho é o `422 UnknownSeriesKeyIdError`: a partir daqui a pergunta
   `n_points` é fazível.
2. **A cauda de publicação é a próxima a morder, e ela é aritmética, não palpite.** Cadência do
   coletor = 300 s; para `FLOW`, `as_of` recusa o bucket quando `age_ms >= 60_000`. Um bucket que só
   fica legível mais de um minuto depois de fechar **nunca** é servido — o terceiro teste do arquivo
   novo pina isso. Dono: fase `05`/`T-05.9`+ (painel) e quem implantar o coletor; ⚠️ não é ato de
   builder de `T-05.8` mudar `available_at`, `max_staleness_ms` ou a cadência.
3. **Herdado, sem dono aqui:** a `COINALYZE_API_KEY` vazada segue pendente de rotação pelo owner.
