# `T-04.8` — coletor de long/short em PRODUÇÃO: `DoD-1` e `DoD-4` medidos

**Data:** 2026-09-16T20:29Z · **Componente:** `infra` · **Feature:** `cinco-metricas-do-core`
**Base:** `master@7295137` · **Ambiente:** produção viva (`deploy-postgres-1`, `deploy-api-1`)
**Escrita:** nenhuma. Só `SELECT` e `GET`. Sem deploy, sem migração, sem ledger além deste registro.

---

## 1 · Veredito

✅ **`DoD-1` e `DoD-4` PASSAM, e o `DoD-2` (que era de outra task) passa junto** — a série
`count_long_short_ratio` está viva em produção, escrita pelo writer e legível pelo caminho real de
leitura. **A tarefa não construiu nada: ela mediu o que as fases seguintes já haviam deployado.**

⚠️ E encontra **1 achado que nenhum DoD cobre**: 229 runs `REJECTED` no mesmo endpoint (§4).

---

## 2 · `DoD-1` — `count(*)` de `md.series` > 0

```bash
docker exec deploy-postgres-1 sh -lc \
  'psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -tAc \
   "select src_label_raw, count(*), max(bucket_end) from md.series group by 1 order by 1;"'
```

| `src_label_raw` | linhas | `max(bucket_end)` |
|---|---:|---|
| `/fapi/v1/klines` | 1.206.806 | `1789590240000` |
| `/fapi/v1/premiumIndex` | 71.624 | `1789590264003` |
| **`/futures/data/globalLongShortAccountRatio`** | **33.318** | **`1789590000000`** |
| `/futures/data/openInterestHist` | 114.566 | `1789590000000` |
| `/v1/liquidation-history` | 233.000 | `1789590180000` |

**33.318 > 0** ⇒ `DoD-1` ✅. O `[MEDIDO 2026-09-10]` do plano dizia `0`; o universo deixou de ser
vazio, então nenhum número desta página é o `0` ambíguo que `ADR-012` nomeia.

## 3 · `DoD-4` — run fechado com `n_written > 0`, carimbado pelo writer

```bash
docker exec deploy-postgres-1 sh -lc \
  'psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -tAc \
   "select endpoint, verdict, count(*) runs, sum(n_written) written, max(ended_at) last_end,
           count(writer_accounted_at) accounted
    from md.ingest_run where endpoint like '"'"'%LongShort%'"'"' group by 1,2 order by 1,2;"'
```

| `verdict` | runs | `n_written` | último `ended_at` | com `writer_accounted_at` |
|---|---:|---:|---|---:|
| `ACCEPTED` | **4.127** | **31.322** | `2026-09-16T20:29:35Z` | **2.150** |
| `REJECTED` | 229 | 0 | `2026-09-16T04:54:09Z` | 0 |

`n_written = 31.322 > 0` com run fechado **há segundos** ⇒ `DoD-4` ✅.

## 4 · ⚠️ Achado que NENHUM DoD desta fase cobre — 229 `REJECTED`

`229` runs `REJECTED` (`n_written = 0`), o último em `2026-09-16T04:54:09Z` — **nenhum nas ~15 h
seguintes**. `DoD-4` pede *um* run fechado com escrita, e isso está satisfeito com folga; mas
`229 / (4.127 + 229) = 5,3%` de rejeição **não é ruído e ninguém as explicou**.

⛔ **`[NÃO MEDIDO]`: a causa.** Não abri `notes`/`api_code` das rejeições — fazê-lo aqui seria
escopo de `T-04.9`, que é quem julga a conjunção. Fica nomeado para lá, não silenciado aqui.

## 5 · `DoD-2` de brinde — o bloqueio de 2026-09-12 caiu

`BLOQUEIO-F04-RATIO-NAO-CARREGA.md` media `HTTP 200` com `60 linhas, 0 valores, 60 SEM_PONTO`,
porque `CARRY_FORWARD_BY_NATURE[Nature.RATIO] = False`. Hoje, mesma série, janela de 180 min:

```bash
NOW=$(( $(date +%s) * 1000 )); START=$(( NOW - 180*60*1000 ))
curl -s "http://localhost:8000/api/v1/series-history?series_key_id=279d3172f5f2572d71c72f23cb7249edff91b405c2b1e7bc88c3b664963d8e3e\
&symbol=BTCUSDT&interval=1m&bar_policy=final_only&window_start_ms=$START&window_end_ms=$NOW&knowledge_time_ms=$NOW"
# 180 slots · 129 com valor · 51 sem
```

A causa do conserto é `ADR-038` (o carimbo MODELED é a grade nativa): a amostra devolve
`available_at = 1789579541475` para `event_time = 1789579560000` e `1789579620000` — **um
`available_at` menor que o `event_time`, repetido em dois slots**, que é a escada de `5m` numa
grade de `1m` aparecendo no dado.

⇒ `T-04.7` pode medir o `DoD-3` **agora**.

> ⚠️ **CORREÇÃO 2026-09-16, e o erro era meu:** esta seção dizia *"`129 ÷ 5 = 25,8` barras nativas em
> 3 h ⇒ `N ≥ 30` exige janela ≥ 4 h"*. **O divisor `÷5` está errado** — barra nativa desta série
> conta-se por **publicação** (`available_at` distintos). Medido 2× (builder de `T-04.5` e remedição
> da QA de `T-04.7`, 240 min, `n = 240`): **49 publicações** para 48 baldes; `÷5` dá 35 (**−28,6%**) e
> `event_time % 300_000` dá 11 (**−4,5×**). Intervalos entre publicações
> `28×360s, 15×180s, 3×300s, 1×330s, 1×120s`, de média exata 300 s — grade **não** fixa.
> Na janela da própria rota o DOM entrega **850** barras nativas sobre `5760` slots ⇒ `DoD-3` passa
> com folga de 28×, sem janela de 4 h nenhuma.

---

**Ledger:** nenhum `gate-record` desta task (é medição, não gate de fase). Quem fecha a `04` é
`T-04.9`.
