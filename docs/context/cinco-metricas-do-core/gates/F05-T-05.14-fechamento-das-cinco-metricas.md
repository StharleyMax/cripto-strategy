# `T-05.14` — fechamento vertical de liquidações, **e o fechamento das CINCO métricas do core**

**Data:** 2026-09-16 · **Autor:** loop principal (orquestrador) · **Veredito: FECHADA, com 1 DoD PARCIAL por decisão do owner**

Escrito pelo orquestrador, **não pelo QA** — pelo precedente que o próprio QA fixou ao recusar redigir
o fechamento da fase `01`: *"QA redigir o fechamento que QA aprova é ciclo sem gate"*.

## A conjunção do `DoD-8`, item a item

`tasks.toml` `T-05.14` exige a **CONJUNÇÃO** de 8 itens. Nenhum foi presumido.

| # | item | estado | número, com a fonte |
|---|---|---|---|
| 1 | as duas coortes em `md.series` | ✅ | `193.405` linhas / 8 séries; BTC long `33.055`, short `33.038`, ids batendo com `coinalyze_liquidation_key()` |
| 2 | `series-history` para as duas | ✅ | `77` pontos por coorte em `1.440` grades |
| 3 | Playwright `N >= 30` **em janela com liquidação** | ✅ | `203` observações por coorte; previsto por SQL **antes** da rodada e casado na unidade (`203/68/135` e `203/127/76`) |
| 4 | run fechado com `n_written > 0` | ✅ | `725` de `955` |
| 5 | **ZERO** `REJECTED` com `api_code` e `notes` ambos nulos | ✅ | **`0`** — era **`2` de `2`** violando. É o defeito que abriu o `NEEDS_FIX` da fase |
| 6 | retenção medida | ✅ | `T-05.2` |
| 7 | cota `<= 5%` do teto | ⚠️ **PARCIAL** | `2,03%` **a `N=4`**, não a `N=10` — ver abaixo |
| 8 | veredito `APPROVED` do `ux-ui-mastery` | ✅ | `T-05.10`, após 1 `NEEDS_FIX` com bloqueio real |

**Portão:** `make verify` **VERDE nos 8** `[MEDIDO 2026-09-16T15:03:59Z sobre `70fab46`, árvore limpa,
`__pycache__` purgado, `PYTHONDONTWRITEBYTECODE=1`]` — `test` `2.482 passed / 96,65%` ·
`test-frontend` `668 pass` · `e2e` **`36 passed`** (era 30 no início do ciclo) · `regras` 0 bloqueio.

## O item 7, e por que ele fica PARCIAL em vez de fingido

> `[DECISÃO-OWNER: 2026-09-16, escolha entre alternativas apresentadas]` — fechar a fase **sem** medir
> a `N=10`, registrando a cota a `N=4` como parcial, com o `N=10` como dívida declarada.

⛔ **Não é `[PREMISSA-OWNER]`:** o owner escolheu entre três alternativas que eu redigi, com o custo de
cada uma declarado — não ditou frase. Detalhe e gatilho em
[`T-05.12-cota-parcial-decisao-owner.md`](T-05.12-cota-parcial-decisao-owner.md). **O gatilho não é a
data — é o `N`:** no instante em que produção passar de 4 símbolos, a dívida vence.

## O que a fase construiu, e o defeito que ela existia para impedir

O backend já estava vivo; **a fatia de frontend não existia** — `grep -rn sum_liquidation frontend/`
devolvia **0 linhas**, e `/symbol` tinha 3 painéis. Hoje tem **6** `data-testid`.

⛔ **E a colisão ausência↔zero estava VIVA, não teórica:** `62` de `191` observações da coorte `long`
são **zero legítimo** (`ZL-3`), contra `zeros_exatos = 0` na fatia de volume — onde o mesmo defeito era
só potencial. Num painel de evento esparso (`77`–`203` pontos em `1.440` grades), empatar *"não houve"*
com *"houve zero"* faria a tela mentir **na maior parte da janela**.

A solução herdou a forma da fase `01` (séries separadas, não `if` de cor) e a **endureceu**: lá a
ordenação foi *medida* sobre um universo e uma liquidação de 2 USD a quebraria; aqui a garantia é
`1 − 0,15 < 0,88` — **desigualdade entre duas constantes de produção**, verificada até `1e-9`.

⚠️ **E ela só ficou assim porque o gate reprovou a primeira versão.** A garantia original — *"nenhuma
barra, de nenhum valor"* — era **falsa**: histograma com `base = 1` em escala log desenha valor
sub-base **para baixo**, e `liquidation_catalog.py:34-38` já media que o default do fornecedor devolve
unidade BASE (`BTC 0.003`), regime em que **toda** barra atravessaria as marcas. O contra-exemplo que
deveria proteger era **vácuo por construção** (`MICRO = 2` com `base = 1`).

## As CINCO métricas do core — o que esta task fecha de verdade

| fatia | métrica | fase | estado |
|---|---|---|---|
| `01` | volume (`klines_volume`) | `01` | ✅ `QA=APPROVED`, `REVIEW=COMPLIANT` |
| `02` | CVD | `02` | ✅ `QA=APPROVED`, `REVIEW=COMPLIANT` |
| `03` | open interest | `03` | ✅ `QA=APPROVED`, `REVIEW=COMPLIANT` |
| `04` | long/short | `04` | ✅ `REVIEW=COMPLIANT` |
| `05` | liquidações | `05` | ✅ esta task |

## ⛔ O que este fechamento NÃO afirma

1. **Não afirma que a liquidação está verificada quanto ao NÍVEL.** Ela é a única série de terceiro e a
   única **sem oráculo**: `Σ(forceOrder)` é limite inferior por documentação da Binance, então o único
   oráculo concebível **falsifica sem nunca confirmar**. Um fornecedor que perdesse `5%` uniformemente
   **passaria em todas as invariantes internas** (`A9`).
2. **Não afirma que nenhum dado se perdeu.** `16` buckets contíguos (35 min, `72,2 bp` do nocional de
   24 h, incluindo o maior long do dia) **nunca entraram** em `md.series`, e o instrumento que deveria
   acusar está quebrado: `n_missing = 0` em `458` de `458` `IngestGap` (`A8`).
3. **Não afirma que a janela desenhada é a declarada.** A `1.280px` só `42,85%` das grades chegam ao
   canvas (`A10`), e isso vale para os **4** painéis.
4. **Não afirma que está no ar.** Esta fase mede o `HEAD`, não o container.

**As 5 pendências novas** (`A8`–`A12`) estão em
[`PENDENCIAS-PARA-AVALIAR-DEPOIS.md`](../PENDENCIAS-PARA-AVALIAR-DEPOIS.md), com dono e endereço.

## Ledger

`harness gate-record cinco-metricas-do-core 05 QA APPROVED` + `... REVIEW COMPLIANT`, depois
`harness tasks resolve cinco-metricas-do-core 05` com as **14** numa chamada — `gate-record` **antes**
do `resolve`, que copia a evidência do ledger e recusa se ela não existir.
