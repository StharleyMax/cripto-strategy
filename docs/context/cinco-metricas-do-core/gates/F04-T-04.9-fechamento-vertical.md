# `T-04.9` — fechamento vertical da fase `04` (long/short): a CONJUNÇÃO dos 7 DoD

**Data:** 2026-09-16T22:5xZ · **Papel:** `qa` · **Componentes:** `sentimento` · `infra` · `web`
**Branch:** `task/cinco-metricas-do-core-f04-front` · **Cabeça julgada:** `710e215`
**Falsificador julgado na redação de 2026-09-16** `[DECISÃO-OWNER, handoff/DECISOES-OWNER.md §D18]`

⛔ **Nenhum `gate-record`, nenhum `approve`, nenhum `advance`, nenhuma escrita em `tasks.toml`** —
quem grava o ledger é o orquestrador. ⛔ **Nenhuma linha de código de produção alterada**: a única
mutação plantada (§6) foi revertida e `git status --porcelain` devolve **vazio**.
⛔ **Nada foi semeado no Postgres compartilhado** — só `SELECT` e `GET`.

---

## 1 · Veredito

# ✅ `APPROVED` — os 7 DoD passam, RE-MEDIDOS contra a árvore de hoje

⚠️ **Nada aqui é laudo reimportado.** `make verify`, o `playwright test` do universo FORTE, o
contrato de design e a sonda contra a Binance foram rodados **nesta árvore**, depois de `fa16c15`
ter mudado `SymbolClient.tsx` em **+405 linhas** — exatamente a mudança que invalidaria um
`APPROVED` velho.

```bash
git diff --stat e5d97c2..710e215 -- frontend/src   # SymbolClient.tsx | 405 ++++-
```

| DoD | veredito | número medido hoje |
|---|---|---|
| **1** · `count(*)` de `md.series` > 0 | ✅ | **33.423** linhas, 4 símbolos |
| **2** · `/api/v1/series-history` com `n_points > 0` | ✅ | **3.063** slots com valor de 5.760 |
| **3** · `N ≥ 30` barras nativas no DOM, por PUBLICAÇÃO | ✅ | **867** = as 867 da API (**28,9×** o alvo) |
| **4** · run fechado com `n_written > 0` | ✅ | **31.423** escritos, último fecho há segundos |
| **5** · `APPROVED` do `ux-ui-mastery`, registrado | ✅ **com resíduo nomeado** (§5) | contrato de design **17 pass**, mutação **1 fail** |
| **6** · o `[INFERRED]` do teto de `5min` testado contra a Binance | ✅ | `1m`→**0** · `3m`→**0** · `5m`→**30** pontos |
| **7** · `make verify` verde | ✅ | **8 portões**, 2.499 testes, cobertura **96,67%** |

**Falsificador da fase (redação `D18`):** **DISPARA** no piso da banda (15 min) — reproduzido por
medição própria — e o **remédio que `D18` prescreve está ENTREGUE e vivo no DOM** (§7).

**Os 229 `REJECTED` que `T-04.8` deixou para esta task: RUÍDO DE INFRA, não defeito da fase** — com
causa medida e com a prova de que **não houve perda de dado** (§8).

---

## 2 · `DoD-7` — `make verify`, a árvore inteira

```bash
make verify   # /tmp/verify-cripto-strategy-20260916T222528Z.log (252K)
```

```
[OK] lint-backend    rc=0  453 source files
[OK] lint-frontend   rc=0  ESLint + tsc --noEmit --strict
[OK] test-frontend   rc=0  716 pass, 0 fail em 4 suítes
[OK] test            rc=0  2499 passed · Total coverage: 96.67%
[OK] boundaries      rc=0  7 kept, 0 broken
[OK] regras          rc=0  0 bloqueio(s), 73 aviso(s)
[OK] política        rc=0
[OK] e2e             rc=0  40 passed (39.0s)
veredito: VERDE — 8 portões mediram e passaram
```

**As 8 regras bloqueantes em vigor** (`harness rules list --severity block`: `core.relative-import`,
`core.silent-except`, `core.print-statement`, `core.hardcoded-secret`,
`web-fullstack.browser-imports-server`, `web-fullstack.tenant-from-request`,
`web-fullstack.server-test-directory-present`, `own.compose-hardcoded-secret`) foram avaliadas
**pela máquina**, no portão `regras` acima: **`0 bloqueio(s)`** sobre a árvore inteira. Cobertura
**96,67%** contra o alvo do repositório; nenhum piso por camada reprovou.

---

## 3 · `DoD-1`, `DoD-2` e `DoD-4` — re-medidos na produção viva

### `DoD-1` — a série existe em `md.series`

```bash
docker exec -i deploy-postgres-1 sh -lc 'psql -U "$POSTGRES_USER" -d "$POSTGRES_DB" -tA' <<'SQL'
select count(*), max(bucket_end), count(distinct symbol)
from md.series where src_label_raw='/futures/data/globalLongShortAccountRatio';
SQL
# 33423 | 1789598100000 | 4
```

**33.423 > 0** ⇒ ✅. O plano media `0` `[MEDIDO 2026-09-10]`; `T-04.8` media `33.318` às 20:29Z e
agora são **33.423** — o universo cresceu **105 linhas em ~2 h**, então o número não é o `0`
ambíguo que `ADR-012` nomeia nem um estoque parado.

### `DoD-2` — o caminho real de leitura devolve pontos

```bash
NOW=$(( $(date +%s) * 1000 )); START=$(( NOW - 5760*60*1000 ))
curl -s "http://localhost:8000/api/v1/series-history?series_key_id=279d3172f5f2572d71c72f23cb7249edff91b405c2b1e7bc88c3b664963d8e3e\
&symbol=BTCUSDT&interval=1m&bar_policy=final_only&window_start_ms=$START&window_end_ms=$NOW&knowledge_time_ms=$NOW"
# rows 5760 · com valor 3063 · available_at distintos 869
```

**3.063 > 0** ⇒ ✅.

### `DoD-4` — run fechado, com escrita e carimbado pelo writer

```bash
select verdict, count(*), sum(n_written), max(ended_at), count(writer_accounted_at)
from md.ingest_run where endpoint like '%LongShort%' group by 1;
```

| `verdict` | runs | `n_written` | último `ended_at` | com `writer_accounted_at` |
|---|---:|---:|---|---:|
| `ACCEPTED` | **4.251** | **31.423** | `2026-09-16T22:35:59Z` | **2.196** |
| `REJECTED` | 229 | 0 | `2026-09-16T04:54:09Z` | 0 |

`n_written = 31.423 > 0`, último fecho **há segundos** ⇒ ✅. As 229 rejeições estão em §8.

---

## 4 · `DoD-3` — o e2e do universo FORTE, RODADO NESTA ÁRVORE

⚠️ **Por que re-rodar:** a QA de `T-04.7` aprovou contra `db2302c`; `fa16c15` mudou o painel depois.
Build de produção próprio, porta `4991` (nenhuma das que os laudos anteriores usaram, para não
medir o servidor de outra árvore — o modo de falha que `T-04.8` registrou):

```bash
cd frontend && INGEST_HEALTH_API_BASE_URL=http://127.0.0.1:8000 npm run build   # rc=0
INGEST_HEALTH_API_BASE_URL=http://127.0.0.1:8000 node_modules/.bin/next start -H 127.0.0.1 -p 4991
E2E_BASE_URL=http://127.0.0.1:4991 E2E_SENTIMENTO_API_BASE_URL=http://127.0.0.1:8000/api/v1 \
  frontend/node_modules/.bin/playwright test --config=frontend/playwright.config.ts \
  frontend/e2e/14-long-short-dado-real.spec.ts
# 4 passed (4.8s)
```

| fato emitido | valor |
|---|---:|
| `series_window_reader_present` | `true` (universo **FORTE**) |
| `long_short_dom_native_bars` | **867** |
| `long_short_api_native_by_publication` | **867** ✅ igualdade exata |
| `long_short_dom_wire_points` / `long_short_api_wire_points` | `3058` / `3058` |
| `long_short_dom_slots` | `5760` |
| `long_short_native_buckets_in_window` | `1152` (teto: `867 ≤ 1152` ✅) |
| `long_short_api_plan_divisor` (o `÷5` do plano) | `611` ⛔ subconta **29,5%** |
| `long_short_api_five_minute_grid` (o jeito do `OiPane`) | `153` ⛔ subconta **5,7×** |
| `long_short_api_non_contiguous_publications` | `0` |
| `long_short_api_multi_valued_publications` | `0` |
| `long_short_last_reading_kind` / `_text` | `present` / **`Leitura atual: 1.5132 ratio`** |

**`867 ≥ 30`** ⇒ `DoD-3` ✅, com folga de **28,9×**, e **não** `SEM_PONTO`: a leitura no DOM é um
valor. A banda estrutural que o spec asserta (`ceil(wire/5) ≤ native ≤ wire`) segue válida
(`612 ≤ 867 ≤ 3058`), e o `÷5` continua sendo o **piso** dela — reconfirmado sem transcrever a
conclusão de ninguém.

---

## 5 · `DoD-5` — `APPROVED` registrado, e o que eu MEDI no código de hoje

O veredito existe e está registrado: [`gates/design-04.md`](design-04.md) §R2, rodada 2,
**`APPROVED`**, `0` must-fix, com a rodada 1 (`NEEDS_FIX`) preservada ao lado. ⇒ a **letra** do
`DoD-5` está paga.

**O que eu não aceitei por transcrição:** que os invariantes do gate continuassem valendo depois de
`fa16c15`. Medidos:

```bash
cd frontend && node --conditions=react-server --test src/app/symbol/long-short-pane-design-contract.test.ts
# tests 17 · pass 17 · fail 0
```

E o verde só vale porque a mutação reprova — **plantei a classe exata do `M-1`** (o defeito que a
rodada 1 reprovou: publicar numeral fabricado no lugar do derivado):

```bash
# {windowStats.min}  ->  1.1395   (o literal que o gate mediu por fora)
node --conditions=react-server --test src/app/symbol/long-short-pane-design-contract.test.ts
# rc=1 · pass 16 · fail 1
# expected: /windowStats\.min\}\s*a\s*\$\{windowStats\.max\}/
```

⇒ o `M-1` **morde no código de hoje**, não só no de ontem. Árvore restaurada:
`git status --porcelain` → vazio.

E a derivação é viva, não congelada — o rodapé renderizado **agora** publica

```
Escala da janela: 1.1395 a 1.8369 · amplitude 0.6974 (42,1% da mediana 1.6575) · n = 3058 grades legíveis
Últimas 4 h:      1.495 a 1.5867 · amplitude 0.0917 (13,1% da amplitude da janela) · n = 169 grades legíveis
```

A linha das 4 h **mudou** em relação à que `T-04.8` registrou (`0.1214` / `17,4%` / `n=170`) porque
o tempo andou; a linha da janela **não** mudou porque o min e o max de 4 dias não mudaram. Um
`toFixed` congelado ou um literal teriam dado o mesmo número nas duas horas. **Isso é evidência de
derivação, não de transcrição.**

### ⚠️ Resíduo nomeado — e ele NÃO bloqueia esta fase, mas não pode virar permanente por omissão

`design-04.md` §R2 declara, na própria cabeça: *"Artefato de CÓDIGO: inalterado … `frontend/` não
foi tocado nesta rodada"*. **O `ux-ui-mastery` julgou o ESTUDO Stitch Rev. 3, não a tela React que
está no ar.** A tradução (`fa16c15`) veio depois e carrega **uma divergência deliberada de FORMA**
(`ratio-format.ts::equilibriumPlacement` deriva o rótulo de borda em vez de transcrever
`▼ 1,0000 … abaixo da base`) — decidida pelo builder, nunca vista pelo validador.

**Por que isto é `OK` e não `FAIL`:** o `DoD-5` pede *"veredito `APPROVED` do `ux-ui-mastery` sobre
o painel novo, registrado"*, e o item `4.6` do plano pede *"veredito … **antes de a fase fechar**"*.
As duas condições estão satisfeitas na letra, e a divergência recusa **uma afirmação falsa** (a
frase fixa mente na primeira vez que a razão cruzar a paridade) — é a mesma classe de defeito que o
`M-1` reprovou, não uma preferência estética.

**Por que fica escrito mesmo assim:** `CLAUDE.md` §Design diz que *"nenhuma decisão de design vale
antes de o validador concordar"*, e esta é uma decisão de forma sem concordância registrada.
**Gatilho, para que a exceção não seja permanente por omissão:** a revalidação de forma da tela
**implementada** (uma rodada, artefato = a rota renderizada, não o HTML do Stitch) é devida **antes
do primeiro `advance` que leve `cinco-metricas-do-core` a `DONE`** — não desta fase. Ação para o
orquestrador, registrada no §10.

---

## 6 · `DoD-6` — o `[INFERRED]` do teto de `5min`, testado contra a Binance

**A pergunta era desta task: "ache o teste e o `n`, ou reprove."** Achei os dois, e re-medi a fonte.

**Medição própria, hoje, contra a origem** `[MEDIDO 2026-09-16, n=3 períodos sondados]`:

```bash
for p in 1m 3m 5m; do
  curl -s "https://fapi.binance.com/futures/data/globalLongShortAccountRatio?symbol=BTCUSDT&period=$p&limit=30"
done
# period=1m -> HTTP 200, n=0  (lista vazia)
# period=3m -> HTTP 200, n=0  (lista vazia)
# period=5m -> HTTP 200, n=30 {'longShortRatio': '1.5031', 'timestamp': 1789588800000, …}
```

⇒ **o `[INFERRED]` é VERDADEIRO**: a grade fina **não existe para ser coletada**, e o teto de `5min`
é **da Binance**, não da Coinalyze. **`interval="5m"` na identidade está certo**, e o falsificador do
plano (*"se a Binance publicar long/short a `1min`, `interval="5m"` está errado"*) **não dispara**.

**O teste, e ele está DENTRO da identidade:**

```
backend/tests/sentimento/test_long_short_catalog.py::
  test_the_five_minute_grid_is_the_origins_and_the_label_is_the_bucket_end
```

`long_short_catalog._VERIFIED_BY` aponta para ele, e `verified_by` é o 15º termo de `SeriesKey` ⇒
**renomear esse teste re-identifica a série** (`series_key_id()` é o `sha256` dos 15 termos). O
docstring congela os dois comandos acima e o `n` de cada um.

```bash
make test-fast K=long_short_catalog   # 8 passed, 2495 deselected in 1.40s
```

**Os `n` que sustentam o item, todos com comando:**

| quantidade | `n` | origem |
|---|---:|---|
| períodos sondados na origem (`1m`/`3m`/`5m`) | **3** | o `curl` acima, re-rodado hoje |
| pontos devolvidos a `period=5m&limit=30` | **30** | idem |
| gaps de `bucket_end` para fixar a inter-chegada | **499** (`min = max = avg = 300.000 ms`) | `gates/F04-builder.md` §3 `[MEDIDO 2026-09-12]` |
| fronteiras de publicação para fixar `label_shift=0` | **2** (`+9,6 s` e `+70,8 s`) | docstring do teste `[MEDIDO 2026-09-12, poll de 10 s]` |

⇒ `DoD-6` ✅.

---

## 7 · O FALSIFICADOR DA FASE, julgado na redação `D18` — **dispara, e o remédio está entregue**

**A redação em vigor, e é contra ELA que julgo** `[DECISÃO-OWNER: 2026-09-16, §D18]`:

> *"se o painel de `count_long_short_ratio` for uma linha VISUALMENTE PLANA **na escala entregue**,
> a fatia **CONSERTA A ESCALA** — dizer quanto a série andou no prazo de decisão, sem exigir zoom."*

### 7.1 A planura: DISPARA no piso da banda — e eu a reproduzi, não a herdei

Medi a excursão vertical em **pixel** com a geometria que a tela entrega, sobre a resposta da API de
hoje (`n = 3.063` slots com valor em 4 dias), em janelas deslizantes de passo 5 min:

| horizonte | janelas (`n`) | excursão mediana | janelas sub-pixel (`< 0,5 px`) |
|---|---:|---:|---:|
| **15 min** | 875 | **`1,10 px`** | **19,1%** |
| 1 h | 892 | `4,12 px` | 3,8% |
| 4 h | 922 | `12,47 px` | 2,1% |

⇒ **no piso da banda operacional (`15min`) o painel é visualmente plano na escala entregue** — a
mediana não chega a dois pixels e ~1 em 5 janelas não move meio pixel. **No teto (`4h`) não é
plano.** Mesma conclusão de `design-04.md` §7.1, obtida por caminho independente.

### 7.2 ⚠️ Uma CORREÇÃO aritmética que achei ao reproduzir, e ela piora o número, nunca o melhora

`design-04.md` §7.1 escreve *"painel de `220px` (`CHART_HEIGHT_PX`), área de plot `154px`"*. O
`220` é a altura passada a `createChart` — **o canvas da série mede `192px`**, porque o eixo de tempo
come o resto:

```bash
# playwright, na rota real: boundingBox do <canvas> dentro de [data-testid="long-short-pane"]
# CANVAS {"x":13,"y":1782.08,"width":1208,"height":192}
```

Com as margens default da biblioteca (o painel não declara `scaleMargins` — `grep -n 'scaleMargins'`
não lista o long/short), a área de plot real é `192 × 0,7 = ` **`134,4 px`**, não `154 px`: `12,7%`
a menos de resolução vertical. ⇒ **a planura publicada em `§7.1` estava SUBESTIMADA.** Não muda
veredito nenhum (o falsificador já disparava), e está aqui porque um número errado que favorece a
própria conclusão é pior que um número errado que a contraria.

### 7.3 O remédio que `D18` manda: ENTREGUE, e verificado no DOM de hoje

`D18` define o remédio na própria escolha do owner: *"o remédio é o que a Rev. 3 já entrega (faixa
de 4 h + rodapé numérico dizem quanto a série andou no prazo de decisão, sem exigir zoom)"*.
Extraído do HTML servido pela rota **agora** (§5): as duas linhas de escala estão na tela, com `n`
declarado, derivadas dos mesmos slots que o gráfico desenha, e a nota de que os números são
*"derivados da série, não redigidos"*.

⇒ **quem lê o painel sabe quanto a série andou (`0.0917` nas últimas 4 h, `13,1%` da amplitude da
janela) sem dar um zoom.** O falsificador está **atendido na redação em vigor**.

**Resíduo declarado, com o custo à vista:** a banda de operação do plano é `15min..4h` e o rodapé
quantifica o **teto** (`4h`), não o **piso** (`15min`) — que é justamente onde a planura morde. O
owner escolheu esta opção **com a medição do piso na frente** (`§D18` cita os `25,5%` sub-pixel de
15 min no próprio menu), então isto é escolha informada, não omissão. Se voltar, volta como task
própria — não é dívida que esta fase esconde.

---

## 8 · Os 229 runs `REJECTED` — **ruído de infra, não defeito**, e a causa é MEDIDA

`T-04.8` §4 deixou a causa explicitamente `[NÃO MEDIDO]` para cá. Abri `notes` e `api_code`:

```bash
select api_code, left(notes,90), count(*), sum(n_expected), sum(n_returned), min(started_at), max(started_at)
from md.ingest_run where endpoint like '%LongShort%' and verdict='REJECTED' group by 1,2;
```

| `api_code` | `notes` | runs | `n_expected` | `n_returned` |
|---|---|---:|---:|---:|
| `NULL` | `gaierror: [Errno -3] Temporary failure in name resolution` | **227** | 0 | 0 |
| `NULL` | `TimeoutError: The read operation timed out` | **2** | 0 | 0 |

**Veredito: RUÍDO DE INFRA (queda de DNS do host), não defeito desta fase.** Quatro medições
independentes sustentam isso, e cada uma sozinha seria fraca:

1. **`api_code` é `NULL` nas 229** — a Binance **nunca respondeu**. Não houve `4xx`/`5xx`, não houve
   payload recusado, não houve contrato quebrado. `gaierror` é falha de resolução de nome: o
   processo não chegou a falar com a origem.
2. **`n_expected = 0` e `n_returned = 0` nas 229** ⇒ **nenhum dado foi descartado em silêncio.** A
   classe de defeito que assustaria — *"a fonte respondeu e o coletor jogou fora"* — está excluída
   por construção, não por opinião.
3. **A mesma falha atingiu os endpoints vizinhos, com contagem quase idêntica** ⇒ é do **host**, não
   do coletor de long/short:

   ```bash
   select endpoint, count(*) from md.ingest_run where verdict='REJECTED' and notes like 'gaierror%' group by 1;
   # /fapi/v1/klines                                  | 229
   # /futures/data/openInterestHist                   | 229
   # /futures/data/globalLongShortAccountRatio        | 227
   ```

   Um defeito do coletor de long/short não derruba `klines` no mesmo instante e na mesma contagem.
4. **Foi uma rajada única, já encerrada** — `227` das `229` numa janela de ~4 h:

   | hora (UTC) | rejeições |
   |---|---:|
   | `2026-09-16T01` · `T02` · `T03` · `T04` | `57` · `59` · `59` · `52` |
   | `2026-09-14T09` | `2` |

   Nenhuma nas **~18 h** seguintes, contra **4.251** `ACCEPTED` no mesmo endpoint.

### ⭐ E a prova que fecha a questão: **ZERO perda de dado**

A dúvida que importa não é *"por que rejeitou"*, é *"ficou buraco?"*. Não ficou:

```bash
with d as (select distinct bucket_end from md.series
           where src_label_raw='/futures/data/globalLongShortAccountRatio' and symbol='BTCUSDT'
             and bucket_end > (extract(epoch from now())*1000)::bigint - 4*86400000),
     s as (select bucket_end, lag(bucket_end) over (order by bucket_end) as prev from d)
select (bucket_end-prev) as gap_ms, count(*) from s where prev is not null group by 1;
# 300000 | 1151      <- UM único valor de gap, e ele é a grade nativa
```

**`n = 1.151` gaps consecutivos em 4 dias, e todos valem exatamente `300.000 ms`.** A rajada de DNS
cai **dentro** dessa janela (14/09 e 16/09) e **não deixou um único balde faltando** — porque o
coletor é REST e refaz a janela: `limit=500` a `5m` são **~41,7 h** de cobertura contra **~3,9 h**
de queda. É exatamente a recuperabilidade que `ADR-036/D4` comprou, medida aqui por acidente.

⇒ **não é defeito da fase `04`, não é `NEEDS_FIX`, e não vira dívida silenciosa.** O que sobra é uma
propriedade do **host** (`5,1%` de rejeição por DNS em 3 endpoints), que pertence ao alarme de
liveness de `infra` — **fora do escopo dos 7 DoD desta fase**, e nomeada aqui para quem for dono
dele, não silenciada.

---

## 9 · ⚠️ Mapeamento de rótulo — dois artefatos estão sob a task errada

Registrado, **sem reescrever histórico** (`git rebase`/`git commit --amend` seriam pior que o erro):

| artefato | rótulo que ele carrega | task de que ele é execução, de fato |
|---|---|---|
| commit **`fa16c15`** — *"`T-04.8` — a Rev. 3 aprovada virou tela"* | `T-04.8` | **`T-04.6`** (`[web]` · *"`ui-designer` desenha o painel novo e o `ux-ui-mastery` emite veredito"*), como continuação de **`T-04.5`** (`[web]` · o painel novo) |
| [`gates/T-04.8-design-implementado.md`](T-04.8-design-implementado.md) | `T-04.8` | idem |

**A `T-04.8` verdadeira é outra:** `tasks.toml:670-671` → *"`[infra]` 04 · Coletor de long/short em
PRODUÇÃO: medir `DoD-1` e `DoD-4`"*, cujo laudo é
[`gates/T-04.8-producao.md`](T-04.8-producao.md) — e **esse** está rotulado certo.

**Por que isto importa e não é pedantismo:** os dois artefatos são de componente **`web`**, e a
`T-04.8` é de componente **`infra`**. Quem for reconstruir o histórico da fase por `git log --grep`
atribuiria `+405` linhas de `frontend/src` a uma task de infraestrutura que declarou, no próprio
laudo, *"a tarefa não construiu nada"*. O mapeamento acima é a correção; o histórico fica como está.

---

## 10 · Ações para o orquestrador (nenhuma bloqueia o fechamento da fase `04`)

1. **Gravar o gate** — é ato dele, não meu:
   `harness gate-record cinco-metricas-do-core 04 QA APPROVED "<notas>"`, e então
   `harness tasks resolve cinco-metricas-do-core 04 <TODAS as tasks da fase>` (a chamada é
   **atômica por fase**: listar `T-04.1..T-04.9`, não o subconjunto que falta).
2. **Revalidação de forma da tela IMPLEMENTADA** (§5) — uma rodada de `ux-ui-mastery` cujo artefato
   é a **rota renderizada**, não o HTML do Stitch. **Devida antes do `advance DONE` da feature**,
   não desta fase.
3. **Alarme de liveness de `infra`** para a classe `gaierror` (§8) — `5,1%` de rejeição em 3
   endpoints numa rajada de 4 h que ninguém viu enquanto acontecia. Dono é `infra`, não a fase `04`.

---

## 11 · O que este laudo NÃO mediu, declarado

- **Leitor de tela, teclado, zoom 200% e dicromacia** continuam `[NÃO MEDIDO]` — o próprio
  `design-04.md` §R2 os declara fora do que ele substitui.
- **Cobertura do `frontend`**: `[N/A MEDIDA]` — não há instrumento de cobertura no front
  (`grep -c coverage frontend/package.json` → `0`). O substituto é **mutação**, e ela foi rodada
  (§5). A cobertura do `backend` está no §2: **96,67%**.
- **A causa raiz do `gaierror`** (DNS do host, resolvedor, rede do VPS) — medi o **efeito** e a
  **ausência de perda**, não o mecanismo. `[NÃO MEDIDO]`, com dono nomeado no §10.3.
