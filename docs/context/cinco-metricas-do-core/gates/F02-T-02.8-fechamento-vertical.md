# `T-02.8` — fechamento vertical da fatia `02` (CVD)

**Data:** 2026-09-15 · **Autor:** loop principal (orquestrador) · **Veredito: FECHADA**

Este é o artefato que faltava para `T-02.8`. Ele não remede a fase — ele **fecha** o que já foi
medido por outros, e acrescenta o único número que nenhum deles tinha: o `make verify` **datado
sobre `master`**, que a verificação de tasks marcou explicitamente como `[NÃO MEDIDO]` por ser
read-only.

## Por que este documento existe, e por que ele não é carimbo

A verificação de tasks (`VERIFICACAO-FASE-02-tasks.md`, commit `4cb94dd`) achou `8/8` com código
em `master` e ofereceu duas saídas: aceitar aquele documento como o fechamento de `T-02.8`, ou
**exigir antes o gate dele com `make verify` datado**. Escolhi a segunda. Um `DoD-VERTICAL` fechado
com o portão `[NÃO MEDIDO]` é exatamente o `rc=0` ambíguo que `ADR-012` nomeia — ninguém saberia
depois se o verde existiu ou se só não foi olhado.

## O portão, medido para este fechamento

```
PYTHONDONTWRITEBYTECODE=1 make verify      # __pycache__ purgado antes
```

`[MEDIDO 2026-09-15T21:45:18Z, sobre `master` em `d0cb997`, árvore limpa]` — **`VERDE — 8 portões
mediram e passaram`**:

| portão | rc | número |
|---|---|---|
| `lint-backend` | 0 | 452 source files |
| `lint-frontend` | 0 | ESLint + `tsc --noEmit --strict` |
| `test-frontend` | 0 | **614 pass, 0 fail** em 4 suítes |
| `test` | 0 | **2.482 passed**, cobertura **96,65%** |
| `boundaries` | 0 | 7 kept, 0 broken |
| `regras` | 0 | 0 bloqueio, 73 avisos |
| `política` | 0 | — |
| `e2e` | 0 | **30 passed (35,2 s)** |

Saída bruta fora do contexto: `/tmp/verify-cripto-strategy-20260915T214518Z.log` (128K).

## `DoD-VERTICAL` — a métrica até o pixel

`[DOC: VERIFICACAO-FASE-02-tasks.md, commit `4cb94dd`, medido contra o app real]` — o `CvdPane`
publica **número na tela**, não `SEM_PONTO`: `cvd_readable_horizon: 2817/5760`, *"Delta atual:
90.722"*, *"Acumulado atual: −8395.724"*, `cvd_legend: 2`. O único `SEM_PONTO` da página é do
painel de **Preço**, que é outra fatia.

⚠️ **Ressalva herdada, não resolvida aqui:** o render de `/symbol` levou **> 4 min** na medição.
É a lentidão já conhecida (`T-03.5-T-03.6-qa-remedicao.md` §A6), não ausência de dado — e foi ela
que motivou o teto de `400_000 ms` no `playwright.config.ts` versionado (PR #222).

## As 8 tasks, e a prova de cada uma

Detalhe por task em [`VERIFICACAO-FASE-02-tasks.md`](VERIFICACAO-FASE-02-tasks.md). Resumo:
`T-02.1` a `T-02.7` com arquivo e símbolo citados em `master`; `T-02.7` mediu produção
(`DoD-1` 53.438 linhas; `DoD-4` `ACCEPTED`, 4.449 runs, `n_written=595.080`; deploy `sha256`-idêntico
ao `master` nos 3 arquivos). `T-02.8` é esta linha.

⛔ **O que este documento NÃO faz:** não reabre `T-02.1`–`T-02.7`, não mede produção de novo (o
número de produção é de `2026-09-15`, de `T-02.7`), e não toca o `E1`/`D15`/`D16`, que são caminho
crítico de outra fase. `tasks.toml` estava **desatualizado** (as 8 como `todo`) e isso não foi
evidência de nada — quem mediu foi código em `master` e PR mergeada (#218, #219, #220).

## Ledger

`harness gate-record cinco-metricas-do-core 02 QA APPROVED` e `... REVIEW COMPLIANT` (este último
já registrado antes), seguidos de `harness tasks resolve cinco-metricas-do-core 02` com as 8 tasks
na mesma chamada — a ordem é `gate-record` **antes** do `resolve`, porque o `resolve` **copia** a
evidência do ledger de gates e recusa com `rc=2` se ela não existir.
