# Estado ao fim de 2026-09-19 — fase `01` de `candle-real-e-eixo-unico`

`[DECISÃO-OWNER: 2026-09-19]` — *"fechar essas tasks atuais n vamos inciar a proximas. Vamos parar
pro hj quando fechar essas tasks"*. Nada foi iniciado depois disso.

## Onde o código está

Ramo **`wave/candle-f01`**, **~37 commits à frente de `master`**, árvore limpa.
⚠️ **Nunca virou PR.** Pelo Corte A (portão por fase) a PR é uma só, no fim — coerente, mas hoje
todo o trabalho da fase está num ramo local **sem code-review formal**. É a primeira coisa de amanhã.

## As 11 tasks: todas mergeadas

`T-01.1` · `T-01.2` · `T-01.3` · `T-01.4` · `T-01.5` · `T-01.6` · `T-01.8` · `T-01.10` · `T-01.11`
mergeadas. `T-01.7` mergeada. `T-01.9` fechada por verificação (zero código restante).
Mais o re-gate do validador e o `[SERIOUS-1-REGATE]`.

## Os `DoD` — 7 de 8 fechados

| `DoD` | estado |
|---|---|
| 2 · a vela tem faixa | ✅ `95/95` com `high > low`, zero degenerada |
| 3 · `price_last_reading` ≠ `:absent` | ✅ `:exact` |
| 4 · ablação de pixel | ✅ `764 px → 0`; **CALA** no controle (ablar OI deixa 764 px idênticos) |
| 5 · zero chamada nova | ✅ `n_calls=348 = ceil(129.600/1500) × 4` — no paginador, não só no array |
| 6 · pegada de disco | ✅ **+1,641 GB**; instrumento corrigido e demonstrado **3×** |
| 7 · a degenerada não existe | ✅ 0 linhas vivas |
| 9 · vela contra a origem | ✅ backfill bate **ao centavo** (240/240) · ⚠️ poll vivo erra 1 tick em ~35% do `CLOSE` |
| **1 · ≥500 pontos por chave** | ⛔ **124** — e **mudou de natureza**, ver abaixo |

## ⛔ O bloqueio do `DoD-1` NÃO é falta de dado

Existem **2.148.504** linhas de `klines_ohlc` cobrindo **90,1 dias**, corretas e pagas
(1,64 GB). Elas **não aparecem** porque `R-1` do acessor é `available_at <= t` com `t` = **a fatia**:
a linha do backfill chega ~33 h depois do bucket, e servi-la **seria lookahead**. **O acessor está
certo.** Faltam **dois modos de leitura** (decisão, que existe; história, que não existe).
Detalhe e falsificador: `gates/T-01.5-dod6-medicao-e-achado-lookahead.md`.
**Dono: `/architect` + `ADR-006`/`SPEC-001` §2.5. Cai na fase `05`.** ⛔ Não reabrir por conta própria.

## O que falta para fechar a fase (amanhã)

1. **A PR** de `wave/candle-f01` — e com ela o code-review que ainda não aconteceu.
2. **Portão de fase**: QA + code-review + design-review (o design já tem APPROVED do validador).
3. **`advance DONE` é gate do OWNER** — nenhum agente pode.

## Aberto, com dono, sem bloquear

- **`CLOSE` do poll vivo** erra ~1 tick em ~35% dos buckets (`live_tail`, lag ~2,3 s). **Simétrico**
  (`pos≈neg`) ⇒ **NÃO é o `[M-9]`**; o viés do volume não se propagou. Causa-raiz: `ADR-034`.
- **`[M-9]`** (volume subestima a origem, unilateral) e **`[M-10]`** (`SEM_PONTO` ambíguo entre zero
  legítimo e não-lido — este achado o exercitou de verdade hoje): `/architect`.
- **`color-tokens.test.ts:177-201`** ainda usa a ablação literal que foi substituída em
  `candle-direction-channel.test.ts`. Mesma classe de fraqueza, outro arquivo.
- **`HIGH` 3/3 negativo + `LOW` 1/1 positivo** — as 4 divergências **estreitam** a vela. Separadas
  não alcançam `minimum_bias_n=4`; agregadas como "estreitamento de faixa" dariam `n=4` unilateral.
  Amostra pequena, não derruba nada. Pergunta para `/architect`.
- **API com transação aberta há 2d10h** (`gates/ESCALADO-api-idle-in-transaction.md`) — o redeploy de
  hoje derrubou o sintoma; a causa fica. `[DECISÃO-OWNER: registrar e seguir]`.
- **10 Epics velhos no Jira** (`CST-112..114`, `CST-140..142`, `CST-168..171`) em "Tarefas pendentes"
  com as features `DONE` no ledger. Oferecido duas vezes, nunca respondido.

## Infra

Disco **12,8 GB livres, 95%**. Serviços no ar: `api`, `web`, `writer`, `collector`, `postgres`,
`redis` — todos redeployados hoje com o código da fase. Container de backfill saiu sozinho (`--rm`).
⚠️ Um segundo backfill de 90 dias custa outros ~1,6 GB.

## Dois erros meus, registrados para não repetir

1. **Acusei defeito no dado que era degrau de `LOCF`** — os campos que me refutavam (`nature: STOCK`,
   `maxStalenessMs: 120000`) estavam na saída que eu mesmo imprimi. **Antes de acusar o dado, separe
   a célula ESCRITA da CARREGADA.** Refutado por `T-01.7`, registro em
   `gates/ACHADO-vela-viva-diverge-da-origem.md` (preservado, não apagado).
2. **Removi a worktree de um builder ainda vivo** — conferi ramo mergeado e árvore limpa, e
   *mergeado não é terminado*. A medição do `DoD 6` só sobreviveu porque veio por hand-back.

---

# 2026-09-21 00:12Z — `ADR-041` DEPLOYADO E MEDIDO VIVO. `DoD-9` agora é só relógio.

## O offset novo está vivo, e há duas provas independentes

**(1) O relógio dos runs.** `md.ingest_run` mostra o ciclo de klines nascendo em `:20,000` de
cada minuto — `00:02:20`, `00:03:20`, … `00:10:20`, `n=9 runs`, todos `verdict=ACCEPTED`,
`n_returned=12`, `n_written=24`. Antes do deploy nasciam em `:02`.

```sql
select min(started_at), count(*) from md.ingest_run
 where endpoint='/fapi/v1/klines' and floor(extract(second from started_at::timestamptz))=20
   and started_at::timestamptz > now() - interval '3 hours';
-- 2026-09-21T00:02:20.000314+00:00 | 9
```

**(2) O dado gravado.** `available_at - bucket_end` sobre as linhas dos últimos 10 min:

```sql
select round(avg(available_at-bucket_end)/1000.0,1), count(*) from md.series
 where src_label_raw='/fapi/v1/klines' and bucket_end > (extract(epoch from now())*1000 - 600000);
-- 22.2 | 228
```

| | offset medido | universo |
|---|---|---|
| **antes** (`+2 s`) | `2,3`–`6,2 s`, média **`3,0`** | `n=936` linhas `[MEDIDO 2026-09-20 23:56Z]` |
| **depois** (`+20 s`) | média **`22,2 s`** | `n=228` linhas `[MEDIDO 2026-09-21 00:11Z]` |

Os `22,2` são os `20` do `ADR-041` mais `~2,2 s` de latência de requisição — o mesmo termo que
fazia o offset de `2 s` medir `3,0`. **Bate com a predição, sem ajuste.**

## ⛔ `T0` do `DoD-9` = `2026-09-21T00:03:00Z`, e NÃO as 23:51 do deploy

Entre `23:51:26Z` (fim do deploy) e `~00:01Z` **não houve coleta nenhuma** — o boot do coletor
ficou preso num `ALTER TABLE` atrás de uma transação ociosa da API (ver
`gates/ESCALADO-api-idle-in-transaction.md`, seção de 2026-09-20). Contar a janela a partir do
deploy incluiria 10 minutos sem dado e ~7 dias de retaguarda de boot.

**As duas janelas disjuntas, com `n≥150` buckets cada, a 1 bucket/min por símbolo:**

| janela | início | fim | buckets |
|---|---|---|---|
| **A** | `2026-09-21T00:03:00Z` | `2026-09-21T02:33:00Z` | `150` |
| **B** | `2026-09-21T02:33:00Z` | `2026-09-21T05:03:00Z` | `150` |

⇒ **`DoD-9` é mensurável a partir de `~05:05Z`.** Não há como encurtar: é `n` de relógio.

```bash
# janela A — rodar a partir das 02:35Z
backend/.venv/bin/python -m src.modules.sentimento.infra.candle_fidelity_cli \
  --symbol BTCUSDT --window-start-ms 1789948980000 --window-end-ms 1789957980000 \
  --knowledge-time-ms $(date -u +%s)000

# janela B — rodar a partir das 05:05Z
backend/.venv/bin/python -m src.modules.sentimento.infra.candle_fidelity_cli \
  --symbol BTCUSDT --window-start-ms 1789957980000 --window-end-ms 1789966980000 \
  --knowledge-time-ms $(date -u +%s)000
```

✅ só com `verdict=accepted`, `rc=0`, e **nenhum** viés unilateral acima de `minimum_bias_n=4`
em `HIGH` ou `LOW`, **nas duas janelas**.

## O que NÃO foi feito e continua aberto

- **O dado gravado antes de `00:03Z` continua sendo prefixo.** `ADR-041` conserta a escrita
  daqui para frente; não reprocessa o passado. Quem decide se limpa/re-popula é o owner —
  a autorização de limpar a base existe, mas limpar não é consertar (ver a memória
  `deploy-docker-autorizado`).
- **O `writer` NÃO foi redeployado** (subiu há 2 h, antes do `ADR-041`). Ele não lê o offset,
  então não precisava — registrado para que ninguém conclua que "tudo subiu".
- **`lock_timeout` no `ALTER` de boot + `PYTHONUNBUFFERED=1`** no `deploy/compose.yml`: os dois
  consertos que teriam transformado a parada silenciosa de hoje em erro nomeado. Sem dono ainda.
