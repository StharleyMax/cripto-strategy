# Handoff — T-03.4

Task completa em `docs/context/candle-real-e-eixo-unico/tasks.toml` (buscar `id = "T-03.4"`,
~linha 417). Leia o bloco inteiro — refs, DoD, os 3 números que derrubaram "recusar", o
default por REGIME DE ERRO (A: Σ/max/min com marca visível; B: first/last com maxStalenessMs).

## Escopo desta dispatch

Só T-03.4: servir SEMPRE com o par de inteiros `{present, expected}` — nunca recusar, nunca
limiar, nunca extrapolar. T-03.1 (`reduce_bucket`) e T-03.3 (SUPPORTED_INTERVAL + reagregação
religada na rota) já estão na sua branch — leia
`docs/context/candle-real-e-eixo-unico/gates/T-03.3-builder.md` antes de mexer em
`series_history.py` (use_case e rota), que T-03.3 já modificou bastante.
Não implemente T-03.6 (coverage no envelope) nem T-03.7 (CA-8') — dependem desta mas são
dispatches futuras.

## Onde trabalhar

Worktree já criado, branch `task/candle-f03-t03-4` a partir da ponta atual de
`wave/candle-f03`: `/home/stharley/Documentos/projects/cripto-strategy-worktrees/candle-f03-t03-4`

Componente: `sentimento`. Gate: `bash backend/scripts/lint.sh` + `bash backend/scripts/test.sh`
(se o worktree não tiver `.venv`, rode `bash backend/scripts/bootstrap.sh` primeiro).

⚠️ Há OUTRA dispatch rodando em paralelo agora (T-03.9, worktree irmão `candle-f03-t03-9`,
componente `web`) — sem overlap de arquivo esperado.

## Ao terminar

Commit(s) na sua branch (não dê push, não abra PR). Emita o QA Gate Context Block e pare.
Se passar de ~150 turnos, escreva o estado neste arquivo e devolva.
