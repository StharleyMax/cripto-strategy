# Handoff — T-05.3

Task completa em `docs/context/candle-real-e-eixo-unico/tasks.toml` (buscar `id = "T-05.3"`,
~linha 718). Plano: `docs/plans/SPEC-008-candle-real-e-eixo-unico/05_historia_sob_demanda.md`,
item 5.2 ("Teto declarado de 90 dias") — leia a seção "Os três números que fixam o desenho" e o
DoD 5/6 do plano (custo de cota e pegada de disco).

## Escopo desta dispatch

Teto declarado de **90 dias** de klines 1m persistidos por símbolo — razão do número:
`4h × 500 velas ≈ 83,3 dias`, 90 é o menor teto redondo que cobre isso. Backfill one-shot/cron
até o teto (nunca serviço de vida longa — `ADR-027/D1`), com o custo de cota **medido**, não
estimado: `90 × 1440 ÷ 1500 ≈ 87 chamadas`, weight 1 cada, contra teto de 2.400/min ⇒ < 4% de um
minuto de cota. DoD 6 do plano: pegada de disco ≤ 2,07M linhas para 4 símbolos (§3.6 da SPEC).

Não implemente T-05.4 (a rota recusar além do teto) — outra dispatch cuida disso em cima do que
você declarar aqui (provavelmente uma constante `MAX_HISTORY_DAYS` ou equivalente que T-05.4 vai
importar). Deixe esse nome óbvio/exportável.

## Onde trabalhar

Worktree já criado, branch `task/candle-f05-t05-3` a partir de `wave/candle-f05`:
`/home/stharley/Documentos/projects/cripto-strategy-worktrees/candle-f05-t05-3`

Componente: `sentimento`. Gate: `bash backend/scripts/lint.sh` + `bash backend/scripts/test.sh`.

⚠️ Esta dispatch roda em PARALELO com T-05.0, que reabre `as_of_accessor.py` (rota completamente
diferente — anti-lookahead). Se notar qualquer sobreposição de arquivo, pare e registre no
handoff em vez de resolver sozinho.

## Ao terminar

Commit(s) na branch da task (não dê push, não abra PR). Emita o QA Gate Context Block e pare —
não rode `gate-record`, não aprove, não avance estado. Se passar de ~150 turnos, escreva o estado
aqui mesmo (`docs/context/candle-real-e-eixo-unico/handoff/T-05.3.md`) e devolva.
