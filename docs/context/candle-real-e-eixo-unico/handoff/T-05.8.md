# Handoff — T-05.8

Task completa em `docs/context/candle-real-e-eixo-unico/tasks.toml` (buscar `id = "T-05.8"`,
~linha 798). Plano: `docs/plans/SPEC-008-candle-real-e-eixo-unico/05_historia_sob_demanda.md`,
DoD verificável itens 1, 2 e 3 — o fechamento e2e do que T-05.0/T-05.1/T-05.2/T-05.6/T-05.7 (todas
já mergeadas nesta branch) construíram em camadas separadas.

## O que provar, contra o app real, via Playwright

1. **Arrastar carrega.** `n=3` arrastos sucessivos para trás aumentam a contagem de barras do
   painel de Preço, monotonicamente. Isto só é executável agora porque `T-05.0` (ADR-042) já
   destravou o backfill que antes vinha `SEM_PONTO` — se este DoD morder, primeiro confirme que
   não é regressão de `T-05.0` antes de assumir bug de paginação.

2. **A parede assimétrica aparece e é NOMEADA.** Arrastando além de ~30 dias em `4h`: o painel de
   Preço **continua** com barras **e** os painéis de OI e long/short mostram o estado nomeado
   (`data-fact="oi_coverage:beyond"`/`"long_short_coverage:beyond"`, `T-05.6`) — **os dois fatos
   no MESMO assert**, `n=2` painéis. Morde se o painel apenas esvaziar em silêncio.

3. ⛔ **Ablação de P5.** Removido o estado nomeado (comente/reverta o guard de render de
   `T-05.6` no próprio teste, não no código de produção), o painel volta a esvaziar sem dizer
   nada — captura antes/depois. Se o pixel não muda sob ablação, não era o estado nomeado.

Reaproveite os fatos de infraestrutura de teste já estabelecidos nos specs 17/18/19 de
`frontend/e2e/` (numeração sequencial: este é provavelmente `20-*.spec.ts`) — stub sintético,
`E2E-FACT` no stdout, `scripts/e2e-env.sh`.

Não implemente T-05.9 (teto de latência 400ms — dispatch irmã, worktree separado, já cobre a
mecânica de tempo) nem T-05.10/T-05.11 (design gate / docs, dependem desta).

## Onde trabalhar

Worktree já criado, branch `task/candle-f05-t05-8` a partir de `wave/candle-f05` (contém T-05.0 a
T-05.7 mergeadas): `/home/stharley/Documentos/projects/cripto-strategy-worktrees/candle-f05-t05-8`

Componente: `web`. Gate: lint + typecheck + `test:app` + o spec Playwright novo isolado.

⚠️ Roda em paralelo com T-05.9 (worktree irmão `candle-f05-t05-9`, ainda em execução — outro spec
e2e novo, numeração sequencial pode colidir se os dois escolherem o mesmo número; confira
`frontend/e2e/` antes de nomear o arquivo, e se colidir, pegue o próximo número livre).

## Ao terminar

Commit(s) na branch da task (não dê push, não abra PR). Emita o QA Gate Context Block e pare —
não rode `gate-record`, não aprove, não avance estado. Se passar de ~150 turnos, escreva o estado
aqui mesmo (`docs/context/candle-real-e-eixo-unico/handoff/T-05.8.md`) e devolva.
