# Handoff — T-04.2

Task completa em `docs/context/candle-real-e-eixo-unico/tasks.toml` (buscar `id = "T-04.2"`,
~linha 582). Leia o bloco inteiro lá — refs, DoD, item 4.2 do plano.

## Escopo desta dispatch

Só T-04.2: o envelope de proveniência (`C-4`) vira contrato próprio, derivado do `SeriesKey` —
e a unidade **permanece contratos (BTC)**, não nocional USD. `T-04.1` (já mergeada nesta branch)
criou `OiProvenanceLabel`/`deriveOiProvenanceLabel` em `panel-status.ts`/`view-model.ts` e o
componente `OiProvenance` em `SymbolClient.tsx` — leia esse código primeiro, essa task estende o
que já existe, não recomeça do zero. `C-4` pede que o envelope seja um **contrato** (tipo próprio,
não um objeto solto) e que ele nunca vire nocional USD — `ADR-036/D2` fixa `unit=BTC`,
`denom=base`, `aggregationScope=Symbol`; DoD 5 do plano MORDE se qualquer entrada nova de fonte
aparecer (isso seria `F5`, fora deste plano). Plano completo:
`docs/plans/SPEC-008-candle-real-e-eixo-unico/04_oi_honesto.md`.

Não implemente T-04.4 (herança pelos demais painéis) nem T-04.5 (ablação completa + assert de
posição via Playwright) — outras dispatches cuidam disso em cima do que você criar.

## Onde trabalhar

Worktree já criado, branch `task/candle-f04-t04-2` a partir de `wave/candle-f04` (já contém
T-04.1 e T-04.3 mergeadas):
`/home/stharley/Documentos/projects/cripto-strategy-worktrees/candle-f04-t04-2`

Componente: `web`. Gate: `npm --prefix frontend run lint && npm --prefix frontend run typecheck`
+ `npm --prefix frontend run test:app`.

## Ao terminar

Commit(s) na branch da task (não dê push, não abra PR — o orquestrador consolida em
`wave/candle-f04`). Emita o QA Gate Context Block e pare — não rode `gate-record`, não aprove,
não avance estado. Se passar de ~150 turnos, escreva o estado aqui mesmo
(`docs/context/candle-real-e-eixo-unico/handoff/T-04.2.md`) e devolva.
