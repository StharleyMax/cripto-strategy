# Handoff — T-03.12

Task completa em `docs/context/candle-real-e-eixo-unico/tasks.toml` (buscar `id = "T-03.12"`,
~linha 546). Refs: plano 03 item 3.8, CLAUDE.md §Design — autonomia delegada com gate.

## O que esta task decide, literal (achado no próprio código pela dispatch anterior)

`frontend/src/app/symbol/SymbolClient.tsx`, docstring de `TimeframeBar` (~linha 2397), já
nomeia esta task como dona de 2 decisões em aberto:

1. **Padrão de interação final da barra**: hoje é `role="group"` + `aria-pressed` (toggle-
   button group), NÃO `role="radiogroup"` + `aria-checked` com roving-tabindex — decisão
   provisória, "forma decision this task does not own". Decida e implemente a forma final.
2. **Marca visível de cobertura parcial** (regime A de T-03.4 — Σ/max/min, viés unilateral):
   o backend já serve `panel.coverage.{earliest_bucket_ms,latest_bucket_ms,source_floor_ms}`
   (T-03.6) e `{present, expected}` por bucket (T-03.4), mas **nada no front ainda renderiza
   essa marca** — confirmado por grep, só comentários citam os campos. É requisito de P-B
   (ADR-040/D3) que a cobertura parcial não seja servida "sem marca visível".

## Ciclo obrigatório — gera → critica → itera (CLAUDE.md §Design)

Você tem autonomia de decisão de UI/UX **desde que o `ux-ui-mastery` concorde**. Implemente sua
melhor decisão para as 2 questões acima, documente o argumento (por que essa forma, não outra),
então **pare e devolva** — o orquestrador principal despacha o gate `ux-ui-mastery` contra o
que você fez. Se vier `NEEDS_FIX`, você será re-despachado com as ações; não se auto-aprove.

## Onde trabalhar

Worktree já criado, branch `task/candle-f03-t03-12` a partir da ponta atual de
`wave/candle-f03`: `/home/stharley/Documentos/projects/cripto-strategy-worktrees/candle-f03-t03-12`
`node_modules` (cópia real, não symlink — Turbopack recusa symlink cruzando a raiz do
filesystem), `.venv` e `data/` já linkados.

Componente: `web`. Consulte `docs/product/STITCH_CONTEXT.md`, `DESIGN_SYSTEM.md` §1.2 (papéis
de cor `action`/`surface`/`provenance`, citados no comentário atual) e o registro shadcn.
Rode `npm run dev` para ver a barra ao vivo — não decida às cegas.

## Ao terminar

Commit(s) na sua branch (não dê push, não abra PR). Emita o QA Gate Context Block com o
argumento de design (não só "feito"), e pare — o gate `ux-ui-mastery` vem depois de você, não
dentro da sua dispatch.
