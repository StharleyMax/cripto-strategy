# Handoff — T-03.10

Task completa em `docs/context/candle-real-e-eixo-unico/tasks.toml` (buscar `id = "T-03.10"`,
~linha 527). Leia o bloco inteiro — refs, [Q8], [M-6].

## Escopo desta dispatch

Só T-03.10: a escala log10 do volume sob TF variável — continua, até prova em contrário
(quem decide se muda é o design_gate/ux-ui-mastery, não você).

⚠️ **Contexto crítico de T-03.9 (já mergeada):** a barra de TF hoje só atualiza estado LOCAL
de seleção — o clique NÃO dispara refetch de `/series-history` para os 6 painéis ainda
(decisão documentada em `docs/context/candle-real-e-eixo-unico/gates/T-03.9-builder.md`,
adiada porque a marca de cobertura parcial de T-03.4 não estava mergeada na hora — T-03.4
JÁ ESTÁ mergeada agora). Se o escopo desta task exigir que o volume escale de fato sob TF
diferente de 1m na tela, você provavelmente precisa religar esse fetch — avalie e documente
a decisão explicitamente no seu relatório, não deixe implícito.

Não implemente T-03.11 (Playwright) nem T-03.12 (veredito ux-ui-mastery) — dispatches futuras.

## Onde trabalhar

Worktree já criado, branch `task/candle-f03-t03-10` a partir da ponta atual de
`wave/candle-f03`: `/home/stharley/Documentos/projects/cripto-strategy-worktrees/candle-f03-t03-10`
`node_modules` e `data/` já linkados — não rode bootstrap.

Componente: `web`. Pointer: `.claude/agents/frontend-builder.md`.

⚠️ Há OUTRA dispatch rodando em paralelo agora (T-03.2, sentimento) — sem overlap esperado.

## Ao terminar

Commit(s) na sua branch (não dê push, não abra PR). Emita o QA Gate Context Block e pare.
Se passar de ~150 turnos, escreva o estado neste arquivo e devolva.
