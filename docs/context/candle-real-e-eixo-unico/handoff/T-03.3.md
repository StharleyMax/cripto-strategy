# Handoff — T-03.3

Task completa em `docs/context/candle-real-e-eixo-unico/tasks.toml` (buscar `id = "T-03.3"`,
~linha 407). Leia o bloco inteiro — refs, DoD, baseline de `series_history.py:48/180-183`.

## Escopo desta dispatch

Só T-03.3: `SUPPORTED_INTERVAL` vira `{1m,5m,15m,1h,4h}`, tudo fora continua 422.
Não implemente outras tasks da fase 03 — T-03.1 (reduce_bucket em
`backend/src/modules/sentimento/domain/series_reduction.py`) já está na sua branch, pronta
para consumo; T-03.2/03.4/03.5/03.6/03.7/03.8 são dispatches paralelas ou futuras, não sua.

## Onde trabalhar

Worktree já criado, branch `task/candle-f03-t03-3` a partir da ponta atual de
`wave/candle-f03` (já inclui T-03.1):
`/home/stharley/Documentos/projects/cripto-strategy-worktrees/candle-f03-t03-3`

Componente: `sentimento`. Gate: `bash backend/scripts/lint.sh` + `bash backend/scripts/test.sh`.

⚠️ Há OUTRA dispatch rodando em paralelo agora (T-03.5, worktree irmão
`candle-f03-t03-5`, branch própria) — ela não toca `series_history.py` nem o conjunto de
intervalos, então não há conflito esperado, mas não assuma nada sobre o estado dela.

## Ao terminar

Commit(s) na sua branch (não dê push, não abra PR). Emita o QA Gate Context Block e pare.
Se passar de ~150 turnos, escreva o estado neste arquivo e devolva.
