# Handoff — T-01.5 (CST-201) · backfill até 90 dias, com a pegada de disco MEDIDA

`[DECISÃO-OWNER: 2026-09-19, escolha entre alternativas apresentadas]` — **teto de 90 dias**, a favor
do que a SPEC já declarava (`D5`). Literal do owner: *"90 dias, como a SPEC declara"*.

## O que esta task é

Plano `docs/plans/SPEC-008-candle-real-e-eixo-unico/01_vela.md` item **1.6** + **DoD 6**. A fusão é
deliberada: `pg_total_relation_size('md.series')` antes/depois **só existe se o backfill rodar** — é a
MESMA execução que produz os dois números.

## Fronteira — declarada nos DOIS eixos

Roda em paralelo com **T-01.10** (`web`, veredito de design). Não há interseção prevista, mas declare:

- **teu**: `backend/src/modules/sentimento/**`, `backend/tests/sentimento/**`, `deploy/**` (se o cron
  for declarado lá), `docs/context/candle-real-e-eixo-unico/gates/T-01.5-builder.md`
- **NÃO teu**: `frontend/**`, `docs/product/**`, qualquer arquivo sob `charts`

Se precisares tocar algo fora disso, **pare e devolva** em vez de tocar — o lote 2 já pagou o preço de
uma fronteira declarada num eixo só (conflito `UU` em `test_series_catalog_use_case.py`).

## Restrições que não são negociáveis

1. ⛔ **Nunca serviço de vida longa** (`ADR-027/D1`). One-shot ou cron. Um daemon reprova a task.
2. ⛔ **Zero chamada nova à Binance por candle já pago** (DoD 5, `RNF-4`) — o array de 12 campos de
   `/fapi/v1/klines` já traz OHLC. O backfill legitimamente chama `/fapi/v1/klines` para a história;
   o que **morde** é uma segunda requisição para o mesmo bucket já coletado.
3. **`[P-seed]` NÃO é violado** — backfill lido da ORIGEM não é dado sintético semeado. **Mas** ele
   escreve no **Postgres compartilhado que serve a tela do owner**. Não invente valor, não use símbolo
   de teste, não deixe resíduo.
4. **Sem chave em documento, nunca.** Referencie só como `$COINALYZE_API_KEY` / o que o `.env` expõe.

## A previsão a bater (e o número que a fecha)

`90d × 1440 × 4 séries × 4 símbolos = 2.073.600 linhas` `[INFERRED: aritmética, SPEC-008 §3.6]`.
O **byte/linha é `[NÃO MEDIDO]`** e ESTA medição é a que o fecha. Estimativa prévia, a confirmar ou
derrubar: 99 B/linha → 0,21 GB (~0,41 GB com índice); 300 B/linha → 0,62 GB (~1,24 GB).

⚠️ **Disco em 94%, 16 GB livres** `[MEDIDO 2026-09-19]`. Volumes locais do Docker somam 1,98 GB. Se a
projeção durante a execução apontar para mais de ~3 GB, **pare, escreva o número medido e devolva** —
a decisão de podar imagem Docker é do owner, não tua.

## O que esta task destrava

Até ela rodar, a vela **não aparece na tela do owner**: a API de produção devolve `n_entries=60` com
zero `klines_ohlc`, e o painel publica `price_last_reading:absent`. `T-01.8` já provou por pixel que a
vela monta quando o dado existe (corpo 22,23/57,81/71,15 px, pavio 88,93/71,15/80,04 px, `n=3` buckets).

## Medições já feitas nesta fase (não refaça)

- `T-01.4` fixou o SINAL do bucket: o bucket é o que **TERMINA** em `ceil(t/B)*B`.
- `T-01.3` levou o laço de pares de `build_klines_to_rows` de 2 para 6 tuplas.
- `T-01.6` (mergeado, **não deployado**) serve as 4 entradas no `/api/v1/series-catalog`.
- `verified_by` entra no `sha256` da chave: **um caractere divergente devolve `200` com `n_points=0`**,
  não `422`. Se a série vier vazia, suspeite da chave antes de suspeitar do backfill.

## Escalado pelo `/architect`, fora do escopo desta task mas adjacente

`[M-9]` — `klines_volume` armazenado **subestima a origem** (−2,2% a −4,5%, `pos=0` em 4/4, `n=960`):
snapshot intrabar gravado como `final_only`. A vela anda no mesmo coletor. **A fase 01 ganhou DoD-9
para DETECTAR sem propagar** — se o backfill expuser o mesmo viés, MEÇA e REPORTE; não conserte aqui.
