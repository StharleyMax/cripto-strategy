# Handoff — T-05.11

Task completa em `docs/context/candle-real-e-eixo-unico/tasks.toml` (buscar `id = "T-05.11"`,
~linha 847). Componente `docs` — sem código de produção, sem teste.

## O que declarar, e por quê

`tasks.toml` (linhas ~49-51) já registra a ausência:

> *"mecanismo de indisponibilidade POR TF — nasceria sem poder disparar (as duas grades nativas
> são 1min e 5min; o TF mais fino é 5m). Declarado com gatilho em T-05.11."*

Contexto: `T-05.5`/`T-05.6` (já mergeadas nesta branch) entregaram os três estados de cobertura
POR PAINEL (`absent`/`not-loaded`/`beyond-coverage`) e o badge único de indisponibilidade. O que
**não** existe, e não é esquecimento: um mecanismo que trate a indisponibilidade como variando
**por timeframe** (ex.: "OI disponível em 5m mas não em 1h"). A razão está no próprio dado: hoje
as únicas grades nativas são `1m` e `5m` (`ADR-036`), e o TF mais fino oferecido na UI é `5m` — um
mecanismo de indisponibilidade-por-TF nasceria sem NENHUM caso onde pudesse disparar. Construí-lo
agora seria código morto por construção.

## Escopo desta dispatch

Escreva a declaração formal, no lugar certo (provavelmente uma seção nova em
`docs/plans/SPEC-008-candle-real-e-eixo-unico/05_historia_sob_demanda.md`, ou onde o padrão do
repo já registra "NÃO-CONSTRUÍDO" — veja `docs/plans/SPEC-008-candle-real-e-eixo-unico/04_oi_honesto.md`
e outras fases para o precedente de prosa "o que esta fase NÃO decide"), com:

1. O que não foi construído (mecanismo de indisponibilidade por TF).
2. Por quê (as duas grades nativas hoje, 1m/5m, tornam o mecanismo impossível de disparar).
3. ⛔ **O gatilho nomeado de reabertura** — literal, seguindo o padrão de outras "lacunas
   conhecidas" deste repositório (ex.: `CLAUDE.md` §Idioma de identificador, linha 12 da tabela,
   que teve gatilho nomeado e endereço): a pergunta volta à mesa quando uma grade nativa mais
   grossa que `5m` entrar no catálogo (ex.: um provider/série cuja menor resolução nativa seja
   `15m` ou `1h`) — nomeie ISSO como o gatilho, com o dono (provavelmente `/architect` da feature
   que introduzir essa nova grade, não esta).

Não é uma 13ª/nova regra normativa fora de escopo — é prosa adjacente ao plano da fase 05,
documentando uma ausência deliberada, mesmo padrão que `CLAUDE.md` já usa em várias seções.

## Onde trabalhar

Worktree já criado, branch `task/candle-f05-t05-11` a partir de `wave/candle-f05` (contém T-05.0
a T-05.9 mergeadas): `/home/stharley/Documentos/projects/cripto-strategy-worktrees/candle-f05-t05-11`

Gate: nenhum lint/teste de código (task de docs) — só revise a prosa você mesmo antes de commitar.

## Ao terminar

Commit na branch da task (não dê push, não abra PR). Emita o QA Gate Context Block e pare — não
rode `gate-record`, não aprove, não avance estado.
