# Handoff — T-03.5

Task completa em `docs/context/candle-real-e-eixo-unico/tasks.toml` (buscar `id = "T-03.5"`,
~linha 437). Leia o bloco inteiro — refs, DoD, o motivo numérico da allowlist de 1 elemento.

## Escopo desta dispatch

Só T-03.5: `(RATIO, POINT) = last` sob ALLOWLIST DE METRIC de 1 elemento
(`count_long_short_ratio`) — nunca por `nature`. Não implemente outras tasks da fase 03 —
T-03.1 (`reduce_bucket`/`REDUCTION_TABLE` em
`backend/src/modules/sentimento/domain/series_reduction.py`) já está na sua branch; o
docstring do módulo já nomeia este limite (gating do allowlist) como não coberto por T-03.1.

## Onde trabalhar

Worktree já criado, branch `task/candle-f03-t03-5` a partir da ponta atual de
`wave/candle-f03` (já inclui T-03.1):
`/home/stharley/Documentos/projects/cripto-strategy-worktrees/candle-f03-t03-5`

Componente: `sentimento`. Gate: `bash backend/scripts/lint.sh` + `bash backend/scripts/test.sh`.

⚠️ Há OUTRA dispatch rodando em paralelo agora (T-03.3, worktree irmão
`candle-f03-t03-3`, branch própria, mexe em `series_history.py`/`SUPPORTED_INTERVAL`) — sem
sobreposição esperada com o gating de metric, mas não assuma nada sobre o estado dela.

## Ao terminar

Commit(s) na sua branch (não dê push, não abra PR). Emita o QA Gate Context Block e pare.
Se passar de ~150 turnos, escreva o estado neste arquivo e devolva.
