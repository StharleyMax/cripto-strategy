# Handoff — T-05.9

Task completa em `docs/context/candle-real-e-eixo-unico/tasks.toml` (buscar `id = "T-05.9"`,
~linha 817). Plano: `docs/plans/SPEC-008-candle-real-e-eixo-unico/05_historia_sob_demanda.md`,
DoD verificável item 7 (⭐ "Teto de latência da história sob demanda: 400 ms para a barra nova
aparecer").

## Escopo desta dispatch

`T-05.2` (já mergeada nesta branch) implementou o paginador (`use-history-pager.ts`,
`browser-series-history-client.ts`, fetch direto do browser contra o FastAPI, teto de ~5.000
slots). Esta task mede, contra o app real via Playwright, o tempo do gatilho de paginação até a
barra nova estar **desenhada** — não até a resposta HTTP chegar.

`[DECISÃO-OWNER: 2026-09-19]`: **p95 ≤ 400 ms**, `n ≥ 10` paginações disparadas por arrasto.
**Morde** com `p95 > 400 ms`, e morde **também** se o assert for de chegada da resposta em vez de
pixel (`MEMORY.md`: "Assert de DOM não prova pixel" — aqui a diferença é o `setData`).

⛔ **Os dois tetos compõem, e isso é parte do DoD**: a paginação NÃO pode rodar dentro do quadro
de pan (teto de 16ms da fase 02, `T-02.7`). `setData` de 129.600 pontos × 6 painéis custa 752,6ms
medido (`D-C3.5`) — é por isso que o teto de ~5.000 slots por grade existe. Se seu teste medir um
pan durante paginação estourando os 16ms da fase 02, é sintoma de que a paginação entrou no
quadro — registre como achado, não regride para "consertar" fora do escopo desta task.

Novo spec Playwright, seguindo a numeração sequencial e a disciplina `E2E-FACT` de
`frontend/e2e/` (ex.: `17-teto-latencia-eixo.spec.ts` para o teto irmão da fase 02,
`18-tf-refetch-e-ablacao.spec.ts`, `19-oi-provenance-ablacao-e-ascii.spec.ts`).

Não dependa de T-05.5/T-05.6/T-05.7 (estados nomeados) — esta task mede a MECÂNICA de paginação
que T-05.2 já entrega, não a UX de indisponibilidade.

## Onde trabalhar

Worktree já criado, branch `task/candle-f05-t05-9` a partir de `wave/candle-f05` (contém T-05.0
a T-05.4 mergeadas): `/home/stharley/Documentos/projects/cripto-strategy-worktrees/candle-f05-t05-9`

Componente: `web`. Gate: lint + typecheck + `test:app` + o spec Playwright novo isolado (a suíte
e2e inteira é cara — rode pelo menos o spec novo, `bash scripts/e2e-env.sh` conforme os specs
existentes).

⚠️ Roda em paralelo com T-05.5 (contrato dos três estados, worktree irmão
`candle-f05-t05-5`) — sem sobreposição de arquivo esperada (você cria um `.spec.ts` novo em
`frontend/e2e/`). Se notar sobreposição real, pare e registre aqui.

## Ao terminar

Commit(s) na branch da task (não dê push, não abra PR). Emita o QA Gate Context Block e pare —
não rode `gate-record`, não aprove, não avance estado. Se passar de ~150 turnos, escreva o estado
aqui mesmo (`docs/context/candle-real-e-eixo-unico/handoff/T-05.9.md`) e devolva.
