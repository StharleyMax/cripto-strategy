# Handoff — T-04.4

Task completa em `docs/context/candle-real-e-eixo-unico/tasks.toml` (buscar `id = "T-04.4"`,
~linha 613). Leia o bloco inteiro lá — refs, DoD, item 4.4 do plano.

## Escopo desta dispatch

Só T-04.4: os demais painéis herdam o conserto de T-04.3 — a fábrica de chave `data-fact`, não
só o painel de preço. `T-04.3` (já mergeada nesta branch) separou `factKey` (ASCII, estável) de
`label` (pt-BR) em `LiveRow`/`SymbolClient.tsx`, usando os nomes que `liveUrls` já declara
(`price`/`oi`/`cvd`, `page.tsx:853-860`) — leia esse diff primeiro (`git log -p
task/candle-f04-t04-3` a partir do commit `8bb81ba` se precisar do histórico). O defeito é da
fábrica, não do painel de preço: confirme que **todo** `data-fact="live_..."` publicado na página
(não só os 3 já cobertos por `LiveRow`) usa a chave ASCII derivada, nunca a string visível.
Plano completo: `docs/plans/SPEC-008-candle-real-e-eixo-unico/04_oi_honesto.md` (DoD 3: zero chave
de máquina não-ASCII — `grep -P '[^\x00-\x7F]'` sobre TODOS os `data-fact="..."` da página tem
que dar 0, universo completo desta vez, não só `LiveRow`).

Não implemente T-04.5 (ablação completa CA-10 + assert de posição via Playwright, DoD final da
fase) — outra dispatch cuida disso em cima do que você criar.

## Onde trabalhar

Worktree já criado, branch `task/candle-f04-t04-4` a partir de `wave/candle-f04` (já contém
T-04.1 e T-04.3 mergeadas):
`/home/stharley/Documentos/projects/cripto-strategy-worktrees/candle-f04-t04-4`

Componente: `web`. Gate: `npm --prefix frontend run lint && npm --prefix frontend run typecheck`
+ `npm --prefix frontend run test:app`. Se quiser confirmar contra o app real (`curl` do DoD-3),
suba `npm --prefix frontend run dev` numa porta livre deste worktree.

## Ao terminar

Commit(s) na branch da task (não dê push, não abra PR — o orquestrador consolida em
`wave/candle-f04`). Emita o QA Gate Context Block e pare — não rode `gate-record`, não aprove,
não avance estado. Se passar de ~150 turnos, escreva o estado aqui mesmo
(`docs/context/candle-real-e-eixo-unico/handoff/T-04.4.md`) e devolva.
