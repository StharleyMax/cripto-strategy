# Estado de T-01.5 — o código fechou; a execução de 90 dias estava EM VOO na entrega

`[ESCRITO 2026-09-19 ~20:40 UTC]` · branch `task/candle-f01-t01-5` · relatório completo em
`docs/context/candle-real-e-eixo-unico/gates/T-01.5-builder.md`

## O que já está commitado e verde

`backend/src/modules/sentimento/infra/klines_backfill_cli.py` (novo, one-shot),
`backend/tests/sentimento/test_klines_backfill_cli.py` (18 testes), os 3 guardas de registro
atualizados com motivo, e o bloco de documentação em `deploy/compose.yml`.

## O que FALTA, e é UM comando

O walk de 90 dias foi disparado às **20:26:05 UTC** e tem ETA de **~2,7 h**
(`3.110.400 linhas / 320 linhas/s`, a vazão MEDIDA do escritor único). Ele é seguro sozinho: a
contrapressão o faz esperar o escritor, e `md.series` é append-only.

**Para fechar o `DoD 6`, rode a medição DEPOIS e compare com a linha ANTES do relatório §3:**

```bash
docker exec deploy-postgres-1 psql -U cripto_strategy -d cripto_strategy -Atc \
  "select (select count(*) from md.series),
          pg_total_relation_size('md.series'),
          hypertable_size('md.series'), now();"
```

⛔ **Leia `hypertable_size`, não `pg_total_relation_size`.** `md.series` é hypertable TimescaleDB e
o comando literal do `DoD 6` devolve **24.576 B** independente do que exista (§2 do relatório).

**A linha ANTES, para subtrair** `[MEDIDO 2026-09-19 20:21:22 UTC]`:
`rows_total = 2.668.877` · `rows_ohlc4 = 0` · `pg_total_relation_size = 24.576` ·
`hypertable_size = 1.281.269.760`.

**A previsão a bater** (byte/linha MEDIDO = 496 B, `n=19.106`): **+1,54 GB** para as 6 identidades
(`3.110.400` linhas), das quais **+1,03 GB** são as 4 chaves `klines_ohlc` (`2.073.600` linhas).
Se o `hypertable_size` final divergir disso em mais de ~10%, o byte/linha mudou e o número novo
manda.

## Como acompanhar / abortar

```bash
docker ps --filter name=klines-backfill-90d          # o container do walk
docker rm -f klines-backfill-90d                     # abortar: seguro, o escrito fica
```

## O que NÃO fecha nesta task, e por quê

`DoD 1` e `DoD 3` continuam abertos porque a **API em produção ainda não tem `T-01.6`**:
`/api/v1/series-catalog` serve `n_entries=60` sem nenhuma `klines_ohlc`, então o painel publica
`price_last_reading:absent` mesmo com o dado em `md.series` `[MEDIDO 2026-09-19 20:39 UTC]`.
Falta **redeploy**, que é ato de `infra`/owner. Ver §9 do relatório.

## Escalado

Duas sessões da **API** em `idle in transaction` há **05h26** e **04h08** segurando relação em
`md.ingest_run`/`md.series`. Enquanto existirem, qualquer DDL futuro bloqueia e arrasta o escritor
único junto — foi o que aconteceu por 2m54s nesta task (§4 do relatório). Componente `infra`/`api`.
