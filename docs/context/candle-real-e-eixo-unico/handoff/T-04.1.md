# Handoff — T-04.1

Task completa em `docs/context/candle-real-e-eixo-unico/tasks.toml` (buscar `id = "T-04.1"`,
~linha 568). Leia o bloco inteiro lá — refs, DoD, item 4.1 do plano.

## Escopo desta dispatch

Só T-04.1: o rótulo do painel de OI passa a soletrar **grandeza · universo · coorte**, lidos do
`SeriesKey` servido pelo backend — nunca escritos à mão no componente. Plano completo:
`docs/plans/SPEC-008-candle-real-e-eixo-unico/04_oi_honesto.md` (DoD 1 e 2 são os que valem para
esta task: `data-fact="oi_provenance:..."` com os 3 termos, e ablação de derivação — trocar a
chave no catálogo de teste muda o rótulo sem tocar no componente).

Não implemente T-04.2 em diante — outra dispatch cuida do envelope de proveniência em cima do
que você criar aqui. Não toque na chave de máquina `data-fact="live_..."` (isso é T-04.3, rodando
em paralelo num worktree irmão — mesmo arquivo `SymbolClient.tsx`, função diferente: você mexe no
rótulo/label do OI, a outra dispatch mexe em como o `data-fact` é montado a partir do label. Se
notar sobreposição real de linhas, registre no handoff e sinalize no QA Gate Context Block —
o orquestrador resolve o merge).

## Onde trabalhar

Worktree já criado, branch `task/candle-f04-t04-1` a partir de `wave/candle-f04`:
`/home/stharley/Documentos/projects/cripto-strategy-worktrees/candle-f04-t04-1`

Componente: `web`. Gate: `npm --prefix frontend run lint && npm --prefix frontend run typecheck`
+ suíte relevante (`harness policy --key test_cmd` para o componente `web`).

## Ao terminar

Commit(s) na branch da task (não dê push, não abra PR — o orquestrador consolida em
`wave/candle-f04`). Emita o QA Gate Context Block e pare — não rode `gate-record`, não aprove,
não avance estado. Se passar de ~150 turnos, escreva o estado aqui mesmo
(`docs/context/candle-real-e-eixo-unico/handoff/T-04.1.md`) e devolva.
