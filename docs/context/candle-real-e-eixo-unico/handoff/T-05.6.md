# Handoff — T-05.6

Task completa em `docs/context/candle-real-e-eixo-unico/tasks.toml` (buscar `id = "T-05.6"`,
~linha 767). Plano: `docs/plans/SPEC-008-candle-real-e-eixo-unico/05_historia_sob_demanda.md`,
DoD verificável itens 2 e 3.

## O que já existe (T-05.5, T-05.7, já mergeadas nesta branch)

- `frontend/src/app/symbol/slot-coverage.ts`: `classifySlotCoverage`/`SlotCoverageState`
  (`"absent" | "not-loaded" | "beyond-coverage"`, `D-C3.6`) e `combineHistoryCoverage`.
- `use-history-pager.ts` já para de pedir página quando o horizonte declarado é alcançado
  (`T-05.7`).

Esta task é o PIXEL: quando um painel entra em `beyond-coverage`, mostrar o estado nomeado —
**um badge só por painel**, e **nada do que já existe é removido**.

## Padrão visual a reusar (não invente um novo)

`SymbolClient.tsx` já tem dois precedentes de "estado nomeado" que resolvem exatamente esse
problema noutros painéis — leia os dois antes de desenhar algo novo:
- `PartialCoverageMark`/`PartialCoverageGlyph` (~linha 679-722): losango vazado + palavra,
  reusado em volume/CVD/liquidação (`~1028`, `~1518`, `~1774`).
- `LongShortIntegrityBadge` (~linha 1942): mesmo critério de "a palavra ao lado carrega a
  mensagem inteira".

DoD do plano:
2. **A parede assimétrica aparece e é NOMEADA.** Arrastando além de ~30 dias em `4h`: o painel de
   Preço **continua** com barras **e** os painéis de OI e long/short mostram o estado nomeado —
   os dois fatos no MESMO assert, `n=2` painéis. Morde se o painel apenas esvaziar em silêncio.
3. ⛔ **Ablação de P5.** Removido o estado nomeado, o painel volta a esvaziar sem dizer nada —
   captura antes/depois. Se o pixel não muda sob ablação, não era o estado nomeado.

Chave de máquina: `data-fact="<panel>_coverage:beyond"` ou equivalente, ASCII, derivada do
`SlotCoverageState` — nunca da microcopy (mesma disciplina de T-04.3/T-04.4: separe a string
visível pt-BR da chave). Microcopy pt-BR (linha 8 da tabela de fronteira do `CLAUDE.md`).

**Um badge só por painel** — não um por slot/barra. Se o painel tem mistura de `not-loaded` e
`beyond-coverage` na janela visível, decida qual estado o badge do painel reporta (provavelmente
`beyond-coverage` vence, já que é o estado mais definitivo — mas isso é sua decisão de design,
documente o porquê).

Não implemente T-05.8 (Playwright de 3 arrastos + ablação completa — outra dispatch, mais pesada,
que prova isso contra o app real) nem T-05.10 (veredito `ux-ui-mastery` — outra dispatch, depois
desta). Esta task entrega o componente + teste de contrato DOM, não o e2e completo.

## Onde trabalhar

Worktree já criado, branch `task/candle-f05-t05-6` a partir de `wave/candle-f05` (contém T-05.0
a T-05.5, T-05.7 mergeadas): `/home/stharley/Documentos/projects/cripto-strategy-worktrees/candle-f05-t05-6`

Componente: `web`. Gate: `npm --prefix frontend run lint && npm --prefix frontend run typecheck`
+ `npm --prefix frontend run test:app`.

⚠️ Roda em paralelo com T-05.9 (Playwright de latência, worktree irmão `candle-f05-t05-9`, ainda
em execução) — arquivos esperados diferentes (você mexe em `SymbolClient.tsx`, T-05.9 é spec e2e
novo). Se notar sobreposição real, pare e registre aqui.

## Ao terminar

Commit(s) na branch da task (não dê push, não abra PR). Emita o QA Gate Context Block e pare —
não rode `gate-record`, não aprove, não avance estado. Se passar de ~150 turnos, escreva o estado
aqui mesmo (`docs/context/candle-real-e-eixo-unico/handoff/T-05.6.md`) e devolva.
