# Handoff — T-01.11 (CST-207) · e2e Playwright contra o app REAL + ablação de P1

Plano `docs/plans/SPEC-008-candle-real-e-eixo-unico/01_vela.md` item **1.11** + **DoD 2/3/4**.

## ⭐ O estado de produção MUDOU — leia antes de medir qualquer coisa

O `api`, o `web` e o `collector` foram redeployados em 2026-09-19 ~21:1x UTC. O que era `[MEDIDO]`
no `tasks.toml` **está desatualizado a teu favor**:

| | `tasks.toml` dizia | agora |
|---|---|---|
| `data-fact="price_last_reading:…"` | `:absent` | **`:exact`** |
| velas montadas | (não existia) | `price_candles:56/5760` e subindo |
| `klines_ohlc` no catálogo | 0 | **16** (4 reduções × 4 símbolos) |

**A vela existe e tem faixa:** `4/4` buckets com `high > low`, **zero degenerada**, faixas de
`29,70` e `22,10` USDT `[MEDIDO pelo loop principal 2026-09-19 ~21:20 UTC]`.

## ⏳ O que ainda NÃO fecha, e o número é este

**`DoD-1` pede `≥ 500` pontos não-nulos por chave, `n=4` chaves. Agora: `56 / 5760` em cada.**
O backfill de 90 dias está **em voo** (container `klines-backfill-90d`, ~74%, do mais ANTIGO para o
mais novo), então a janela visível enche perto do FIM dele.

⛔ **NÃO fique em laço de espera** (R9 — `[MEDIDO: 114 laços, 3,68h, 116,4s cada]`). Construa o e2e
e a ablação primeiro. Se `≥500` não tiver chegado: escreva o estado em
`handoff/T-01.11-estado.md` e devolva com o teste pronto e **declarado como não-executado**.
⛔ **Nunca marque verde sem a medição.**

## Os critérios, e o que cada um morde

- **`CA-2`** (a vela tem faixa): sobre `n=500` buckets, `≥1` com `high > low`. **MORDE** quando toda
  barra tem `high == low` (a degenerada de novo, com dado real por trás) e **MORDE TAMBÉM** se
  `open == close` em 500 de 500 — seria o colapso dos quatro STOCK num só (`ADR-040/D2`).
- **`CA-3`**: `curl -s http://127.0.0.1:3000/symbol | grep -o 'data-fact="price_last_reading:[^"]*"'`
  → **não** `:absent`. **Já passa** — confirme, não presuma.
- **⛔ `CA-4` (ABLAÇÃO), o coração da task:** removido o produtor de preço, **corpo e pavio SOMEM**
  da tela. Captura antes/depois, **assert de POSIÇÃO**. Pixel que sobrevive à ablação estava
  desenhando outra coisa (a degenerada, ou o close sozinho).

## ⛔ Duas regras que já custaram caro aqui

1. **`[P-seed]`: NUNCA semeie o Postgres compartilhado.** Dado sintético de e2e já vazou para a tela
   real do owner. O banco que serve `127.0.0.1:3000` é o **de produção**.
2. **Assert de DOM NÃO prova pixel.** Elemento passa em tudo e não existe na tela. Foi medido aqui.
   O `e2e` atual tem 40 asserções e **nenhuma** olha forma de vela
   (`grep -rn 'candle\|vela' frontend/e2e/*.ts` → 1 linha, um comentário) — isto é teu trabalho.

## ⚠️ Contexto que muda como tu lês um `high` suspeito

A vela gravada **diverge da origem**: o intervalo não contém o low da Binance (erro de `+41,00` num
bucket medido), e buckets adjacentes vêm idênticos. Ver
`gates/ACHADO-vela-viva-diverge-da-origem.md`. **Isso é da `T-01.7`, não tua** — mas se um assert
teu falhar por valor estranho, a causa pode ser essa, e não o teu teste.

## Fronteira — declarada nos DOIS eixos

Roda em paralelo com **T-01.7** (`sentimento`, outra worktree).
- **teu**: `frontend/e2e/**`, `frontend/src/app/symbol/**` (só se o assert exigir marcador novo),
  o relatório em `gates/`
- **NÃO teu**: `backend/**`, `deploy/**`, `docs/product/**`; `docs/INDEX.md` só append
