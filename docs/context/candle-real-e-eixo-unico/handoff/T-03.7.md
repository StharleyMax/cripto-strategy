# Handoff — T-03.7

Task completa em `docs/context/candle-real-e-eixo-unico/tasks.toml` (buscar `id = "T-03.7"`,
~linha 471). Leia o bloco inteiro — as 4 camadas ((a) guarda de não-degenerescência, (b)
fixture da Binance com os números literais, (c) matriz 8x5=20 asserções, (d) ablação de
cobertura 81/240), e por que CA-8 (T-03.8, já mergeada) morde só 1 de 20 trocas.

## Escopo desta dispatch

Só T-03.7: CA-8′ completo, as 4 camadas. `reduce_bucket` (T-03.1), `SUPPORTED_INTERVAL`+
reagregação na rota (T-03.3), `{present,expected}` (T-03.4) já estão na sua branch — a fixture
da Binade do item (b) e a ablação do item (d) consomem exatamente esse par.

## Onde trabalhar

Worktree já criado, branch `task/candle-f03-t03-7` a partir da ponta atual de
`wave/candle-f03`: `/home/stharley/Documentos/projects/cripto-strategy-worktrees/candle-f03-t03-7`
`.venv` e `data/` já linkados.

Componente: `sentimento`. Gate: `bash backend/scripts/lint.sh` + `bash backend/scripts/test.sh`.

⚠️ Há OUTRA dispatch rodando em paralelo agora (T-03.11, web/frontend) — sem overlap esperado.

## Ao terminar

Commit(s) na sua branch (não dê push, não abra PR). Emita o QA Gate Context Block e pare.
Se passar de ~150 turnos, escreva o estado neste arquivo e devolva.
