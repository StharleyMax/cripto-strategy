# Handoff — T-03.8

Task completa em `docs/context/candle-real-e-eixo-unico/tasks.toml` (buscar `id = "T-03.8"`,
~linha 449). Leia o bloco inteiro — refs, CA-8, RN-3, RF-7.

## Escopo desta dispatch

Só T-03.8: testes diferenciais — STOCK NÃO soma (senão 12x o OI real) e FLOW soma. `reduce_bucket`
(T-03.1) já está na sua branch, é o que você está testando; não altere sua implementação.

## Onde trabalhar

Worktree já criado, branch `task/candle-f03-t03-8` a partir da ponta atual de
`wave/candle-f03`: `/home/stharley/Documentos/projects/cripto-strategy-worktrees/candle-f03-t03-8`
`.venv` e `data/` já linkados.

Componente: `sentimento`. Gate: `bash backend/scripts/lint.sh` + `bash backend/scripts/test.sh`.

⚠️ Há OUTRA dispatch rodando em paralelo agora (T-03.6, mesmo componente, mexe em
`series_history_report.py`/`use_cases/series_history.py` — arquivos que você provavelmente
NÃO precisa tocar, já que T-03.8 é teste diferencial sobre `reduce_bucket` puro). Se notar que
precisa mexer nesses dois arquivos, pare e registre o conflito no seu relatório em vez de seguir.

## Ao terminar

Commit(s) na sua branch (não dê push, não abra PR). Emita o QA Gate Context Block e pare.
Se passar de ~150 turnos, escreva o estado neste arquivo e devolva.
