# Handoff — T-05.4

Task completa em `docs/context/candle-real-e-eixo-unico/tasks.toml` (buscar `id = "T-05.4"`,
~linha 736). Plano: `docs/plans/SPEC-008-candle-real-e-eixo-unico/05_historia_sob_demanda.md`,
item 5.3 e DoD verificável 4.

## Escopo desta dispatch

⛔ Além do teto de 90 dias, a rota `series-history` **RECUSA** — nunca serve `200` com
`rows: []`. `200` vazio ali é o `rc=0` ambíguo de `ADR-012`: indistinguível entre "não há dado" e
"o instrumento nunca alcançou". T-05.3 (já mergeada nesta branch) criou
`backend/src/modules/sentimento/domain/history_ceiling.py` com `MAX_HISTORY_DAYS`/`MAX_HISTORY_MS`
como single source of truth — importe de lá (é `domain`, a rota/`use_cases` pode importar; não
duplique o número).

DoD 4 do plano: requisição a `series-history` com janela iniciando antes de 90 dias → recusa
explícita. **Morde** com `200` vazio, `n = 2` casos (91 dias, 400 dias) — teste os dois, não só um.

## Onde trabalhar

Worktree já criado, branch `task/candle-f05-t05-4` a partir de `wave/candle-f05` (já contém
T-05.3 mergeada): `/home/stharley/Documentos/projects/cripto-strategy-worktrees/candle-f05-t05-4`

Componente: `sentimento`. Gate: `bash backend/scripts/lint.sh` + `bash backend/scripts/test.sh`.

⚠️ Roda em paralelo com T-05.0 (reabre `as_of_accessor.py`, worktree irmão
`candle-f05-t05-0`). Rotas diferentes do código — sem sobreposição esperada. Se notar
sobreposição real, pare e registre aqui em vez de resolver sozinho.

## Ao terminar

Commit(s) na branch da task (não dê push, não abra PR). Emita o QA Gate Context Block e pare —
não rode `gate-record`, não aprove, não avance estado. Se passar de ~150 turnos, escreva o estado
aqui mesmo (`docs/context/candle-real-e-eixo-unico/handoff/T-05.4.md`) e devolva.
