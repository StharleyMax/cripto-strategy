# Handoff — T-04.5

Task completa em `docs/context/candle-real-e-eixo-unico/tasks.toml` (buscar `id = "T-04.5"`,
~linha 626). Leia o bloco inteiro lá — refs, DoD, item da fase.

## Escopo desta dispatch

Fecha o DoD verificável inteiro da fase 04 (`docs/plans/SPEC-008-candle-real-e-eixo-unico/04_oi_honesto.md`,
seção "DoD verificável"), items 1-5, com prova **e2e Playwright contra o app real** — os builders
anteriores (T-04.1/T-04.2/T-04.3/T-04.4, já mergeadas nesta branch) cobriram cada um por unit
test/contrato; esta task é a integração final:

1. CA-9: `data-fact="oi_provenance:..."` presente com os 3 termos, contra o app real.
2. ⛔ Ablação CA-10: trocada a chave da série no catálogo de teste, o rótulo muda **sem tocar no
   componente** — prove isso via Playwright fixture (não só unit test; os unit tests de T-04.1
   já cobrem a função pura, esta task prova que o pixel muda).
3. Zero chave de máquina não-ASCII, universo COMPLETO da página (`grep -P '[^\x00-\x7F]'` sobre
   todos os `data-fact="..."`) — T-04.4 já auditou isso estaticamente; esta task confirma contra
   o app real rodando.
4. **O pixel está na tela** — assert de posição (visível, não só presente no DOM;
   `MEMORY.md`: "Assert de DOM não prova pixel" — não aceite `data-fact` presente com elemento
   fora da tela).
5. DoD 5 (nada de fonte mudou) já está coberto por T-04.2 (`openInterestAdr036D2Violations`) —
   não precisa reprovar, só não regrida.

Novo spec Playwright, seguindo o padrão dos existentes em `frontend/e2e/` (ex.:
`17-teto-latencia-eixo.spec.ts`, `18-tf-refetch-e-ablacao.spec.ts`) — numeração sequencial,
mesma disciplina de `E2E-FACT` no stdout.

## Onde trabalhar

Worktree já criado, branch `task/candle-f04-t04-5` a partir de `wave/candle-f04` (já contém
T-04.1 a T-04.4 mergeadas):
`/home/stharley/Documentos/projects/cripto-strategy-worktrees/candle-f04-t04-5`

Componente: `web`. Gate: lint + typecheck + `test:app` + a suíte e2e Playwright relevante (rode
pelo menos o spec novo isolado; a suíte inteira e2e é cara — siga a mesma disciplina batida nas
fases anteriores desta feature, ver `frontend/e2e/README.md` se existir ou o padrão dos specs
17/18 para como subir o servidor de teste).

## Ao terminar

Commit(s) na branch da task (não dê push, não abra PR — o orquestrador consolida em
`wave/candle-f04`). Emita o QA Gate Context Block e pare — não rode `gate-record`, não aprove,
não avance estado. Se passar de ~150 turnos, escreva o estado aqui mesmo
(`docs/context/candle-real-e-eixo-unico/handoff/T-04.5.md`) e devolva.
