# Handoff — T-03.11

Task completa em `docs/context/candle-real-e-eixo-unico/tasks.toml` (buscar `id = "T-03.11"`,
~linha 537). Leia o bloco inteiro — DoD 6/7/8, CA-7, RN-4, ADR-037/D4, baseline de
`data-oi-wire-points="5760"` vs `data-oi-native-bars="1152"`.

## Contexto obrigatório — leia antes de escrever código

A barra de TF (T-03.9) e a escala de volume (T-03.10) **deliberadamente não ligaram** o clique
no TF a um refetch real de `/series-history` — ambas adiaram essa wiring esperando o campo
`panel.coverage` (T-03.6), que **já está mergeado agora**
(`docs/context/candle-real-e-eixo-unico/gates/T-03.6-builder.md`). Esta task (T-03.11) é onde
essa wiring finalmente acontece — sem ela, DoD 6 ("clicar 4h e ler a contagem de barras de CADA
UM dos 6 painéis") é inverificável. Leia
`docs/context/candle-real-e-eixo-unico/gates/T-03.9-builder.md` e
`.../T-03.10-builder.md` para o estado exato de onde parou.

⚠️ **Correção de um relatório anterior:** T-03.9 registrou "sem servidor Playwright nesta
instalação". Isso referia ao MCP de browser-automation, não ao Playwright do projeto — o
projeto TEM `@playwright/test` configurado (`frontend/playwright.config.ts`,
`npm run test:e2e`), browsers já em cache (`~/.cache/ms-playwright/chromium-1243`), e uma
suíte de 17 specs em `frontend/e2e/` (`01-...` a `17-teto-latencia-eixo.spec.ts`) no mesmo
padrão que você deve seguir. Sua spec nova é `frontend/e2e/18-<nome>.spec.ts`. Suba o dev
server (`npm run dev`) para rodar contra app real — não contra mock.

## Escopo desta dispatch

Só T-03.11: ligar TF→refetch dos 6 painéis (pré-requisito de fato para o resto), DoD 6
(contagem de barras dos 6 painéis muda com o TF), DoD 7 (ablação: volta a 1m, contagem volta),
DoD 8 (sob TF explícito, `wire-points` deixa de ser 5x `native-bars`, OU o relatório carrega o
veredito de upsampling de ADR-037/D4). Não implemente T-03.12 (veredito ux-ui-mastery).

## Onde trabalhar

Worktree já criado, branch `task/candle-f03-t03-11` a partir da ponta atual de
`wave/candle-f03`: `/home/stharley/Documentos/projects/cripto-strategy-worktrees/candle-f03-t03-11`
`node_modules` e `data/` já linkados.

Componente: `web`. Pointer: `.claude/agents/frontend-builder.md`.

⚠️ Há OUTRA dispatch rodando em paralelo agora (T-03.7, sentimento/backend) — sem overlap
esperado.

## Ao terminar

Commit(s) na sua branch (não dê push, não abra PR). Emita o QA Gate Context Block e pare.
Se passar de ~150 turnos, escreva o estado neste arquivo e devolva.
