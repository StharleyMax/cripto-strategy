# Handoff — T-01.7 (CST-203) · a vela armazenada contra a KLINE DA PRÓPRIA BINANCE

⭐ **Esta task já tem EVIDÊNCIA, não hipótese.** Leia primeiro:
`docs/context/candle-real-e-eixo-unico/gates/ACHADO-vela-viva-diverge-da-origem.md`.
O loop principal mediu, em 2026-09-19 ~21:20 UTC, **dois defeitos no dado vivo**. Teu trabalho é
transformar isso num assert que MORDE, não redescobrir.

## Os dois defeitos já medidos

**1 — buckets adjacentes byte a byte idênticos.** `20:25` == `20:26` e `21:13` == `21:14`, nas 4
reduções, ao centavo (`n=4` buckets, `2/2` pares). O mesmo kline sob dois `event_time`.

**2 — o intervalo gravado NÃO CONTÉM o low da origem.** Em `21:13` gravamos `[81001.00, 81023.10]`
contra `[80960.00, 81023.00]` da Binance — low **+41,00** acima do verdadeiro, e o `close` real
(`80966.60`) não aparece em nenhuma redução. ⚠️ A conclusão **independe** de qual chave é qual
redução.

## O universo do assert (do `tasks.toml`, colhido da ORIGEM)

`BTCUSDT`, bucket fechado **2026-09-18 12:00→16:00 UTC**, 240/240 minutos:
`first(open)=78.031,00` · `max(high)=81.156,80` · `min(low)=77.923,50` · `last(close)=80.688,70`.
**Auto-verificável pelo owner no gráfico da Binance** — é a melhor propriedade desta task, não a perca.

## ⛔ O que MORDE

Divergência de OPEN/HIGH/LOW/CLOSE além da tolerância **declarada por ti** (declare-a, com o motivo),
e **em particular viés unilateral** (`pos=0` em `n` buckets). Viés unilateral é assinatura de
snapshot intrabarra gravado como `final_only` — não é ruído.

## ⚠️ A pergunta que o achado deixou ABERTA, e é tua

`[NÃO MEDIDO]`: **qual caminho produziu o defeito.** O backfill (CLI) e o coletor compartilham
`build_klines_to_rows` (que `T-01.3` levou de 2 para 6 tuplas). Pode ser o compartilhado, ou só um
dos dois. **Separe os dois casos** — é a diferença entre um conserto e dois.

## ⏳ O dado da janela ainda está CHEGANDO — e isto muda teu plano

Quando isto foi escrito: a janela de `2026-09-18 12:00→16:00` devolvia **`0 / 240` pontos** nas 4
reduções, e a janela visível de 4 dias, **`56 / 5760`**. O backfill de 90 dias está **em voo**
(container `klines-backfill-90d`, ~74%, caminhando do mais ANTIGO para o mais novo), então a janela
do assert enche perto do FIM dele.

⛔ **NÃO fique em laço de espera** (R9 — `[MEDIDO: 114 laços, 3,68h]`). Construa o arnês de
comparação, os testes e o assert **primeiro**; consulte a cobertura quando precisar. Se o dado não
tiver chegado ao fim do teu trabalho: escreva o estado em
`docs/context/candle-real-e-eixo-unico/handoff/T-01.7-estado.md` e **devolva com o assert pronto e
NÃO executado** — declarado como tal. ⛔ **Nunca invente número nem marque verde sem a medição.**

## Como consultar a cobertura (a API está no ar)

```bash
curl -s "http://127.0.0.1:8000/api/v1/series-history?series_key_id=<id>&symbol=BTCUSDT\
&interval=1m&window_start_ms=<ms>&window_end_ms=<ms>&knowledge_time_ms=<ms>&bar_policy=final_only"
```
Os valores vêm como **string decimal** — use `Decimal`, nunca `float`. Campo do instante:
`event_time`. Ausência vem como `{"value": null, "absence": "SEM_PONTO"}`.

## ⛔ O que esta task NÃO faz

**NÃO conserta a causa-raiz** — é de `ADR-034` + `/architect` (`[M-9]`). Ela **impede a feature de
propagar o defeito em silêncio** para o preço: a diferença entre dívida declarada e defeito novo.
Se a correção parecer óbvia, **escreva a recomendação e devolva** — não a implemente aqui.

## Fronteira — declarada nos DOIS eixos

Roda em paralelo com **T-01.11** (`web`, e2e, outra worktree).
- **teu**: `backend/src/modules/sentimento/**`, `backend/tests/sentimento/**`, o relatório em `gates/`
- **NÃO teu**: `frontend/**`, `deploy/**`, `docs/product/**`; `docs/INDEX.md` só append
