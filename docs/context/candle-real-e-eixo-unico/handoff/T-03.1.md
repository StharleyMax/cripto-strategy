# Handoff — T-03.1

Task completa em `docs/context/candle-real-e-eixo-unico/tasks.toml` (buscar `id = "T-03.1"`,
~linha 379). Leia o bloco inteiro lá — refs, DoD, a tabela total de (nature, reduction).

## Escopo desta dispatch

Só T-03.1: `reduce(nature, reduction)` como função PURA sobre os 8 pares, num lugar só.
Não implemente T-03.2 em diante — outras dispatches cuidam delas em cima do que você criar.

## Onde trabalhar

Worktree já criado, branch `task/candle-f03-t03-1` a partir de `wave/candle-f03`:
`/home/stharley/Documentos/projects/cripto-strategy-worktrees/candle-f03-t03-1`

Componente: `sentimento`. Gate: `bash backend/scripts/lint.sh` + `bash backend/scripts/test.sh`
(`harness policy --key test_cmd`). Sem pointer de doutrina específico do papel/componente
(`harness policy --key agents.by_component.sentimento.builder` → campo ausente) — siga a skill
`/build` padrão.

## Ao terminar

Commit(s) na branch da task (não dê push, não abra PR — o orquestrador consolida em
`wave/candle-f03`). Emita o QA Gate Context Block e pare — não rode `gate-record`, não aprove,
não avance estado. Se passar de ~150 turnos, escreva o estado aqui mesmo
(`docs/context/candle-real-e-eixo-unico/handoff/T-03.1.md`) e devolva.
