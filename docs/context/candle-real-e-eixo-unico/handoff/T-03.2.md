# Handoff — T-03.2

Task completa em `docs/context/candle-real-e-eixo-unico/tasks.toml` (buscar `id = "T-03.2"`,
~linha 391). Leia o bloco inteiro — refs, DoD 4 (FUSÃO), o comando `curl` do DoD.

## Escopo desta dispatch

Só T-03.2: a função FALHA ALTO em par não coberto (já garantido por T-03.1 via
`UncoveredReductionPairError`), e o TESTE DE TOTALIDADE prova que os 8 pares têm função
declarada — inclusive contra `/series-catalog` ao vivo, conforme o DoD.
T-03.1 (`reduce_bucket`/`REDUCTION_TABLE`), T-03.3, T-03.4, T-03.5 já estão na sua branch.

Não implemente T-03.6/03.7/03.8 — dispatches paralelas ou futuras.

## Onde trabalhar

Worktree já criado, branch `task/candle-f03-t03-2` a partir da ponta atual de
`wave/candle-f03`: `/home/stharley/Documentos/projects/cripto-strategy-worktrees/candle-f03-t03-2`
`.venv` e `data/` já linkados — não rode bootstrap.

Componente: `sentimento`. Gate: `bash backend/scripts/lint.sh` + `bash backend/scripts/test.sh`.

⚠️ Há OUTRA dispatch rodando em paralelo agora (T-03.10, web) — sem overlap esperado.

## Ao terminar

Commit(s) na sua branch (não dê push, não abra PR). Emita o QA Gate Context Block e pare.
Se passar de ~150 turnos, escreva o estado neste arquivo e devolva.
