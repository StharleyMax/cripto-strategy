# Handoff — T-03.6

Task completa em `docs/context/candle-real-e-eixo-unico/tasks.toml` (buscar `id = "T-03.6"`,
~linha 458). Leia o bloco inteiro — refs, D8, D-C3.7, SPEC-008 §7.3.

## Escopo desta dispatch

Só T-03.6: o envelope de `/series-history` ganha coverage
`{earliest_bucket_ms, latest_bucket_ms, source_floor_ms}`. T-03.4 (par `{present,expected}`
por bucket, em `series_history_report.py` / `use_cases/series_history.py`) já está na sua
branch — leia `docs/context/candle-real-e-eixo-unico/gates/T-03.4-builder.md` antes de mexer
nesses arquivos, T-03.4 já os alterou bastante.

⚠️ **Isto é pré-requisito nomeado de T-03.11** (Playwright, "TF move os seis" — a wiring do
clique no TF → refetch dos 6 painéis foi deliberadamente adiada por T-03.9 e T-03.10
justamente esperando este campo; ver `gates/T-03.9-builder.md` e `gates/T-03.10-builder.md`).
Não implemente a wiring do front aqui — é escopo de T-03.11, componente `web`. Sua entrega é
só o campo `coverage` no envelope, no backend.

## Onde trabalhar

Worktree já criado, branch `task/candle-f03-t03-6` a partir da ponta atual de
`wave/candle-f03`: `/home/stharley/Documentos/projects/cripto-strategy-worktrees/candle-f03-t03-6`
`.venv` e `data/` já linkados.

Componente: `sentimento`. Gate: `bash backend/scripts/lint.sh` + `bash backend/scripts/test.sh`.

⚠️ Há OUTRA dispatch rodando em paralelo agora (T-03.8, mesmo componente, arquivo de teste
diferencial novo — baixo risco de overlap, mas não assuma nada sobre o estado dela).

## Ao terminar

Commit(s) na sua branch (não dê push, não abra PR). Emita o QA Gate Context Block e pare.
Se passar de ~150 turnos, escreva o estado neste arquivo e devolva.
