# T-01.7 — ESTADO: o assert esta PRONTO e a janela declarada continua VAZIA

**Escrito em 2026-09-19T22:4xZ.** Relatorio completo: `gates/T-01.7-builder.md`.

## O que esta pronto

O arnes existe, e testado (50 testes, zero rede) e **ja rodou no dado vivo**, com resultado.
O que **nao** rodou e o assert sobre a janela que `tasks.toml` declara.

## O comando, literal, para repetir quando o backfill chegar

```bash
cd backend && .venv/bin/python -m src.modules.sentimento.infra.candle_fidelity_cli \
  --symbol BTCUSDT \
  --window-start-ms 1789732800000 \
  --window-end-ms   1789747200000 \
  --knowledge-time-ms "$(date -u +%s000)"
```

`1789732800000` = `2026-09-18 12:00:00Z`, `1789747200000` = `2026-09-18 16:00:00Z`.

**Codigos de saida:** `0` fiel · `1` divergente · `3` **nao mediu** (janela vazia ou recusa).

## O que ele devolveu hoje

```
rc=3
symbol=BTCUSDT verdict=not_measured
universe: origin_buckets=240 compared=0 absent=960 carried=0 divergences=0
```

`[MEDIDO 2026-09-19]` — a ORIGEM respondeu `240/240` buckets; o NOSSO lado, `0/240` nas quatro
reducoes. `rc=3` e **deliberado**: janela vazia nao e janela pequena (`ADR-012`).

## A quadrupla auto-verificavel do owner, ainda por conferir

`tasks.toml` colheu da origem, para `BTCUSDT`, `2026-09-18 12:00→16:00 UTC`, `240/240` minutos:
`first(open)=78.031,00` · `max(high)=81.156,80` · `min(low)=77.923,50` · `last(close)=80.688,70`.

`aggregate_stored_window(..., expected_buckets=240)` devolve exatamente esses quatro numeros a
partir do nosso lado — e **recusa** enquanto faltar um unico minuto, em vez de agregar o que
chegou: `first(open)` de uma janela sem o primeiro minuto e a abertura de outro minuto com o
nome certo.

## ⚠️ O que JA foi medido, e muda a leitura do achado

Rode `gates/T-01.7-builder.md` §2 e §3 antes de tratar o achado como verdade estabelecida:

* os dois "defeitos" do `ACHADO-vela-viva-diverge-da-origem.md` sao o **degrau de `LOCF`** que o
  catalogo declara (`max_staleness_ms = 120_000`), nao vela duplicada e nao pavio truncado;
* o defeito **real** medido esta no `CLOSE`: `26` de `79` buckets (`32,9%`), quase sempre
  `±0,10` (um tick), `pos=16 / neg=10` — **atravessa o zero**, logo **nao** e o vies unilateral
  do `[M-9]`. `OPEN` e `HIGH` sao `79/79` exatos.
