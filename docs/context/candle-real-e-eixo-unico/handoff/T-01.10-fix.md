# Handoff — correção do NEEDS_FIX de T-01.10 (CST-206)

O gate de design reprovou a vela. **Leia o laudo antes de tocar em qualquer coisa:**
`docs/context/candle-real-e-eixo-unico/gates/T-01.10-designgate.md` (513 linhas) — ele traz a
medição de cada achado com o comando que a produziu.

## ⭐ Isto NÃO é decisão de design nova — é regressão contra design APROVADO

`docs/product/STITCH_CONTEXT.md:1255-1257` já traz os três estados, e eu confirmei a citação:

```html
<!-- Vazado (Up)   -->  border border-[#089981] bg-transparent
<!-- Cheio (Down)  -->  bg-[#f23645]
<!-- Doji (Cross)  -->  bg-[#8b949e]        <= NEUTRO, sem hue de direção
```

⇒ **Não abra pergunta de design, não invente token, não chame o Stitch.** O alvo está escrito.
Implementar o que já foi aprovado não precisa de nova autoridade.

## Os quatro achados, e o que cada um exige

- **`[BLOCKER-1]`** — direção só por MATIZ. Alta e baixa emitem o mesmo conjunto de ops de canvas
  (`fillRect`+`stroke`+`fill`); só o hex muda. Cinza `135` vs `129`, delta **6/255**, razão
  **1,092** — o mesmo número de `ADR-010:113`. Reprova **SC 1.4.1 nível A**.
  **A1:** `frontend/src/charts/color-tokens.ts` emite os 3 estados — alta VAZADA
  (`upColor: "rgba(0,0,0,0)"` + borda no token), baixa CHEIA.
- **`[BLOCKER-2]`** — ⚠️ **o pior, e não estava na minha hipótese: o doji é pintado como ALTA.**
  Eu confirmei na fonte da lib instalada: `lightweight-charts@5.2.1`,
  `dist/lightweight-charts.development.mjs:2811` e `:2825` resolvem
  `const isUp = ensure(...Open) <= ensure(...Close)` ⇒ `open == close` cai no ramo de alta, e o
  render do doji fica **byte a byte igual** ao da alta. Não é canal pobre — é **afirmação falsa**
  contra `ADR-010:110`. **A2:** override de cor **por item** (`#8b949e`), já que a lib não expõe
  um terceiro ramo.
- **`[SERIOUS-1]`** — `frontend/src/charts/color-tokens.test.ts:123` **cimenta** o defeito: o
  `deepEqual` dá 18/18 verde porque **nenhum dos 18 mede forma**. **A3:** teste dos 3 estados +
  ablação de cinza. ⛔ Um teste que passaria igual com o defeito presente não conta como prova.
- **`[SERIOUS-2]`** — corrigir o token **sozinho não entrega nada na vista padrão**: `fitContent`
  sobre 5.760 slots clampa `barSpacing` em **0,500 px** e **zero interior vazado é pintado**; o
  canal só nasce a **≥ 3,5 px/barra** (`n=8` densidades). **A4:** o DoD da correção tem de
  **assertar onde o interior existe**, não só que o token mudou.
- **`[MINOR-1]`** — cortar a oração de tutorial (o laudo diz qual).

## Fora do teu escopo — não construa

- **`[SERIOUS-3]`** preço sem marca posicional de ausência (o volume tem, no mesmo painel).
  É **requisito novo, 0 linhas em `SPEC-008`** ⇒ foi escalado ao owner. **Não implemente.**
- A lacuna **PASSOU** (`RN-1`/`RF-4`) — ausência vira `WhitespaceItem`, degenerada = 0 linhas.
  Não mexa no que passou.

## Fronteira — declarada nos DOIS eixos

Roda em paralelo com **T-01.5** (`sentimento`, backfill, outro worktree).

- **teu**: `frontend/src/charts/**`, `frontend/src/app/symbol/**` (só o necessário para o override
  por item), o relatório em `docs/context/candle-real-e-eixo-unico/gates/`
- **NÃO teu**: `backend/**`, `deploy/**`, `docs/product/**` (o design já está aprovado — consome,
  não reescreve), e ⛔ **não reescreva linha existente em `docs/INDEX.md`** (append-only)
