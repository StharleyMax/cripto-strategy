# Handoff — T-04.3

Task completa em `docs/context/candle-real-e-eixo-unico/tasks.toml` (buscar `id = "T-04.3"`,
~linha 599). Leia o bloco inteiro lá — refs, DoD, item 4.3 do plano.

## Escopo desta dispatch

Só T-04.3: a chave de máquina deixa de nascer da microcopy. Hoje
`frontend/src/app/symbol/SymbolClient.tsx:2192` monta `data-fact={\`live_${label}:…\`}` a partir
da string visível, e a página publica `data-fact="live_preço:attempted"` — com acento. Plano
completo: `docs/plans/SPEC-008-candle-real-e-eixo-unico/04_oi_honesto.md` (DoD 3 é o que vale
para esta task: zero chave de máquina não-ASCII — `grep -P '[^\x00-\x7F]'` sobre os
`data-fact="..."` publicados tem que dar 0). A chave passa a ser ASCII, derivada do `SeriesKey`;
a microcopy (o `label` visível) continua em pt-BR — o conserto é separar as duas strings, não
traduzir a UI.

Não implemente T-04.4 em diante (herança da fábrica pelos demais painéis é outra dispatch) nem
T-04.1 (rótulo/label do painel de OI — dispatch paralela, worktree irmão, mesmo arquivo
`SymbolClient.tsx`, função diferente: você mexe em como o `data-fact` é derivado, a outra mexe no
texto do label). Se notar sobreposição real de linhas, registre no handoff e sinalize no QA Gate
Context Block — o orquestrador resolve o merge.

## Onde trabalhar

Worktree já criado, branch `task/candle-f04-t04-3` a partir de `wave/candle-f04`:
`/home/stharley/Documentos/projects/cripto-strategy-worktrees/candle-f04-t04-3`

Componente: `web`. Gate: `npm --prefix frontend run lint && npm --prefix frontend run typecheck`
+ suíte relevante (`harness policy --key test_cmd` para o componente `web`).

## Ao terminar

Commit(s) na branch da task (não dê push, não abra PR — o orquestrador consolida em
`wave/candle-f04`). Emita o QA Gate Context Block e pare — não rode `gate-record`, não aprove,
não avance estado. Se passar de ~150 turnos, escreva o estado aqui mesmo
(`docs/context/candle-real-e-eixo-unico/handoff/T-04.3.md`) e devolva.
