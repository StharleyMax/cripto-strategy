# `T-03.7` (`CST-222`) — `CA-8′` em quatro camadas: guarda, fixture da Binance, matriz 8×5, ablação

> Fase `03` · componente `sentimento` · `SPEC-008` §5.4, `ADR-040/D2`, `plano 03` DoD 1.
> Branch `task/candle-f03-t03-7`, worktree `candle-f03-t03-7`, base `wave/candle-f03` (já com
> `T-03.1`/`T-03.3`/`T-03.4`/`T-03.6`/`T-03.8` mergeadas).

## Escopo e por que `CA-8` foi retirado

`PRD-008`'s `CA-8` (*"OI reagregado a `1h` == último do bucket, e ≠ soma dos 12 de `5min`"*) foi
RETIRADO, não estendido: `JULGAMENTO-QUANT-ARCHITECT.md` §4 mediu que ele MORDE apenas `last↔Σ`
(1 de 20 trocas possíveis) e passa VERDE sobre a vela degenerada `o=h=l=c` que `RN-2` mede o
view-model produzir — sobre uma fixture flat, as 5 funções candidatas (`Σ`, `first`, `max`,
`min`, `last`) coincidem, e qualquer escolha errada em `REDUCTION_TABLE` ainda passaria. `T-03.8`
(já mergeada) implementa o `CA-8` literal; esta task é a substituição, `CA-8′`, inteira.

## O que este ciclo adicionou

Arquivo novo: `backend/tests/sentimento/test_series_reduction_ca8_prime.py`, 12 testes em 4
camadas, todas sobre a MESMA fixture real:

**(a) guarda de não-degenerescência** (`test_ca8_prime_camada_a_…`) — `first`/`max`/`min`/`last`
do `close` de 240 minutos reais precisam ser 4 valores dois a dois distintos, e roda primeiro no
arquivo: sem isso as camadas seguintes medem uma vela plana e passam por coincidência.

**(b) fixture colhida da Binance**, não do banco (`[P-seed]` respeitado — leitura da origem, não
semeadura): `BTCUSDT`, `1m`, bucket fechado `2026-09-18 12:00→16:00 UTC`, `240/240` minutos.

```bash
curl -s "https://fapi.binance.com/fapi/v1/klines?symbol=BTCUSDT&interval=1m&startTime=1789732800000&endTime=1789747199999&limit=241"
```

`[MEDIDO 2026-09-22]` — os 240 `[open,high,low,close,volume]` estão embutidos, literais, no
arquivo (`_RAW_1M_BTCUSDT_20260918_1200_1600_UTC`). Reproduzem `JULGAMENTO-QUANT-ARCHITECT.md`
§4.1 dígito a dígito (medido em `2026-09-19`, confirmado de novo em `2026-09-22` — o bucket já
fechou e não se move):

| função | valor | o que a troca daria |
|---|---|---|
| `Σ(volume)` | `93.465,237` | — |
| `first(open)` | `78.031,00` | — |
| `max(high)` | `81.156,80` | `max(close)` = `81.062,60` (`-11,7 bp`) |
| `min(low)` | `77.923,50` | `min(close)` = `77.944,80` (`+2,7 bp`) |
| `last(close)` | `80.688,70` | `first(close)` = `77.984,40` (`-3,35%`) |

`test_ca8_prime_camada_b_…` pina esses 8 números contra a fixture — regressão se o array for
editado à mão.

**(c) matriz exaustiva** — `test_ca8_prime_camada_c_only_the_declared_function_matches…`,
parametrizada sobre os 8 `(nature, reduction)` de `REDUCTION_TABLE` × as 5 funções candidatas
(`40` células). Para cada par: `reduce_bucket` E a função declarada batem com o literal; as
outras 4 não batem. As 4 células que colapsam em `(STOCK,CLOSE)/(STOCK,LAST)/(STOCK,POINT)/
(RATIO,POINT)` (a colisão que `series_reduction.py` já documenta) reduzem a informação a
`5 cenários × 4 trocas erradas = 20` — o número que `plano 03` DoD 1(c) nomeia
(`test_ca8_prime_camada_c_scenario_table_covers_…` guarda que a tabela de cenários não driftou
dos 8 pares reais).

**(d) ablação de cobertura 81/240** — `test_ca8_prime_camada_d_81_of_240_…` chama
`build_series_history_report` (produção, `T-03.3`/`T-03.4`) com só os 81 primeiros dos 240 fatos
reais de `klines_volume` presentes, via `interval="4h"`. Assertado:
`coverage == BucketCoverage(present=81, expected=240)`, servido `== Σ(81 reais) = 5170.515`,
`!= Σ(240) = 93465.237` e `!= mean(81)×240 = 15320.0444…` — a extrapolação silenciosa proibida
por `ADR-040/D3` (`P-B`).

## Falsificador — a mutação que a camada (c) rejeita

`REDUCTION_TABLE` mutada em `src/modules/sentimento/domain/series_reduction.py`
(`(STOCK,HIGH): _max` → `_min`), suíte rodada, revertida com `git checkout --`:

```
tests/sentimento/test_series_reduction_ca8_prime.py::test_ca8_prime_camada_c_only_the_declared_function_matches_the_real_fixture[Nature.STOCK-Reduction.HIGH] FAILED
assert reduce_bucket(...) == pytest.approx(81156.8, abs=1e-9)  ->  Obtained: 77971.0
```

MORDE — não decoração passando por coincidência de fixture. Código de produção confirmado
idêntico ao de antes da mutação (`git diff --stat` vazio após o revert).

## Comandos rodados e resultado

- `bash backend/scripts/lint.sh` → `All checks passed!` / `469 files already formatted` /
  `Success: no issues found in 469 source files` (ruff + ruff format + mypy --strict).
- `.venv/bin/python -m pytest tests/sentimento/test_series_reduction_ca8_prime.py -v` →
  `12 passed in 0.12s` (universo: 12 testes, este arquivo).
- `bash backend/scripts/test.sh -q` → suíte inteira, `2715` testes coletados
  (`pytest --collect-only -q`, universo do backend inteiro), `rc=0`. Cobertura:
  `TOTAL 96.32%` (piso `70%`); por camada (`ADR-009/D1`): `domain 99.7%` (meta `90%`),
  `use_cases 99.6%` (meta `80%`), `infra 92.4%` (meta `70%`) — as 3 camadas declaradas, `[OK]`
  nas 3.
- `harness rules --mode sweep --changed-only` → `rc=0`, sem achado (universo: 1 arquivo
  alterado, `backend/tests/sentimento/test_series_reduction_ca8_prime.py`).

## Doc delta

Nenhum documento normativo mudou: `SPEC-008` §5.4, `ADR-040/D2`, `JULGAMENTO-QUANT-ARCHITECT.md`
§4 e `plano 03` DoD 1 já fixavam as 4 camadas e os números da fixture antes deste ciclo — este
ciclo torna o falsificador EXECUTÁVEL, não redefine critério. Nenhum ADR novo: nenhuma decisão de
arquitetura, só teste sobre `reduce_bucket`, já decidido por `ADR-040/D2` (`T-03.1`). A única
`SeriesRow` nova é de teste (`_lagged_row`, câmada d), zero mudança de schema ou de contrato.
