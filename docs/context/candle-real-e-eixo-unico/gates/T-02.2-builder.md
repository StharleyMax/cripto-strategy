# `T-02.2` (`CST-209`) — `TimeAxisController` PURO em `charts`, sem `IChartApi`, sem `fetch`

> Fase `02` · componente `charts` · `D-C3.1`, `RF-5`, `C-3`, `ADR-003/FR-1`, `ADR-003/FR-2`.
> Branch `task/candle-f02-t02-2`, worktree `t02-2`, base `wave/candle-f02` (já com `T-02.1`+`T-02.5`).

## Escopo, e por que ele é mais estreito que o título de `D-C3.1` inteiro

`D-C3.1` (`JULGAMENTO-FRONTEND-ARCHITECT.md:120-144`) descreve a fronteira inteira `charts`↔`web` e
lista, no mesmo bloco de "forma mínima da interface", quatro assinaturas: `toLogicalRange`,
`fromLogicalRange`, `reduceRangeEvent` e `historyRequest`. Mas o plano da fase (`02_eixo_unico.md`)
divide `D-C3.1` em itens com donos diferentes:

- **item `2.1` = esta task** — só o controller puro de RANGE (a álgebra + a assinatura como valor).
- **item `2.2`/`2.2b` = `T-02.3`** (`depends_on = ["T-02.2"]`) — a guarda de reentrância (`D-C3.2`) e
  a âncora de troca de TF preservando borda direita + N velas (`D-C3.3`). `tasks.toml:277-289`
  referencia `D-C3.3` só em `T-02.3`, não aqui.
- **`historyRequest`/`HistoryRequest`** — `D-C3.4` (predicado de borda) e `D-C3.5` (paginação) são
  referenciados em `tasks.toml:693,708`, dentro das fases `03`/`05`, não da `02`. Implementar o
  descritor de histórico aqui congelaria uma forma que aquelas tasks ainda não decidiram.

⇒ **Esta task NÃO exporta `historyRequest`.** Isto é declarado no docstring do próprio módulo, não
escondido: quem ler `time-axis-controller.ts` sem este gate doc ainda vê a razão.

## O que foi criado

| arquivo | estado |
|---|---|
| `frontend/src/charts/time-axis-controller.ts` | novo — `TimeAxis`, `TimeRange`, `LogicalRange` (valores puros); `toLogicalRange`/`fromLogicalRange` (conversão instante↔índice, SEM arredondar — `D-C3.3` mediu que arredondar reintroduz o erro de `325,3 h`); `reduceRangeEvent` (dedupe por epsilon, `DEFAULT_RANGE_EPSILON_MS = 0.5`); `TimeAxisController`/`createTimeAxisController` — o objeto nomeado pelo título da task, que só empacota as funções acima ligadas a um `axis` imutável, sem estado mutável de range e sem a guarda `applying` (que é `T-02.3`) |
| `frontend/src/charts/time-axis-controller.test.ts` | novo — 13 testes `node --test`, zero `jsdom` |

Nenhum outro arquivo tocado — `git status --short` no worktree mostra só os dois `??` acima.
`frontend/src/app/symbol/SymbolClient.tsx` **não foi tocado** (a fiação é `T-02.4`).
`frontend/src/charts/index.ts` (o barril `web`) **não foi tocado** — nada em `web` importa este
módulo ainda; a task que abre essa porta é `T-02.4`.

## Prova de pureza (o falsificador literal de `D-C3.1`)

O DoD da decisão é **"o controller compila e é testado sem `jsdom`"**
(`JULGAMENTO-FRONTEND-ARCHITECT.md:483`). Duas provas, não uma:

1. **Compila isolado, sem `jsdom` em lugar nenhum do arquivo de teste** — `time-axis-controller.test.ts`
   não importa `jsdom` (contraste: `headless-chart.test.ts` importa `jsdom` explicitamente porque SEU
   assunto precisa de `window`; este não pode precisar).
2. **A CÓDIGO (não a prosa) não contém `IChartApi`, `fetch(...)`, nem `lightweight-charts`** —
   `time-axis-controller.test.ts`, teste `"PURITY (D-C3.1 falsifier)"`: lê o arquivo-fonte, STRIPA
   comentários (`/* */` e `//`), e afirma ausência dos três tokens no código restante. Roda o par
   morde/cala na MESMA passada: depois de stripar, afirma ausência (cala); e afirma que o arquivo
   ORIGINAL (com prosa) CONTÉM `"IChartApi"` (morde — prova que o stripping realmente remove algo,
   não que o arquivo nunca tocou no assunto).

```
npm --prefix frontend run test:charts -- --test-name-pattern='PURITY'
# ✔ PURITY (D-C3.1 falsifier): the module CODE (not its prose) names neither IChartApi, fetch, nor lightweight-charts
```

## Testes e portões rodados

- `node --test 'src/charts/time-axis-controller.test.ts'` → **13 pass, 0 fail** (isolado).
- `npm --prefix frontend run test:charts` → **214 pass, 0 fail** (universo: `src/charts/*.test.ts`,
  era 201 antes de `T-02.1` + esta task = 201 + 13 = 214, confere).
- `npm --prefix frontend run lint` → rc=0, limpo.
- `npm --prefix frontend run typecheck` (`tsc -p tsconfig.json --noEmit --strict`) → rc=0, limpo.
- `make verify` — rodado em background nesta sessão (log `/tmp/verify-t02-2.log` +
  `/tmp/verify-t02-2-20260922T001529Z.log`, não versionados). Resultado final:
  `lint-backend rc=0` (461 arquivos) · `lint-frontend rc=0` · `test-frontend rc=0` (**770 pass, 0
  fail** em 4 suítes — 757 da baseline de `T-02.1` + 13 novos desta task, confere) · `test rc=0`
  (backend pytest, **2623 passed, cobertura 96,31%** — `backend/` intocado por este diff) ·
  `boundaries rc=0` (7 kept, 0 broken) · `regras rc=0` (0 bloqueio, 73 aviso) · `política rc=0`.
  **`e2e` RECUSOU medir (`rc=3`)**: `grep '^RECUSA:' <log>` → `"porta 8811 (API) ja tem algo
  escutando — escolha outra via E2E_API_PORT"` — colisão de porta com outro processo na máquina
  (provavelmente outra worktree rodando e2e em paralelo), não uma reprovação do diff. **Veredito
  literal do `make verify`: `INDETERMINADO`, não `verde`** — a doutrina do agente já recusa
  Playwright ao vivo por padrão (nenhum servidor Playwright cadastrado nesta instalação), e este
  `rc=3` é o mesmo tipo de ausência de ambiente, registrado aqui em vez de arredondado para "passou".
- `__pycache__` purgado (`find . -name __pycache__ -type d -exec rm -rf {} +`) antes do `make verify`.

## Cobertura

Não há alvo de cobertura declarado para `frontend/` neste repositório (`[MEDIDO 2026-09-19]`, ver
`JULGAMENTO-FRONTEND-ARCHITECT.md:472`: o portão é `test:charts` verde, não percentual de linha).
13 testes cobrem: conversão instante↔lógico (com e sem arredondamento, incluindo o caso fracionário
medido em `probe4.mjs`), round-trip exato, rejeição de `stepMs` não-positivo, `reduceRangeEvent` nos
três regimes (idêntico, dentro do epsilon, além do epsilon, epsilon customizado), e o
`createTimeAxisController` (binding, imutabilidade do `axis`, rejeição de `stepMs` inválido).

## Doc delta

- `frontend/src/charts/time-axis-controller.ts`: docstring do módulo já documenta o escopo (o que
  esta task NÃO faz e por quê) — não precisa de documento externo.
- `docs/INDEX.md`: sem entrada nova, mesma convenção que `T-02.1-builder.md` já registrou (gate doc
  de fase não é catalogado ali, só docstring de código + este arquivo).
- `docs/plans/SPEC-008-candle-real-e-eixo-unico/02_eixo_unico.md`: sem mudança — item `2.1` já
  descrevia exatamente isto.
- `docs/context/candle-real-e-eixo-unico/handoff/JULGAMENTO-FRONTEND-ARCHITECT.md`: sem mudança —
  `D-C3.1` já estava decidida; esta task a implementa.

## Bloqueado

Nada bloqueado do lado do código desta task — os 6 gates que este diff pode afetar (`lint-backend`,
`lint-frontend`, `test-frontend`, `test`, `boundaries`, `regras`, `política`) estão todos `rc=0`.
Pendência que NÃO é deste builder:

1. **`e2e` recusou medir** (`rc=3`, colisão de porta 8811 com outro processo — não uma reprovação de
   código). Recomenda-se reconferir com `make verify` isolado (porta livre, ou `E2E_API_PORT`
   diferente) antes do merge da wave — este diff não adiciona nem modifica nenhum teste `e2e`, então
   não há hipótese de que ELE seja a causa da colisão.
2. `T-02.3` (guarda de reentrância + âncora de TF, `D-C3.2`/`D-C3.3`) e `T-02.4` (`web` assina/
   despacha/aplica) constroem sobre este módulo — nenhuma delas é escopo desta task.
