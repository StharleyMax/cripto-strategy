# Gate — T-04.6: veredito do ux-ui-mastery sobre o rótulo de proveniência do OI

**Veredito: APPROVED** (score 88/100)

Revisão ao vivo (`npm run dev`, `/symbol/BTCUSDT` contra API real, screenshot + `getComputedStyle`
do nó `[data-fact^="oi_provenance"]`), não só leitura de código. Renderizado confirmado:

```
Grandeza: contracts (BTC) · Universo: binance/usdm_futures · Coorte: all
```
`data-fact="oi_provenance:grandeza=contracts (BTC);universo=binance/usdm_futures;coorte=all"`

## Por que aprova

- Resolve o defeito real que o owner circulou em vermelho (número sem unidade, confundível com
  nocional do Coinalyze) — verificado ao vivo, não só por asserção estática.
- Caminho `unresolved` mostra "Procedência não identificada" em vez de inventar rótulo —
  mesma disciplina de `LiquidationProvenance`/`LongShortProvenance`.
- Contraste medido `rgb(139,148,158)` = token `provenanceWeak`, **5,82:1**, passa AA — confere
  com `frontend/src/charts/color-contrast.test.ts:280`.
- Reusa `text-provenance-weak` já aprovado — zero cor/classe nova.

## Achado único (não bloqueante)

`frontend/src/app/symbol/SymbolClient.tsx:1295-1310` — as 4 linhas do `OiPane` renderizam no
mesmo peso visual, sem diferenciação entre o número e os 3 metadados. Difere do padrão já
aprovado em `LongShortPane` (ênfase em negrito no termo que desambigua). **Não reprova**: o
`OiPane` herda essa característica da família Price/Volume/OI/CVD inteira, que já era assim
antes de T-04.1 — não é regressão introduzida por esta task, e o requisito funcional (visível
sempre, sem interação) já está cumprido.

**Ação sugerida, não bloqueante, para harmonização futura (fora do escopo desta fase):** envolver
`provenance.grandeza` em `<strong className="font-bold text-on-surface">` (mesma técnica de
`SymbolClient.tsx:1989`).

Domínios avaliados: Heuristic 9 · Research 10 · Mobile 7 (não testado em viewport móvel ao vivo) ·
Desktop 9 · Visual 7 · Accessibility 9 · Interaction N/A · Future-Readiness 8 · System
Architecture 9 · Ethics/Content 10.
