# `T-02.1` · Relatório de build — retroativo de `backend/tests`

**Feature:** `codigo-em-ingles` · **Tracker:** `CST-95` · **Fase:** `02` · **Componentes:** `docs` + `sentimento`
**Branch:** `task/ci-T-02.1-retroativo-backend-tests` · **Base comum:** `master@c7df90c` · **Commit:** `73b8392`
**Rev de todas as medições abaixo:** o rev do builder, não o do plano — `ADR-015/D2` manda re-medir.

---

## 1. O que foi feito

`git mv` dos 2 arquivos + os 40 identificadores do mapa **normativo e fechado** de `SPEC-002` §3.1 + as
**3 citações VIVAS**, tudo **no mesmo commit** (`RN-1`).

| de | para |
|---|---|
| `backend/tests/sentimento/test_durabilidade_da_infra.py` | `backend/tests/sentimento/test_infrastructure_durability.py` |
| `backend/tests/sentimento/test_etl_backlog_retomavel.py` | `backend/tests/sentimento/test_resumable_etl_backlog.py` |

**Método:** reescrita apenas de token `NAME`, via `tokenize`. String literal e comentário ficam intactos
**por construção**, não por cuidado — é o que preserva `b"conteudo"`, as mensagens de asserção em
português e o `# noqa` (`RN-3`: renomear o continente, não traduzir o conteúdo).

Cobertura do mapa, medida: **7/7** tokens em `file1` e **35/35** em `file2` encontrados na árvore.
Nenhuma entrada do mapa ficou órfã ⇒ nenhum item do mapa era erro de digitação.

## 2. As 3 citações VIVAS, no mesmo commit

| citação | linhas | tratamento |
|---|---|---|
| `backend/README.md` | `:568` `:569` `:717` `:743` `:815` | 5 linhas, só o caminho; a prosa em volta intacta |
| `backend/src/.../infra/jsonl_checkpoint.py` | `:22` | docstring; **única linha de `backend/src`** desta fase |
| `test_resumable_etl_backlog.py` | `:263` | citação cruzada entre os 2 arquivos |

**Mais 2 citações NÃO enumeradas pelo plano, dentro do próprio arquivo renomeado**, declaradas por
serem âncoras mortas se ficassem: `:61` (docstring citando o parâmetro `alvo`, renomeado para `target`)
e `:155` (docstring citando `test_reprocessar_o_mesmo_item_...`, renomeado). Ambas invisíveis ao `ast`
de `CA-F2-2`, porque são string.

## 3. As HISTÓRICAS, preservadas

`git diff --numstat c7df90c...HEAD -- docs/INDEX.md docs/adr docs/plans docs/specs` → **vazio** (`CA-F2-7`).
`docs/INDEX.md` é append-only por `CLAUDE.md` e **não foi tocado, nem para acrescentar**.

## 4. Linha de base — o falsificador barato

`make test` → rc=0 · **107 passed · 370 statements · 54 branches · domain 124/124 · use_cases 52/52 ·
infra 194/194 · 100%** — **idêntica antes e depois** `[MEDIDO, universo 107 testes]`.
`make lint` → rc=0 (`ruff` + `ruff format` 29 arquivos + `mypy --strict` 29 arquivos + `eslint`).

## 5. Bancada de mutação — n=3, e uma NÃO morde

Verde não prova nada até uma mutação reprovar. Cada mutação foi **verificada por `diff` antes de medir**
(bancada que não altera o alvo é o defeito que esta casa já cometeu 4×).

| # | mutação | resultado | veredito |
|---|---|---|---|
| A | apagar 1 statement de `backend/src` | `make test` **rc=0**, **`107 passed`**, mas `TOTAL` **370 → 369** | `CA-F2-3` **morde** — e **só pelo 370** |
| B | apagar a docstring de um teste renomeado | `make lint` **rc=2**, `D103` | `CA-F2-8` **morde** |
| C | apagar o `# noqa: S603` | `make lint` **rc=0** | **NÃO morde** |

**A mutação A é a prova do critério inteiro:** a suíte continuou verde e a contagem de testes continuou
`107`. Um builder que olhasse `rc` ou "107 passed" teria publicado um statement apagado. É exatamente o
que o plano diz — *"o número que denuncia é o `370`, não o `107`"* — e agora está demonstrado, não citado.

**A mutação C é resultado negativo e vai registrada:** o `CA-F2-3` nomeia *"apague um `noqa`"* como
falsificador, e **ele não vale**. Motivo medido: `S603` está em `per-file-ignores` para `tests/**/*.py`
(`backend/pyproject.toml:136`), logo aquele `# noqa: S603` é **vestigial** — suprime regra já desligada.
Não removi o `noqa`: está fora do escopo desta task. **Fica nomeado, não silenciado.**

---

## 6. ERRATA 1 — `ligados_distintos` é **54**, não os **55** que o DoD exige

**A contagem certa é a minha; o número do DoD está errado, e a causa é o próprio mapa normativo.**

```
$ python -c "<ast: Name(Store) U arg U FunctionDef U AsyncFunctionDef U ClassDef>"
file1  ANTES 15 -> DEPOIS 15   (DoD exige 15)  OK
file2  ANTES 55 -> DEPOIS 54   (DoD exige 55)  DIVERGE
```

**Por que 54 é o valor correto, e não sinal de código apagado.** Aplicando o mapa fechado ao conjunto
ANTIGO de nomes, o conjunto previsto tem **54** elementos, e ele bate **exatamente** com o medido:

```
DEPOIS - previsto (nomes a mais)   : []
previsto - DEPOIS (nomes perdidos) : []
```

**A colisão, nomeada:** o mapa manda `processo`→`process`, e **`process` já existia** no conjunto antigo
— é o método `ContadorDeTrabalho.process` (hoje `WorkCounter.process`). Dois nomes distintos antes, **um
só** depois: `55 - 1 = 54`. O mapa **não é injetivo** sobre o conjunto de nomes deste arquivo.

**O `55` do DoD pressupõe bijeção**, e essa premissa é falsa contra o mapa que o próprio `SPEC-002` §3.1
fixa. A metade `ligados_distintos` do `CA-F2-2` existe para pegar **código apagado** (*"só a primeira
seria satisfeita apagando código"*) — e o que ela pegou aqui não é apagamento: nada se perdeu
(`previsto - DEPOIS = []`), e a linha de base comportamental (`107`/`370`/`54`) está intacta.

⇒ **Leitura correta do critério:** `file1` = **15** e `file2` = **54**. Registro a errata em vez de
forçar o código a bater com o número — forçar significaria desfazer `processo`→`process`, que é
**mandado pelo mapa normativo**, ou inventar um 55º nome.

## 7. ERRATA 2 — o script do `CA-F2-2` **não consegue** devolver `em_PT=0`

O script publicado em `PRD-002` §6 classifica com `x in PT or x.startswith("test_")`. A cláusula
`startswith` conta **todo** nome `test_*`, em qualquer idioma. Medido sobre a árvore já renomeada:

```
test_infrastructure_durability.py   em_PT pelo script VERBATIM = 2    (nunca 0)
test_resumable_etl_backlog.py       em_PT pelo script VERBATIM = 12   (nunca 0)
```

O `or startswith("test_")` era um atalho **verdadeiro só no rev de ancoragem**, onde por acaso todo nome
`test_*` era português. Depois do rename ele vira ruído puro, e `CA-F2-2` fica **insatisfazível como
escrito**.

**Medição correta, com o conjunto FECHADO dos 40** — que é a coluna esquerda de `SPEC-002` §3.1, ou
seja, a classificação **humana e assinada**, não um detector (a substituição por detector é o que o
critério proíbe, e com razão: `so` é inglês):

```
conjunto FECHADO = 26 nomes PT + 14 nomes test_ antigos = 40   <- deriva independente do "40" da SPEC
test_infrastructure_durability.py   em_PT = 0   CALA
test_resumable_etl_backlog.py       em_PT = 0   CALA
```

⇒ **`CA-F2-2` metade (a): CUMPRIDO**, `em_PT = 0` nos dois, contra lista fechada e assinada.

## 8. ERRATA 3 — o `9` do `CA-F2-9` está certo; o **comando** dele erra

```
$ grep -rnE 'logger\.(info|warning|debug|error)\("' backend/src | wc -l
6        <- e o DoD espera 9
```

**Não é regressão minha:** o mesmo comando devolve **6 na base `c7df90c` e 6 no `HEAD`**, e as linhas
são **byte-idênticas** (`diff` das duas saídas → vazio). Nenhum evento de log foi tocado.

**O universo real são 10 chamadas `logger.*(`**, das quais **9 nomeiam evento** e 1 é `logger.info(line)`
(variável, `ingest_health_cli.py:113`). O regex exige `"` **na mesma linha** do `(`, então perde as **3
chamadas multi-linha** — `ingest_health_query_read`, `etl_drenagem_concluida`, `ingestion_verified`.
`6 + 3 = 9`. ⇒ **o valor `9` do DoD é correto; o comando que o mede é que não vê o que afirma ver.**

Os **4 eventos em português intactos**, verificados um a um por `grep -F` (rc=0 cada):
`etl_item_publicado` · `etl_item_concluido` · `etl_drenagem_concluida` · `checkpoint_cauda_truncada`.

## 9. `CA-F2-4` — o verificador de âncora, dois lados, por token

Comando literal, escopo INTEGRAL, unidade **linhas** (`git grep -c -F`):

| token | MORDE (base `c7df90c`) | CALA (`HEAD`) | CALA fora dos docs desta feature |
|---|---|---|---|
| `test_durabilidade_da_infra` | **9** linhas (piso do plano: 4) | 5 | **0** |
| `test_etl_backlog_retomavel` | **9** linhas (piso do plano: 3) | 6 | **0** |

Ambos os `n` do lado MORDE são **> 0** ⇒ nenhum token morto (`RN-8` / `ADR-015/D2`). Os dois estão
**acima do piso de sanidade**, o que confirma que ninguém mexeu nas âncoras antes de mim.

**Toda âncora de código, `README` e `backend/src` está em ZERO.** O residual é **inteiramente**
`docs/context/codigo-em-ingles/` — `tasks.toml:208,212` e `tasks_review.md:46,47,98,282,347` — que é o
**auto-envenenamento que o cabeçalho do próprio `tasks.toml` previu** ("*e vai subir de novo: este
arquivo, o `tasks_review.md` e o `handoff_to_builder.md` citam os dois tokens*").

**Por que esse residual NÃO é âncora e não deve ser reescrito** — e o argumento é decidível, não
estético: essas linhas **mencionam o token como objeto de discussão**, não resolvem um caminho contra a
árvore. `tasks.toml:208` diz literalmente *"o builder mede `test_durabilidade_da_infra` … declara `n > 0`
ANTES do rename"*. Trocar ali pelo nome novo mandaria um builder medir um token cujo MORDE é **0 por
construção** — e um MORDE `n=0` **reprova no ato da declaração** por `RN-8`. A reescrita destruiria o
sentido da frase. `tasks_review.md:46,47` é **tabela de medição** de um rev passado, e `:98` é
**transcrição de comando**: veredito já dado sobre rev já passado ⇒ **HISTÓRICA** por `ADR-015/D3`.

Some-se que `tasks.toml` é **proibido de editar** por instrução explícita desta task.

## 10. Demais critérios

| # | critério | comando | resultado |
|---|---|---|---|
| `CA-F2-1a` | exatamente 2 renames | `git diff --name-status c7df90c...HEAD -- backend/tests \| grep -c '^R'` | **2** |
| `CA-F2-1b` | nada além dos 3 arquivos | `git diff --name-only … \| grep -v …` | **vazio** |
| `CA-F2-5` | `sentimento/` fica, `sentiment/` não nasce | `test -d` nos dois | **rc=0** e **rc=1** |
| `CA-F2-6` | `backend/src` só na docstring | `git diff --name-only`; linhas de conteúdo | **1 arquivo**, **2 linhas**, dentro da docstring |
| `CA-F2-8` | árvore limpa | `make lint` | **rc=0** |
| `D1.10` | nenhuma `[[rules.own]]` de idioma | `git diff … -- harness.toml \| grep -c 'rules.own'` | **0** (e `harness.toml` sequer foi tocado) |

## 11. Uma decisão de escopo, declarada

O mapa fechado dos 40 vem do `ast` do `PRD-002`, que vê `Name(Store)`, `arg`, `FunctionDef` e `ClassDef`
— e é **cego a atributo**. Dois atributos em português sobreviveriam à letra do mapa: `self.chamadas` e
`self._alvo`. Renomeei os dois (`calls`, `_target`) porque **o mapa já traduz esses mesmos tokens**
(`chamadas`→`calls`, `alvo`→`target`) e deixar `self._alvo` ao lado do parâmetro `target` seria
incoerência nascida de um cego do instrumento, não de uma decisão. **Não altera nenhuma contagem**:
atributo não entra no `ligados_distintos`, e a linha de base seguiu em `107`/`370`/`54`.
`[INFERRED: leitura do mapa como dicionário de tradução, não como lista de ocorrências — declarada, não implícita]`

## 12. Doc delta

- `backend/README.md`: **atualizado**, 5 linhas — as citações VIVAS de `CA-F2-4`. Compartilhado com
  `T-03.1` (`:455`/`:457` lá, ~110 linhas de distância); conflito, se houver, resolve mantendo os dois lados.
- `backend/src/.../jsonl_checkpoint.py`: **atualizado**, 1 linha de docstring.
- `docs/INDEX.md`: **sem mudança** — append-only, e `CA-F2-7` exige `numstat` vazio. Não tocado nem para acrescentar.
- `docs/adr`, `docs/plans`, `docs/specs`: **sem mudança** — HISTÓRICAS (`CA-F2-7`).
- `docs/context/codigo-em-ingles/tasks.toml`: **sem mudança** — edição proibida por instrução, e as
  ocorrências são menção-de-token, não âncora (§9).
- **ADR: não necessária.** As 3 erratas corrigem *números e comandos de DoD*, não decisões. Nenhuma
  alternativa de arquitetura foi fechada aqui. As erratas 1 e 2 tocam `SPEC-002` §3.1/`PRD-002` §6 e as
  duas ADRs em vigor (`ADR-013`, `ADR-015`) seguem de pé sem emenda.

## 13. O que fica BLOQUEADO, nomeado

1. **`CA-F2-2` metade (b) não fecha como escrita** — exige `55`, o valor correto é `54` (§6). Precisa de
   errata no `SPEC-002`/`tasks.toml`, que **não posso editar**.
2. **`CA-F2-2` metade (a) não fecha pelo script verbatim** — insatisfazível por construção (§7). Cumprida
   pela lista fechada dos 40.
3. **`CA-F2-4` não chega a `0` no escopo integral literal** (§9) — o residual está em `tasks.toml` e
   `tasks_review.md`, que são menção-de-token e proibidos de editar. `0` fora deles.
4. **`CA-F2-9` não devolve `9` pelo comando publicado** (§8) — devolve `6`, na base e no `HEAD`. O
   invariante está provado por outro caminho (linhas byte-idênticas + os 4 eventos por `grep -F`).

Os quatro são **defeito de critério, não de implementação**, e nenhum deles é falha de comportamento: a
linha de base está intacta e os dois portões (`make lint`, `make test`) estão em `rc=0`.
