# `T-03.1` · `CST-96` — relatório de `/qa`

**Feature:** `codigo-em-ingles` · **Fase:** `03` · **Componentes:** `docs` + `web`
**Papel:** `/qa` · **Data:** 2026-08-29 · **Rev sob teste:** `f35e24a` · **Base:** `master@c7df90c`
**Branch:** `task/ci-T-03.1-retroativo-frontend` · **PR:** #27
**Bancada:** worktree `/tmp/claude-1002/wt/ci-T-03.1-retroativo-frontend` (o do builder) + worktree
próprio `/tmp/claude-1002/wt/qa-ci-T-03.1-base` em `c7df90c`, para o lado ANTES.

> ⚠️ **Este arquivo também é âncora HISTÓRICA e cita os nomes antigos de propósito.** Ele
> incrementa a contagem crua pela terceira vez (`[G-A1]`). Ver §3.

---

## Veredito: **`NEEDS_FIX`** — a ÁRVORE está certa, o ARTEFATO DE PORTÃO não

**Nada no código precisa mudar.** Os 4 renames estão corretos, atômicos, comportamentalmente
equivalentes, e a prova de dois lados sobrevive ao rename — reproduzi os quatro casos e os dois
que mordem, mordem **dentro** de `features/panel/`. A linha de base está intacta ao byte.

O que reprova é o **relatório de build**, em dois números: **dois valores publicados não saem do
comando publicado ao lado deles**, e um terceiro é justificado por uma causa **medivelmente falsa**.
`CLAUDE.md` chama isso pelo nome — *"nenhum número sem o comando que o produziu"* — e este
repositório já achou três defeitos reais por essa disciplina. A correção é **só de documento**,
`docs/context/codigo-em-ingles/gates/T-03.1-build.md`, e não toca uma linha de produção.

---

## 0. O que eu rodei, e a ordem

```
$ make setup                                        # rc=0  — sem isto, tudo abaixo seria rc=3
$ bash backend/scripts/test.sh                      # rc=0  — chamada DIRETA, para ver rc=3 se houvesse
$ bash backend/scripts/lint.sh                      # rc=0
$ npm --prefix frontend run lint                    # rc=0
$ make lint                                         # rc=0
$ bash docs/context/codigo-em-ingles/gates/T-03.1-qa-bench.sh   # rc=0, 41 checagens
```

**`rc=3` não apareceu em nenhum comando**, e isso é afirmação medida e não descuido: `make setup`
rodou primeiro nos DOIS worktrees, e a própria bancada que eu acrescentei recusa com `rc=3` quando
falta `frontend/node_modules` — falsificador exercitado, §5.

---

## 1. A prova de dois lados, RE-EXECUTADA por mim depois do rename — **os 4 casos, e a mordida cai DENTRO de `features/panel/`**

`[MEDIDO 2026-08-29 em f35e24a, worktree do builder, `git status --porcelain` vazio antes e depois]`

| # | lado | comando | `rc` | **onde a mordida caiu** |
|---|---|---|---|---|
| 1/4 | **MORDE** | `printf … > frontend/src/features/panel/tipos.ts` ; `npm --prefix frontend run lint` | **1** | `…/frontend/src/features/panel/tipos.ts:1:38` e `:2:33` — **2** erros `@typescript-eslint/no-explicit-any` |
| 2/4 | CALA | `rm …/panel/tipos.ts` ; `npm --prefix frontend run lint` | **0** | — |
| 3/4 | **MORDE** | `printf … > …/panel/serie.tsx` ; `harness rules --mode file --path …/panel/serie.tsx --surface ci` | **1** | `[BLOQUEIO] [web-fullstack.browser-imports-server] frontend/src/features/panel/serie.tsx:1` |
| 4/4 | CALA | `test -f …/panel/Filter.tsx` ; `classify` ; `rules --mode file … --surface ci` | **0 · 0 · 0** | saída de **0 byte** |
| sweep | — | `harness rules --mode sweep` | **0** | **1 `[AVISO]`** (`browser-test-file-present`), **0 `[BLOQUEIO]`** |

**Os quatro reproduzem, e os dois que mordem mordem no diretório novo.** Nenhum mordeu fora dele
— o achado grave que o coordenador nomeou **não se materializou**.

### 1.1 O falsificador que fecha o argumento, e ele NÃO estava no relatório de build

O caso 3 sozinho **não** prova que o rename preservou a prova, e vale dizer por quê: plantei o
mesmo violador no caminho **antigo** e a regra mordeu igual —

```
$ mkdir -p frontend/src/features/painel && printf '…' > frontend/src/features/painel/serie.tsx
$ harness rules --mode file --path frontend/src/features/painel/serie.tsx --surface ci
[BLOQUEIO] [web-fullstack.browser-imports-server] frontend/src/features/painel/serie.tsx:1: …
>>> rc=1
```

⇒ a metade MORDE é **cega ao nome do diretório**: ela casa por `include_prefixes` = `frontend/src/`.
**Quem carrega o risco é a metade CALA**, e ela é a que o `harness.toml:149` cita. Medido:

```
$ harness rules --mode file --path frontend/src/features/painel/Filtro.tsx --surface ci   # caminho MORTO
>>> rc=0, 0 byte          ← IDÊNTICO ao lado CALA legítimo
$ harness code-paths classify frontend/src/features/painel/Filtro.tsx
producao: … include_prefixes + include_globs casam e nada exclui        >>> rc=0
$ test -f frontend/src/features/painel/Filtro.tsx                        >>> rc=1
```

**Se a `T-03.1` tivesse renomeado `Filtro.tsx` sem consertar `harness.toml:149`, a prova teria
continuado verde sobre um arquivo que não existe** — e `classify` não pegaria, porque é cego à
existência. **O único instrumento que pega é o `test -f` do passo (1)/(2) de `CA-F3-6`.** O builder
o executou. É por isso que juntar rename e correção da citação num só commit era obrigatório, e é
por isso que `CA-F3-6` tem três lados. **Aprovado, e com o falsificador exercitado.**

---

## 2. ⛔ `NEEDS_FIX-1` · A tabela de `CA-F3-4` mistura DOIS universos, e o comando publicado não produz os números publicados

`ADR-015/D2` decide que **cada token é medido nos dois lados**. Se um lado sai de um universo e o
outro de outro, a tabela mostra um colapso que **nenhum comando produz**, e o `0` deixa de ser
evidência sobre aquele `n`.

**O relatório de build §2 publica este comando para o tipo CAMINHO:**

```
$ git grep -n -F '<token>' -- harness.toml README.md backend/README.md frontend/README.md \
      docs/context/plataforma-dados backend/src backend/tests frontend/src | wc -l
```

**Rodei-o literalmente, no rev de ancoragem dele (`c7df90c`):**

| token | MORDE **publicado** em §2 | MORDE que o **comando publicado** devolve |
|---|---|---|
| `Filtro.tsx` | **36** | **8** |
| `painel/` | **29** | **17** |
| `rotas.ts` | **8** | **1** |
| `formatar-percentual.ts` | **3** | **1** |

`[MEDIDO 2026-08-29 em c7df90c, worktree próprio, comando literal do relatório, n=4 tokens]`

**De onde saem 36/29/8/3.** Trocando um único elemento do pathspec — `docs/context/plataforma-dados`
por `docs/context` **integral** — os quatro reproduzem **exatamente**:

```
$ git grep -n -F '<token>' -- harness.toml README.md backend/README.md frontend/README.md \
      docs/context backend/src backend/tests frontend/src | wc -l
Filtro.tsx 36 · painel/ 29 · rotas.ts 8 · formatar-percentual.ts 3      (rev c7df90c)
```

**E é aqui que o defeito fica visível: sob ESSE mesmo escopo, o lado CALA não é `0`.**

| token | escopo **integral** (`docs/context`) | escopo **enumerado** (`docs/context/plataforma-dados`) |
|---|---|---|
| `Filtro.tsx` | **36 → 32** | **8 → 0** |
| `painel/` | **29 → 15** | **17 → 0** |
| `rotas.ts` | **8 → 10**  ⬅ **SOBE** | **1 → 0** |
| `formatar-percentual.ts` | **3 → 5** ⬅ **SOBE** | **1 → 0** |

`[MEDIDO 2026-08-29, `c7df90c` → `f35e24a`, n=4 tokens × 2 escopos × 2 lados; bloco `F` da bancada]`

> ⇒ **A coluna MORDE da §2 é do escopo integral; a coluna CALA é do escopo enumerado.** Lidas
> juntas, elas afirmam `36 → 0`, e **nenhum pathspec deste repositório produz esse par.** Em dois
> tokens o escopo integral vai para **cima**, não para zero.

**O que NÃO estou dizendo.** Não estou dizendo que o builder devia ter reescrito
`docs/context/codigo-em-ingles/` — §3 abaixo diz o contrário, e a escolha dele está certa. A
escolha do escopo **enumerado** é a correta; o defeito é ter publicado o **outro** escopo na coluna
MORDE sem dizer que trocou de universo entre uma coluna e a seguinte.

**Efeito colateral que inverte um argumento do próprio relatório.** §2 conclui: *"A deriva contra o
piso do plano é para cima em 4 tokens"*. **Sob o escopo enumerado — o que a §3 defende — a deriva é
ZERO:** `8/17/1/1` em `c7df90c` é **idêntico** ao piso que o plano publica em `5f4ece0`
`[MEDIDO: SPEC-002 §4.3, tabela de MORDE]`. A emenda 1 ao DoD (*"o `n` é RE-MEDIDO, nunca
congelado"*) continua **certa como regra** — mas o exemplo com que o relatório a ilustra é do
universo errado.

**A correção é uma tabela e um parágrafo, e a árvore não muda.** Publicar a §2 no escopo
**enumerado** nos dois lados: `Filtro.tsx` **8→0** · `painel/` **17→0** · `rotas.ts` **1→0** ·
`formatar-percentual.ts` **1→0`, todos com MORDE ≥ 1 (`ADR-015/D2` satisfeito), e mover 36/29/8/3
para a §3, que é onde o escopo integral pertence e onde ele já é discutido.

---

## 3. ✅ O resíduo de `docs/context/codigo-em-ingles/` — **o argumento se sustenta**, e nenhuma das 32 é VIVA

### 3.1 As contagens, e o incremento auto-infligido está confirmado

```
$ git grep -n -F '<token>' <rev> -- docs/context/codigo-em-ingles | wc -l
```

| token | `c7df90c` | `90c65e3` (o rename) | `f35e24a` (o relatório de build) |
|---|---|---|---|
| `Filtro.tsx` | 28 | 28 | **32** |
| `painel/` | 12 | 12 | **15** |
| `rotas.ts` | 7 | 7 | **10** |
| `formatar-percentual.ts` | 2 | 2 | **5** |

`[MEDIDO 2026-08-29, n=4 tokens × 3 revs]` — **o rename não moveu nenhum dos quatro; quem moveu os
quatro foi o relatório de build.** `28 → 32` é exatamente o que o builder declarou. **Ele não
escondeu, e o número confere.**

### 3.2 O argumento é correto — e não é o que o relatório cita

O relatório apoia-se em `SPEC-002` §4.1 `[G-A1]` (*"o critério é sempre classe + conjunto enumerado
+ rev"*). Isso está certo, mas é a metade **negativa** do argumento. A metade **positiva**, que é a
que fecha, está em `SPEC-002` §4.2 e eu a medi:

```
$ git grep -c -F '<token>' 5f4ece0 -- docs/context      # o rev de ancoragem da SPEC
Filtro.tsx             -> docs/context/plataforma-dados/handoff_to_builder.md:1 · …/tasks.toml:1
formatar-percentual.ts -> docs/context/plataforma-dados/tasks.toml:1
rotas.ts               -> (nenhum)
features/painel        -> (nenhum)
```

⇒ **No rev em que a `SPEC-002` §4.2 enumerou a lista VIVA, `docs/context/codigo-em-ingles/` não
existia como citador.** A reticência de §4.2 (`docs/context/…/tasks.toml`) resolve, medida, para
`plataforma-dados` — e a `tasks.toml` desta feature a resolve por escrito, nomeando os **5 arquivos
VIVOS**. **O conjunto VIVO foi fechado antes do resíduo nascer**, e o resíduo é `[G-A1]` acontecendo,
não uma citação omitida.

**E os 5 VIVOS foram todos a zero** `[MEDIDO, bloco `E` da bancada]`:

| token | MORDE (`c7df90c`) | CALA (`f35e24a`) |
|---|---|---|
| `Filtro.tsx` · `painel/` · `rotas.ts` · `formatar-percentual.ts` | 8 · 17 · 1 · 1 | **0 · 0 · 0 · 0** |
| `configPainel` · `ROTAS` · `Rota` · `formatarPercentual` · `razao` · `casas` · `sinal` | — | **0** cada, escopo só-código |

Todos com MORDE ≥ 1 no lado enumerado ⇒ **nenhum token reprova por `n=0` no ato da declaração**
(`ADR-015/D2`).

### 3.3 Nenhuma das 32 é VIVA disfarçada de histórica — o teste que apliquei

`ADR-015/D3`: *"é **VIVA** quando existe uma obrigação em vigor, nomeável, que manda alguém resolver
aquele caminho contra a árvore de novo"*. Apliquei aos 49 lugares (união dos 4 tokens; `Filtro.tsx`
é o subconjunto de 32), arquivo a arquivo:

| arquivo | n | classe | por quê, com o teste de `D3` |
|---|---|---|---|
| `tasks.toml` | 10 | **HISTÓRICA** | **5 no cabeçalho** (registros `[MEDIDO … em 7c3599b]` — veredito sobre rev passado) e **5 dentro do `refs` da própria `T-03.1`**. Ver a medição mecânica abaixo |
| `tasks_review.md` | 18 | **HISTÓRICA** | tabelas de `n` por rev, saída de `git grep` citada, e a demonstração do word-splitting do `zsh` — todas rotuladas com o rev em que foram medidas |
| `handoff_to_builder.md` | 5 | **HISTÓRICA** | a tabela MORDE `8→12`/`17→19` e a ilustração `require-code …/Filtro.tsx`. **Re-executá-la hoje devolve o MESMO veredito** — `require-code`/`classify` são cegos à existência (medido em §1.1) ⇒ nenhuma âncora quebrou |
| `handoff_to_architect.md` | 4 | **HISTÓRICA** | peer review de um documento já revisto; conta citações de um rev passado |
| `gates/T-03.1-build.md` | 12 | **HISTÓRICA** | o próprio relatório se declara âncora histórica no cabeçalho |

**A checagem mecânica, e é a que eu não quis deixar como prosa** — o risco real é uma citação antiga
viver num `[[tasks]]` **ainda aberto**, porque aí ela instrui trabalho por vir:

```
$ grep -n 'id = "T-03.1"' …/tasks.toml            -> 221      (a T-03.1 vai de 221 a 252)
$ git grep -n -F -e 'Filtro.tsx' -e 'painel/' -e 'rotas.ts' -e 'formatar-percentual.ts' \
      -- docs/context/codigo-em-ingles/tasks.toml -> linhas 15 35 36 81 112 232 236 237 238 241
```

⇒ **as 10 estão no cabeçalho (≤ 128) ou dentro do bloco da própria `T-03.1` (221–252).**
**Zero em `T-01.1`, `T-01.2`, `T-01.3`, `T-02.1`, `T-04.1`, `T-04.2`** — as seis tasks `todo` que
sobram não citam nome antigo nenhum. Automatizado no bloco `E` da bancada, para que a próxima fase
não precise repetir a leitura à mão.

**A única ação em vigor que resta neste diretório é o `scope add`, e ela é limpa:**

```
harness pipeline scope codigo-em-ingles add CLAUDE.md backend/tests docs/INDEX.md docs/context/codigo-em-ingles
```

Quatro prefixos, **nenhum** contém nome antigo. **Nada a corrigir.** `[MEDIDO: tasks.toml:93-94,
handoff_to_builder.md:127-129]`

**Concordo com o builder em não tocar**, e acrescento o argumento que faltava: reescrever
`tasks_review.md` e o `handoff_to_architect.md` apagaria a **evidência do próprio `[G-A1]`** — a
medição `8 → 12` cujo objeto é a citação. Um documento que registra "esta contagem cresce sozinha"
não pode ser normalizado sem destruir o que ele mede.

---

## 4. ⚠️ `NEEDS_FIX-2` · `CA-F1-6` — **nem 13 nem 15**; sob o comando do plano são **14**, e a causa publicada é falsa

O plano publica **13**, o relatório publica **15**, e os dois usam **comandos diferentes**. Rodei os
dois, nos dois revs.

```
# forma da SPEC-002 §4.3 / plano 01:
$ git ls-tree -r --name-only <rev> | grep -E '^(backend/src|backend/tests|frontend/src)/' \
    | awk -F/ '{for(i=1;i<NF;i++) print $i}' | sort -u | grep -vxE 'sentimento|charts|convergencia|backtest|web|docs'
# forma do relatório de build:
$ git ls-files backend/src backend/tests frontend/src | xargs -n1 dirname | tr '/' '\n' | sort -u
```

| forma | `c7df90c` (antes) | `f35e24a` (depois) | conjunto |
|---|---|---|---|
| **SPEC / plano** (com `grep -vxE`) | **14** | **14** | `app backend components domain features frontend helpers infra modules `**`painel`**`→`**`panel`**` src tests ui use_cases` |
| **relatório de build** (sem filtro) | **15** | **15** | o mesmo **+ `sentimento`** |

`[MEDIDO 2026-08-29, n=2 comandos × 2 revs]`

**Duas correções, e nenhuma é a que o relatório fez:**

**(a) A causa publicada está errada.** §6 diz: *"a diferença é quais raízes entram (`backend`,
`frontend`, `src`)"*. **Falso, e o `diff` dos dois conjuntos o mostra em uma linha:** o conjunto do
plano **já contém** `backend`, `frontend`, `src` e `tests`. A **única** diferença é `sentimento` —
o `grep -vxE` que filtra os nomes de componente e que o comando do relatório **não tem**.

```
$ diff <(forma-SPEC f35e24a) <(forma-build f35e24a)
10a11
> sentimento
```

**(b) O `13` do plano está errado sob o próprio comando do plano — o certo é `14`.** O plano previu
*"Depois de `03`: **13**, zero português"*, e a aritmética supõe que o segmento `painel` **sai** do
conjunto. Ele não sai: ele vira `panel`, que é inglês e **continua no conjunto**. **`14 → 14`**, com
o número de segmentos em português indo de **1 → 0**. `docs/plans/SPEC-002-codigo-em-ingles/03_retroativo_frontend.md:47`
e a linha do plano `01` propagam o mesmo `13`.

**O veredito do critério vale nas duas formas, e isso eu confirmo:** o conjunto de segmentos em
português é `{sentimento}`, **tamanho 1**, e casa **por igualdade de string** com
`harness policy --key components`. **`CA-F1-6` CALA.** `[MEDIDO, bloco `D` da bancada]`

**A instinto do builder — *"publico o meu comando em vez de ajustar o número"* — está certo**, e é o
que `CLAUDE.md` manda. O que reprova é ter acoplado a ele um **diagnóstico não medido** da
divergência. Correção: substituir a frase da causa pelo `diff` acima, e nomear o `13` do plano como
achado a devolver ao `/architect` — porque `CA-F1-6` é *"o único critério que sobrevive ao fim da
feature"*, e um número errado num detector de longa vida é ruído permanente.

---

## 5. ✅ Cobertura ≠ ESLint — **o que garante que os 4 arquivos ainda fazem o que faziam**

**A ausência, medida antes de qualquer conserto:**

```
$ cat frontend/package.json | grep -A2 '"scripts"'    ->  um script: "lint": "eslint src"
$ sed -n '/^lint-frontend:/,/^$/p' Makefile           ->  npm --prefix frontend run lint, e nada mais
$ find frontend -name '*.test.*' -not -path '*/node_modules/*' | wc -l   ->  0
```

⇒ **não há `vitest`, não há `tsc` no portão, não há um único teste de frontend.** ESLint lê **regra
de estilo**, não **comportamento**: ele aprovaria com nota cheia um `format-percentage.ts` cujo
`toFixed` passou a arredondar para o outro lado. Depois de um rename que troca **9 identificadores
em 4 arquivos**, *"o lint passou"* **não é evidência**.

**O que eu NÃO podia fazer, e por quê.** Criar `Filter.test.tsx` fecharia o `[AVISO]`
`web-fullstack.browser-test-file-present` e quebraria `CA-F3-1` (`find frontend/src -type f` → 4).
O `NÃO FAZ` da task proíbe as duas coisas, **e a proibição está certa** — é dívida de outra trilha.

**O que eu fiz:** `docs/context/codigo-em-ingles/gates/T-03.1-qa-bench.sh`. Ele extrai as duas
versões por `git show` para um `mktemp -d` **fora do repositório**, compila as duas com o `tsc` que
já está em `frontend/node_modules`, e **executa as duas lado a lado**. `frontend/src` não é tocado;
nenhum `*.test.*` nasce; `find frontend/src -type f` continua **4** e o `sweep` continua **1 `[AVISO]`,
0 `[BLOQUEIO]`** com o arquivo na árvore `[MEDIDO]`.

```
$ bash docs/context/codigo-em-ingles/gates/T-03.1-qa-bench.sh
>>> rc=0 — 41 checagens, 0 falha
```

**O invariante não é *"o objeto é igual"*, e essa distinção é o contrato:** a chave `painel:` →
`panel:` muda **por mapa normativo** enquanto o valor `"/painel"` fica em português (`CA-F3-8`,
`[Q2]` do owner). Logo: **chaves mapeadas pelo mapa, VALORES idênticos.** Um `deepEqual` ingênuo
reprovaria o build correto — e reprovou, na primeira versão desta bancada.

| o que | asserções | resultado |
|---|---|---|
| `formatarPercentual(razao, casas)` × `formatPercentage(ratio, digits)` — 13 razões (`0`, `±1`, `±0.5`, `1e-9`, `-0`, `±Infinity`, `NaN`, …) × 5 casas (`0,1,2,5`, omitido) | **65** | **saída idêntica em todas** |
| `ROUTES` × `ROTAS` — aridade, chaves pelo mapa, e o **valor** de cada chave | **4** | idênticos; `ROUTES.panel === "/painel"` |
| `panelConfig` × `configPainel` — `deepEqual` | **1** | idêntico (`{ retry: 3, any: true }`) |
| **total** | **70** | — |

**E verde não prova nada até uma mutação reprovar.** A bancada **se falsifica sozinha**, num bloco
que roda sempre:

```
=== A' · mutacao — o bloco A tem de REPROVAR quando o comportamento muda ===
  sed -i 's/toFixed(digits)/toFixed(digits + 1)/'   # no lado NOVO, recompila, re-executa
  [OK  ] mutante morto: toFixed(digits) -> toFixed(digits + 1) reprova o bloco A
```

**Se o mutante sobrevivesse, a bancada reprovaria a si mesma** (`fail`, `rc=1`) — porque um `assert`
que nunca olha nada é indistinguível de um que olha.

**E ela distingue `rc=1` de `rc=3`**, que é a regra desta fase. Exercitado, não afirmado:

```
$ mv frontend/node_modules /tmp/nm_stash && bash …/T-03.1-qa-bench.sh
RECUSA (rc=3): frontend/node_modules ausente — sem ele nao ha `tsc` e NADA foi medido.
>>> rc=3
```

**Complemento estrutural, para os arquivos sem comportamento executável.** `Filter.tsx` precisaria
de runtime React, que não está instalado. Para ele e para os outros três a evidência é o `diff`
literal velho×novo, que contém **apenas** o mapa de `SPEC-002` §3.2 e nada mais:

```
$ diff <(git show c7df90c:frontend/src/features/painel/Filtro.tsx) <(git show HEAD:frontend/src/features/panel/Filter.tsx)
9c9
< export function Filtro() {                 ---   > export function Filter() {
```

**Uma linha, e é o identificador do mapa.** Os outros três, idem: `ROTAS`/`Rota`/`painel:` em
`routes.ts`, os quatro de `format-percentage.ts` (+ a citação de caminho no comentário),
`configPainel` em `config.ts`. **Zero mudança fora do mapa normativo** `[MEDIDO, n=4 arquivos]`.

---

## 6. ✅ Linha de base — medida nos DOIS revs, em worktrees separados

```
$ diff <(grep -E 'passed|^TOTAL|\[OK  \]' qa_test_antes.log) \
       <(grep -E 'passed|^TOTAL|\[OK  \]' qa_test_depois.log)
2c2
< 107 passed in 5.95s
---
> 107 passed in 13.07s
```

**A única diferença é o relógio de parede**, que não é medição de suíte.

| | `c7df90c` (worktree próprio) | `f35e24a` (worktree do builder) |
|---|---|---|
| `bash backend/scripts/test.sh` | **rc=0**, `107 passed` | **rc=0**, `107 passed` |
| `TOTAL` | **370** statements · **0** miss · **54** branch · **0** partial · **100%** | **idêntico** |
| por camada | `domain 124/124` · `use_cases 52/52` · `infra 194/194` | **idêntico** |
| `bash backend/scripts/lint.sh` | — | **rc=0** |
| `npm --prefix frontend run lint` | — | **rc=0** |
| `make lint` | — | **rc=0** |

`107 · 370 · 54 · 124/52/194` **conferidos, e `CA-F2-3` reprova inclusive para mais.** `backend/src`
não aparece no diff — o rename não vazou para o backend.

---

## 7. As 7 regras bloqueantes, uma a uma, com evidência citável

`harness rules list --severity block` → **7**, antes e depois. Nenhuma `[[rules.own]]` nasceu:
`git diff c7df90c...HEAD -- harness.toml | grep -c 'rules.own'` → **0** (`ADR-011/D1.10` reprovaria).

| # | regra | veredito | evidência |
|---|---|---|---|
| 1 | `core.relative-import` | **OK** | `harness rules --mode sweep` → rc=0, **0 `[BLOQUEIO]`**. Escopo `code` = `backend/src`, `backend/tests`, `frontend/src`; os 4 arquivos de `frontend/src` foram varridos |
| 2 | `core.silent-except` | **OK** | idem sweep |
| 3 | `core.print-statement` | **OK** | idem sweep — e o `console.log` do violador plantado é **removido pelo `rm`** do caso 3; `git status` vazio depois |
| 4 | `core.hardcoded-secret` | **OK** | idem sweep, escopo `production` |
| 5 | `web-fullstack.browser-imports-server` | **OK**, e **MEDIDA NOS DOIS LADOS** | CALA: `rules --mode file --path …/panel/Filter.tsx --surface ci` → rc=0, **0 byte**. MORDE: caso 3/4 → **rc=1**, `[BLOQUEIO]` em `…/panel/serie.tsx:1` |
| 6 | `web-fullstack.tenant-from-request` | **OK** | idem sweep |
| 7 | `web-fullstack.server-test-directory-present` | **OK** | `path-presence`; `backend/tests` existe e a suíte roda com 107 testes |

**O arquivo que eu acrescentei não entra em nenhuma:**
`harness code-paths classify docs/context/codigo-em-ingles/gates/T-03.1-qa-bench.sh` →
`nao-producao — nenhum include_prefixes casa`, e `rules --mode file … --surface ci` → **rc=0, 0 byte**.
`bash -n` → **rc=0**.

---

## 8. DoD da fase, item a item

| # | critério | veredito | evidência |
|---|---|---|---|
| `CA-F3-1` | 4 arquivos, nome-base inglês, zero segmento português | **OK** | `find frontend/src -type f` → **4**: `app/routes.ts`, `components/ui/format-percentage.ts`, `features/panel/config.ts`, `features/panel/Filter.tsx` |
| `CA-F3-2` | a evidência protegida SOBREVIVE | **OK** | `grep -F 'Filtro: any resultado serve' …/Filter.tsx` → 1 linha, rc=0; `grep -F 'any: true' …/config.ts` → 1 linha, rc=0 |
| `CA-F3-3` | os 4 casos, DEPOIS do rename | **OK** | §1 — reproduzidos por mim, 2 mordem dentro de `features/panel/` |
| `CA-F3-4` | nenhum nome antigo em citação viva | **OK na árvore, `NEEDS_FIX` no relatório** | §3.2: os 5 VIVOS a **0**. §2: a tabela publicada mistura escopos |
| `CA-F3-5` | a sonda é atualizada **e re-medida** | **OK** | `grep -c 'Filtro' harness.toml` → **0**; `classify …/__sonda__/Filter.test.tsx` → `producao`, **rc=0** |
| `CA-F3-6` | os TRÊS lados | **OK** | (1) rc=0 ×4 · (2) **rc=1 ×4** · (3) `producao` ×4. Bloco `B` da bancada, + o controle de vacuidade de §1.1 |
| `CA-F3-7` | nenhuma HISTÓRICA tocada | **OK** | `git diff --numstat c7df90c...HEAD -- docs/INDEX.md docs/adr docs/plans docs/proposta-discovery.md docs/specs/PRD-001-plataforma-dados.md` → **vazio**. Base comum, três pontos, correto |
| `CA-F3-8` | `"/painel"` fica em português | **OK** | `routes.ts:15` → `panel: "/painel",` — mista de propósito |
| `CA-F3-9` | árvore verde e MEDIDA, `rc=3` reprova | **OK** | §6 |
| `CA-F1-4` | nenhuma regra nasce | **OK** | §7 |
| `CA-F1-6` | medidor de erosão | **OK no veredito, `NEEDS_FIX` no número** | §4 — `{sentimento}`, tamanho 1; mas 13 ≠ 14 e a causa publicada é falsa |
| `RN-3` | continente renomeia, conteúdo citado não | **OK** | `frontend/README.md:250`: a célula `` `Filter.tsx` `` atualizou e `<p>Filtro: any resultado serve</p>` **na mesma linha** não |
| `NÃO FAZ` | sem `Filter.test.tsx`, sem fechar o `[AVISO]`, sem `[[rules.own]]` | **OK** | sweep → **1 `[AVISO]`**; `grep -c 'rules.own'` no diff → 0; a minha bancada mora em `docs/`, fora de `frontend/src` |

---

## 9. Anomalias — resposta desconhecida não é `OK` e não é `FAIL`

**`shellcheck` não está instalado** `[MEDIDO: `command -v shellcheck` → rc=1]`. A bancada que eu
acrescentei passou `bash -n` (rc=0) e executou de ponta a ponta duas vezes, mas **não houve análise
estática de shell**. Não é regra bloqueante deste repositório (`harness policy --key test_cmd`
declara apenas `sentimento`: `backend/scripts/{lint,test}.sh`), e por isso **não reprova** — mas
fica declarado em vez de virar um verde que ninguém mediu.

---

## 10. Fora do escopo — confirmado, não é achado meu

**`T-01.1`/`T-01.2` não estão na árvore.** `grep -c 'ingles\|inglês' CLAUDE.md` → **0** em `c7df90c`
`[MEDIDO]`. O builder declarou e o argumento dele se sustenta: o mapa aplicado é `SPEC-002` §3.2,
que **está** na árvore e é normativo e fechado. **É decisão de quem mergeia.**

**`backend/README.md` colide em arquivo com a `T-02.1`.** Linhas `:455`/`:457` aqui, `:568+` lá,
~110 de distância. Previsto pela emenda 3 e pelo handoff. **A segunda a mergear rebaseia.**

**`harness pipeline scope codigo-em-ingles list` → 0 byte.** O portão devolve verde por
`plataforma-dados`. **Não mexi no ledger** — é decisão do owner, e a linha de comando pronta não
contém nome antigo (§3.3).

---

## 11. Ações — `NEEDS_FIX`, e as três são só de documento

1. **`T-03.1-build.md` §2** — republicar a tabela de `CA-F3-4` no escopo **enumerado** nos dois
   lados (`8→0`, `17→0`, `1→0`, `1→0`), e mover `36/29/8/3` para a §3, rotulados como escopo
   integral. Retirar a frase da *"deriva para cima em 4 tokens"*: sob o escopo enumerado a deriva é
   **zero**.
2. **`T-03.1-build.md` §6** — trocar *"a diferença é quais raízes entram"* pelo `diff` medido: a
   única diferença é `sentimento`, filtrado pelo `grep -vxE` que o comando do relatório omite.
   Publicar as **duas** formas com os seus `n` (**14** e **15**).
3. **Devolver ao `/architect`** (não é do builder consertar): `SPEC-002` §`CA-F1-6` e
   `docs/plans/SPEC-002-codigo-em-ingles/{01_convencao_escrita.md:37, 03_retroativo_frontend.md:47}`
   publicam **13** para depois de `F3`; o comando deles devolve **14**, porque `painel` não sai do
   conjunto — vira `panel`. **`CA-F1-6` é o único critério que sobrevive ao fim da feature**, e o
   número errado fica.

**Nenhuma ação toca código de produção.** Depois das três, esta fase está pronta para `APPROVED`.

---

## Anexos

- `docs/context/codigo-em-ingles/gates/T-03.1-qa-bench.sh` — a bancada, `rc=0`, 41 checagens,
  mutação incluída, recusa `rc=3` exercitada. **Re-executável:**
  `bash docs/context/codigo-em-ingles/gates/T-03.1-qa-bench.sh [REV_ANTES] [REV_DEPOIS]`
- `docs/context/codigo-em-ingles/gates/T-03.1-build.md` — o relatório sob teste.
