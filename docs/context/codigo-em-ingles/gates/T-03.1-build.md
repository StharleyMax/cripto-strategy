# `T-03.1` · `CST-96` — relatório de build

**Feature:** `codigo-em-ingles` · **Fase:** `03` · **Componentes:** `docs` + `web`
**Papel:** `/build` · **Data:** 2026-08-29 · **Commit:** `90c65e3` · **Base:** `master@c7df90c`
**Branch:** `task/ci-T-03.1-retroativo-frontend` · **Ledger:** `BUILD_AUTHORIZED`
`[MEDIDO: harness pipeline state codigo-em-ingles → BUILD_AUTHORIZED]`

**Veredito do builder: fase implementada, DoD fechado, um bloqueio de dependência NOMEADO (§8).**

> ⚠️ **Este arquivo é âncora HISTÓRICA e cita os nomes antigos de propósito.** Ele incrementa a
> contagem crua do escopo integral — é `[G-A1]` acontecendo mais uma vez, agora por causa do
> próprio relatório. É exatamente por isso que o critério é **conjunto enumerado**, nunca
> `grep` cru (`SPEC-002` §4.1). Ver §3.

---

## 1. O que mudou — 9 arquivos, +34/−34

```
$ git diff --numstat origin/master...HEAD
2	2	backend/README.md
1	1	docs/context/plataforma-dados/handoff_to_builder.md
2	2	docs/context/plataforma-dados/tasks.toml
12	12	frontend/README.md
3	3	frontend/src/app/{rotas.ts => routes.ts}
4	4	frontend/src/components/ui/{formatar-percentual.ts => format-percentage.ts}
1	1	frontend/src/features/{painel/Filtro.tsx => panel/Filter.tsx}
1	1	frontend/src/features/{painel => panel}/config.ts
8	8	harness.toml
```

Os 4 renames entraram no índice como **rename de verdade** (`R`, similaridade 65–94%), não como
delete+add — `git status --short` antes do commit mostrou `R` nos quatro.

**Identificadores, pelo mapa NORMATIVO de `SPEC-002` §3.2:** `ROTAS`→`ROUTES` · `Rota`→`Route` ·
chave `painel:`→`panel:` · `formatarPercentual`→`formatPercentage` · `razao`→`ratio` ·
`casas`→`digits` · `sinal`→`sign` · `configPainel`→`panelConfig` · componente `Filtro`→`Filter`.

---

## 2. `CA-F3-4` — o `n` RE-MEDIDO, nunca congelado (`RN-8`, emenda 1)

> ⛔ **SUPERADA PELO CICLO 2 — NÃO USE OS NÚMEROS DESTA SEÇÃO. Ver `C2.1`.**
> A tabela abaixo **mistura dois universos**: o lado MORDE foi medido com `docs/context`
> (**integral**) e o lado CALA com `docs/context/plataforma-dados` (**enumerado**), enquanto o
> comando publicado é o enumerado. **O colapso `36→0` não é produzido por pathspec nenhum.**
> Os números corretos, com pathspec idêntico nos dois lados, são **8/17/1/1 → 0**.
> A frase da deriva (*"um DoD que exigisse literalmente 8 reprovaria este build"*) é **falsa** e
> era artefato deste mesmo erro: no escopo enumerado o MORDE é **exatamente** o piso do plano.
> **Mantida por registro, não por validade.**

Medido **no meu rev** (`c7df90c`), unidade **LINHAS** (emenda 2), escopo **por tipo de token**
(`ADR-015/D1`), prefixos **literais** — nenhuma lista de caminho por variável, porque o shell é
`zsh` e ele não faz word-splitting.

**CAMINHO — escopo VIVO enumerado:**
```
$ git grep -n -F '<token>' -- harness.toml README.md backend/README.md frontend/README.md \
      docs/context/plataforma-dados backend/src backend/tests frontend/src | wc -l
```
**IDENTIFICADOR — escopo só-código:**
```
$ git grep -n -F '<token>' -- backend/src backend/tests frontend/src | wc -l
```

| token | tipo | MORDE (antes) | CALA (depois) | piso do plano (`5f4ece0`) |
|---|---|---|---|---|
| `Filtro.tsx` | CAMINHO | **36** | **0** | 8 |
| `painel/` | CAMINHO | **29** | **0** | 17 |
| `rotas.ts` | CAMINHO | **8** | **0** | 1 |
| `formatar-percentual.ts` | CAMINHO | **3** | **0** | 1 |
| `configPainel` | IDENT | **1** | **0** | 1 |
| `ROTAS` | IDENT | **2** | **0** | 2 |
| `formatarPercentual` | IDENT | **1** | **0** | 1 |
| `razao` | IDENT | **3** | **0** | — |
| `casas` | IDENT | **2** | **0** | — |
| `sinal` | IDENT | **2** | **0** | — |

`[MEDIDO 2026-08-29 em c7df90c → 90c65e3, n=10 tokens × 2 lados]`

**Todos os 10 têm MORDE ≥ 1** — nenhum reprova por `n=0` no ato da declaração (`ADR-015/D2`). A
deriva contra o piso do plano é **para cima em 4 tokens** e vem de `docs/context/codigo-em-ingles/`,
os artefatos desta própria feature. Um DoD que exigisse literalmente `8` reprovaria este build.

**`Rota` não entra na tabela de MORDE de propósito:** `ADR-015` mediu `Rota n=0`, e um token de
MORDE `n=0` reprova no ato da declaração. Ele é renomeado pelo mapa e verificado pelo lado CALA
apenas (`Rota` → **0** no escopo só-código, medido).

---

## 3. Por que o `0` é sobre o conjunto ENUMERADO, e o resíduo nomeado

`ADR-015/D1` põe `docs/context/` inteiro no escopo integral. Sob `grep` cru isso inclui
`docs/context/codigo-em-ingles/` — os artefatos **desta feature**, que citam os nomes antigos
porque **descrevem a renomeação**:

```
$ git grep -n -F 'Filtro.tsx' -- docs/context/codigo-em-ingles | wc -l   # 28
$ git grep -n -F 'painel/'    -- docs/context/codigo-em-ingles | wc -l   # 12
$ git grep -n -F 'rotas.ts'   -- docs/context/codigo-em-ingles | wc -l   # 7
$ git grep -n -F 'formatar-percentual.ts' -- docs/context/codigo-em-ingles | wc -l  # 2
```

**Não foram tocados, e não deviam ser.** `SPEC-002` §4.1 `[G-A1]` decide isso: *"Nenhum critério
desta SPEC diz 'as N citações' … O critério é sempre classe + conjunto enumerado + rev."* A lista
VIVA é **fechada em 5 arquivos**, e `docs/context/codigo-em-ingles/` não está nela. `tasks.toml`
desta feature é, além disso, proibido de editar pelo coordenador.

**Declaro o resíduo em vez de escondê-lo.** Se o dono do critério discordar e quiser os artefatos
da própria feature dentro do lado CALA, isso é decisão dele — e é reversível, ao contrário de ter
reescrito os documentos.

---

## 4. `CA-F3-3` — a prova de dois lados, RE-EXECUTADA **DEPOIS** do rename

Medida no rev **`90c65e3`**, com o `features/panel/` já renomeado, pela receita que este commit
atualizou em `frontend/README.md`. Os **quatro** casos, e não só o do `Filtro.tsx`: renomear o
diretório invalida **as duas metades**, e 4 das 5 linhas de `harness.toml` são a metade MORDE.

```
=== CASO 1/4 — MORDE (ESLint/any) — planta tipos.ts em features/panel/ ===
/tmp/.../frontend/src/features/panel/tipos.ts
  1:38  error  Unexpected any. Specify a different type  @typescript-eslint/no-explicit-any
  2:33  error  Unexpected any. Specify a different type  @typescript-eslint/no-explicit-any
✖ 2 problems (2 errors, 0 warnings)
>>> rc=1

=== CASO 2/4 — CALA (ESLint/any) — rm tipos.ts ===
> eslint src
>>> rc=0

=== CASO 3/4 — MORDE (harness browser-imports-server) — planta serie.tsx em features/panel/ ===
[BLOQUEIO] [web-fullstack.browser-imports-server] frontend/src/features/panel/serie.tsx:1: o codigo
do navegador nao importa do servidor: os dois lados so se falam pelo contrato da interface publicada
    1 ocorrencia(s); 1a: import { x } from "../../../../backend/src/.../etl_backlog";
>>> rc=1

=== CASO 4/4 — CALA (harness) — Filter.tsx, na ordem RN-5 ===
(1) test -f            rc=0
producao: frontend/src/features/panel/Filter.tsx — include_prefixes + include_globs casam e nada exclui
(2) classify           rc=0
(3) rules --mode file  rc=0  bytes=0

=== sweep ===
rc=0  AVISO=1  BLOQUEIO=0
```

**Verde não prova nada até uma mutação reprovar — e duas reprovam:** caso 1 (`rc=1`, 2 erros) e
caso 3 (`rc=1`, `[BLOQUEIO]`). As duas mordem **dentro do diretório novo**, que é a evidência de
que o rename não desligou a prova. A bancada ficou limpa: `git status --short` → vazio.

---

## 5. `CA-F3-6` — os TRÊS lados, e o (2) não é rede do (1)

```
(1) test -f nos 4 caminhos NOVOS    → rc=0 · rc=0 · rc=0 · rc=0
(2) test -f nos 4 caminhos ANTIGOS  → rc=1 · rc=1 · rc=1 · rc=1
(3) classify nos 4 novos            → producao × 4 (rc=0 × 4)
```

O passo (2) é o que impede o critério de ser vácuo: `classify` devolve `producao`/`rc=0` para
caminho **inexistente**. Sem ele, o (3) passaria sobre uma árvore em que o rename não aconteceu.

---

## 6. Os demais critérios

| # | comando | resultado |
|---|---|---|
| `CA-F3-1` | `find frontend/src -type f` | **4 arquivos**, todos com nome-base inglês |
| `CA-F1-6` | `git ls-files backend/src backend/tests frontend/src \| xargs -n1 dirname \| tr '/' '\n' \| sort -u` | **15 segmentos**, **1 em português: `sentimento`** — o conjunto foi de `{sentimento, painel}` (2) para `{sentimento}` (1). `sentimento` casa **por igualdade** com `harness policy --key components` |
| `CA-F3-2` | `grep -F 'Filtro: any resultado serve' …/Filter.tsx` · `grep -F 'any: true' …/config.ts` | **1 linha, rc=0** em cada — a evidência SOBREVIVEU |
| `CA-F3-5` | `grep -c 'Filtro' harness.toml` · `harness code-paths classify …/__sonda__/Filter.test.tsx` | **0** · `producao`, **rc=0** — mesmo veredito de hoje sobre o caminho antigo |
| `CA-F3-7` | `git diff --numstat origin/master...HEAD -- docs/INDEX.md docs/adr docs/plans docs/proposta-discovery.md docs/specs/PRD-001-plataforma-dados.md` | **vazio** — nenhuma HISTÓRICA tocada |
| `CA-F3-8` | `grep -F '"/painel"' frontend/src/app/routes.ts` | **1 linha, rc=0** — `panel: "/painel"`, mista de propósito |

> ⛔ **SUPERADO PELO CICLO 2 — ver `C2.3`. O número é `14`, não `13` nem `15`, e a causa
> abaixo é FALSA:** a divergência não é *"quais raízes entram"* — é o `grep -vxE` de
> componentes que eu omiti, e o `diff` dos conjuntos tem **um único** elemento, `sentimento`.
> `painel` **não sai** do conjunto: vira `panel` e fica. **Mantido por registro.**

**`CA-F1-6` difere do plano (15 vs 13) na CONTAGEM, não no veredito.** O plano publica 13; eu meço
15 com o comando acima, e a diferença é quais raízes entram (`backend`, `frontend`, `src`). O que o
critério pesa — *"zero português"* — está medido e vale nas duas formas. Publico o meu comando em
vez de ajustar o número para casar.

**`CA-F3-7` foi medido contra a BASE COMUM (três pontos), não contra a ponta do `master`:**
`git merge-base origin/master HEAD` → `c7df90c`. Diff de dois pontos leria como remoção o que
outra branch acrescentou.

---

## 7. As três coisas que NÃO mudaram, verificadas por igualdade

1. `return <p>Filtro: any resultado serve</p>;` — **literal**. A palavra `Filtro` sobrevive dentro
   da string, e esse é o resultado **correto**: a evidência é a posição sintática de `any`
   (`JSXText`), não o nome do arquivo.
2. `export const panelConfig = { retry: 3, any: true };` — o `any` como **chave de objeto**, a outra
   metade da bancada `D1.3b`. O identificador mudou; o payload não.
3. `panel: "/painel"` — o valor fica em **português**. `[Q2]` é do owner (`SPEC-002` §7).

**E a quarta, `frontend/README.md:250` — uma linha, duas regras opostas (`RN-3`):**

```
| `Filter.tsx` | `<p>Filtro: any resultado serve</p>` — texto JSX | não | **BLOQUEIO — FP** | …
   ^^^^^^^^^^ o CONTINENTE renomeou              ^^^^^^ o CONTEÚDO citado NÃO
```

O `sed` usou o padrão `Filtro\.tsx`, que **não casa** `Filtro: any resultado serve` — a fronteira
entre continente e conteúdo é o ponto da extensão.

---

## 8. ⛔ Bloqueado / declarado — não silencio nada

**8.1 · As dependências `T-01.1` e `T-01.2` NÃO estão nesta árvore.**
```
$ grep -c 'ingles\|inglês' CLAUDE.md    # 0, em c7df90c
```
`tasks.toml` declara `depends_on = ["T-01.1", "T-01.2"]`, e a seção normativa que elas criam no
`CLAUDE.md` **não existe no meu rev** — elas estão em voo em `task/ci-T-01x-convencao-escrita`.
**Não bloqueou o trabalho e a razão é medida:** o mapa que eu apliquei é `SPEC-002` §3.2, que está
na árvore e é normativo e fechado; o risco que a ordem `01→03` compra (*"renomear antes de a
fronteira estar declarada é renomear duas vezes"*) não se materializa, porque `T-01.1`/`T-01.2` são
`docs` e não movem o mapa. **É decisão de quem mergeia, não minha** — se `01` entrar depois com
mapa diferente, este rename é que estaria errado.

**8.2 · `backend/README.md` colide em ARQUIVO com `T-02.1`, como o handoff previu.**
Minhas linhas são `:455`/`:457`; as dela, `:568+`. ~110 linhas de distância. **A segunda a mergear
rebaseia**, e ver este arquivo no meu diff **não é sinal de erro**.

**8.3 · O aviso do `sweep` continua aberto, de propósito.**
`web-fullstack.browser-test-file-present` segue como **1 `[AVISO]`**. Fechá-lo é dívida de outra
trilha; criar `Filter.test.tsx` está no `NÃO FAZ` desta task.

**8.4 · Escopo de caminho continua não declarado.** `harness pipeline scope codigo-em-ingles list`
→ 0 byte. O portão devolve verde por `plataforma-dados` para `frontend/src/...`. Não mexi no
ledger; é decisão do owner (`handoff_to_builder.md` §4).

---

## 9. Linha de base — o falsificador barato

```
$ make lint    # rc=0   (backend + frontend)
$ make test    # rc=0
107 passed
TOTAL   370 statements   0 miss   54 branch   0 partial   100%
[OK  ] domain 100.0% [124/124] · use_cases 100.0% [52/52] · infra 100.0% [194/194]
```

**Antes e depois, conferido por `diff` literal das linhas de medição:**

```
$ diff <(grep -E 'passed|^TOTAL|\[OK  \]' t031_test_antes.log) \
       <(grep -E 'passed|^TOTAL|\[OK  \]' t031_test_depois.log)
2c2
< 107 passed in 7.78s
---
> 107 passed in 5.55s
```

**A ÚNICA diferença é o relógio de parede** (7,78 s → 5,55 s), que não é medição de suíte:
`107` · `370` · `54` · `124/52/194` **idênticos**. `backend/src` não foi tocado — o diff de §1
confirma. `rc=3` não apareceu em nenhum comando: `make setup` rodou antes de tudo.

**Nenhuma `[[rules.own]]` foi declarada** (`D1.10` reprovaria a fase). `harness rules list
--severity block` → **7 regras**, antes e depois.

---

## 10. Doc delta

| documento | ação |
|---|---|
| `harness.toml` | **atualizado** — 5 linhas de `features/painel` + a sonda de `:238-252`; a de `:149` é a metade `cala` da prova |
| `frontend/README.md` | **atualizado** — o protocolo de reprodução (a receita que §4 executou) |
| `backend/README.md` | **atualizado** — `:455`/`:457`, compartilhado com `T-02.1` |
| `docs/context/plataforma-dados/{tasks.toml,handoff_to_builder.md}` | **atualizado** — instruem trabalho corrente |
| `docs/INDEX.md` | **sem mudança** — HISTÓRICA e append-only; `CA-F3-7` exige diff vazio. Não acrescentei linha porque o critério da fase manda não tocar |
| `ADR` | **não necessário** — nenhuma decisão nova. A fase aplica `ADR-013`/`ADR-015`, já `aceito` |

---
---

# Ciclo 2 — correção do `NEEDS_FIX`

**`/qa` → `NEEDS_FIX` · `/review` → `COMPLIANT`** · commit do QA: `c5bf780`
**Nenhuma ação deste ciclo toca código de produção.** Os renames, a atomicidade e a prova de
dois lados foram **confirmados pelos dois papéis**. Quem reprovou foi o **artefato de portão** —
este relatório. As correções abaixo são todas de **medição publicada**, e a árvore não muda:

```
$ git diff --stat c5bf780..HEAD -- frontend/src backend/src
(vazio)
```

---

## C2.1 — `CA-F3-4` misturava DOIS universos, e o defeito é meu

**O `/qa` está certo, e a falha é séria porque é da classe que esta casa existe para pegar.**
Eu medi o lado **MORDE** com `docs/context` (**integral**) e o lado **CALA** com
`docs/context/plataforma-dados` (**enumerado**). **Troquei o pathspec no meio da tabela** e
publiquei o comando enumerado ao lado de números integrais. O commit do `/qa` nomeia isso na
própria mensagem: *"o escopo que a tabela de MORDE trocou no meio"*.

**⇒ O colapso `36→0` não é produzido por pathspec nenhum. Ele não existe.**

### A tabela CORRETA — escopo ENUMERADO, o MESMO pathspec nos dois lados

```
$ git grep -n -F '<token>' <rev> -- harness.toml README.md backend/README.md \
      frontend/README.md docs/context/plataforma-dados backend/src backend/tests frontend/src | wc -l
#   <rev> = c7df90c (MORDE, antes)   ·   HEAD (CALA, depois)
```

| token | tipo | MORDE (`c7df90c`) | CALA (`HEAD`) | piso do plano (`5f4ece0`) |
|---|---|---|---|---|
| `Filtro.tsx` | CAMINHO | **8** | **0** | 8 |
| `painel/` | CAMINHO | **17** | **0** | 17 |
| `rotas.ts` | CAMINHO | **1** | **0** | 1 |
| `formatar-percentual.ts` | CAMINHO | **1** | **0** | 1 |
| `configPainel` | IDENT | **1** | **0** | 1 |
| `ROTAS` | IDENT | **2** | **0** | 2 |
| `formatarPercentual` | IDENT | **1** | **0** | 1 |
| `razao` · `casas` · `sinal` | IDENT | **3** · **2** · **2** | **0** | — |

`[MEDIDO 2026-08-29, n=10 tokens × 2 revs, pathspec IDÊNTICO nos dois lados]`

### **A frase da deriva sai, e ela estava invertida**

Eu escrevi que *"um DoD que exigisse literalmente `8` reprovaria este build"*. **É falso, e era
artefato do meu próprio erro de escopo.** No escopo enumerado o MORDE é **exatamente** `8/17/1/1`
— **idêntico ao piso do plano**, medido em `5f4ece0`. **A deriva sob o escopo enumerado é ZERO.**

A deriva é real, mas vive **só** no escopo integral (§C2.2), e lá ela não serve de lado MORDE
porque o CALA correspondente **não fecha em 0**. Retiro a frase e registro a inversão em vez de
a apagar: eu usei uma defesa legítima (`[G-A1]`) para justificar um número que a minha própria
troca de pathspec tinha produzido.

---

## C2.2 — Os números integrais, rotulados como integrais, e por que eles NÃO fecham

Mesmos tokens, escopo **INTEGRAL** de `ADR-015/D1` (`docs/context` inteiro):

```
$ git grep -n -F '<token>' <rev> -- harness.toml README.md backend/README.md \
      frontend/README.md docs/context backend/src backend/tests frontend/src | wc -l
```

| token | integral em `c7df90c` | integral em `HEAD` |
|---|---|---|
| `Filtro.tsx` | 36 | **52** |
| `painel/` | 29 | **35** |
| `rotas.ts` | 8 | **23** |
| `formatar-percentual.ts` | 3 | **18** |

**Sob o escopo integral o lado CALA SOBE, não fecha** — e sobe porque os documentos que
*descrevem* a renomeação (incluindo este relatório e o do `/qa`) citam os nomes antigos. É
`[G-A1]` no seu estado puro. **É por isso que o critério é `classe + conjunto enumerado + rev`
(`SPEC-002` §4.1) e nunca um `grep` cru:** o escopo integral **não tem** lado CALA que feche, hoje
ou nunca.

A bancada do `/qa` verifica exatamente esta consistência, e passa nos 4 tokens:
`"escopo enumerado CALA em 0 e o integral NAO — publicar MORDE integral com CALA enumerado seria
hibrido"`.

---

## C2.3 — `CA-F1-6` é **14**, e a causa que eu publiquei era falsa

`/qa` e `/review` chegaram a **14** independentemente. Reproduzi com o comando **canônico da
`SPEC-002` §5** — o que eu tinha omitido é o `grep -vxE` final.

```
$ git ls-tree -r --name-only <rev> | grep -E '^(backend/src|backend/tests|frontend/src)/' \
    | awk -F/ '{for(i=1;i<NF;i++) print $i}' | sort -u \
    | grep -vxE 'sentimento|charts|convergencia|backtest|web|docs'
```

| forma | `c7df90c` | `HEAD` |
|---|---|---|
| **canônica** (com o filtro de componentes) | **14** — 13 inglês + `painel` | **14** — 14 inglês, **0 português** |
| **sem filtro** (o conjunto de erosão, inclui a exceção nomeada) | **15** | **15** |

```
$ diff <(canônica c7df90c) <(canônica HEAD)
10c10
< painel
---
> panel
```

**O `13` que eu publiquei supunha que `painel` SAI do conjunto. Ele não sai — vira `panel` e
FICA.** O tamanho do conjunto não muda; muda o idioma de **um** elemento.

**E a causa que eu dei para a divergência era falsa.** Eu escrevi *"quais raízes entram
(`backend`, `frontend`, `src`)"*. O `diff` dos dois conjuntos no mesmo rev tem **um único
elemento**:

```
$ diff <(canônica HEAD) <(sem-filtro HEAD)
10a11
> sentimento
```

**A diferença 14 × 15 é `sentimento`, e só** — o componente que o `grep -vxE` remove e que eu
omiti. Nada a ver com raízes.

**Veredito do critério, que é o que `CA-F1-6` pesa:** conjunto de segmentos em português =
**`{sentimento}`, tamanho 1** (era `{sentimento, painel}`, tamanho 2). `sentimento` casa **por
igualdade de string** com `harness policy --key components` — é a exceção nomeada, não erosão.
Na forma canônica, que filtra os componentes, o português vai de **1 (`painel`) para 0**.

---

## C2.4 — ⇑ PARA O `/architect`: `SPEC-002` e dois planos publicam `13`, e o comando **deles** devolve `14`

**Não corrijo — o artefato é de outro dono. O parágrafo para encaminhar:**

> **`CA-F1-6` publica um número que o próprio comando dela não devolve, em três lugares.**
> `SPEC-002` §5 diz *"Depois de `03`: **13**, zero português"*; `01_convencao_escrita.md:37`
> repete o **13**; `03_retroativo_frontend.md:47` (`CA-F3-1`) diz *"`CA-F1-6` devolve **13**
> segmentos, zero português"*. **O comando canônico da própria `SPEC-002` §5 devolve `14` depois
> da fase `03`** `[MEDIDO 2026-08-29 em `90c65e3`, comando literal da §5, n=2 revs]`.
>
> **A premissa do `13` é que `painel` sai do conjunto.** Ele não sai: o mapa normativo de §3.2 o
> renomeia para `panel`, e `panel` é um segmento de caminho como qualquer outro. O tamanho do
> conjunto é invariante sob renomeação — **só o idioma de um elemento muda**. A própria §5 já
> registra a leitura certa para o lado de antes (*"14 segmentos … 13 inglês e 1 português"*); o
> erro está só na projeção do **depois**, que subtraiu um elemento em vez de o traduzir.
>
> **O conserto é de uma palavra em três lugares:** *"depois de `03`: **14**, zero português"*.
> **Nenhuma decisão da SPEC muda** — o critério mede idioma de segmento, e esse veredito
> (`{sentimento}`, tamanho 1; zero português após o filtro de componentes) está satisfeito. O que
> muda é o número publicado ao lado do comando.

---

## C2.5 — Dívida herdada: `ADR-003:234` aponta para um arquivo que este commit matou

**Não é minha para consertar, e não a toquei — `ADR-003` é citação HISTÓRICA (`ADR-015/D3`), e
reescrevê-la seria a violação que `RN-2` existe para evitar.** Registro para o fio não se perder:

> `ADR-003:234` declara que **`T-05.1` / `CST-35`** mede o lado **`cala`** do contrato executável
> contra código real — e nomeia **`frontend/src/app/rotas.ts`** como esse alvo. **Este commit
> renomeia esse caminho para `frontend/src/app/routes.ts`.** Quando a `T-05.1` for executada, o
> alvo que a `ADR-003` nomeia **não existirá**, e — pelo defeito de `SPEC-002` §0.4 —
> `code-paths classify` devolverá `producao`/`rc=0` para ele **assim mesmo**. É a mesma armadilha
> que `CA-F3-6` fecha com o passo (2), agora numa task futura de **outra** feature.

Já registrada pelo coordenador na **PR #32**. O dono do encaminhamento é quem detém `T-05.1`.

---

## C2.6 — Ressalva do `/review` a um `[INFO]` meu, e ele tem razão

O `/review` observou que a âncora de `backend/README.md:262,286` sobrevive como **prosa**, não
como título, e que **`grep -cF` é cego ao tipo do token** (`ADR-015/D1`). **Medi, e o ponto é
ainda mais forte do que a ressalva:**

```
$ grep -cF 'Idioma de docstring é convenção, não portão' README.md
0
$ grep -nE '^#+ .*Idioma de docstring' README.md
83:## Idioma de docstring é convenção, **não portão**
```

**A seção EXISTE (`README.md:83`); a string literal citada devolve `0`**, porque o título carrega
`**` no meio. Um verificador de igualdade de string teria declarado esta âncora **morta** — e ela
está viva. **Minha decisão de não tocar continua certa**, mas o `[INFO]` que a acompanhava
apoiava-se num `grep` que não sustenta o que eu disse que sustentava. Registro a ressalva em vez
de a diluir: **contagem de string não decide tipo de âncora.**

---

## C2.7 — O que o `/qa` construiu, e ele respondeu à pergunta que a task não respondia

**`docs/context/codigo-em-ingles/gates/T-03.1-qa-bench.sh`** (257 linhas). A pergunta que ficava
sem resposta: **`frontend/` não tem suíte** — `package.json` declara **um** script, `"lint":
"eslint src"`. ESLint lê **regra de estilo, não comportamento**: ele aprovaria com nota cheia um
`format-percentage.ts` cujo `toFixed` passou a arredondar para o outro lado. Depois de um rename
que troca **9 identificadores em 4 arquivos**, *"o lint passou"* **não é evidência** de que os
arquivos ainda fazem o que faziam.

A bancada **compila e EXECUTA as duas versões** (extraídas por `git show` para fora do repo) e
compara. Rodada por mim neste rev:

```
$ bash docs/context/codigo-em-ingles/gates/T-03.1-qa-bench.sh
=== VEREDITO DA BANCADA ===
rc=0 — todas as checagens passaram
```

- **70 asserções de equivalência** comportamental entre o antes e o depois;
- **mutação `toFixed(digits)` → `toFixed(digits+1)` MATA o mutante** — verde que não prova nada
  até uma mutação reprovar, e esta reprova;
- **a recusa `rc=3` é exercitada** e distinguida de `rc=1` (`rc=3` é *"não mediu"*, não *"passou"*);
- **nada é escrito em `frontend/src`** (`CA-F3-1` continua contando 4 arquivos) e **nenhum
  `*.test.*` nasce**, o que mantém o `[AVISO]` `browser-test-file-present` aberto, como o `NÃO
  FAZ` da task manda.

A seção **F** dela verifica precisamente o defeito de C2.1 e é o instrumento que o pegaria de
novo: *"escopo enumerado CALA em 0 e o integral NÃO — publicar MORDE integral com CALA enumerado
seria híbrido"*.

---

## C2.8 — Linha de base, reconferida no ciclo 2

```
$ make lint   # rc=0        $ make test   # rc=0
107 passed · TOTAL 370 statements · 54 branch · 100% · domain 124/124 · use_cases 52/52 · infra 194/194
$ harness rules --mode sweep    # rc=0, 1 [AVISO], 0 [BLOQUEIO]
```

**Inalterada.** Nenhum arquivo de `frontend/src` ou `backend/src` mudou neste ciclo.
