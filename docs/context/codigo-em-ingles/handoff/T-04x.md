# Handoff — `codigo-em-ingles` fase 04 · `T-04.1` (`CST-97`) + `T-04.2` (`CST-98`)

**Base:** `master@aac3442` · **Worktree:** `/tmp/claude-1002/wt/ci-T-04x` ·
**Branch:** `tasks/ci-T-04x-superficies-de-contrato`

**Esta fase entrega TEXTO. ZERO linha de código.** `CA-F4-4` é o lado que prova:
`git diff --name-only master... -- backend frontend` → **VAZIO**.

## Estado medido, e é por isso que você foi despachado

`git log --grep='T-04.1\|T-04.2'` → **zero commits**. A fase nunca foi construída, embora as fases
01–03 estejam entregues e mergeadas `[MEDIDO 2026-08-29 em aac3442]`.

| critério | comando | medido | veredito |
|---|---|---|---|
| `CA-F4-1` | linha 10 tem `[INFERRED:` e não tem `NÃO DECIDIDO` | `1` / `0` | ✅ já feito |
| `CA-F4-2` | `grep -cF 'T-07.13' CLAUDE.md` | **`0`** | ⛔ **falta** |
| `CA-F4-3` | linha 12 tem o marcador **e** `owner` | `1` / `1` | ✅ já feito |
| `CA-F4-6` | registro da resposta do owner em `docs/context/codigo-em-ingles/` | **não existe** | ⛔ **falta** |
| `CA-F1-1` | `grep -c '^| [0-9]* | \*\*' CLAUDE.md` | `12` | ✅ não pode mudar |

**Então o trabalho real é `CA-F4-2` e `CA-F4-6`.** Não reescreva o que já passa.

## ⛔ LEIA ISTO ANTES DE MEDIR `CA-F4-5` — o critério é CEGO

Documento completo: `docs/context/codigo-em-ingles/gates/CA-F4-5-instrumento-cego.md`.

`CA-F4-5` publica **9 eventos** e manda medir com
`grep -rnE 'logger\.(info|warning|debug|error)\("' backend/src | wc -l`, **que devolve 7**.

**A afirmação `9 eventos, 4 PT + 5 EN` está CERTA** — por AST ela reproduz exatamente. **O comando é
que erra, nos dois sentidos:** perde 3 eventos cuja chamada está quebrada em duas linhas
(`etl_drenagem_concluida`, `ingest_health_query_read`, `ingestion_verified`) e conta 1 mensagem
formatada como se fosse evento (`probe_bucket_coupling.py:75`). `9 − 3 + 1 = 7`.

**O que fazer com isso, e não é ambíguo:**

1. **NÃO reescreva `CA-F4-5`** nem nenhum outro critério. Critério de aceite é ato do `/tech-lead`.
2. **Meça por AST** e publique os 9 com o call site. O `grep` publicado não certifica nada.
3. **Não trate `7` como reprovação da sua entrega** — ele é defeito do instrumento, e sua task não
   renomeia evento nenhum. O que você tem de provar é que os **4 em português continuam intactos**,
   por `grep -F` de cada nome, que é a metade do critério que funciona.
4. **Registre a divergência** no seu relatório de gate, citando o documento acima.

## `CA-F4-2` — o que falta escrever, e por que não é formalidade

O `CLAUDE.md` linha 11 diz que reabrir a coluna de contrato *"é ato daquela ADR, não desta feature"* —
mas **não nomeia o momento de reabrir**. A `T-04.1` manda escrever que a reabertura acontece quando
`T-07.12`/`T-07.13` (fase 07, componente `web`) escreverem o consumidor da projeção canônica, **e que
quem decide é `ADR-008/D3`, não esta feature**.

Sem isso a exceção fica **sem gatilho** — que é exatamente o defeito que a task existe para consertar.
`CA-F4-2` mede por três `grep -F`: `janela_de_perda` (hoje 2 ✅), `ADR-008/D3` (hoje 1 ✅), `T-07.13`
(hoje **0** ⛔).

**⚠️ FRONTEIRA, e ela protege um critério já escrito** (achado do `/tech-lead`, `tasks_review.md`
§6.2): a coluna de contrato entra como **PROSA ADJACENTE** à tabela de fronteira, **NÃO como uma 13ª
linha dela**. `CA-F1-1` exige **12** linhas; uma 13ª faria `CA-F1-1` reprovar sobre um `CLAUDE.md`
correto. **Re-meça `CA-F1-1` ao fim e confirme que continua 12.**

## `CA-F4-6` — a resposta do owner precisa de casa própria

O owner respondeu em 2026-08-29, escolhendo entre alternativas apresentadas: **não existe consumidor
externo** dos 4 eventos de log em português nem das 4 chaves de `extra={}`.

A resposta está no **ledger** (`harness pipeline show codigo-em-ingles`, motivo do `approve build` de
`15:07:17Z`) e em `docs/INDEX.md:86` — **mas não em `docs/context/codigo-em-ingles/`, onde a task a
prometeu.** A própria `T-04.2` antecipou o risco: *"é o deliverable mais fácil de ser engolido por uma
task cujo trabalho principal é editar o `CLAUDE.md`"*. Foi engolido por um evento de ledger.

**Escreva o documento** em `docs/context/codigo-em-ingles/`, com: a pergunta literal, a resposta, **a
data**, o que **foi** medido (`git ls-files | grep -icE 'dashboard|alert|grafana|prometheus|loki|logql'`
→ **0**) e o que isso **NÃO** prova (ausência de consumidor — `ingest_health_cli.py:69` antecipa
hospedeiro externo, e o que roda na VPS do owner está fora do alcance de qualquer agente).

**O rótulo importa:** é `[DECISÃO-OWNER: 2026-08-29, escolha entre alternativas apresentadas]`, **não**
`[PREMISSA-OWNER]` — o owner escolheu de um menu, não ditou a frase. O `CLAUDE.md` é explícito sobre a
diferença e já produziu defeito aqui.

**Consequência a registrar:** com o *não*, renomear os 4 é **barato** e o caminho caro (emissão dupla,
migração coordenada) **não se aplica** — mas a migração continua sendo **fase própria e futura**, não
esta.

## O que NÃO fazer — omitir qualquer uma reprova a fase

- **NENHUM dos 4 eventos em português é renomeado por esta SPEC.** `CA-F4-5`, metade que funciona.
- **NENHUMA `[[rules.own]]` de idioma.** `ADR-011/D1.10`: declarar uma **reprova a fase**.
- **Zero linha de `backend/` ou `frontend/`.** `CA-F4-4`.
- **`docs/INDEX.md` CRESCE, não muda** — `CA-F4-7`: zero linhas removidas. Verifique **por conjunto**,
  não por `git diff | grep '^-'`: linha movida conta como removida num diff e não foi removida.
- Não mexa em `docs/context/plataforma-dados/` — há dois builders rodando em paralelo agora.

## Protocolo de custo — vinculante, R6/R7

`docs/protocolo-de-despacho.md` mudou hoje (PR #37). **Leia R6 e R7 antes de começar.**

- **`make verify`** roda os seis portões numa chamada, ~10 linhas impressas, saída bruta em arquivo.
- **Se projetar passar de ~150 turnos, PARE**, escreva o estado neste arquivo e devolva.
- Agrupe medições independentes numa chamada só; não releia arquivo já lido.

**Nota de ambiente:** este worktree não tem `.venv` nem `frontend/node_modules`, e o `pre-push` recusa
com `rc=3` (*"não mediu"*) sem eles. Linke-os do repositório principal
(`/home/stharley/Documentos/projects/cripto-strategy/`) para os portões **medirem de verdade** — e
**apague os symlinks antes de qualquer `git add -A`**: o `.gitignore` usa `.venv/` e `node_modules/`
com barra final, e symlink não casa padrão de diretório.

## Entrega

Commit no branch `tasks/ci-T-04x-superficies-de-contrato`, **uma PR** contra `master` cobrindo as duas
tasks. Relatório completo em `docs/context/codigo-em-ingles/gates/T-04x-build.md`.

**Devolva no máximo 15 linhas:** veredito, os números com o comando que os produziu, e o caminho do
relatório. **NUNCA cole o corpo.**
